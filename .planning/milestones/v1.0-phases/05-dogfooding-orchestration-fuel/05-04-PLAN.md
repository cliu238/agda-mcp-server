---
phase: 05-dogfooding-orchestration-fuel
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - .agents/skills/agda-dogfooding/SKILL.md
  - scripts/dogfood/install-dogfood-skill.mjs
  - test/unit/tools/dogfood-install-skill.test.ts
autonomous: true
requirements: [PROC-01]

must_haves:
  truths:
    - "[D-07, Pitfall 1] .agents/skills/agda-dogfooding/SKILL.md is NOT gitignored (git check-ignore -v exits non-zero for this path) — the canonical Skill content ships with the repo, unlike a naive .claude/skills/ or .codex/skills/ placement which this repo's own .gitignore would silently swallow"
    - "[D-07] Running install-dogfood-skill.mjs is idempotent: a fresh run creates .claude/skills/agda-dogfooding as a symlink to the canonical .agents/ content; a second run leaves the same correct end state without throwing; a pre-existing REAL (non-symlink) directory at that path is never overwritten"
    - "[D-07, D-03] The Skill's content mechanically documents PROC-01's hard gate (expectedSignature, refuse-to-start), explicitly wires in when to call agda_capture_session (the antidote to the CHG zero-captures-in-the-wild risk), and describes the legitimate scaffold-hole workflow (intentional {!!} + --allow-unsolved-metas is in-progress work, never a defect)"
    - "[Pitfall 2] The Skill's launch line references a LOCAL git checkout of this repo (scripts/ is not published to npm — package.json's files omits it), never a bare npx agda-mcp-server invocation for the proxy itself"
  artifacts:
    - path: ".agents/skills/agda-dogfooding/SKILL.md"
      provides: "the PROC-01 dogfooding runbook + driver-prompt, packaged as a cross-tool Agent Skill"
    - path: "scripts/dogfood/install-dogfood-skill.mjs"
      provides: "installDogfoodSkill() — the idempotent .claude/skills/ symlink installer"
      exports: ["installDogfoodSkill"]
  key_links:
    - from: "scripts/dogfood/install-dogfood-skill.mjs"
      to: ".agents/skills/agda-dogfooding"
      via: "symlinkSync(canonical, claudeLink, \"dir\")"
      pattern: "symlinkSync"
---

<objective>
## Phase Goal

**As a** maintainer dogfooding this server (or an AI coding agent such as Codex or Claude Code, driven interactively by the maintainer), **I want to** point my MCP session at pinned real Agda corpora through a recording proxy that captures defects and auto-persists them, then wrap up a finished run through the oracle triad and an anti-phantom N-times gate, **so that** harvesting real defects becomes a repeatable, on-demand loop instead of a one-off manual campaign that silently produces zero structured reports (the exact gap Codex-Homotopy-Group's multi-week campaign exposed).

*(Same phase-level user story as 05-01/05-02/05-03 — see 05-01's objective for the full MVP_MODE adaptation note. This plan is a THIRD slice, running in parallel with 05-03: after it lands, an agent (Codex or Claude Code) can actually DISCOVER the dogfooding runbook as a native Agent Skill — the one remaining piece PROC-01 names explicitly ("a written dogfooding runbook + driver-prompt snippet") that turns "point Codex at stdlib and harvest defects" into something an agent can be told to do, rather than something only the maintainer remembers. This plan depends only on 05-02 (the proxy whose exact CLI shape it documents); it does not depend on 05-03, since the wrap-up CLI's shape is already fixed by 05-03's own plan text, which this plan's Skill content follows verbatim rather than waiting on 05-03's execution.)*

Deliver the PROC-01 runbook as a cross-tool Agent Skill: tracked at `.agents/skills/agda-dogfooding/SKILL.md` (Codex-native, NOT gitignored — closing Pitfall 1's `.claude/`/`.codex/` gitignore trap), with a tiny idempotent install script that symlinks it into `.claude/skills/` for Claude Code's own discovery.

Purpose: this is the "driver-prompt snippet" PROC-01 names — the antidote to the STATE.md-flagged open risk that a multi-week real campaign produced zero structured captures despite good intentions; it must actively script when to call `agda_capture_session`, not just describe the server's tools in the abstract.
Output: `.agents/skills/agda-dogfooding/SKILL.md`, `scripts/dogfood/install-dogfood-skill.mjs`, and passing tests proving both the gitignore-safety property and the installer's idempotency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dogfooding-orchestration-fuel/05-CONTEXT.md
@.planning/phases/05-dogfooding-orchestration-fuel/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .agents/skills/agda-dogfooding/SKILL.md — the runbook + driver-prompt</name>
  <files>.agents/skills/agda-dogfooding/SKILL.md, test/unit/tools/dogfood-install-skill.test.ts</files>
  <read_first>
    - docs/assistant-workflows.md (full file, 206 lines) — this repo's OWN established voice/format for "how to tell an agent to use these tools": numbered `## N. <topic>` sections, a fenced tool-call example, a "What you get" bullet list, then a "Typical pattern" numbered flow. Use this file's voice/structure for the SKILL.md body (concrete tool-call examples, not abstract prose) — it is the best in-repo precedent, even though it targets a different discovery mechanism.
    - .planning/phases/05-dogfooding-orchestration-fuel/05-RESEARCH.md (Code Examples § "SKILL.md skeleton") — the audited frontmatter (`name: agda-dogfooding`, a `description` naming when to use it) and section skeleton (hard gate, when-to-capture, scaffold-hole workflow, "do not trust ok blindly yet") — use this as the STRUCTURAL starting point, expanded with this task's own concrete CLI lines below.
    - scripts/dogfood/dogfood-run.mjs and scripts/dogfood/dogfood-wrapup.mjs (Plans 05-02/05-03's own finished output, by the time this task runs — Wave 3 executes after Wave 2) — confirm the EXACT `--manifest`/`--corpus-root`/`--run-id` and `<run-id> [--rerun-n N] [--queue-path P]` flag names before writing the Skill's own example invocation lines, so the documented commands match the real, implemented CLI rather than a guess.
    - .planning/research/FUEL-CORPORA.md — the CHG artifact table (`.codex/skills/agda-unimath-skills/SKILL.md`'s "do not trust MCP ok-complete" warning, and the required-flags/sanctioned-axioms facts) — the template this Skill's OWN "do not trust ok" section tracks, framed as a retraction GOAL rather than a permanent warning.
    - package.json (`files` field) — confirms `scripts/` is NOT published to npm; the Skill's launch line must reference a local git checkout path, never `npx agda-mcp-server` for the PROXY specifically (Pitfall 2).
  </read_first>
  <action>
    Create `.agents/skills/agda-dogfooding/SKILL.md` with YAML frontmatter `name: agda-dogfooding` and a `description` stating: use this Skill when proving theorems against a real Agda corpus (agda-stdlib, agda-unimath, or a project built on them, per `scripts/data/fuel-corpora.json`) through the `agda-mcp-server` MCP tools, to reliably capture defects instead of silently working around them. Keep the frontmatter to this tool-agnostic `name`/`description` pair only (per Pitfall 3 — both Codex and Claude Code render the same file format even though their discovery DIRECTORY differs).
    Write the body in `docs/assistant-workflows.md`'s own numbered-section voice, covering, in order: (1) "Before you start (the hard gate)" — state that this session's MCP server was launched through `scripts/dogfood/dogfood-run.mjs`, which REFUSES to start unless a task manifest listing every target's expected top-level signature was supplied up front (D-03); if the agent does not know its target's expected signature, it must STOP and ask the maintainer rather than guess one after the fact. (2) "Launching a dogfood session" — give the EXACT, concrete command lines (verified against Plans 05-02/05-03's real flag names per this task's `<read_first>`): a `codex mcp add agda-dogfood --env AGDA_MCP_CAPTURE=1 -- node /path/to/agda-mcp-server/scripts/dogfood/dogfood-run.mjs --manifest /path/to/task-manifest.json --corpus-root /path/to/target-corpus` style line and the analogous `claude mcp add --transport stdio agda-dogfood --env AGDA_MCP_CAPTURE=1 -- node /path/to/agda-mcp-server/scripts/dogfood/dogfood-run.mjs --manifest ... --corpus-root ...` line, EXPLICITLY noting: (a) `/path/to/agda-mcp-server` must be a LOCAL git checkout with `npm run build` already run — `scripts/` is not published to npm, so `npx agda-mcp-server@<ver>` (the launch line for the BARE server, still valid on its own) cannot be substituted here for the dogfooding PROXY; (b) the proxy itself sets `AGDA_MCP_CAPTURE=1` on its child unconditionally, so the explicit `--env` above is for illustration/documentation clarity, not strictly required by the proxy's own behavior. (3) "When to capture" — call `agda_capture_session` (not `agda_bug_report_bundle`) whenever: a tool result looks successful but something feels wrong (a "green" you don't fully trust — capturing a SUSPICIOUS green is explicitly in scope, not just outright failures); you are stuck after 2-3 attempts at the same goal; `agda_load`/`agda_typecheck` disagrees with what you expected given a recent edit to a DEPENDENCY (not just the file itself) — the #64/#61 transitive-staleness shape this whole server exists to catch. ALWAYS pass `expectedSignature` (from the task manifest) when calling `agda_capture_session` for a proving task — do not skip this because it feels redundant; it is optional-but-strongly-nagged at capture time specifically so crash/failure captures (which inherently have no expected signature) are never blocked, but a proving-task capture without it makes ORCL-03 vacuous. (4) "What is NOT a defect: the scaffold-hole workflow" — placing a deliberate `{!!}` plus a file-level `--allow-unsolved-metas` while building out a proof's shape is normal, in-progress work, NOT a defect; only capture a hole as suspicious if the proof is believed COMPLETE and the server still reports one, or a hole appears somewhere not deliberately placed. (5) "Do not trust 'ok' blindly (yet)" — frame this as a RETRACTION GOAL: Codex-Homotopy-Group's own `.codex/skills/agda-unimath-skills/SKILL.md` currently carries a "do not trust MCP ok-complete" warning, born from a MEASURED v0.6.7 false-green; this Loop-② scaffold's measurable win is retracting that warning once enough real dogfood runs accumulate evidence the server's verdicts are trustworthy again — until then, treat a surprising "ok" on a freshly-edited dependency as capture-worthy (per section 3), not as ground truth. (6) "The pinned fuel corpora" — list the 4 D-02 corpora by `key` (`agda-stdlib`, `agda-unimath`, `codex-homotopy-group`, `autoformalizing-hopf`, per `scripts/data/fuel-corpora.json`), noting `codex-homotopy-group` is the FIRST OFFICIAL dogfood-run target (it already has measured false-greens against this exact server and ready-made policy facts). (7) "After the session: wrap-up" — once the proxy session ends (it prints the exact next command as its own exit message), run `npx tsx scripts/dogfood/dogfood-wrapup.mjs <run-id>` from the local checkout to judge every staged capture through the oracle triad, N-rerun-classify any load-family candidate, and auto-file true defects into the fix queue as `new` — flaky classifications are tagged in a gitignored side-channel file, never silently dropped and never filed as a confirmed defect.
    Create `test/unit/tools/dogfood-install-skill.test.ts` with an initial test (more added in Task 2): `test("SKILL.md is not gitignored", ...)` — `expect(() => execFileSync("git", ["check-ignore", "-v", ".agents/skills/agda-dogfooding/SKILL.md"], { cwd: SERVER_REPO_ROOT }))` `.toThrow()` (imported `execFileSync` from `"node:child_process"`, `SERVER_REPO_ROOT` from `"../../../src/repo-root.js"` — `git check-ignore` exits 1 and `execFileSync` throws on a non-zero exit code EXACTLY when the path is NOT ignored, which is the desired/passing state). Add a second test asserting the file's content, read via `readFileSync`, matches all of: contains `"expectedSignature"`; contains (case-insensitively) `"hard gate"` or `"refuse"`; contains `"agda_capture_session"`; contains `"scaffold-hole"` or `"--allow-unsolved-metas"`; contains `"dogfood-wrapup"`; contains `"npm run build"` or `"local"` or `"checkout"` (proving the local-checkout caveat from Pitfall 2 is documented, not just the bare `npx` line).
  </action>
  <acceptance_criteria>
    - Behavior: the gitignore-safety test and the content-completeness test both pass.
    - Test command: `npx vitest run test/unit/tools/dogfood-install-skill.test.ts`
    - Source assertion: `grep -c "^name: agda-dogfooding" .agents/skills/agda-dogfooding/SKILL.md` is at least 1; `grep -c "expectedSignature" .agents/skills/agda-dogfooding/SKILL.md` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/tools/dogfood-install-skill.test.ts</automated>
  </verify>
  <done>SKILL.md exists at .agents/skills/agda-dogfooding/, is proven NOT gitignored, and its content is proven to cover the hard gate, the capture verb, the scaffold-hole workflow, the trust-retraction framing, and the local-checkout launch caveat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: install-dogfood-skill.mjs — idempotent .claude/skills/ symlink</name>
  <files>scripts/dogfood/install-dogfood-skill.mjs, test/unit/tools/dogfood-install-skill.test.ts</files>
  <read_first>
    - scripts/promote-capture.mjs (lines 29-42) — the "check first, degrade gracefully, never throw on the common/expected case" shape to mirror for the pre-existing-symlink check (adapted from an index-read to an `lstatSync`-based existence/type check).
    - src/repo-root.ts (line 97) — `SERVER_REPO_ROOT`, imported from `"../src/repo-root.js"` (one level up from `scripts/dogfood/` — wait, `scripts/dogfood/` is two levels from repo root same as `scripts/oracle/`, so the import path is `"../../src/repo-root.js"`, matching this plan's other scripts). Used as this function's DEFAULT `serverRepoRoot`, but the function itself must accept an override parameter for testability (unlike `scripts/promote-capture.mjs`, which deliberately hardcodes `process.cwd()` for a DIFFERENT reason — cross-machine capture promotion — this installer must work identically regardless of caller cwd, so its default is `SERVER_REPO_ROOT`, not `process.cwd()`).
    - .planning/phases/05-dogfooding-orchestration-fuel/05-RESEARCH.md (Common Pitfalls § Pitfall 1, Code Examples § "Install-time skill symlink") — the audited sketch this task's shape is based on, EXTENDED here with the parametrized `serverRepoRoot` (for DI/testability) and the "do not blindly overwrite an existing real directory" case the original sketch did not yet handle.
    - scripts/test-with-sentinel.mjs (lines 12-18) — `isMainModule`, imported from `"../test-with-sentinel.mjs"`.
  </read_first>
  <behavior>
    - Test 1: given a FRESH temp directory (with `.agents/skills/agda-dogfooding/` already populated, simulating this repo's own tracked content) as `serverRepoRoot`, `installDogfoodSkill({ serverRepoRoot })` creates `<serverRepoRoot>/.claude/skills/agda-dogfooding` as a symlink whose target (`readlinkSync`) equals `<serverRepoRoot>/.agents/skills/agda-dogfooding`, and returns `{ action: "created", ... }`.
    - Test 2: calling `installDogfoodSkill({ serverRepoRoot })` a SECOND time against the same temp directory does not throw, leaves the exact same correct symlink in place, and returns `{ action: "already-linked", ... }`.
    - Test 3: given a temp directory where `.claude/skills/agda-dogfooding` ALREADY EXISTS as a real (non-symlink) directory (simulating a pre-existing, unrelated local file), `installDogfoodSkill({ serverRepoRoot })` does NOT throw, does NOT delete/overwrite that directory (assert its contents/identity are unchanged), and returns `{ action: "skipped-existing-non-symlink", ... }`.
  </behavior>
  <action>
    Create `scripts/dogfood/install-dogfood-skill.mjs` with the standard MIT-License-plus-purpose header (documenting: D-07's Pitfall-1 fix — the canonical Skill lives at `.agents/skills/` (tracked); this script creates a LOCAL, gitignored symlink at `.claude/skills/` so Claude Code's own project-skill discovery finds the same content).
    Export `function installDogfoodSkill({ serverRepoRoot = SERVER_REPO_ROOT } = {})`: compute `canonical = join(serverRepoRoot, ".agents", "skills", "agda-dogfooding")` and `claudeSkillsDir = join(serverRepoRoot, ".claude", "skills")` and `claudeLink = join(claudeSkillsDir, "agda-dogfooding")`. Call `mkdirSync(claudeSkillsDir, { recursive: true })`. Attempt `lstatSync(claudeLink)` inside a try/catch that yields `null` on `ENOENT` (no existing entry). If `null`: call `symlinkSync(canonical, claudeLink, "dir")` and return `{ action: "created", canonicalPath: canonical, claudeLinkPath: claudeLink }`. If the existing entry IS a symlink (`.isSymbolicLink()`): read its target via `readlinkSync(claudeLink)` — if it already equals `canonical`, return `{ action: "already-linked", canonicalPath: canonical, claudeLinkPath: claudeLink }` without touching anything; otherwise `unlinkSync(claudeLink)` then `symlinkSync(canonical, claudeLink, "dir")` and return `{ action: "relinked", canonicalPath: canonical, claudeLinkPath: claudeLink }` (still idempotent with respect to the DESIRED end state, even if some prior partial/stale run left a dangling or misdirected link). If the existing entry is NOT a symlink (a real pre-existing file or directory): change NOTHING and return `{ action: "skipped-existing-non-symlink", canonicalPath: canonical, claudeLinkPath: claudeLink }` — never call `symlinkSync`/`unlinkSync` in this branch, and print a one-line warning to stderr naming the unexpected existing path when this function is invoked from the CLI (not from a test).
    Export `async function scriptMain()`: calls `installDogfoodSkill()` with defaults and prints the returned `action`/`claudeLinkPath` to stdout; add the `isMainModule` CLI-entry guard footer.
    Import `SERVER_REPO_ROOT` from `"../../src/repo-root.js"`; import `mkdirSync`, `lstatSync`, `symlinkSync`, `unlinkSync`, `readlinkSync` from `"node:fs"`; import `join` from `"node:path"`.
    Extend `test/unit/tools/dogfood-install-skill.test.ts` (created in Task 1) with the 3 behaviors above, each using its own `mkdtempSync`-created fake `serverRepoRoot` (with `.agents/skills/agda-dogfooding/` pre-populated via `mkdirSync`+`writeFileSync` of a dummy file, simulating this repo's own tracked Skill directory) and `afterEach rmSync` cleanup — never touching the REAL repo's own `.claude/skills/` directory from any of these 3 tests.
  </action>
  <acceptance_criteria>
    - Behavior: all 3 behaviors above pass (create, idempotent-no-op, never-overwrite-a-real-directory).
    - Test command: `npx vitest run test/unit/tools/dogfood-install-skill.test.ts`
    - Source assertion: `grep -c "isSymbolicLink" scripts/dogfood/install-dogfood-skill.mjs` is at least 1 (confirms the type-check-before-mutate guard exists); `grep -c "process.cwd()" scripts/dogfood/install-dogfood-skill.mjs` is 0 (confirms the default root is SERVER_REPO_ROOT, never a cwd-dependent fallback).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/tools/dogfood-install-skill.test.ts</automated>
  </verify>
  <done>install-dogfood-skill.mjs exports installDogfoodSkill(); all 5 tests in dogfood-install-skill.test.ts (2 from Task 1 + 3 from this task) pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Filesystem symlink creation under `.claude/skills/` | Local, maintainer-triggered, one-shot setup step — not remote/untrusted input. |
| SKILL.md prose content | Read by an AI agent as instructions — must not itself instruct unsafe actions or leak private material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | `install-dogfood-skill.mjs` overwriting a pre-existing non-symlink `.claude/skills/agda-dogfooding` | mitigate | `lstatSync` type-checks the target before acting; a real pre-existing file/directory is left completely untouched (`action: "skipped-existing-non-symlink"`), proven by an automated test, never blindly clobbered by `symlinkSync`. |
| T-05-04-02 | Information Disclosure | SKILL.md prose accidentally embedding real source/credentials from the two private fuel corpora | accept | SKILL.md references only corpus KEYS/repo NAMES already public in `FUEL-CORPORA.md`/`fuel-corpora.json` (Plan 05-01) — no source excerpts from `Codex-Homotopy-Group` or `autoformalizing-hopf` are pasted into this Skill. |

</threat_model>

<verification>
Run `npx vitest run test/unit/tools/dogfood-install-skill.test.ts` — all 5 tests green. Run `git check-ignore -v .agents/skills/agda-dogfooding/SKILL.md` by hand and confirm it exits 1 (no match printed).
</verification>

<success_criteria>
- `.agents/skills/agda-dogfooding/SKILL.md` exists, is tracked (not gitignored), and documents the hard gate, the capture verb, the scaffold-hole workflow, the CHG trust-retraction goal, and the local-checkout launch caveat.
- `install-dogfood-skill.mjs`'s `installDogfoodSkill()` idempotently symlinks `.claude/skills/agda-dogfooding` to the canonical content, and never overwrites a genuinely pre-existing non-symlink directory at that path.
- All 5 tests pass.
</success_criteria>

<output>
Create `.planning/phases/05-dogfooding-orchestration-fuel/05-04-SUMMARY.md` when done
</output>
