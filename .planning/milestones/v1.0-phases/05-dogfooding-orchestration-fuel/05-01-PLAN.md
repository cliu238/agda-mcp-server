---
phase: 05-dogfooding-orchestration-fuel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/data/fuel-corpora.json
  - scripts/data/oracle-policy/agda-stdlib.json
  - scripts/data/oracle-policy/codex-homotopy-group.json
  - scripts/data/oracle-policy/autoformalizing-hopf.json
  - test/fixtures/fuel-corpora.ts
  - test/unit/fixtures/fuel-corpora.test.ts
  - test/fixtures/task-manifest-schema.ts
  - scripts/dogfood/task-manifest.mjs
  - test/unit/tools/dogfood-task-manifest.test.ts
autonomous: true
requirements: [PROC-01, PROC-02]

must_haves:
  truths:
    - "[D-02] The four pinned fuel corpora (agda-stdlib, agda-unimath, codex-homotopy-group, autoformalizing-hopf) are each pinned to a real, resolved 40-character commit SHA — not a placeholder — and every entry's policyKey resolves via the EXISTING loadOraclePolicy() unchanged to a non-null policy object"
    - "[D-02] codex-homotopy-group is explicitly documented as the first official dogfood-run target (it directly dogfoods this server and has measured false-greens against it)"
    - "[D-03] loadTaskManifest() mechanically refuses (throws) when the manifest path is missing/undefined, when the file does not parse as JSON, or when the parsed array is empty; it accepts a well-formed non-empty array and returns it fully typed — this IS PROC-01's 'up front' hard gate, not runbook prose"
  artifacts:
    - path: "scripts/data/fuel-corpora.json"
      provides: "PROC-02 pinned fuel-pointer manifest — a bare array of exactly 4 corpus entries"
      min_lines: 20
    - path: "scripts/data/oracle-policy/agda-stdlib.json"
      provides: "near-empty ORCL-02 policy for the agda-stdlib fuel corpus (no HoTT axioms to whitelist)"
    - path: "scripts/data/oracle-policy/codex-homotopy-group.json"
      provides: "ORCL-02 policy mirroring agda-unimath's sanctioned axioms + required flags (this corpus proves theorems using agda-unimath)"
    - path: "scripts/data/oracle-policy/autoformalizing-hopf.json"
      provides: "ORCL-02 policy mirroring agda-unimath's sanctioned axioms + required flags (also built on agda-unimath)"
    - path: "test/fixtures/fuel-corpora.ts"
      provides: "typed loader — exports fuelCorpora, fuelCorpusEntrySchema, FuelCorpusEntry"
      exports: ["fuelCorpora", "fuelCorpusEntrySchema", "FuelCorpusEntry"]
    - path: "test/fixtures/task-manifest-schema.ts"
      provides: "the PROC-01 task-manifest zod contract — schema only, no committed data instance"
      exports: ["taskManifestEntrySchema", "taskManifestSchema", "TaskManifestEntry"]
    - path: "scripts/dogfood/task-manifest.mjs"
      provides: "loadTaskManifest() — the PROC-01 mechanical pre-flight hard gate (D-03)"
      exports: ["loadTaskManifest"]
  key_links:
    - from: "test/fixtures/fuel-corpora.ts"
      to: "scripts/data/fuel-corpora.json"
      via: "loadValidatedJsonData cross-directory read (../../scripts/data/fuel-corpora.json)"
      pattern: "fuel-corpora\\.json"
    - from: "test/unit/fixtures/fuel-corpora.test.ts"
      to: "scripts/oracle/orcl-02-soundness-scan.mjs"
      via: "loadOraclePolicy(entry.policyKey) cross-reference check, imported unchanged"
      pattern: "loadOraclePolicy"
    - from: "scripts/dogfood/task-manifest.mjs"
      to: "test/fixtures/task-manifest-schema.ts"
      via: "taskManifestSchema.parse(raw)"
      pattern: "taskManifestSchema"
---

<objective>
## Phase Goal

**As a** maintainer dogfooding this server (or an AI coding agent such as Codex or Claude Code, driven interactively by the maintainer), **I want to** point my MCP session at pinned real Agda corpora through a recording proxy that captures defects and auto-persists them, then wrap up a finished run through the oracle triad and an anti-phantom N-times gate, **so that** harvesting real defects becomes a repeatable, on-demand loop instead of a one-off manual campaign that silently produces zero structured reports (the exact gap Codex-Homotopy-Group's multi-week campaign exposed).

*(Adapted from ROADMAP.md's outcome-technical Phase 5 goal line — "The process is reproducible: point an agent at pinned real corpora, drive the server over MCP stdio (single AgdaSession), record transcripts, and auto-persist captures on demand." — into user-story form per MVP_MODE. No scope invented; this restates the same 4 ROADMAP success criteria. This phase is infrastructure/scripts (the capstone orchestration layer over Phases 1-4's already-complete capture/oracle/queue machinery), not a UI feature: the "user" is the maintainer and the AI agents CONTEXT.md's D-01 already names as the actual driver model, so "vertical slice" here means "one requirement's worth of real, working capability," not a clickable screen. This plan is the FIRST slice: after it lands, the two data/schema contracts every later plan in this phase reads or enforces — the pinned fuel-pointer set (PROC-02) and the task-manifest hard-gate function (PROC-01's mechanical enforcement) — exist, are validated, and are independently useful even before the proxy that consumes them is built.)*

Deliver PROC-02's pinned fuel-pointer set (four real corpora at real, resolved commits, each cross-referencing a per-project ORCL-02 policy file) and PROC-01's mechanical pre-flight hard gate (a task-manifest schema plus the `loadTaskManifest()` function that later refuses to let `dogfood-run.mjs` start without one). Both are pure data/contract work with no interdependency between them, so they land together as this phase's Wave-1 foundation.

Purpose: `scripts/dogfood/dogfood-run.mjs` (Plan 05-02) imports `loadTaskManifest` directly; `scripts/oracle/orcl-02-soundness-scan.mjs`'s existing `loadOraclePolicy()` (Phase 2, unchanged) reads the new policy files by `policyKey`; the runbook Skill (Plan 05-04) documents the four pinned corpora by name. This is the interface-first foundation every later plan in this phase either imports or documents.
Output: `scripts/data/fuel-corpora.json` + 3 new `scripts/data/oracle-policy/*.json` siblings + `test/fixtures/fuel-corpora.ts` (typed loader) + `test/fixtures/task-manifest-schema.ts` (zod contract) + `scripts/dogfood/task-manifest.mjs` (the hard-gate function), each with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dogfooding-orchestration-fuel/05-CONTEXT.md
@.planning/phases/05-dogfooding-orchestration-fuel/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PROC-02 fuel-pointer manifest + 3 new oracle-policy sibling files + typed loader</name>
  <files>scripts/data/fuel-corpora.json, scripts/data/oracle-policy/agda-stdlib.json, scripts/data/oracle-policy/codex-homotopy-group.json, scripts/data/oracle-policy/autoformalizing-hopf.json, test/fixtures/fuel-corpora.ts, test/unit/fixtures/fuel-corpora.test.ts</files>
  <read_first>
    - scripts/data/oracle-policy/agda-unimath.json (full file, 13 lines) — the EXACT existing policy shape to mirror verbatim for the 3 new sibling files: a top-level object with `$comment`, `sanctionedAxioms` (array of strings: "univalence", "function-extensionality", "replacement"), `requiredFlags` (array of strings: "--without-K", "--exact-split", "--no-import-sorts", "--auto-inline", "--no-require-unique-meta-solutions", "--no-postfix-projections"), `forbiddenFlags` (empty array). Do not add fields this schema does not have.
    - scripts/oracle/orcl-02-soundness-scan.mjs (lines 35-76) — `oraclePolicySchema` (the exact zod shape the 3 new JSON files must validate against, UNCHANGED) and `loadOraclePolicy(projectKey)` — never throws, degrades to `null` on a missing/malformed file, and CR-01-hardened via a bare-filename-only regex on `projectKey`. This plan adds sibling DATA files only; this function itself is not touched.
    - test/fixtures/capture-regression-matrix.ts (full file, 82 lines) — the EXACT typed-loader idiom to copy for `test/fixtures/fuel-corpora.ts`: a zod object schema, `z.infer` type export, `loadValidatedJsonData(import.meta.dirname, "<relative-path>", z.array(schema))`, and a header comment stating this file's SSOT role. `fuel-corpora.ts` differs in ONE way: its JSON source lives in `scripts/data/` (not a sibling `test/fixtures/*.json`), so the relative path argument is `"../../scripts/data/fuel-corpora.json"` (two levels up from `test/fixtures/`, then into `scripts/data/`) — this is deliberate: the fuel-pointer manifest's single source of truth lives under `scripts/data/` (alongside the oracle-policy directory it cross-references), not duplicated under `test/fixtures/`.
    - test/helpers/json-data.ts (full file, 13 lines) — `loadValidatedJsonData(moduleDir, relativePath, schema)`, imported verbatim from `"../helpers/json-data.js"`.
    - test/unit/fixtures/capture-regression-matrix.test.ts (full file, 66 lines) — the exact test shape to mirror: "loads and validates, even when empty" plus per-field assertions (here: unique keys, exact key set, 40-hex-char pinnedRef shape, policyKey cross-resolution).
  </read_first>
  <behavior>
    - Test 1: `fuelCorpora` is an array (`Array.isArray(fuelCorpora)` true) and its length is exactly 4.
    - Test 2: `fuelCorpora.map(e => e.key).sort()` deep-equals exactly `["agda-stdlib", "agda-unimath", "autoformalizing-hopf", "codex-homotopy-group"]` — no duplicates, no extra/missing corpus.
    - Test 3: every entry's `pinnedRef` matches `/^[0-9a-f]{40}$/i` (a full commit SHA, never a short SHA or a bare tag/branch name).
    - Test 4: every entry's `policyKey`, passed through `loadOraclePolicy` (imported from `../../scripts/oracle/orcl-02-soundness-scan.mjs`), resolves to a non-null object (never `null` — proving the cross-reference into the Phase-2 policy loader is live, not just a string that happens to match nothing).
    - Test 5: `fuelCorpora.find(e => e.key === "codex-homotopy-group").notes` (or an equivalent field) mentions this corpus is the first official dogfood-run target (D-02) — a simple substring assertion proving the manifest itself documents this, not just this plan's own prose.
  </behavior>
  <action>
    Create `scripts/data/oracle-policy/agda-stdlib.json` as a top-level object matching `oraclePolicySchema` exactly: `$comment` explaining this is PROC-02's policy for the agda-stdlib fuel corpus (Phase 5) — agda-stdlib is a general-purpose standard library, not a HoTT/univalent library, so it has no established sanctioned-axiom regime or project-wide required-flag convention analogous to agda-unimath's `--without-K` baseline; left intentionally near-empty pending real dogfooding evidence against this corpus, so ORCL-02 will honestly report against an empty whitelist rather than a fabricated one. `sanctionedAxioms: []`, `requiredFlags: []`, `forbiddenFlags: []`.
    Create `scripts/data/oracle-policy/codex-homotopy-group.json`: `$comment` explaining this is PROC-02's policy for the Codex-Homotopy-Group fuel corpus (private repo `emilyriehl/Codex-Homotopy-Group`), which proves theorems using agda-unimath and therefore mirrors `agda-unimath.json`'s own sanctioned axioms and required flags verbatim (D-02: this corpus inherits agda-unimath's axiom/flag regime; values sourced from REQUIREMENTS.md's own PROC-02 wording and FUEL-CORPORA.md) — also note this is the FIRST OFFICIAL dogfood-run target per D-02. `sanctionedAxioms: ["univalence", "function-extensionality", "replacement"]`, `requiredFlags: ["--without-K", "--exact-split", "--no-import-sorts", "--auto-inline", "--no-require-unique-meta-solutions", "--no-postfix-projections"]`, `forbiddenFlags: []`.
    Create `scripts/data/oracle-policy/autoformalizing-hopf.json`: `$comment` explaining this is PROC-02's policy for the autoformalizing-hopf fuel corpus (private repo `emilyriehl/autoformalizing-hopf`, the Hopf/π₃(S²) motivating experiment), also built on agda-unimath and mirroring the same sanctioned axioms and required flags for the identical reason. Same `sanctionedAxioms`/`requiredFlags`/`forbiddenFlags` values as `codex-homotopy-group.json`.
    Create `scripts/data/fuel-corpora.json` as a BARE top-level JSON array (deliberately not an object wrapper — matches `capture-regression-matrix.json`'s/`fix-queue.json`'s own bare-array convention for a collection of like entries, rather than `oracle-policy/*.json`'s single-object-with-`$comment` shape, since this file is a LIST of 4 corpora) containing exactly four entries with fields `key`, `repo`, `access`, `pinnedRef`, `policyKey`, `notes`. Entry 1 — `key: "agda-stdlib"`, `repo: "agda/agda-stdlib"`, `access: "public"`, `pinnedRef: "039e1f4abda49f307f7ed1f5e15232a1c8e9c48c"` (the commit tag `v2.1.1` dereferences to; resolved via `git ls-remote --tags https://github.com/agda/agda-stdlib.git` on 2026-07-02), `policyKey: "agda-stdlib"`, `notes: "Pinned to tag v2.1.1's commit, aligning with .github/workflows/ci.yml's wenkokke/setup-agda agda-stdlib-version pin. Resolved via git ls-remote on 2026-07-02 — re-verify if consumed long after that date."`. Entry 2 — `key: "agda-unimath"`, `repo: "UniMath/agda-unimath"`, `access: "public"`, `pinnedRef: "c85d7fb834778f96a66576318cdc4ef3d4b80a26"` (the default-branch HEAD resolved via `git ls-remote https://github.com/UniMath/agda-unimath.git HEAD` on 2026-07-02), `policyKey: "agda-unimath"`, `notes: "Pinned to default-branch HEAD resolved 2026-07-02. Host of the Hopf/pi_3(S^2) work; targets Agda 2.8.0 per its own docs — re-verify at clone time."`. Entry 3 — `key: "codex-homotopy-group"`, `repo: "emilyriehl/Codex-Homotopy-Group"`, `access: "private"`, `pinnedRef: "81a2f26f19845decccd7ad840d3d8ab5840762c3"` (the `main` branch HEAD resolved via `gh api repos/emilyriehl/Codex-Homotopy-Group/commits/main` on 2026-07-02), `policyKey: "codex-homotopy-group"`, `notes: "Pinned to main HEAD resolved 2026-07-02 via gh api. Directly dogfoods this server (v0.6.7 measured false-greens); the FIRST OFFICIAL dogfood-run target per D-02. Re-clone via: gh repo clone emilyriehl/Codex-Homotopy-Group."`. Entry 4 — `key: "autoformalizing-hopf"`, `repo: "emilyriehl/autoformalizing-hopf"`, `access: "private"`, `pinnedRef: "ad347342cfcd0471ed68eee869cc3ce78d77c34f"` (the `main` branch HEAD resolved via `gh api repos/emilyriehl/autoformalizing-hopf/commits/main` on 2026-07-02), `policyKey: "autoformalizing-hopf"`, `notes: "Pinned to main HEAD resolved 2026-07-02 via gh api. The motivating Hopf/pi_3(S^2) paper repo; also built on agda-unimath. Re-clone via: gh repo clone emilyriehl/autoformalizing-hopf."`.
    Create `test/fixtures/fuel-corpora.ts` exporting `fuelCorpusEntrySchema` (a zod object) with fields: `key` (string, min 1), `repo` (string matching `/^[^/\s]+\/[^/\s]+$/` — an "owner/repo" shape), `access` (enum: `"public"`, `"private"`), `pinnedRef` (string matching `/^[0-9a-f]{40}$/i` — a full commit SHA, never a short SHA or tag name, so the manifest can never silently drift on an ambiguous ref), `policyKey` (string, min 1), `notes` (optional string). Export `type FuelCorpusEntry = z.infer<typeof fuelCorpusEntrySchema>` and `export const fuelCorpora: FuelCorpusEntry[] = loadValidatedJsonData(import.meta.dirname, "../../scripts/data/fuel-corpora.json", z.array(fuelCorpusEntrySchema));`, importing `loadValidatedJsonData` from `"../helpers/json-data.js"`. Add a header comment recording: this is PROC-02's SSOT (D-02: exactly the four requirement-named corpora, no more); the raw JSON lives under `scripts/data/` (not duplicated here) because `scripts/oracle/orcl-02-soundness-scan.mjs`'s existing `loadOraclePolicy()` already reads sibling policy files from that same directory tree at runtime via a DIFFERENT loader (`src/json-data.ts`'s `loadJsonData`, which takes a `baseUrl` rather than a `moduleDir` — the two loaders are NOT interchangeable, do not conflate them); this file is the test-side typed-loader half only.
    Create `test/unit/fixtures/fuel-corpora.test.ts` (matching `test/unit/fixtures/capture-regression-matrix.test.ts`'s file shape) covering the 5 behaviors above; import `loadOraclePolicy` from `"../../../scripts/oracle/orcl-02-soundness-scan.mjs"` for the cross-resolution test (a `// @ts-expect-error script module lacks types` comment immediately above that import, matching this repo's established idiom for `.test.ts` files importing untyped `.mjs` modules).
  </action>
  <acceptance_criteria>
    - Behavior: all 5 behaviors above pass; `loadOraclePolicy` (unchanged) resolves every new `policyKey` to a real policy object.
    - Test command: `npx vitest run test/unit/fixtures/fuel-corpora.test.ts`
    - Source assertion: `grep -c '"$comment"' scripts/data/oracle-policy/agda-stdlib.json scripts/data/oracle-policy/codex-homotopy-group.json scripts/data/oracle-policy/autoformalizing-hopf.json` reports at least 1 per file; `grep -c "loadValidatedJsonData" test/fixtures/fuel-corpora.ts` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/fixtures/fuel-corpora.test.ts</automated>
  </verify>
  <done>fuel-corpora.json + 3 new oracle-policy sibling files exist and validate; fuel-corpora.ts exports a working typed loader; all 5 schema/cross-reference tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PROC-01 task-manifest schema + loadTaskManifest() mechanical hard gate</name>
  <files>test/fixtures/task-manifest-schema.ts, scripts/dogfood/task-manifest.mjs, test/unit/tools/dogfood-task-manifest.test.ts</files>
  <read_first>
    - test/fixtures/fix-queue.ts (lines 27-42, the `TRIAGE_CLASSES` + `fixQueueEntrySchema` object-definition shape) — the exact `z.object({...})` + per-field comment idiom to copy for `taskManifestEntrySchema`.
    - scripts/queue/intake.mjs (lines 106-127) — the "read arg, validate, clear stderr message on failure, never `process.exit()` inside a reusable library function" shape. `task-manifest.mjs`'s `loadTaskManifest` differs in ONE way: it is a LIBRARY function called from `dogfood-run.mjs`'s own pre-flight (not a standalone CLI itself), so it THROWS a descriptive `Error` rather than setting `process.exitCode` itself — the caller (`dogfood-run.mjs`, Plan 05-02) is the one place that translates a thrown gate failure into a non-zero exit + stderr message.
    - scripts/oracle/orcl-02-soundness-scan.mjs (lines 63-76) — `loadOraclePolicy`'s graceful-degrade-on-malformed-input shape (useful precedent for "how to fail informatively"), though `loadTaskManifest` deliberately does the OPPOSITE (throws, never degrades) since a missing/empty manifest must hard-stop the whole run per D-03, not silently continue.
    - .planning/phases/05-dogfooding-orchestration-fuel/05-CONTEXT.md (D-03) — the exact rationale for why this must be a MECHANICAL gate (CHG's multi-week campaign produced zero structured captures despite prose instructions) and why it must fire "up front" (before ORCL-03 would otherwise run vacuous).
  </read_first>
  <behavior>
    - Test 1: `loadTaskManifest(undefined)` throws an Error whose message mentions `--manifest` (proves the missing-flag case names the exact CLI flag the caller must supply).
    - Test 2: `loadTaskManifest(pathToNonJsonFile)` throws an Error whose message includes the offending path (a file containing invalid JSON text).
    - Test 3: `loadTaskManifest(pathToEmptyArrayJson)` throws (the zod `.min(1)` non-empty-array requirement — this IS the D-03 hard gate's core mechanical assertion).
    - Test 4: `loadTaskManifest(pathToOneEntryValidJson)` returns an array of length 1 whose single element's `target`/`expectedSignature`/`corpus` fields exactly match the input file's content, with `notes` correctly present-or-absent per the input.
  </behavior>
  <action>
    Create `test/fixtures/task-manifest-schema.ts` exporting `taskManifestEntrySchema` (a zod object) with fields: `target` (string, min 1 — a human-readable description of the proof target), `expectedSignature` (string, min 1 — the D-03 hard-gate payload; reuses the SAME "name : Type" top-level-colon-split convention `scripts/oracle/orcl-03-conformance.mjs`'s `parseExpectedSignature` and `agda_capture_session`'s own `expectedSignature` input already use, so this field feeds directly into the capture verb without reformatting), `corpus` (string, min 1 — cross-references a `scripts/data/fuel-corpora.json` entry's `key` field BY CONVENTION only; this schema does not enforce that cross-reference, since a task manifest may reference a corpus not yet in the pinned set), `notes` (optional string). Export `taskManifestSchema = z.array(taskManifestEntrySchema).min(1)` — the non-empty-array requirement IS the mechanical hard gate. Export `type TaskManifestEntry = z.infer<typeof taskManifestEntrySchema>`. Add a header comment noting: this file is a SCHEMA ONLY — deliberately no committed task-manifest JSON data instance, since a real task manifest is per-run and often references a private/access-gated corpus.
    Create `scripts/dogfood/task-manifest.mjs` exporting `loadTaskManifest(manifestPath)`: if `manifestPath` is falsy/undefined, throw `new Error(...)` with a message instructing the caller to pass `--manifest <path-to-task-manifest.json>` and naming D-03/the Skill as the source of the requirement (e.g. reference `.agents/skills/agda-dogfooding/SKILL.md`, the file Plan 05-04 creates). Otherwise, read the file via `readFileSync(manifestPath, "utf8")` and `JSON.parse` it inside a try/catch that re-throws a new Error naming both the path and the underlying parse error's message on failure. Then call `taskManifestSchema.parse(raw)` (imported from `"../../test/fixtures/task-manifest-schema.js"`) — let the zod error propagate (its own message already names which field/constraint failed, including the `.min(1)` empty-array case) OR wrap it with a short `Task manifest at ${manifestPath} failed validation: ` prefix for a clearer top-level message while preserving the underlying zod detail. On success, return the validated array. This function is synchronous (matches `scripts/oracle/run-oracle.mjs`'s own sync `JSON.parse(readFileSync(...))` artifact-reading pattern) and NEVER calls `process.exit`/sets `process.exitCode` itself — it only throws; Plan 05-02's `dogfood-run.mjs` is the sole place a thrown gate failure becomes a CLI exit.
    Add the standard MIT-License-plus-purpose header comment (matching every existing `scripts/oracle/*.mjs`/`scripts/queue/*.mjs` file) documenting: this implements PROC-01's D-03 mechanical pre-flight hard gate; ships as `scripts/` per the "no new src/ tool surface" convention; must be imported via `npx tsx` (not plain `node`) by any CLI wrapper, since it imports a `.ts` sibling (`test/fixtures/task-manifest-schema.ts`) via a `.js`-suffixed specifier.
    Create `test/unit/tools/dogfood-task-manifest.test.ts` covering the 4 behaviors above, using `mkdtempSync`/`afterEach rmSync` to create the temp JSON fixture files (a non-JSON text file, an empty-array JSON file, a valid one-entry JSON file), mirroring `test/unit/tools/queue-intake.test.ts`'s tmpdir-per-test shape. Import `loadTaskManifest` from `"../../../scripts/dogfood/task-manifest.mjs"` with the `// @ts-expect-error script module lacks types` comment immediately above.
  </action>
  <acceptance_criteria>
    - Behavior: all 4 behaviors above pass; the hard gate throws (never silently degrades) on every invalid input shape.
    - Test command: `npx vitest run test/unit/tools/dogfood-task-manifest.test.ts`
    - Source assertion: `grep -c "process.exit" scripts/dogfood/task-manifest.mjs` is 0 (this library function must never terminate the process itself); `grep -c "taskManifestSchema" scripts/dogfood/task-manifest.mjs` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/tools/dogfood-task-manifest.test.ts</automated>
  </verify>
  <done>task-manifest-schema.ts exports the zod contract; task-manifest.mjs's loadTaskManifest() mechanically refuses every invalid input and accepts a valid one; all 4 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Hand-edited JSON config | `scripts/data/fuel-corpora.json` and the 3 new `oracle-policy/*.json` files are maintainer-edited, git-tracked data — not runtime-untrusted input, but still parsed via zod so a malformed edit fails loudly (a schema mismatch) rather than silently degrading ORCL-01/02's behavior. |
| A future task-manifest JSON (no instance committed by this plan) | Will be hand-edited per-run by the maintainer; `taskManifestSchema` is the validation boundary Plan 05-02's `dogfood-run.mjs` sits behind. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | scripts/data/fuel-corpora.json / oracle-policy/*.json | mitigate | zod schema validation (`fuelCorpusEntrySchema` / the existing unchanged `oraclePolicySchema`) rejects a malformed shape at load time; `pinnedRef`'s 40-hex-char regex specifically rejects a truncated SHA or a bare tag/branch name that could otherwise resolve ambiguously across time. |
| T-05-01-02 | Tampering | a future task-manifest JSON's `corpus`/`target`/`expectedSignature` string fields | accept | These are free-form strings validated only for non-emptiness in this plan — none of them are resolved into a filesystem path anywhere in this plan's code, so there is no path-traversal surface here. The ACTUAL path-resolving consumer, `loadOraclePolicy`, already carries the CR-01 bare-filename regex fix and is reused unchanged by later plans. |
| T-05-01-03 | Repudiation | scripts/dogfood/task-manifest.mjs's thrown errors | accept | This library function never writes to disk or mutates state — a thrown Error is the complete, correct behavior; no audit trail is needed beyond the caller's own stderr output (Plan 05-02). |

</threat_model>

<verification>
Run `npx vitest run test/unit/fixtures/fuel-corpora.test.ts test/unit/tools/dogfood-task-manifest.test.ts` — all green. Run `npx tsc -p tsconfig.json --noEmit` — clean (both new `.ts` files typecheck).
</verification>

<success_criteria>
- `scripts/data/fuel-corpora.json` pins exactly the 4 D-02 corpora to real, resolved 40-char commit SHAs; every `policyKey` resolves via the unchanged `loadOraclePolicy()`.
- `test/fixtures/fuel-corpora.ts` exports a working, validated typed loader.
- `test/fixtures/task-manifest-schema.ts` + `scripts/dogfood/task-manifest.mjs`'s `loadTaskManifest()` together implement PROC-01's D-03 mechanical hard gate: an undefined path, invalid JSON, or empty array all throw; a valid non-empty manifest is accepted and returned typed.
- All 9 new tests (5 fuel-corpora + 4 task-manifest) pass; no existing test regresses.
</success_criteria>

<output>
Create `.planning/phases/05-dogfooding-orchestration-fuel/05-01-SUMMARY.md` when done
</output>
