---
phase: 04-triage-fix-queue
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - scripts/queue/priority.mjs
  - scripts/queue/dashboard.mjs
  - docs/FIX-QUEUE-DASHBOARD.md
  - test/unit/tools/queue-priority.test.ts
  - test/unit/tools/queue-dashboard.test.ts
autonomous: true
requirements: [QUEUE-02]

must_haves:
  truths:
    - "comparePriority orders every queue entry false-green > crash > wrong-result > missing-feature, ties broken by recurrence descending — one clear ordering for the maintainer and agents to work from (QUEUE-02's forced priority ordering)"
    - "Priority is never stored on a queue entry — it is always computed on the fly from defectKind + recurrence by this plan's comparePriority, so it can never independently drift out of sync with the data it derives from"
    - "A derived, human-readable Markdown dashboard (priority-sorted table + per-status counts + the close-rate metric) is regenerated by script whenever the queue changes; it is a view, never hand-edited — JSON remains the only truth (D-03)"
    - "The close-rate metric (closed/total) is recorded and visible from day one, not deferred to a later phase (D-06)"
    - "An advisory WIP-limit warning on the fixing status is surfaced on the dashboard when exceeded — a hard block is never manufactured, since that would recreate exactly the evaporation QUEUE-01 exists to prevent (D-06)"
    - "docs/FIX-QUEUE-DASHBOARD.md, once regenerated against the real 13-entry seed cargo (Plan 04-03), reflects real data, not a placeholder"
  artifacts:
    - path: "scripts/queue/priority.mjs"
      provides: "DEFECT_KIND_WEIGHT / comparePriority / sortByPriority — the ONE place the QUEUE-02 ordering lives"
    - path: "scripts/queue/dashboard.mjs"
      provides: "renderDashboard / regenerateDashboard"
    - path: "docs/FIX-QUEUE-DASHBOARD.md"
      provides: "the real, regenerated dashboard reflecting the seeded 13-entry queue"
  key_links:
    - from: "scripts/queue/dashboard.mjs"
      to: "scripts/queue/priority.mjs"
      via: "import { sortByPriority } from \"./priority.mjs\""
      pattern: "sortByPriority"
    - from: "scripts/queue/dashboard.mjs"
      to: "test/fixtures/fix-queue.ts"
      via: "the CLI's import of the real fixQueue constant"
      pattern: "fixQueue"
---

<objective>
## Phase Goal

**As a** maintainer (and the AI agents dogfooding this server), **I want to** have every captured defect persist, flow, and get prioritized/classified inside a durable in-repo queue, **so that** intake neither evaporates at session end nor outpaces fix throughput into a write-only graveyard.

*(Same phase-level user story as 04-01 — see that plan's objective for the full MVP_MODE adaptation note. This plan is the LAST slice: after it lands, the maintainer (or an agent) can answer "what should I work on next?" from one glance at a regenerated dashboard, instead of reading 13 raw JSON objects by hand.)*

Deliver QUEUE-02's forced prioritization signal — a pure, deterministic `comparePriority` composing already-captured data (`defectKind`, `recurrence`) into one ordering — and the D-03 human-readable dashboard that surfaces it, the per-status counts, the close-rate throughput metric (Pitfall 6's day-one guard), and an advisory WIP-limit warning on the `fixing` status.

Purpose: this is the LAST wave because it wants to regenerate the REAL dashboard against the REAL seeded cargo (Plan 04-03), not a synthetic placeholder — proving the generator works against actual data, not just unit fixtures.
Output: `scripts/queue/priority.mjs`, `scripts/queue/dashboard.mjs`, a real regenerated `docs/FIX-QUEUE-DASHBOARD.md`, and passing tests for both scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-fix-queue/04-CONTEXT.md
@.planning/phases/04-triage-fix-queue/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: priority.mjs — the one true ordering</name>
  <files>scripts/queue/priority.mjs, test/unit/tools/queue-priority.test.ts</files>
  <read_first>
    - scripts/oracle/verdict-schema.mjs (lines ~35-71) — `composeVerdict`'s "one pure composition function, comment naming it as the ONLY place this logic lives" structural pattern — copy the STRUCTURE (a documented, single-source-of-truth pure function), not the verdict-specific content.
    - test/unit/tools/oracle-verdict-schema.test.ts — the pure-fn test shape and the `// @ts-expect-error script module lacks types` import-comment idiom to copy exactly.
    - test/fixtures/fix-queue.ts (Plan 04-01) — `FixQueueEntry.defectKind`/`.recurrence` — the only two fields this module reads.
  </read_first>
  <behavior>
    - Test 1: given one synthetic entry per defectKind (`false-green`, `crash`, `wrong-result`, `missing-feature`) in shuffled input order, `sortByPriority` returns them in exactly that band order.
    - Test 2: within the SAME defectKind, an entry with `recurrence: 5` sorts before one with `recurrence: 2`.
    - Test 3: `sortByPriority` does not mutate its input array (the input array's own element order is unchanged after the call — assert against a copy taken before the call).
    - Test 4: calling `sortByPriority` twice on the same input produces deep-equal results (deterministic, no reliance on unstable sort ordering for equal-priority entries beyond what the comparator itself defines).
  </behavior>
  <action>
    Create `scripts/queue/priority.mjs` exporting: `DEFECT_KIND_WEIGHT` — a frozen plain object `{ "false-green": 0, "crash": 1, "wrong-result": 2, "missing-feature": 3 }` (lower number = higher priority), with a comment naming this as the ONE place QUEUE-02's ordering weight lives — both this module and `scripts/queue/dashboard.mjs` (Task 2) import this SAME table (indirectly, via `comparePriority`/`sortByPriority`) rather than each independently re-deriving an ordering, so the two can never drift apart. `comparePriority(a, b)` — returns `DEFECT_KIND_WEIGHT[a.defectKind] - DEFECT_KIND_WEIGHT[b.defectKind]` when that difference is non-zero; otherwise returns `b.recurrence - a.recurrence` (higher recurrence sorts first within the same band — QUEUE-02's exact tie-break wording, reusing the recurrence count `readDedupIndex`/`upsertQueueEntry` already maintain, no new signal invented). `sortByPriority(entries)` — a thin, non-mutating wrapper: `return [...entries].sort(comparePriority);` (spreads into a new array first, so the caller's own array is never mutated in place).
    Write `test/unit/tools/queue-priority.test.ts` (the `// @ts-expect-error` import-comment idiom above the relative import of `../../../scripts/queue/priority.mjs`) covering the 4 behaviors above using minimal synthetic `{ defectKind, recurrence }`-shaped objects (a full `FixQueueEntry` is not required for this pure function's own tests — only the two fields it reads).
  </action>
  <acceptance_criteria>
    - Behavior: false-green > crash > wrong-result > missing-feature ordering holds; recurrence-descending tie-break holds within a band; the function is pure/non-mutating and deterministic.
    - Test command: `npx vitest run test/unit/tools/queue-priority.test.ts`
    - Source assertion: `grep -c "DEFECT_KIND_WEIGHT" scripts/queue/priority.mjs` is at least 1; `grep -c "\\[\\.\\.\\.entries\\]" scripts/queue/priority.mjs` is at least 1 (confirms the non-mutating spread).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/tools/queue-priority.test.ts</automated>
  </verify>
  <done>All 4 behaviors pass as tests; comparePriority/sortByPriority are exported and pure.</done>
</task>

<task type="auto">
  <name>Task 2: dashboard.mjs — the regenerated human-readable view (+ real regeneration)</name>
  <files>scripts/queue/dashboard.mjs, docs/FIX-QUEUE-DASHBOARD.md, test/unit/tools/queue-dashboard.test.ts</files>
  <read_first>
    - scripts/emit-regression.mjs (CLI arg-parsing + `isMainModule` + stdout/stderr conventions) — the CLI shape to mirror.
    - src/session/safe-source-io.ts's `writeFileAtomic` — the write primitive to reuse for the markdown output.
    - scripts/oracle/run-oracle.mjs (line ~49) — the `import { writeFileAtomic } from "../../src/session/safe-source-io.js";` relative-depth pattern; `scripts/queue/` needs the identical two-levels-up depth.
    - test/unit/tools/emit-regression.test.ts — the tmpdir-per-test + `afterEach` cleanup test shape to mirror (never write into the real `docs/` directory from a test).
    - test/fixtures/fix-queue.ts (Plan 04-01/04-03) — the real, now-13-entry `fixQueue` export this script's CLI path reads.
  </read_first>
  <action>
    Create `scripts/queue/dashboard.mjs` exporting: `renderDashboard(entries)` — a PURE function (no `Date.now()`/live timestamp inside its own return value, so its output is deterministically testable) returning a markdown string with, in order: (1) a one-line title (e.g. `"# Fix Queue Dashboard"`) plus a brief "regenerated by scripts/queue/dashboard.mjs — never hand-edit" notice (D-03); (2) a priority-sorted table built from `sortByPriority(entries)` (imported from `./priority.mjs`) with columns Fingerprint (first 8 characters only, for readability), Status, DefectKind, TriageClass, Title, Recurrence — hand-rolled via plain string-join (no new dependency — confirmed no markdown-table package exists anywhere in this repo); (3) a per-status counts section — one line per distinct `status` value present in `entries` with its count; (4) a close-rate metric line — `closed / total` where `closed` counts entries whose `status` is `locked` or `rejected` (the D-06 throughput metric, present from day one); (5) an advisory WIP-limit line — count entries with `status === "fixing"`; if that count exceeds `3` (the chosen advisory default — Claude's-discretion threshold, documented inline as a simple, easily-adjusted constant, never a hard block per Pitfall 7/D-06), render a `"WARNING: N entries in fixing exceeds the advisory WIP limit of 3"` line; otherwise render a normal `"X entries in fixing (advisory WIP limit: 3)"` info line.
    `regenerateDashboard(entries, dashboardPath)` — calls `renderDashboard(entries)` and writes the result via `writeFileAtomic` (imported from `../../src/session/safe-source-io.js`).
    A CLI (`isMainModule` guard from `../test-with-sentinel.mjs`) importing the REAL `fixQueue` from `../../test/fixtures/fix-queue.js` and `SERVER_REPO_ROOT` from `../../src/repo-root.js`, calling `regenerateDashboard(fixQueue, join(SERVER_REPO_ROOT, "docs/FIX-QUEUE-DASHBOARD.md"))`.
    Run this CLI for real once (`npx tsx scripts/queue/dashboard.mjs`) so `docs/FIX-QUEUE-DASHBOARD.md` is committed reflecting the actual 13-entry seed cargo from Plan 04-03 — confirm by inspection that the per-status counts sum to 13 and the close-rate line shows `1/13`.
    Write `test/unit/tools/queue-dashboard.test.ts` asserting: `renderDashboard([])` (empty queue) returns a valid string containing the header and a `0`-total close-rate line, never throwing; `renderDashboard(entries)` with 2-3 synthetic entries produces table rows in the SAME order `sortByPriority(entries)` itself returns (cross-check against `sortByPriority` directly — imported from `./priority.mjs` — never a hardcoded expectation, so the two can never independently drift); the close-rate line's numerator/denominator match a hand-computed count for a fixture with a known status mix; the WIP-limit line switches from the info wording to the warning wording when a fixture crosses 3 `fixing`-status entries; `regenerateDashboard` writes the file (assert `existsSync` + the written content equals `renderDashboard`'s own return value for the same input) using a per-test tmpdir (`mkdtempSync`/`afterEach rmSync`, mirroring `emit-regression.test.ts`'s shape) — never writing into the real `docs/` directory from this test.
  </action>
  <acceptance_criteria>
    - Behavior: renderDashboard/regenerateDashboard pass all tests; docs/FIX-QUEUE-DASHBOARD.md exists and reflects the real 13-entry cargo.
    - Test command: `npx vitest run test/unit/tools/queue-dashboard.test.ts`
    - Source assertion: `grep -c "sortByPriority" scripts/queue/dashboard.mjs` is at least 1 (confirms it imports the shared comparator rather than re-deriving one); `grep -c "close-rate\|closeRate" scripts/queue/dashboard.mjs` (case-sensitive substring check for either spelling) is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/tools/queue-dashboard.test.ts</automated>
  </verify>
  <done>All dashboard tests pass; docs/FIX-QUEUE-DASHBOARD.md is committed and its per-status counts sum to 13 with a 1/13 close-rate line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Purely local: reads the already-validated `fixQueue` array and writes one markdown file via `writeFileAtomic`. No network, subprocess, or external/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | scripts/queue/dashboard.mjs's file write | mitigate | `writeFileAtomic` (temp-file + rename) — concurrent readers never observe a half-written dashboard. |
| T-04-05-02 | Repudiation | docs/FIX-QUEUE-DASHBOARD.md | accept | The dashboard is an explicitly-labeled, regenerated VIEW (D-03) — it is never treated as an independent source of truth, so a stale or manually-edited copy cannot corrupt the underlying queue data; git history over the JSON file remains authoritative. |

</threat_model>

<verification>
Run `npx vitest run test/unit/tools/queue-priority.test.ts test/unit/tools/queue-dashboard.test.ts` — all green. Confirm `docs/FIX-QUEUE-DASHBOARD.md` exists and its counts sum to 13.
</verification>

<success_criteria>
- comparePriority/sortByPriority implement QUEUE-02's exact forced ordering, proven by tests.
- renderDashboard/regenerateDashboard produce a deterministic, priority-sorted, close-rate-and-WIP-limit-carrying markdown view.
- docs/FIX-QUEUE-DASHBOARD.md is committed and reflects the real seeded queue.
</success_criteria>

<output>
Create `.planning/phases/04-triage-fix-queue/04-05-SUMMARY.md` when done
</output>
