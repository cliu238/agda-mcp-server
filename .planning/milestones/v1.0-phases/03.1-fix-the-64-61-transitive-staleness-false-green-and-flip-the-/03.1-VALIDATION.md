---
phase: 03.1
slug: fix-the-64-61-transitive-staleness-false-green-and-flip-the-
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-02
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest ^4.1.2 |
| **Config file** | vitest.config.ts |
| **Quick run command** | `npx vitest run test/unit --reporter=dot` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~60s pure; strict-phase real-Agda suite ~+51s slower after the fix (measured latency tradeoff), RUN_AGDA_INTEGRATION-gated |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run test/unit --reporter=dot`
- **After every plan wave:** Run `npm test`
- **Before `/gsd:verify-work`:** Full suite must be green, incl. `RUN_AGDA_INTEGRATION=1` on the flagship + guard
- **Max feedback latency:** 180 seconds (strict-phase real-Agda cases are slower post-fix)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.1-01-T1 (load-terminus-tracker module) | 03.1-01 | 1 | FIX-01 | T-03.1-01-02 | Strict-mode predicate satisfied ONLY by sawLoadError — the D-02 false-RED guard, verified in isolation before any transport/Agda involvement | unit | `npx vitest run test/unit/session/load-terminus-tracker.test.ts` | ❌ Wave-0-equivalent — new file, created by this task itself | ⬜ pending |
| 03.1-01-T2 (transport rewire + threading) | 03.1-01 | 1 | FIX-01 | T-03.1-01-01, T-03.1-01-02 | Terminus tracking delegated to the tracker module; 500-line ceiling honored; metas-path behavior byte-for-byte unchanged | unit | `npx vitest run test/unit/session/agda-transport.test.ts test/unit/session/command-completion.test.ts` | ✓ (extends existing files) | ⬜ pending |
| 03.1-01-T3 (runLoadNoMetas call-site flip) | 03.1-01 | 1 | FIX-01 | T-03.1-01-01 | runLoadNoMetas requests the strict guard; no new classification value; command-builder SSOT untouched | unit | `npx vitest run test/unit/agda/session-load-impl.test.ts` | ✓ (existing, unmodified by design) | ⬜ pending |
| 03.1-02-T1 (lock flip + D-05 guard entry) | 03.1-02 | 2 | FIX-01 | T-03.1-02-01 | Flagship flips red->locked and matches ORCL-01's cold expected value; the D-05 guard entry proves the widened window does not false-RED a genuinely clean strict load, under the exact fault-injection env that exposed the original bug | integration (real Agda) | `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts` | ✓ (existing runner + fixture; JSON-only edit) | ⬜ pending |
| 03.1-02-T2 (full no-regression sweep) | 03.1-02 | 2 | FIX-01 | (none new — re-confirms T-03.1-01-01/02 hold end-to-end) | Metas path unaffected; all 72 existing fixtures' strict phase still classify correctly; whole suite green | integration + unit + property (real Agda) | `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/agda/agda-load.test.ts` (primary; `AGDA_FIXTURE_PHASES=strict` fixture-matrix run and `RUN_AGDA_INTEGRATION=1 npm test` are required companion commands per the task's acceptance criteria) | ✓ (existing suites, unmodified) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `src/session/load-terminus-tracker.ts` — new sibling module (created and unit-tested within Plan 03.1-01 Task 1, before Task 2 wires it in — interface-first ordering substitutes for a separate Wave 0)
- [ ] `test/unit/session/load-terminus-tracker.test.ts` — new unit test file (created within Plan 03.1-01 Task 1, same task as the module it tests)
- [ ] `test/fixtures/capture-regression-matrix.json`'s new `guard-no-metas-clean-load-under-fault-injection` entry — created within Plan 03.1-02 Task 1 (Pattern 3: reuses the existing runner/fixture, zero new test infrastructure)

No file referenced by any task's `<verify>` is missing without a corresponding creation step in an earlier task of the same or an earlier plan — every `<automated>` command in the Per-Task Verification Map above resolves to a file that either already exists or is created earlier in the same task.

---

## Manual-Only Verifications

All phase behaviors have automated verification.

(The phase's `<threat_model>` blocks accept two residual risks — a pre-existing operator-misconfiguration exposure on `AGDA_MCP_LOAD_TERMINUS_IDLE_MS`, and the disclosed process-crash-during-load gap noted in Plan 03.1-01's objective — neither requires a manual test step; both are pre-existing/out-of-scope risk acceptances, not new verifiable behaviors.)

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — all 5 tasks across both plans carry an `<automated>` command; Task 1 of Plan 03.1-01 satisfies its own Wave-0-equivalent need in the same task (interface-first ordering).
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — every task has one.
- [x] Wave 0 covers all MISSING references — the new sibling module + its test + the new matrix entry are all created before anything downstream depends on them (see Wave 0 Requirements above).
- [x] No watch-mode flags — every command uses `vitest run`, never bare `vitest` (watch mode).
- [x] Feedback latency < 180s — per-task `<automated>` commands are scoped to single test files (fast, or a single small real-Agda integration file for 03.1-02-T1); the two known-slower checks (the 72-fixture `AGDA_FIXTURE_PHASES=strict` breadth run at ~132s, and the full `RUN_AGDA_INTEGRATION=1 npm test`) are deliberately NOT a task's primary `<automated>` gate — they are required companion commands in 03.1-02-T2's acceptance criteria and the phase-level `<verification>` sections, matching 03.1-RESEARCH.md's own tiered Sampling Rate (fast per-task-commit cadence vs. slower per-wave/phase-gate cadence).
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** pending
