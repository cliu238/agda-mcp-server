---
phase: 03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - test/fixtures/capture-regression-matrix.json
  - test/fixtures/capture-regression-matrix.ts
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "The issue-64-61-transitive-staleness capture-regression matrix entry flips status: \"red\" -> status: \"locked\", and RUN_AGDA_INTEGRATION=1's capture-regression.test.ts asserts matchesExpected(observed, entry.expected) === true for it — D-03's definition of done, going green for real against the Plan 03.1-01 fix"
    - "A new mutation-less guard entry (guard-no-metas-clean-load-under-fault-injection), reusing the SAME FixtureDeps/TransitiveStaleness fixture and the SAME pathological serverEnv as the flagship, proves a legitimately goal-less agda_load_no_metas load still reports ok-complete/success:true under the widened idle window — the D-02 false-RED risk, checked at the real MCP-tool-call boundary (D-05)"
    - "The metas path (agda_load / agda_typecheck, via agda-load.test.ts) is unaffected by the fix — every one of its tests still passes exactly as before Plan 03.1-01 (D-04's scope fence, D-05's no-regression requirement)"
    - "Every hole/error/clean shape across the existing 72-entry fixture matrix still classifies correctly under agda_load_no_metas (an AGDA_FIXTURE_PHASES=strict breadth run) — no regression across shape diversity introduced by the predicate change (D-05)"
    - "The full existing suite (unit + property + integration) stays green under RUN_AGDA_INTEGRATION=1 — the phase's overarching no-regression success criterion (D-04, D-05)"
    - "No other live defect (agda_auto's flag leak, agda_give's ok:true-wrapping-error, agda_search_definitions' hardcoded layout) is touched or locked by this plan — those remain Phase 4 fix-queue items (D-04)"
  artifacts:
    - path: "test/fixtures/capture-regression-matrix.json"
      provides: "The flagship flipped to locked, plus the new D-05 guard entry — both proven against real Agda"
      contains: "\"status\": \"locked\""
    - path: "test/fixtures/capture-regression-matrix.ts"
      provides: "A clarified header-comment scope for hand-authored entries (defect-content provenance, not a blanket ban on ever adding an entry by hand)"
      contains: "no hand-built synthetic bundles"
  key_links:
    - from: "test/fixtures/capture-regression-matrix.json"
      to: "test/integration/mcp/capture-regression.test.ts"
      via: "captureRegressionMatrix iteration — a status !== \"red\" entry asserts matchesExpected === true"
      pattern: "issue-64-61-transitive-staleness"
    - from: "test/fixtures/capture-regression-matrix.json (guard entry)"
      to: "test/fixtures/agda/FixtureDeps/TransitiveStaleness"
      via: "fixtureDir field, no mutation field — a single clean-load call, not a trigger-and-reload replay"
      pattern: "guard-no-metas-clean-load-under-fault-injection"
---

<objective>
Prove the Plan 03.1-01 fix end-to-end against real Agda: flip the flagship `issue-64-61-transitive-staleness` matrix entry from `red` to `locked`, add the D-05 mutation-less guard entry proving a genuinely clean strict load is not false-RED'd by the widened window, then run the full no-regression sweep (metas-path control, 72-fixture strict-path breadth, whole suite).

Purpose: this is the phase's actual closed-loop proof — capture -> lock -> fix -> stay-locked — not just a code change sitting unverified. D-03's "definition of done" is specifically this matrix flip going green; D-05's guard requirement and no-regression requirement are specifically proven here, against a real `agda` binary, not asserted from unit tests alone.
Output: the matrix entry flip, the new guard entry, and a green `RUN_AGDA_INTEGRATION=1` suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-/03.1-CONTEXT.md
@.planning/phases/03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-/03.1-RESEARCH.md
@.planning/phases/03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-/03.1-PATTERNS.md
</context>

<interfaces>
<!-- test/fixtures/capture-regression-matrix.json — CURRENT full file (the one entry to flip; template for the new entry). -->
```json
[
  {
    "id": "issue-64-61-transitive-staleness",
    "issue": [64, 61],
    "status": "red",
    "tool": "agda_load_no_metas",
    "fixtureDir": "FixtureDeps/TransitiveStaleness",
    "entryFile": "Main.agda",
    "mutation": { "targetFile": "Dep.agda", "sourceFile": "Dep.broken.agda" },
    "serverEnv": { "AGDA_MCP_IDLE_COMPLETION_MS": "1", "AGDA_MCP_POST_STATUS_IDLE_MS": "1" },
    "expected": {
      "classification": "type-error", "success": false, "goalCount": 0,
      "invisibleGoalCount": 0, "hasHoles": false, "errorCategories": ["UnequalTerms"]
    }
  }
]
```

<!-- The new guard entry to append (03.1-RESEARCH.md Pattern 3, validated 6/6 empirically). No "mutation" field -- schema-optional, meaning a single clean-load call rather than a trigger-and-reload replay. -->
```json
{
  "id": "guard-no-metas-clean-load-under-fault-injection",
  "issue": [64, 61],
  "status": "locked",
  "tool": "agda_load_no_metas",
  "fixtureDir": "FixtureDeps/TransitiveStaleness",
  "entryFile": "Main.agda",
  "serverEnv": { "AGDA_MCP_IDLE_COMPLETION_MS": "1", "AGDA_MCP_POST_STATUS_IDLE_MS": "1" },
  "expected": {
    "classification": "ok-complete", "success": true, "goalCount": 0,
    "invisibleGoalCount": 0, "hasHoles": false, "errorCategories": []
  }
}
```

<!-- test/integration/mcp/capture-regression.test.ts — the ONE generic runner, unchanged, that both entries above already drive correctly. -->
```typescript
for (const entry of captureRegressionMatrix) {
  const expectMatch = entry.status !== "red"; // "red" => defect live, must NOT match yet
  it(
    `${entry.id}: ${entry.tool} ${expectMatch ? "matches" : "does NOT yet match"} ORCL-01 cold expected value`,
    async () => {
      const { observed } = await replayCaptureRegressionEntry(entry, FIXTURES_ROOT);
      expect(matchesExpected(observed, entry.expected)).toBe(expectMatch);
    },
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Flip the flagship lock and add the D-05 guard entry</name>
  <files>test/fixtures/capture-regression-matrix.json, test/fixtures/capture-regression-matrix.ts</files>
  <read_first>
    test/fixtures/capture-regression-matrix.json (current, full — the one entry to flip)
    test/fixtures/capture-regression-matrix.ts (full, 65 lines — the header comment to clarify, plus the zod schema confirming `mutation` is optional and `status` accepts "locked")
    03.1-PATTERNS.md "test/fixtures/capture-regression-matrix.json (test/config data, batch) — MODIFY" section in full (both JSON changes, the schema excerpt, the runner excerpt)
    03.1-RESEARCH.md "Pattern 3: The D-05 guard test reuses the existing matrix" (the validated 6/6 empirical result) and "Open Questions" item 1 (the hand-authored-entry judgment call this task resolves)
    test/integration/mcp/capture-regression.test.ts (full, 55 lines — confirms ZERO runner code changes are needed; `expectMatch = entry.status !== "red"` already does the right thing for both a flipped-to-"locked" entry and a freshly-added-as-"locked" entry)
    test/helpers/capture-regression-runner.ts lines 82-100 (confirms a mutation-less entry just calls the tool once and uses that result — no second, mutated reload)
  </read_first>
  <action>
    In test/fixtures/capture-regression-matrix.json: change the existing issue-64-61-transitive-staleness entry's "status": "red" to "status": "locked" (D-03's definition of done) — every other field on this entry, including its expected block (ORCL-01's own cold-replay tuple from Phase 3), stays untouched. Then append a second entry to the array with the exact shape shown in this plan's interfaces block: id "guard-no-metas-clean-load-under-fault-injection", issue [64, 61], status "locked", tool "agda_load_no_metas", fixtureDir "FixtureDeps/TransitiveStaleness", entryFile "Main.agda", the SAME serverEnv fault-injection block as the flagship (AGDA_MCP_IDLE_COMPLETION_MS "1", AGDA_MCP_POST_STATUS_IDLE_MS "1"), NO mutation field — this is what makes it a clean-load assertion rather than a defect-trigger replay, and mutation is already optional in the schema — and expected: classification "ok-complete", success true, goalCount 0, invisibleGoalCount 0, hasHoles false, errorCategories an empty array. This is the definitionally-correct outcome for an unmutated, hole-free load, empirically confirmed 6/6 by 03.1-RESEARCH.md against this exact fault-injection env. This entry deliberately reuses the SAME already-committed, already-real FixtureDeps/TransitiveStaleness fixture pair Phase 3 captured — it introduces no new fixture content, so it is not the kind of hand-built synthetic bundle the file's header comment (see next edit) warns against.

    In test/fixtures/capture-regression-matrix.ts: extend the header comment's "No hand-authored entries (D-09 part1: \"no hand-built synthetic bundles\")" line with one clarifying sentence: this restriction is about a captured DEFECT's content provenance — an entry must not fabricate a fake bug — not a blanket ban on ever adding an entry by hand; a mutation-less guard entry over already-real, already-captured fixture content (like the one this plan adds) is not a synthetic bundle. Do not change the zod schema, the loader call, or any exported type in this file — this is a comment-only edit.

    Neither edit requires touching test/helpers/capture-regression-runner.ts or test/integration/mcp/capture-regression.test.ts — both already handle a status:"locked" entry, mutation-less or not, exactly as designed.
  </action>
  <verify>
    <automated>RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts` reports 2 passed, 0 failed — verbose names confirm both "issue-64-61-transitive-staleness: agda_load_no_metas matches ORCL-01 cold expected value" (the flip, D-03) and "guard-no-metas-clean-load-under-fault-injection: agda_load_no_metas matches ORCL-01 cold expected value" (the guard, D-05/D-02) now pass. A Node one-liner confirms the matrix has exactly 2 entries and both report status "locked": `node -e "const m=JSON.parse(require('fs').readFileSync('test/fixtures/capture-regression-matrix.json','utf8')); process.exit(m.length===2 && m.every(e=>e.status==='locked') ? 0 : 1)"` exits 0. `git diff --stat -- src/` is empty — this task never touches src/, confirming Plan 03.1-01 already shipped the entire fix and this task is pure proof.
  </acceptance_criteria>
  <done>The flagship defect is closed and locked; the D-02 false-RED risk is proven closed at the real MCP-tool-call boundary, under the exact fault-injection env that exposed the original bug — both facts are now durable, automated, real-Agda-gated regression tests, not one-off manual observations.</done>
</task>

<task type="auto">
  <name>Task 2: Full no-regression sweep — metas-path control, strict-path breadth, whole suite</name>
  <files>(none modified — verification-only task; exercises test/integration/agda/agda-load.test.ts, test/integration/agda/agda-fixture-matrix.test.ts, and the full suite)</files>
  <read_first>
    03.1-RESEARCH.md "Validation Architecture" section in full (the Phase Requirements -> Test Map table and the Sampling Rate tiers this task executes)
    03.1-VALIDATION.md Sampling Rate section ("Before /gsd:verify-work: Full suite must be green, incl. RUN_AGDA_INTEGRATION=1 on the flagship + guard")
    test/integration/agda/agda-load.test.ts (full, 335 lines — the metas-path control suite; D-05 requires this untouched by Plan 03.1-01)
    test/integration/agda/agda-fixture-matrix.test.ts lines 1-40 (the AGDA_FIXTURE_PHASES env-driven phase selection — confirms "strict" is already in the DEFAULT phase set, so AGDA_FIXTURE_PHASES=strict narrows to ONLY that phase rather than adding a new one)
  </read_first>
  <action>
    Run, in order, reading the output of each before proceeding: (1) `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/agda/agda-load.test.ts` — the metas-path control; D-05 requires every one of these tests to pass exactly as before Plan 03.1-01, since this file exercises agda_load/agda_typecheck, whose call site (runLoad's { awaitGoalTerminus: true }) Plan 03.1-01 never touched. (2) `RUN_AGDA_INTEGRATION=1 AGDA_FIXTURE_PHASES=strict npx vitest run test/integration/agda/agda-fixture-matrix.test.ts` — the strict-path breadth check across all 72 existing fixtures; every hole/error/abstract-meta/clean shape must still classify exactly as its expectedStrictSuccess/expectedStrictClassification says, proving the predicate change doesn't misclassify any shape beyond the one it targets. Expect this run to be noticeably slower than pre-fix (03.1-RESEARCH.md measured roughly 81s to 132s) — this is the disclosed, intentional latency tradeoff (every genuinely clean fixture now pays the widened idle window once), not a regression. (3) `RUN_AGDA_INTEGRATION=1 npm test` — the whole suite (unit, property, and integration, including both this plan's and Plan 03.1-01's own work). All three commands must exit 0 with zero failures. If step (2) or (3) surfaces any failure outside this phase's own fixtures/matrix, stop and report the exact failing test name and message rather than re-running or silently proceeding; 03.1-RESEARCH.md's Pitfall 4 documents ONE known, pre-existing, non-regressing source of nondeterminism — the metas-path control's EXACT fail-closed classification string under the extreme 1ms fault-injection env (used only by the flagship/guard entries in Plan 03.1-02 Task 1, not by any of this task's three commands, which all run under default timing) can vary between type-error and load-incomplete-no-terminus across runs; both are success:false, so this variance cannot surface in this task's own commands.
  </action>
  <verify>
    <automated>RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/agda/agda-load.test.ts</automated>
  </verify>
  <acceptance_criteria>
    All three commands in the action above exit 0. Step (1) reports every agda-load.test.ts test passing (matching or exceeding 03.1-RESEARCH.md's recorded baseline of 37 — a shrink or any failure is not acceptable). Step (2) reports every test in agda-fixture-matrix.test.ts's strict phase passing across all 72 fixtures (matching or exceeding the recorded baseline of 73 tests, 0 failed). Step (3) reports the whole suite green with 0 failed — a growth in the total pass count versus Phase 3's last recorded figures is expected and fine (this phase's own two new capture-regression entries add to it); a shrink or any unexpected failure is not.
  </acceptance_criteria>
  <done>D-04's scope fence and D-05's no-regression requirement are both proven, not just asserted: the metas path is untouched, the full 72-shape strict-path breadth is intact, and the whole suite — including both of this phase's own new regression entries — is green under real Agda.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Matrix entry fields (fixtureDir/entryFile, no mutation) -> capture-regression-runner.ts's isolated-copy replay | Pre-existing boundary from Phase 3; the new guard entry reuses the SAME already-sandboxed fixture and resolver — no new field, no new path, no new file-write introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-02-01 | Tampering | A malformed matrix entry causing the runner to misbehave | mitigate | Already covered — zod schema validation at load time (loadValidatedJsonData / captureRegressionEntrySchema), unchanged by this plan; both edits in this plan are schema-conformant (confirmed: `mutation` is `.optional()`, `status` accepts "locked") |
| T-03.1-02-SC | Tampering | npm/pip/cargo installs | N/A | No new package-manager installs in this plan |
</threat_model>

<verification>
- `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts` — 2 passed (the flip plus the guard).
- `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/agda/agda-load.test.ts` — metas-path control green.
- `RUN_AGDA_INTEGRATION=1 AGDA_FIXTURE_PHASES=strict npx vitest run test/integration/agda/agda-fixture-matrix.test.ts` — full strict-path breadth green.
- `RUN_AGDA_INTEGRATION=1 npm test` — whole suite green.
- Manual confirmation (read the verbose test names) that both the flipped flagship entry and the new guard entry report a passing "matches" assertion, not a coincidental pass.
</verification>

<success_criteria>
- The issue-64-61-transitive-staleness capture-regression matrix entry is status:"locked" and its test now asserts matchesExpected === true (D-03).
- The D-05 guard entry exists and passes, proving the D-02 false-RED risk is closed at the real tool-call boundary.
- The metas path, the full 72-fixture strict-path breadth, and the whole test suite all stay green (D-04, D-05).
- This is the concrete, observable close of the capture -> lock -> fix -> stay-locked loop for the #64/#61 defect — the phase goal.
</success_criteria>

<output>
Create `.planning/phases/03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-/03.1-02-SUMMARY.md` when done
</output>
