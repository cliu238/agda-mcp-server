---
phase: 06-backlog-digestion-policy-fix-reverify
plan: 05
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - src/tools/analysis-tools.ts
  - src/tools/file/search-definitions.ts
  - test/unit/tools/analysis-tools.test.ts
  - test/unit/tools/file-tools.test.ts
autonomous: true
requirements: [REVERIFY-02]
must_haves:
  truths:
    - "agda_proof_status never appends \"All goals solved.\" when constraints hold text — zero goals + non-empty constraints yields an honest not-confirmed-complete summary (fingerprint fdc90bfde12fb938)"
    - "agda_search_definitions accepts an optional directory parameter so src/-layout projects (agda-unimath's real layout) are searchable; default \"agda\" behavior is unchanged (fingerprint eb7439cb3ed9d6b9)"
    - "The new directory input cannot escape the project root — it routes through the same resolveFileWithinRoot/resolveExistingPathWithinRoot sandbox as agda_project_progress"
    - "Both fixes carry from-RED regression tests"
  artifacts:
    - path: "src/tools/analysis-tools.ts"
      provides: "Corrected completeness condition (goals AND constraints both consulted)"
    - path: "src/tools/file/search-definitions.ts"
      provides: "Optional directory input, sandboxed, default agda; not-found nextAction names the parameter"
      contains: "directory"
    - path: "test/unit/tools/analysis-tools.test.ts"
      provides: "agda_proof_status coverage (currently ZERO — confirmed gap)"
    - path: "test/unit/tools/file-tools.test.ts"
      provides: "src/-layout success case + directory sandbox-escape case"
  key_links:
    - from: "src/tools/file/search-definitions.ts"
      to: "src/repo-root.ts"
      via: "resolveFileWithinRoot + resolveExistingPathWithinRoot on the caller-supplied directory"
      pattern: "resolveExistingPathWithinRoot"
    - from: "src/tools/analysis-tools.ts"
      to: "session.query.constraints()"
      via: "constraints.text consulted in the completeness condition"
      pattern: "constraints\\.text"
---

<objective>
Fix the remaining two named confirmed defects, in QUEUE-02 order after plan 06-04's false-green band: agda_proof_status's contradictory "All goals solved." tagline (wrong-result, `fdc90bfde12fb938`) and agda_search_definitions' hardcoded `agda/` layout (missing-feature, `eb7439cb3ed9d6b9`). Runs in wave 3 parallel to 06-04 — the two plans share zero files.

Purpose: REVERIFY-02 — completes the named-4 "点名必修" fix set (D-10).
Output: two fixed tools + extended unit suites, from RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-CONTEXT.md
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-PATTERNS.md

<interfaces>
<!-- Exact current shapes at the fix sites. -->

src/tools/analysis-tools.ts lines 57-82 (agda_proof_status callback): builds output from `const metas = await session.goal.metas();` and `const constraints = await session.query.constraints();`. Bug: the final branch is `if (metas.goals.length === 0) { output += "All goals solved.\n"; }` — it ignores constraints.text entirely, so a stale reload that parked a real error in constraintsText still prints "All goals solved." right after the constraints block that displayed the error. Read the surrounding callback for the exact data payload fields it returns (goalCount / hasConstraints / constraintsText naming) before editing.

src/tools/file/search-definitions.ts lines 88-90 (the hardcoded root):
  const requestedSearchRoot = tier ? resolveFileWithinRoot(repoRoot, join("agda", tier)) : resolveFileWithinRoot(repoRoot, "agda");
Its not-found error path throws missingPathToolError("directory", ...) with a nextAction pointing at agda_list_modules. File is 207 lines.

The shipped analog to copy (src/tools/agent-ux/project-tools.ts lines 154-189, agda_project_progress): inputSchema `directory: z.string().optional().describe("Directory under project root to scan (default: agda/)")`; callback does `const requested = directory ?? "agda"; const scanRoot = resolveExistingPathWithinRoot(repoRoot, resolveFileWithinRoot(repoRoot, requested));`.

Known same-root-cause sites that are OUT OF SCOPE for this plan (D-09/D-10 name only agda_search_definitions): src/tools/file/list-modules.ts lines 44/94, src/tools/agent-ux/shared.ts line 168, project-tools.ts lines 188/309 — do NOT touch them; plan 06-06 records the broader observation in the queue notes if needed.

Fake-harness pattern: test/unit/tools/analysis-tools.test.ts lines 7-52 (createCapturingServer + fake session + clearToolManifest); test/unit/tools/file-tools.test.ts lines 1-45 + 265-293 (same server fake, REAL mkdtempSync fixtures — not mocked fs — and an existing symlink-escape precedent test for search-definitions).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agda_proof_status — completeness condition consults constraints (wrong-result fix)</name>
  <files>src/tools/analysis-tools.ts, test/unit/tools/analysis-tools.test.ts</files>
  <read_first>src/tools/analysis-tools.ts (the full agda_proof_status registration — locate its callback and data payload; the buggy branch sits near lines 57-82), test/unit/tools/analysis-tools.test.ts (harness + fake-session shape lines 1-60), test/fixtures/fix-queue.json entry fdc90bfde12fb938 (defect contract), .planning/research/CHG-REVERIFY.md Defect 1 "Related sub-finding" paragraph (the live observed contradiction)</read_first>
  <behavior>
    RED tests first in test/unit/tools/analysis-tools.test.ts (agda_proof_status currently has ZERO coverage — confirmed gap):
    - Fake session with goal.metas -> { goals: [] } AND query.constraints -> { text: "1.1-5: error: [UnequalTerms]\nBool !=< Nat" } (mirror the fake-session field set the existing agda_term_search test builds — getGoalIds/getLastClassification/getLoadedFile/isFileStale plus whatever the proof_status callback touches): the output text must NOT contain "All goals solved." and MUST contain a line stating constraints remain / completeness not confirmed.
    - Fake session with goals [] and constraints { text: "" }: output DOES contain "All goals solved." (clean case preserved).
    - Fake session with one goal and empty constraints: output contains the goal listing, no "All goals solved." (existing behavior preserved).
  </behavior>
  <action>
    In src/tools/analysis-tools.ts's agda_proof_status callback: change the final completeness branch from checking only metas.goals.length to `metas.goals.length === 0 && !constraints.text`; add an else-if for the zero-goals-but-constraints case appending the honest line: "No visible goals, but constraints remain — the file is NOT confirmed complete. See the Constraints section above." (exact wording is discretionary; it MUST contain "NOT confirmed complete" for grep-ability and must NOT contain "All goals solved."). Do not change the structured data payload fields (the tool is "technically correct in its structured fields" per CHG-REVERIFY — this is a presentation-text fix; keep the fix localized to the summary text).
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/analysis-tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The three new tests pass; RED evidence recorded (failing run before the src edit).
    - `grep -n "All goals solved" src/tools/analysis-tools.ts` shows the string emitted ONLY under the combined condition (goals empty AND no constraints text).
    - `grep -n "NOT confirmed complete" src/tools/analysis-tools.ts` returns exactly 1 match.
    - `npx vitest run test/unit/tools` passes; `wc -l src/tools/analysis-tools.ts` <= 500.
  </acceptance_criteria>
  <done>agda_proof_status can no longer contradict its own constraints block; the zero-goals+constraints state reads as not-confirmed-complete, from RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agda_search_definitions — optional sandboxed directory parameter (missing-feature fix)</name>
  <files>src/tools/file/search-definitions.ts, test/unit/tools/file-tools.test.ts</files>
  <read_first>src/tools/file/search-definitions.ts (full, 207 lines — input schema, the lines 88-90 hardcoding, the missingPathToolError not-found path, tier semantics), src/tools/agent-ux/project-tools.ts lines 154-189 (the analog to copy verbatim in shape), src/repo-root.ts (resolveFileWithinRoot / resolveExistingPathWithinRoot / PathSandboxError contracts), test/unit/tools/file-tools.test.ts (harness lines 1-45, the existing search-definitions symlink-escape test lines 265-293), test/fixtures/fix-queue.json entry eb7439cb3ed9d6b9</read_first>
  <behavior>
    RED tests first in test/unit/tools/file-tools.test.ts (real mkdtempSync fixtures, the file's own convention):
    - src/-layout success: temp project root containing src/Target.agda (module Target with a searchable symbol, mirroring the CHG-REVERIFY Defect-4 fixture: a data decl + one named definition); callback({ query: "<symbol>", directory: "src" }) returns a non-error envelope with matchCount >= 1 — this is the exact call shape that today returns not-found.
    - Default preserved: same content under agda/Target.agda; callback({ query: "<symbol>" }) (no directory) still succeeds.
    - Sandbox: callback({ query: "x", directory: "../../outside" }) yields the invalid-path/error envelope (PathSandboxError translation), never a read outside the root.
    - Not-found guidance: callback({ query: "x" }) against a root with NO agda/ dir returns the error envelope whose diagnostics nextAction mentions the `directory` parameter.
  </behavior>
  <action>
    In src/tools/file/search-definitions.ts:
    1. Add `directory: z.string().optional().describe("Directory under project root to search (default: agda/). Use e.g. \"src\" for src/-layout projects like agda-unimath.")` to the input schema, and thread it through the callback signature.
    2. Replace the lines 88-90 hardcoding with the project-tools shape: `const baseDir = directory ?? "agda";` then requestedSearchRoot = resolveFileWithinRoot(repoRoot, tier ? join(baseDir, tier) : baseDir), and keep/route the existing existence check through resolveExistingPathWithinRoot exactly as the current code + analog do — the caller-supplied value MUST pass through both sandbox helpers, never a bare join/resolve (Shared Patterns: Path sandboxing).
    3. Update the tool description string to mention the new parameter, and extend the not-found missingPathToolError path's nextAction text to name `directory` (e.g. append: "For src/-layout projects pass directory: \"src\"."). Keep tier semantics identical (tier joins under the chosen base directory).
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/file-tools.test.ts && npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All four new tests pass; RED evidence recorded for the src/-layout case (fails with not-found before the fix).
    - `grep -n "join(\"agda\"" src/tools/file/search-definitions.ts` returns zero matches (no hardcoded root remains in this file).
    - `grep -n "resolveExistingPathWithinRoot\|resolveFileWithinRoot" src/tools/file/search-definitions.ts` shows the directory value passing through the sandbox pair.
    - The pre-existing symlink-escape test still passes unmodified.
    - `wc -l src/tools/file/search-definitions.ts` <= 500; `npx vitest run test/unit/tools` green; tsc --noEmit green.
  </acceptance_criteria>
  <done>src/-layout projects are searchable via an explicit, sandboxed directory parameter; agda/ default and tier semantics unchanged; escape attempts still die in PathSandboxError.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client args → filesystem walk | the NEW `directory` input is untrusted and selects the search root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15 | Tampering / path traversal | search-definitions directory input | mitigate | resolveFileWithinRoot + resolveExistingPathWithinRoot sandbox pair (PathSandboxError -> invalid-path classification); explicit escape test in the suite |
| T-06-16 | Info disclosure | search results outside project root via symlinks | mitigate | existing symlink-containment test kept green (regression gate in acceptance criteria) |
| T-06-17 | Spoofing | "All goals solved." false completeness signal | mitigate | Task 1's combined condition; grep-able NOT-confirmed-complete line |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies installed |
</threat_model>

<verification>
- `npx vitest run test/unit/tools` green; `npx tsc -p tsconfig.json --noEmit` green; `npm run build` green.
- `wc -l` on both touched src files <= 500.
</verification>

<success_criteria>
- All four named confirmed defects (with plan 06-04) now have landed fixes + from-RED regression tests — the inputs plan 06-06 needs to flip every named entry to locked.
</success_criteria>

<output>
Create `.planning/phases/06-backlog-digestion-policy-fix-reverify/06-05-SUMMARY.md` when done. The summary MUST list the exact regression-test identifiers (file :: test name) per fingerprint for plan 06-06's queue notes.
</output>
