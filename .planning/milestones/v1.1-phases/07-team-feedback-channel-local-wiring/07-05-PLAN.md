---
phase: 07-team-feedback-channel-local-wiring
plan: 05
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - scripts/team/archive-extract.mjs
  - scripts/team/cron-ingest-wrapup.mjs
  - test/unit/tools/team-archive-extract.test.ts
  - test/unit/tools/team-cron-ingest-wrapup.test.ts
autonomous: true
requirements: [TEAM-04]

must_haves:
  truths:
    - "Decisions implemented here — D-01: the cron judge intakes confirmed entries via existing intake machinery then auto-commits and pushes directly to main (no PR-per-batch); D-02: local mode exercises the same write-back code path (--no-push dev flag only skips the push step); D-03: supersedes v2 TEAM-07 placeholder (write-back automated now); D-11: judge is a manually invoked, cron-able script (true unattended scheduling is Phase 8); D-14: scripts/team/ layout"
    - "A tar.gz containing a path-traversal entry (absolute path, or a name with a '..' segment) is rejected — pre-extraction listing check AND a post-extraction realpath containment re-check both independently catch it — nothing is ever written outside its own disposable scratch directory."
    - "Running cron-ingest-wrapup.mjs against a storage dir with unprocessed archives judges each staged capture through the SAME oracle triad + N-rerun flake gate as local dogfooding (via the unmodified wrapUpCapture), resolving policyKey from the archive's own recorded taskManifestCorpora — never from any local .agda-lib."
    - "A confirmed defect from an uploaded archive is upserted into test/fixtures/fix-queue.json with correct dedup (recurrence bump, not a duplicate row) against a pre-existing entry sharing its fingerprint."
    - "After judging, the run summary reports the fraction of judged captures whose oracle outcome was inconclusive/no-policy/no-target (the abstention rate) — printed to stdout and persisted to a summary file."
    - "A second cron-ingest-wrapup.mjs run against the same storage dir re-judges nothing already marked processed."
    - "Absent --no-push, a run that filed or updated at least one queue entry commits and pushes test/fixtures/fix-queue.json to the current branch; with --no-push, it commits locally only and never calls git push."
  artifacts:
    - path: "scripts/team/archive-extract.mjs"
      provides: "extractArchiveSafely — pre-list + post-extraction-realpath sandboxed tar extraction, decompressed-size ceiling"
      exports: ["DEFAULT_MAX_DECOMPRESSED_BYTES", "extractArchiveSafely"]
    - path: "scripts/team/cron-ingest-wrapup.mjs"
      provides: "Unattended judge: discovery, policyKey resolution, per-archive judging, run summary, git write-back"
      exports: ["discoverUnprocessedArchives", "resolveCronPolicyKey", "processArchive", "writeBackQueue", "scriptMain"]
    - path: "test/unit/tools/team-archive-extract.test.ts"
      provides: "Coverage of a REAL crafted path-traversal tar entry being rejected pre- and post-extraction"
    - path: "test/unit/tools/team-cron-ingest-wrapup.test.ts"
      provides: "DI-driven coverage of discovery/idempotency/policyKey resolution/abstention-rate math/write-back gating"
  key_links:
    - from: "scripts/team/cron-ingest-wrapup.mjs"
      to: "scripts/team/archive-extract.mjs"
      via: "import { extractArchiveSafely }"
      pattern: "extractArchiveSafely"
    - from: "scripts/team/cron-ingest-wrapup.mjs"
      to: "scripts/dogfood/dogfood-wrapup.mjs"
      via: "import { wrapUpCapture } — UNCHANGED function, never re-invoked as a CLI"
      pattern: "import \\{ wrapUpCapture \\} from \"\\.\\./dogfood/dogfood-wrapup\\.js\""
    - from: "scripts/team/cron-ingest-wrapup.mjs"
      to: "scripts/data/fuel-corpora.json"
      via: "fuelCorpora.find((entry) => entry.key === corpus) — the SAME lookup dogfood-wrapup.mjs's resolveWrapupPolicyKey already uses"
      pattern: "fuelCorpora\\.find"
    - from: "scripts/team/cron-ingest-wrapup.mjs writeBackQueue"
      to: "git remote (push)"
      via: "execFileSync(\"git\", [...], {shell:false})"
      pattern: "execFileSync\\(\"git\""
---

<objective>
Build TEAM-04: the unattended cron judge. `scripts/team/archive-extract.mjs` safely extracts an ingested archive into a disposable, sandboxed scratch directory (never bare tar trust — pre-extraction listing validation AND post-extraction realpath containment, mirroring the codebase's own proven `materializeCaptureEnvironment` doctrine). `scripts/team/cron-ingest-wrapup.mjs` drives each unprocessed archive through the UNCHANGED oracle-triad wrap-up machinery (`wrapUpCapture`, imported directly — never re-invoked as a CLI), resolves each bundle's `policyKey` via Phase 6's fix (consuming `taskManifestCorpora`, never re-deriving from `.agda-lib`), surfaces the per-run INCONCLUSIVE/abstention rate, marks archives processed (the cheap early-dedup layer against retry-storm re-judging), and commits+pushes queue changes to `main` per D-01/D-02.

Purpose: this is the last piece of local plumbing E2E-01 (07-06) exercises live.
Output: two new scripts + two new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-team-feedback-channel-local-wiring/07-CONTEXT.md
@.planning/phases/07-team-feedback-channel-local-wiring/07-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
scripts/team/ingest-server.mjs
scripts/dogfood/dogfood-wrapup.mjs
</context>

<interfaces>
<!-- Exact exports from 07-03 (storage layout this plan reads) and 07-04 (the schema field
     this plan consumes), both real on disk by this plan's wave. -->
Storage layout (07-03): `<AGDA_MCP_TEAM_STORAGE_DIR>/<person>/<date:YYYY-MM-DD>/<runId>.tar.gz`.
`resolveTeamStorageDir()` (scripts/team/ingest-server.mjs) is the SAME resolver this plan reuses
to find that root.

Archive internal layout (produced by 07-02's upload-run.mjs, unpacked by this plan):
```
<extracted root>/
  runs/<run-id>/run-report.json   (includes taskManifestCorpora: string[], per 07-04)
  runs/<run-id>/...                (transcript.jsonl, run-report.md, etc. — not needed here)
  captures/<basename of each original stagedPath>.json
  agent-logs/claude/*.jsonl        (not needed by judging)
  agent-logs/codex/*.jsonl         (not needed by judging)
```

From src/repo-root.ts:
```typescript
export class PathSandboxError extends Error { readonly targetPath: string; }
export function resolveFileWithinRoot(projectRoot: string, targetPath: string): string;
export function resolveExistingPathWithinRoot(projectRoot: string, targetPath: string): string; // realpath-based, catches symlink escapes AFTER extraction
export const SERVER_REPO_ROOT: string;
```

From scripts/dogfood/dogfood-wrapup.mjs (import UNCHANGED — never modify wrapUpCapture itself):
```javascript
export async function wrapUpCapture(artifactPath, artifact, config = {}) // config: {queueJsonPath, flakyLogPath, n, policyKey, deps}
// returns one of:
//   { filed:false, classification:"not-a-candidate", verdict }
//   { filed:false, classification:"flaky"|"replay-inconclusive", tag, verdict, flake }
//   { filed:true,  classification:"deterministic", verdict }
```
`verdict.orcl01.kind` and `verdict.orcl02.kind` are the per-predicate outcome strings this
plan's abstention-rate computation reads (`"inconclusive"` for orcl01; `"no-policy"`/`"no-target"`
for orcl02 — see scripts/oracle/verdict-schema.mjs's header comment for the full outcome-kind
unions).

From scripts/dogfood/dogfood-wrapup.mjs's own `resolveWrapupPolicyKey` (lines 291-334, the
precedent this plan's `resolveCronPolicyKey` mirrors, minus the `--policy` flag and manifest-file
branches that make no sense for an unattended, upload-sourced judge):
```javascript
const corpora = [...new Set(manifest.map((entry) => entry.corpus))];
const [corpus] = corpora;
const fuelCorpusEntry = fuelCorpora.find((entry) => entry.key === corpus);
return fuelCorpusEntry?.policyKey; // undefined on any mismatch — NEVER re-derive from .agda-lib
```

From test/fixtures/fuel-corpora.ts: `export const fuelCorpora: Array<{key, repo, access, pinnedRef, policyKey, notes}>;`

From scripts/queue/mirror-github.mjs lines 40-75 (the ONLY prior `git`-adjacent-CLI precedent in
this repo, `execFileSync`-argv-array + explicit-flag-gate shape — `git commit`/`git push`
themselves have zero prior art anywhere in this codebase and must be written fresh per this
plan, modeled on this shape):
```javascript
export function isGhAvailable(options = {}) {
  const execFile = options.deps?.execFileSync ?? execFileSync;
  try { execFile("gh", ["--version"], {encoding:"utf8", stdio:[...], timeout:5000, shell:false}); return true; }
  catch { return false; }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extractArchiveSafely — sandboxed, disposable extraction</name>
  <files>scripts/team/archive-extract.mjs, test/unit/tools/team-archive-extract.test.ts</files>
  <read_first>
    scripts/oracle/orcl-01-differential.mjs lines 85-131 (materializeCaptureEnvironment — the mkdtempSync-scratch-dir + PathSandboxError-catch-and-skip doctrine THIS task ports to real tar extraction)
    src/repo-root.ts (resolveExistingPathWithinRoot's realpath-based containment check — this task's primary reused primitive)
    scripts/queue/mirror-github.mjs lines 56-75 (options.deps?.execFileSync dependency-injection convention, mirrored here for the tar spawn calls)
  </read_first>
  <behavior>
    - extractArchiveSafely against a REAL, well-formed tar.gz (a small fixture with a couple of plain files) returns {ok:true, scratchDir, cleanup}, and every original file is readable under scratchDir with identical content.
    - extractArchiveSafely against a tar CRAFTED with a literal `../escaped.txt` entry name (built via the real system tar over a relative `-C <childDir> ../escaped.txt` invocation, the exact recipe that produces a raw `../escaped.txt` entry name) returns {ok:false, reason:"unsafe-entry-path", detail: "../escaped.txt"} WITHOUT ever calling the extract step (assert via a spy on options.deps.execFileSync that only ONE invocation — the listing — occurred, never a second "-x" invocation).
    - extractArchiveSafely against an archive whose LISTING is clean but whose extraction (simulated via a deps override) leaves behind an unexpected file resolving outside scratchDir returns {ok:false, reason:"post-extraction-escape", ...} and removes scratchDir before returning (defense-in-depth path, exercised by injecting a fake "extract" step that plants a file via a symlink-like fixture rather than by finding a real tar bypass).
    - extractArchiveSafely against an archive whose cumulative decompressed content exceeds a small INJECTED maxDecompressedBytes returns {ok:false, reason:"decompressed-size-exceeded"} and removes scratchDir.
    - On EVERY {ok:false} path, scratchDir no longer exists on disk afterward (no leaked temp directories).
    - The returned {ok:true, cleanup} function, once called, removes scratchDir.
  </behavior>
  <action>
    Create scripts/team/archive-extract.mjs.

    export const DEFAULT_MAX_DECOMPRESSED_BYTES = 5 * 1024 * 1024 * 1024 (5 GiB — a generous, documented defense-in-depth ceiling, not derived from any specific requirement number; comment this explicitly as an engineering default, not a literal spec value).

    export async function extractArchiveSafely(archivePath, options = {}): const execFile = options.deps?.execFileSync ?? execFileSync (from "node:child_process"); const maxDecompressedBytes = options.maxDecompressedBytes ?? DEFAULT_MAX_DECOMPRESSED_BYTES.

    Step A (pre-check): let listing; try { listing = execFile("tar", ["-tf", archivePath], {encoding:"utf8", stdio:["ignore","pipe","pipe"], shell:false}); } catch (err) { return {ok:false, reason:"listing-failed", detail: err instanceof Error ? err.message : String(err)}; }. const entries = listing.split("\n").filter(Boolean); const unsafe = entries.find((e) => e.startsWith("/") || e.split("/").includes("..")); if (unsafe) return {ok:false, reason:"unsafe-entry-path", detail: unsafe}; — reject the WHOLE archive on the first offending entry, before any extraction.

    Step B (extract into a disposable scratch dir): const mkdtemp = options.deps?.mkdtempSync ?? mkdtempSync (from "node:fs"); const scratchDir = mkdtemp(join(tmpdir(), "agda-mcp-team-extract-")) (tmpdir from "node:os", join from "node:path"). Run extraction BOUNDED, not fire-and-forget (plan-checker finding 2 / PITFALLS.md Pitfall 6 — never materialize an untrusted archive unbounded onto disk): spawn tar asynchronously — const child = (options.deps?.spawn ?? spawn)("tar", ["-x", "-f", archivePath, "-C", scratchDir], {stdio:["ignore","pipe","pipe"], shell:false}) (spawn from "node:child_process") — and while it runs, poll the scratch dir's cumulative on-disk size every 500ms (reuse the same du/stat-walk helper Step C uses); if cumulative bytes exceed maxDecompressedBytes mid-extraction, child.kill("SIGKILL"), rmSync(scratchDir,{recursive:true,force:true}), and return {ok:false, reason:"decompressed-size-exceeded-during-extraction", detail:...}. On child exit non-zero: rmSync scratch and return {ok:false, reason:"extraction-failed", detail: stderr tail}. — never pass `-P`/`--absolute-names` (refusing absolute paths is tar's own default second layer). Note: `tar -tf`/`-xf` auto-detect gzip compression — no manual zlib step is needed for extraction (empirically confirmed: `tar -tf`/`-xf` work directly against a `.tar.gz` without a `-z` flag).

    Step C (post-extraction realpath containment + cumulative size, ONE combined walk): let totalBytes = 0; const relPaths = (options.deps?.readdirSync ?? readdirSync)(scratchDir, {recursive:true}) (readdirSync from "node:fs", Node 20+'s recursive option). for (const relPath of relPaths) { let contained; try { contained = resolveExistingPathWithinRoot(scratchDir, relPath); } catch (err) { if (err instanceof PathSandboxError) { rmSync(scratchDir,{recursive:true,force:true}); return {ok:false, reason:"post-extraction-escape", detail: relPath}; } throw err; } const stats = statSync(contained); if (stats.isFile()) { totalBytes += stats.size; if (totalBytes > maxDecompressedBytes) { rmSync(scratchDir,{recursive:true,force:true}); return {ok:false, reason:"decompressed-size-exceeded", detail: `${totalBytes} bytes exceeds ${maxDecompressedBytes}`}; } } }.

    Return {ok:true, scratchDir, cleanup: () => rmSync(scratchDir, {recursive:true, force:true})}.

    Import PathSandboxError and resolveExistingPathWithinRoot from "../../src/repo-root.js".
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/team-archive-extract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/team-archive-extract.test.ts` passes.
    - At least one test builds a REAL tar file via the system `tar` binary containing a genuine `../` entry (using the `tar -C <childDir> -cf evil.tar ../secret.txt` recipe) and asserts `extractArchiveSafely` rejects it with `reason: "unsafe-entry-path"`.
    - At least one test asserts that after ANY `{ok:false}` result, the returned/attempted scratch directory path no longer exists on disk.
    - `grep -n "resolveExistingPathWithinRoot" scripts/team/archive-extract.mjs` confirms the post-extraction realpath check is present (not just `resolveFileWithinRoot`, which does not follow symlinks).
  </acceptance_criteria>
  <done>scripts/team/archive-extract.mjs exists exporting extractArchiveSafely with pre-list + post-extraction-realpath + decompressed-size defenses, all three independently proven by tests including one against a real crafted traversal tar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cron-ingest-wrapup.mjs — discovery, judging, abstention rate, write-back</name>
  <files>scripts/team/cron-ingest-wrapup.mjs, test/unit/tools/team-cron-ingest-wrapup.test.ts</files>
  <read_first>
    scripts/team/archive-extract.mjs (Task 1's exports — import directly)
    scripts/dogfood/dogfood-wrapup.mjs lines 189-267, 291-334, 408-459 (wrapUpCapture's exact config shape; resolveWrapupPolicyKey's fuelCorpora lookup idiom to mirror; the per-item error-isolation + summary-with-counts loop shape to copy verbatim)
    scripts/data/fuel-corpora.json and test/fixtures/fuel-corpora.ts (the policyKey lookup table)
    scripts/queue/mirror-github.mjs lines 40-75, 144-201 (execFileSync-argv-array + explicit-flag-gate shape for the git write-back — model, not reuse, since git commit/push have zero prior art)
    src/repo-root.ts (SERVER_REPO_ROOT)
  </read_first>
  <behavior>
    - discoverUnprocessedArchives(storageDir) walks <storageDir>/<person>/<date>/*.tar.gz three levels deep and returns every archive WITHOUT a sibling `<archive>.processed.json`, skipping any that already has one.
    - resolveCronPolicyKey(taskManifestCorpora): returns the single fuel-corpora.json policyKey when taskManifestCorpora has EXACTLY one entry that matches a known fuelCorpora[].key; returns undefined for an empty array, a multi-value array, or an unknown corpus key (never guesses, never throws).
    - processArchive for a "true-green" capture (via a fake wrapUpCapture returning filed:false/not-a-candidate) does not call the fake upsertQueueEntry and marks the archive processed.
    - processArchive for a capture whose fake wrapUpCapture returns filed:true correctly counts toward `filed` in the run summary.
    - processArchive resolves each staged capture's on-disk path as `<scratchDir>/captures/<basename of the run-report.json stagedCaptures[i].stagedPath>` — NEVER the original (uploader-machine-only) absolute stagedPath.
    - The abstention-rate computation counts a capture as abstained when verdict.orcl01.kind === "inconclusive" OR verdict.orcl02.kind is "no-policy" or "no-target", divided by totalCaptures, surfaced as a number in [0,1] on the run summary.
    - A second discoverUnprocessedArchives call after processArchive has written a `.processed.json` sidecar for an archive no longer returns that archive.
    - writeBackQueue with a fake execFileSync and filedCount > 0 and no --no-push calls execFileSync exactly 3 times in order: ["git","add",...], ["git","commit",...], ["git","push"] — all with shell:false and cwd: SERVER_REPO_ROOT.
    - writeBackQueue with --no-push calls execFileSync exactly 2 times (add, commit) and NEVER calls it a 3rd time with "push".
    - writeBackQueue with filedCount === 0 calls execFileSync zero times (nothing changed, nothing to commit).
    - A thrown error from the fake execFileSync (e.g. "nothing to commit") is caught and surfaced in the returned result object rather than crashing scriptMain.
  </behavior>
  <action>
    Create scripts/team/cron-ingest-wrapup.mjs.

    discoverUnprocessedArchives(storageDir): if !existsSync(storageDir) return []. For each person subdir, for each date subdir, for each `*.tar.gz` file, compute processedPath = `${archivePath}.processed.json`; if !existsSync(processedPath), push {archivePath, person, date}. Return the array (order: oldest-by-path-sort is fine, no strict ordering requirement).

    resolveCronPolicyKey(taskManifestCorpora): const corpora = Array.isArray(taskManifestCorpora) ? [...new Set(taskManifestCorpora)] : []; if (corpora.length !== 1) return undefined; const entry = fuelCorpora.find((e) => e.key === corpora[0]); return entry?.policyKey; — import fuelCorpora from "../../test/fixtures/fuel-corpora.js" (the SAME typed-constant import dogfood-wrapup.mjs itself uses, per its own header note distinguishing this from src/json-data.ts's runtime loader).

    processArchive({archivePath}, config = {}): const extractFn = config.deps?.extractArchiveSafely ?? extractArchiveSafely; const extracted = await extractFn(archivePath); if (!extracted.ok) { write a `${archivePath}.processed.json` sidecar recording {processedAt, ok:false, reason: extracted.reason} via writeFileAtomic (so a permanently-broken archive is not retried forever — an unsafe/corrupt archive is "processed" as a terminal failure, not silently retried every cron tick) and return {archivePath, error: extracted.reason, results:[]}; } try { const runsDir = join(extracted.scratchDir, "runs"); const runIds = existsSync(runsDir) ? readdirSync(runsDir) : []; if (runIds.length !== 1) { record a processed.json with ok:false, reason:"unexpected-run-count", return early; } const runId = runIds[0]; const reportPath = join(runsDir, runId, "run-report.json"); const report = JSON.parse(readFileSync(reportPath, "utf8")); const policyKey = resolveCronPolicyKey(report.taskManifestCorpora); const results = []; for (const staged of Array.isArray(report.stagedCaptures) ? report.stagedCaptures : []) { const artifactPath = join(extracted.scratchDir, "captures", basename(staged.stagedPath)); try { const artifact = JSON.parse(readFileSync(artifactPath, "utf8")); const wrapUpFn = config.deps?.wrapUpCapture ?? wrapUpCapture; const outcome = await wrapUpFn(artifactPath, artifact, {queueJsonPath: config.queueJsonPath, flakyLogPath: config.flakyLogPath, n: config.rerunN ?? 3, policyKey}); results.push({artifactPath, ...outcome}); } catch (err) { results.push({artifactPath, filed:false, classification:"error", error: err instanceof Error ? err.message : String(err)}); } } await writeFileAtomic(`${archivePath}.processed.json`, JSON.stringify({processedAt: new Date().toISOString(), ok:true, runId, results}, null, 2)); return {archivePath, runId, results}; } finally { extracted.cleanup(); }

    Note in a comment above discoverUnprocessedArchives: the `.processed.json` sidecar check IS the cheap early-dedup layer PITFALLS.md's Pitfall 7 asks for — a retried upload of the same run-id lands at the SAME storage path (07-03's layout is keyed by person/date/runId, and upload-run.mjs always reuses the SAME stable runId across retries of one run) and is skipped here BEFORE the expensive oracle triad ever runs again.

    Run summary aggregation (in scriptMain, mirroring dogfood-wrapup.mjs's own summary object shape at lines 232-243 exactly, renamed for archives): totalArchives, totalCaptures (sum of all results[].length across archives), filed, flaky, replayInconclusive, notACandidate, errors — all `results.filter(...)` counts exactly like dogfood-wrapup.mjs's own summary. Additionally compute `abstained = results.filter((r) => r.verdict?.orcl01?.kind === "inconclusive" || r.verdict?.orcl02?.kind === "no-policy" || r.verdict?.orcl02?.kind === "no-target").length` and `abstentionRate = totalCaptures > 0 ? abstained / totalCaptures : 0`.

    writeBackQueue({queueJsonPath, noPush, filedCount, deps} = {}): if (!filedCount || filedCount <= 0) return {committed:false, skipped:"nothing-to-commit"}. const execFile = deps?.execFileSync ?? execFileSync (from "node:child_process"). const relQueuePath = relative(SERVER_REPO_ROOT, queueJsonPath); if (relQueuePath.startsWith("..")) return {committed:false, skipped:"queue-path-outside-repo"} (relative, from "node:path" — the general, no-special-casing guard for test-mode queue paths outside the repo). try { execFile("git", ["add", relQueuePath], {cwd: SERVER_REPO_ROOT, shell:false}); execFile("git", ["commit", "-m", `queue(team-cron): intake ${filedCount} confirmed finding(s) from unattended cron judge`], {cwd: SERVER_REPO_ROOT, shell:false}); let pushed = false; if (!noPush) { execFile("git", ["push"], {cwd: SERVER_REPO_ROOT, shell:false}); pushed = true; } return {committed:true, pushed}; } catch (err) { return {committed:false, error: err instanceof Error ? err.message : String(err)}; }

    scriptMain(argv): parse `--no-push` (boolean flag), `--rerun-n <N>` (default 3, same positive-integer-or-throw validation as dogfood-wrapup.mjs's own parseWrapupArgv), `--storage-dir <path>` (default resolveTeamStorageDir(), imported from "../team/ingest-server.js"), `--queue-path <path>` (default join(SERVER_REPO_ROOT, "test/fixtures/fix-queue.json")). const archives = discoverUnprocessedArchives(storageDir); const allResults = []; for (const info of archives) { try { const outcome = await processArchive(info, {queueJsonPath, flakyLogPath: join(SERVER_REPO_ROOT, ".agda-mcp","team","cron-flaky.jsonl"), rerunN}); allResults.push(outcome); } catch (err) { allResults.push({archivePath: info.archivePath, error: String(err)}); } }. Flatten every archive's `results[]` into one array for the summary math described above. filedCount = flattened.filter((r) => r.filed).length. const writeBack = await writeBackQueue({queueJsonPath, noPush, filedCount}). Build and print the summary (stdout: `[cron-ingest-wrapup] processed ${totalArchives} archive(s), ${totalCaptures} capture(s) — ${filed} filed, ${abstained} abstained (${(abstentionRate*100).toFixed(1)}%), ${errors} error(s).`). writeFileAtomic the full summary object to `join(SERVER_REPO_ROOT, ".agda-mcp", "team", "cron-runs", `${new Date().toISOString().replace(/[:.]/g,"-")}.json`)` (mkdirSync recursive first). process.exitCode = 1 if errors > 0 (mirrors dogfood-wrapup.mjs's own convention). End with the isMainModule guard.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/team-cron-ingest-wrapup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/team-cron-ingest-wrapup.test.ts` passes, fully DI-driven (fake extractArchiveSafely/wrapUpCapture/execFileSync — zero real Agda/git/subprocess cost).
    - A test asserts a second `discoverUnprocessedArchives` call skips an archive after its `.processed.json` sidecar is written.
    - A test asserts `writeBackQueue` calls `execFileSync` with `["git","push"]` as the 3rd call when `noPush` is falsy and NEVER when `noPush` is true.
    - A test asserts `resolveCronPolicyKey` returns `undefined` (never throws, never guesses) for a multi-corpus or unknown-corpus array.
    - `grep -n "import { wrapUpCapture }" scripts/team/cron-ingest-wrapup.mjs` confirms direct ESM import, never a `spawn`/`execFileSync` re-invocation of dogfood-wrapup.mjs's own CLI.
    - `grep -n "abstentionRate" scripts/team/cron-ingest-wrapup.mjs` confirms the rate is computed and included in the printed/persisted summary.
  </acceptance_criteria>
  <done>scripts/team/cron-ingest-wrapup.mjs exists with all listed exports; discovery is idempotent across runs; policyKey resolution never re-derives from .agda-lib; the run summary surfaces the abstention rate; write-back correctly gates on --no-push and on filedCount > 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Already-ingest-accepted archive (07-03) -> extraction -> local filesystem | Archive content originates from a remote (albeit trusted-team) machine |
| Judged verdict -> tracked git repo (`main`) | Automated write-back with the operator's own repo-write credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-07-18 | Tampering | tar-slip / path-traversal / symlink escape during extraction | mitigate | Two independent layers (Task 1): pre-extraction listing rejection of absolute/`..` entries, AND post-extraction `resolveExistingPathWithinRoot` realpath containment re-check — never bare tar trust, per PITFALLS.md Pitfall 6 |
| T-07-19 | Denial of Service | Decompression bomb inflating far past the 512 MiB ingest cap | mitigate | `DEFAULT_MAX_DECOMPRESSED_BYTES` ceiling (5 GiB) enforced DURING extraction via a 500ms scratch-dir size watcher that SIGKILLs tar on breach (Step B), plus the post-extraction walk re-check (Step C) as the second layer — archive rejected and scratch dir removed on breach in both layers |

**Checker-mandated additions (findings 3 & 4):**
- **Server-version skew (Pitfall 10):** `processArchive` MUST compare `artifact.manifest.serverVersion` (already recorded on every capture by `src/agda/session-capture/manifest-builder.ts`) against the judge's own server version (`getServerVersion()` / package.json version); when they differ, annotate the resulting queue entry's `notes` with `version-skew: captured=<v1> judged=<v2>` and surface a per-run skew count in the run summary. Verdicts are NOT invalidated by skew — it is recorded context, not a gate.
- **Deferral (TEAM-04 `agdaDirContents.libraries` absolute-path probe gap):** cross-machine remapping of uploader-absolute library paths in `agdaDirHashProbe` is explicitly DEFERRED from this phase — CHG (the E2E-01 acceptance corpus) vendors agda-unimath so `agdaDirContents.libraries` is empty on the acceptance path, and the abstention-rate surfacing this plan ships will visibly flag any future corpus that hits the gap. Recorded here per plan-checker finding 4; revisit when a non-vendored team corpus arrives.

| T-07-20 | Elevation of Privilege | The git write-back credential (repo push access to `main`) | accept (local mode) | Phase 7 runs with the OPERATOR's own already-trusted local git credential; D-01 explicitly notes Phase 8 must scope a service credential minimally (fine-grained PAT/deploy key) — flagged for that phase's DEBT-05 security review, not re-litigated here |
| T-07-21 | Repudiation | An automated commit lands on `main` with unclear provenance | mitigate | Commit message names the finding count and that it came from the unattended cron judge; recovery path is `git revert` (D-01); `--no-push` dev flag avoids polluting remote history during iteration |
| T-07-22 | Tampering | An unresolvable/mismatched policyKey silently degrading cheat-detection for an uploaded corpus | mitigate | `resolveCronPolicyKey` only ever returns a value on an exact, unambiguous `fuelCorpora[].key` match; `wrapUpCapture`/`runOracle`/`judgeOrcl02` (all UNCHANGED, Phase 6) already throw `PolicyResolutionError` uncaught for an explicitly-requested-but-unresolvable key — loud, never silent (D-03), inherited automatically since this plan never re-derives from `.agda-lib` |
</threat_model>

<verification>
Both tasks' automated commands pass. Confirm the "cheap early-dedup" reasoning (Pitfall 7) is documented as a code comment, not just asserted in this plan.
</verification>

<success_criteria>
- Archive extraction is proven safe against a real crafted path-traversal tar, with two independent defensive layers.
- The cron judge reuses the exact same oracle-triad/flake-gate/queue-intake machinery as local dogfooding, with zero duplicated judging logic.
- policyKey resolution for uploaded corpora never falls back to `.agda-lib` name derivation.
- The abstention rate is a first-class, surfaced number every run.
- Re-running the judge is idempotent; write-back is correctly gated on both `--no-push` and on there being anything to commit at all.
</success_criteria>

<output>
Create `.planning/phases/07-team-feedback-channel-local-wiring/07-05-SUMMARY.md` when done
</output>
