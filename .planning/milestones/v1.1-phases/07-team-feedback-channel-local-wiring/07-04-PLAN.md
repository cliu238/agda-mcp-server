---
phase: 07-team-feedback-channel-local-wiring
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - scripts/dogfood/transcript-writer.mjs
  - scripts/dogfood/dogfood-run.mjs
  - scripts/dogfood/dogfood-wrapup.mjs
  - test/unit/tools/dogfood-transcript-writer.test.ts
  - test/unit/tools/dogfood-wrapup-upload-chain.test.ts
autonomous: true
requirements: [TEAM-02, TEAM-04]

must_haves:
  truths:
    - "Decisions implemented here — D-06: the maintainer-as-teammate wire (key issuance → upload → ingest) proven locally end-to-end; D-08: failed uploads land in the bounded retry queue and are retried on the next invocation (fail-open, never blocks the teammate)"
    - "run-report.json now records taskManifestCorpora: string[] — the distinct fuel-corpora.json corpus key(s) the run's own task manifest referenced — defaulting to [] for backward compatibility when omitted."
    - "Running dogfood-wrapup.mjs with AGDA_MCP_TEAM_UPLOAD_KEY/URL set automatically invokes upload-run.mjs for that run-id after judging completes, even when some captures in that run errored."
    - "dogfood-wrapup.mjs's own process.exitCode still reflects judging errors (unchanged from before this plan) — a failed or no-op upload chain step never clears or overwrites an already-nonzero exit code."
  artifacts:
    - path: "scripts/dogfood/transcript-writer.mjs"
      provides: "getReport() gains an additive taskManifestCorpora field"
    - path: "scripts/dogfood/dogfood-run.mjs"
      provides: "Threads the task manifest's distinct corpus values into getReport()"
    - path: "scripts/dogfood/dogfood-wrapup.mjs"
      provides: "chainUploadRun — the D-12 unconditional tail step, joined-with-';' semantics"
      exports: ["chainUploadRun"]
    - path: "test/unit/tools/dogfood-wrapup-upload-chain.test.ts"
      provides: "DI-driven coverage that chaining never affects dogfood-wrapup.mjs's own exit code"
  key_links:
    - from: "scripts/dogfood/dogfood-run.mjs"
      to: "scripts/dogfood/transcript-writer.mjs getReport()"
      via: "taskManifestCorpora parameter"
      pattern: "taskManifestCorpora"
    - from: "scripts/dogfood/dogfood-wrapup.mjs scriptMain tail"
      to: "scripts/dogfood/upload-run.mjs"
      via: "spawn (never execFileSync — network call may be slow)"
      pattern: "chainUploadRun"
---

<objective>
Wire TEAM-02's upload client into the existing dogfooding pipeline with two small, additive changes: (1) `run-report.json` gains a `taskManifestCorpora` field so an unattended judge (07-05) can resolve a bundle's `policyKey` without a human typing `--policy`; (2) `dogfood-wrapup.mjs` unconditionally chains `upload-run.mjs` at the end of its run (D-12), joined with the same "always runs regardless of prior errors" semantics as a shell `;` — never `&&`.

Purpose: closes the two remaining plumbing gaps between the existing v1.0 dogfood loop and the new team-channel scripts (07-02, 07-05), with zero behavior change to anyone not using the team channel (both additions default to empty/no-op).
Output: two modified pipeline files gain one additive field/step each; two test files (one new, one extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-team-feedback-channel-local-wiring/07-CONTEXT.md
@.planning/phases/07-team-feedback-channel-local-wiring/07-PATTERNS.md
scripts/dogfood/upload-run.mjs
</context>

<interfaces>
<!-- Exact exports from 07-02 (scripts/dogfood/upload-run.mjs), now real on disk. This plan's
     Task 2 spawns this script's CLI; it does not need to import its internal functions. -->
CLI contract: `npx tsx scripts/dogfood/upload-run.mjs <run-id>` — always exits 0 regardless of
upload success/failure (fail-open all the way to the process exit code, per 07-02's design).

From scripts/dogfood/transcript-writer.mjs (getReport's CURRENT signature, before this plan's change):
```javascript
getReport({ runId, startedAt, corpusRoot, manifestPath }) {
  // returns { schemaVersion, runId, startedAt, endedAt, corpusRoot, manifestPath,
  //           totalToolCalls, perTool, stagedCaptures, transcriptPath }
}
```

From scripts/dogfood/dogfood-run.mjs (the exact call site this plan touches, `runDogfoodProxy`):
```javascript
loadTaskManifest(manifestPath); // return value currently discarded
// ... later, inside finalize():
const report = recorder.getReport({ runId, startedAt, corpusRoot, manifestPath });
```

From scripts/dogfood/dogfood-wrapup.mjs (the exact tail this plan extends, end of `scriptMain`):
```javascript
await writeFileAtomic(join(runDir, "wrapup-report.json"), JSON.stringify(summary, null, 2));
process.stdout.write(`[dogfood-wrapup] run ${runId}: ...\n`);
if (summary.errors > 0) {
  process.exitCode = 1;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: taskManifestCorpora — additive run-report.json field</name>
  <files>scripts/dogfood/transcript-writer.mjs, scripts/dogfood/dogfood-run.mjs, test/unit/tools/dogfood-transcript-writer.test.ts</files>
  <read_first>
    scripts/dogfood/transcript-writer.mjs (getReport's exact current body — lines ~204-221)
    scripts/dogfood/dogfood-run.mjs (runDogfoodProxy — the `loadTaskManifest(manifestPath);` call whose return value is currently discarded, and the `finalize()` closure's `getReport({...})` call site)
    scripts/dogfood/dogfood-wrapup.mjs lines 291-334 (`resolveWrapupPolicyKey` — the EXISTING `[...new Set(manifest.map((entry) => entry.corpus))]` dedup-and-array-from-Set idiom to reuse verbatim, not reinvent)
    test/unit/tools/dogfood-transcript-writer.test.ts (the existing getReport() call sites at lines 64-69, 135-140, 158-163 — none currently pass taskManifestCorpora; the new field must default so these keep passing UNMODIFIED)
  </read_first>
  <action>
    In scripts/dogfood/transcript-writer.mjs: change `getReport({ runId, startedAt, corpusRoot, manifestPath })`'s destructuring to `getReport({ runId, startedAt, corpusRoot, manifestPath, taskManifestCorpora = [] })` and add `taskManifestCorpora` to the returned object (placed after `manifestPath`, before `totalToolCalls`, matching the existing field ordering style). `schemaVersion` stays `1` (additive, backward-compatible — ARCHITECTURE.md Pattern 4 explicitly calls this out). Do not touch `writeRunReport` — it already serializes whatever object it is given.

    In scripts/dogfood/dogfood-run.mjs's `runDogfoodProxy`: change `loadTaskManifest(manifestPath);` to `const manifest = loadTaskManifest(manifestPath);` (capture the return value this task now needs — `runDogfoodProxy` is one function scope, so `manifest` is in closure for `finalize()` defined later in the same function body). Inside `finalize()`, immediately before the `recorder.getReport({...})` call, compute `const taskManifestCorpora = [...new Set(manifest.map((entry) => entry.corpus))];` (the exact idiom cited above from `resolveWrapupPolicyKey`) and add `taskManifestCorpora` to the object passed into `getReport(...)`.

    In test/unit/tools/dogfood-transcript-writer.test.ts: add two NEW test cases (do not modify the three existing getReport() call sites — they must keep passing exactly as written, proving backward compatibility): (1) calling `getReport({runId, startedAt, corpusRoot, manifestPath})` WITHOUT `taskManifestCorpora` yields `report.taskManifestCorpora` equal to `[]`. (2) calling `getReport({..., taskManifestCorpora: ["codex-homotopy-group"]})` yields that exact array back on the report.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/dogfood-transcript-writer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/dogfood-transcript-writer.test.ts` passes, including the 3 pre-existing cases UNCHANGED and the 2 new cases.
    - `grep -n "taskManifestCorpora = \[\]" scripts/dogfood/transcript-writer.mjs` confirms the default.
    - `grep -n "const manifest = loadTaskManifest" scripts/dogfood/dogfood-run.mjs` confirms the return value is now captured.
    - `npx vitest run test/unit/tools/dogfood-run-spawn-options.test.ts` still passes unmodified (this task must not break `buildDogfoodChildOptions`/`computeProxyExitCode` coverage).
  </acceptance_criteria>
  <done>run-report.json's schema additively gains taskManifestCorpora, populated correctly by dogfood-run.mjs from the already-loaded task manifest, with full backward compatibility proven by unmodified pre-existing tests still passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chainUploadRun — D-12 unconditional upload-chain tail step</name>
  <files>scripts/dogfood/dogfood-wrapup.mjs, test/unit/tools/dogfood-wrapup-upload-chain.test.ts</files>
  <read_first>
    scripts/dogfood/dogfood-wrapup.mjs lines 372-476 (the full `scriptMain` — the exact tail this task extends, after `writeFileAtomic(wrapup-report.json)` and the `summary.errors > 0` exit-code branch)
    scripts/dogfood/dogfood-run.mjs lines 226-236 (promoteCapture's best-effort, log-and-continue try/catch shape — the fail-open pattern to mirror exactly)
    scripts/dogfood/dogfood-run.mjs lines 26, 155-159 (spawn argv-array convention, never execFileSync for a potentially-slow operation)
  </read_first>
  <behavior>
    - chainUploadRun(runId, {deps}) spawns a child process invoking upload-run.mjs's CLI with runId as its argument (via `deps.spawn` when provided, else the real `spawn`), awaits its close event, and returns `{attempted:true}` regardless of the child's own exit code.
    - chainUploadRun never throws — a spawn failure (missing tsx, ENOENT) is caught and logged to stderr, returning `{attempted:false, reason:"spawn-failed"}`.
    - Calling scriptMain's tail with `summary.errors > 0` already having set `process.exitCode = 1`: after chainUploadRun runs (regardless of what it returns), `process.exitCode` is still `1` — chaining never clears or downgrades an existing non-zero exit code.
    - Calling scriptMain's tail with `summary.errors === 0`: `process.exitCode` remains `undefined`/unset after chainUploadRun runs (a successful or even a failed/no-op chain attempt never sets a non-zero exit code on its own).
  </behavior>
  <action>
    In scripts/dogfood/dogfood-wrapup.mjs, add a new exported function `chainUploadRun(runId, options = {})`: const spawnFn = options.deps?.spawn ?? spawn (spawn already imported at the top of this file? if not, add `import { spawn } from "node:child_process";` alongside the existing imports). Resolve the path to upload-run.mjs via `fileURLToPath(new URL("./upload-run.js", import.meta.url))` (both files live in the SAME scripts/dogfood/ directory — a sibling-relative resolution, not a SERVER_REPO_ROOT-based join, so this works regardless of cwd). Spawn `spawnFn("npx", ["tsx", uploadRunPath, runId], {stdio:["ignore","inherit","inherit"], shell:false})` wrapped in a Promise that resolves on the child's 'close' event and on its 'error' event alike (never rejects — an 'error' event resolves `{attempted:false, reason:"spawn-failed"}` after writing one stderr line, mirroring `dogfood-run.mjs`'s own `promoteCapture` best-effort try/catch-and-log shape at lines 226-236 verbatim in spirit). Return `{attempted:true}` on a normal close regardless of the child's exit code — this function's own return value is informational only and MUST NEVER be used by its caller to set `process.exitCode`.

    At the very end of `scriptMain`, AFTER the existing `if (summary.errors > 0) { process.exitCode = 1; }` block (so this step never executes before that check, preserving the D-12 "joined with ';' never '&&'" requirement — i.e. it must run regardless of whether that branch fired), add an unconditional call: `await chainUploadRun(runId);` wrapped in its own try/catch that only logs on catch (belt-and-suspenders — chainUploadRun itself should never throw per its own contract, but the call site does not trust that alone). Do NOT reassign `process.exitCode` anywhere in or after this new call.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/dogfood-wrapup-upload-chain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/dogfood-wrapup-upload-chain.test.ts` passes, including a case that pre-sets `process.exitCode = 1` before invoking the chain step and asserts it is STILL `1` afterward.
    - `grep -n "spawn" scripts/dogfood/dogfood-wrapup.mjs` confirms `spawn` (not `execFileSync`) is used for the chain step.
    - `npx vitest run test/unit/tools/dogfood-wrapup-filing.test.ts` still passes unmodified (this task must not break `wrapUpCapture`/`resolveWrapupPolicyKey`/`buildQueueEntryFromVerdict` coverage).
    - Manually inspect that the new `await chainUploadRun(runId);` call appears strictly AFTER the `if (summary.errors > 0)` block in `scriptMain`, never before or inside it.
  </acceptance_criteria>
  <done>dogfood-wrapup.mjs exports chainUploadRun and calls it unconditionally at the very end of scriptMain, proven never to affect the pre-existing judging-error exit code, with zero regression to existing wrapUpCapture/resolveWrapupPolicyKey tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| dogfood-wrapup.mjs's own process -> a spawned upload-run.mjs child | Internal, same-machine, same-trust-level process spawn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-07-16 | Tampering (of intent) | Upload chaining accidentally masking/overwriting a real judging-error exit code | mitigate | `chainUploadRun` is called strictly AFTER the `summary.errors > 0` exit-code branch and never reassigns `process.exitCode` itself — proven by a dedicated test |
| T-07-17 | Information Disclosure | `taskManifestCorpora` recording which internal corpus a run targeted | accept | Low sensitivity — an internal fuel-corpora.json key name, not secret content; consumed only by the local unattended judge (07-05) |
</threat_model>

<verification>
Both tasks' automated commands pass, plus the two named pre-existing regression checks (`dogfood-run-spawn-options.test.ts`, `dogfood-wrapup-filing.test.ts`) stay green.
</verification>

<success_criteria>
- `run-report.json` additively records `taskManifestCorpora` with zero backward-compatibility breakage.
- `dogfood-wrapup.mjs` always attempts the upload chain step, and it never interferes with the script's own judging-error exit code.
</success_criteria>

<output>
Create `.planning/phases/07-team-feedback-channel-local-wiring/07-04-SUMMARY.md` when done
</output>
