---
phase: 08-pinned-environment-distribution-thin-k8s-deployment
plan: 06
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - docs/DEPLOY-OPERATIONS.md
autonomous: true
requirements: [DEPLOY-01, TEAM-05]

must_haves:
  truths:
    - "POLICY-01's case-exact, loud-fail policy resolution passes when re-run on the cluster's own Linux/CephFS filesystem — not just the maintainer's case-insensitive Mac"
    - "Local mode (ingest-server.mjs + upload-run.mjs, fully offline from the cluster) still passes its full existing test coverage after every Phase 8 packaging change — proving DEPLOY-01's 'local mode remains a working fallback' criterion holds at PHASE END, not just when Plan 08-03 first proved it"
    - "A real, pushed git tag exists whose checked-out state contains TEAM-01 through TEAM-05 and DEPLOY-01's own artifacts — v1.0 (already pushed) predates all of Phase 6-9's work and is therefore NOT sufficient for the installer to pin (D-11)"
    - "Plan actions cite D-11 for traceability (grep -c 'D-[0-9][0-9]' .planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-06-PLAN.md returns >= 1)"
  artifacts:
    - path: "docs/DEPLOY-OPERATIONS.md"
      provides: "Final section: POLICY-01 cluster re-verify command + result, local-mode regression re-run result, the cut tag's name and commit"
  key_links:
    - from: "docs/DEPLOY-OPERATIONS.md"
      to: "test/unit/tools/oracle-orcl-02.test.ts"
      via: "documented: kubectl exec deployment/agda-mcp-ingest -n llm-gateway -- npx vitest run test/unit/tools/oracle-orcl-02.test.ts -t \"policy resolution is case-exact and loud\""
      pattern: "oracle-orcl-02\\.test\\.ts"
---

<objective>
Close DEPLOY-01's third acceptance criterion (POLICY-01 re-verified on the cluster; local mode confirmed still working) and TEAM-05's version-pinning dependency (D-11: cut and push a git tag the installer can actually reference) — the final acceptance sweep of the phase.

Purpose: this is the phase's closeout. Every prior plan's work gets its last cross-check here, and the installer this phase built finally has something real to pin.
Output: a passing cluster-side POLICY-01 test run, a passing local-mode regression re-run, and a pushed `v1.1` git tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-CONTEXT.md

@test/unit/tools/oracle-orcl-02.test.ts
@docs/DEPLOY-OPERATIONS.md

<interfaces>
<!-- The exact test this plan re-runs on the cluster — verified present and ungated (no RUN_AGDA_INTEGRATION, no Agda subprocess) in Phase 6's own 06-01-SUMMARY.md. -->

`test/unit/tools/oracle-orcl-02.test.ts` contains `describe("policy resolution is case-exact and loud (POLICY-01)")` with 6 pure filesystem/string-comparison tests (A-F) — including the exact real CHG case mismatch (`Codex-Homotopy-Group` vs `codex-homotopy-group.json`). These tests already proved themselves on `ubuntu-latest` CI (case-sensitive ext4) in Phase 6; this plan re-runs them specifically INSIDE the deployed pod (also Linux, also case-sensitive, but a genuinely different filesystem — CephFS via the PVC mount does not affect this test, since the policy files under test live in the image's own `/app/scripts/data/oracle-policy/` layer, not on the PVC — confirming this distinction in the runbook is part of this task's job).

git tag state verified 2026-07-04 during this phase's planning: `v1.0` IS already pushed to `origin` (`https://github.com/cliu238/agda-mcp-server.git`) — `git describe --tags` on the phase-planning-time HEAD showed `v1.0-165-gf87ab4e`, i.e. 165 commits past v1.0, which is ALL of Phase 6 (POLICY-01), Phase 7 (the entire team channel), and Phase 9 (debt sweep). `v1.0` therefore cannot be what TEAM-05's installer pins — a teammate checking it out would get a server with none of `scripts/team/*.mjs`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POLICY-01 case-sensitivity re-verify on the cluster</name>
  <files>docs/DEPLOY-OPERATIONS.md</files>
  <read_first>test/unit/tools/oracle-orcl-02.test.ts</read_first>
  <action>
    Via the one-shot SSH pattern, run `kubectl exec deployment/agda-mcp-ingest -n llm-gateway -- npx vitest run test/unit/tools/oracle-orcl-02.test.ts -t "policy resolution is case-exact and loud"` against the LIVE running ingest pod (its image already contains the full repo checkout + all devDependencies, per Plan 08-02's `npm ci` without `--omit=dev`). Capture the full output. Append the exact command and its full pass/fail summary line to `docs/DEPLOY-OPERATIONS.md`, explicitly noting this is the SAME test suite Phase 6 already proved on `ubuntu-latest` CI — this run's value is confirming it ALSO passes on the actual deployed cluster pod's filesystem, closing the loop the phase's own planning notes called for, not re-deriving new test coverage.
  </action>
  <verify>
    <automated>echo "See docs/DEPLOY-OPERATIONS.md for the captured kubectl exec output; local proxy for the same suite: npx vitest run test/unit/tools/oracle-orcl-02.test.ts -t \"policy resolution is case-exact and loud\""</automated>
  </verify>
  <acceptance_criteria>
    The captured cluster-pod test run shows all 6 tests in the `"policy resolution is case-exact and loud (POLICY-01)"` describe block passing, with the real CHG case-mismatch test asserting a thrown `PolicyResolutionError` (never a silent no-policy abstention). `docs/DEPLOY-OPERATIONS.md` records the full command and output.
  </acceptance_criteria>
  <done>The exact command ran against the live pod, all 6 tests passed, and the evidence is recorded in the runbook.</done>
</task>

<task type="auto">
  <name>Task 2: local-mode regression re-run + cut and push the v1.1 git tag (D-11)</name>
  <files>docs/DEPLOY-OPERATIONS.md</files>
  <read_first>docs/DEPLOY-OPERATIONS.md</read_first>
  <action>
    Re-run the existing TEAM-0x local-mode test coverage in full to confirm nothing in this phase's packaging work (Dockerfile, k8s manifests, CI/CD workflow, new `scripts/team/clone-fuel-corpora.mjs`/`install-pinned-env.*` files) regressed the ALREADY-SHIPPED local ingest/upload/cron path: `npx vitest run test/unit/tools/team-ingest-server.test.ts test/unit/tools/team-issue-key.test.ts test/unit/tools/team-archive-extract.test.ts test/unit/tools/team-cron-ingest-wrapup.test.ts test/unit/tools/dogfood-upload-run.test.ts test/integration/team/`. Then, once this passes and Task 1 has passed: cut the phase-closing tag. Verify `git status` is clean and the current commit is the intended final Phase 8 commit; run `git tag -a v1.1 -m "v1.1 Feed the Loop: Phases 6-9 — POLICY-01 fix, team feedback channel, pinned-environment distribution + k8s deployment, residual debt sweep"` then `git push origin v1.1`. This is a DIFFERENT, NEWER tag than `v1.0` (per this plan's own `<interfaces>` verification that `v1.0` predates all of Phases 6-9) — D-11's requirement ("a real git tag must be cut and pushed for the installer to pin") is satisfied by `v1.1`, not by the already-pushed-but-stale `v1.0`. Append the tag name, the commit SHA it points at, and the local-mode regression suite's pass count to `docs/DEPLOY-OPERATIONS.md`'s final section.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/team-ingest-server.test.ts test/unit/tools/team-issue-key.test.ts test/unit/tools/team-archive-extract.test.ts test/unit/tools/team-cron-ingest-wrapup.test.ts test/unit/tools/dogfood-upload-run.test.ts test/integration/team/</automated>
  </verify>
  <acceptance_criteria>
    The regression suite exits 0 with zero failures. `git ls-remote --tags origin | grep -q "refs/tags/v1.1$"` succeeds after the push. `git describe --tags v1.1` names the exact commit tagged, and that commit's tree contains `scripts/team/install-pinned-env.sh`, `Dockerfile`, and `k8s/deployment.yaml` (`git show v1.1:scripts/team/install-pinned-env.sh` and the same for the other two paths all exit 0).
  </acceptance_criteria>
  <done>Local-mode regression suite is green, `v1.1` is tagged and pushed, and its content is confirmed to include this phase's own deliverables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this machine -> origin (GitHub) | pushing a new tag is a repo-write action using this session's own already-authenticated git credential — the SAME credential every other phase's docs-commit already used, nothing new |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-19 | Tampering | `git tag`/`git push origin v1.1` | accept | Ordinary, already-authorized git-write access this session already holds (same as every other phase-completion commit in this project's history) — tagging a milestone is a normal, expected repository operation, not a new trust boundary |
</threat_model>

<verification>
Task 1's cluster-pod POLICY-01 run passes; Task 2's local-mode regression suite passes; `v1.1` exists on `origin` and its tree contains this phase's own artifacts.
</verification>

<success_criteria>
- POLICY-01's case-sensitivity fix is confirmed passing on the actual deployed cluster pod, not just CI.
- Local mode is confirmed still fully working at phase end.
- `v1.1` is cut, pushed, and verifiably contains TEAM-05 and DEPLOY-01's own deliverables — the installer's `git checkout "$(git tag --sort=-v:refname | head -1)"` step (documented in `docs/TEAM-ONBOARDING.md`) now resolves to a tag that actually works.
</success_criteria>

<output>
Create `.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-06-SUMMARY.md` when done
</output>
