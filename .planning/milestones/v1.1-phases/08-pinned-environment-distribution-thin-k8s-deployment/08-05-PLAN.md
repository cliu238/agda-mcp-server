---
phase: 08-pinned-environment-distribution-thin-k8s-deployment
plan: 05
type: execute
wave: 3
depends_on: ["08-04"]
files_modified:
  - docs/DEPLOY-OPERATIONS.md
autonomous: true
requirements: [DEPLOY-01]

must_haves:
  truths:
    - "A real archive uploaded from off-cluster (this machine) via the real Bearer key and the real https://dev.sites.idies.jhu.edu/agda-mcp/ingest URL lands durably on the Ceph PVC at the documented person/date/runId layout"
    - "Manually triggering the cron-judge CronJob processes the uploaded archive and either files a fix-queue entry or an honest not-a-candidate/abstention outcome — never crashes, never hangs past activeDeadlineSeconds"
    - "After the cron run, the baked-in repo checkout inside the pod shows ZERO git working-tree changes — proving D-09's write-back-disabled property held in practice, not just in manifest configuration"
    - "Plan actions cite D-08, D-09 for traceability (grep -c 'D-[0-9][0-9]' .planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-05-PLAN.md returns >= 2)"
  artifacts:
    - path: "docs/DEPLOY-OPERATIONS.md"
      provides: "Extended with the real verification commands proven in this plan: real upload, manual cron trigger, PVC inspection, write-back-disabled confirmation"
  key_links:
    - from: "docs/DEPLOY-OPERATIONS.md"
      to: "scripts/team/cron-ingest-wrapup.mjs"
      via: "documented: kubectl create job --from=cronjob/agda-mcp-cron-judge -n llm-gateway <manual-run-name>"
      pattern: "create job --from=cronjob"
---

<objective>
Prove DEPLOY-01's functional core on the REAL, now-deployed cluster (Plan 08-04 completed the first deploy): a real teammate-shaped upload reaches the ingest endpoint and lands on the Ceph PVC, and the cron-judge CronJob processes it end to end without ever needing (or exercising) a repo-write git credential.

Purpose: Plan 08-04 proved the SERVICE is up (healthz 200). This plan proves the SERVICE WORKS — the two behaviors DEPLOY-01 actually exists for.
Output: a real archive on the cluster PVC, a completed manual cron-judge Job run, and `docs/DEPLOY-OPERATIONS.md` extended with the exact commands used as a repeatable verification runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-CONTEXT.md
@.claude/skills/agda-mcp-k8s-deploy/SKILL.md

@scripts/dogfood/upload-run.mjs
@scripts/team/cron-ingest-wrapup.mjs
@k8s/cronjob.yaml
@docs/DEPLOY-OPERATIONS.md

<interfaces>
<!-- Exact remote commands this plan runs, via the one-shot SSH pattern from SKILL.md -->

Manual cron trigger (CronJobs are not directly runnable; a one-off Job clones the CronJob's own template):
```
kubectl create job --from=cronjob/agda-mcp-cron-judge agda-mcp-cron-judge-manual-verify -n llm-gateway
kubectl wait --for=condition=complete --timeout=1800s job/agda-mcp-cron-judge-manual-verify -n llm-gateway
kubectl logs job/agda-mcp-cron-judge-manual-verify -n llm-gateway
```

Write-back-disabled proof (run inside the SAME pod the manual Job created, before it terminates, or via `kubectl exec` on the still-Completed pod if the cluster retains it):
```
kubectl exec job/agda-mcp-cron-judge-manual-verify -n llm-gateway -- git -C /app status --short
```
Expected output: empty (zero lines) — `cron-ingest-wrapup.mjs`'s `--queue-path /data/cluster-queue/fix-queue.json` never touches any file under `/app` (the repo checkout baked into the image), so `git status` inside the pod must show no changes at all.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: real upload through the deployed cluster endpoint + PVC landing verification</name>
  <files>docs/DEPLOY-OPERATIONS.md</files>
  <read_first>scripts/dogfood/upload-run.mjs, scripts/team/issue-key.mjs, docs/DEPLOY-OPERATIONS.md</read_first>
  <action>
    From this machine (off-cluster, simulating a real teammate): if a usable Bearer key from Plan 08-04's `agda-mcp-team-keys` bootstrap is not already on hand, issue one via `scripts/team/issue-key.mjs issue <person>` locally and sync it to the cluster Secret per `docs/DEPLOY-OPERATIONS.md`'s Task-2-documented rotation procedure. Build a minimal, real `run-report.json` fixture (same minimal shape used by Plan 08-03's acceptance test — `stagedCaptures: []` is acceptable; this task is proving TRANSPORT, not proving a specific capture's verdict) and call `runUploadForRun` (imported directly, per the established DI-free "call the real exported function" pattern from Plan 08-03) with `key: <the real key>` and `url: "https://dev.sites.idies.jhu.edu/agda-mcp/ingest"`. Then, via the one-shot SSH pattern, `kubectl exec deployment/agda-mcp-ingest -n llm-gateway -- find /data/team-uploads -name '*.tar.gz'` to confirm the archive landed at the expected `<person>/<date>/<runId>.tar.gz` path on the real Ceph-backed PVC (D-07/D-10's storage design proven live, not just in manifest text). Append the exact commands used (with the person slug and expected path pattern, key value redacted) to `docs/DEPLOY-OPERATIONS.md`'s "Verification" section.
  </action>
  <verify>
    <automated>curl -sf https://dev.sites.idies.jhu.edu/agda-mcp/healthz | grep -q "^ok$"</automated>
  </verify>
  <acceptance_criteria>
    `runUploadForRun` returns exactly `{ attempted: true, uploaded: true }` against the real cluster URL. The remote `find /data/team-uploads -name '*.tar.gz'` output includes a path matching `<person>/<today's-date>/<runId>.tar.gz`. `docs/DEPLOY-OPERATIONS.md` contains the redacted command sequence used.
  </acceptance_criteria>
  <done>A real archive exists on the live PVC, reached via the public ingress URL and a real issued key; the runbook records how to repeat this check.</done>
</task>

<task type="auto">
  <name>Task 2: manual cron-judge trigger + PVC queue-accumulation + write-back-disabled proof (D-08, D-09)</name>
  <files>docs/DEPLOY-OPERATIONS.md</files>
  <read_first>k8s/cronjob.yaml, scripts/team/cron-ingest-wrapup.mjs</read_first>
  <action>
    Via the one-shot SSH pattern, run the manual-trigger commands from this plan's `<interfaces>` block against the real `agda-mcp-cron-judge` CronJob (D-08: it shares the SAME image + PVC as the ingest Deployment — no separate manifest to apply). Wait for the Job to reach `Completed` (bounded by `activeDeadlineSeconds: 21600` on the CronJob template — a real cold ORCL-01 replay against Task 1's uploaded archive may legitimately take a long time if `stagedCaptures` is non-empty; Task 1's own fixture used an EMPTY `stagedCaptures` array specifically so this manual verification run completes quickly rather than waiting hours). Inspect the Job's logs for the `[cron-ingest-wrapup] processed...` summary line. Then run the write-back-disabled proof command from `<interfaces>` and confirm empty output. Finally, `kubectl exec` (or a fresh throwaway pod using the same image+PVC mount) to `find /data/cluster-queue` and confirm `fix-queue.json` exists at that PVC path (D-09: the queue accumulates there, durably, independent of pod lifecycle) — read its content and confirm it is valid JSON (an array, possibly empty if Task 1's zero-capture archive produced zero filing candidates, which is the expected, honest outcome for an empty-`stagedCaptures` fixture). Append the full command sequence and its literal output to `docs/DEPLOY-OPERATIONS.md`'s "Verification" section, completing the runbook Plan 08-04 started.
  </action>
  <verify>
    <automated>test -f docs/DEPLOY-OPERATIONS.md && grep -q "create job --from=cronjob" docs/DEPLOY-OPERATIONS.md && grep -q "git -C /app status --short" docs/DEPLOY-OPERATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    The manual Job reaches `Completed` (not `Failed`, not stuck `Active` past `activeDeadlineSeconds`). `git -C /app status --short` executed inside the Job's pod prints nothing. `/data/cluster-queue/fix-queue.json` exists and parses as valid JSON. `docs/DEPLOY-OPERATIONS.md` records all of the above as a repeatable runbook section.
  </acceptance_criteria>
  <done>A real cron-judge run completed against real cluster infrastructure, the queue accumulated on the PVC exactly as D-09 specifies, and zero git working-tree changes occurred inside the pod's baked-in checkout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this machine -> public ingress -> ingest pod | the SAME trust boundary Plan 08-02 already modeled; this plan exercises it with real traffic instead of a dry-run |
| cron-judge pod -> Ceph PVC | the archive extracted and judged here is REAL data (even if a minimal fixture), landing on the SAME PVC teammates' real logs will later occupy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Tampering | manual verification Job's fixture archive | accept | A minimal, non-sensitive synthetic fixture (empty `stagedCaptures`) — deliberately not a real teammate's proof session, so this plan's own verification traffic never mixes real colleague data with test data on the shared PVC |
| T-08-18 | Repudiation | absence of a git write-back credential | mitigate | This plan's own Task 2 is the FIRST live confirmation (not just static manifest review) that the cron-judge pod can complete a full run with zero git working-tree mutation — closing the loop D-09/T-08-09 (Plan 08-02) opened in configuration only |
</threat_model>

<verification>
Both tasks' acceptance criteria hold simultaneously against the live cluster; `docs/DEPLOY-OPERATIONS.md` contains a complete, copy-pasteable verification runbook a future maintainer can re-run after any redeploy.
</verification>

<success_criteria>
- A real archive, uploaded through the public ingress URL with a real key, is confirmed present on the Ceph PVC.
- A real manual cron-judge run completes, accumulates its queue on the PVC (never in the repo checkout), and leaves the pod's git working tree clean.
</success_criteria>

<output>
Create `.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-05-SUMMARY.md` when done
</output>
