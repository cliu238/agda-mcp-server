---
milestone: v1.0
audited: 2026-07-03T05:10:00Z
status: tech_debt
scores:
  requirements: 19/19
  phases: 6/6
  integration: 25/27
  flows: 5/5
nyquist:
  compliant_phases: 6
  partial_phases: 0
  missing_phases: 0
  overall: COMPLIANT
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 01-capture-foundation
    items:
      - "WR-01 durability edge: resetRecordedActions() runs after drain but before writeFileAtomic — a write failure (disk full/permissions) in that window loses the drained action log with no artifact (src/tools/register-capture-session.ts:120 vs :196)"
      - "Gap plans 01-06/01-07 never executed; blocking content superseded (CR-03 closed by Phase 3 commit 0a79b73, WR-02 by 04-02) but residual minor items remain open: WR-08 stale mergedArgv, WR-12 test-fixture pollution (01-06); CR-01 path traversal + CR-02 false-PASS in scripts/verify-cold-replay.mjs (01-07)"
      - "scripts/verify-cold-replay.mjs is an orphaned, unguarded stopgap (zero importers; superseded by ORCL-01's materializeCaptureEnvironment which does guard traversal) — harden per 01-07 or delete (integration W3)"
  - phase: 04-triage-fix-queue
    items:
      - "Integration W1: scripts/promote-capture.mjs:82 still writes .agda-mcp/captures/index.json and its header claims readDedupIndex() reads it, but Phase 4 repointed readDedupIndex() to test/fixtures/fix-queue.json — zero readers remain; the proxy's auto-persist promoteCapture call records into a dead-ended file; header comment is stale (CAP-02 partial)"
  - phase: 05-dogfooding-orchestration-fuel
    items:
      - "Integration W2 (most substantive): ORCL-02 policy resolution has no runtime passthrough — runOracle/judgeOrcl02 derive the policy key only from the corpus root's .agda-lib name; fuel-corpora.json's policyKey column is test-validated but never consumed at runtime. If a pinned corpus's .agda-lib name mismatches its policy filename (plausible for codex-homotopy-group / autoformalizing-hopf, both built on agda-unimath), ORCL-02 degrades to no-policy abstention and the 'cheat findings file unconditionally' path silently never fires for that corpus (fails loudly as NOT trueGreen, but never auto-files). Fix: add --policy passthrough or consume policyKey from the task manifest (ORCL-02, PROC-02)"
      - "4 open Info findings from final code review (intentionally out of fix scope): IN-01 argv flag-token/run-id sanitization; IN-02 dangling-symlink risk in install-dogfood-skill.mjs; IN-04 git check-ignore exit-status ambiguity in its test; IN-05 wrapup catch handler re-dereferences staged.stagedPath (corruption-only)"
  - phase: repo-wide
    items:
      - "Integration W4: npx tsc -p tsconfig.test.json --noEmit not clean, including milestone-scope files (capture-regression-runner.ts structuredContent unknown; 8 ReplayManifest index-signature errors in oracle test files) — the .mjs ↔ .ts fixture seams have no compile-time contract enforcement (runtime contracts proven by vitest)"
      - "Integration W5: dogfood-wrapup filing tests assert against a locally-defined minimal fixQueueEntrySchema surface with upsertQueueEntry mocked — a future required-field addition would pass Phase-5 tests and only surface at runtime (integration checker proved the seam holds today: 3/3 safeParse OK on real output)"
      - "No SECURITY.md in any phase while workflow.security_enforcement=true — run /gsd:secure-phase retroactively (at minimum for Phase 5, whose scripts spawn processes and write files)"
      - "Codebase drift: 24 structural elements since last mapping — run /gsd:map-codebase to refresh planning context"
      - "8 needs-reverify CHG defect specs sit in the fix queue (v0.6.7-era measurements) — by-design queue cargo, listed for visibility; re-verify against current main via the now-shipped dogfood wrap-up loop"
---

# Milestone v1.0 Audit — Agda MCP Server: Self-Improvement Loop (Loop ②)

**Status: tech_debt** — all requirements satisfied, all phases verified, all E2E flows complete, zero critical blockers; accumulated non-blocking items listed above need review.

## Definition of Done

> Reproducible scaffold for Loop ②: use it → surface a defect → capture it → fix and lock it with a regression test → use it again.

**Achieved.** The full loop is wired and live-proven: dogfood proxy session → `agda_capture_session` staged artifact → oracle triad + N-rerun anti-phantom flake gate → fix-queue filing (or flaky side-channel) → fix (03.1 flagship precedent) → from-RED regression lock → re-use with pinned fuel.

## Requirements Coverage — 19/19 satisfied

| Requirement | Phase | Traceability | SUMMARY frontmatter | VERIFICATION | Final |
|-------------|-------|--------------|---------------------|--------------|-------|
| CAP-01..CAP-05 | 1 | [x] Complete | listed | passed (re-verified 2026-07-03) | satisfied |
| ORCL-01..ORCL-03 | 2 | [x] Complete | listed | passed | satisfied |
| REPRO-01, LOCK-01..03 | 3 | [x] Complete | listed | passed | satisfied |
| FIX-01 | 03.1 | [x] Complete | listed | passed | satisfied |
| QUEUE-01..QUEUE-04 | 4 | [x] Complete | listed | passed | satisfied |
| PROC-01, PROC-02 | 5 | [x] Complete | listed | passed | satisfied |

No orphaned requirements (every REQ-ID appears in its phase's VERIFICATION.md). Phase 1's VERIFICATION was re-verified during this audit: its single blocking gap (CR-03 silent staged-artifact overwrite) was closed by Phase 3 (`0a79b73` + `af34cc7`, regression-locked at `test/unit/tools/register-capture-session.test.ts:294`), status flipped to passed with a dated addendum.

## Phase Verifications — 6/6 passed

| Phase | Status | Note |
|-------|--------|------|
| 1 Capture Foundation | passed | re-verified 2026-07-03 (blocker closed by Phase 3) |
| 2 Oracle Triad | passed | |
| 3 Regression Lock Pipeline | passed | |
| 03.1 Flagship false-green fix | passed | |
| 4 Triage / Fix Queue | passed | |
| 5 Dogfooding Orchestration + Fuel | passed | 20/20 must-haves; post-verification code-review fix chain ended clean |

## Cross-Phase Integration — 25/27 wired, 5/5 flows complete (checker verdict: PASS)

Live-proven flows: (1) use→capture via real proxy + real server; (2) capture→judge via oracle triad + flake gate (flagship N-reruns deterministic); (3) judge→file with strict ORCL-02-cheat-first precedence and flaky side-channel; (4) file→fix→lock (flagship queue entry `locked` with matrix backlink, runner green live); (5) runbook discoverability (tracked SKILL.md + tested symlink installer).

Two warning-degraded links: W1 (orphaned `captures/index.json` write-side) and W2 (no runtime policy passthrough for ORCL-02) — both in the tech-debt list with fix directions; neither breaks a flow today.

## Nyquist Compliance — COMPLIANT (6/6 phases have VALIDATION.md, all `nyquist_compliant: true`)

## Verdict

Ship it as v1.0 with the tech-debt list tracked. The highest-value follow-ups, in order: W2 (policy passthrough — protects the cheat-filing guarantee on the two flagship corpora), W1 (retire or repoint the orphaned index write), delete-or-harden `verify-cold-replay.mjs`, `/gsd:secure-phase 5`, WR-01 reset-after-write.
