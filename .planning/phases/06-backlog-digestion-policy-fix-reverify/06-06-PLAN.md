---
phase: 06-backlog-digestion-policy-fix-reverify
plan: 06
type: execute
wave: 4
depends_on: ["06-04", "06-05"]
files_modified:
  - test/fixtures/fix-queue.json
  - .planning/research/RT-REVERIFY.md
autonomous: true
requirements: [REVERIFY-02, REVERIFY-01]
must_haves:
  truths:
    - "Every one of the 4 named confirmed entries (5abecc959e43fef3, bfcba437f5426fd6, eb7439cb3ed9d6b9, fdc90bfde12fb938) is status locked with closedAt set and notes naming its regression-lock test(s) (success criterion 4)"
    - "Every RT-confirmed entry ends locked, rejected, or explicitly DEFERRED with a recorded reason — none silently stalled (D-10)"
    - "Zero entries remain needsReverify: true and zero confirmed entries sit in an undocumented non-terminal state — the phase's two REVERIFY criteria are mechanically greppable"
    - "Where the Phase-3 emit-regression pipeline structurally cannot apply (non-load-family defects), the queue notes record the alternative lock mechanism explicitly (from-RED vitest tests) — the D-11 deviation is documented per-entry, never silent"
  artifacts:
    - path: "test/fixtures/fix-queue.json"
      provides: "Terminal ledger: named-4 locked; RT confirms dispositioned; schema-valid"
    - path: ".planning/research/RT-REVERIFY.md"
      provides: "Closing acceptance section: final per-entry disposition table for all 13 queue rows touched by Phase 6"
  key_links:
    - from: "test/fixtures/fix-queue.json"
      to: "06-04/06-05 regression tests"
      via: "notes lines naming file :: test-name lock references"
      pattern: "Regression lock:"
    - from: "test/fixtures/fix-queue.json"
      to: "test/fixtures/capture-regression-matrix.json"
      via: "matrixEntryId backlink IF any load-family RT confirm was locked through emit-regression (conditional)"
      pattern: "matrixEntryId"
---

<objective>
Close the loop's ledger: flip the four fixed named entries to `locked`, disposition every RT-confirmed entry (fix small in-scope ones, or explicitly re-triage large-redesign ones with recorded reasons per D-10), and run the phase's final acceptance sweep so success criteria 3 and 4 are both mechanically checkable.

Lock-mechanism rule (the recorded D-11 adaptation, decided at planning time with evidence): D-11 names the Phase-3 emit-regression pipeline "per the flagship precedent". That pipeline's own contracts make it applicable ONLY to load-family false-green candidates — `judgeRefusal` refuses any verdict without an ORCL-01 `server-false-green-candidate` + `coldTuple`, and `replayCaptureRegressionEntry` drives `entry.tool` solely with a `{file}` argument asserting the load tuple (classification/goalCount/invisibleGoalCount/hasHoles/errorCategories). The four named defects are non-load-family tool-envelope defects, so forcing them through the emitter would require either fabricating a cold expected value (exactly what Phase-3's D-06 anti-golden-master refusal forbids) or rebuilding the matrix machinery (new machinery, against this phase's "cargo, not machinery" scope). Therefore: (a) non-load-family entries lock via their from-RED vitest regression tests, matrixEntryId stays null (the frozen schema enforces only closedAt for locked), and the notes record BOTH the test references and this mechanism rationale; (b) IF any RT re-verification confirmed a load-family false-green candidate with a deterministic wrapup-filed capture, THAT entry locks through the full emit-regression flow (from-RED matrix entry + matrixEntryId backlink), honoring D-11's literal mechanism where it applies.

Purpose: REVERIFY-02 completion + REVERIFY-01 final assertion — "every real proof session reliably converts into a stronger server" gets its first sustained ledger proof.
Output: terminal fix-queue.json states + RT-REVERIFY.md closing table (+ conditional matrix entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-CONTEXT.md
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-03-SUMMARY.md
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-04-SUMMARY.md
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-05-SUMMARY.md

<interfaces>
<!-- Ledger contracts. -->

Frozen queue schema (test/fixtures/fix-queue.ts): statuses new|triaged|fixing|locked|rejected; locked/rejected REQUIRE closedAt non-null (superRefine); rejected REQUIRES rejectedReason; matrixEntryId nullable; needsReverify is a boolean annotation, never a status. Validation command after every edit: `npx vitest run test/unit/fixtures/fix-queue.test.ts`.

Priority order (scripts/queue/priority.mjs): sortByPriority over { defectKind, recurrence } — false-green(0) > crash(1) > wrong-result(2) > missing-feature(3), ties by recurrence DESC. Recompute at execution time over the ACTUAL post-reverify queue (recurrences may have bumped): `npx tsx -e` is unreliable for TS imports — instead write a 5-line gitignored tmp/sort-queue.mjs importing sortByPriority from ../scripts/queue/priority.mjs and the queue JSON, print fingerprint+kind+recurrence order, run with npx tsx.

emit-regression flow (ONLY if a load-family RT confirm exists): `npx tsx scripts/emit-regression.mjs <staged-artifact.json> --id <slug> --issue <n> --fixture-dir <dir> --tool <agda_load|agda_load_no_metas|agda_typecheck>` — refuses unless the FRESH oracle run shows server-false-green-candidate; writes a status "red" matrix entry + fixtures; after the fix lands the replay flips green and the matrix status is manually edited to "locked"; then the queue entry gets matrixEntryId = <slug>, status locked, closedAt.

Lock-note format for non-load-family entries (exact prefix for grep-ability):
"Regression lock: <test-file> :: <test-name>[; more]. Mechanism: from-RED vitest test (non-load-family defect — emit-regression/judgeRefusal + replayCaptureRegressionEntry support only load-family ORCL-01 false-green candidates; matrixEntryId intentionally null). Fixed in Phase 6 wave 3."

Deferral-note format (exact prefix for grep-ability):
"DEFERRED (Phase 6): <reason — e.g. requires the UX report's response-schema rework, out of D-10 appetite>; remains triaged for a later milestone's queue review."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the four named fixed entries to locked with recorded lock references</name>
  <files>test/fixtures/fix-queue.json</files>
  <read_first>test/fixtures/fix-queue.json (current state of all 13+ rows after waves 1-3), test/fixtures/fix-queue.ts (terminal-status superRefine), .planning/phases/06-backlog-digestion-policy-fix-reverify/06-04-SUMMARY.md and 06-05-SUMMARY.md (the file :: test-name lock references each fix plan was required to emit), scripts/queue/priority.mjs (ordering table)</read_first>
  <action>
    For each of the four named entries, in priority order (recompute via sortByPriority; expected: 5abecc959e43fef3 and bfcba437f5426fd6 [false-green band, recurrence-tied], then fdc90bfde12fb938 [wrong-result], then eb7439cb3ed9d6b9 [missing-feature]):
    - Set status "locked", closedAt to now-ISO.
    - Append the lock note per the interfaces "Regression lock:" format, filling the exact test references from the 06-04/06-05 summaries (agda_auto: agent-ux.test.ts throw cases + goal-tools-give.test.ts envelope case; agda_give: goal-operations-give.test.ts + goal-tools-give.test.ts; agda_proof_status: analysis-tools.test.ts cases; agda_search_definitions: file-tools.test.ts cases).
    - RT4 entry 004d161b839ce725 (relatedFingerprint-linked to 5abe, resolved together per D-08): if 06-02 confirmed it, flip it to locked with the SAME lock references + a cross-ref line to 5abecc959e43fef3; if it was rejected as unreproducible in 06-02, leave that terminal state untouched.
    - Keep every fingerprint, createdAt, recurrence, relatedFingerprint value untouched; matrixEntryId stays null for these rows (mechanism note explains why).
    Validate with `npx vitest run test/unit/fixtures/fix-queue.test.ts` after each entry's edit, not just at the end.
  </action>
  <verify>
    <automated>npx vitest run test/unit/fixtures/fix-queue.test.ts && test "$(grep -c '"status": "locked"' test/fixtures/fix-queue.json)" -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - Entries 5abecc959e43fef3, bfcba437f5426fd6, eb7439cb3ed9d6b9, fdc90bfde12fb938 all show `"status": "locked"` with non-null closedAt (flagship e6f0c1169032b9d5 makes the locked count >= 5).
    - `grep -c "Regression lock:" test/fixtures/fix-queue.json` >= 4 — every newly-locked row names its lock tests and mechanism.
    - `npx vitest run test/unit/fixtures/fix-queue.test.ts` passes (superRefine holds).
    - The named regression tests actually exist and pass: `npx vitest run test/unit/agda/agent-ux.test.ts test/unit/agda/goal-operations-give.test.ts test/unit/tools/goal-tools-give.test.ts test/unit/tools/analysis-tools.test.ts test/unit/tools/file-tools.test.ts` exits 0 (a lock note may never reference a red or missing test).
  </acceptance_criteria>
  <done>The named-4 (plus RT4 if confirmed) are locked with auditable, passing lock references — success criterion 4's mandatory core.</done>
</task>

<task type="auto">
  <name>Task 2: Disposition remaining RT confirms, conditional emit-regression lock, and phase acceptance sweep</name>
  <files>test/fixtures/fix-queue.json, .planning/research/RT-REVERIFY.md</files>
  <read_first>.planning/phases/06-backlog-digestion-policy-fix-reverify/06-03-SUMMARY.md (which RTs confirmed + any wrapup-auto-filed cargo flagged there), test/fixtures/fix-queue.json (post-Task-1 state), scripts/emit-regression.mjs (CLI flags + judgeRefusal contract, lines 52-77 and 386-491 — ONLY needed on the conditional load-family path), .planning/phases/06-backlog-digestion-policy-fix-reverify/06-CONTEXT.md (D-10 appetite text + deferred-ideas list)</read_first>
  <action>
    1. Enumerate every remaining confirmed non-terminal entry (status triaged/fixing, from RT1-RT3/RT5-RT8 confirms and any genuine wrapup-filed new cargo 06-02/06-03 flagged). Order them with sortByPriority.
    2. Per entry, apply D-10's appetite rule mechanically: FIX NOW only if the defect has an evident <= 2-src-file fix with an obvious from-RED unit test AND doing so keeps this plan within its context budget (if fixed: implement fix + RED-first test following the wave-3 plans' conventions — 500-line ceiling, layering, sandbox patterns — then lock the entry per Task 1's note format, listing the touched files in this plan's summary as a files_modified deviation). Otherwise DEFER: keep status triaged, append the "DEFERRED (Phase 6):" note with the concrete reason (the UX report's response-schema rework direction is the pre-approved deferral class for RT6-style five-state asks; timeout-state taxonomy for RT7-style asks likewise if confirmed). Every entry MUST end locked, rejected, or DEFERRED-with-reason — none silently stalled.
    3. Conditional emit-regression path: if (and only if) 06-03 flagged a deterministic LOAD-FAMILY false-green candidate with a staged capture, run the full flow: emit-regression (from-RED matrix entry, --id rt-derived slug, --tool the load-family tool, --fixture-dir a new FixtureDeps subdir) BEFORE its fix, then fix, then flip the matrix entry to locked and set the queue row's matrixEntryId + locked + closedAt. If no such candidate exists (the expected case — CHG-REVERIFY found the flagship class already fixed), record in RT-REVERIFY.md's closing section: "No load-family false-green candidate was confirmed; the emit-regression path had no applicable cargo this phase."
    4. Acceptance sweep (the phase's own gates, all must pass): `grep -c '"needsReverify": true' test/fixtures/fix-queue.json` == 0; `grep -c '"status": "new"' test/fixtures/fix-queue.json` == 0 (every entry left new was dispositioned); every `"status": "triaged"` row's notes contain "DEFERRED (Phase 6):" (assert via jq or a small tmp script: for each triaged row, notes match the prefix); every locked/rejected row has closedAt. Then the full gates: `npm run build`, `npx vitest run` (entire suite), `npx tsc -p tsconfig.json --noEmit`.
    5. Append the closing section to .planning/research/RT-REVERIFY.md: a final disposition table (fingerprint | title-short | end state | lock/deferral reference) covering every row Phase 6 touched, plus the acceptance-gate outputs (grep counts, suite result) and the D-11 mechanism note (copy the objective's lock-mechanism rule paragraph in condensed form).
  </action>
  <verify>
    <automated>test "$(grep -c '"needsReverify": true' test/fixtures/fix-queue.json)" = "0" && test "$(grep -c '"status": "new"' test/fixtures/fix-queue.json)" = "0" && npx vitest run test/unit/fixtures/fix-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Zero `"needsReverify": true` and zero `"status": "new"` rows remain.
    - Every remaining `"status": "triaged"` row's notes contain the literal "DEFERRED (Phase 6):" prefix (checked row-by-row; report the count).
    - Every locked/rejected row has non-null closedAt (schema test green proves it).
    - Full `npx vitest run` exits 0 and `npm run build` exits 0 — the loop's ledger closes on a green tree.
    - RT-REVERIFY.md ends with the disposition table covering >= 12 rows (13-entry queue minus any rows untouched by the phase, each accounted for) and the emit-regression applicability statement.
    - If the conditional path fired: capture-regression-matrix.json gained exactly one entry whose id the corresponding queue row's matrixEntryId references, and `npx vitest run test/integration/mcp/capture-regression.test.ts` passes with it (run only in that branch; requires local agda).
  </acceptance_criteria>
  <done>Every pre-v1.1 backlog entry sits in a definitive, evidenced, schema-valid end state; the phase's four success criteria are all mechanically demonstrated on a green tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor edits → fix-queue.json + capture-regression-matrix.json SSOTs | terminal-state ledger writes must stay schema-valid and auditable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18 | Tampering | ledger hand-edits | mitigate | zod schema test after every entry edit; git diff audit trail; lock notes must reference PASSING tests (asserted in acceptance) |
| T-06-19 | Repudiation | "locked" claims without a real lock | mitigate | acceptance runs the referenced test files in the same sweep — a note naming a missing/red test fails the plan |
| T-06-20 | Tampering | golden-mastering a wrong expected value via forced emit-regression | mitigate | emit-regression path is conditional on a genuine ORCL-01 candidate; judgeRefusal's refusal logic is never bypassed with --force in this plan |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies installed |
</threat_model>

<verification>
- The three grep gates (needsReverify 0, new 0, DEFERRED-prefix on all triaged) + schema test + full suite + build all green.
</verification>

<success_criteria>
- Success criterion 3 (re-asserted terminal): zero needsReverify.
- Success criterion 4: every confirmed live defect locked or explicitly re-triaged with a recorded reason — none silently stalled; the named-4 all locked.
</success_criteria>

<output>
Create `.planning/phases/06-backlog-digestion-policy-fix-reverify/06-06-SUMMARY.md` when done. If Task 2 fixed additional RT-confirmed defects, the summary MUST list the extra files modified (deviation record) and their lock test references.
</output>
