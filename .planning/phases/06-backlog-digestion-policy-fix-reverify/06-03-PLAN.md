---
phase: 06-backlog-digestion-policy-fix-reverify
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - test/fixtures/fix-queue.json
  - .planning/research/RT-REVERIFY.md
autonomous: true
requirements: [REVERIFY-01]
must_haves:
  truths:
    - "RT5, RT6, RT7, RT8 each have a definitive verdict measured against current main through the shipped pipeline (D-06)"
    - "After this plan, ZERO fix-queue entries remain needsReverify: true — success criterion 3's mechanical gate"
    - "Every flipped entry satisfies the frozen schema: confirmed = triaged + needsReverify false + evidence; unreproducible = rejected + cannot-reproduce + closedAt + evidence (D-07)"
    - "RT6's relatedFingerprint link to the agda_proof_status entry (fdc90bfde12fb938) and RT8's link to the flagship (e6f0c1169032b9d5) are honored: evidence cross-referenced, no duplicate rows (D-08)"
  artifacts:
    - path: ".planning/research/RT-REVERIFY.md"
      provides: "Completed 8-row summary table + RT5-RT8 verdict sections"
      min_lines: 120
    - path: "test/fixtures/fix-queue.json"
      provides: "All 8 RT entries in definitive end states"
  key_links:
    - from: "test/fixtures/fix-queue.json"
      to: ".planning/research/RT-REVERIFY.md"
      via: "notes citations with run-ids"
      pattern: "RT-REVERIFY"
---

<objective>
Complete REVERIFY-01: re-verify RT5-RT8 through the shipped pipeline (same rig plan 06-02 stood up), flip the last four `needsReverify` entries to definitive states, and finish the committed evidence report. After this plan the phase's success criterion 3 is fully checkable: zero entries remain `needsReverify`.

Purpose: REVERIFY-01 (second half) + the D-12 gate — waves 3+ (fixes) may only start once all 8 pre-fix measurements exist.
Output: fix-queue.json (final RT transitions) + completed .planning/research/RT-REVERIFY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-CONTEXT.md
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-02-SUMMARY.md
@.agents/skills/agda-dogfooding/SKILL.md

<interfaces>
<!-- RT7's timeout lever and RT8's staleness fixtures — the two session designs that differ from 06-02's shapes. -->

Timeout configuration (src/session/command-completion.ts):
- `AGDA_MCP_COMMAND_TIMEOUT_MS` (default 120000) — per-command transport timeout; positive-int env var read at call time. Exporting it in the DRIVER's env when spawning the proxy propagates to the child server (proxy child env = { ...process.env, ..., AGDA_MCP_ROOT }).
- Related: AGDA_MCP_IDLE_COMPLETION_MS (250), AGDA_MCP_POST_STATUS_IDLE_MS (50), AGDA_MCP_LOAD_TERMINUS_IDLE_MS (2000).

RT8 fixture set (already committed, the flagship's own): test/fixtures/agda/FixtureDeps/TransitiveStaleness/{Main.agda, Dep.agda, Dep.broken.agda}. Module headers are `module FixtureDeps.TransitiveStaleness.Main where` etc. — the corpus copy MUST preserve the FixtureDeps/TransitiveStaleness/ directory nesting under the corpus root and pass file paths as "FixtureDeps/TransitiveStaleness/Main.agda" (mirrors replayCaptureRegressionEntry's copy shape, which is proven green in CI).

RT8 current-state context (fix-queue entry 3306edf4c2d01c53 notes): current-main agda_load already attaches a session-regression diagnostic with previousClassification; the open question is whether agda_load_no_metas specifically still lacks the combined previous+new report. CHG-REVERIFY.md's warm-inject observations (previousClassification, staleBeforeLoad, session-regression diagnostic) are the comparison baseline.

previousClassification schema fields exist at src/session/tool-presentation.ts line 23 (previousClassification, previousLoadedAtMs — nullable/optional).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify RT5 (mutation-tool reload failure) + RT6 (five-state conflation)</name>
  <files>test/fixtures/fix-queue.json, .planning/research/RT-REVERIFY.md</files>
  <read_first>.planning/phases/06-backlog-digestion-policy-fix-reverify/06-02-SUMMARY.md (rig reuse: driver path, manifest recipe, queue-edit recipe), test/fixtures/fix-queue.json (entries a0ae86c7deb9754e RT5, ad2b6d31f58f1759 RT6, and fdc90bfde12fb938 for the RT6 cross-link), test/fixtures/agda/WriteGiveSimple.agda, test/fixtures/agda/MixedGoalsErrors.agda, test/fixtures/agda/MixedVisibleInvisible.agda, src/tools/goal-tools.ts lines 97-145 (agda_case_split writeToFile path via applyEditAndReload — a candidate live probe surface)</read_first>
  <action>
    RT5 session (a0ae86c7deb9754e, run-id `rt5-<date>`): corpus tmp/rt-reverify/rt5/ with WriteGiveSimple.agda. Drive a mutation tool whose post-write reload FAILS: load, take a goalId, then agda_give { goalId, expr: <an expr that Agda accepts as a give but whose written result breaks the file — if hard to induce, instead use agda_apply_edit with a raw text edit that replaces a correct clause with an ill-typed one> , writeToFile: true }. The spec: a mutation tool that writes and then fails to reload must return partial/failure with post-reload diagnostics — confirmed iff the envelope reports success-shaped output (ok:true, no failure classification) while the post-write reload diagnostics show errors; unreproducible iff the envelope surfaces a partial/failure state carrying the post-reload diagnostics. Record the exact tool probed and the applyEditAndReload output text. Capture + wrapup (rig recipe from 06-02).
    RT6 session (ad2b6d31f58f1759, run-id `rt6-<date>`): corpus tmp/rt-reverify/rt6/ with MixedGoalsErrors.agda AND MixedVisibleInvisible.agda. agda_load each; the spec asks agda_load to distinguish five states (visible goals / hidden metas / constraints / source holes / file completeness). Compare the observed envelope data fields (classification, goalCount, invisibleGoalCount, hasHoles, plus any constraints signal) against the five asked-for distinctions; verdict rules: unreproducible/satisfied iff current data fields already expose each distinct state observed in the fixtures; confirmed (as missing-feature) iff at least one distinction remains conflated — name WHICH in notes. The no-goals-but-constraints-hold-an-error SUB-case belongs to fdc90bfde12fb938 (agda_proof_status, fixed separately in wave 3) — do not double-count it here (the entries are relatedFingerprint-linked); note the cross-reference in both rows' notes instead.
    Queue updates per the 06-02 D-07 recipe (hand-edit + `npx vitest run test/unit/fixtures/fix-queue.test.ts`). D-08 duplicate sweep after each wrapup. Append RT5/RT6 sections + summary rows to RT-REVERIFY.md.
    IMPORTANT for RT6 if confirmed: the plausible fix is the UX report's "Suggested Schema Direction" response-schema rework — that is the deferred-ideas re-triage candidate (D-10). Record the observation precisely; the DISPOSITION (fix vs re-triage) belongs to plan 06-06, not here.
  </action>
  <verify>
    <automated>npx vitest run test/unit/fixtures/fix-queue.test.ts && ls .agda-mcp/runs/ | grep -c "^rt[56]-"</automated>
  </verify>
  <acceptance_criteria>
    - `.agda-mcp/runs/rt5-*/wrapup-report.json` and `.agda-mcp/runs/rt6-*/wrapup-report.json` exist.
    - Entries a0ae86c7deb9754e and ad2b6d31f58f1759 no longer carry `"needsReverify": true` and sit in schema-valid end states (triaged or rejected+cannot-reproduce+closedAt).
    - RT6's notes and fdc90bfde12fb938's notes both contain the rt6 run-id cross-reference line (no status change to fdc90bfde12fb938 in this plan).
    - RT-REVERIFY.md contains "## RT5" and "## RT6" sections with Verdict lines and quoted envelope evidence.
    - `npx vitest run test/unit/fixtures/fix-queue.test.ts` passes.
  </acceptance_criteria>
  <done>RT5 and RT6 have definitive pipeline-measured verdicts; RT6's schema-rework scope question is documented for 06-06's disposition call.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify RT7 (timeout identification) + RT8 (stale-reload combined classification) and close out REVERIFY-01</name>
  <files>test/fixtures/fix-queue.json, .planning/research/RT-REVERIFY.md</files>
  <read_first>test/fixtures/fix-queue.json (entries b6821f42952c6ff8 RT7, 3306edf4c2d01c53 RT8, e6f0c1169032b9d5 flagship for RT8's cross-link), src/session/command-completion.ts (timeout env vars, lines 41-79), test/fixtures/agda/FixtureDeps/TransitiveStaleness/ (all three files), .planning/research/CHG-REVERIFY.md Defect 1 Observed section (the warm-inject baseline: previousClassification / staleBeforeLoad / session-regression diagnostic shapes)</read_first>
  <action>
    RT7 session (b6821f42952c6ff8, run-id `rt7-<date>`): corpus tmp/rt-reverify/rt7/ with any small fixture (HoleQuestionMark.agda). Spawn the proxy with AGDA_MCP_COMMAND_TIMEOUT_MS set low in the driver env (start at "300"; if the load completes anyway, retry the run with "50" — the version-detection preflight also consumes the window, so too-low values may fail before the user command; pick the largest value that reliably times out and record it). agda_load -> the command times out -> record the timeout envelope + diagnostics VERBATIM. Spec: a timeout must identify whether Agda exited, is still running, or produced no protocol response. Confirmed iff the diagnostics are coarse/conflating (e.g. suggest a crash/startup failure when the process is demonstrably still alive); unreproducible iff the envelope already distinguishes those states. Then capture — note that a failed load leaves no load-family tuple, so wrapup's ORCL-01/flake-gate will honestly skip/not-apply; the RT7 evidence is the recorded envelope in the transcript + capture, and the wrapup-report still documents the pipeline pass (D-06 satisfied). Run wrapup with the SAME env unset (normal timeouts) so the oracle itself is not sabotaged.
    RT8 session (3306edf4c2d01c53, run-id `rt8-<date>`): corpus tmp/rt-reverify/rt8/ with the FixtureDeps/TransitiveStaleness/ tree copied intact (nested dirs preserved). Drive agda_load_no_metas { file: "FixtureDeps/TransitiveStaleness/Main.agda" } (expect ok-complete) -> out-of-band overwrite of the corpus copy's Dep.agda with Dep.broken.agda's content (writeFileSync in the driver — the canonical out-of-band edit) -> agda_load_no_metas Main.agda again. Spec: the stale reload must report previous+new classification together with the reason. Confirmed iff the second envelope lacks previousClassification/staleBeforeLoad/session-regression-style context for agda_load_no_metas specifically; unreproducible iff those fields/diagnostics are present (compare against CHG-REVERIFY's agda_load baseline). Also run agda_load on the same file for an in-session comparison row. Capture + wrapup. Cross-reference the flagship entry e6f0c1169032b9d5 in notes (relatedFingerprint) — the flagship stays locked and untouched.
    Queue updates per D-07 recipe for both entries; D-08 duplicate sweep (RT8's warm/cold differential could legitimately auto-file if a REAL regression resurfaced — if wrapup files a deterministic false-green candidate here, that is genuine new cargo: keep it, cross-link it to 3306edf4c2d01c53, and flag it loudly in the summary for 06-06).
    Finish RT-REVERIFY.md: complete the 8-row summary table, add RT7/RT8 sections, and a closing "REVERIFY-01 acceptance" paragraph stating the grep gate result.
  </action>
  <verify>
    <automated>test "$(grep -c '"needsReverify": true' test/fixtures/fix-queue.json)" = "0" && npx vitest run test/unit/fixtures/fix-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"needsReverify": true' test/fixtures/fix-queue.json` returns exactly 0 — success criterion 3's mechanical gate.
    - `.agda-mcp/runs/rt7-*/wrapup-report.json` and `.agda-mcp/runs/rt8-*/wrapup-report.json` exist.
    - Every formerly-needsReverify entry is status "triaged" (confirmed, needsReverify false, evidence note with run-id) or "rejected" (rejectedReason "cannot-reproduce", closedAt set, evidence note) — verified by the schema test passing plus a manual scan documented in the task output.
    - RT-REVERIFY.md's summary table has 8 filled rows; RT7's section records the exact AGDA_MCP_COMMAND_TIMEOUT_MS value used; RT8's section quotes the presence/absence of previousClassification in the agda_load_no_metas reload envelope.
    - `git status --porcelain` shows only the two tracked files modified.
  </acceptance_criteria>
  <done>All 8 RT specs definitively re-verified through the shipped pipeline; zero needsReverify remain; the D-12 gate for wave-3 fixes is open with pre-fix measurements on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Driver env → child server | AGDA_MCP_COMMAND_TIMEOUT_MS manipulation is a deliberate local fault-injection lever for RT7 |
| Executor edits → fix-queue.json SSOT | same as plan 06-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | DoS | low-timeout env leaking into wrapup's oracle runs | mitigate | RT7's env override is set ONLY on the driver->proxy spawn; wrapup runs in a fresh shell with default env (explicit instruction + recorded value in report) |
| T-06-10 | Tampering | fix-queue.json hand-edits | mitigate | schema test after every edit; git audit trail |
| T-06-11 | Repudiation | verdict provenance | mitigate | run-ids + commit SHA + agda version recorded in RT-REVERIFY.md |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies installed |
</threat_model>

<verification>
- `grep -c '"needsReverify": true' test/fixtures/fix-queue.json` == 0.
- `npx vitest run test/unit/fixtures/fix-queue.test.ts` green.
- Four new wrapup-report.json artifacts exist (rt5-rt8 run-ids).
</verification>

<success_criteria>
- Success criterion 3 fully satisfied: all 8 needsReverify entries show confirmed (fresh capture + verdict evidence) or closed unreproducible (with evidence); zero remain needsReverify.
- Confirmed set for REVERIFY-02 is now enumerated (the 4 seeded confirmed entries + whatever RT1-RT8 confirmed) — input to waves 3-4.
</success_criteria>

<output>
Create `.planning/phases/06-backlog-digestion-policy-fix-reverify/06-03-SUMMARY.md` when done.
</output>
