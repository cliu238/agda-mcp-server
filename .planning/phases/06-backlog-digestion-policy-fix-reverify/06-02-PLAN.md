---
phase: 06-backlog-digestion-policy-fix-reverify
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/fix-queue.json
  - .planning/research/RT-REVERIFY.md
autonomous: true
requirements: [REVERIFY-01]
must_haves:
  truths:
    - "RT1, RT2, RT3, RT4 each have a definitive verdict measured against current main THROUGH the shipped pipeline (dogfood-run.mjs proxy -> agda_capture_session -> dogfood-wrapup.mjs oracle triad + flake gate), not ad-hoc harness scripts (D-06)"
    - "Each of the four fix-queue entries (03f7c711c0209369, e5f6de1fa365b887, eaea6321183bdf7b, 004d161b839ce725) is either confirmed (needsReverify false + status triaged + evidence in notes) or closed unreproducible (status rejected + rejectedReason cannot-reproduce + closedAt + evidence) — per the frozen schema, no new status values (D-07)"
    - "No duplicate disconnected new entries are left behind by wrapup auto-filing (D-08 manual-merge discipline)"
    - "RT4's re-verification is complete BEFORE any agda_auto fix lands (D-08 no-race rule — the fix plan is gated behind this plan's wave)"
  artifacts:
    - path: ".planning/research/RT-REVERIFY.md"
      provides: "Committed evidence report: per-RT verdict/repro/observed/implication sections for RT1-RT4 (CHG-REVERIFY.md structure), run-ids + fingerprints recorded"
      min_lines: 60
    - path: "test/fixtures/fix-queue.json"
      provides: "RT1-RT4 entries transitioned per D-07 with evidence summarized in notes"
      contains: "RT-REVERIFY.md"
  key_links:
    - from: "test/fixtures/fix-queue.json"
      to: ".planning/research/RT-REVERIFY.md"
      via: "notes field citing the report + run-id"
      pattern: "RT-REVERIFY"
    - from: ".planning/research/RT-REVERIFY.md"
      to: ".agda-mcp/runs/<run-id>/wrapup-report.json"
      via: "recorded run-ids (gitignored artifacts referenced by id, per the evidence convention)"
      pattern: "run-id"
---

<objective>
Re-verify RT1-RT4 (the first four of the eight `needsReverify` CHG defect specs, all v0.6.7-era measurements) against current main, driving each through the SHIPPED Loop-2 pipeline: a live `dogfood-run.mjs` recording-proxy session against a small disposable fixture corpus, `agda_capture_session` per spec, then `dogfood-wrapup.mjs` (oracle triad + N-rerun flake gate). Record definitive per-entry verdicts in the queue and a committed evidence report.

Purpose: REVERIFY-01 (first half) — closes half the stale backlog and gives D-12 its pre-fix measurements. This plan runs in wave 1, in parallel with the POLICY-01 plumbing (independent file surfaces per the phase decision record).
Output: fix-queue.json transitions for 4 entries + .planning/research/RT-REVERIFY.md (RT1-RT4 sections).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-CONTEXT.md
@.agents/skills/agda-dogfooding/SKILL.md
@.planning/research/CHG-REVERIFY.md

<interfaces>
<!-- Pipeline contracts the executor drives. Extracted from the codebase. -->

Proxy launch (scripts/dogfood/dogfood-run.mjs):
- CLI: `npx tsx scripts/dogfood/dogfood-run.mjs --manifest <path> --corpus-root <path> [--run-id <id>]`
- Hard gate: loadTaskManifest throws before any spawn on missing/invalid/empty manifest.
- The child server gets AGDA_MCP_CAPTURE=1 unconditionally and AGDA_MCP_ROOT=<corpus-root>; the child inherits the proxy's process.env, so env vars exported when spawning the proxy (e.g. AGDA_MCP_COMMAND_TIMEOUT_MS) reach the server.
- Run artifacts land under SERVER_REPO_ROOT/.agda-mcp/runs/<run-id>/ (transcript.jsonl, run-report.json); captures are staged under <corpus-root>/.agda-mcp/captures/ (register-capture-session.ts line 190) and auto-promoted.
- On session end the proxy prints: "To judge them: npx tsx scripts/dogfood/dogfood-wrapup.mjs <run-id>".

Task manifest shape (test/fixtures/task-manifest-schema.ts): non-empty array of { target: string, expectedSignature: string ("name : Type" convention), corpus: string, notes?: string }. Deliberately NOT committed; `corpus` cross-references fuel-corpora keys by convention only — for these local fixtures use the literal value "rt-local-fixture".

Driver skeleton (the executor acts as the MCP agent; a gitignored tmp/ driver connecting THROUGH the proxy is the sanctioned client side — what D-06 forbids is bypassing the proxy/capture/wrapup pipeline the way quick-task 260702-29k's direct-harness scripts did):

```js
// tmp/rt-driver.mjs — run with: npx tsx tmp/rt-driver.mjs (gitignored, never committed)
import { Client } from "@modelcontextprotocol/sdk/client/index.js";
import { StdioClientTransport } from "@modelcontextprotocol/sdk/client/stdio.js";
const transport = new StdioClientTransport({
  command: "npx",
  args: ["tsx", "scripts/dogfood/dogfood-run.mjs",
         "--manifest", MANIFEST_PATH, "--corpus-root", CORPUS_ROOT, "--run-id", RUN_ID],
  env: { ...process.env /*, AGDA_MCP_COMMAND_TIMEOUT_MS: "..." for RT7 only */ },
  stderr: "pipe",
});
const client = new Client({ name: "rt-reverify-driver", version: "0.0.0" });
await client.connect(transport);
const res = await client.callTool({ name: "agda_load", arguments: { file: "HoleQuestionMark.agda" } });
// ... spec-specific calls ... then ALWAYS:
await client.callTool({ name: "agda_capture_session", arguments: { expectedSignature, note } });
await transport.close(); // triggers proxy finalize -> run-report.json
```
Print each result's `structuredContent` (JSON.stringify) — those envelopes ARE the evidence.

Wrap-up: `npx tsx scripts/dogfood/dogfood-wrapup.mjs <run-id>` — judges every staged capture (oracle triad, N=3 flake gate), writes .agda-mcp/runs/<run-id>/wrapup-report.json, files ONLY explicit orcl01 false-green candidates / orcl02 cheats into test/fixtures/fix-queue.json as "new".

Queue mutation: hand-edit of test/fixtures/fix-queue.json is sanctioned (flat git-audited SSOT) — validate after EVERY edit with `npx vitest run test/unit/fixtures/fix-queue.test.ts` (schema superRefine enforces rejectedReason/closedAt invariants).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up the RT session rig and re-verify RT1 + RT2</name>
  <files>test/fixtures/fix-queue.json, .planning/research/RT-REVERIFY.md</files>
  <read_first>scripts/dogfood/dogfood-run.mjs (CLI flags lines 80-106, finalize/report lines 266-341), scripts/dogfood/dogfood-wrapup.mjs (scriptMain flow lines 284-373), test/fixtures/task-manifest-schema.ts, test/fixtures/fix-queue.json (entries 03f7c711c0209369 and e5f6de1fa365b887), test/fixtures/agda/HoleQuestionMark.agda, test/fixtures/agda/NavigationQueries.agda, .planning/research/CHG-REVERIFY.md (report structure to mirror + verdict-calibration precedent), test/unit/fixtures/fix-queue.test.ts</read_first>
  <action>
    Preflight: `command -v agda && agda --version` (require >= 2.6.4.3) and `npm run build` (the proxy spawns dist/index.js). If agda is absent, STOP and report a blocker — do not fake verdicts (the 260702-29k calibration rule: no verdict is guessed).
    Rig: create gitignored working dirs `tmp/rt-reverify/rt1/` and `tmp/rt-reverify/rt2/` as corpus roots. Copy EXISTING fixtures (D-05: small files, no new committed fixtures needed): rt1 gets test/fixtures/agda/HoleQuestionMark.agda; rt2 gets test/fixtures/agda/NavigationQueries.agda. Write a per-RT task-manifest JSON in each corpus dir: rt1 entry { target: "RT1 visible-hole completeness", expectedSignature: "question : Nat", corpus: "rt-local-fixture" }; rt2 entry { target: "RT2 not-in-scope query", expectedSignature: "add : Nat -> Nat -> Nat", corpus: "rt-local-fixture" }. Write the driver (tmp/rt-driver.mjs per the interfaces skeleton, parameterized by env or argv for corpus/manifest/run-id/calls).
    RT1 session (run-id `rt1-<date>`): agda_load HoleQuestionMark.agda -> record the full envelope. The spec: a file with a visible hole must NEVER yield a completeness claim. Confirmed iff classification is "ok-complete" OR success:true with hasHoles:false; unreproducible iff classification is "ok-with-holes"/equivalent with hasHoles:true and no completeness claim. Then agda_capture_session { expectedSignature: "question : Nat", note: "RT1 re-verification: visible hole vs completeness claim, spec fingerprint 03f7c711c0209369" }. Close transport; run `npx tsx scripts/dogfood/dogfood-wrapup.mjs rt1-<date>`; keep the tmp corpus dir alive until wrapup completes (the oracle reads artifact.manifest.repoRoot).
    RT2 session (run-id `rt2-<date>`): agda_load NavigationQueries.agda -> then probe the query family with an out-of-scope identifier: agda_infer { expr: "definitelyNotInScopeXyz" } and agda_compute { expr: "definitelyNotInScopeXyz" } (top-level, no goalId). Spec: must return ok:false / structured NotInScope — confirmed iff any probed tool returns ok:true wrapping the NotInScope text; record WHICH exact tool (the entry's own notes say affectedTool "agda_query" is best-inferred and must be confirmed). Capture with note naming fingerprint e5f6de1fa365b887 and the tools probed; wrapup.
    Queue updates (D-07, hand-edit + schema validation): for each of the two entries — CONFIRMED: set needsReverify false, status "new"->"triaged", append to notes: "Re-verified <date> against current main through dogfood-run/dogfood-wrapup (run-id <id>): CONFIRMED — <one-line observed evidence>. See .planning/research/RT-REVERIFY.md." (for RT2 also correct affectedTool to the confirmed tool name). UNREPRODUCIBLE: set needsReverify false, status "rejected", rejectedReason "cannot-reproduce", closedAt now-ISO, notes append: "Re-verified <date> (run-id <id>): does not reproduce on current main — <observed correct behavior>. Measurement conditions in .planning/research/RT-REVERIFY.md." Never invent a sixth status; never claim "never existed" — cannot-reproduce with conditions recorded (the 260702-29k honesty rule).
    D-08 sweep: after each wrapup, diff fix-queue.json; if wrapup auto-filed a new entry duplicating the seeded RT root cause, merge its capturePath/verdictPath evidence into the seeded entry's notes and REMOVE the disconnected row (record the merge in notes); a genuinely-new unrelated find stays as legitimate new cargo.
    Start .planning/research/RT-REVERIFY.md mirroring CHG-REVERIFY.md's structure: header (date, agda --version, current-main commit, node/tsx), summary table rows RT1-RT4 (RT3/RT4 filled by Task 2), then per-RT sections: Verdict / v0.6.7 claim / Repro (manifest + calls + run-id) / Observed (quoted envelope fields) / Implication.
  </action>
  <verify>
    <automated>npx vitest run test/unit/fixtures/fix-queue.test.ts && test -f .planning/research/RT-REVERIFY.md && ls .agda-mcp/runs/ | grep -c "^rt[12]-"</automated>
  </verify>
  <acceptance_criteria>
    - `.agda-mcp/runs/rt1-*/wrapup-report.json` and `.agda-mcp/runs/rt2-*/wrapup-report.json` both exist (pipeline actually ran end-to-end; D-06 proof).
    - `grep -A3 '"fingerprint": "03f7c711c0209369"' test/fixtures/fix-queue.json` shows either status "triaged" or status "rejected" — and NOT `"needsReverify": true`; same for e5f6de1fa365b887.
    - `npx vitest run test/unit/fixtures/fix-queue.test.ts` passes after the edits (schema invariants hold: rejected entries carry rejectedReason + closedAt).
    - RT-REVERIFY.md contains sections "## RT1" and "## RT2" each with a Verdict line and at least one quoted envelope field (classification or ok value) as observed evidence.
    - Zero rows in fix-queue.json have a title starting with "Dogfood-surfaced:" that duplicates an RT1/RT2 root cause (D-08: `grep -c '"title": "Dogfood-surfaced' test/fixtures/fix-queue.json` count explained in notes if nonzero).
    - No files under tmp/ or .agda-mcp/ are staged for commit (`git status --porcelain | grep -E "tmp/|\.agda-mcp/"` is empty).
  </acceptance_criteria>
  <done>RT1 and RT2 have definitive pipeline-measured verdicts recorded in queue + report; the session rig (driver + corpus-dir + manifest recipe) is proven and documented for reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify RT3 + RT4 and finish the RT1-RT4 evidence sections</name>
  <files>test/fixtures/fix-queue.json, .planning/research/RT-REVERIFY.md</files>
  <read_first>test/fixtures/fix-queue.json (entries eaea6321183bdf7b RT3 and 004d161b839ce725 RT4 — note: eaea6321183bdf7b is the RT3 fingerprint; also read agda_auto entry 5abecc959e43fef3's notes for the relatedFingerprint/manual-merge warning), src/tools/goal-tools.ts lines 417-454 (agda_goal_type_context_check renders "(no checked term returned)"), .planning/research/CHG-REVERIFY.md Defect 2 section (the exact agda_auto repro call shape + the auto-closes-goal-in-session deviation note), test/fixtures/agda/TypeError.agda, test/fixtures/agda/WriteCaseSplit.agda</read_first>
  <action>
    RT3 session (fingerprint eaea6321183bdf7b, run-id `rt3-<date>`): corpus tmp/rt-reverify/rt3/ with WriteCaseSplit.agda (has holes -> open goals). agda_load -> take goalIds from data (never hardcode) -> agda_goal_type_context_check { goalId, expr: "definitelyNotInScopeXyz" } and a second probe with an ill-typed expr (e.g. give a Nat-typed goal the expr "true"). Spec: a FAILING context-check must not surface "no checked term" inside an ok:true result. Confirmed iff envelope is ok:true and text/data carries "(no checked term returned)" or embedded Agda error text; unreproducible iff the failure surfaces as ok:false / error envelope. Confirm the exact affected tool name in notes (entry says best-inferred "agda_context"; the live probe decides — the rendering site is agda_goal_type_context_check per goal-tools.ts line 443). Capture (expectedSignature from manifest, note naming the fingerprint) + wrapup.
    RT4 session (fingerprint 004d161b839ce725, run-id `rt4-<date>`): corpus tmp/rt-reverify/rt4/ with HoleQuestionMark.agda. agda_load -> goalId from data -> agda_auto { goalId, depth: 5, listCandidates: true, hints: ["-t 999999"], excludeHints: ["--unsafe"], writeToFile: false } (the EXACT CHG-REVERIFY Defect-2 shape). Spec: flag-shaped hints must not enter the Agsy payload verbatim, and a diagnostic must never be reported as a solution. Confirmed iff data.searchPayload contains the literal "-h -t 999999" (or "-x --unsafe") OR hasSolution:true with data.solution containing "error: [". Known re-verified alive 2026-07-02 — expect CONFIRMED, but measure anyway (this is the pre-fix measurement D-12 requires and the D-08 gate the agda_auto fix waits on). If the baseline auto call closes the goal in-session, reload the file first (the documented CHG-REVERIFY deviation). Capture + wrapup.
    Queue updates per D-07 exactly as Task 1's recipe. RT4 extra (D-08): its entry and agda_auto 5abecc959e43fef3 are relatedFingerprint-linked — attach the fresh run-id/evidence note to BOTH rows (the 5abe row stays "triaged"; only its notes gain the fresh-measurement line: "RT4 re-measurement <date> run-id <id> re-confirms the same root cause; fix + lock pending Phase 6 wave 3."). Do NOT flip 5abe's status here.
    D-08 duplicate sweep after each wrapup (same rule as Task 1).
    Complete RT-REVERIFY.md's RT3/RT4 sections + fill their summary-table rows. Leave the report open-ended (RT5-RT8 appended by plan 06-03) with an explicit "RT5-RT8: see continuation (plan 06-03)" marker line.
  </action>
  <verify>
    <automated>npx vitest run test/unit/fixtures/fix-queue.test.ts && grep -c '"needsReverify": true' test/fixtures/fix-queue.json | grep -x 4</automated>
  </verify>
  <acceptance_criteria>
    - `.agda-mcp/runs/rt3-*/wrapup-report.json` and `.agda-mcp/runs/rt4-*/wrapup-report.json` exist.
    - `grep -c '"needsReverify": true' test/fixtures/fix-queue.json` returns exactly 4 (RT5-RT8 remain; RT1-RT4 all flipped).
    - Entry 004d161b839ce725 shows a definitive state (triaged-confirmed expected) with a notes line containing its rt4 run-id; entry 5abecc959e43fef3 notes contain the same run-id (evidence attached to both linked rows) while its status remains "triaged".
    - RT-REVERIFY.md has all four RT sections with Verdict lines; the summary table has 4 filled rows; the file names the exact tool confirmed for RT3.
    - `npx vitest run test/unit/fixtures/fix-queue.test.ts` passes.
    - `git status --porcelain` shows only test/fixtures/fix-queue.json and .planning/research/RT-REVERIFY.md as modified/added (drivers, manifests, corpora, runs all gitignored).
  </acceptance_criteria>
  <done>RT1-RT4 all definitively re-verified through the shipped pipeline with queue + committed evidence; RT4's pre-fix measurement is on record, unblocking the D-08-gated agda_auto fix in wave 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Captured artifacts → oracle triad | wrapup judges artifacts recorded from live sessions; artifact paths/content feed ORCL-01/02/03 |
| Executor edits → fix-queue.json SSOT | hand-edits of the tracked queue must not corrupt the schema or audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | fix-queue.json hand-edits | mitigate | every edit validated by `npx vitest run test/unit/fixtures/fix-queue.test.ts` (zod superRefine); git diff is the audit trail |
| T-06-06 | Repudiation | verdict evidence provenance | mitigate | committed RT-REVERIFY.md records run-ids, commit SHA, agda version; raw captures stay gitignored per the established evidence convention |
| T-06-07 | Elevation | oracle materialization of artifact-controlled paths | accept | shipped CR-01/CR-02 sandboxing in orcl-02/orcl-01 already contains artifact paths; this plan only feeds it self-authored fixtures |
| T-06-08 | Info disclosure | committing tmp session artifacts | mitigate | tmp/ and .agda-mcp/ are gitignored; acceptance criterion asserts a clean porcelain apart from the two tracked files |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies installed |
</threat_model>

<verification>
- Both tasks' wrapup-report.json artifacts exist under .agda-mcp/runs/ for all four run-ids.
- `grep -c '"needsReverify": true' test/fixtures/fix-queue.json` == 4 after this plan.
- `npx vitest run test/unit/fixtures/fix-queue.test.ts` green.
</verification>

<success_criteria>
- 4 of 8 needsReverify entries reached a definitive, schema-valid end state (triaged-confirmed or rejected/cannot-reproduce) with pipeline-produced evidence — the first half of success criterion 3.
- The pipeline itself (proxy -> capture -> wrapup) was exercised as real cargo, per D-06.
</success_criteria>

<output>
Create `.planning/phases/06-backlog-digestion-policy-fix-reverify/06-02-SUMMARY.md` when done.
</output>
