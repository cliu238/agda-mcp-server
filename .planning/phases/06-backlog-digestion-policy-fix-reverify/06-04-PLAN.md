---
phase: 06-backlog-digestion-policy-fix-reverify
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - src/agda/refactor-helpers.ts
  - src/agda/types.ts
  - src/agda/goal-operations.ts
  - src/tools/tool-errors.ts
  - src/tools/goal-tools.ts
  - test/unit/agda/agent-ux.test.ts
  - test/unit/agda/goal-operations-give.test.ts
  - test/unit/tools/goal-tools-give.test.ts
autonomous: true
requirements: [REVERIFY-02]
must_haves:
  truths:
    - "agda_auto with a flag-shaped hint (e.g. \"-t 999999\") returns ok:false — the hint is never concatenated into the Agsy search payload, and a diagnostic can never be reported as a solution via that injection path (fingerprints 5abecc959e43fef3 + RT4 004d161b839ce725)"
    - "agda_give of an ill-typed expression returns ok:false with classification give-rejected — never ok:true wrapping Agda's own rejection text (fingerprint bfcba437f5426fd6)"
    - "Both fixes carry from-RED regression tests: each test was demonstrated failing against the pre-fix code before the fix landed"
    - "src/tools/goal-tools.ts stays at or under the 500-line ceiling; domain logic stays in src/agda (no fat tool handlers)"
  artifacts:
    - path: "src/agda/refactor-helpers.ts"
      provides: "buildAutoSearchPayload boundary validation (rejects flag-shaped / whitespace-containing hint tokens)"
    - path: "src/agda/goal-operations.ts"
      provides: "give() rejection detection via the DisplayInfo info.kind === Error idiom"
    - path: "src/agda/types.ts"
      provides: "GiveResult gains optional rejected / rejectionText fields"
      contains: "rejected"
    - path: "src/tools/tool-errors.ts"
      provides: "giveRejectedError() ToolInvocationError factory (classification give-rejected)"
      exports: ["giveRejectedError"]
    - path: "test/unit/tools/goal-tools-give.test.ts"
      provides: "Envelope-level proof: rejected give -> isError true / give-rejected classification"
  key_links:
    - from: "src/tools/goal-tools.ts"
      to: "src/agda/goal-operations.ts"
      via: "result.rejected branch after session.goal.give()"
      pattern: "result\\.rejected"
    - from: "src/tools/goal-tools.ts"
      to: "src/tools/tool-errors.ts"
      via: "throw giveRejectedError(...)"
      pattern: "giveRejectedError"
    - from: "src/agda/goal-operations.ts"
      to: "src/agda/parse-load-responses.ts idiom"
      via: "DisplayInfo info.kind === \"Error\" scan over responses"
      pattern: "kind === \"Error\""
---

<objective>
Fix the two highest-priority confirmed live defects (QUEUE-02 band false-green, recurrence-tied, file order): the agda_auto CLI-flag hint injection (`5abecc959e43fef3`, shared root cause with RT4 `004d161b839ce725`) and the agda_give ok:true-wrapping-rejection (`bfcba437f5426fd6`). Both are src/ server fixes with from-RED regression tests.

Wave gate honored: this plan depends on 06-03, so ALL 8 RT re-verifications (including RT4's pre-fix measurement, D-08's no-race rule) are complete before either fix lands (D-12).

Lock-mechanism note (recorded here, executed in plan 06-06): these are non-load-family tool defects. The Phase-3 emit-regression pipeline structurally cannot lock them — judgeRefusal requires an ORCL-01 server-false-green-candidate with a coldTuple, and replayCaptureRegressionEntry only drives `entry.tool` with a `{file}` argument and asserts load-tuple fields. The durable regression lock for these entries is therefore the from-RED vitest tests this plan commits; plan 06-06 records that mechanism explicitly in each queue entry's notes when flipping to locked (matrixEntryId stays null, which the frozen schema permits — only closedAt is enforced for locked).

Purpose: REVERIFY-02 — the loop's first sustained real workload, priority-ordered.
Output: two fixed tools + three test files proving the fixes from RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-CONTEXT.md
@.planning/phases/06-backlog-digestion-policy-fix-reverify/06-PATTERNS.md
@.planning/research/CHG-REVERIFY.md

<interfaces>
<!-- Exact current shapes at the fix sites. -->

src/agda/refactor-helpers.ts lines 107-131 — buildAutoSearchPayload(options: AutoSearchOptions): string. Current hint handling: `for (const hint of options.hints ?? []) { if (hint.trim().length > 0) flags.push(`-h ${hint.trim()}`); }` and the same for excludeHints with `-x`. A hint "-t 999999" therefore emits literal `-h -t 999999` (two extra Agsy flags). Sole production caller: src/tools/goal-tools.ts line 381 (agda_auto callback). Existing test: test/unit/agda/agent-ux.test.ts lines 139-152.

src/agda/types.ts lines 191-195 — `export interface GiveResult { result: string; replacementText?: string | null; }`. NOTE: refine()/refineExact()/intro() ALSO return GiveResult — new fields MUST be optional so those functions stay type-correct without scope creep (the confirmed defect is give-only; refine/intro population is RT5/schema-rework territory, out of scope here).

src/agda/goal-operations.ts lines 106-122 — give() sends Cmd_give, calls throwOnFatalProtocolStderr (matches only three stderr regexes — an ill-typed give never hits stderr), then returns { result: decodeGiveLikeResponse(responses), replacementText: resolveGiveReplacementText(responses, expr) }. An Agda rejection arrives as a normal DisplayInfo response whose text decodeGiveLikeResponse happily renders.

The detection idiom to replicate (src/agda/parse-load-responses.ts lines 113-127): iterate responses; for resp.kind === "DisplayInfo", parseResponseWithSchema(displayInfoResponseSchema, resp); if display.info.kind === "Error", collect decodeDisplayInfoEvents([resp]).at(-1)?.text. Same check also used at src/protocol/responses/backend.ts line 26. Check parse-load-responses.ts's own imports for where displayInfoResponseSchema / decodeDisplayInfoEvents live.

src/tools/goal-tools.ts lines 167-195 — agda_give callback: `const result = await session.goal.give(goalId, exprStr);` then output building ("**Result:** ..." or "Expression accepted."), then the hasReplacementText write branch. File is at 494/500 lines — the fix adds AT MOST ~5 lines here (one import name + a 2-3 line rejected branch); ALL construction logic goes in tool-errors.ts.

src/tools/tool-errors.ts (115 lines) — home of ToolInvocationError and missingPathToolError (the factory template: builds a ToolInvocationError with message/classification/diagnostics[{severity, message, code, nextAction}]/data). registerGoalTextTool's catch converts any throw via makeTextToolErrorResult -> toToolInvocationError, so a thrown ToolInvocationError surfaces with its own classification (src/tools/tool-registration.ts lines 397-423). A goal-text callback CANNOT set a non-ok classification by returning — throwing is the only channel (returns are always wrapped in okEnvelope).

Fake-harness test pattern: test/unit/tools/analysis-tools.test.ts lines 7-52 — createCapturingServer() Map-backed fake + inline fake session `as any`; register, get callback, call, assert result.isError / structuredContent fields. clearToolManifest() first (import from src/tools/manifest.js — check the existing test's import line).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agda_auto — reject flag-shaped hint tokens at the buildAutoSearchPayload boundary</name>
  <files>src/agda/refactor-helpers.ts, test/unit/agda/agent-ux.test.ts, test/unit/tools/goal-tools-give.test.ts</files>
  <read_first>src/agda/refactor-helpers.ts (full, 131 lines), test/unit/agda/agent-ux.test.ts (describe "buildAutoSearchPayload" lines 139-152 + the file's imports), src/tools/goal-tools.ts lines 355-415 (agda_auto callback — payload built BEFORE session.goal.autoOne), test/fixtures/fix-queue.json entry 5abecc959e43fef3 (the defect contract in its summary/notes)</read_first>
  <behavior>
    Write RED tests first in test/unit/agda/agent-ux.test.ts:
    - buildAutoSearchPayload({ hints: ["-t 999999"] }) throws, message matching /not a valid Agsy hint/ and containing the offending token.
    - buildAutoSearchPayload({ excludeHints: ["--unsafe"] }) throws likewise.
    - buildAutoSearchPayload({ hints: ["foo bar"] }) throws (whitespace would split into a second token).
    - Existing behavior preserved: { depth: 5, listCandidates: true, hints: ["helper"], excludeHints: ["bad"] } still renders "-d 5", "--list-candidates", "-h helper", "-x bad" (the current test stays green).
    - Hyphenated identifiers that do NOT lead with '-' pass through (e.g. "Nat-helper").
    Run the new tests BEFORE changing refactor-helpers.ts and record the failures (RED evidence in the task commit/summary).
  </behavior>
  <action>
    In src/agda/refactor-helpers.ts, inside buildAutoSearchPayload: before pushing any hint/excludeHint token, validate the trimmed value — if it starts with "-" or contains any whitespace (/\s/ test), throw a plain Error with a message of the shape: agda_auto hint <JSON.stringify(token)> is not a valid Agsy hint: hints must be bare identifier/module names (no leading "-", no whitespace); refusing to inject it into the search payload. Plain Error is correct at this layer (src/agda cannot import the tools-layer ToolInvocationError; registerGoalTextTool's catch already converts the throw into an ok:false error envelope). Keep depth/listCandidates handling untouched (server-constructed, trusted). Add a short JSDoc line on the function documenting the contract: hints are opaque identifier tokens, never raw Agsy flags (the CHG-REVERIFY implication wording).
    Envelope-level proof (goes in the NEW test file test/unit/tools/goal-tools-give.test.ts, which Task 2 shares — create it here with the auto case): using the analysis-tools.test.ts fake-harness pattern, register goal tools with a fake session (getGoalIds: () => [0], plus the minimal methods registerGoalTextTool's gates touch — read src/tools/tool-registration.ts validateGoalId/sessionErrorStateGate usage and mirror analysis-tools.test.ts's fake session fields), then call the captured agda_auto callback with { goalId: 0, hints: ["-t 999999"], writeToFile: false } and assert result.isError === true and the text names the rejected hint — proving the defect's actual contract (ok is no longer true) without any Agda subprocess. The fake session's goal.autoOne must be a vi.fn() that the test asserts was NEVER called (the throw happens at payload build).
  </action>
  <verify>
    <automated>npx vitest run test/unit/agda/agent-ux.test.ts test/unit/tools/goal-tools-give.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The three new throw tests + the preserved-behavior tests pass; the suite run exits 0.
    - RED evidence recorded: the summary/commit trail shows the new tests failing before the src change (test-first commit or captured output).
    - `grep -n "startsWith(\"-\")\|/\\\\s/" src/agda/refactor-helpers.ts` (or equivalent) shows the validation present inside buildAutoSearchPayload for BOTH hints and excludeHints loops.
    - The envelope test asserts isError === true AND autoOne was not called for the flag-shaped hint.
    - `wc -l src/agda/refactor-helpers.ts` <= 500.
    - `npx vitest run test/unit` passes (no collateral regressions).
  </acceptance_criteria>
  <done>Flag-shaped hints can no longer reach the Agsy payload; the injection path behind both 5abecc959e43fef3 and RT4 is closed with from-RED tests at the pure-function AND envelope levels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agda_give — detect Agda's rejection and surface ok:false / give-rejected</name>
  <files>src/agda/types.ts, src/agda/goal-operations.ts, src/tools/tool-errors.ts, src/tools/goal-tools.ts, test/unit/agda/goal-operations-give.test.ts, test/unit/tools/goal-tools-give.test.ts</files>
  <read_first>src/agda/goal-operations.ts (full, 228 lines — give() at 106-122, imports at top), src/agda/parse-load-responses.ts lines 100-140 (the info.kind === "Error" idiom + its imports), src/protocol/responses/proof-actions.ts (decodeGiveLikeResponse lines 97-117, hasReplacementText), src/agda/types.ts lines 185-200 (GiveResult), src/tools/tool-errors.ts (full — missingPathToolError as the factory template), src/tools/goal-tools.ts lines 147-196 (agda_give callback + file's import block), src/tools/tool-registration.ts lines 353-425 (registerGoalTextTool wrap/catch), test/fixtures/fix-queue.json entry bfcba437f5426fd6, .planning/research/CHG-REVERIFY.md Defect 3 (the exact observed envelope: ok true, data.result = "1.1-5: error: [UnequalTerms]\nBool !=< Nat...", replacementText null)</read_first>
  <behavior>
    RED tests first:
    - test/unit/agda/goal-operations-give.test.ts (new): call give() with a fake ctx ({ requireFile: no-op, sendCommand: async () => responses, iotcm: (c) => c, syncGoalIdsFromResponses: no-op } as the AgdaCommandContext surface give() actually touches — read give()'s body for the exact member list) where responses is a synthetic array containing a DisplayInfo response whose info.kind is "Error" with the UnequalTerms text (copy a real DisplayInfo/Error JSON shape from existing unit tests — grep test/unit for displayInfoResponseSchema or "DisplayInfo" fixtures) and NO GiveAction response. Assert the returned GiveResult has rejected === true and rejectionText containing "UnequalTerms". Companion case: responses containing a successful give shape (GiveAction present / replacement text resolvable) yields rejected === false.
    - test/unit/tools/goal-tools-give.test.ts: fake session whose goal.give resolves to { result: "1.1-5: error: [UnequalTerms]...", replacementText: null, rejected: true, rejectionText: "1.1-5: error: [UnequalTerms]..." } -> agda_give callback with { goalId: 0, expr: "true", writeToFile: false } -> assert result.isError === true and the structured envelope's classification === "give-rejected" (read makeTextToolErrorResult/toToolInvocationError to locate where classification appears in the result envelope). Companion: rejected: false + replacementText "true" -> isError false, ok envelope. Both are RED against the current callback (it ignores rejection entirely).
  </behavior>
  <action>
    1. src/agda/types.ts: extend GiveResult with OPTIONAL `rejected?: boolean` and `rejectionText?: string | null`, JSDoc noting: populated by give(); refine/refineExact/intro do not populate them yet (give-only fix, fingerprint bfcba437f5426fd6).
    2. src/agda/goal-operations.ts: add a module-private helper (name it detectResponseError(responses): string | null) implementing the parse-load-responses idiom — scan for resp.kind === "DisplayInfo", parse with displayInfoResponseSchema via parseResponseWithSchema, and when info.kind === "Error" return the decoded text (decodeDisplayInfoEvents([resp]).at(-1)?.text ?? ""). Import whatever parse-load-responses.ts imports for those helpers (same module paths). In give(): after computing replacementText, set errText = detectResponseError(responses); rejected = errText !== null && !hasReplacementText(replacementText) (import hasReplacementText from the proof-actions module goal-tools already uses — a rejection is an Error display WITH no confirmed replacement; a successful give with a mere warning display must not be marked rejected). Return { result, replacementText, rejected, rejectionText: rejected ? errText : null }.
    3. src/tools/tool-errors.ts: add `export function giveRejectedError(goalId: number, expr: string, rejectionText: string | null): ToolInvocationError` following missingPathToolError's construction style — message: Agda rejected `<expr>` for goal ?<goalId> — the expression does not satisfy the goal type; classification: "give-rejected"; one error diagnostic whose message is the rejectionText (fallback to the message) with code "give-rejected" and nextAction: "Inspect the goal with agda_goal_type_context_check, adjust the expression, and retry. The file was not modified."; data: { goalId, expr, result: rejectionText ?? "", replacementText: null, written: false }.
    4. src/tools/goal-tools.ts agda_give callback: immediately after `const result = await session.goal.give(goalId, exprStr);` insert the guard `if (result.rejected) { throw giveRejectedError(goalId, exprStr, result.rejectionText ?? null); }` and add giveRejectedError to the existing tool-helpers/tool-errors import surface (check whether src/tools/tool-helpers.ts re-exports tool-errors — search-definitions.ts imports ToolInvocationError from ../tool-helpers.js; if the barrel re-exports it, extend the SAME import line, else import from ./tool-errors.js directly). Total delta to this file MUST stay within the 500-line ceiling — if the file would exceed 500 lines, move the throw line's message assembly fully into giveRejectedError (it already is) and re-measure; do not restructure the file in this task.
  </action>
  <verify>
    <automated>npx vitest run test/unit/agda/goal-operations-give.test.ts test/unit/tools/goal-tools-give.test.ts && npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files pass; RED evidence recorded for both (failing runs before the src changes).
    - `npx tsc -p tsconfig.json --noEmit` exits 0 (optional fields keep refine/refineExact/intro type-correct with zero changes to them).
    - `wc -l src/tools/goal-tools.ts` <= 500 (currently 494; the guard + import must fit).
    - `grep -n "result.rejected" src/tools/goal-tools.ts` shows exactly one guard in the agda_give callback; `grep -n "giveRejectedError" src/tools/tool-errors.ts src/tools/goal-tools.ts` shows factory + single call site.
    - `grep -c "rejected" src/agda/goal-operations.ts` >= 2 (helper + give() population); refine/refineExact/intro bodies unchanged (`git diff src/agda/goal-operations.ts` touches only give() + the new helper + imports).
    - `npx vitest run test/unit` passes.
  </acceptance_criteria>
  <done>A rejected give surfaces as ok:false / classification give-rejected with the Agda rejection text in diagnostics; an accepted give is unchanged; the highest-risk silent-broken-proof-step pattern is closed with from-RED tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client args → Agsy CLI payload | agda_auto hints/excludeHints are untrusted strings previously concatenated into a command payload — this plan's Task 1 IS the mitigation |
| Agda response stream → GiveResult | response-derived rejection text flows into diagnostics shown to the agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Tampering | buildAutoSearchPayload hint injection | mitigate | boundary validation: reject leading-'-' and whitespace tokens before they join the payload (the command-builder quoted() precedent: never trust caller strings into a wire payload) |
| T-06-13 | Spoofing | false success signals (ok:true on rejected give / hasSolution on diagnostic) | mitigate | rejection detection + ToolInvocationError give-rejected; envelope-level tests lock ok:false |
| T-06-14 | Info disclosure | rejectionText echoed into diagnostics | accept | text is Agda's own error for the user's own file — already shown by every load-family tool |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies installed |
</threat_model>

<verification>
- `npx vitest run test/unit` green; `npx tsc -p tsconfig.json --noEmit` green; `npm run build` green.
- `wc -l src/tools/goal-tools.ts src/agda/goal-operations.ts src/agda/refactor-helpers.ts` all <= 500.
</verification>

<success_criteria>
- The two false-green-band confirmed defects are FIXED with from-RED regression tests (the lock artifacts plan 06-06 will reference when flipping the queue entries to locked).
- No layering violation: rejection detection in src/agda, error construction in tool-errors.ts, a <= 5-line guard in the tool adapter.
</success_criteria>

<output>
Create `.planning/phases/06-backlog-digestion-policy-fix-reverify/06-04-SUMMARY.md` when done. The summary MUST list the exact regression-test identifiers (file :: test name) for each fingerprint — plan 06-06 copies them into the queue notes as the lock references.
</output>
