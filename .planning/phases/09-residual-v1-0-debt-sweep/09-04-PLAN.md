---
phase: 09-residual-v1-0-debt-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - test/helpers/capture-regression-runner.ts
  - test/helpers/typecheck-disposable.ts
  - test/property/reporting/bug-report.property.test.ts
  - test/property/session/session-snapshot.property.test.ts
  - test/property/session/tool-recommendation.property.test.ts
  - test/unit/agda/command-serialization.test.ts
  - test/unit/agda/completeness.test.ts
  - test/unit/agda/process-termination.test.ts
  - test/unit/session/tool-recommendation.test.ts
  - test/unit/tools/dogfood-agent-log-selection.test.ts
  - test/unit/tools/dogfood-upload-run.test.ts
  - test/unit/tools/dogfood-wrapup-filing.test.ts
  - test/unit/tools/dogfood-wrapup-upload-chain.test.ts
  - test/unit/tools/emit-regression.test.ts
  - test/unit/tools/oracle-orcl-01.test.ts
  - test/unit/tools/oracle-orcl-02.test.ts
  - test/unit/tools/oracle-orcl-03.test.ts
  - test/unit/tools/oracle-run-oracle.test.ts
  - test/unit/tools/output-schema-invariants.test.ts
  - test/unit/tools/team-issue-key.test.ts
autonomous: true
requirements: [DEBT-06]

must_haves:
  truths:
    - "Per D-02, all 20 files owned by this plan compile cleanly under npx tsc -p tsconfig.test.json --noEmit -- zero errors, not just the two error families named in the original v1.0 audit"
    - "The @ts-expect-error placement fix reuses this repo's own working precedent (test-all-continuing.test.ts), not a novel workaround"
    - "W5's loosely-typed fix-queue mock in dogfood-wrapup-filing.test.ts is validated against the real fixQueueEntrySchema"
  artifacts:
    - path: "test/unit/tools/output-schema-invariants.test.ts"
      provides: "profiling-complete LoadResult/TypeCheckResult test fixtures"
      contains: "profiling"
  key_links: []
---

<objective>
Close the bulk of DEBT-06. As of 2026-07-04, `npx tsc -p tsconfig.test.json --noEmit` reports 95 errors across 21 files (grown from the audit's 2 named families via post-Phase-6/7 drift). Per D-02 the scope is "fully clean." This plan fixes 20 of 21 files via fully-diagnosed, mechanical, type-only edits. The 21st file (`agda-transport.test.ts`) is deferred to plan `09-06` -- its mock-type errors are entangled with a file CONCERNS.md flags as fragile, and gets isolated treatment there. This plan also folds in W5 (dogfood-wrapup-filing.test.ts's loosely-typed fix-queue mock), since it already owns that file.

Purpose: the permanent CI gate added in `09-06` is only meaningful once its backlog is cleared.
Output: 20 files with zero tsc errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-residual-v1-0-debt-sweep/09-CONTEXT.md
</context>

<interfaces>
<!-- Diagnosed against LIVE tsc output 2026-07-04. Re-run tsc first (state may drift further) -- fix any new error the same way, never skip. -->

Relevant src/ types (unchanged, for context): `src/tools/manifest.ts:44` `ToolManifestEntry.requiresLoadedSession: boolean` (required). `src/agda/types.ts:145,312` `LoadResult.profiling`/`TypeCheckResult.profiling: string | null` (required). `src/agda/session-capture/artifact-types.ts:21` `ReplayManifest` has no index signature (by design). `src/reporting/bug-report.ts:73-75` `fingerprintBugReport`'s param type excludes `title` (by design). `src/session/session-snapshot.ts:66` `SnapshotInput.projectRootExists: boolean` (required).

FAMILY A (add `profiling: null`): `typecheck-disposable.ts:26-36` (use `result.profiling`, the real value, not `null`) -- `completeness.test.ts:46,58` -- `output-schema-invariants.test.ts:150,151,163,175,208,253,254` (7 sites).

FAMILY B (add `requiresLoadedSession: <bool>` to every entry, ~13 entries each): `tool-recommendation.property.test.ts:32-46`, `tool-recommendation.test.ts` (unit, `:12-26` array + used at `:35,124`). Per manifest.ts's doc comment: `session`/`reporting` category -> `false`; `proof`/`process`/`navigation` -> `true`. Keep both files' mapping consistent.

FAMILY C (`@ts-expect-error` placement, 8 files): TS only suppresses the line immediately below the directive. A multi-line `import {\n...\n} from "x.mjs";` reports its error on the CLOSING `} from` line, not the `import {` line, so the directive (placed above `import {`) goes unused while the real error is unsuppressed. Working precedent already in this repo: `test-all-continuing.test.ts:8-9` places the comment directly above `} from "...";`. Apply that exact move (locate by comment text + its import, re-verify current line numbers first) in: `dogfood-agent-log-selection.test.ts`, `dogfood-upload-run.test.ts`, `dogfood-wrapup-filing.test.ts`, `emit-regression.test.ts`, `oracle-orcl-01.test.ts`, `oracle-orcl-02.test.ts`, `oracle-orcl-03.test.ts`, `team-issue-key.test.ts`.

FAMILY D (`ReplayManifest` typed as `Record<string,unknown>`, 3 files, identical shape): each has `interface FakeArtifact { manifest: Record<string,unknown>; ... }` and `function baseArtifact(overrides: { manifest?: Record<string,unknown>; ... })`. Change to `manifest: ReplayManifest` / `manifest?: Partial<ReplayManifest>`, add `import type { ReplayManifest } from "../../../src/agda/session-capture/artifact-types.js";`. Files: `oracle-orcl-01.test.ts:64-75`, `oracle-orcl-03.test.ts:69-81`, `oracle-run-oracle.test.ts:72-85` (this one does NOT need the family-C fix, its import is already single-line).

FAMILY E (mock arity collapses inference to `unknown`/`[]`): `dogfood-upload-run.test.ts:190-197,217-221` -- `const staged = await withEnvOverride(...)` infers `unknown`; cast the awaited value to `{ stagingDir: string; cleanup: () => void }` at both call sites (fixes all 10 "'staged' is unknown" errors). `dogfood-upload-run.test.ts:504` -- `vi.fn(async () => ({ok,status}))` has 0 params so `.mock.calls[0]` is `[]`; give it two ignored params `(_url: string, _init?: unknown)` (fixes the `:509-510` tuple-cast errors). `dogfood-wrapup-filing.test.ts:264,280` -- same cause; give `runOracleFn` two ignored params `(_capturePath?: string, _options?: unknown)` (fixes `:274,275,288`). `dogfood-wrapup-upload-chain.test.ts:45` -- `vi.fn(() => fakeChild)`; give it three REQUIRED (non-optional) ignored params `(_command: string, _args: string[], _options: { shell?: boolean })` (required, not optional, so the destructure at `:50` AND the `.shell` access at `:53` both resolve -- this is type-only, JS never enforces mock arity at runtime).

FAMILY F (one-offs): `capture-regression-runner.ts:102` -- `result.structuredContent` is `unknown`; cast in two steps (`as { data: unknown }` then `.data as {...}` using the existing inner literal shape). `bug-report.property.test.ts:64` -- `fingerprintBugReport({...input, title: newTitle})` excess-property-checks against a type excluding `title`; assign the spread to a `const withNewTitle: BugReportBundleInput = {...}` first, then pass that variable (not a literal). `session-snapshot.property.test.ts:34-44` -- add `projectRootExists: fc.boolean(),` to the `fc.record({...})` generator. `command-serialization.test.ts:302,337,366` -- three `(async (_proc, _cmd, options) => {...}) as unknown as typeof session["transport"][...]` assignments; `as unknown as X` gives no contextual typing, so annotate all 3: `(_proc: unknown, _cmd: unknown, options: { armEscalation?: boolean } | undefined)`. `process-termination.test.ts:134-135` -- `... as typeof setTimeout` and `... as NodeJS.Timeout` both fail as single-step casts; change both to two-step `as unknown as X`, mirroring this file's own existing idiom. `dogfood-wrapup-filing.test.ts` W5 fold-in (separate from its family-C fix): its `FakeQueueEntry` interface (~line 87) and `upsertFn` mock are validated only against a locally-defined minimal shape (audit finding W5 -- a future required schema field would pass here and only break at runtime); import `fixQueueEntrySchema` from `../../fixtures/fix-queue.js` and add, in at least Test 1 and Test 7, an assertion that `fixQueueEntrySchema.parse(upsertFn.mock.calls[0][0])` does not throw (additive, keep `FakeQueueEntry` for existing field-access convenience).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix independent type-literal and cast defects (9 files)</name>
  <files>test/helpers/capture-regression-runner.ts, test/helpers/typecheck-disposable.ts, test/property/reporting/bug-report.property.test.ts, test/property/session/session-snapshot.property.test.ts, test/property/session/tool-recommendation.property.test.ts, test/unit/session/tool-recommendation.test.ts, test/unit/agda/completeness.test.ts, test/unit/agda/command-serialization.test.ts, test/unit/agda/process-termination.test.ts</files>
  <read_first>
    src/tools/manifest.ts, src/agda/types.ts, src/reporting/bug-report.ts, src/session/session-snapshot.ts, and each of the 9 files above at the line ranges named in FAMILY A, B, F
  </read_first>
  <action>
    Apply FAMILY A's typecheck-disposable.ts and completeness.test.ts items (output-schema-invariants.test.ts is Task 2's), FAMILY B, and FAMILY F's non-dogfood-wrapup-filing items, exactly per the interfaces section, one file at a time, rechecking tsc after each.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.test.json --noEmit > /tmp/tsc-09-04-t1.txt 2>&1; grep -c "typecheck-disposable\|tool-recommendation\|completeness.test\|command-serialization\|process-termination\|bug-report.property\|session-snapshot.property\|capture-regression-runner" /tmp/tsc-09-04-t1.txt</automated>
  </verify>
  <acceptance_criteria>The verify command prints 0. `npx vitest run test/unit/agda/completeness.test.ts test/unit/agda/command-serialization.test.ts test/unit/agda/process-termination.test.ts` passes with no new failures.</acceptance_criteria>
  <done>All 9 files compile cleanly; their test suites still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix @ts-expect-error placement, ReplayManifest typing, and mock-arity defects (11 files)</name>
  <files>test/unit/tools/dogfood-agent-log-selection.test.ts, test/unit/tools/dogfood-upload-run.test.ts, test/unit/tools/dogfood-wrapup-filing.test.ts, test/unit/tools/dogfood-wrapup-upload-chain.test.ts, test/unit/tools/emit-regression.test.ts, test/unit/tools/oracle-orcl-01.test.ts, test/unit/tools/oracle-orcl-02.test.ts, test/unit/tools/oracle-orcl-03.test.ts, test/unit/tools/oracle-run-oracle.test.ts, test/unit/tools/output-schema-invariants.test.ts, test/unit/tools/team-issue-key.test.ts</files>
  <read_first>
    test/unit/tools/test-all-continuing.test.ts (lines 1-10), src/agda/session-capture/artifact-types.ts, test/fixtures/fix-queue.ts, and each of the 11 files at the locations named in FAMILY C, D, E, and the W5 fold-in
  </read_first>
  <action>
    Apply FAMILY C to all 8 named files. Apply FAMILY D to oracle-orcl-01/03/run-oracle.test.ts. Apply FAMILY E to dogfood-upload-run/wrapup-filing/wrapup-upload-chain.test.ts. Apply FAMILY A's remaining output-schema-invariants.test.ts sites. Apply the W5 fold-in to dogfood-wrapup-filing.test.ts. Several files get more than one family (dogfood-upload-run: C+E; oracle-orcl-01: C+D; dogfood-wrapup-filing: C+E+W5) -- finish one file's fixes before moving on. Recheck tsc after each file.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.test.json --noEmit > /tmp/tsc-09-04-t2.txt 2>&1; grep -c "dogfood-agent-log-selection\|dogfood-upload-run\|dogfood-wrapup-filing\|dogfood-wrapup-upload-chain\|emit-regression\|oracle-orcl\|oracle-run-oracle\|output-schema-invariants\|team-issue-key" /tmp/tsc-09-04-t2.txt</automated>
  </verify>
  <acceptance_criteria>The verify command prints 0. `npx vitest run test/unit/tools/dogfood-wrapup-filing.test.ts test/unit/tools/dogfood-upload-run.test.ts test/unit/tools/oracle-orcl-01.test.ts` passes with no new failures. `grep -c "fixQueueEntrySchema" test/unit/tools/dogfood-wrapup-filing.test.ts` returns at least 1.</acceptance_criteria>
  <done>All 11 files compile cleanly; W5's schema-conformance gap is closed; existing suites pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Type-only test-code edits; no new runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-09-07 | Tampering | Type-only test edits accidentally altering runtime mock behavior undetected | mitigate | Diff review restricted to type annotations/imports/casts only; `npx vitest run` before/after each file for pass-count parity |
| T-09-08 | Information Disclosure | W5's loosely-typed fix-queue mock masking a future required-field regression | mitigate | Validate `upsertQueueEntry` mock calls against the real `fixQueueEntrySchema` |
| T-09-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | Zero new dependencies |
</threat_model>

<verification>
After both tasks, run `npx tsc -p tsconfig.test.json --noEmit` and confirm none of this plan's 20 files appear. Run `npm test`.
</verification>

<success_criteria>
- All 20 files in `files_modified` compile cleanly under `tsconfig.test.json`.
- The `@ts-expect-error` fix reuses this repo's own working precedent.
- W5's fix-queue mock is schema-validated.
- `npm test` passes.
</success_criteria>

<output>
Create `.planning/phases/09-residual-v1-0-debt-sweep/09-04-SUMMARY.md` when done
</output>
