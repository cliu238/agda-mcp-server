---
phase: 09-residual-v1-0-debt-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-cold-replay.mjs
  - scripts/promote-capture.mjs
  - scripts/dogfood/dogfood-run.mjs
  - scripts/oracle/orcl-01-differential.mjs
  - scripts/oracle/cold-agda-session.mjs
  - scripts/oracle/run-oracle.mjs
  - scripts/queue/intake.mjs
  - .planning/PROJECT.md
autonomous: true
requirements: [DEBT-01, DEBT-02]

must_haves:
  truths:
    - "Per D-04/D-05, scripts/verify-cold-replay.mjs and scripts/promote-capture.mjs no longer exist in the repository, and both deletions are recorded with zero-real-importer evidence in PROJECT.md's Key Decisions table"
    - "dogfood-run.mjs's real-time proxy no longer calls a dead-ended capture-promotion path, and run-report.json's stagedCaptures recording (owned entirely by transcript-writer.mjs, independent of the deleted call) is unaffected"
    - "No comment anywhere in scripts/ describes verify-cold-replay.mjs or promote-capture.mjs as if they still exist"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "DEBT-01 and DEBT-02 decision records with zero-importer evidence"
      contains: "DEBT-01"
  key_links:
    - from: "scripts/dogfood/dogfood-run.mjs"
      to: "scripts/dogfood/transcript-writer.mjs"
      via: "recorder.recordToClientLine populates stagedCaptures unconditionally, independent of the removed promoteCapture call"
      pattern: "recordToClientLine"
---

<objective>
Resolve DEBT-01 and DEBT-02 from the v1.0 audit: delete the orphaned `scripts/verify-cold-replay.mjs` (superseded by ORCL-01's `materializeCaptureEnvironment`, which already guards path traversal) and retire `scripts/promote-capture.mjs`'s dead-ended `.agda-mcp/captures/index.json` write (Phase 4 repointed `readDedupIndex()` to `test/fixtures/fix-queue.json`, so nothing reads that index anymore). `promote-capture.mjs` is NOT an orphan like `verify-cold-replay.mjs` — it is actively called from `dogfood-run.mjs`'s live proxy on every capture — so retiring it means deleting the script AND its one real call site, not leaving an inert no-op behind.

Purpose: Close two of the audit's five highest-value follow-ups (W1, W3) so this dead code and its dangling comment references don't silently carry into v1.2.
Output: Two deleted scripts, a cleaned-up `dogfood-run.mjs` call site, four scripts with corrected comments, and two new PROJECT.md decision records with evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-residual-v1-0-debt-sweep/09-CONTEXT.md
</context>

<interfaces>
<!-- Confirmed 2026-07-04 by direct source read + repo-wide grep. Executor should use these facts directly — no re-discovery needed. -->

From `scripts/dogfood/dogfood-run.mjs` (632 lines):
- Line 35: `import { promoteCapture } from "../promote-capture.mjs";` — the only real (non-comment) importer of `promote-capture.mjs` anywhere in the repo.
- Lines 11-12 (header comment): "...auto-persists every observed agda_capture_session result via the existing scripts/promote-capture.mjs..." — becomes false once this plan lands.
- Lines 356-402: the `fromServer.on("line", (line) => { const event = recorder.recordToClientLine(line); ... })` handler. `recorder.recordToClientLine(line)` (line 359) is called UNCONDITIONALLY and is the ONLY thing that populates `recorder`'s internal `stagedCaptures` array (confirmed in `transcript-writer.mjs`, independent of anything below it).
- Lines 376-401: `if (event?.isCaptureSession) { const staged = event.stagedCapture; if (staged) { void (async () => { try { await promoteCapture(staged.stagedPath); } catch (err) { ...stderr... } })(); } else { ...stderr... } }` — this ENTIRE block exists only to call the now-retired `promoteCapture`. Deleting it does not touch `stagedCaptures`/run-report.json population (that already happened at line 359, before this block runs).

From `test/helpers/repo-root.ts` / repo-wide grep (2026-07-04), confirmed zero real (non-comment) importers of either target file outside the one call site above:
- `scripts/verify-cold-replay.mjs`: referenced only in comments at `scripts/promote-capture.mjs:17`, `scripts/oracle/orcl-01-differential.mjs:22`, `scripts/oracle/cold-agda-session.mjs:11` and `:48`.
- `scripts/promote-capture.mjs`: referenced in comments at `scripts/oracle/run-oracle.mjs:260`, `scripts/queue/intake.mjs:33`, `scripts/dogfood/install-dogfood-skill.mjs:31,52` (this file is edited by a LATER plan in this phase for a different reason — do not touch it here to avoid a cross-plan file conflict), plus one comment in `src/tools/register-capture-session.ts:50` (also edited by a later plan in this phase — do not touch it here either).
- Neither script has a `test/` file exercising it (confirmed: `grep -rln "verify-cold-replay\|promote-capture" test/` returns zero hits) — no test deletions needed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete verify-cold-replay.mjs and promote-capture.mjs, and remove promote-capture.mjs's one real call site</name>
  <files>scripts/verify-cold-replay.mjs, scripts/promote-capture.mjs, scripts/dogfood/dogfood-run.mjs</files>
  <read_first>
    scripts/verify-cold-replay.mjs
    scripts/promote-capture.mjs
    scripts/dogfood/dogfood-run.mjs (lines 1-40, 340-405)
    scripts/dogfood/transcript-writer.mjs (lines 85-220, to confirm stagedCaptures independence)
    .planning/milestones/v1.0-phases/01-capture-foundation/01-07-PLAN.md
  </read_first>
  <action>
    Re-verify the zero-real-importer evidence in &lt;interfaces&gt; is still current (repo state can drift between planning and execution): run the two grep commands in acceptance_criteria BEFORE deleting anything. If either grep surfaces a NEW real (non-comment) importer not listed above, STOP and report it instead of deleting — do not silently reduce scope.

    Once confirmed: `git rm scripts/verify-cold-replay.mjs scripts/promote-capture.mjs`.

    In `scripts/dogfood/dogfood-run.mjs`:
    1. Remove the import line `import { promoteCapture } from "../promote-capture.mjs";` (line 35).
    2. Remove the entire `if (event?.isCaptureSession) { ... }` block (lines 376-401) verbatim — do NOT touch anything above line 376 (in particular, leave `const event = recorder.recordToClientLine(line);` at line 359 and the `if (process.stdout.writable) { ... }` / `if (event) { void scheduleReportWrite(...); }` blocks at lines 360-374 completely unchanged — those own `stagedCaptures`/run-report.json population and are independent of this deletion).
    3. Update the header comment (lines 11-12) to drop the now-false "...auto-persists every observed agda_capture_session result via the existing scripts/promote-capture.mjs..." clause — rephrase to state plainly that the proxy records an unbounded transcript + per-tool-call run report via `transcript-writer.mjs`, with no mention of promotion.
  </action>
  <acceptance_criteria>
    Before deleting: `grep -rn "verify-cold-replay" --include="*.mjs" --include="*.ts" /Users/eric/projects6/agda-mcp-server/scripts /Users/eric/projects6/agda-mcp-server/src /Users/eric/projects6/agda-mcp-server/test | grep -v "^.*:.*//"` shows only comment-adjacent lines (manually confirm none are real `import`/`require` statements); same check for `"promote-capture\|promoteCapture"` shows exactly one real import (`dogfood-run.mjs:35`, about to be removed).
    After: `git status` shows both files deleted; `ls scripts/verify-cold-replay.mjs scripts/promote-capture.mjs` reports "No such file or directory" for both; `node --check scripts/dogfood/dogfood-run.mjs` exits 0 (no syntax error introduced); `grep -c "promoteCapture\|isCaptureSession" scripts/dogfood/dogfood-run.mjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/dogfood/dogfood-run.mjs && npx vitest run test/unit/tools/dogfood-run-spawn-options.test.ts test/unit/tools/dogfood-transcript-writer.test.ts</automated>
  </verify>
  <done>Both dead scripts are deleted, dogfood-run.mjs no longer imports or calls promoteCapture, and run-report.json's stagedCaptures behavior (owned by transcript-writer.mjs) is verified unaffected by the existing test suite.</done>
</task>

<task type="auto">
  <name>Task 2: Fix dangling comment references and record both decisions in PROJECT.md</name>
  <files>scripts/oracle/orcl-01-differential.mjs, scripts/oracle/cold-agda-session.mjs, scripts/oracle/run-oracle.mjs, scripts/queue/intake.mjs, .planning/PROJECT.md</files>
  <read_first>
    scripts/oracle/orcl-01-differential.mjs (lines 18-26)
    scripts/oracle/cold-agda-session.mjs (lines 1-15, 44-52)
    scripts/oracle/run-oracle.mjs (lines 256-264)
    scripts/queue/intake.mjs (lines 28-38)
    .planning/PROJECT.md (Key Decisions table, current last rows around lines 97-112)
  </read_first>
  <action>
    In each of the four script files, rephrase the comment that names `verify-cold-replay.mjs` or `promote-capture.mjs` so it no longer implies the file still exists:
    - `orcl-01-differential.mjs:22`: "the seed this plan supersedes" — keep the historical note but mark it explicitly as deleted (e.g. append "(deleted 2026-07, see PROJECT.md Key Decisions)"), do not leave a bare live-sounding filename reference.
    - `cold-agda-session.mjs:11` and `:48`: both describe "generalizes/reuses `scripts/verify-cold-replay.mjs`'s `runColdLoad()`" — rephrase to describe the multi-command lifecycle directly (what it does) without naming a file that no longer exists; a brief "(originally generalized from a since-deleted single-command cold-replay script)" is acceptable if it preserves the historical rationale.
    - `run-oracle.mjs:260`: "matches this project's own promote-capture.mjs precedent for this exact category of file" — rephrase to describe the pattern inline ("single-writer, out-of-band, best-effort `appendFileSync`") without the dangling cross-file pointer.
    - `queue/intake.mjs:33`: "Mirrors scripts/promote-capture.mjs's readExistingIndex guard" — rephrase to describe the guard behavior inline ("absent/malformed file degrades to an empty value, never throws") without the dangling cross-file pointer.

    In `.planning/PROJECT.md`, append two new rows to the existing Key Decisions table (same `| Decision | Rationale | Outcome |` three-column format as the existing rows), after the current last row:
    - DEBT-01 row: Decision = "Delete scripts/verify-cold-replay.mjs (v1.0 audit W3)"; Rationale = "Zero real importers (re-verified 2026-07 by repo-wide grep); functionally superseded by ORCL-01's materializeCaptureEnvironment, which already guards path traversal that verify-cold-replay.mjs's own CR-01 finding left unguarded"; Outcome = "Decided (v1.1) — closes unexecuted plan 01-07 (CR-01/CR-02) as superseded-by-deletion".
    - DEBT-02 row: Decision = "Delete scripts/promote-capture.mjs and its dogfood-run.mjs call site (v1.0 audit W1)"; Rationale = "Phase 4 repointed readDedupIndex() to test/fixtures/fix-queue.json, so nothing has read .agda-mcp/captures/index.json since; the write was calling out to a file with zero readers"; Outcome = "Decided (v1.1)".
  </action>
  <acceptance_criteria>
    `grep -rn "verify-cold-replay\.mjs\|promote-capture\.mjs" /Users/eric/projects6/agda-mcp-server/scripts` returns zero matches anywhere in the repo's scripts tree (the two remaining known comment mentions in `install-dogfood-skill.mjs` and `register-capture-session.ts` are explicitly out of this plan's file scope — verify they are NOT touched by this task via `git diff --stat`).
    `grep -c "DEBT-01" .planning/PROJECT.md` and `grep -c "DEBT-02" .planning/PROJECT.md` both return at least 1.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All four scripts' comments accurately describe current reality with no dangling references to deleted files, and PROJECT.md's Key Decisions table records both DEBT-01 and DEBT-02 with evidence; full test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan deletes internal dead-code tooling and corrects documentation; it introduces no new trust boundary or runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-09-01 | Tampering | Deleting a script that might have a hidden/dynamic importer not caught by a static grep | mitigate | Grep across `scripts/`, `src/`, `test/` for both the literal filenames AND the exported function names (`promoteCapture`, `verifyColdReplay`/`scriptMain`) before deletion (Task 1's pre-check); full `npm test` run after deletion as a dynamic-import safety net |
| T-09-02 | Repudiation | A debt-sweep deletion decision made with no recorded audit trail | mitigate | PROJECT.md Key Decisions table entries citing zero-importer evidence and the superseding mechanism (D-04, D-05) |
| T-09-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | Zero new dependencies added by this plan — Package Legitimacy Gate not triggered |
</threat_model>

<verification>
Run the full `npm test` suite after both tasks to confirm zero regressions from the deletions, call-site removal, and comment rewrites. Manually spot-check `git log -1 --stat` shows exactly the 8 files in `files_modified` (2 deleted, 6 modified).
</verification>

<success_criteria>
- `scripts/verify-cold-replay.mjs` and `scripts/promote-capture.mjs` no longer exist.
- `scripts/dogfood/dogfood-run.mjs` no longer imports or calls `promoteCapture`; its own tests and `dogfood-transcript-writer.test.ts` still pass.
- Zero comments anywhere in `scripts/` reference either deleted file as if it still exists.
- PROJECT.md's Key Decisions table has new rows citing DEBT-01 and DEBT-02 with evidence.
- `npm test` passes.
</success_criteria>

<output>
Create `.planning/phases/09-residual-v1-0-debt-sweep/09-01-SUMMARY.md` when done
</output>
