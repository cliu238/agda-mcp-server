---
phase: 09-residual-v1-0-debt-sweep
plan: 06
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05"]
files_modified:
  - test/unit/session/agda-transport.test.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [DEBT-06, DEBT-07]

must_haves:
  truths:
    - "Per D-02/D-03, npx tsc -p tsconfig.test.json --noEmit exits 0 with zero errors across the entire repository, and a permanent CI step enforces this on every future push/PR"
    - "Per D-09, the .planning/codebase/ map refresh (DEBT-07) is explicitly deferred to the orchestrator's /gsd:map-codebase run, not silently dropped"
    - "agda-transport.test.ts's fixes are type-only: the test suite's pass count is identical before and after"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Permanent typecheck:test CI gate"
      contains: "typecheck:test"
    - path: "package.json"
      provides: "typecheck:test npm script"
      contains: "typecheck:test"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "the verify job's new Typecheck tests step runs npm run typecheck:test"
      pattern: "typecheck:test"
---

<objective>
Finish DEBT-06 and close DEBT-07. This is the phase finale: it depends on every other plan in Phase 9 because (a) its full-repo tsc verification is only meaningful once all other plans' edits have landed, and (b) DEBT-07's map-codebase refresh is explicitly meant to run last, after all of this phase's code changes are in, so it captures a final, non-immediately-stale snapshot.

Task 1 fixes `test/unit/session/agda-transport.test.ts`'s remaining 12 tsc errors -- deliberately isolated here rather than bundled into plan `09-04`'s 20-file sweep, because this file's mock objects encode previously-fixed timing races (per `.planning/codebase/CONCERNS.md`'s explicit fragility warning) and the fix must be provably type-only. Task 2 confirms the whole repo is clean, adds the permanent CI gate (D-03), and records DEBT-07's orchestrator-level deferral (D-09, mirroring how this phase already treats DEBT-05/07 as split between executor work and orchestrator work).

Purpose: without this plan, the other 5 plans' tsc fixes are a one-time cleanup, not a durable guarantee -- this is what makes it permanent.
Output: a clean agda-transport.test.ts, a repo-wide zero-error tsc run, a new `typecheck:test` npm script wired into CI, and an explicit DEBT-07 deferral note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-residual-v1-0-debt-sweep/09-CONTEXT.md
@.planning/codebase/CONCERNS.md
</context>

<interfaces>
<!-- Confirmed 2026-07-04 against live tsc output. All 12 errors are in ONE file, at lines 359-360, 423-424, 467-474, 525-526, 547, 574-575, 681, 688. -->

Every error is one of two shapes, repeated across several distinct fake-ChildProcess object literals in this file: (1) a `stdin: { write(): void }` (or similar) inline type does not structurally satisfy `Writable.write`'s overloaded, `boolean`-returning signature once intersected with `Partial<ChildProcess>`; (2) a `once(event: string, listener: (...args: unknown[]) => void): unknown` (or similar) inline type does not structurally satisfy `ChildProcess`'s own overloaded, event-name-specific `once`/`on` signatures once intersected. Line 547's `TS2349 "This expression is not callable ... never"` on `closeListener?.();` is very likely a knock-on effect of one of these upstream mock-shape collapses -- re-run tsc after each block's fix rather than assuming it needs an independent fix.

This is exclusively a TYPE ANNOTATION problem: `npm test`'s vitest run does not type-check (esbuild transpile only), so these mocks already work correctly at runtime today -- the fix must not change that.

Current package.json has no `typecheck`-family script. The `build` script is `"tsc -p tsconfig.json && node scripts/copy-json-assets.mjs"` -- the new script follows the same `tsc -p <config> --noEmit` shape but against `tsconfig.test.json` with `--noEmit` (no build artifacts needed for a pure type-check).

Current `.github/workflows/ci.yml`'s `verify` job (lines 8-29) runs on `ubuntu-latest` with no Agda install: checkout -> setup-node -> `npm ci` -> `npm audit --audit-level=high` -> `npm run verify`. This is the right home for a fast, Agda-free static check (add a new step, not a new parallel job, per this plan's own CI-wiring discretion) -- insert it right after "Install dependencies" and before "Audit dependencies" so a type regression fails fast, before the slower audit/build/pack steps run.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix agda-transport.test.ts's remaining mock-shape type errors (type-only, zero runtime change)</name>
  <files>test/unit/session/agda-transport.test.ts</files>
  <read_first>
    test/unit/session/agda-transport.test.ts (full file -- required, given 12 scattered error sites across several distinct mock object literals)
    .planning/codebase/CONCERNS.md ("AgdaTransport idle/timeout/terminus state machine" fragility section)
  </read_first>
  <action>
    Record a baseline: `npx vitest run test/unit/session/agda-transport.test.ts` and note the exact passed/failed/skipped counts before making any change. For each fake-ChildProcess/fake-Writable mock object literal at the error lines named in the interfaces section, loosen ONLY its inline structural type annotation (the `stdin`/`write` and `once`/`on` shapes) to accept variadic, loosely-typed signatures (for example a `write` typed as accepting any args and returning any, and a `once`/`on` typed as accepting an event name and a variadic-args listener returning any) so the literal structurally satisfies its `Partial<ChildProcess> & {...}` intersection. Do not change any mock's actual JS body, return values, or control flow -- this is exclusively a change to the type annotations surrounding each literal. Investigate line 547's `TS2349` last, after the other fixes, and re-run tsc to see if it resolves as a side effect before treating it as independent.
  </action>
  <verify>
    <automated>npx vitest run test/unit/session/agda-transport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc -p tsconfig.test.json --noEmit 2>&1 | grep -c agda-transport` prints 0. The post-fix `npx vitest run test/unit/session/agda-transport.test.ts` reports the IDENTICAL passed/failed/skipped counts as the pre-fix baseline. `git diff test/unit/session/agda-transport.test.ts` contains no changed string literals, numeric literals, or function-body statements outside type-annotation positions (manually confirm this by reading the diff, not by tooling).
  </acceptance_criteria>
  <done>agda-transport.test.ts compiles cleanly with zero tsc errors, and its test suite's pass count is unchanged from before the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Full tsc-clean verification, permanent CI gate, and DEBT-07 deferral</name>
  <files>package.json, .github/workflows/ci.yml</files>
  <read_first>
    package.json (scripts block)
    .github/workflows/ci.yml (verify job, lines 8-29)
  </read_first>
  <action>
    Re-run `npx tsc -p tsconfig.test.json --noEmit` across the whole repository (unscoped). If it reports ANY error not already covered by plans `09-01` through `09-05` and this plan's Task 1 (freshness check -- new commits could have landed since planning), fix it now using the same type-only, minimal-diff discipline used throughout this phase; do not stop with a known-remaining error. Add a new npm script `"typecheck:test": "tsc -p tsconfig.test.json --noEmit"` to `package.json`'s `scripts` block, placed near `"build"`. Add a new step named "Typecheck tests" to `.github/workflows/ci.yml`'s existing `verify` job, positioned after "Install dependencies" and before "Audit dependencies", running `npm run typecheck:test` -- do not create a new parallel job. Run `npm test` and `npm run build` locally to confirm zero regressions across the entire phase's changes. Record DEBT-07's disposition explicitly in this task's own SUMMARY output (not in any source file): `.planning/codebase/` refresh via `/gsd:map-codebase` is an ORCHESTRATOR-level workflow (spawns its own specialized mapping subagents across STACK/ARCHITECTURE/CONVENTIONS/etc., outside a single execute-phase task's scope), deliberately NOT run by this plan, deferred to the phase orchestrator/user to invoke after all of Phase 9's plans are merged -- per D-09.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.test.json --noEmit; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    The verify command's last line is `EXIT:0` with zero error lines above it. `grep -n "typecheck:test" package.json` matches. `grep -n "Typecheck tests" .github/workflows/ci.yml` matches and appears (by line number) before "Audit dependencies". `npm test` and `npm run build` both exit 0.
  </acceptance_criteria>
  <done>The entire repository compiles cleanly under tsconfig.test.json, a permanent CI step enforces this going forward, and DEBT-07's orchestrator-level deferral is explicitly recorded rather than silently skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow configuration | An additive-only change to an existing job; no new permissions, secrets, or external actions introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-09-12 | Tampering | agda-transport.test.ts's fragile timing-sensitive mocks being subtly altered under "just make tsc pass" pressure | mitigate | Type-only diff constraint plus before/after test-count parity gate (Task 1's acceptance_criteria) |
| T-09-13 | Repudiation | A future test-code type regression going unnoticed -- the exact drift that grew this debt from 2 families to 95 errors | mitigate | Permanent `typecheck:test` CI step fails the build on any future regression |
| T-09-14 | Tampering (CI config) | A careless ci.yml edit widening permissions or removing an existing gate | mitigate | Additive-only change (one new step in the existing job); no permissions/secrets/existing steps touched |
| T-09-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | Zero new dependencies |
</threat_model>

<verification>
`npx tsc -p tsconfig.test.json --noEmit` exits 0 repo-wide. `npm test` and `npm run build` both pass. The new CI step is present and correctly positioned.
</verification>

<success_criteria>
- `agda-transport.test.ts` compiles cleanly with an unchanged test pass count.
- `npx tsc -p tsconfig.test.json --noEmit` exits 0 across the whole repository.
- `.github/workflows/ci.yml` runs `npm run typecheck:test` on every push/PR, positioned before the slower audit/build steps.
- DEBT-07's orchestrator-level deferral is recorded in this plan's SUMMARY.
- `npm test` and `npm run build` both pass.
</success_criteria>

<output>
Create `.planning/phases/09-residual-v1-0-debt-sweep/09-06-SUMMARY.md` when done
</output>
