---
phase: 09-residual-v1-0-debt-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md
autonomous: true
requirements: [DEBT-05]

must_haves:
  truths:
    - "Per D-01/D-08, a phase-scoped SECURITY.md (not the root vulnerability-disclosure policy) exists at .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md, consolidating Phase 5's process-spawning threat register and Phase 7's network-surface threat register in one pass"
    - "The upload retry queue's plaintext Bearer key storage (07-02-SUMMARY.md's Threat Flags entry) is consciously documented as an explicit trust-boundary widening, not silently folded into an unrelated disposition"
    - "Every T-05-* and T-07-* threat ID from the source PLAN.md files appears in the consolidated register — zero silently dropped"
  artifacts:
    - path: ".planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md"
      provides: "Consolidated retroactive threat register for Phases 5 and 7, per the project's SECURITY.md template"
      contains: "T-07-08"
  key_links: []
---

<objective>
Satisfy DEBT-05: the v1.0 audit found no `SECURITY.md` exists for any phase despite `workflow.security_enforcement: true`, flagging Phase 5 (process-spawning dogfooding/oracle scripts) as the minimum retroactive-review target. Per D-01's sequencing decision, this phase runs after Phase 7 landed the team-feedback network surfaces (ingest endpoint, upload client, cron judge write-back), so this review's scope is EXTENDED to cover both in one consolidated pass rather than reviewing Phase 5 alone and leaving Phase 7 for later. Per D-08, the existing root `SECURITY.md` is a vulnerability-disclosure policy and does NOT satisfy this requirement — the deliverable is a distinct, phase-scoped threat-model artifact.

This is a consolidation task, not new threat discovery: every threat below was already identified, mitigated, and verified during Phase 5 and Phase 7's own planning/review/verification cycles (each 05-0N-PLAN.md and 07-0N-PLAN.md's own threat_model block, and 07-REVIEW.md's resolved CR/WR/IN findings). This plan reads and synthesizes; it does not re-derive.

Purpose: Close the audit's "no SECURITY.md" gap and give the maintainer one place that answers "what are this server's actual trust boundaries and are they covered" without re-reading 10 separate phase documents.
Output: `.planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md`, following the project's SECURITY.md template, with a complete Threat Register, Accepted Risks Log, and Audit Trail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-residual-v1-0-debt-sweep/09-CONTEXT.md
@$HOME/.claude/get-shit-done/templates/SECURITY.md
</context>

<interfaces>
<!-- Threat ID inventory extracted 2026-07-04 directly from source PLAN.md STRIDE Threat Register tables. Executor should use this as the canonical checklist -- do not re-derive from scratch, just verify completeness against it. -->

Phase 5 (.planning/milestones/v1.0-phases/05-dogfooding-orchestration-fuel/), 9 threats:
- 05-01-PLAN.md: T-05-01-01 (Tampering, fuel-corpora.json/oracle-policy JSON, mitigate -- zod schema + 40-hex pinnedRef regex), T-05-01-02 (Tampering, task-manifest string fields, accept -- no path resolution in this plan), T-05-01-03 (Repudiation, thrown errors, accept -- library function, caller owns logging).
- 05-02-PLAN.md: T-05-02-01 (Tampering, dogfood-run.mjs child spawn, mitigate -- argv-array spawn, never shell), T-05-02-02 (Elevation of Privilege, a second AgdaSession, mitigate -- source-text regex test enforces zero import), T-05-02-03 (Information Disclosure, unbounded transcript, mitigate -- gitignored .agda-mcp/runs/), T-05-02-04 (Denial of Service, orphaned Agda child, mitigate -- finalize() always kills before exit), T-05-02-05 (Tampering, promoteCapture's dedup-index write, accept -- reused unchanged at the time; NOTE for this plan: promoteCapture itself is deleted by sibling plan 09-01 in this same phase, so this threat's COMPONENT no longer exists -- record it as "closed (component removed)", not "accept").
- 05-03-PLAN.md: T-05-03-01 (Information Disclosure, private-corpus source into FixQueueEntry, mitigate), T-05-03-02 (Tampering, materializeCaptureEnvironment temp-dir writes, accept -- reused unchanged, CR-01-hardened), T-05-03-03 (Repudiation, a real ORCL-02 cheat dropped by a co-occurring flaky ORCL-01 signal, mitigate -- strict precedence order), T-05-03-04 (Repudiation, an abstention misfiled as a confirmed defect, mitigate -- explicit-kind check only).
- 05-04-PLAN.md: T-05-04-01 (Tampering, install-dogfood-skill.mjs overwriting a real file, mitigate), T-05-04-02 (Information Disclosure, SKILL.md leaking private corpus source, accept).

Phase 7 (.planning/phases/07-team-feedback-channel-local-wiring/), 24 threat rows (T-07-01 through T-07-24, confirmed via grep -- numbers are non-contiguous across files by design):
- 07-01-PLAN.md: T-07-01..T-07-05 (key registry: hash-only storage, 0600 perms, no-cache re-read, timingSafeEqual, .gitignore entry -- all mitigate).
- 07-02-PLAN.md: T-07-06..T-07-09, T-07-SC (AppleDouble exclusion, fail-open DoS, full-fidelity local retry-queue disclosure [accept], no-record-of-failure, zero-new-deps).
- 07-03-PLAN.md: T-07-10..T-07-15 (Bearer auth, path traversal via person/runId, oversized/slow body DoS, no-extraction-at-ingest, loopback-only bind, no-audit-record [accept, out of ~100-line budget]).
- 07-04-PLAN.md: T-07-16..T-07-17 (upload-chain exit-code masking, taskManifestCorpora low-sensitivity disclosure [accept]).
- 07-05-PLAN.md: T-07-18..T-07-22 (tar-slip/path-traversal extraction, decompression-bomb DoS, git write-back credential scope [accept, LOCAL MODE ONLY -- explicitly flagged in its own row: "flagged for that phase's DEBT-05 security review, not re-litigated here" -- this plan's job is to RE-AFFIRM this disposition for local mode and note it stays open for Phase 8's cluster deployment, not to resolve it], unclear commit provenance, policyKey resolution silently degrading).
- 07-06-PLAN.md: T-07-23..T-07-24 (upload key visible in operator's own shell history [accept, single-operator machine], E2E acceptance run's own commit landing on main [accept, intended]).

Widened trust boundary NOT captured as its own threat ID (from 07-02-SUMMARY.md's "Threat Flags" section): retry-queue NDJSON entries in scripts/dogfood/upload-run.mjs store the plaintext AGDA_MCP_TEAM_UPLOAD_KEY (not just url/runId) alongside the already-accepted full-fidelity logs/captures living in the same .agda-mcp/team/ local directory. 07-02-SUMMARY.md explicitly says T-07-08's accept disposition covers the directory generally, but the plan's own literal entry-field list did not name "key", so this specific widening should be explicitly re-affirmed rather than assumed covered. This consolidation is where that re-affirmation happens.

Package Legitimacy Gate: not applicable -- this plan installs no packages and reviews no new dependency surface (Phase 5/7 already covered their own zero-new-deps threats above).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build the consolidated Phase 5 + Phase 7 threat register</name>
  <files>.planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md</files>
  <read_first>
    $HOME/.claude/get-shit-done/templates/SECURITY.md
    .planning/milestones/v1.0-phases/05-dogfooding-orchestration-fuel/05-01-PLAN.md (threat_model block)
    .planning/milestones/v1.0-phases/05-dogfooding-orchestration-fuel/05-02-PLAN.md (threat_model block)
    .planning/milestones/v1.0-phases/05-dogfooding-orchestration-fuel/05-03-PLAN.md (threat_model block)
    .planning/milestones/v1.0-phases/05-dogfooding-orchestration-fuel/05-04-PLAN.md (threat_model block)
    .planning/phases/07-team-feedback-channel-local-wiring/07-01-PLAN.md through 07-06-PLAN.md (threat_model blocks)
    .planning/phases/07-team-feedback-channel-local-wiring/07-02-SUMMARY.md (Threat Flags section)
  </read_first>
  <action>
    Create `.planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md` using the SECURITY.md template's structure (frontmatter, Trust Boundaries, Threat Register, Accepted Risks Log, Security Audit Trail, Sign-Off). Set frontmatter `phase: 09`, `asvs_level: 1`, `created:` today's date.

    Populate Trust Boundaries with the union of both phases' boundaries: local process spawning (dogfooding/oracle scripts), teammate's local machine to network to ingest server, and the ingest server's local storage root.

    Populate the Threat Register with all 14 Phase-5 rows and all 25 Phase-7 rows (including T-07-SC) from the interfaces section above, copying Threat ID / Category / Component / Disposition / Mitigation verbatim from their source files, with Status = "closed" for every row (each was already verified during its own phase's VERIFICATION.md). Apply the ONE noted correction: T-05-02-05's component (promoteCapture's dedup-index write) no longer exists after sibling plan 09-01 deletes it this same phase -- mark it "closed (component removed 2026-07, DEBT-02)" rather than a bare "accept".

    Add ONE new row for the plaintext-Bearer-key widening (not present as its own ID in any source file): assign it T-07-08b, Category = Information Disclosure, Component = scripts/dogfood/upload-run.mjs's retry-queue NDJSON entries, Disposition = accept, Mitigation/rationale = "Same local, single-user trust boundary as T-07-08 (trusted internal team, one-time consent, full-fidelity by design); explicitly re-affirmed here per 07-02-SUMMARY.md's own flag that the plan's literal field list omitted the key field from its accepted-disclosure enumeration."

    In the Accepted Risks Log, carry forward T-07-20 (git write-back credential scope, accepted for LOCAL mode) verbatim, explicitly noting it remains open for Phase 8's k8s deployment (not resolved by this plan) -- cite the source row's own words: "flagged for that phase's DEBT-05 security review, not re-litigated here."
  </action>
  <verify>
    <automated>grep -c "^| T-05-" .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    `grep -c "^| T-05-" .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md` equals 14. `grep -c "^| T-07-" .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md` equals 26 (25 original, including T-07-SC, + the new T-07-08b). `grep -n "plaintext" .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md` finds the AGDA_MCP_TEAM_UPLOAD_KEY entry. Frontmatter `threats_open: 0`.
  </acceptance_criteria>
  <done>09-SECURITY.md exists with a complete 40-row threat register (14 Phase-5 + 26 Phase-7, including the newly-named plaintext-key widening) and an Accepted Risks Log carrying forward T-07-20's local-mode-only disposition.</done>
</task>

<task type="auto">
  <name>Task 2: Verify completeness against source files and sign off</name>
  <files>.planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md</files>
  <read_first>
    .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md (as drafted in Task 1)
  </read_first>
  <action>
    Extract every threat ID token matching the shape "T-05-..." or "T-07-..." from the 10 source PLAN.md files listed in Task 1's read_first via grep, save the sorted unique list to a scratch file, then run the same extraction against the newly-written 09-SECURITY.md and save that sorted unique list to a second scratch file. Diff the two lists (e.g. `diff file1 file2` or `grep -vxFf`). Any ID present in the source list but absent from the 09-SECURITY.md list is a gap -- go back to Task 1's output and add it; do not proceed with a known gap. Fill in the Security Audit Trail table's one row (today's date, Threats Total = 34, Closed = 34, Open = 0, Run By = "Claude (DEBT-05 consolidation, Phase 9)"). Check every Sign-Off box (all threats have a disposition; accepted risks are documented; threats_open: 0) and set frontmatter `status: verified`.
  </action>
  <verify>
    <automated>grep -oE "T-0[57]-[0-9A-Za-z-]+" .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md | sort -u | wc -l</automated>
  </verify>
  <acceptance_criteria>
    The scratch-file diff from source PLAN.md files to 09-SECURITY.md shows zero lines present only in the source list (every source threat ID appears in 09-SECURITY.md). `grep -n "status: verified" .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md` matches. `grep -n "threats_open: 0" .planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md` matches.
  </acceptance_criteria>
  <done>Every source threat ID is confirmed present in 09-SECURITY.md via an explicit diff check, the Audit Trail and Sign-Off sections are complete, and frontmatter status is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This document's own completeness | The consolidation itself is the control surface -- an incomplete register would misrepresent actual coverage to future readers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-09-05 | Repudiation | 09-SECURITY.md itself | mitigate | Task 2's explicit source-to-output ID diff ensures no source threat ID is silently dropped during consolidation |
| T-09-06 | Information Disclosure | Consolidated doc restates the plaintext-Bearer-key and accepted-log-disclosure trust boundaries | accept | These are pre-existing, already-accepted v1.1 risks (T-07-08, T-07-08b, T-07-17, T-07-23); this plan documents, it does not introduce, them |
| T-09-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | Zero new dependencies -- pure documentation synthesis |
</threat_model>

<verification>
Run the Task 2 diff check to confirm zero source threat IDs are missing from the consolidated register. Manually skim the final 09-SECURITY.md once for readability (a maintainer should be able to answer "what are this server's trust boundaries" without opening any other file).
</verification>

<success_criteria>
- `.planning/phases/09-residual-v1-0-debt-sweep/09-SECURITY.md` exists with all 14 Phase-5 and 26 Phase-7 (25 original incl. T-07-SC, + T-07-08b) threat rows.
- The plaintext-Bearer-key-in-retry-queue widening is explicitly documented as T-07-08b.
- T-07-20's local-mode-only acceptance is carried forward, flagged for Phase 8 re-review, not silently resolved.
- Frontmatter shows `threats_open: 0` and `status: verified`.
</success_criteria>

<output>
Create `.planning/phases/09-residual-v1-0-debt-sweep/09-03-SUMMARY.md` when done
</output>
