---
phase: 08-pinned-environment-distribution-thin-k8s-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - k8s/deployment.yaml
  - k8s/ingress.yaml
  - k8s/cronjob.yaml
  - k8s/team-keys-secret.yaml.template
autonomous: true
requirements: [DEPLOY-01]

must_haves:
  truths:
    - "The image builds Node + a real, version-verified Agda 2.8.0 + all 4 pinned fuel-corpora source clones — no _build caches (D-10, CACHE theme deleted)"
    - "The container runs as non-root UID/GID 2231 everywhere (Ceph ownership requirement) — never root, never a default UID"
    - "The ingest Deployment and cron-judge CronJob together fit within the measured ~3 CPU / 7Gi memory quota headroom in llm-gateway, with an explicit documented margin"
    - "The cron-judge CronJob is wired so NO repo-write git credential is ever needed on the cluster (D-09) — queue accumulation targets a PVC path outside the repo checkout, with --no-push as explicit belt-and-suspenders"
    - "Plan actions cite D-04 (fuel-root reuse), D-07, D-08, D-09, D-10 for traceability (grep -c 'D-[0-9][0-9]' .planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-02-PLAN.md returns >= 5)"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build: haskell:9.10-bookworm (cabal-installs Agda 2.8.0, GHC 9.10.3 verified tested-with) -> node:24-slim final stage with git, non-root UID 2231, baked-in fuel corpora, npm ci (no --omit=dev)"
    - path: "k8s/deployment.yaml"
      provides: "Deployment agda-mcp-ingest + Service agda-mcp-ingest, namespace llm-gateway, port 4000, PVC subPath mount, team-keys Secret mount"
    - path: "k8s/ingress.yaml"
      provides: "Ingress on dev.sites.idies.jhu.edu path /agda-mcp, proxy-body-size 512m matching TEAM-03's cap"
    - path: "k8s/cronjob.yaml"
      provides: "CronJob agda-mcp-cron-judge sharing the same image + PVC, GHCRTS memory cap, --queue-path outside repo + --no-push (D-09)"
  key_links:
    - from: "k8s/deployment.yaml"
      to: "scripts/team/ingest-server.mjs"
      via: "env AGDA_MCP_TEAM_STORAGE_DIR=/data/team-uploads, AGDA_MCP_TEAM_INGEST_PORT=4000, AGDA_MCP_TEAM_INGEST_HOST=0.0.0.0, AGDA_MCP_TEAM_KEYS_PATH=/etc/agda-mcp-team/team-keys.json"
      pattern: "AGDA_MCP_TEAM_STORAGE_DIR"
    - from: "k8s/cronjob.yaml"
      to: "scripts/team/cron-ingest-wrapup.mjs"
      via: "args: [\"scripts/team/cron-ingest-wrapup.mjs\", \"--queue-path\", \"/data/cluster-queue/fix-queue.json\", \"--no-push\"]"
      pattern: "--no-push"
    - from: "Dockerfile"
      to: "scripts/team/clone-fuel-corpora.mjs"
      via: "RUN --mount=type=secret,id=fuel_corpora_token node scripts/team/clone-fuel-corpora.mjs --root \"$AGDA_MCP_FUEL_ROOT\""
      pattern: "clone-fuel-corpora\\.mjs"
---

<objective>
Produce the packaging artifacts DEPLOY-01 names literally: a `linux/amd64`-buildable Dockerfile containing Node + pinned Agda 2.8.0 + all 4 pinned fuel-corpus source clones (no caches), and the k8s manifests (Deployment/Service/Ingress/CronJob) targeting the ALREADY-VERIFIED `llm-gateway` namespace and `sciserver-datavolumes-01-rw` PVC per the live cluster facts in `.claude/skills/agda-mcp-k8s-deploy/SKILL.md`. Everything in this plan is buildable/lintable locally — no cluster credentials, no live cluster mutation.

Purpose: this is the packaging half of DEPLOY-01; 08-04 wires CI/CD around these exact artifacts and performs the first real deploy.
Output: `Dockerfile`, `.dockerignore`, `k8s/deployment.yaml`, `k8s/ingress.yaml`, `k8s/cronjob.yaml`, `k8s/team-keys-secret.yaml.template`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-CONTEXT.md
@.claude/skills/agda-mcp-k8s-deploy/SKILL.md

@/Users/eric/projects6/litellm/.claude/skills/litellm-k8s-deploy/assets/k8s/deployment.yaml
@/Users/eric/projects6/litellm/.claude/skills/litellm-k8s-deploy/assets/k8s/ingress.yaml
@scripts/team/ingest-server.mjs
@scripts/team/cron-ingest-wrapup.mjs
@scripts/data/fuel-corpora.json

<interfaces>
<!-- Verified facts this plan's manifests MUST match exactly — see SKILL.md for provenance. -->

Cluster facts (live-verified 2026-07-04, `.claude/skills/agda-mcp-k8s-deploy/SKILL.md`):
- Namespace: `llm-gateway` (already exists, reused — do NOT create a namespace.yaml).
- PVC: `sciserver-datavolumes-01-rw`, CephFS (fs.rename-safe), RWX, 4.6TB free — reuse via `subPath`, never create a new PVC.
- Pod identity: `runAsUser: 2231`, `fsGroup: 2231` (else `Permission denied` on the Ceph mount).
- ResourceQuota `llm-gateway-compute`: hard 4 CPU / 8Gi memory / 5 pods; litellm already uses ~1 CPU / 1Gi -> **~3 CPU / 7Gi headroom** for everything this plan adds.
- URL: shared host `dev.sites.idies.jhu.edu`, own path `/agda-mcp`, own Ingress resource (litellm uses `/litellm` on the SAME host via ITS OWN separate Ingress — do not edit litellm's Ingress).
- Ingress non-negotiables: `nginx.ingress.kubernetes.io/proxy-body-size: "512m"` (matches TEAM-03's `AGDA_MCP_TEAM_INGEST_MAX_BYTES` default of 536870912 bytes), `pathType: ImplementationSpecific` (not `Prefix` — the path is a regex).

From scripts/team/ingest-server.mjs (env vars this plan's Deployment sets — file NOT modified by this plan):
```javascript
export function resolveTeamStorageDir() { /* AGDA_MCP_TEAM_STORAGE_DIR, default .agda-mcp/team/storage */ }
export function resolveIngestMaxBytes() { /* AGDA_MCP_TEAM_INGEST_MAX_BYTES, default 536_870_912 */ }
export function resolveIngestPort() { /* AGDA_MCP_TEAM_INGEST_PORT, default 8787 */ }
export function resolveIngestHost() { /* AGDA_MCP_TEAM_INGEST_HOST, default "127.0.0.1" (loopback) */ }
```
`GET /healthz` returns `200 "ok"` (unauthenticated) — this is the manifest's liveness/readiness probe path.

From scripts/team/issue-key.mjs:
```javascript
export function resolveKeysPath() { /* AGDA_MCP_TEAM_KEYS_PATH, default scripts/team/data/team-keys.json */ }
```
`readKeyRegistry` always re-reads from disk on every request (never caches) — a k8s Secret-volume update propagates to the NEXT request within the kubelet's own sync period (no pod restart required), consistent with TEAM-01's "revocation takes effect on the next request" invariant.

From scripts/team/cron-ingest-wrapup.mjs CLI (file NOT modified by this plan):
```
npx tsx scripts/team/cron-ingest-wrapup.mjs [--no-push] [--rerun-n <N>] [--storage-dir <path>] [--queue-path <path>]
```
`writeBackQueue` computes `relative(SERVER_REPO_ROOT, queueJsonPath)`; if that starts with `".."` (i.e. `queueJsonPath` is OUTSIDE the repo checkout), it returns `{ committed: false, skipped: "queue-path-outside-repo" }` and makes ZERO git calls — this is a structural guarantee, not just a flag. Pointing `--queue-path` at a PVC path (outside `/app`, the repo checkout root inside the container) combined with `--no-push` gives two independent, redundant enforcements of D-09.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile + .dockerignore — Node 24 + Agda 2.8.0 (cabal, GHC 9.10.3) + 4 pinned fuel corpora, non-root UID 2231</name>
  <files>Dockerfile, .dockerignore</files>
  <read_first>scripts/team/clone-fuel-corpora.mjs (Plan 08-01's output — reused verbatim; BEST-EFFORT read: in parallel wave-1 execution this file may not exist yet — the CLI contract `--root <path>` pinned in this plan's frontmatter is sufficient to author the Dockerfile text without it; the real integration runs in 08-04 after wave 1 completes), package.json, .github/workflows/ci.yml</read_first>
  <action>
    Create `Dockerfile` as a 2-stage BuildKit build. Add `# syntax=docker/dockerfile:1` as the literal first line (required for `--mount=type=secret`).

    Stage 1 (`agda-builder`): `FROM haskell:9.10-bookworm@sha256:c8c84efbcf7bd7106b94d8e83ae150489adb57116ec01d67cd547bb967fd9e69` — this exact digest was resolved live via `docker buildx imagetools inspect haskell:9.10-bookworm` on 2026-07-04 and carries GHC 9.10.3, which Hackage's own `Agda-2.8.0.cabal` `tested-with` field lists explicitly (verified — do not substitute a different GHC line without re-checking that field). Run `cabal update && cabal install Agda-2.8.0 --install-method=copy --installdir=/agda-bin --overwrite-policy=always -f-enable-cluster-counting` as one `RUN`. The `-f-enable-cluster-counting` flag disables Agda's optional `text-icu` dependency (only needed for LaTeX cluster-counting, irrelevant to a headless MCP judge) — this deliberately avoids a cross-Debian-release ICU ABI mismatch between this bullseye-unrelated bookworm build stage and the final stage below (document this exact rationale in a Dockerfile comment).

    Stage 2 (`final`): `FROM node:24-slim@sha256:b31e7a42fdf8b8aa5f5ed477c72d694301273f1069c5a2f71d53c6482e99a2fc` (same digest-pinning discipline — resolved live 2026-07-04; both stages are bookworm-based so no libc/ICU ABI mismatch survives the `COPY --from=agda-builder`). `apt-get update && apt-get install -y --no-install-recommends git ca-certificates && rm -rf /var/lib/apt/lists/*`. `COPY --from=agda-builder /agda-bin/agda /usr/local/bin/agda` and the same for `/agda-bin/agda-mode`. Create the fixed UID/GID: `groupadd -g 2231 agdamcp && useradd -u 2231 -g 2231 -m -d /home/agdamcp -s /usr/sbin/nologin agdamcp` (Ceph ownership requirement — UID 2231 is NOT a convention this Dockerfile invents, it is dictated by the cluster's existing Ceph directory ownership per the SKILL.md's live-verified facts). Set `ENV AGDA_MCP_FUEL_ROOT=/opt/agda-mcp-fuel`. `WORKDIR /app`. Copy `package.json`/`package-lock.json` first and run `npm ci` BEFORE `COPY . .` (layer-cache discipline: only re-run `npm ci` when the lockfile changes) — this `npm ci` has NO `--omit=dev` flag, because `tsx`/`typescript` are `devDependencies` that every `scripts/*.mjs` file needs at runtime, not just at build time (per `.planning/research/ARCHITECTURE.md` §2's explicit Dockerfile note — cite this). Then `COPY . .` and `RUN npm run build`. Then `RUN mkdir -p "$AGDA_MCP_FUEL_ROOT"` and, as its own `RUN --mount=type=secret,id=fuel_corpora_token` step, invoke `sh -c 'GH_TOKEN="$(cat /run/secrets/fuel_corpora_token 2>/dev/null || true)" node scripts/team/clone-fuel-corpora.mjs --root "$AGDA_MCP_FUEL_ROOT"'` — this reuses Plan 08-01's `clone-fuel-corpora.mjs` VERBATIM (D-04's clone convention, same script, different root value — the SAME "one code path, one env var, two values" pattern `AGDA_MCP_TEAM_STORAGE_DIR` already established) and the BuildKit secret mount keeps the private-repo token out of ENV/ARG/`docker history`; the token's OTHER escape path — git persisting the credentialed clone URL into each clone's .git/config, which IS layer content — is closed by 08-01's post-clone `remote set-url` scrub (verify inside the built image: `git -C /opt/agda-mcp-fuel/<key> config --get remote.origin.url` shows the clean URL). Finish with `RUN chown -R agdamcp:agdamcp /app "$AGDA_MCP_FUEL_ROOT"`, `USER agdamcp`, `EXPOSE 4000`, `CMD ["node", "dist/index.js"]`.

    Create `.dockerignore` excluding: `node_modules`, `dist`, `.git`, `.agda-mcp`, `coverage`, `.nyc_output`, `test-output`, `*.log`, `.planning`, `.claude`, `.codex`, `k8s`, `*.tgz`, `.DS_Store`.
  </action>
  <verify>
    <automated>docker build --check -f Dockerfile . 2>&1 | tee /tmp/agda-mcp-dockerfile-check.log; grep -qi "error" /tmp/agda-mcp-dockerfile-check.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    `docker build --check -f Dockerfile .` (BuildKit lint mode — no execution, no secrets/network needed) reports zero errors. `grep -c '^FROM' Dockerfile` returns `2`. `grep -q "USER agdamcp" Dockerfile`, `grep -q "EXPOSE 4000" Dockerfile`, and `grep -q -- "-f-enable-cluster-counting" Dockerfile` all succeed. `grep -q "npm ci\"" Dockerfile || grep -qE "RUN npm ci$" Dockerfile` succeeds AND `grep -q -- "--omit=dev" Dockerfile` FAILS (proves `--omit=dev` is deliberately absent, per the research note this task cites). A full authenticated build (`docker buildx build --platform linux/amd64 --secret id=fuel_corpora_token,env=GH_TOKEN -t agda-mcp-server:local-test .`) is documented as the secondary, credential-dependent verification performed in Plan 08-04 once the real GH secret exists — not required to pass locally in this plan.
  </acceptance_criteria>
  <done>Dockerfile + .dockerignore exist, pass the BuildKit lint check, and contain the exact non-root/digest-pinning/secret-mount properties listed above.</done>
</task>

<task type="auto">
  <name>Task 2: k8s manifests — Deployment/Service/Ingress/CronJob targeting the verified llm-gateway namespace + PVC</name>
  <files>k8s/deployment.yaml, k8s/ingress.yaml, k8s/cronjob.yaml, k8s/team-keys-secret.yaml.template</files>
  <read_first>.claude/skills/agda-mcp-k8s-deploy/SKILL.md, /Users/eric/projects6/litellm/.claude/skills/litellm-k8s-deploy/assets/k8s/deployment.yaml, /Users/eric/projects6/litellm/.claude/skills/litellm-k8s-deploy/assets/k8s/ingress.yaml</read_first>
  <action>
    Create `k8s/deployment.yaml` containing BOTH a `Deployment` named `agda-mcp-ingest` and a `Service` named `agda-mcp-ingest` (litellm's own file combines these via a `---` separator — mirror that shape), namespace `llm-gateway` on both. Deployment: 1 replica; `securityContext: { runAsNonRoot: true, runAsUser: 2231, fsGroup: 2231, seccompProfile: { type: RuntimeDefault } }`; `imagePullSecrets: [{ name: ghcr-credentials }]`; container image `ghcr.io/cliu238/agda-mcp-server:latest` (this repo's own pushable `origin` remote is `cliu238/agda-mcp-server` — verify this still matches `git remote get-url origin` at execution time; GHCR requires an all-lowercase owner/repo, already satisfied); `imagePullPolicy: Always`; `containerPort: 4000`; env vars `AGDA_MCP_TEAM_STORAGE_DIR=/data/team-uploads`, `AGDA_MCP_TEAM_INGEST_PORT=4000`, `AGDA_MCP_TEAM_INGEST_HOST=0.0.0.0` (a deliberate, documented override of the script's loopback-only default — inside a pod's own network namespace this is the correct, expected bind-all, not a public exposure: the pod is only reachable via the Service/Ingress, both namespace/cluster-scoped), `AGDA_MCP_TEAM_INGEST_MAX_BYTES=536870912`, `AGDA_MCP_TEAM_KEYS_PATH=/etc/agda-mcp-team/team-keys.json`; `volumeMounts`: the PVC `sciserver-datavolumes-01-rw` mounted at `/data/team-uploads` with `subPath: agda-mcp/team-storage` (D-07's project-`subPath` convention, reusing the existing PVC per the SKILL's explicit "do NOT create a new PVC" instruction), and a `secret` volume `agda-mcp-team-keys` mounted READ-ONLY at `/etc/agda-mcp-team` with `defaultMode: 0400`; container `securityContext: { allowPrivilegeEscalation: false, capabilities: { drop: [ALL] } }`; `resources: { requests: { memory: "128Mi", cpu: "100m" }, limits: { memory: "512Mi", cpu: "500m" } }`; `livenessProbe`/`readinessProbe`/`startupProbe` all `httpGet: { path: /healthz, port: 4000 }` (mirror litellm's own three-probe shape and timing values). Service: `ClusterIP`, port 4000 -> targetPort 4000.

    Create `k8s/ingress.yaml`: host `dev.sites.idies.jhu.edu`, path `/agda-mcp(/|$)(.*)`, `pathType: ImplementationSpecific`, backend `agda-mcp-ingest` port 4000, annotations `nginx.ingress.kubernetes.io/rewrite-target: /$2`, `nginx.ingress.kubernetes.io/use-regex: "true"`, `nginx.ingress.kubernetes.io/ssl-redirect: "true"`, `nginx.ingress.kubernetes.io/proxy-body-size: "512m"` (matches `AGDA_MCP_TEAM_INGEST_MAX_BYTES` above).

    Create `k8s/cronjob.yaml`: `CronJob` named `agda-mcp-cron-judge`, namespace `llm-gateway`, `schedule: "0 6 * * *"` (daily 06:00 UTC — Claude's Discretion per 08-CONTEXT.md, tunable), `concurrencyPolicy: Forbid` (the judge's own `.processed.json`-marker idempotency and `writeBackQueue`'s file-level operations are NOT designed for two simultaneous cron pods racing the same PVC path), `successfulJobsHistoryLimit: 3`, `failedJobsHistoryLimit: 3`, `jobTemplate.spec.activeDeadlineSeconds: 21600` (6h — ORCL-01 cold replays against CHG/Hopf-scale corpora can run for hours per D-08). Pod spec: SAME image + `imagePullSecrets` + `securityContext` (UID/GID 2231) as the Deployment; container command `["npx", "tsx", "scripts/team/cron-ingest-wrapup.mjs", "--queue-path", "/data/cluster-queue/fix-queue.json", "--no-push"]` (D-09: BOTH the outside-repo `--queue-path` — which structurally disables `writeBackQueue`'s git calls per this plan's own `<interfaces>` note — AND the explicit `--no-push` flag are present, redundant by design so the "no repo-write credential ever reaches the cluster" property does not depend on remembering only one of the two); env `AGDA_MCP_TEAM_STORAGE_DIR=/data/team-uploads` (SAME subPath as the ingest Deployment — the judge reads what ingest wrote) and `GHCRTS=-M5g` (the orchestrator-recorded memory-cap default from 08-CONTEXT.md's planning notes, adjusted from the suggested 6G to 5G in THIS plan — see the Decisions section below for the exact arithmetic and why); `volumeMounts`: the SAME PVC with TWO subPaths — `agda-mcp/team-storage` at `/data/team-uploads` and `agda-mcp/cluster-fix-queue` at `/data/cluster-queue` (this second subPath is where D-09's queue accumulates durably across pod restarts — a fresh CronJob pod has no other persistent storage); `resources: { requests: { memory: "1Gi", cpu: "250m" }, limits: { memory: "6Gi", cpu: "2000m" } }`; `restartPolicy: Never`.

    Create `k8s/team-keys-secret.yaml.template` (comment-only reference, mirrors litellm's own `secrets.yaml.template` "DO NOT commit with real values" convention exactly): document that the REAL secret is created imperatively from a LOCAL `team-keys.json` produced by `scripts/team/issue-key.mjs` (never generated on the cluster itself) via `kubectl create secret generic agda-mcp-team-keys --namespace=llm-gateway --from-file=team-keys.json=<local-path-to-team-keys.json> --dry-run=client -o yaml | kubectl apply -f -`, and that adding/revoking a teammate's key requires re-running `issue-key.mjs`/`revoke` locally and re-running this SAME sync command — never editing the Secret object directly. Include the literal `apiVersion: v1 / kind: Secret / type: Opaque` shape as a comment-only reference (matching `k8s/team-keys-secret.yaml.template`'s own documented, never-applied-as-is convention).
  </action>
  <verify>
    <automated>kubectl apply --dry-run=client -f k8s/deployment.yaml -f k8s/ingress.yaml -f k8s/cronjob.yaml</automated>
  </verify>
  <acceptance_criteria>
    `kubectl apply --dry-run=client -f k8s/deployment.yaml -f k8s/ingress.yaml -f k8s/cronjob.yaml` exits 0 with no schema errors (client-side validation only — contacts no live cluster). `grep -c "runAsUser: 2231" k8s/deployment.yaml k8s/cronjob.yaml` returns `2` total (one per file). `grep -q "sciserver-datavolumes-01-rw" k8s/deployment.yaml` and the same in `k8s/cronjob.yaml` both succeed. `grep -q "proxy-body-size: \"512m\"" k8s/ingress.yaml` succeeds. `grep -q -- "--no-push" k8s/cronjob.yaml` succeeds. `grep -q "namespace: llm-gateway" k8s/deployment.yaml k8s/ingress.yaml k8s/cronjob.yaml` succeeds for all three (no `namespace.yaml` is created — the namespace already exists per the SKILL.md and is never touched by this plan).
  </acceptance_criteria>
  <done>All 4 k8s files exist, pass client-side dry-run validation, and match every cluster fact cited in this plan's `<interfaces>` block exactly (namespace, PVC name, UID, proxy-body-size, port).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> Ingress -> ingest Service | untrusted HTTP arrives at `/agda-mcp`; Bearer-key auth happens inside `ingest-server.mjs` (unchanged), not at the Ingress layer |
| Docker build -> GHCR | the built image is pushed to a registry other CI runs and cluster nodes pull from |
| CronJob pod -> PVC | the ONE place captured teammate session data (logs, proof transcripts) and the accumulating fix-queue both live durably |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering (supply chain) | Dockerfile base images | mitigate | Both `haskell:9.10-bookworm` and `node:24-slim` are pinned by a live-resolved `sha256` digest (not a floating tag) — a registry-side tag repoint can never silently change what this Dockerfile builds |
| T-08-06 | Information Disclosure | `fuel_corpora_token` BuildKit secret | mitigate | Mounted via `--mount=type=secret` (keeps it out of ENV/ARG/`docker history`) AND scrubbed from every clone's .git/config by 08-01's post-clone `remote set-url` (the secret mount alone does NOT prevent the RUN step writing the token into files that become layer content — plan-checker blocker fix). Layer verification = both `docker history` AND `git config --get remote.origin.url` inside the built image |
| T-08-07 | Information Disclosure | `agda-mcp-team-keys` (hashed Bearer-key registry) | mitigate | Delivered as a k8s Secret volume mounted read-only (`defaultMode: 0400`) at a path outside the PVC's teammate-log data — never baked into the image, never a plain ConfigMap, never committed as YAML with real values (only a `.template` file is committed) |
| T-08-08 | Denial of Service | CronJob's Agda cold-replay memory use vs. shared quota headroom | mitigate | `GHCRTS=-M5g` caps Agda's own GHC heap at 5Gi WITHIN the pod's 6Gi memory limit (1Gi headroom for Node/OS so GHC's own graceful heap-limit exit fires before the kubelet's cgroup OOM-killer would) — see Decisions below for the full arithmetic; if a build still OOMs in practice, the documented escalation is an IDIES quota-bump ticket, an explicit fail-loud default per 08-CONTEXT.md's planning notes, never a silent assumption |
| T-08-09 | Tampering | Cron judge's git write-back capability | mitigate | `--queue-path /data/cluster-queue/fix-queue.json` (outside the repo checkout at `/app`) makes `writeBackQueue`'s own `relative(SERVER_REPO_ROOT, queueJsonPath).startsWith("..")` guard return true, structurally skipping every git call; `--no-push` is added as a second, explicit, redundant enforcement (D-09) — NO git-push-capable credential is provisioned anywhere in this plan's manifests, directly shrinking Phase 7's AR-01 accepted-risk scope (T-07-20) rather than extending it to the cluster |
| T-08-10 | Elevation of Privilege | container process identity | mitigate | `runAsNonRoot: true`, `runAsUser: 2231`, `fsGroup: 2231`, `allowPrivilegeEscalation: false`, `capabilities: drop: [ALL]` on both the Deployment and the CronJob — UID 2231 is dictated by the cluster's own Ceph directory ownership, not a locally-invented convention |
| T-08-11 | Information Disclosure | teammate proof-session logs/keys on the shared PVC | accept | Already an accepted risk from Phase 7 (colleague logs visible to whoever can read the storage root) — this plan does not widen it; D-09 explicitly SHRINKS the adjacent AR-01 risk by keeping git-write credentials off the cluster entirely (see T-08-09) |
</threat_model>

<verification>
`docker build --check -f Dockerfile .` clean; `kubectl apply --dry-run=client -f k8s/` clean; no `namespace.yaml`/new-PVC manifest exists anywhere in `k8s/`.
</verification>

<success_criteria>
- Dockerfile builds `linux/amd64` (verified by lint; full build verified with real credentials in Plan 08-04) and contains Node 24 + Agda 2.8.0 + all 4 pinned fuel-corpus source clones, no `_build` caches.
- k8s manifests dry-run clean and match every live-verified cluster fact (namespace, PVC, UID, quota-fitting resource requests/limits, proxy-body-size).
- Memory arithmetic is documented and internally consistent: ingest (512Mi limit) + cron judge (6Gi limit) = 6.5Gi peak, within the ~7Gi measured headroom with a 0.5Gi margin; 0.5+2.0 = 2.5 CPU peak, within the ~3 CPU headroom with a 0.5 CPU margin.
</success_criteria>

<output>
Create `.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-02-SUMMARY.md` when done
</output>
