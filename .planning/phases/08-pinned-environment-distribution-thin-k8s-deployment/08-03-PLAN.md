---
phase: 08-pinned-environment-distribution-thin-k8s-deployment
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - docs/TEAM-ONBOARDING.md
  - test/integration/team/team-onboarding-walkthrough.test.ts
autonomous: true
requirements: [TEAM-05]

must_haves:
  truths:
    - "A documented onboarding path exists covering Windows (via WSL2, D-02), the no-npm-account distribution vehicle (D-01), and the overnight-first-build expectation with no bespoke cache system"
    - "An automated test proves a simulated fresh teammate — clean temp HOME, no pre-existing .agda-mcp/ or fuel clones — can run the installer to a genuinely successful upload against a local ingest endpoint (documented fallback), with zero real Agda dependency (DI-faked corpus clone, fixture-based run report)"
    - "Local mode is proven end-to-end BEFORE any cluster exists — satisfies DEPLOY-01's 'local mode remains a working fallback' criterion at its point of first truth"
    - "Plan actions cite D-01, D-02 for traceability (grep -c 'D-[0-9][0-9]' .planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-03-PLAN.md returns >= 2)"
  artifacts:
    - path: "docs/TEAM-ONBOARDING.md"
      provides: "Zero-to-uploading walkthrough doc: prerequisites, WSL2 section, install script invocation, upload-key setup, first-build expectation, known limitations"
    - path: "test/integration/team/team-onboarding-walkthrough.test.ts"
      provides: "Fresh-teammate simulation: clean temp HOME -> installer core (Plan 08-01) -> local ingest-server.mjs instance -> real issued key -> real tar.gz upload -> 200 + archive on disk"
  key_links:
    - from: "test/integration/team/team-onboarding-walkthrough.test.ts"
      to: "scripts/team/install-pinned-env.mjs"
      via: "import { scriptMain } from \"../../../scripts/team/install-pinned-env.mjs\""
      pattern: "install-pinned-env\\.mjs"
    - from: "test/integration/team/team-onboarding-walkthrough.test.ts"
      to: "scripts/dogfood/upload-run.mjs"
      via: "import { runUploadForRun } from \"../../../scripts/dogfood/upload-run.mjs\""
      pattern: "runUploadForRun"
    - from: "docs/TEAM-ONBOARDING.md"
      to: "scripts/team/install-pinned-env.sh"
      via: "documented invocation: bash scripts/team/install-pinned-env.sh"
      pattern: "install-pinned-env\\.sh"
---

<objective>
Close TEAM-05 with the two pieces that turn Plan 08-01's engine into something a real teammate can follow, and PROVE it works end to end without any cluster or real Agda dependency: the onboarding document, and a genuinely-executed "fresh teammate" acceptance walkthrough that drives the installer to a real, successful upload against a local ingest endpoint.

Purpose: TEAM-05's acceptance bar (per this phase's planning notes) is a REAL zero-to-uploading walkthrough, not a description of one. This plan is that proof, using the local endpoint as the documented fallback target (the cluster does not exist yet at this point in the phase's wave order).
Output: `docs/TEAM-ONBOARDING.md`, `test/integration/team/team-onboarding-walkthrough.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-CONTEXT.md

@scripts/team/install-pinned-env.mjs
@scripts/team/clone-fuel-corpora.mjs
@scripts/team/ingest-server.mjs
@scripts/team/issue-key.mjs
@scripts/dogfood/upload-run.mjs
@scripts/dogfood/transcript-writer.mjs

<interfaces>
<!-- Exact, verified exports the acceptance test drives directly — no exploration needed. -->

From scripts/team/ingest-server.mjs:
```javascript
export function createIngestServer({ storageDir, maxBytes, keysPath }) { /* returns an http.Server, not yet listening */ }
```
Archives land at `<storageDir>/<person>/<date>/<runId>.tar.gz`.

From scripts/team/issue-key.mjs:
```javascript
export async function issueKey(person, keysPath) { /* returns { person, rawKey, issuedAt } */ }
```

From scripts/dogfood/upload-run.mjs:
```javascript
export async function runUploadForRun(runId, options = {}) { /* returns { attempted: true, uploaded: true } on success */ }
```
Internally reads `<AGDA_MCP_DOGFOOD_RUNS_ROOT or default>/<runId>/run-report.json` (via `resolveRunsRoot()` from `transcript-writer.mjs`) — this file must exist on disk BEFORE calling `runUploadForRun`.

From scripts/dogfood/transcript-writer.mjs:
```javascript
export function resolveRunsRoot() { /* AGDA_MCP_DOGFOOD_RUNS_ROOT env override, else SERVER_REPO_ROOT/.agda-mcp/runs */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/TEAM-ONBOARDING.md — the documented zero-to-uploading path (D-01, D-02, D-03, D-04)</name>
  <files>docs/TEAM-ONBOARDING.md</files>
  <read_first>scripts/team/install-pinned-env.mjs, scripts/team/issue-key.mjs, scripts/data/fuel-corpora.json</read_first>
  <action>
    Write docs/TEAM-ONBOARDING.md (mirrors the structure of existing docs/*.md files in this repo -- plain prose plus fenced command blocks, no doc-generation tooling) with these sections, in order:

    1. Prerequisites -- Node greater-or-equal 24, git, and Agda 2.8.0 (or install instructions the reader follows themselves via nix or ghcup -- D-03: this server never force-installs Agda). A dedicated Windows subsection stating Windows teammates install WSL2 first and run every subsequent step inside it -- run-pinned-agda.sh stays POSIX-only and the fuel-corpus clone root resolves to the WSL home, never a native Windows path (D-02, forced by the phase boundary: no script grows Windows-specific path handling).
    2. Step 1, get the pinned server -- clone the repo and check out the highest git tag by version sort (never a hardcoded version string that would go stale the moment a newer tag is cut -- D-11's tag is cut in this phase's final plan, 08-06). No npm account is used or needed anywhere in this step or any step below (D-01: git install, not npm publish).
    3. Step 2, run the installer -- `bash scripts/team/install-pinned-env.sh`. Document exactly what it does (Node/git gate, Agda locate+verify against the pinned 2.8.0, tooling/scripts/run-pinned-agda.sh generation, all 4 fuel corpora cloned under ~/agda-mcp-fuel/&lt;key&gt; or $AGDA_MCP_FUEL_ROOT if set, npm ci) and what to do if it exits 1 (Agda missing/mismatched -- follow the printed nix/ghcup instructions and re-run).
    4. Step 3, first corpus build (overnight, once) -- explicitly state this is a documented step, not automated: open each cloned corpus under ~/agda-mcp-fuel/ in an editor/agent session and let its own first typecheck run to completion once (agda-unimath-scale corpora can take hours) -- no bespoke cache system exists or is expected (the CACHE theme was deliberately deleted from this milestone).
    5. Step 4, get an upload key -- the maintainer runs scripts/team/issue-key.mjs's issue subcommand out of band and shares the printed key plus the printed consent statement with you over a side channel (Slack/email -- never committed anywhere). Set AGDA_MCP_TEAM_UPLOAD_KEY and AGDA_MCP_TEAM_UPLOAD_URL (the real cluster endpoint, live once Plan 08-04 deploys it, e.g. https://dev.sites.idies.jhu.edu/agda-mcp/ingest) in your shell profile. Document a Local fallback: if the cluster endpoint is unreachable, run `npx tsx scripts/team/ingest-server.mjs` yourself and point AGDA_MCP_TEAM_UPLOAD_URL at http://127.0.0.1:8787/ingest instead -- state explicitly that this is the exact mechanism this plan's own acceptance test (Task 2) exercises end to end.
    6. Step 5, do proof work and upload -- run a dogfooding session via scripts/dogfood/dogfood-run.mjs, then scripts/dogfood/dogfood-wrapup.mjs, then scripts/dogfood/upload-run.mjs (or let it auto-chain at wrap-up's end, per existing TEAM-02 behavior -- no new flag this plan introduces).
    7. Known limitations -- state plainly that the cron judge's flaky-classification log (.agda-mcp/team/cron-flaky.jsonl) is not configurable via CLI/env and, on the cluster, writes to the pod's ephemeral filesystem rather than the PVC -- it does not survive a CronJob pod restart. The fix-queue.json accumulation on the PVC (D-09) IS durable and is the authoritative record; the flaky log is diagnostic-only. Note this is a recorded, deliberate scope boundary (fixing it would add a new CLI flag to a Phase-7-owned script, outside this phase's "no new application logic" boundary), not an oversight.
  </action>
  <verify>
    <automated>test -f docs/TEAM-ONBOARDING.md && grep -q "WSL2" docs/TEAM-ONBOARDING.md && grep -qi "no npm account" docs/TEAM-ONBOARDING.md && grep -q "AGDA_MCP_TEAM_UPLOAD_KEY" docs/TEAM-ONBOARDING.md && grep -qi "known limitation" docs/TEAM-ONBOARDING.md</automated>
  </verify>
  <acceptance_criteria>
    All five conditions in the verify command are true simultaneously (single `&&`-chained command exits 0). The document contains all 7 sections named in the action text, in order (spot-checked by the section headers grepping in sequence).
  </acceptance_criteria>
  <done>docs/TEAM-ONBOARDING.md exists and covers prerequisites through known limitations, citing D-01 through D-04 and D-09 where the corresponding behavior is described.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fresh-teammate acceptance walkthrough — installer to a real local upload</name>
  <files>test/integration/team/team-onboarding-walkthrough.test.ts</files>
  <read_first>scripts/team/install-pinned-env.mjs, scripts/team/clone-fuel-corpora.mjs, scripts/team/ingest-server.mjs, scripts/team/issue-key.mjs, scripts/dogfood/upload-run.mjs, test/unit/tools/dogfood-upload-run.test.ts</read_first>
  <behavior>
    One test, `"fresh teammate: install -> issue key -> local ingest -> real upload succeeds"`, structured as:
    1. Create a clean `mkdtempSync` scratch dir simulating a fresh teammate's environment: a temp "fuel root", a temp "runs root", a temp "keys path", a temp "storage dir". Do NOT touch the real repo's `.agda-mcp/` or `~/agda-mcp-fuel/`.
    2. Drive `install-pinned-env.mjs`'s exported pieces DIRECTLY (not the CLI): call `locateAgdaBinary`/`getAgdaVersion`/`versionSatisfies` with `deps.execFileSync` FAKES that simulate a correctly-pinned Agda 2.8.0 being present (no real Agda binary needed — this proves the SCRIPT LOGIC's happy path, matching this codebase's own DI-first testing convention for expensive/external operations); call `writeRunPinnedAgdaScript` for real against the temp scratch dir and assert the generated file is executable and contains an `exec` line. Call `cloneAllFuelCorpora` with a `deps.execFileSync` fake that no-ops successfully for all 4 entries (proving the ORCHESTRATION wiring — Task 1 of Plan 08-01 already separately proves the REAL git-clone mechanics against a local bare-repo fixture; this test's job is the end-to-end CHAIN, not re-proving git mechanics).
    3. Start a REAL `createIngestServer({ storageDir: <temp>, maxBytes: 536_870_912, keysPath: <temp keys path> })` listening on `127.0.0.1` port `0` (OS-assigned, avoids port collisions) — read back the assigned port from the server's own `.address()`.
    4. Call the REAL `issueKey("fresh-teammate", <temp keys path>)` and capture the returned `rawKey`.
    5. Write a minimal, valid `run-report.json` to `<tempRunsRoot>/<runId>/run-report.json` (`schemaVersion`, `runId`, `startedAt`, `endedAt`, `corpusRoot`, `manifestPath`, `totalToolCalls: 0`, `perTool: {}`, `stagedCaptures: []`, `transcriptPath`, `taskManifestCorpora: []` — an empty `stagedCaptures` array is a legitimate, valid run report; the archive will simply contain no capture files).
    6. Set `AGDA_MCP_DOGFOOD_RUNS_ROOT` to the temp runs root for the duration of the call (restore afterward in a `finally`/`afterEach`, mirroring `dogfood-upload-run.test.ts`'s own `withEnvOverride` helper — reuse the SAME pattern, do not invent a new one).
    7. Call the REAL `runUploadForRun(runId, { key: rawKey, url: \`http://127.0.0.1:${port}/ingest\` })` — no `deps.fetch` override; this hits the real local server over a real loopback socket, exercising the real tar/gzip pack + real streamed HTTP POST + real Bearer-auth verification + real streamed size-cap guard, end to end.
    8. Assert the result is exactly `{ attempted: true, uploaded: true }`.
    9. Assert the archive file exists on disk at `<storageDir>/fresh-teammate/<today's-date>/<runId>.tar.gz` (matches `ingest-server.mjs`'s own documented `<person>/<date>/<runId>.tar.gz` layout).
    10. Close the server and clean up all temp dirs in an `afterEach`.
  </behavior>
  <action>
    Write the test exactly as scoped in `<behavior>`, following this repo's established integration-test style (see `test/integration/mcp/dogfood-proxy-passthrough.test.ts` for `mkdtempSync`/`afterEach` cleanup conventions and `test/unit/tools/dogfood-upload-run.test.ts` for the `withEnvOverride` helper — import or replicate it verbatim, do not diverge). Do not gate this test behind `RUN_AGDA_INTEGRATION` — it needs zero real Agda binary and must run on every `npm test`/CI invocation, proving TEAM-05's local-mode path continuously, not just once during this phase's execution.
  </action>
  <verify>
    <automated>npx vitest run test/integration/team/team-onboarding-walkthrough.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run test/integration/team/team-onboarding-walkthrough.test.ts` exits 0. The test asserts the literal `{ attempted: true, uploaded: true }` return shape from `runUploadForRun` (not a looser truthy check). The test asserts the archive file's existence via `existsSync` on the exact `<storageDir>/fresh-teammate/<date>/<runId>.tar.gz` path (not just "the server returned 200").
  </acceptance_criteria>
  <done>Test file exists, passes, requires no real Agda binary, requires no network access beyond loopback, and leaves zero residue in the real repo's `.agda-mcp/` directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onboarding doc reader -> maintainer | the upload key + consent statement travel over an out-of-band side channel the doc names explicitly, never a committed file |
| acceptance test -> loopback network | the ONE real network call in this plan's test is a loopback HTTP POST to a server the SAME test process started |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Information Disclosure | upload key distribution (documented, not code) | accept | Locked v1.1 decision carried from Phase 7 (TEAM-01): one-time consent via key issuance, trusted internal team, no committed secrets — this plan's doc only names the ALREADY-established out-of-band channel, introduces no new exposure |
| T-08-13 | Tampering | acceptance test's fixture `run-report.json` | accept | Test-only, written to a `mkdtempSync` scratch dir the SAME test process created and cleans up — never a shared or persistent path, zero production exposure |
</threat_model>

<verification>
`npx vitest run test/integration/team/` green; `npx vitest run test/unit/` shows zero regressions (this plan modifies no existing source file).
</verification>

<success_criteria>
- `docs/TEAM-ONBOARDING.md` exists and documents every step from prerequisites through known limitations.
- The fresh-teammate acceptance test passes deterministically and requires neither a real Agda binary nor any non-loopback network access.
</success_criteria>

<output>
Create `.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-03-SUMMARY.md` when done
</output>
