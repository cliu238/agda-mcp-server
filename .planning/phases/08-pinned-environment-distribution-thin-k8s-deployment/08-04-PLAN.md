---
phase: 08-pinned-environment-distribution-thin-k8s-deployment
plan: 04
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - .github/workflows/deploy-ingest.yml
  - docs/DEPLOY-OPERATIONS.md
autonomous: false
requirements: [DEPLOY-01]
user_setup:
  - service: github-actions-secrets
    why: "GHCR build+deploy workflow (D-06) needs a cluster-deploy SSH key and a read-scoped PAT for cloning 2 private fuel corpora during the image build; neither can be minted by Claude"
    env_vars:
      - name: K8S_SSH_PRIVATE_KEY
        source: "The github_actions_k8s SSH private key, base64-encoded to a single line, from the JHU work Mac (Tailscale-reachable at ericliu@100.120.232.115) where it lives -- see .claude/skills/agda-mcp-k8s-deploy/SKILL.md's Cluster Access section"
      - name: FUEL_CORPORA_READ_TOKEN
        source: "A fine-grained GitHub PAT, read-only, scoped to exactly emilyriehl/Codex-Homotopy-Group and emilyriehl/autoformalizing-hopf, created via GitHub Settings > Developer settings > Fine-grained tokens by whichever account already has collaborator read access to those 2 private repos"
    dashboard_config: []

must_haves:
  truths:
    - "Pushing to main triggers the deploy workflow unconditionally — no path filter, no tag gate (D-06's explicit choice, verbatim litellm mechanism minus its own path-filter job)"
    - "GHCR pull credentials and the team-keys registry reach the cluster exclusively as k8s Secrets — never baked into the image, never committed to a manifest"
    - "The first real deploy succeeds: the ingest Deployment reaches Running, and https://dev.sites.idies.jhu.edu/agda-mcp/healthz returns 200"
    - "Plan actions cite D-06, D-10 for traceability (grep -c 'D-[0-9][0-9]' .planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-04-PLAN.md returns >= 2)"
  artifacts:
    - path: ".github/workflows/deploy-ingest.yml"
      provides: "GHCR build+push (linux/amd64, BuildKit secret for the private-corpus PAT) then SSH-jump kubectl apply+rollout, triggered on every push to main"
    - path: "docs/DEPLOY-OPERATIONS.md"
      provides: "Maintainer runbook: cluster access recap, secret rotation commands, deploy verification commands — extended further by Plans 08-05/08-06"
  key_links:
    - from: ".github/workflows/deploy-ingest.yml"
      to: "Dockerfile"
      via: "docker/build-push-action with file: ./Dockerfile, platforms: linux/amd64, secrets: fuel_corpora_token=${{ secrets.FUEL_CORPORA_READ_TOKEN }}"
      pattern: "fuel_corpora_token"
    - from: ".github/workflows/deploy-ingest.yml"
      to: "k8s/deployment.yaml"
      via: "remote kubectl apply -f k8s/ && kubectl rollout restart deployment/agda-mcp-ingest -n llm-gateway"
      pattern: "kubectl apply -f k8s/"
---

<objective>
Wire the CI/CD pipeline that turns Plan 08-02's Dockerfile + k8s manifests into a running cluster deployment, bootstrap the cluster-side secrets it depends on (GHCR pull credentials, the team-keys registry), and drive the FIRST real deploy to completion.

Purpose: this is where DEPLOY-01 stops being packaging and starts being a real, reachable service. D-06 locks the trigger policy (auto-deploy on every push to main, litellm's mechanism verbatim); this plan implements it plus the one genuinely-unavoidable human step (minting credentials only a human can produce).
Output: `.github/workflows/deploy-ingest.yml`, `docs/DEPLOY-OPERATIONS.md`, the `ghcr-credentials` and `agda-mcp-team-keys` k8s Secrets created on the live cluster, the `K8S_SSH_PRIVATE_KEY` and `FUEL_CORPORA_READ_TOKEN` GitHub Actions secrets set, and a first successful deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-CONTEXT.md
@.claude/skills/agda-mcp-k8s-deploy/SKILL.md

@/Users/eric/projects6/litellm/.claude/skills/litellm-k8s-deploy/assets/.github/workflows/deploy.yml
@.github/workflows/ci.yml
@k8s/deployment.yaml
@scripts/team/issue-key.mjs

<interfaces>
<!-- Pinned action SHAs this plan MUST use — resolved live 2026-07-04, do not substitute floating tags. -->

Already established in this repo's own ci.yml (reuse verbatim):
- `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683` (# v4.2.2)

New pins for this workflow (resolved via `git ls-remote --tags` on 2026-07-04):
- `docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c` (# v4.2.0)
- `docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0` (# v4.4.0)
- `docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302` (# v6.2.0)
- `docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8` (# v6.19.2)

Cluster access (from .claude/skills/agda-mcp-k8s-deploy/SKILL.md — read this file's "Cluster Access" section before Task 2/3):
- The CI RUNNER connects DIRECTLY (2-hop: `dslogin01.pha.jhu.edu` -> `k8slgn.idies.jhu.edu:14132`) using the `K8S_SSH_PRIVATE_KEY` secret decoded on the runner itself — this is litellm's OWN proven `deploy.yml` shape and needs NO Tailscale hop (GH-hosted runners reach `dslogin01.pha.jhu.edu` directly over the public internet; the Tailscale hop is only needed for a HUMAN/interactive session from a machine that does not hold the key file, which is a different access path from CI's own).
- INTERACTIVE work in Task 2/3 of THIS plan (bootstrapping cluster secrets by hand, run by whoever executes this plan) DOES use the documented 3-hop one-shot script pattern (this Mac -> ericliu@100.120.232.115 Tailscale -> cliu238@dslogin01.pha.jhu.edu -> k8slgn.idies.jhu.edu:14132) — do not conflate the two access paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .github/workflows/deploy-ingest.yml — GHCR build+push then SSH-jump kubectl deploy, on every push to main (D-06)</name>
  <files>.github/workflows/deploy-ingest.yml</files>
  <read_first>/Users/eric/projects6/litellm/.claude/skills/litellm-k8s-deploy/assets/.github/workflows/deploy.yml, .github/workflows/ci.yml</read_first>
  <action>
    Create `.github/workflows/deploy-ingest.yml`, name "Deploy agda-mcp ingest to k8s-dev", triggers `on: push: branches: [main]` and `workflow_dispatch:` — deliberately WITHOUT litellm's own `check-changes`/`dorny/paths-filter` job (D-06's explicit locked choice: "auto-deploy on every push to main... accepting that main-branch commits unrelated to the ingest also rebuild the image and roll the endpoint" — cite this decision in a workflow comment so a future reader does not "fix" this by re-adding a path filter).

    Job `build-and-push` (`runs-on: ubuntu-latest`, `permissions: { contents: read, packages: write }`): checkout (pinned SHA above); `docker/setup-buildx-action`; `docker/login-action` against `ghcr.io` with `username: ${{ github.actor }}`, `password: ${{ secrets.GITHUB_TOKEN }}`; `docker/metadata-action` producing `images: ghcr.io/${{ github.repository }}` with tags `type=raw,value=latest,enable={{is_default_branch}}` and `type=sha,prefix={{branch}}-`; `docker/build-push-action` with `context: .`, `file: ./Dockerfile`, `platforms: linux/amd64` (D-10: `linux/amd64` explicitly, even though GH-hosted runners are amd64 natively — state this explicitly rather than omitting `platforms:` so the intent is self-documenting), `push: true`, `tags`/`labels` from the metadata step, `secrets: | fuel_corpora_token=${{ secrets.FUEL_CORPORA_READ_TOKEN }}` (wires the BuildKit secret Plan 08-02's Dockerfile expects for cloning the 2 private fuel corpora), `no-cache: true` (matches litellm's own gotcha #3 — stale-looking deploys otherwise).

    Job `deploy` (`needs: build-and-push`, `runs-on: ubuntu-latest`): checkout; a step that writes `${{ secrets.K8S_SSH_PRIVATE_KEY }}` (base64) to `~/.ssh/id_rsa` via `base64 -d`, `chmod 600`, starts `ssh-agent`, `ssh-add`s it, and persists `SSH_AUTH_SOCK` to `$GITHUB_ENV` (litellm's own exact shape — reuse verbatim); a step that runs the direct 2-hop SSH (`ssh -A cliu238@dslogin01.pha.jhu.edu 'ssh -A -p 14132 k8slgn.idies.jhu.edu bash -s' << 'EOF' ... EOF`) which, on the remote side: exports `KUBECONFIG=/var/k8s/users/cliu238/k8s-dev-llm-gateway-admins.conf`, clones or `git pull`s `https://github.com/cliu238/agda-mcp-server.git` under the login node's own home directory, runs `kubectl apply -f k8s/`, `kubectl rollout restart deployment/agda-mcp-ingest -n llm-gateway`, `kubectl rollout status deployment/agda-mcp-ingest -n llm-gateway --timeout=5m`, then `kubectl get pods,ingress -n llm-gateway` for the run's own log visibility.
  </action>
  <verify>
    <automated>python3 -c "import sys; content = open('.github/workflows/deploy-ingest.yml').read(); assert 'paths-filter' not in content, 'D-06 forbids a path filter'; assert 'platforms: linux/amd64' in content; assert 'K8S_SSH_PRIVATE_KEY' in content; assert 'FUEL_CORPORA_READ_TOKEN' in content; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    The verify script prints `ok` and exits 0. `grep -c "on:" .github/workflows/deploy-ingest.yml` shows the workflow triggers on `push: branches: [main]` with no `paths:`/`paths-filter` key anywhere in the file. The workflow YAML parses cleanly (`python3 -c "import yaml" 2>/dev/null && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-ingest.yml'))"` if PyYAML is available; otherwise rely on the `docker`/`kubectl` dry-run checks already performed against the referenced Dockerfile/manifests in Plan 08-02, since this file's OWN correctness is fully exercised by Task 3's real triggered run).
  </acceptance_criteria>
  <done>Workflow file exists, cites D-06 in a comment, contains no path filter, and references every pinned action SHA from this plan's interfaces block.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap cluster secrets — ghcr-credentials + agda-mcp-team-keys (fully automatable, no human action needed)</name>
  <files>docs/DEPLOY-OPERATIONS.md</files>
  <read_first>.claude/skills/agda-mcp-k8s-deploy/SKILL.md, scripts/team/issue-key.mjs</read_first>
  <action>
    Using the one-shot SSH script pattern documented in `.claude/skills/agda-mcp-k8s-deploy/SKILL.md`'s "Cluster Access" section (upload a script to `ericliu@100.120.232.115`, then run it — do not attempt live nested-quoting through all 3 hops directly):

    1. Create the `ghcr-credentials` pull secret: locally run `gh auth token` to obtain the CI-equivalent GHCR read token tied to this project's own already-authenticated `gh` session (the SAME mechanism litellm's own skill documents for this exact purpose), then embed a remote step doing `kubectl create secret docker-registry ghcr-credentials --namespace=llm-gateway --docker-server=ghcr.io --docker-username=<gh-username> --docker-password=<token> --dry-run=client -o yaml | kubectl apply -f -` (idempotent apply, safe to re-run — do not use bare `kubectl create`, which errors on a pre-existing secret). Never echo the token to any log line the one-shot script prints back to stdout.
    2. Create the `agda-mcp-team-keys` secret: locally run `scripts/team/issue-key.mjs`'s `issue` subcommand for a real first person (or the maintainer's own slug, as a placeholder that can be rotated later), producing a local `scripts/team/data/team-keys.json`. Base64-encode that file's content, embed it in the SAME one-shot remote script (the identical inline-transmission technique the SSH-key setup already uses for a comparably sensitive value), and on the remote side: decode it to a temp file, `kubectl create secret generic agda-mcp-team-keys --namespace=llm-gateway --from-file=team-keys.json=<temp-file> --dry-run=client -o yaml | kubectl apply -f -`, then delete the temp file.
    3. Write `docs/DEPLOY-OPERATIONS.md` (new file, maintainer runbook) documenting: the cluster access recap (link to the SKILL.md, do not duplicate its content); the EXACT secret-rotation commands from steps 1-2 above as a repeatable procedure (adding/revoking a teammate requires re-running `issue-key.mjs` locally then re-running step 2's sync); and a placeholder "Verification" section that Plans 08-05/08-06 will append to.
  </action>
  <verify>
    <automated>test -f docs/DEPLOY-OPERATIONS.md && grep -q "ghcr-credentials" docs/DEPLOY-OPERATIONS.md && grep -q "agda-mcp-team-keys" docs/DEPLOY-OPERATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    Both k8s Secrets exist on the live cluster: the one-shot script's own remote output includes `kubectl get secret ghcr-credentials agda-mcp-team-keys -n llm-gateway` showing both by name (run this exact command as the final line of the remote script and capture its output as this task's evidence). `docs/DEPLOY-OPERATIONS.md` exists and documents both rotation procedures.
  </acceptance_criteria>
  <done>Both secrets exist on the cluster (confirmed via a live `kubectl get secret` in the same session), and the rotation procedure is documented for future teammates.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Confirm GitHub Actions secrets + trigger and verify the first live deploy</name>
  <files>none — this task sets GitHub Actions repository secrets and a k8s deploy trigger, no repo files are modified</files>
  <read_first>.claude/skills/agda-mcp-k8s-deploy/SKILL.md (CI/CD gotchas 1, 2, 3, 13, 18)</read_first>
  <what-needed>
    Two GitHub Actions repository secrets that only a human can produce: `K8S_SSH_PRIVATE_KEY` (the `github_actions_k8s` SSH private key exists ONLY on the JHU work Mac, not reachable for direct file access from this session) and `FUEL_CORPORA_READ_TOKEN` (a fine-grained GitHub PAT scoped to 2 specific private repos owned by a third party, `emilyriehl` — minting a PAT requires GitHub's own web UI, which has no `gh` CLI equivalent).
  </what-needed>
  <how-to-do-it>
    For `K8S_SSH_PRIVATE_KEY`: from the JHU work Mac (or via the documented Tailscale hop from this Mac: `ssh ericliu@100.120.232.115`), run exactly: `gh secret set K8S_SSH_PRIVATE_KEY --repo cliu238/agda-mcp-server --body "$(base64 -i ~/.ssh/github_actions_k8s | tr -d '\n')"`. Confirm with `gh secret list --repo cliu238/agda-mcp-server` showing `K8S_SSH_PRIVATE_KEY` with a recent "Updated" timestamp.

    For `FUEL_CORPORA_READ_TOKEN`: generate a fine-grained PAT at https://github.com/settings/tokens?type=beta scoped to Repository access: only `emilyriehl/Codex-Homotopy-Group` and `emilyriehl/autoformalizing-hopf`, Permissions: Contents (read-only). Then run: `gh secret set FUEL_CORPORA_READ_TOKEN --repo cliu238/agda-mcp-server --body "<paste-the-pat>"`.

    Once both secrets are confirmed set, push the current branch's commits to `origin/main` (this triggers Task 1's workflow automatically per D-06 — no manual `workflow_dispatch` needed unless the push itself does not land for some reason). Watch it with `gh run watch --repo cliu238/agda-mcp-server` until both jobs (`build-and-push`, `deploy`) show success. Then verify the live endpoint: `curl -sf https://dev.sites.idies.jhu.edu/agda-mcp/healthz` must print `ok` with an HTTP 200.
  </how-to-do-it>
  <action>
    (Restated for tooling that expects a uniform action element -- see how-to-do-it above for the full detail.) A human sets K8S_SSH_PRIVATE_KEY and FUEL_CORPORA_READ_TOKEN as GitHub Actions repository secrets on cliu238/agda-mcp-server, then pushes to main to trigger Task 1's deploy-ingest.yml, watches it to completion, and confirms the live healthz endpoint.
  </action>
  <resume-signal>Reply with the `gh run watch` outcome (success/failure + run URL) and the `curl` output once both secrets are confirmed set and the first deploy has completed.</resume-signal>
  <verify>
    <automated>gh secret list --repo cliu238/agda-mcp-server | grep -q "K8S_SSH_PRIVATE_KEY" && gh secret list --repo cliu238/agda-mcp-server | grep -q "FUEL_CORPORA_READ_TOKEN" && curl -sf https://dev.sites.idies.jhu.edu/agda-mcp/healthz | grep -q "^ok$"</automated>
  </verify>
  <acceptance_criteria>
    `gh secret list --repo cliu238/agda-mcp-server` lists both `K8S_SSH_PRIVATE_KEY` and `FUEL_CORPORA_READ_TOKEN`. `gh run list --repo cliu238/agda-mcp-server --workflow deploy-ingest.yml --limit 1` shows `conclusion: success`. `curl -sf https://dev.sites.idies.jhu.edu/agda-mcp/healthz` prints exactly `ok`.
  </acceptance_criteria>
  <done>Both GitHub secrets are set, the triggered deploy-ingest.yml run completed successfully, and the live healthz endpoint returns ok.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> GHCR | the built image is pushed using `secrets.GITHUB_TOKEN` (repo-scoped, auto-rotated, never a long-lived PAT) |
| GitHub Actions runner -> k8s login node | a real SSH private key, decoded from a base64 GH secret, grants `kubectl apply`/`rollout restart` capability in `llm-gateway` |
| Docker build -> `emilyriehl`'s private repos | `FUEL_CORPORA_READ_TOKEN` is scoped read-only to exactly 2 named repos, never a broad/organization-wide token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Information Disclosure | `K8S_SSH_PRIVATE_KEY` in transit/at rest | mitigate | Stored ONLY as a GitHub encrypted repository secret (base64 single-line, per the documented gotcha), decoded to a runner-local file that is destroyed when the ephemeral runner VM is torn down after the job — never echoed to any log line, never committed |
| T-08-15 | Elevation of Privilege | `FUEL_CORPORA_READ_TOKEN` scope | mitigate | Fine-grained PAT, explicitly restricted to exactly `emilyriehl/Codex-Homotopy-Group` + `emilyriehl/autoformalizing-hopf`, read-only Contents permission — never a classic PAT with account-wide scope |
| T-08-16 | Repudiation | `docker/login-action` credential | accept | Uses `secrets.GITHUB_TOKEN` (the workflow's own auto-issued, per-run, auto-expiring token) rather than a personal token — this is the SAME pattern `ci.yml` and litellm's own `deploy.yml` already use, no new credential class introduced |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | Zero new npm/pip/cargo packages are installed by this plan or this phase — the package-legitimacy gate does not apply |
</threat_model>

<verification>
`gh secret list --repo cliu238/agda-mcp-server` shows both `K8S_SSH_PRIVATE_KEY` and `FUEL_CORPORA_READ_TOKEN`; `gh run list --repo cliu238/agda-mcp-server --workflow deploy-ingest.yml --limit 1` shows a `success` conclusion; `curl -sf https://dev.sites.idies.jhu.edu/agda-mcp/healthz` returns `ok`.
</verification>

<success_criteria>
- `.github/workflows/deploy-ingest.yml` exists, triggers unconditionally on push to main, and pins every action by SHA.
- Both cluster secrets (`ghcr-credentials`, `agda-mcp-team-keys`) exist in `llm-gateway`.
- The first live deploy succeeds and `https://dev.sites.idies.jhu.edu/agda-mcp/healthz` returns 200.
</success_criteria>

<output>
Create `.planning/phases/08-pinned-environment-distribution-thin-k8s-deployment/08-04-SUMMARY.md` when done
</output>
