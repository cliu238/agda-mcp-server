# Continue Here — v1.1 Autonomous Run Handoff

**Paused:** 2026-07-05T00:35Z (context-window handoff mid-Phase-8)
**Mode:** `/gsd-autonomous` full-milestone run (v1.1 "Feed the Loop")

## Position

| Phase | Status |
|---|---|
| 6 Backlog Digestion | ✅ Complete + verified (15/15) |
| 7 Team Feedback Channel | ✅ Complete + verified (36/38, E2E-01 strongest form: live defect 0bc76d15c2fec8df captured→fixed→locked) |
| 9 Debt Sweep | ✅ Complete + verified (30/30, WR-01 human-signed with in-flight guard) |
| 8 Pinned-Env + k8s | 🔶 IN FLIGHT — wave 2 of 4 |

**Phase 8 plan state:** 08-01/08-02/08-03 complete + merged + tracked. **08-04 at 2/3 tasks** (workflow `deploy-ingest.yml` authored `c6e76e8`; cluster secrets bootstrapped `b2a3328`; Task 3 = first-deploy verification IN PROGRESS). 08-05 (cluster acceptance) and 08-06 (POLICY-01 cluster re-verify + cut/push v1.1 tag) not started.

## RESOLVED 2026-07-05T02:20Z — service LIVE, healthz ok

All three attempt-2 issues fixed and verified live:
1. Pull secret: NEW dedicated `agda-mcp-ghcr` secret (litellm's shared ghcr-credentials
   must never be overwritten — mutual 403s both directions; verified). Token file removed.
2. SSH keepalives + 10m rollout budget in deploy-ingest.yml (attempt-3 confirmed the
   keepalive held the full 10-min wait).
3. D-10 asserts fixed (git output is piped/swallowed by clone script; assert its own
   2/4-ready summary + no public skip lines) — D-10 job GREEN on run 28725583968.
4. NEW bug found by attempt 3: image CMD = MCP stdio server -> silent-exit CrashLoop.
   Deployment now overrides command with `npx tsx scripts/team/ingest-server.mjs`
   (mirrors cronjob pattern). Cronjob also moved to agda-mcp-ghcr. Fixed in ece6ff1,
   validated by DIRECT kubectl apply: pod 1/1 Running, `listening on 0.0.0.0:4000`,
   public healthz returns ok.
Watching confirmatory run 28726436348 (pushed ece6ff1) to full green, then: 08-04
closure executor -> 08-05 -> 08-06 -> review/verify -> lifecycle (sequence below).
macOS gotcha: local tar smuggles ._* AppleDouble files through the manifest pipe —
use COPYFILE_DISABLE=1 for local applies (GitHub runners unaffected).

## (superseded) Immediate next action — UPDATED 2026-07-05T01:10Z

**Deploy attempt 2 postmortem (run 28724465955):** manifests fix WORKED — all 4 resources `created` on the cluster (deployment/service/ingress/cronjob in llm-gateway). Two remaining issues:

1. **Pods in ImagePullBackOff — GHCR 403.** `ghcr.io/cliu238/agda-mcp-server:latest` is a PRIVATE package (must stay private: image bakes emilyriehl's two private corpora). The cluster's `ghcr-credentials` secret is 143 days old (litellm-era) and cannot read it. Classic `repo` scope does NOT cover package pulls — needs `read:packages`.
   → **PENDING USER ACTION** (requested, may already be done when you resume): user creates classic PAT with ONLY `read:packages`, saves to `~/.agda-mcp-ghcr-token` (chmod 600). CHECK IF THIS FILE EXISTS FIRST.
   → When it exists: recreate the cluster secret via the 3-hop SSH pattern:
     `kubectl create secret docker-registry ghcr-credentials -n llm-gateway --docker-server=ghcr.io --docker-username=cliu238 --docker-password="$(cat ~/.agda-mcp-ghcr-token)" --dry-run=client -o yaml | kubectl apply -f -`
     (pipe the token value through the SSH one-shot script; NEVER echo it into logs), then `kubectl delete pods -l app=agda-mcp-ingest -n llm-gateway` to force re-pull, then `rm ~/.agda-mcp-ghcr-token`, then curl healthz.
     NOTE: there are TWO stale replicasets (the apply+restart double); after pods pull OK, `kubectl get pods` should settle to 1 ready ingest pod.
2. **SSH idle timeout in the deploy job** (`client_loop: send disconnect: Broken pipe` during the silent 5-min rollout wait): add `-o ServerAliveInterval=30 -o ServerAliveCountMax=10` to BOTH ssh hops in `.github/workflows/deploy-ingest.yml`'s deploy step, commit+push (push re-triggers deploy — harmless, idempotent, and by then the pull secret works so rollout should go green end-to-end).
3. **Uncredentialed-build job still red** after --progress=plain fix — NOT yet diagnosed post-fix (this run's failure may predate assertions seeing output; check its --log-failed tail on the NEXT run before touching anything).

Then resume the original sequence below (08-04 closure -> 08-05 -> 08-06 -> review/verify -> lifecycle).

## Original sequence

1. Check deploy run **28724465955** (`gh run view 28724465955 -R cliu238/agda-mcp-server --json conclusion,jobs`) — it was in_progress at pause (attempt 2, after fixing attempt 1's false-green). Then `curl -sf https://dev.sites.idies.jhu.edu/agda-mcp/healthz` (expect `ok`).
   - Attempt 1 postmortem: build+push GREEN (image IS on GHCR), deploy job FALSE-GREEN (remote clone predated k8s/, no set -e). Fixed in `870ad4d`: manifests now base64-tar through the SSH pipe + `set -euo pipefail` + `--progress=plain` on the uncredentialed-build job.
2. If green + healthz ok: spawn a FRESH gsd-executor continuation for 08-04 Task 3 closure (verify deploy evidence, write+commit 08-04-SUMMARY.md; checkpoint protocol — do not resume the old agent).
3. Then wave 3: 08-05 executor (main tree, cluster acceptance: real upload through cluster + manual cron-judge trigger + write-back-disabled proof). Wave 4: 08-06 (POLICY-01 cluster re-verify, local-mode regression, cut+push **v1.1 tag ONLY AFTER acceptance passes**).
4. Then: code-review chain (Skill gsd-code-review 8 → --fix --auto loop, cap 3), gsd-verifier (phase 8), `gsd-sdk query phase.complete 8`, PROJECT.md evolution.
5. Lifecycle: `Skill(gsd-audit-milestone)` → route on status → `Skill(gsd-complete-milestone, "v1.1")` → `Skill(gsd-cleanup)`.

## Secrets / credentials state (all set, verified)

- `FUEL_CORPORA_READ_TOKEN` = classic PAT w/ `repo` scope (fine-grained IMPOSSIBLE: the 2 private corpora are under emilyriehl's personal account — cross-owner repos never appear in fine-grained picker). Set by user 2026-07-05T00:01Z.
- `K8S_SSH_PRIVATE_KEY` = base64 single-line, fetched from JHU Mac via Tailscale, set 00:02Z.
- Cluster secrets live: `ghcr-credentials` (refreshed), `agda-mcp-team-keys` (placeholder key for cliu238, rotatable via `npx tsx scripts/team/issue-key.mjs issue cliu238`).

## Environment gotchas (WILL bite otherwise)

- **Node**: shell default is v22 — ALWAYS `export PATH="/Users/eric/.local/share/mise/installs/node/24/bin:$PATH"` before any test run (2 installer tests genuinely gate on Node>=24).
- **Cluster access**: ONLY via `.claude/skills/agda-mcp-k8s-deploy/SKILL.md` (3-hop SSH one-shot script pattern; load the skill FIRST for ANY cluster-touching work).
- **No docker daemon locally**; no gtar. Docker builds happen on GitHub runners only.
- **NEVER `git stash`** (repo-wide rule; bit two executors).
- **Worktree merges**: use `gsd-sdk query worktree.cleanup-wave --manifest <file>`; it hard-blocks on `base_mismatch` if main moved mid-wave (avoid mid-wave main commits!) and on branch deletions (merge those manually after inspecting `git diff --diff-filter=D`).
- **CI**: main verify job GREEN. Integration lane has a 5-file quarantine (`AGDA_MCP_CI_QUARANTINE` in ci.yml = pre-existing v1.0 Linux deltas, queue entry `fb57abbe7df6dfe8`). Deploy workflow does NOT gate on CI.
- **gh default repo** resolves to InvariantHoldings upstream — always pass `-R cliu238/agda-mcp-server`.

## Session decisions not recorded elsewhere

- Memory-cap default for cluster corpus builds: `GHCRTS=-M5g` inside 6Gi pod limit (GHC graceful exit before kubelet OOM; documented in 08-02/08-04 docs).
- Origin push authorized by user (162 commits + v1.0 tag, 2026-07-04); D-06 auto-deploy-on-main is live — EVERY main push triggers a deploy.
- fix-queue: 18 entries (9→10 locked pending nothing; current: 10 locked / 6 triaged / 1 rejected / 1 new-ish — see file). Phase-7 finds: `0bc76d15c2fec8df` locked, `2eb1768df88bfb07` triaged.
- Milestone audit should surface: Phase-7 VERIFICATION F-1/F-2 items (both resolved-or-recorded: push done; libraries-probe gap = 2eb1768df88bfb07), review-chain Info findings left open by policy (4 in 07-REVIEW, 3 in 09-REVIEW).

## Resume

`/gsd-resume-work` (reads this file), or continue manually from "Immediate next action". Background watchers from the old session are dead — poll `gh run` state directly.
