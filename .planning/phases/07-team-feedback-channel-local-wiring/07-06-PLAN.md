---
phase: 07-team-feedback-channel-local-wiring
plan: 06
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05"]
files_modified:
  - test/fixtures/fix-queue.json
autonomous: false
requirements: [E2E-01]

must_haves:
  truths:
    - "CHG's vendored agda-unimath build is confirmed settled (no active agda process against it, and no .agdai file modified in the last 2 minutes) before any live proving begins."
    - "A live Codex session against the CHG corpus, driven through dogfood-run.mjs per .agents/skills/agda-dogfooding/SKILL.md, produces at least one staged capture and a wrap-up run that automatically uploads to the local ingest server (D-12)."
    - "The uploaded archive is visible on disk under the local ingest storage root at <storageDir>/eric/<today-UTC>/<run-id>.tar.gz."
    - "Running the real cron judge (with push, not --no-push) against that storage reaches a definitive per-capture verdict for every capture in that run, with policyKey resolved correctly for codex-homotopy-group (never derived from .agda-lib name — the exact case-mismatch corpus POLICY-01 fixed)."
    - "Any confirmed defect is upserted into test/fixtures/fix-queue.json with correct recurrence/dedup against pre-existing CHG entries, and the change is committed and pushed to the current branch."
  artifacts:
    - path: "test/fixtures/fix-queue.json"
      provides: "The tracked queue SSOT, updated (or verified unchanged-but-correctly-deduped) by this run"
    - path: ".agda-mcp/team/cron-runs/<timestamp>.json"
      provides: "The cron judge's own run summary — the loop-to-verdict evidence artifact for this acceptance"
  key_links:
    - from: "Live Codex session (dogfood-run.mjs -> dogfood-wrapup.mjs)"
      to: "scripts/team/ingest-server.mjs (local, port 8787)"
      via: "07-02's upload-run.mjs chained per D-12"
      pattern: "N/A — verified by inspecting the ingest storage directory after the session"
    - from: "scripts/team/cron-ingest-wrapup.mjs"
      to: "test/fixtures/fix-queue.json"
      via: "wrapUpCapture -> upsertQueueEntry -> writeBackQueue (git commit+push)"
      pattern: "git log -1 --stat"
---

<objective>
Prove the ENTIRE Phase 7 loop live, with zero fixture shortcuts: a fresh Codex proof session on the pinned, real CHG corpus flows through capture -> upload -> ingest -> cron judge -> fix-queue intake, reaching a definitive per-capture verdict with correct dedup against existing CHG entries (E2E-01). Acceptance is loop-to-verdict, not "must find a new defect" (D-05) — any confirmed defect continues into the existing REVERIFY-02 fix->lock flow, which is out of this plan's own scope.

Purpose: this is the phase's own acceptance gate — every other plan (07-01..07-05) is proven only in isolation (unit tests, DI-driven fakes) until this run exercises them together against real Agda, a real Codex session, and the real tracked queue.
Output: a real, git-visible commit (or a verified no-op if the session's capture(s) turned out true-green) plus a cron-run summary artifact as evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-team-feedback-channel-local-wiring/07-CONTEXT.md
@.planning/phases/07-team-feedback-channel-local-wiring/07-PATTERNS.md
@.agents/skills/agda-dogfooding/SKILL.md
scripts/data/fuel-corpora.json
</context>

<interfaces>
Fixed identifiers this plan uses (all defaults from 07-01..07-05, no overrides needed for the
real acceptance run):
- Ingest endpoint: `http://127.0.0.1:8787/ingest` (default host/port from 07-03).
- Storage root: `<SERVER_REPO_ROOT>/.agda-mcp/team/storage` (07-03's default `resolveTeamStorageDir()`).
- Key registry: `<SERVER_REPO_ROOT>/scripts/team/data/team-keys.json` (07-01's default `resolveKeysPath()`).
- Person identifier for this run: `eric` (D-06: the maintainer plays the teammate locally).
- Pinned corpus: `codex-homotopy-group` (scripts/data/fuel-corpora.json), local checkout expected
  at `$HOME/projects6/Codex-Homotopy-Group` (the path this checkout was verified against during
  planning — confirm it still exists before proceeding; ask the user for the real path if not).

CLI contracts assembled from prior plans in this phase:
```
npx tsx scripts/team/issue-key.mjs issue eric
npx tsx scripts/team/ingest-server.mjs                       # AGDA_MCP_TEAM_INGEST_PORT defaults to 8787
npx tsx scripts/dogfood/dogfood-run.mjs --manifest <path> --corpus-root "$HOME/projects6/Codex-Homotopy-Group"
npx tsx scripts/dogfood/dogfood-wrapup.mjs <run-id>           # auto-chains upload-run.mjs per D-12 when the two env vars below are set
npx tsx scripts/team/cron-ingest-wrapup.mjs                   # real push — do NOT pass --no-push for this acceptance run (D-02)
```
Env vars the human must export in THEIR OWN terminal (the one driving Codex) before step 2:
`AGDA_MCP_TEAM_UPLOAD_KEY=<the raw key printed by issue-key.mjs issue eric>`
`AGDA_MCP_TEAM_UPLOAD_URL=http://127.0.0.1:8787/ingest`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build-readiness probe + local service bring-up</name>
  <files>(none — operational only)</files>
  <read_first>
    scripts/team/issue-key.mjs (issue subcommand's exact stdout shape)
    scripts/team/ingest-server.mjs (default port/host, /healthz route)
    .agents/skills/agda-dogfooding/SKILL.md (the exact runbook the human follows in Task 2 — do not deviate from its documented commands)
  </read_first>
  <action>
    Confirm `$HOME/projects6/Codex-Homotopy-Group` exists (`test -d`); if it does not, STOP this task and ask the user for the real local corpus path before doing anything else — do not guess a second path.

    Run the readiness probe (single bash invocation, no sleeping): capture (a) `pgrep -fl agda 2>/dev/null | grep -i "Codex-Homotopy-Group\|agda-unimath"` (a running build process signal), (b) `find "$HOME/projects6/Codex-Homotopy-Group/agda-unimath/_build" -name '*.agdai' 2>/dev/null | wc -l` (total interface count), (c) `find "$HOME/projects6/Codex-Homotopy-Group/agda-unimath/_build" -name '*.agdai' -newermt '-2 minutes' 2>/dev/null | wc -l` (files touched in the last 2 minutes — a non-zero count means the build is still actively writing).

    Readiness = (a) is empty AND (b) > 100 AND (c) == 0. If NOT ready: STOP here, report the exact three observed values to the user, and do not proceed to Task 2 — this plan is not complete until a subsequent readiness check passes.

    If ready: run `npx tsx scripts/team/issue-key.mjs issue eric` from the agda-mcp-server checkout root and capture its printed raw key (do not echo it into any file — hold it only in this turn's own output for the human to copy in Task 2's instructions).

    Start the local ingest server in the background (`run_in_background: true`): `npx tsx scripts/team/ingest-server.mjs`. Confirm it is actually listening via `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8787/healthz` returning `200` (retry once after a couple seconds if the first attempt races the server's own startup — do not use a long blocking sleep).

    Locate (or, if genuinely absent, ask the user to point at) a task manifest JSON for this session (per PROC-01's D-03 hard gate — dogfood-run.mjs refuses to start without one) naming at least one real, currently-open CHG proof goal with its expected top-level signature; this is chosen at execution time from real open goals in CHG (D-05) — do not fabricate a signature.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8787/healthz</automated>
  </verify>
  <acceptance_criteria>
    - The readiness probe's three values are printed verbatim in this turn's output before any decision is made.
    - `curl .../healthz` returns exactly `200`.
    - A fresh Bearer key for `eric` was minted this turn (not reused from a stale prior run).
    - A task-manifest path exists and is ready to pass to `dogfood-run.mjs --manifest`.
  </acceptance_criteria>
  <done>CHG's build is confirmed settled, the ingest server is live and healthy, a fresh key exists for `eric`, and a valid task manifest is ready — OR this task has explicitly stopped and reported why it is not yet safe to proceed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Live Codex proving session on CHG (human-driven)</name>
  <files>(none — human-driven manual step; produces .agda-mcp/runs/&lt;run-id&gt;/ and .agda-mcp/captures/*.json as side effects of the human's own session, the latter on the CHG checkout, not this repo)</files>
  <read_first>
    .agents/skills/agda-dogfooding/SKILL.md (the exact runbook to follow, unchanged — sections 1-3 and 7)
  </read_first>
  <action>
    Hand off to the human: present the exact env-var export commands and the unmodified SKILL.md steps (below, under how-to-verify) for them to run in their OWN terminal. This step is NOT automatable — D-04/D-05/D-06 require a human actually proving a real theorem through Codex against a currently-open CHG goal chosen at execution time; there is no scripted or fixture substitute for a live proof session. Wait for the human's reply naming the concrete run-id before proceeding to Task 3.
  </action>
  <what-built>
    A local, healthy ingest endpoint (`http://127.0.0.1:8787`) and a fresh, per-person Bearer key for `eric` are ready. CHG's overnight build has been confirmed settled. Everything needed for a live, complete dogfooding session — through automatic upload — is now in place.
  </what-built>
  <how-to-verify>
    In your OWN terminal (not this agent's), from the `agda-mcp-server` checkout root:

    1. Export the two env vars so the wrap-up step's upload chain (D-12) actually fires:
       `export AGDA_MCP_TEAM_UPLOAD_KEY=<the raw key printed in Task 1>`
       `export AGDA_MCP_TEAM_UPLOAD_URL=http://127.0.0.1:8787/ingest`
    2. Follow `.agents/skills/agda-dogfooding/SKILL.md` sections 1-3 UNCHANGED: launch the dogfood proxy against `$HOME/projects6/Codex-Homotopy-Group` with the task manifest from Task 1, drive Codex on a REAL open CHG goal, and call `agda_capture_session` at least once per section 3's guidance (a suspicious-green capture counts — acceptance is loop-to-verdict, not "must find a new defect").
    3. When the session ends, copy the `<run-id>` the proxy prints, then run:
       `npx tsx scripts/dogfood/dogfood-wrapup.mjs <run-id>`
       This judges the capture(s) locally AND (because the two env vars above are set) automatically uploads the run archive to the local ingest server.
  </how-to-verify>
  <resume-signal>Reply with the exact `<run-id>` once dogfood-wrapup.mjs has finished (its own stdout line names the run and the judging counts) — or describe what went wrong.</resume-signal>
  <verify>
    <automated>MISSING — inherently a human-driven live proof session; no automated substitute exists. Verification is the human's own resume-signal reply naming a real run-id.</automated>
  </verify>
  <acceptance_criteria>
    - The human replies with a concrete run-id string (the sortable ISO-timestamp + 8-hex-char shape `dogfood-run.mjs`'s own default generates, e.g. `2026-07-04T12-00-00-000Z-a1b2c3d4`).
    - The human confirms `dogfood-wrapup.mjs <run-id>` printed a `[dogfood-wrapup] run ... judged` summary line, not an error about a missing run report.
  </acceptance_criteria>
  <done>The human has completed a live Codex proving session against CHG, captured at least one artifact, and run dogfood-wrapup.mjs for that run-id — and has reported the run-id back.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Automated post-session verification + final confirmation</name>
  <files>(none — reads/verifies existing artifacts; test/fixtures/fix-queue.json is modified only as a side effect of running scripts/team/cron-ingest-wrapup.mjs)</files>
  <read_first>
    scripts/team/cron-ingest-wrapup.mjs (the exact CLI this task runs for real)
    test/fixtures/fix-queue.json (read its current CHG-fingerprinted entries BEFORE running the judge, so the dedup/recurrence-bump claim below can be verified by diff)
  </read_first>
  <action>
    Given the run-id from Task 2: confirm the archive landed on disk at `.agda-mcp/team/storage/eric/<today-UTC>/<run-id>.tar.gz`. Snapshot `test/fixtures/fix-queue.json`'s current content (for the later diff). Run the REAL cron judge with NO `--no-push` flag (this acceptance run uses the real commit+push path per D-02): `npx tsx scripts/team/cron-ingest-wrapup.mjs`. Capture its stdout summary line and read the newest file under `.agda-mcp/team/cron-runs/`. Diff `test/fixtures/fix-queue.json` against the pre-run snapshot. Present the full result to the human per what-built/how-to-verify below and wait for their explicit approval before considering this plan (and the phase) complete.
  </action>
  <what-built>
    Given the `<run-id>` from Task 2: confirmed the archive landed on disk at `.agda-mcp/team/storage/eric/<today-UTC>/<run-id>.tar.gz`. Ran the REAL cron judge (no `--no-push` — this acceptance run uses the real commit+push path per D-02): `npx tsx scripts/team/cron-ingest-wrapup.mjs`. Captured its stdout summary line (archives/captures/filed/abstention-rate) and read the newest file under `.agda-mcp/team/cron-runs/`. Diffed `test/fixtures/fix-queue.json` against its pre-run state (via `git diff test/fixtures/fix-queue.json`) to confirm either (a) an existing CHG-fingerprinted entry's `recurrence` bumped by exactly 1 with no new row, or (b) a genuinely new entry appeared with a fingerprint not previously present, or (c) the file is byte-identical (a true-green capture, correctly not filed) — all three are valid loop-to-verdict outcomes per D-05. Confirmed `git log -1 --stat` shows the automated commit (only if the queue file actually changed) and that it was pushed.
  </what-built>
  <how-to-verify>
    Independently re-run these yourself if you want to double-check:
    1. `ls -la .agda-mcp/team/storage/eric/*/` — confirm the archive file is present.
    2. `cat .agda-mcp/team/cron-runs/<newest-file>.json | head -40` — read the per-capture results and the abstention rate.
    3. `git log -3 --stat -- test/fixtures/fix-queue.json` — confirm either a new automated commit exists (queue changed) or none does (queue correctly unchanged for a true-green run).
    4. If a defect WAS confirmed and filed: note its fingerprint — it now needs to continue through the existing REVERIFY-02 fix -> regression-lock -> `locked` flow (a separate, already-shipped Phase-6-era workflow, out of this plan's own scope).
  </how-to-verify>
  <resume-signal>Type "approved" once you've confirmed a definitive per-capture verdict was reached with correct dedup behavior — or describe any discrepancy.</resume-signal>
  <verify>
    <automated>npx tsx scripts/team/cron-ingest-wrapup.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cron-ingest-wrapup.mjs`'s stdout summary line names a non-zero `totalCaptures` count and an explicit abstention-rate percentage.
    - `git diff test/fixtures/fix-queue.json` (captured before the human's final approval) shows exactly one of: a `recurrence` bump on an existing CHG entry, a genuinely new entry, or no diff at all — never an unexplained multi-entry mutation.
    - If the queue file changed, `git log -1 --stat -- test/fixtures/fix-queue.json` shows the new automated commit, and it is present on the remote (pushed).
  </acceptance_criteria>
  <done>The human has replied "approved" after independently confirming a definitive per-capture verdict was reached with correct dedup behavior against the real, tracked fix-queue SSOT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Live human-driven Codex session -> local ingest -> unattended judge -> git push | The full, real trust chain every other plan in this phase only exercised in isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-07-23 | Information Disclosure | Upload key/URL visible in the human's own shell history/env during the live session | accept | Local, trusted, single-operator machine (D-06); the key is scoped to `eric` and revocable via `issue-key.mjs revoke` immediately after this acceptance run if desired |
| T-07-24 | Repudiation | This E2E run's own automated commit landing on the real tracked branch | accept | This is D-01/D-02's INTENDED behavior for the acceptance run specifically (real commit+push path, not `--no-push`); recovery is `git revert` if the outcome is ever wrong |
</threat_model>

<verification>
Task 1's automated `curl .../healthz` returns 200. Task 2 and Task 3 are human-gated per the plan's own checkpoints — this plan is not complete until Task 3's `resume-signal` is received as "approved".
</verification>

<success_criteria>
- CHG's build readiness was verified before any live proving began.
- A real, live Codex session on CHG produced at least one judged, dedup-correct outcome in `test/fixtures/fix-queue.json` (whether that outcome is a bump, a new entry, or a confirmed no-change).
- The full chain (capture -> upload -> ingest -> cron judge -> queue write-back) ran with zero fixture shortcuts.
- The per-run INCONCLUSIVE/abstention rate was surfaced and reviewed.
</success_criteria>

<output>
Create `.planning/phases/07-team-feedback-channel-local-wiring/07-06-SUMMARY.md` when done
</output>
