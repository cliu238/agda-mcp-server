---
phase: 11-auto-sync-productionization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/upstream-sync/SKILL.md
  - test/unit/tools/sync-skill-section7.test.ts
  - test/fixtures/sync-upstream-fixture.mjs
  - test/unit/tools/sync-upstream-fixture.test.ts
autonomous: true
requirements: [SYNC-02]

must_haves:
  truths:
    - "SKILL.md Section 7 states that whenever gsd-sdk resolves on PATH, a non-silent run produces a real .planning/quick/<id>-upstream-sync-<date>/ PLAN+SUMMARY pair plus a STATE.md Quick-Tasks row, committed WITH the merge (or the revert, for rollbacks)"
    - "SKILL.md Section 7 still states the docs/UPSTREAM-SYNC-LOG.md fallback, gated on gsd-sdk being absent rather than on an environment name (local vs cloud)"
    - "Sections 1-6 and 8 of SKILL.md are unchanged by this edit — only Section 7's body differs from before"
    - "createDivergedFixtureRepos() produces a fork-standin clone with both an origin and an upstream remote, diverged by exactly 1 commit on the upstream side — mirroring SKILL.md Section 1's real two-remote layout at fixture scale"
  artifacts:
    - path: ".agents/skills/upstream-sync/SKILL.md"
      provides: "SYNC-02's GSD-native bookkeeping upgrade to Section 7 (the one in-skill edit this phase makes)"
      contains: "gsd-sdk"
    - path: "test/fixtures/sync-upstream-fixture.mjs"
      provides: "the synthetic 1-commit-ahead fixture needed for SYNC-02's manual/semi-automated bookkeeping proof (11-VALIDATION.md Wave 0 gap)"
      exports: ["createDivergedFixtureRepos"]
  key_links:
    - from: "test/fixtures/sync-upstream-fixture.mjs::createDivergedFixtureRepos"
      to: "git rev-list --count"
      via: "shape-verifying assertion in test/unit/tools/sync-upstream-fixture.test.ts"
      pattern: "rev-list"
---

<objective>
Land the ONE settled in-skill edit this phase makes (SKILL.md Section 7's bookkeeping upgrade, per SYNC-02) and build the synthetic "upstream is 1 trivial commit ahead" fixture repo 11-VALIDATION.md flags as a Wave-0 gap — the deterministic, automatable half of SYNC-02's proof. The other half (does a real `claude -p` session correctly exercise Section 7 against this fixture) is out of scope for automated testing per 11-VALIDATION.md's own classification ("Manual-Only Verification") and is not attempted here.

Purpose: SYNC-02 requires GSD-native bookkeeping to replace the skill's current "cloud has no gsd-sdk, use plain git" framing with an availability check — since even a LOCAL carrier run cannot assume `gsd-sdk` resolves on a bare launchd PATH (the same D-03 concern Plan 11-01 already encodes for `agda`/`node`/`gh`/`claude`). This plan makes that policy change exactly once, in the one place it belongs (the skill itself), and prepares the fixture future manual verification needs without waiting for a real upstream commit.
Output: An upgraded `.agents/skills/upstream-sync/SKILL.md` Section 7, a regression-locking content test for it, and a reusable diverged-repo fixture with its own shape-verifying test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-auto-sync-productionization/11-CONTEXT.md
@.planning/phases/11-auto-sync-productionization/11-PATTERNS.md
@.agents/skills/upstream-sync/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade SKILL.md Section 7 to the gsd-sdk-availability-gated bookkeeping contract</name>
  <files>.agents/skills/upstream-sync/SKILL.md, test/unit/tools/sync-skill-section7.test.ts</files>
  <read_first>
    - .agents/skills/upstream-sync/SKILL.md in full (already the complete, current 177-line file — read the WHOLE file, not just Section 7, so the edit's boundaries are precise) — current Section 7 body is: "The cloud environment has NO gsd-sdk — use plain `git` commits only. Every non-silent run appends one entry to `docs/UPSTREAM-SYNC-LOG.md`... Local/manual runs MAY wrap the whole sync in `/gsd-quick` instead; cloud runs must not attempt it."
    - 11-PATTERNS.md's ".agents/skills/upstream-sync/SKILL.md Section 7 edit" section — quotes the exact current text to replace and states the precise upgrade contract this task must encode, including "The edit upgrades the 'Local/manual runs MAY wrap... instead' line (currently optional/local-only) into the primary path whenever gsd-sdk resolves on PATH — this is exactly why the wrapper's preflight (D-03) must put gsd-sdk on PATH itself."
    - 11-CONTEXT.md's "Decisions made this discussion" preamble bullet on the Section 7 upgrade (the settled, do-not-relitigate wording: "when gsd-sdk is available, each non-silent sync produces a real .planning/quick/<id>-upstream-sync-<date>/ PLAN+SUMMARY + STATE.md Quick-Tasks row committed with the merge; docs/UPSTREAM-SYNC-LOG.md fallback when gsd-sdk absent (SYNC-02)").
    - .planning/REQUIREMENTS.md's exact SYNC-02 wording.
    - .planning/STATE.md's "Quick Tasks Completed" table (e.g. the `260705-79k-upstream-auto-sync-via-scheduled-cloud-r` row) — a real, already-existing example of the `.planning/quick/<id>-.../` naming convention this Section 7 upgrade must reference precisely.
  </read_first>
  <action>
    Edit ONLY the body of `## 7. Bookkeeping (env-adaptive)` in .agents/skills/upstream-sync/SKILL.md (currently the block starting "The cloud environment has NO gsd-sdk..." through "...cloud runs must not attempt it."). Replace it with a gsd-sdk-AVAILABILITY-gated contract (not an environment-name-gated one): first, check whether `gsd-sdk` resolves (e.g. `command -v gsd-sdk`); when it resolves, every non-silent run (mirrors Section 4's own "non-silent" framing — anything other than Step 2's 0-commits EXIT SILENTLY branch) creates a real `.planning/quick/<id>-upstream-sync-<date>/` directory containing a PLAN.md + SUMMARY.md pair (state that `<id>` follows this repo's existing quick-task id convention, citing the real STATE.md example above) plus one new row appended to STATE.md's "Quick Tasks Completed" table, BOTH committed together WITH the merge commit (or the revert, for rollbacks — matching Section 6's existing "preserve work" discipline). When `gsd-sdk` is NOT resolvable, keep the EXISTING `docs/UPSTREAM-SYNC-LOG.md` fallback behavior verbatim (same field list: date, environment, upstream range, conflicted files, gate result, deploy run id + healthz outcome, final outcome — do not change this branch's content, only its trigger condition). Retitle or rephrase the section's opening line so it no longer asserts "the cloud environment has NO gsd-sdk" as a blanket, environment-name-based fact — state instead that gsd-sdk availability is checked directly, since even a local run cannot assume it is on PATH (cite D-03's identical PATH-bareness concern for `agda`/`node`/`gh`/`claude`, established in Plan 11-01). Retain the section's final "Local/manual runs MAY wrap..." sentence's spirit but make gsd-sdk-availability the actual branch condition rather than a local/cloud distinction. Do not touch Sections 1-6 or 8, the YAML frontmatter, or any other line in the file.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-skill-section7.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-skill-section7.test.ts` exits 0 — this new test reads the file and asserts: `## 7. Bookkeeping` section body contains `gsd-sdk`, `.planning/quick`, and `STATE.md`; Section headers `## 1. Overview` through `## 8. Cadence notes` all still appear, in the original order; the Section 3 guarded-files list still contains all 9 original paths (e.g. `src/agda/session-load-impl.ts`, `src/session/agda-transport.ts`); the YAML frontmatter's `name: upstream-sync` line is unchanged
    - `git diff --stat .agents/skills/upstream-sync/SKILL.md` shows exactly 1 file changed with a modest line delta (not a full-file rewrite)
    - `grep -c "cloud environment has NO gsd-sdk" .agents/skills/upstream-sync/SKILL.md` equals 0 (the old blanket-assumption phrasing is gone)
  </acceptance_criteria>
  <done>Section 7 now gates on gsd-sdk PATH-availability (not environment name); every other section is byte-identical to before; a regression test locks both the new content and the untouched sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synthetic "upstream is 1 commit ahead" fixture for SYNC-02's bookkeeping proof</name>
  <files>test/fixtures/sync-upstream-fixture.mjs, test/unit/tools/sync-upstream-fixture.test.ts</files>
  <read_first>
    - test/unit/tools/team-clone-fuel-corpora.test.ts lines 42-64 — the `createBareFixtureRepo` helper (bare + work dir, config identity, one commit, push, capture SHA) this fixture extends.
    - 11-PATTERNS.md's "Synthetic one-commit-ahead fixture" section — the exact extension instructions: "for SYNC-02's scenario you need two clones off the same bare repo... diverged by exactly one commit on the 'upstream' side... models upstream-sync's own origin/upstream two-remote setup... at fixture scale."
    - .agents/skills/upstream-sync/SKILL.md Section 1 (Overview — the real `origin`/`upstream` two-remote layout) and Section 4 Steps 1-2 (`git fetch upstream`; `git rev-list --count main..upstream/main` — the exact commands this fixture's own shape-test reproduces at fixture scale).
    - This session's own verified real `git remote -v` output (cite directly): `origin` fetch+push = `https://github.com/cliu238/agda-mcp-server.git`; `upstream` fetch = `https://github.com/InvariantHoldings/agda-mcp-server.git`, `upstream` push = the literal sentinel string `DISABLED-push-to-cliu238-origin-instead` — this fixture's remote NAMES (not real URLs; use local filesystem paths) must mirror this exact two-remote naming.
  </read_first>
  <behavior>
    - createDivergedFixtureRepos() returns {forkClonePath, upstreamRemotePath, divergedCommitSha}; forkClonePath has a remote named "origin" and a remote named "upstream" (both local filesystem paths, mirroring the fixture style already used for "origin" in createBareFixtureRepo)
    - after `git -C forkClonePath fetch upstream`, `git -C forkClonePath rev-list --count HEAD..upstream/main` trims to exactly "1"
    - forkClonePath's own HEAD is unaffected by the fetch (fetch never merges) — `git -C forkClonePath rev-parse HEAD` differs from `divergedCommitSha`
  </behavior>
  <action>
    Create test/fixtures/sync-upstream-fixture.mjs exporting `createDivergedFixtureRepos()`. Reuse `createBareFixtureRepo`'s exact git-plumbing sequence (bare init, work-dir init, `user.email`/`user.name` config, one commit, `remote add origin <bareRepoPath>`, push) to build the "fork" side (this becomes `forkClonePath`), then build a SECOND working tree ("upstreamWorkDir") that is itself a checkout of the SAME bare repo's history plus exactly one additional trivial commit (e.g. appending one line to README.md, committed with a distinct message), and add it to `forkClonePath` as a remote named `upstream` pointing at `upstreamWorkDir`'s own filesystem path (a plain working directory can serve as a local fetch source the same way `bareRepoPath` does for `origin`, provided nothing is mid-receive there — no second bare repo is required). Return `{forkClonePath, upstreamRemotePath: upstreamWorkDir, divergedCommitSha}`. File header follows the Shared Patterns MIT-license + WHY-comment convention; this file is a TEST FIXTURE (not a `scripts/sync/*.mjs` carrier module), so it may freely use whatever style matches its sibling `test/fixtures/*.mjs` files (e.g. `test/fixtures/dogfood-fake-mcp-child.mjs`) rather than the plain-node carrier convention.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-upstream-fixture.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-upstream-fixture.test.ts` exits 0
    - The test asserts `git -C <forkClonePath> remote` output contains both `origin` and `upstream` (mirroring the exact two-remote naming from the real `git remote -v` output captured this session)
    - The test asserts the post-fetch `git rev-list --count` result is the literal string `"1"`, not merely a truthy/nonzero check
    - `node --check test/fixtures/sync-upstream-fixture.mjs` exits 0
  </acceptance_criteria>
  <done>createDivergedFixtureRepos produces a real, local, no-network two-remote git layout diverged by exactly one commit, ready for a future manual/semi-automated SYNC-02 proof run against a real or synthetic `claude -p "/upstream-sync"` session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md policy text -> a live `claude -p` session's interpretation | This plan edits POLICY TEXT only; it does not itself execute the skill or touch the merge/verify/escalate/rollback mechanics |
| test fixture repos -> local filesystem only | `createDivergedFixtureRepos` never touches a network remote; all "remotes" are local paths, zero credential surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering / Elevation of Privilege | .agents/skills/upstream-sync/SKILL.md (Sections 2-6, unchanged by this plan) | accept | The already-locked D-06 boundary (`--dangerously-skip-permissions` bounded by clone isolation + the skill's own guarded-file list + 2-fix-attempt bound) is NOT touched by this plan — the regression test in Task 1 explicitly locks Sections 1-6/8 as byte-identical, so this plan cannot silently weaken that boundary |
| T-11-08 | Repudiation (bookkeeping gap) | SKILL.md Section 7 (edited) | mitigate | The upgraded Section 7 makes bookkeeping availability-gated (gsd-sdk on PATH) rather than environment-name-gated, closing the gap where a LOCAL run with a bare launchd PATH could silently fall through to neither bookkeeping path being satisfied |
| T-11-SC | Supply chain (package installs) | N/A | accept | Not applicable — zero new npm/pip/cargo packages (11-RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
`npx vitest run test/unit/tools/sync-skill-section7.test.ts test/unit/tools/sync-upstream-fixture.test.ts` exits 0. `git diff .agents/skills/upstream-sync/SKILL.md` shows changes confined to the Section 7 heading's body.
</verification>

<success_criteria>
- SKILL.md Section 7 encodes the gsd-sdk-availability-gated bookkeeping contract; every other section is untouched and regression-locked by test.
- A reusable, local, no-network fixture exists for SYNC-02's future manual/semi-automated non-silent-run proof.
</success_criteria>

<output>
Create `.planning/phases/11-auto-sync-productionization/11-02-SUMMARY.md` when done
</output>
