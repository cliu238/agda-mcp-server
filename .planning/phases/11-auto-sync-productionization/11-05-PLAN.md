---
phase: 11-auto-sync-productionization
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - docs/UPSTREAM-SYNC-CARRIER.md
autonomous: false
requirements: [SYNC-03]

must_haves:
  truths:
    - "The real com.cliu238.agda-mcp-server.upstream-sync LaunchAgent is installed and registered on this machine (launchctl list shows it, last exit code 0)"
    - "One real launchctl kickstart of that job completed end-to-end through the wrapper's own guard/preflight/spawn/outcome-classification chain — identical execution context to a genuine 03:00 calendar fire (D-07) — without waiting up to 3 days for a natural fire"
    - "A 0-new-upstream-commits (no-op) outcome is treated as a fully accepted, successful SYNC-03 proof (D-07) — the heartbeat/log trace IS the required bookkeeping artifact for that outcome, per D-04/D-07"
    - "The captured state.json/carrier.log evidence, and the runbook's own literal-output section quoting it, contain zero raw secrets, tokens, or full environment dumps"
  artifacts:
    - path: "~/.agda-mcp/upstream-sync/state.json"
      provides: "the live proof: lastWrapperTouchAt (and, if a real invocation occurred, lastClaudeInvocationAt/lastClaudeInvocationOutcome) advanced past the pre-kickstart baseline"
    - path: "docs/UPSTREAM-SYNC-CARRIER.md"
      provides: "a new 'Literal output from the <date> D-07 kickstart proof run' section, quoting the real captured evidence verbatim"
  key_links:
    - from: "launchctl kickstart -k gui/$(id -u)/com.cliu238.agda-mcp-server.upstream-sync"
      to: "scripts/sync/run-upstream-sync.mjs"
      via: "the installed plist's ProgramArguments, fired on-demand instead of waiting for the calendar"
      pattern: "kickstart"
---

<objective>
Prove SYNC-03 for real: install the carrier on this machine (if not already installed from a prior manual test) and trigger exactly one `launchctl kickstart` of the real job — the same execution context, environment, and permissions a genuine 03:00 calendar fire would have (D-07) — then capture the resulting evidence. Per the phase's own settled decision, a 0-new-upstream-commits no-op outcome (the likely real-world result, since Phase 10 just fully reconciled upstream) is a complete, accepted success; this task does not attempt to manufacture a more "interesting" outcome.

Purpose: This is the phase's final, irreducible proof obligation — SYNC-03 explicitly requires "one real end-to-end headless carrier run" through the actual gates, not a simulation. Everything in Plans 11-01 through 11-04 exists to make this one live run safe, observable, and loudly-failing-if-broken; this plan is where that gets exercised for real.
Output: A live, running, registered LaunchAgent on this machine; one real kickstart's worth of `state.json`/`carrier.log` evidence; a runbook entry quoting it verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-auto-sync-productionization/11-CONTEXT.md
@.planning/phases/11-auto-sync-productionization/11-VALIDATION.md
@docs/UPSTREAM-SYNC-CARRIER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install for real, kickstart once, capture the evidence</name>
  <files>docs/UPSTREAM-SYNC-CARRIER.md</files>
  <read_first>
    - docs/UPSTREAM-SYNC-CARRIER.md (Plan 11-04's own output — the exact install/kickstart/log-location commands to run; do not re-derive a different invocation here).
    - 11-CONTEXT.md's D-07 (kickstart is the accepted SYNC-03 proof mechanism; a no-op outcome is an ACCEPTED SUCCESS, not a partial result) and the "Specific Ideas" note ("the plan should treat 'kickstart -> pre-flight green -> skill enters -> 0 commits -> silent exit -> heartbeat updated' as full success, not try to manufacture a merge").
    - 11-VALIDATION.md's SYNC-03 row and "Manual-Only Verifications" table (the exact expected inspection commands and acceptance framing).
    - scripts/sync/run-upstream-sync.mjs, scripts/sync/install-launchd-carrier.mjs (Plans 11-03/11-04 output — the real behavior being exercised; re-read only if you need to confirm an exact env var or path name before running the real commands).
  </read_first>
  <action>
    Run `node scripts/sync/install-launchd-carrier.mjs` for real on this machine (installs the real plist, creates the real dedicated clone with its two-remote layout and in-clone skill-discovery symlink, runs `launchctl bootstrap`) — capture its full stdout/stderr. Confirm `launchctl list | grep com.cliu238.agda-mcp-server.upstream-sync` shows the job registered. Record the CURRENT `~/.agda-mcp/upstream-sync/state.json` content (or its absence, on a genuinely first-ever install) as the pre-kickstart baseline. Run `launchctl kickstart -k gui/$(id -u)/com.cliu238.agda-mcp-server.upstream-sync` — the D-07 proof trigger, identical execution context to a real 03:00 fire. Poll `~/.agda-mcp/upstream-sync/state.json`'s `lastWrapperTouchAt` field (bounded wait — up to roughly 15 minutes, polling every 15-30 seconds) until it advances past the pre-kickstart baseline, confirming the wrapper genuinely ran (rather than the kickstart command merely returning without the job actually executing). Read the final `state.json`, the tail of `carrier.log`, and the tails of `launchd-stdout.log`/`launchd-stderr.log`. Append a new "Literal output from the <today's date> D-07 kickstart proof run" section to `docs/UPSTREAM-SYNC-CARRIER.md` (mirror `docs/DEPLOY-OPERATIONS.md`'s exact "Literal output from the `<date>` run" house-style convention) quoting the real captured `state.json` content, the relevant `carrier.log` tail lines, and the `launchctl list` grep line verbatim. Before appending, scan every piece of captured text for anything that looks like a secret/token/full environment dump — per this plan's own security gate, none should be present given Plan 11-01's `notifyCarrierFailure`/Plan 11-03's `computeClaudeOutcome` logging discipline, but confirm directly rather than assuming. A 0-new-upstream-commits outcome (the CONTEXT.md-predicted likely result, since Phase 10 just merged everything upstream had) is a fully accepted, successful SYNC-03 proof — do not attempt to manufacture a fake upstream commit to force a "more interesting" result.
  </action>
  <verify>
    <automated>launchctl list | grep -c com.cliu238.agda-mcp-server.upstream-sync</automated>
  </verify>
  <acceptance_criteria>
    - `launchctl list | grep com.cliu238.agda-mcp-server.upstream-sync` exits 0 with output containing the label and a last-exit-code of 0
    - `~/.agda-mcp/upstream-sync/state.json` exists and its `lastWrapperTouchAt` timestamp is strictly newer than the pre-kickstart baseline captured at the start of this task
    - `~/.agda-mcp/upstream-sync/carrier.log` has at least one new line appended, timestamped after the kickstart command ran
    - `docs/UPSTREAM-SYNC-CARRIER.md` contains a new "Literal output from" section quoting the real captured evidence
    - A manual read of the captured `carrier.log` tail and the new runbook section confirms no raw secret/token/environment-dump content is present
  </acceptance_criteria>
  <done>The real installed LaunchAgent, kickstarted once through its actual guard/preflight/spawn chain, produced a genuine heartbeat/log update — a no-op or a real merge attempt are both fully acceptable per D-07 — captured verbatim in the runbook.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms the live SYNC-03 proof</name>
  <files>docs/UPSTREAM-SYNC-CARRIER.md (reviewed, not further modified by this task)</files>
  <read_first>
    - docs/UPSTREAM-SYNC-CARRIER.md's freshly-appended "Literal output from..." section (Task 1's own output).
    - 11-CONTEXT.md's D-07 and "Specific Ideas" note (what outcome to expect and accept) and D-05 (the dual-channel notification path — only relevant if the captured outcome shows a failure, which is not the expected result here).
  </read_first>
  <what-built>
    Claude installed the real launchd carrier on this Mac (`com.cliu238.agda-mcp-server.upstream-sync`) and triggered one real `launchctl kickstart` run — the same execution context, environment, and permissions a genuine 03:00 calendar fire would have — then captured the resulting `state.json`/`carrier.log` evidence into `docs/UPSTREAM-SYNC-CARRIER.md`.
  </what-built>
  <action>
    Present Task 1's captured evidence (the `launchctl list` line, the `state.json`/`carrier.log` tails, and the new runbook section) to the operator, then pause for their explicit confirmation before SYNC-03 is considered proven — this is the one step in this plan Claude cannot self-certify, since it includes a visual (notification-banner) check Claude has no way to observe from within its own session.
  </action>
  <how-to-verify>
    1. Run `launchctl list | grep com.cliu238.agda-mcp-server.upstream-sync` — confirm it is present with a last exit code of 0.
    2. Open `~/.agda-mcp/upstream-sync/carrier.log` and confirm the newest entry's timestamp matches the just-completed kickstart run.
    3. Read `docs/UPSTREAM-SYNC-CARRIER.md`'s new "Literal output from..." section — confirm the recorded outcome (no-op, or a real merge attempt) matches what you'd expect given Phase 10 just fully reconciled upstream, and confirm no raw secret/token/environment-dump content is visible anywhere in it.
    4. ONLY if the captured outcome shows a carrier-level FAILURE path (not the expected no-op success): confirm you actually SEE a macOS notification banner on screen (this is the one thing genuinely unverifiable by Claude itself — command exit 0 does not prove a banner rendered). If no banner appeared, check System Settings -> Notifications for the requesting process and note it as a follow-up — do not block sign-off on this alone, since the `gh issue create` channel is the primary, independently-verifiable channel per D-05's design.
  </how-to-verify>
  <verify>
    <automated>launchctl list | grep -c com.cliu238.agda-mcp-server.upstream-sync</automated>
  </verify>
  <acceptance_criteria>
    The human's "approved" response confirms: the launchd job is registered with last exit code 0; carrier.log/state.json show a fresh, correctly-timestamped entry; the runbook's captured evidence matches the expected outcome class and contains no leaked secrets; and (only if applicable) the notification banner was visually confirmed.
  </acceptance_criteria>
  <resume-signal>Type "approved" once steps 1-3 (and step 4, only if applicable) are confirmed, or describe any issue observed.</resume-signal>
  <done>Operator has typed "approved", confirming the live launchd job, its fresh heartbeat/log evidence, and the runbook's captured section are all correct and free of leaked secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The live kickstart itself | The FIRST real execution of `--dangerously-skip-permissions` via this exact unattended pathway, inside the dedicated, physically-isolated sync clone |
| Captured evidence -> committed runbook | `docs/UPSTREAM-SYNC-CARRIER.md`'s new literal-output section becomes a permanently committed, public-repo-visible artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering / Elevation of Privilege | The live `--dangerously-skip-permissions` kickstart run | accept | This IS the D-06/D-07-approved live proof — the already-locked mitigation (clone isolation + the skill's guarded-file list + 2-fix-attempt bound) applies unchanged; this is a foreground, operator-supervised kickstart (not a blind background fire), giving the human a chance to intervene if anything looks wrong |
| T-11-06 | Information Disclosure | Task 1's captured evidence, appended into the committed runbook | mitigate | Task 1's own action explicitly requires a manual secret/token/env-dump scan before appending; Task 2's checkpoint step 3 requires the human to independently confirm the same before approving |
| T-11-SC | Supply chain (package installs) | N/A | accept | Not applicable — zero new npm/pip/cargo packages |
</threat_model>

<verification>
`launchctl list | grep com.cliu238.agda-mcp-server.upstream-sync` exits 0 with last-exit-code 0. `~/.agda-mcp/upstream-sync/state.json` and `carrier.log` both show a fresh, post-kickstart timestamp. `docs/UPSTREAM-SYNC-CARRIER.md` contains the new literal-output section. The human checkpoint is explicitly approved.
</verification>

<success_criteria>
SYNC-03 is proven: one real end-to-end headless carrier run has executed through its actual guard/preflight/spawn/outcome chain on this machine, via `launchctl kickstart` (not a 3-day wait), with the resulting bookkeeping artifact (heartbeat/log trace, per D-04/D-07, since a no-op outcome is the expected result) captured and human-confirmed.
</success_criteria>

<output>
Create `.planning/phases/11-auto-sync-productionization/11-05-SUMMARY.md` when done
</output>
