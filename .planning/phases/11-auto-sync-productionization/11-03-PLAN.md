---
phase: 11-auto-sync-productionization
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - scripts/sync/run-upstream-sync.mjs
  - test/unit/tools/sync-run-upstream-sync.test.ts
  - test/fixtures/sync-nested-sleep-child.mjs
autonomous: true
requirements: [SYNC-01]

must_haves:
  truths:
    - "run-upstream-sync.mjs composes shouldRunSync + the UPSTREAM_SYNC_FORCE bypass before doing anything else — a guard-blocked run touches heartbeat/log and exits 0 without ever calling any preflight assertion or spawning claude"
    - "A preflight assertion failure notifies via notifyCarrierFailure, writes heartbeat/log with a failure outcome, and NEVER reaches the claude spawn step"
    - "The hard-timeout kill signals the ENTIRE process group (direct child + its own grandchild), not just the direct child — proven by a real, short, zero-LLM-cost subprocess test"
    - "Every terminal path (guard-exit, preflight-fail, claude success, claude failure) writes the interval-guard state and appends to carrier.log — D-04's 'every run touches heartbeat+log' is true with no exception"
  artifacts:
    - path: "scripts/sync/run-upstream-sync.mjs"
      provides: "the carrier's orchestrator: guard -> preflight -> spawn+timeout-kill -> outcome classification -> state/log write"
      exports: ["computeClaudeOutcome", "scriptMain"]
    - path: "test/fixtures/sync-nested-sleep-child.mjs"
      provides: "a real nested-grandchild-spawning fixture standing in for a hung claude -p that itself spawned a hung agda/npm"
  key_links:
    - from: "scripts/sync/run-upstream-sync.mjs"
      to: "scripts/sync/interval-guard.mjs::shouldRunSync"
      via: "import"
      pattern: "shouldRunSync"
    - from: "scripts/sync/run-upstream-sync.mjs"
      to: "scripts/sync/preflight-assertions.mjs::assert*"
      via: "import, gates the spawn"
      pattern: "assertAgdaPresent|assertNode24Present|assertGhAuth|assertClaudeAuth|assertCleanSyncClone|assertSkillDiscoverable"
    - from: "scripts/sync/run-upstream-sync.mjs"
      to: "scripts/sync/carrier-notify.mjs::notifyCarrierFailure"
      via: "import, called on any failure path"
      pattern: "notifyCarrierFailure"
    - from: "scripts/sync/run-upstream-sync.mjs"
      to: "claude -p \"/upstream-sync\" --dangerously-skip-permissions"
      via: "child_process.spawn, detached:true, cwd=dedicated sync clone"
      pattern: "dangerously-skip-permissions"
---

<objective>
Build `scripts/sync/run-upstream-sync.mjs` — the carrier's single orchestrator, the exact file launchd's `ProgramArguments` will point at. It composes Plan 11-01's three modules in the order RESEARCH.md's own architecture diagram specifies (interval-guard -> preflight -> spawn+timeout-kill -> outcome classification -> state/log write, notifying on any failure path), then proves the single highest-risk mechanism in this whole phase — the process-group hard-timeout kill — with a real, short, zero-LLM-cost subprocess test mirroring the codebase's own already-shipped `dogfood-run.mjs` mechanism.

Purpose: This is the exact file 11-RESEARCH.md frames as "a composite of two exact matches" (`cron-ingest-wrapup.mjs`'s CLI/DI/summary shape plus `dogfood-run.mjs`'s spawn+timeout-kill mechanics) and the phase's single highest-complexity, highest-risk piece — RESEARCH.md's own live reproduction proved a naive `spawn({timeout})` leaves a hung grandchild (e.g. a wedged `agda`) running forever past the deadline. Getting this file right is what makes SYNC-01's "real-Agda full verification gates" promise actually enforceable rather than aspirational.
Output: `scripts/sync/run-upstream-sync.mjs`, its Vitest suite (DI-stubbed decision-logic tests plus one real-subprocess process-group-kill integration test), and the small fixture the latter needs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-auto-sync-productionization/11-CONTEXT.md
@.planning/phases/11-auto-sync-productionization/11-RESEARCH.md
@.planning/phases/11-auto-sync-productionization/11-PATTERNS.md

<interfaces>
<!-- Exact contracts this plan imports from Plan 11-01's output — no further exploration needed. -->

From scripts/sync/interval-guard.mjs (Plan 11-01):
```javascript
export function shouldRunSync(lastClaudeInvocationAt, now = new Date(), minDays = 3);
export function readIntervalState(statePath); // -> {lastWrapperTouchAt, lastClaudeInvocationAt, lastClaudeInvocationOutcome} | null
export function writeIntervalState(statePath, state);
```

From scripts/sync/preflight-assertions.mjs (Plan 11-01):
```javascript
export function assertAgdaPresent(deps = {});          // -> {ok, detail}
export function assertNode24Present(deps = {});        // -> {ok, detail}
export function assertGhAuth(deps = {});                // -> {ok, detail}
export function assertClaudeAuth(deps = {});             // -> {ok, detail}
export function assertCleanSyncClone(clonePath, deps = {});     // -> {ok, reason?}
export function assertSkillDiscoverable(clonePath, deps = {});  // -> {ok, reason?}
```

From scripts/sync/carrier-notify.mjs (Plan 11-01):
```javascript
export function notifyCarrierFailure(rawFailureInfo, deps = {}); // sanitizes internally, always tries both channels
```

From RESEARCH.md's Pattern 2 Code Example (the exact spawn+timeout-kill shape to mirror, extended per PATTERNS.md's note below):
```javascript
const HARD_TIMEOUT_MS = 2 * 60 * 60 * 1000;
const GRACE_MS = 10_000;
const child = spawn(resolvedCommand, args, { cwd: syncClonePath, env: resolvedEnv, detached: true });
const killTimer = setTimeout(() => {
  try { process.kill(-child.pid, "SIGTERM"); } catch {}
  setTimeout(() => { try { process.kill(-child.pid, "SIGKILL"); } catch {} }, GRACE_MS);
}, HARD_TIMEOUT_MS);
```

From scripts/dogfood/dogfood-run.mjs lines 200-243 (the STRONGER analog to mirror instead of RESEARCH.md's own inline example — already-shipped, already-tested in this codebase):
```javascript
export function computeProxyExitCode({ childExitCode, childSignalCode, childFailed, proxyKilledChild }) { /* ... */ }
function killChildGroup(childProc, signal) { /* guards non-number pid, swallows ESRCH */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run-upstream-sync.mjs — guard/preflight/spawn/outcome decision logic</name>
  <files>scripts/sync/run-upstream-sync.mjs, test/unit/tools/sync-run-upstream-sync.test.ts</files>
  <read_first>
    - 11-RESEARCH.md "Architecture Patterns" > "System Architecture Diagram" (the exact 5-step flow this file implements, including the "regardless of whether the skill found 0 or N upstream commits — that distinction is the SKILL's" note on step 5).
    - 11-RESEARCH.md "Code Examples" > "Parsing `claude -p --output-format json`'s captured shape" (the exact verified JSON-array shape: `[{"type":"system",...},...,{"type":"result","subtype":"success","is_error":false,...}]` — find the `type:"result"` event, check `is_error===false && subtype==="success"`).
    - 11-RESEARCH.md Pitfall 2 ("`--bare` silently breaks authentication") — never pass `--bare` to the `claude -p` invocation this task builds.
    - 11-RESEARCH.md Pitfall 4 (interval-guard vs `launchctl kickstart` conflict) and its recommended fix: an explicit bypass env var — this task names it `UPSTREAM_SYNC_FORCE`.
    - 11-PATTERNS.md "scripts/sync/run-upstream-sync.mjs" section in full, BOTH analogs: Analog 1 (`cron-ingest-wrapup.mjs`'s scriptMain shape, lines 76-92 quoted there: DI-via-deps, per-run summary, `isMainModule` gate) and Analog 2 (`dogfood-run.mjs`'s `computeProxyExitCode`/`killChildGroup`, quoted at lines 134-144 there) — read the explicit "Keep"/"Change" notes for both.
    - scripts/team/cron-ingest-wrapup.mjs lines 645-705 (the exact `scriptMain` shape: per-run JSON summary under a gitignored dir, one-line stdout digest, `process.exitCode = 1` on failure, `isMainModule` gate — mirror the SHAPE, not the target directory, which this file changes per its own PATTERNS.md note).
    - .planning/phases/11-auto-sync-productionization/11-CONTEXT.md D-04 (every run — including guard-exits and preflight-failures — touches heartbeat+log, outside git) and D-05 (dual-channel notify fires ONLY on carrier-level failure, never on a skill-level 0-commit success).
    - This file MUST stay plain-`node`-loadable (11-PATTERNS.md's "Cross-File Decision Point"): zero `src/*.ts` imports; compute any local paths itself rather than importing `SERVER_REPO_ROOT` from `src/repo-root.ts`.
  </read_first>
  <behavior>
    - computeClaudeOutcome(stdout, {timedOut:false}) given the RESEARCH-verified JSON array with `is_error:false, subtype:"success"` returns {ok:true}
    - computeClaudeOutcome(stdout, {timedOut:false}) given `is_error:true` in the result event returns {ok:false, reason:"skill-reported-error"}
    - computeClaudeOutcome("not json", {timedOut:false}) returns {ok:false, reason:"unparseable-output"}
    - computeClaudeOutcome(anyStdout, {timedOut:true}) returns {ok:false, reason:"timeout"} regardless of stdout content
    - computeClaudeOutcome("[]", {timedOut:false, childExitCode:1}) returns {ok:false, reason:"crash"} (no result event found, non-zero exit)
    - Given `process.env.UPSTREAM_SYNC_FORCE` unset and a DI-injected `shouldRunSync`-equivalent state showing < 3 days elapsed: the run writes heartbeat/state (via injected fake writeIntervalState/log-append spies) and returns/exits 0 WITHOUT calling any injected preflight-assertion spy and WITHOUT calling the injected spawn spy
    - Given `process.env.UPSTREAM_SYNC_FORCE === "1"` and the same < 3 days state: preflight assertions ARE called (guard bypassed)
    - Given one injected preflight assertion returns {ok:false}: the injected `notifyCarrierFailure` spy is called exactly once with an allowlist-shaped raw info object, heartbeat/state is written with a failure outcome, `process.exitCode` is set to 1, and the injected spawn spy is NEVER called
  </behavior>
  <action>
    Create scripts/sync/run-upstream-sync.mjs. Define constants: `DEFAULT_HARD_TIMEOUT_MS` read from `process.env.UPSTREAM_SYNC_TIMEOUT_MS`, falling back to `7_200_000` (2h, per CONTEXT.md's "~2h suggested" discretion note and RESEARCH.md Assumption A3); `GRACE_MS = 10_000`; `DEFAULT_MAX_BUDGET_USD` read from `process.env.UPSTREAM_SYNC_MAX_BUDGET_USD`, falling back to `25` (a conservative default per RESEARCH.md Open Question 2's "$20-50" range, documented here as tunable via that env var); `DEFAULT_MIN_DAYS = 3`. Default paths (each overridable via an env var, mirroring `AGDA_MCP_FUEL_ROOT`'s exact override precedent in clone-fuel-corpora.mjs): sync clone at `join(homedir(), ".agda-mcp", "upstream-sync", "clone")` (override `UPSTREAM_SYNC_CLONE_ROOT`), state root at `join(homedir(), ".agda-mcp", "upstream-sync")` (override `UPSTREAM_SYNC_STATE_ROOT`) containing `state.json` and `carrier.log`. Implement `scriptMain()` following this exact order: (1) read the interval-guard state via `readIntervalState`, compute `forced = process.env.UPSTREAM_SYNC_FORCE === "1"`, and short-circuit when `!forced && !shouldRunSync(state?.lastClaudeInvocationAt, new Date(), DEFAULT_MIN_DAYS)` — write `{...state, lastWrapperTouchAt: now}` via `writeIntervalState`, append one `carrier.log` line, return without calling any preflight assertion; (2) call all six imported preflight assertions in sequence — on ANY `{ok:false}`, build a raw failure info object (`{step:"preflight", checks:{agda,node24,gh,claude,syncClone,skillDiscoverable}, ...}`, booleans only, no raw `detail` strings passed through unfiltered) and call `notifyCarrierFailure`, write state with `lastClaudeInvocationOutcome:"preflight-failed"`, append `carrier.log`, set `process.exitCode = 1`, return WITHOUT spawning; (3) spawn `"claude"` (bare command name — PATH resolution happens via the plist's `EnvironmentVariables`, built by Plan 11-04's installer, not by this file) with args `["-p", "/upstream-sync", "--dangerously-skip-permissions", "--output-format", "json", "--max-budget-usd", String(maxBudgetUsd)]`, `{ cwd: syncClonePath, env: process.env, detached: true }` — NEVER add `--bare` (Pitfall 2); (4) enforce the hard timeout via a `setTimeout` calling a `killChildGroup(child, signal)` helper ported near-verbatim from `scripts/dogfood/dogfood-run.mjs` lines 233-243 (guards a non-number `pid`, swallows ESRCH), escalating from `SIGTERM` to `SIGKILL` after `GRACE_MS` if the child is still alive — cite `dogfood-run.mjs`'s own `finalize()` SIGTERM-then-timed-SIGKILL escalation shape (lines 447-475) as the two-stage pattern to mirror, not just the single-signal `killChildGroup` call; (5) on child close/exit, call the separately-exported pure `computeClaudeOutcome(bufferedStdout, {timedOut, childExitCode, childSignalCode})` — on `{ok:true}`, write state with `lastClaudeInvocationAt: now, lastClaudeInvocationOutcome:"success"` (regardless of how many upstream commits the skill itself found — that distinction is the skill's own business per the architecture diagram) and append `carrier.log`; on `{ok:false}`, call `notifyCarrierFailure` with a raw info object built ONLY from `{step:"claude-spawn", exitCode, signalCode, timedOut, reason}` (never the raw stdout blob), write state with outcome `"failure"`, append `carrier.log`, set `process.exitCode = 1`. Add `isMainModule`-gated CLI entry at the bottom (mirror `scripts/team/cron-ingest-wrapup.mjs`'s exact gate). Compute any needed local paths without importing from `src/*.ts` (this module needs none — all its paths are HOME-relative, not repo-relative).
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-run-upstream-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-run-upstream-sync.test.ts` exits 0 (this task's own DI-stubbed cases; Task 2 appends the real-subprocess case to the same file afterward)
    - `grep -c "export function computeClaudeOutcome" scripts/sync/run-upstream-sync.mjs` equals 1
    - `grep -c "notifyCarrierFailure" scripts/sync/run-upstream-sync.mjs` >= 1 (imported AND called, not dead)
    - `grep -c -- "--bare" scripts/sync/run-upstream-sync.mjs` equals 0 (Pitfall 2 — never pass `--bare`)
    - `grep -c "dangerously-skip-permissions" scripts/sync/run-upstream-sync.mjs` equals 1 (D-06's locked flag, present exactly where the spawn happens)
    - `node --check scripts/sync/run-upstream-sync.mjs` exits 0
    - `grep -c "from \"../../src/" scripts/sync/run-upstream-sync.mjs` (and `../src/`) both equal 0
  </acceptance_criteria>
  <done>run-upstream-sync.mjs composes interval-guard, preflight-assertions, and carrier-notify in the documented order; every terminal branch writes heartbeat+log; a preflight failure never reaches the spawn step; computeClaudeOutcome correctly classifies all 5 documented outcome shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Real process-group-kill integration test — proves the hard timeout reaps the whole tree</name>
  <files>test/unit/tools/sync-run-upstream-sync.test.ts, test/fixtures/sync-nested-sleep-child.mjs</files>
  <read_first>
    - test/unit/tools/dogfood-run-report-checkpoint.test.ts in full (already read this session — the file header's framing of WHY plain `child.kill()` is insufficient; the `killGroup`/`isAlive`/`spawnProxy`/`waitFor` helpers at lines 92-214; the `afterEach` belt-and-suspenders reaper).
    - test/fixtures/dogfood-fake-mcp-child.mjs (read this file — the existing lightweight fixture-substitution contract this new fixture mirrors, including how it reports its own PID/state back to the test).
    - 11-RESEARCH.md Pitfall 1 ("`spawn({timeout})` alone does not kill grandchild processes") — this test proves the fix for the EXACT failure mode this pitfall describes, reproduced live during this phase's own research session.
    - scripts/sync/run-upstream-sync.mjs (this plan's own Task 1 output — the real hard-timeout-kill code path this test spawns as a genuine OS subprocess, including the exact env var names `UPSTREAM_SYNC_TIMEOUT_MS` and whatever test-only child-substitution env var Task 1 did NOT yet define — this task introduces it).
  </read_first>
  <action>
    Create test/fixtures/sync-nested-sleep-child.mjs: a tiny plain-`node` script that, on start, immediately spawns its OWN detached grandchild (e.g. `spawn("sleep", ["30"])`), writes that grandchild's PID to a file path taken from an env var (name it `UPSTREAM_SYNC_TEST_GRANDCHILD_PIDFILE`, mirroring `dogfood-fake-mcp-child.mjs`'s own `AGDA_MCP_DOGFOOD_TEST_CHILD_GRANDCHILD_PIDFILE` precedent exactly), then itself sits idle (never exits on its own) so the wrapper's hard-timeout kill is the only thing that can end it. Add a test-only substitution seam to scripts/sync/run-upstream-sync.mjs's Task-1 implementation ONLY if one is not already present: read `process.env.UPSTREAM_SYNC_TEST_CHILD_ENTRY` and, when set, spawn that path with plain `node` instead of the real `"claude"` binary — mirror `buildDogfoodChildOptions`'s exact `AGDA_MCP_DOGFOOD_TEST_CHILD_ENTRY` substitution technique (dogfood-run.mjs lines 90-98), which has zero effect on any real invocation since the env var is unset there. Append a new test to test/unit/tools/sync-run-upstream-sync.test.ts (do not create a second file) that: spawns `scripts/sync/run-upstream-sync.mjs` as a REAL OS subprocess via plain `node` (not `tsx` — this file is plain-node-loadable, unlike `dogfood-run.mjs`'s own `tsx`-requiring test), with `UPSTREAM_SYNC_TEST_CHILD_ENTRY` pointing at the new nested-sleep-child fixture, `UPSTREAM_SYNC_TIMEOUT_MS` set to a small value (e.g. `200`) so the test completes in real seconds, `UPSTREAM_SYNC_FORCE=1` (bypass the interval guard so the spawn actually happens), and DI-free real env vars for the clone/state paths pointed at a temp directory; after the wrapper's own kill-and-exit, uses `isAlive(pid)` (ported from `dogfood-run-report-checkpoint.test.ts`) to confirm BOTH the direct child's PID and the grandchild PID recorded in the pidfile are dead. Reuse `killGroup`/`waitFor`/the `afterEach` reaper array near-verbatim (cite the exact source file) so a failing assertion can never leak a live 30-second `sleep` process into later tests or CI.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-run-upstream-sync.test.ts -t "process group kill"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-run-upstream-sync.test.ts -t "process group kill"` exits 0 — matches 11-VALIDATION.md's exact required command
    - `test/fixtures/sync-nested-sleep-child.mjs` exists; `node --check test/fixtures/sync-nested-sleep-child.mjs` exits 0
    - `grep -c "isAlive" test/unit/tools/sync-run-upstream-sync.test.ts` >= 1 (the grandchild-liveness assertion, not just the direct child)
    - `grep -c "afterEach" test/unit/tools/sync-run-upstream-sync.test.ts` >= 1 (belt-and-suspenders reaper present)
    - `npx vitest run test/unit/tools/sync-run-upstream-sync.test.ts` (the whole file, both tasks' cases together) exits 0
  </acceptance_criteria>
  <done>A real, short (seconds), zero-LLM-cost subprocess test proves the hard-timeout kill reaps the ENTIRE process tree — direct child and its own independently-spawned grandchild — directly exercising the exact pitfall RESEARCH.md's own live reproduction found and fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run-upstream-sync.mjs -> `claude -p` subprocess | The spawned session runs with `--dangerously-skip-permissions` inside the dedicated, physically-isolated sync clone (D-02/D-06) |
| run-upstream-sync.mjs -> carrier.log / state.json | Local-only, outside git; must never contain raw secrets or the full `claude -p` stdout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Denial of Service (resource leak) | scripts/sync/run-upstream-sync.mjs's hard-timeout kill | mitigate | `detached:true` + negative-PID `process.kill(-pid, signal)`, SIGTERM-then-graced-SIGKILL escalation, ported from the already-shipped `dogfood-run.mjs`; proven by Task 2's real subprocess test against a genuine nested grandchild |
| T-11-01 | Tampering / Elevation of Privilege (via silent degradation) | run-upstream-sync.mjs's preflight-orchestration step | mitigate (consumes Plan 11-01's assertions) | ANY of the six assertions failing aborts BEFORE the spawn — verified by a DI-stubbed test asserting the spawn spy is never called on a preflight failure |
| T-11-06 | Information Disclosure | run-upstream-sync.mjs's carrier.log / notifyCarrierFailure call sites | mitigate | Only `computeClaudeOutcome`'s classified `{ok, reason}` is ever logged or passed into `notifyCarrierFailure` — the raw `claude -p` stdout blob (which could reflect session content) is never written to carrier.log or included in a notification/issue body |
| T-11-04 | Tampering / Elevation of Privilege | `--dangerously-skip-permissions` spawn (D-06, already locked) | accept | This plan implements the LOCKED invocation exactly as specified (cwd = dedicated clone, flag present) — it does not introduce or expand this boundary, only wires the already-approved design |
| T-11-SC | Supply chain (package installs) | N/A | accept | Not applicable — zero new npm/pip/cargo packages |
</threat_model>

<verification>
`npx vitest run test/unit/tools/sync-run-upstream-sync.test.ts` (whole file) exits 0, including the real-subprocess process-group-kill case. `node --check scripts/sync/run-upstream-sync.mjs` exits 0. Confirm via `grep` that `--bare` never appears and `--dangerously-skip-permissions` appears exactly once.
</verification>

<success_criteria>
- run-upstream-sync.mjs exists, composes all three Plan 11-01 modules in the documented order, and exports `computeClaudeOutcome` as an independently-tested pure function.
- The process-group hard-timeout kill is proven — via a real, short, zero-LLM-cost subprocess test — to reap an entire nested process tree, not just the direct child.
</success_criteria>

<output>
Create `.planning/phases/11-auto-sync-productionization/11-03-SUMMARY.md` when done
</output>
