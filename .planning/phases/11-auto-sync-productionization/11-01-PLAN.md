---
phase: 11-auto-sync-productionization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sync/interval-guard.mjs
  - test/unit/tools/sync-interval-guard.test.ts
  - scripts/sync/preflight-assertions.mjs
  - test/unit/tools/sync-preflight-assertions.test.ts
  - scripts/sync/carrier-notify.mjs
  - test/unit/tools/sync-carrier-notify.test.ts
autonomous: true
requirements: [SYNC-01]

must_haves:
  truths:
    - "shouldRunSync(lastClaudeInvocationAt, now, minDays) returns true with no prior record, true when elapsed >= minDays, false otherwise, and fails OPEN (true) on an unparseable timestamp (RESEARCH.md Pitfall 5)"
    - "Each of assertAgdaPresent/assertNode24Present/assertGhAuth/assertClaudeAuth/assertCleanSyncClone/assertSkillDiscoverable returns {ok:false, detail} — never throws — when its DI-injected fake execFileSync/fs reports a missing or broken tool"
    - "notifyCarrierFailure always attempts BOTH the gh-issue channel and the macOS-notification channel, even when the gh channel throws or fails first (D-05: channels are independent, never sequential-gated)"
    - "No raw environment variables, tokens, or full command stdout ever reach a notification/issue body — only allowlisted fields survive buildAllowlistedDiagnostic"
  artifacts:
    - path: "scripts/sync/interval-guard.mjs"
      provides: "pure cadence-guard function plus the tiny state file's read/write helpers"
      exports: ["shouldRunSync", "readIntervalState", "writeIntervalState"]
    - path: "scripts/sync/preflight-assertions.mjs"
      provides: "D-03's 6 pre-flight assertions, each DI-testable and never-throwing"
      exports: ["assertAgdaPresent", "assertNode24Present", "assertGhAuth", "assertClaudeAuth", "assertCleanSyncClone", "assertSkillDiscoverable"]
    - path: "scripts/sync/carrier-notify.mjs"
      provides: "D-05's dual-channel carrier-failure notifier with mandatory diagnostic allowlisting"
      exports: ["buildAllowlistedDiagnostic", "tryGhIssueCreate", "tryMacNotify", "notifyCarrierFailure"]
  key_links:
    - from: "scripts/sync/carrier-notify.mjs::notifyCarrierFailure"
      to: "gh issue create"
      via: "execFileSync(\"gh\", [\"issue\", \"create\", ...]) inside tryGhIssueCreate"
      pattern: "gh.*issue.*create"
    - from: "scripts/sync/preflight-assertions.mjs::assertSkillDiscoverable"
      to: "<syncClone>/.claude/skills/upstream-sync/SKILL.md"
      via: "existsSync on the resolved symlink target"
      pattern: "\\.claude.*skills.*upstream-sync"
---

<objective>
Build the three foundational, dependency-free carrier modules the Plan 11-03 controller composes: `interval-guard.mjs` (SYNC-01's ~3-day cadence, D-04's fail-open state), `preflight-assertions.mjs` (D-03's six guaranteed-real-Agda-gate assertions), and `carrier-notify.mjs` (D-05's dual-channel failure notifier with mandatory diagnostic allowlisting). All three are pure/DI-testable utility modules with zero cross-dependencies among themselves — this plan is Wave 1 precisely so the heavier Plan 11-03 controller (Wave 2) can import already-built, already-tested contracts instead of scavenger-hunting for their shape.

Purpose: Close SYNC-01's three highest-severity risks identified by RESEARCH.md's live, empirical verification on this machine — a bare launchd PATH silently degrading the LOCAL gate to CLOUD (Pitfall 3), a missing pre-flight check masking a broken carrier as a false-green (D-03), and a carrier failure going unnoticed because only one notification channel was tried (D-05) — before any of this logic is composed into the real controller.
Output: `scripts/sync/interval-guard.mjs`, `scripts/sync/preflight-assertions.mjs`, `scripts/sync/carrier-notify.mjs`, and their three DI-stubbed Vitest suites, all plain-`node`-loadable (zero `src/*.ts` imports, per the Cross-File Decision Point in 11-PATTERNS.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-auto-sync-productionization/11-CONTEXT.md
@.planning/phases/11-auto-sync-productionization/11-RESEARCH.md
@.planning/phases/11-auto-sync-productionization/11-PATTERNS.md
@.agents/skills/upstream-sync/SKILL.md

<interfaces>
<!-- These are the exact contracts Plan 11-03 will import. Do not change these
     signatures without updating this comment's downstream consumer note. -->

From RESEARCH.md's own Code Examples section (copy near-verbatim, cited per file):
```javascript
export function shouldRunSync(lastClaudeInvocationAt, now = new Date(), minDays = 3) {
  if (!lastClaudeInvocationAt) return true; // no prior record -> fail OPEN toward running
  const last = new Date(lastClaudeInvocationAt);
  if (Number.isNaN(last.getTime())) return true; // corrupt timestamp -> same fail-open rule
  const elapsedMs = now.getTime() - last.getTime();
  return elapsedMs >= minDays * 24 * 60 * 60 * 1000;
}
```

State file shape (`~/.agda-mcp/upstream-sync/state.json`, per RESEARCH.md's Recommended Project Structure):
`{ lastWrapperTouchAt: string, lastClaudeInvocationAt: string | null, lastClaudeInvocationOutcome: "success" | "failure" | "guard-blocked" | "preflight-failed" | null }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: interval-guard.mjs — pure cadence guard + state file read/write</name>
  <files>scripts/sync/interval-guard.mjs, test/unit/tools/sync-interval-guard.test.ts</files>
  <read_first>
    - 11-RESEARCH.md "Code Examples" section (the exact `shouldRunSync` function body, and the "Interval guard (pure, unit-testable core logic)" heading) — copy this function verbatim, do not re-derive it.
    - 11-RESEARCH.md Pitfall 5 ("Treating a missing/corrupt interval-guard state file as 'block forever'") and Pitfall 4 (the kickstart/guard interaction — note that the `UPSTREAM_SYNC_FORCE` bypass is composed by the CALLER in Plan 11-03, not inside this file).
    - 11-PATTERNS.md "No Analog Found" table row for `scripts/sync/interval-guard.mjs` (confirms RESEARCH.md's own code example is the authoritative source, no further analog search needed).
    - scripts/team/install-pinned-env.mjs lines 1-20 (file header convention: MIT-license line, WHY-comment block, "Run with: node ..." line — mirror this exactly; this file is plain-`node`-loadable, no `tsx`).
    - test/unit/tools/team-install-pinned-env.test.ts lines 44-66 (the `makeTempDir`/`afterEach` cleanup array pattern — reuse verbatim for this file's own temp-dir-based state.json tests).
  </read_first>
  <behavior>
    - shouldRunSync(undefined, now, 3) === true (no prior record recorded yet)
    - shouldRunSync(new Date(now - 2 days).toISOString(), now, 3) === false (too soon)
    - shouldRunSync(new Date(now - 4 days).toISOString(), now, 3) === true (elapsed)
    - shouldRunSync("not-a-real-date", now, 3) === true (corrupt timestamp -> fail open, Pitfall 5)
    - readIntervalState(pathToMissingFile) === null (never throws on ENOENT)
    - readIntervalState(pathToFileContainingGarbageJSON) === null (never throws on parse error)
    - writeIntervalState(path, state) followed by readIntervalState(path) round-trips the exact same object (creating parent directories as needed)
  </behavior>
  <action>
    Create scripts/sync/interval-guard.mjs. Export `shouldRunSync(lastClaudeInvocationAt, now = new Date(), minDays = 3)` copied verbatim from 11-RESEARCH.md's Code Examples section (cite the source in a comment). Export `readIntervalState(statePath)`: returns `null` if the file does not exist (`existsSync` guard) or if `JSON.parse` throws — never throws outward itself, mirroring `readFuelCorpora`'s exact fail-to-empty contract in scripts/team/clone-fuel-corpora.mjs. Export `writeIntervalState(statePath, state)`: `mkdirSync(dirname(statePath), { recursive: true })` then `writeFileSync(statePath, JSON.stringify(state, null, 2))` — plain fs calls, no atomic-write import (this file must stay plain-`node`-loadable with zero `src/*.ts`-backed specifiers, per the Cross-File Decision Point in 11-PATTERNS.md; do not import `writeFileAtomic` from `src/session/safe-source-io.js`). `statePath` is always an explicit parameter — this file never reads `process.env` or computes its own default path; Plan 11-03's controller owns path resolution (including the `UPSTREAM_SYNC_STATE_ROOT` override). File header follows the Shared Patterns convention from 11-PATTERNS.md (MIT-license line, one-paragraph WHY comment citing SYNC-01/D-04, `// Run with: node scripts/sync/interval-guard.mjs` — though this file has no CLI entry point of its own, so state that explicitly instead: "imported only, no CLI entry point"). Add `isMainModule`-gated CLI entry only if you add one; otherwise omit the bottom `if (isMainModule(...))` block entirely since this module is import-only.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-interval-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-interval-guard.test.ts` exits 0
    - `node --check scripts/sync/interval-guard.mjs` exits 0
    - `grep -c "export function shouldRunSync" scripts/sync/interval-guard.mjs` equals 1
    - `grep -c "export function readIntervalState" scripts/sync/interval-guard.mjs` equals 1
    - `grep -c "export function writeIntervalState" scripts/sync/interval-guard.mjs` equals 1
    - `grep -c "from \"../../src/" scripts/sync/interval-guard.mjs` (and the `../src/` variant) both equal 0 — confirms zero `.ts`-backed imports
  </acceptance_criteria>
  <done>All three functions exported with the exact documented signatures; all 7 behavior cases pass; the file has zero `src/*.ts` imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: preflight-assertions.mjs — D-03's six guaranteed-real-Agda-gate assertions</name>
  <files>scripts/sync/preflight-assertions.mjs, test/unit/tools/sync-preflight-assertions.test.ts</files>
  <read_first>
    - 11-RESEARCH.md "Code Examples" section, "Pre-flight assertions (DI-friendly, matches this codebase's `deps` convention)" — the exact `assertClaudeAuth`/`assertGhAuth` bodies to mirror, and RESEARCH.md's Architectural Responsibility Map row "Environment/PATH/tool pre-flight assertions".
    - 11-PATTERNS.md "scripts/sync/preflight-assertions.mjs" section in full — both analogs' keep/change notes, especially the explicit divergence: "this is the single make-or-break clone... an unclean/wrong-branch/mid-merge state must be a loud, thrown/asserted failure... reuse the git-subprocess mechanics, not the silent-degrade error contract" of `cloneFuelCorpus`.
    - scripts/team/install-pinned-env.mjs lines 45-92 (`locateAgdaBinary`, `getAgdaVersion`, `versionSatisfies`, `checkNodeVersion` — the exact verify-and-instruct, never-throw pattern to mirror for `assertAgdaPresent`/`assertNode24Present`).
    - scripts/queue/mirror-github.mjs lines 56-75 (`isGhAvailable` — the `gh --version` + `gh auth status` double-probe, `timeout: 5000`, `shell: false` pattern to mirror for `assertGhAuth`).
    - scripts/team/clone-fuel-corpora.mjs lines 132-153 (the `GIT_TERMINAL_PROMPT: "0"` gitOpts object and the exists/branch/status git-plumbing style to mirror for `assertCleanSyncClone`'s subprocess calls).
    - This planning session's own verified finding (not documented anywhere else — cite directly): `readlink .claude/skills/upstream-sync` on this machine resolves to the ABSOLUTE path `/Users/eric/projects6/agda-mcp-server/.agents/skills/upstream-sync` (the working checkout), and `.claude/` is gitignored (`.gitignore` line 51). A fresh `git clone` of `origin` therefore never brings this symlink along, and even if it did, an absolute symlink copied byte-for-byte would still point at the WORKING checkout, not the clone. `assertSkillDiscoverable` exists specifically to catch this exact silent-degradation class before any spawn happens.
    - .agents/skills/upstream-sync/SKILL.md Section 5 ("Detect the environment: `command -v agda` succeeds -> LOCAL; otherwise CLOUD") — this is the exact silent-degradation mechanism D-03 exists to prevent; every assertion here fails loudly BEFORE the skill ever gets a chance to make its own (potentially wrong) determination.
  </read_first>
  <behavior>
    - assertAgdaPresent({execFileSync: fakeThatResolvesWhichAndVersion}) returns {ok:true, detail:{path, version}}; with a fake that throws on `which agda`, returns {ok:false, detail} — never throws outward
    - assertNode24Present({execFileSync: fakeReturning "v24.16.0"}) returns {ok:true}; with a fake returning "v22.22.0", returns {ok:false, detail} naming the detected major version
    - assertGhAuth({execFileSync: fakeSucceedingOnBothCalls}) returns {ok:true}; with a fake that throws on `gh auth status` specifically (but not `gh --version`), returns {ok:false}
    - assertClaudeAuth({execFileSync: fakeReturning '{"loggedIn":true,...}'}) returns {ok:true, detail}; with `loggedIn:false` in the parsed JSON, returns {ok:false}; with a throwing fake, returns {ok:false} without throwing
    - assertCleanSyncClone(missingPath, deps) returns {ok:false, reason:"missing"} when `existsSync` is false — no git calls attempted
    - assertCleanSyncClone(clonePath, deps) returns {ok:false, reason:"dirty"} when a fake `git status --porcelain` returns non-empty output; {ok:false, reason:"wrong-branch"} when `rev-parse --abbrev-ref HEAD` returns something other than "main"; {ok:true} when fetch succeeds, branch is "main", porcelain is empty, and no `.git/MERGE_HEAD` exists
    - assertSkillDiscoverable(clonePath) returns {ok:true} when `<clonePath>/.claude/skills/upstream-sync/SKILL.md` exists (via a real temp-dir symlink fixture, not a DI fake — this check is pure fs, no subprocess); {ok:false, reason:"symlink-missing-or-dangling"} when the symlink is absent, and — critically — {ok:false} when the symlink exists but resolves OUTSIDE clonePath (simulating the exact stale-absolute-path failure mode found on this machine)
  </behavior>
  <action>
    Create scripts/sync/preflight-assertions.mjs exporting exactly six functions: `assertAgdaPresent(deps = {})`, `assertNode24Present(deps = {})`, `assertGhAuth(deps = {})`, `assertClaudeAuth(deps = {})`, `assertCleanSyncClone(clonePath, deps = {})`, `assertSkillDiscoverable(clonePath, deps = {})` — matching RESEARCH.md's Recommended Project Structure naming exactly (with `assertSkillDiscoverable` added beyond RESEARCH.md's original 5-item list, justified by this session's own symlink-target finding above; note this addition explicitly in a header comment). Every function takes `deps.execFileSync ?? execFileSync` (and, for the last two, real `existsSync`/`readFileSync` — no DI needed there since fs checks are already fast/deterministic without mocking). `assertAgdaPresent`/`assertNode24Present` mirror `locateAgdaBinary`+`getAgdaVersion`+`versionSatisfies`'s three-step shape but for Node 24 use a major-version-only comparison (`>= 24`), not an exact-match `versionSatisfies`. `assertGhAuth` mirrors `isGhAvailable` exactly (`gh --version` then `gh auth status`, `stdio: ["ignore","pipe","ignore"]`, `timeout: 5000`, `shell: false`). `assertClaudeAuth` mirrors RESEARCH.md's own `assertClaudeAuth` code example (`claude auth status`, parse JSON, check `loggedIn === true`). `assertCleanSyncClone` first checks `existsSync(clonePath)` (missing -> immediate `{ok:false, reason:"missing"}`, no git calls), then runs `git -C <clonePath> fetch origin` with `env: {...process.env, GIT_TERMINAL_PROMPT: "0"}` (cite `clone-fuel-corpora.mjs`'s exact gitOpts shape), then checks `git -C <clonePath> rev-parse --abbrev-ref HEAD` equals `"main"`, `git -C <clonePath> status --porcelain` is empty, and `existsSync(join(clonePath, ".git", "MERGE_HEAD"))` is false — any failure returns a specific `reason` string (`"dirty"`, `"wrong-branch"`, `"mid-merge"`, `"fetch-failed"`), never auto-resets or force-heals anything (RESEARCH.md Pitfall 5: this IS diagnostic evidence, never erase it). `assertSkillDiscoverable(clonePath)` checks `existsSync(join(clonePath, ".claude", "skills", "upstream-sync", "SKILL.md"))` — `existsSync` already follows symlinks and naturally returns `false` for a dangling or wrong-target link, so no explicit `realpathSync` call is needed; still verify via the behavior tests above that a symlink resolving OUTSIDE `clonePath` (the exact real-world failure mode discovered this session) also reports `false`.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-preflight-assertions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-preflight-assertions.test.ts` exits 0
    - `node --check scripts/sync/preflight-assertions.mjs` exits 0
    - `grep -c "export function assert" scripts/sync/preflight-assertions.mjs` equals 6
    - `grep -c "GIT_TERMINAL_PROMPT" scripts/sync/preflight-assertions.mjs` >= 1
    - `grep -c "MERGE_HEAD" scripts/sync/preflight-assertions.mjs` >= 1
  </acceptance_criteria>
  <done>All 6 assertions exported, DI-testable, never throw; assertCleanSyncClone never force-resets; assertSkillDiscoverable specifically catches a symlink resolving outside the clone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: carrier-notify.mjs — D-05's dual-channel failure notifier with mandatory allowlisting</name>
  <files>scripts/sync/carrier-notify.mjs, test/unit/tools/sync-carrier-notify.test.ts</files>
  <read_first>
    - 11-RESEARCH.md "Security Domain" > "Known Threat Patterns for this stack" table (the two Information-Disclosure rows this task directly mitigates) and the "Don't Hand-Roll" table row "Notify the user of a failure".
    - 11-PATTERNS.md "scripts/sync/carrier-notify.mjs" section in full (the `mirrorEntry` keep/change notes: "D-05 wants this to always run on carrier failure... then chain to the second channel (osascript) regardless of whether the gh issue create call itself succeeded or failed — never let a gh failure suppress the local notification").
    - scripts/queue/mirror-github.mjs lines 144-201 (`mirrorEntry` — the `gh issue create` argv shape and the loud-throw-on-unparseable-URL discipline to mirror inside `tryGhIssueCreate`, adapted to never throw OUTWARD from `tryGhIssueCreate` itself — this file's contract is "never throw", stricter than `mirrorEntry`'s own).
    - This plan's own security_threat_model context: diagnostic body must allowlist ONLY tool-presence booleans, exit codes, and short error strings — never raw `env`/full stdout/secret values.
  </read_first>
  <behavior>
    - buildAllowlistedDiagnostic(raw) given a raw object containing a nested fake `env` blob and a 4000-character fake stdout string, returns an object whose own keys are limited to an explicit allowlist (e.g. `step`, `toolChecks` as booleans only, `exitCode`, `shortError` truncated to a small bound) — asserts the returned object has NO `env` key, NO key whose value is longer than the truncation bound, and `JSON.stringify(result)` never contains the raw fake secret-looking substring the test injected
    - tryGhIssueCreate(diagnostic, {execFileSync: fakeSucceeding}) returns {ok:true, issueUrl} parsed from the `.../issues/NNN` stdout shape (mirror mirrorEntry's own regex); with a throwing fake, returns {ok:false, reason} — never throws outward
    - tryMacNotify(diagnostic, {execFileSync: fakeSucceeding}) returns {ok:true}; with a throwing fake, returns {ok:false} — never throws outward
    - notifyCarrierFailure(rawFailureInfo, deps) with a `tryGhIssueCreate`-triggering deps.execFileSync that THROWS for the `gh` call but SUCCEEDS for the `osascript` call: asserts the `osascript` call still happened (spy call count >= 1) — the gh failure must never gate the mac-notify attempt
    - notifyCarrierFailure builds its own diagnostic via buildAllowlistedDiagnostic internally (callers cannot forget to sanitize) — a test asserts the execFileSync spy's captured `--body`/`-e` argument strings never contain a raw secret-looking substring injected into rawFailureInfo
  </behavior>
  <action>
    Create scripts/sync/carrier-notify.mjs exporting `buildAllowlistedDiagnostic(rawFailureInfo)`, `tryGhIssueCreate(diagnostic, deps = {})`, `tryMacNotify(diagnostic, deps = {})`, and `notifyCarrierFailure(rawFailureInfo, deps = {})`. `buildAllowlistedDiagnostic` reads ONLY a small fixed set of fields from `rawFailureInfo` (e.g. `step`, boolean-only `toolChecks`, `exitCode`, and a `shortError` string hard-truncated to a small character bound such as 200) — mirror `buildMirrorPayload`'s own "read ONLY these fields, nothing else" discipline from scripts/queue/mirror-github.mjs, and mirror `renderIssueBody`'s plain-string-concatenation rendering (never `JSON.stringify` the whole raw object). `tryGhIssueCreate` calls `execFileSync("gh", ["issue", "create", "--title", <short title>, "--body", <rendered from the allowlisted diagnostic only>], { encoding: "utf8", timeout: 15000, shell: false })`, parses the `.../issues/(\d+)` URL (mirror `mirrorEntry`'s exact regex), and wraps ANY thrown error (including an unparseable-URL case) into `{ ok: false, reason }` — unlike `mirrorEntry`, this function itself must never throw, since `notifyCarrierFailure` needs to proceed to the second channel unconditionally. `tryMacNotify` calls `execFileSync("osascript", ["-e", <short display-notification script built only from the allowlisted diagnostic>], { timeout: 5000, shell: false })`, returning `{ok:true}`/`{ok:false, reason}` — never throws. `notifyCarrierFailure` computes the diagnostic ONCE via `buildAllowlistedDiagnostic(rawFailureInfo)`, then calls `tryGhIssueCreate` followed unconditionally by `tryMacNotify` (wrap the first call's own outcome so a throw or `{ok:false}` never skips the second — e.g. call both inside independent try/catch-or-already-safe blocks, do not `return` early after the first). File header follows the Shared Patterns convention (MIT-license, WHY comment citing D-05 + the two Information-Disclosure threats, "imported only, no CLI entry point" — same as Task 1).
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-carrier-notify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-carrier-notify.test.ts` exits 0
    - `node --check scripts/sync/carrier-notify.mjs` exits 0
    - `grep -c "export function notifyCarrierFailure" scripts/sync/carrier-notify.mjs` equals 1
    - `grep -c "export function buildAllowlistedDiagnostic" scripts/sync/carrier-notify.mjs` equals 1
    - `grep -c "JSON.stringify(rawFailureInfo" scripts/sync/carrier-notify.mjs` equals 0 (never dumps the whole raw object)
  </acceptance_criteria>
  <done>Both channels are always attempted regardless of the first channel's outcome; every notification/issue body is built exclusively from the allowlisted diagnostic, never from raw failure input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| carrier wrapper -> local CLIs (`gh`, `osascript`, `git`) | This plan's assertions/notifier shell out to already-authenticated, already-installed local tools; no network-facing input crosses here except the diagnostic TEXT this plan constructs itself |
| carrier wrapper -> gh issue tracker (public repo) | `tryGhIssueCreate`'s rendered body becomes a PUBLIC, remote-visible artifact — anything included here is disclosed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering / Elevation of Privilege (via silent degradation) | scripts/sync/preflight-assertions.mjs | mitigate | Six independent, never-throwing assertions (agda/node24/gh/claude/clean-clone/skill-discoverable) each report `{ok:false}` rather than allowing a caller to proceed on an assumed-good state; Plan 11-03 wires these to gate the entire spawn |
| T-11-03 | Information Disclosure | scripts/sync/carrier-notify.mjs | mitigate | `buildAllowlistedDiagnostic` is the single mandatory chokepoint every notify path routes through — reads a fixed, small field allowlist only; `notifyCarrierFailure` never accepts a pre-built diagnostic from a caller, always rebuilds it itself, so no caller can bypass sanitization |
| T-11-05 | Information Disclosure (low severity) | scripts/sync/carrier-notify.mjs (osascript channel) | accept | Notification banner content is visible on an always-on, physically-accessible single-user Mac; D-05 already accepts this, no new exposure introduced by this plan |
| T-11-SC | Supply chain (package installs) | N/A | accept | Not applicable — this phase installs zero new npm/pip/cargo packages (11-RESEARCH.md's own Package Legitimacy Audit: "Not applicable") |
</threat_model>

<verification>
Run `npx vitest run test/unit/tools/sync-interval-guard.test.ts test/unit/tools/sync-preflight-assertions.test.ts test/unit/tools/sync-carrier-notify.test.ts` — all three files exit 0 with zero real subprocess/LLM cost (every subprocess call is DI-stubbed). Confirm zero `src/*.ts` imports across all three new `.mjs` files via `grep -rL "from \"\.\./\.\./src/" scripts/sync/*.mjs` (expect all three files listed, meaning none match).
</verification>

<success_criteria>
- All three modules exist, exporting exactly the function names listed in this plan's `must_haves.artifacts`.
- All three Vitest suites pass with zero real subprocess/LLM invocations.
- `assertSkillDiscoverable` and `buildAllowlistedDiagnostic` — the two functions this plan adds beyond RESEARCH.md's literal enumeration — are present and independently tested.
</success_criteria>

<output>
Create `.planning/phases/11-auto-sync-productionization/11-01-SUMMARY.md` when done
</output>
