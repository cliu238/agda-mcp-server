---
phase: 11-auto-sync-productionization
plan: 04
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - scripts/sync/install-launchd-carrier.mjs
  - test/unit/tools/sync-install-launchd-carrier.test.ts
  - scripts/sync/uninstall-launchd-carrier.mjs
  - test/unit/tools/sync-uninstall-launchd-carrier.test.ts
  - docs/UPSTREAM-SYNC-CARRIER.md
  - test/unit/tools/sync-docs-carrier-runbook.test.ts
autonomous: true
requirements: [SYNC-01, SYNC-02]

must_haves:
  truths:
    - "install-launchd-carrier.mjs resolves the PINNED Node 24 (not the ambient mise-global default) plus agda/gh/claude/gsd-sdk, and bakes the resulting PATH into the plist's own EnvironmentVariables dict — never a hardcoded this-machine path in tracked source"
    - "The rendered plist passes plutil -lint; the installer aborts loudly (exit 1, no bootstrap attempt) on a lint failure"
    - "The dedicated sync clone the installer creates has BOTH an origin remote and an upstream remote (with upstream's push URL disabled), reproducing this repo's own real two-remote layout exactly"
    - "The dedicated sync clone's own .claude/skills/upstream-sync symlink resolves to THAT SAME clone's .agents/skills/upstream-sync — never the working checkout's absolute path"
    - "install-launchd-carrier.mjs is safely re-runnable: it best-effort bootouts any existing job before bootstrapping fresh, and skips re-cloning an already-present sync clone"
    - "docs/UPSTREAM-SYNC-CARRIER.md documents install/uninstall/kickstart/log-locations/the UPSTREAM_SYNC_FORCE bypass/the unclean-clone recovery recipe without duplicating SKILL.md's own policy content"
  artifacts:
    - path: "scripts/sync/install-launchd-carrier.mjs"
      provides: "the one-time (idempotent) installer: tool-path resolution, plist render+lint+write, dedicated clone creation, launchctl bootstrap"
      exports: ["resolveNode24Path", "renderPlist", "scriptMain"]
    - path: "scripts/sync/uninstall-launchd-carrier.mjs"
      provides: "the inverse operation: bootout + plist removal only, never touching accumulated clone/state/logs"
      exports: ["scriptMain"]
    - path: "docs/UPSTREAM-SYNC-CARRIER.md"
      provides: "the maintainer runbook, sibling to docs/DEPLOY-OPERATIONS.md"
  key_links:
    - from: "scripts/sync/install-launchd-carrier.mjs"
      to: "~/Library/LaunchAgents/com.cliu238.agda-mcp-server.upstream-sync.plist"
      via: "writeFileSync + plutil -lint + launchctl bootstrap"
      pattern: "launchctl.*bootstrap"
    - from: "plist ProgramArguments[1]"
      to: "scripts/sync/run-upstream-sync.mjs"
      via: "absolute repo path, resolved at install time"
      pattern: "run-upstream-sync\\.mjs"
    - from: "<clone>/.claude/skills/upstream-sync"
      to: "<clone>/.agents/skills/upstream-sync"
      via: "symlinkSync, created fresh inside the clone (gitignored, not carried by git clone)"
      pattern: "symlinkSync"
---

<objective>
Build the installer (`install-launchd-carrier.mjs`) and its inverse (`uninstall-launchd-carrier.mjs`), plus the maintainer runbook (`docs/UPSTREAM-SYNC-CARRIER.md`). The installer is where every "resolve once, use forever" decision from RESEARCH.md's Pattern 3 becomes real: it resolves the pinned Node 24 (not the ambient default), renders and lints the plist, bakes the resolved PATH into the plist's `EnvironmentVariables` (closing Pitfall 3's PATH-bareness risk once and for all), and idempotently creates the D-02 dedicated sync clone — including two details this planning session discovered are load-bearing but undocumented elsewhere: reproducing this repo's own real `origin`/`upstream` two-remote layout, and recreating the `.claude/skills/upstream-sync` symlink INSIDE the fresh clone (since it is gitignored and, even if copied, would still point at the wrong checkout).

Purpose: Everything Plan 11-01/11-03 built is inert until it is actually installed on this machine as a real, persistent launchd LaunchAgent. This plan is where the carrier becomes a real, re-runnable, documented piece of infrastructure rather than a set of loose scripts.
Output: `scripts/sync/install-launchd-carrier.mjs`, `scripts/sync/uninstall-launchd-carrier.mjs`, their Vitest suites, and `docs/UPSTREAM-SYNC-CARRIER.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-auto-sync-productionization/11-CONTEXT.md
@.planning/phases/11-auto-sync-productionization/11-RESEARCH.md
@.planning/phases/11-auto-sync-productionization/11-PATTERNS.md
@docs/DEPLOY-OPERATIONS.md

<interfaces>
<!-- Facts verified directly on this machine during phase planning — use these
     literal values, do not re-derive or re-probe them. -->

Real git remotes on this exact repo (`git remote -v`), which the dedicated clone must reproduce:
```
origin    https://github.com/cliu238/agda-mcp-server.git (fetch)
origin    https://github.com/cliu238/agda-mcp-server.git (push)
upstream  https://github.com/InvariantHoldings/agda-mcp-server.git (fetch)
upstream  DISABLED-push-to-cliu238-origin-instead (push)
```

Skill-discovery symlink (gitignored, `.gitignore` line 51 `.claude/	.claude/skills/upstream-sync`):
`.claude/skills/upstream-sync` is a symlink whose `readlink` target is the ABSOLUTE path
`/Users/eric/projects6/agda-mcp-server/.agents/skills/upstream-sync` (the working checkout).
A fresh `git clone` of `origin` never carries this file (gitignored); even if it did, an
absolute symlink copied verbatim would still resolve to the WRONG checkout. The installer
must create a NEW symlink inside the fresh clone pointing at THAT clone's own
`.agents/skills/upstream-sync` directory.

RESEARCH.md's Pattern 1 plist skeleton (Label/ProgramArguments/StartCalendarInterval/StandardOutPath/StandardErrorPath — this plan ADDS an `EnvironmentVariables` dict, not shown in RESEARCH.md's own skeleton, to close Pitfall 3):
```xml
<key>Label</key><string>com.cliu238.agda-mcp-server.upstream-sync</string>
<key>ProgramArguments</key>
<array>
  <string>ABSOLUTE_RESOLVED_NODE24_PATH</string>
  <string>ABSOLUTE_PATH_TO_run-upstream-sync.mjs</string>
</array>
<key>StartCalendarInterval</key>
<dict><key>Hour</key><integer>3</integer><key>Minute</key><integer>0</integer></dict>
<key>StandardOutPath</key><string>~/.agda-mcp/upstream-sync/launchd-stdout.log</string>
<key>StandardErrorPath</key><string>~/.agda-mcp/upstream-sync/launchd-stderr.log</string>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: install-launchd-carrier.mjs — resolve, render, lint, clone, bootstrap</name>
  <files>scripts/sync/install-launchd-carrier.mjs, test/unit/tools/sync-install-launchd-carrier.test.ts</files>
  <read_first>
    - 11-RESEARCH.md Pattern 1 (plist skeleton) and Pattern 3 (install-time tool-path resolution philosophy: "resolve once, use resolved absolute paths... never launchd-time PATH guessing") and Pitfall 6 (must WRITE into `~/Library/LaunchAgents/` AND bootstrap — a scratch-path-only bootstrap vanishes after reboot) and Pitfall 8 (`ThrottleInterval` 10s default — a dev-iteration caution to mention in docs, not code).
    - 11-PATTERNS.md's "scripts/sync/install-launchd-carrier.mjs" section in full — `generateRunPinnedAgdaScript`/`writeRunPinnedAgdaScript`'s safe single-quote splice pattern, `scriptMain`'s loud-partial-install order, the `lintPlist` code example.
    - scripts/team/install-pinned-env.mjs lines 106-127 (`generateRunPinnedAgdaScript`'s single-quote splice for embedding a resolved, teammate-supplied path safely into generated text — the SAME embedding concern applies to plist XML string values) and lines 172-249 (`scriptMain`'s verify-THEN-instruct-THEN-exit-1 ordering, never printing a "done." line until every step succeeds).
    - scripts/team/clone-fuel-corpora.mjs lines 132-243 (the `GIT_TERMINAL_PROMPT: "0"` gitOpts, and lines 211-224's plain `git clone <url> destDir` branch specifically — reuse ONLY this public-clone shape, never the token-embedding private-repo branches, since this repo's own `origin` is public).
    - This plan's own `<interfaces>` block above: the real, verified `git remote -v` output to reproduce, and the `.claude/skills/upstream-sync` symlink-target finding this task must close.
    - scripts/sync/preflight-assertions.mjs's `assertSkillDiscoverable` and `assertCleanSyncClone` (Plan 11-01 output) — the exact runtime checks this installer's own clone-creation step must satisfy.
    - scripts/sync/run-upstream-sync.mjs (Plan 11-03 output) — its real, existing absolute file path is what `ProgramArguments[1]` must reference.
  </read_first>
  <behavior>
    - resolveNode24Path({execFileSync: fakeReturning a mise-where-style directory for ["where","node@24"]}) returns "<dir>/bin/node"; with a fake where `mise` itself throws, falls back to a `which node` call and emits a stderr warning (assert via a captured-stderr helper) rather than throwing
    - renderPlist({label, node24Path, scriptPath, resolvedPathString, ...}) returns a string containing the correct `<key>Label</key><string>com.cliu238.agda-mcp-server.upstream-sync</string>`, both `ProgramArguments` entries, `<key>Hour</key><integer>3</integer>`, an `EnvironmentVariables` dict containing the resolved PATH string, and the two `StandardOutPath`/`StandardErrorPath` entries — pure string-building, no I/O
    - scriptMain (fully DI-stubbed: fake execFileSync for every `which`/`mise`/`git`/`launchctl`/`plutil` call, `deps.homeDir`/`deps.repoRoot` overrides pointed at a temp dir): asserts `plutil -lint` runs against the exact path just written; asserts `launchctl bootout` is attempted BEFORE `launchctl bootstrap` (both present in the mock's call order); asserts the dedicated-clone git command sequence runs in order (`clone`, `remote add upstream ...`, `remote set-url --push upstream DISABLED-push-to-cliu238-origin-instead`); asserts a `symlinkSync`-equivalent step targets `<clone>/.claude/skills/upstream-sync` -> `<clone>/.agents/skills/upstream-sync`; asserts that when the injected `plutil` fake reports a lint failure, NO `launchctl bootstrap` call happens and `process.exitCode` is set to 1
    - scriptMain re-run against a fixture where the clone directory ALREADY exists: asserts NO second `git clone` call happens (idempotent skip), while the plist re-render/re-lint/re-bootstrap steps still run
  </behavior>
  <action>
    Create scripts/sync/install-launchd-carrier.mjs. Export `resolveNode24Path(deps = {})`: run `execFileSync("mise", ["where", "node@24"], {...})`, trim, append `/bin/node`; on any throw (mise absent), fall back to `execFileSync("which", ["node"], {...})` with a `process.stderr.write` warning that the resolved node may not be the pinned 24.x (cite: a bare `which node` alone would resolve the ambient mise-global default — v22.22.0 on this machine per RESEARCH.md's own Environment Availability table — never the project-pinned 24, which is exactly why `mise where node@24` is tried first). Export `renderPlist({label, node24Path, scriptPath, resolvedPathString, stdoutLogPath, stderrLogPath})`: a pure string-template function producing the full plist XML per RESEARCH.md's Pattern 1 skeleton PLUS a new `<key>EnvironmentVariables</key><dict><key>PATH</key><string>{resolvedPathString}</string></dict>` block (this is the concrete mechanism closing Pitfall 3 — RESEARCH.md itself left open whether PATH resolution bakes into the plist or a separate runtime-read file; this plan resolves it as the plist's own native `EnvironmentVariables` key, since launchd applies it identically for a calendar fire AND a `kickstart`, needing zero extra runtime file I/O in run-upstream-sync.mjs). Export `scriptMain(deps = {})` implementing, in order: (1) resolve absolute paths for `agda`, `gh`, `claude`, `gsd-sdk` via `which`-style `execFileSync` calls (mirror `locateAgdaBinary`) and Node 24 via `resolveNode24Path`; build `resolvedPathString` = the deduplicated resolved tool directories joined with `:`, followed by `/usr/bin:/bin:/usr/sbin:/sbin`; (2) render the plist via `renderPlist` with `label = "com.cliu238.agda-mcp-server.upstream-sync"`, write it to `~/Library/LaunchAgents/<label>.plist` (Pitfall 6 — the real directory, never a scratch path); (3) run `plutil -lint <path>` — on failure, `process.stderr.write` the specific lint error and `process.exitCode = 1`, return WITHOUT attempting any bootstrap; (4) if the dedicated clone directory does not yet exist (`existsSync` guard, idempotent skip otherwise): `git clone <resolved-origin-https-url> <cloneDir>` (mirror `cloneFuelCorpus`'s plain public-clone branch, `GIT_TERMINAL_PROMPT:"0"`), then `git -C <cloneDir> remote add upstream https://github.com/InvariantHoldings/agda-mcp-server.git`, then `git -C <cloneDir> remote set-url --push upstream DISABLED-push-to-cliu238-origin-instead` (reproduce the verified real remote layout exactly — cite this plan's own `<interfaces>` block); (5) idempotently ensure `<cloneDir>/.claude/skills/upstream-sync` exists as a symlink to `<cloneDir>/.agents/skills/upstream-sync` — `mkdirSync(join(cloneDir, ".claude", "skills"), {recursive:true})` then `symlinkSync` (skip if already correctly linked; this step exists specifically because `.claude/` is gitignored and a copied absolute symlink would point at the wrong checkout — cite this session's own finding); (6) best-effort `execFileSync("launchctl", ["bootout", \`gui/${uid}/${label}\`])` (swallow any failure — "not currently loaded" is the expected first-install outcome), THEN `execFileSync("launchctl", ["bootstrap", \`gui/${uid}\`, plistPath])` — this bootout-then-bootstrap order is what makes re-running the installer safe (Pitfall 6); (7) print a final one-line "done." summary ONLY after every step above succeeded (CR-01 loud-partial-install discipline, mirror `install-pinned-env.mjs`'s own gate exactly) naming the plist path and label; any failed step aborts with a specific stderr line and `process.exitCode = 1`, never a false "done." Add `isMainModule` CLI gate.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-install-launchd-carrier.test.ts -t "plist lint"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-install-launchd-carrier.test.ts -t "plist lint"` exits 0 (matches 11-VALIDATION.md's exact required command)
    - `npx vitest run test/unit/tools/sync-install-launchd-carrier.test.ts` (whole file) exits 0
    - `grep -c "EnvironmentVariables" scripts/sync/install-launchd-carrier.mjs` >= 1
    - `grep -c "mise" scripts/sync/install-launchd-carrier.mjs` >= 1
    - `grep -c "DISABLED-push-to-cliu238-origin-instead" scripts/sync/install-launchd-carrier.mjs` equals 1
    - `grep -c "symlinkSync" scripts/sync/install-launchd-carrier.mjs` >= 1
    - `grep -c "bootout" scripts/sync/install-launchd-carrier.mjs` >= 1 (present and ordered before `bootstrap`)
    - `node --check scripts/sync/install-launchd-carrier.mjs` exits 0
  </acceptance_criteria>
  <done>install-launchd-carrier.mjs resolves the pinned Node 24 (not the ambient default), renders and lints a plist with a resolved PATH baked into EnvironmentVariables, idempotently creates the two-remote dedicated clone with its in-clone skill-discovery symlink, and is safely re-runnable.</done>
</task>

<task type="auto">
  <name>Task 2: uninstall-launchd-carrier.mjs — reverse the schedule, never the accumulated state</name>
  <files>scripts/sync/uninstall-launchd-carrier.mjs, test/unit/tools/sync-uninstall-launchd-carrier.test.ts</files>
  <read_first>
    - 11-PATTERNS.md's "scripts/sync/uninstall-launchd-carrier.mjs" section ("No strong analog — smallest of the six new files... `execFileSync('launchctl', ['bootout', ...])` then delete the plist file").
    - scripts/sync/install-launchd-carrier.mjs (this plan's own Task 1 output — the exact label (`com.cliu238.agda-mcp-server.upstream-sync`) and plist-path convention (`~/Library/LaunchAgents/<label>.plist`) to invert).
    - 11-CONTEXT.md's D-02/Pitfall-5 recovery philosophy: deleting the dedicated clone (or state/logs) is an explicit, human-triggered decision, never bundled into an automatic "uninstall."
  </read_first>
  <action>
    Create scripts/sync/uninstall-launchd-carrier.mjs exporting `scriptMain(deps = {})`: best-effort `execFileSync("launchctl", ["bootout", \`gui/${uid}/com.cliu238.agda-mcp-server.upstream-sync\`], {...})` (swallow a "not loaded" failure — this must succeed even when the job was never installed), then `existsSync`-guard-delete `~/Library/LaunchAgents/com.cliu238.agda-mcp-server.upstream-sync.plist` if present (never throw if already gone). Deliberately do NOT touch the dedicated sync clone, `state.json`, or `carrier.log` — uninstalling the SCHEDULE is a different decision from discarding accumulated state, which stays for the operator to remove by hand (matches the already-established "explicit human-triggered" recovery philosophy for the clone). Print a one-line confirmation naming the label. Add `isMainModule` CLI gate.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-uninstall-launchd-carrier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-uninstall-launchd-carrier.test.ts` exits 0
    - Test asserts `execFileSync` was called with `["bootout", "gui/<uid>/com.cliu238.agda-mcp-server.upstream-sync"]` via an injected fake
    - Test asserts the plist file is removed from a temp `LaunchAgents`-stand-in directory (via a `deps.launchAgentsDir` override)
    - Test asserts NO call in the mock's captured arguments references `clone`, `state.json`, or `carrier.log`
    - `node --check scripts/sync/uninstall-launchd-carrier.mjs` exits 0
  </acceptance_criteria>
  <done>uninstall-launchd-carrier.mjs cleanly reverses only install's bootstrap+plist-write steps; accumulated clone/state/logs survive untouched.</done>
</task>

<task type="auto">
  <name>Task 3: docs/UPSTREAM-SYNC-CARRIER.md — the maintainer runbook</name>
  <files>docs/UPSTREAM-SYNC-CARRIER.md, test/unit/tools/sync-docs-carrier-runbook.test.ts</files>
  <read_first>
    - docs/DEPLOY-OPERATIONS.md in full (already read this session — the exact house style: opening framing linking to the canonical skill doc without duplicating it; "recipe, then a fenced command block, then — where a live run already happened — a literal captured-output block"; explicit "Cleanup (always):" sections).
    - 11-PATTERNS.md's "docs/UPSTREAM-SYNC-CARRIER.md" section (the full content checklist: install/uninstall/kickstart/log-locations/UPSTREAM_SYNC_FORCE bypass/recovery-path-for-an-unclean-clone/one-time notification-banner check).
    - scripts/sync/install-launchd-carrier.mjs, scripts/sync/uninstall-launchd-carrier.mjs, scripts/sync/run-upstream-sync.mjs (this plan's own Task 1-2 output plus Plan 11-03's — the exact commands, flags, env vars, and file paths to document accurately, never re-derive or invent a different convention here).
    - 11-RESEARCH.md Pitfall 4 (`UPSTREAM_SYNC_FORCE` bypass — needed for out-of-band verification without the interval-guard blocking it), Pitfall 5 (unclean-clone recovery — delete-then-reinstall, NEVER auto-force-reset), Pitfall 8 (`ThrottleInterval` 10s default — a dev-iteration caution worth one sentence), and Open Question 1 (the notification banner's on-screen visibility is not verifiable by command exit code alone — a one-time manual check).
  </read_first>
  <action>
    Create docs/UPSTREAM-SYNC-CARRIER.md following DEPLOY-OPERATIONS.md's exact house style: an opening paragraph linking to `.agents/skills/upstream-sync/SKILL.md` as the canonical policy document ("read that file before touching the carrier by hand; do not duplicate its content here — this doc only lists the repeatable command recipes that build on top of it"). Cover, each as its own recipe-then-command-block subsection: install (`node scripts/sync/install-launchd-carrier.mjs`); uninstall (`node scripts/sync/uninstall-launchd-carrier.mjs`); the D-07 kickstart proof recipe (`launchctl kickstart -k gui/$(id -u)/com.cliu238.agda-mcp-server.upstream-sync`); log/heartbeat/state file locations (`~/.agda-mcp/upstream-sync/{state.json,carrier.log,launchd-stdout.log,launchd-stderr.log}`); the `UPSTREAM_SYNC_FORCE=1` bypass for out-of-band verification without waiting out the interval guard; the unclean-sync-clone recovery recipe (delete `~/.agda-mcp/upstream-sync/clone`, re-run the installer — it idempotently recreates ONLY the missing clone — never manually force-reset a dirty clone in place, since that erases the exact diagnostic evidence of whatever went wrong); a one-time notification-banner visual-confirmation step (trigger a forced carrier failure, confirm the macOS banner actually renders; if not, check System Settings -> Notifications for the relevant process); and a short `ThrottleInterval` dev-iteration note (wait >=10s between manual `kickstart` attempts while iterating). Close with a "Cleanup (always):" section for any throwaway manual `launchctl bootstrap`/`bootout` experiments, mirroring DEPLOY-OPERATIONS.md's own convention.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/sync-docs-carrier-runbook.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/unit/tools/sync-docs-carrier-runbook.test.ts` exits 0 — a content-assertion test reading `docs/UPSTREAM-SYNC-CARRIER.md` and confirming it contains: `launchctl kickstart`, `UPSTREAM_SYNC_FORCE`, `install-launchd-carrier.mjs`, `uninstall-launchd-carrier.mjs`, `carrier.log`, and a link to `upstream-sync/SKILL.md`
    - `grep -c "upstream-sync/SKILL.md" docs/UPSTREAM-SYNC-CARRIER.md` >= 1 (links to, never duplicates, the canonical policy doc)
    - File exists under `docs/`, alongside `DEPLOY-OPERATIONS.md`
  </acceptance_criteria>
  <done>docs/UPSTREAM-SYNC-CARRIER.md gives a maintainer everything needed to install/uninstall/verify/recover the carrier without reading source, and is itself regression-locked by a content test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install-launchd-carrier.mjs -> `~/Library/LaunchAgents/` | Writes a real, persistent, user-level launchd job — the plist itself and the resolved PATH it carries are local artifacts outside git |
| install-launchd-carrier.mjs -> dedicated sync clone | Clones this repo's OWN public `origin` — no credential embedding needed or attempted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering (credential persistence) | scripts/sync/install-launchd-carrier.mjs's clone step | mitigate | Clones via the plain public HTTPS `origin` URL only (mirrors `cloneFuelCorpus`'s public-entry branch) — never the token-embedding branch that exists in `clone-fuel-corpora.mjs` for PRIVATE corpora, which does not apply here since this repo's own origin is public |
| T-11-01 | Tampering / Elevation of Privilege (via silent degradation) | install-launchd-carrier.mjs's tool-path resolution | mitigate | Resolution failures (missing `mise`, missing tool) degrade to a documented, loud stderr warning and a non-2xx exit — never a silently-wrong PATH baked into the shipped plist |
| T-11-09 | Information Disclosure (local docs) | docs/UPSTREAM-SYNC-CARRIER.md | accept | Documents real local paths under the operator's own home directory (`~/Library/LaunchAgents/...`, `~/.agda-mcp/...`) — consistent with this repo's own existing `docs/DEPLOY-OPERATIONS.md` convention (which already documents comparable local paths); no new class of exposure |
| T-11-SC | Supply chain (package installs) | N/A | accept | Not applicable — zero new npm/pip/cargo packages |
</threat_model>

<verification>
`npx vitest run test/unit/tools/sync-install-launchd-carrier.test.ts test/unit/tools/sync-uninstall-launchd-carrier.test.ts test/unit/tools/sync-docs-carrier-runbook.test.ts` — all three exit 0. `grep` confirms the plist template includes `EnvironmentVariables`, the clone step reproduces the exact `DISABLED-push-to-cliu238-origin-instead` sentinel, and the in-clone `.claude/skills/upstream-sync` symlink step is present.
</verification>

<success_criteria>
- The carrier is fully installable/uninstallable via two small, idempotent, DI-tested scripts.
- The dedicated clone the installer creates is immediately usable by `assertCleanSyncClone`/`assertSkillDiscoverable` (Plan 11-01) without any manual follow-up step.
- A maintainer runbook exists, documenting exactly what was built without duplicating SKILL.md's policy content.
</success_criteria>

<output>
Create `.planning/phases/11-auto-sync-productionization/11-04-SUMMARY.md` when done
</output>
