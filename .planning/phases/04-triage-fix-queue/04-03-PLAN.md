---
phase: 04-triage-fix-queue
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - test/fixtures/fix-queue.json
  - scripts/queue/seed-initial-cargo.mjs
  - test/unit/fixtures/fix-queue.test.ts
autonomous: true
requirements: [QUEUE-01]

must_haves:
  truths:
    - "The queue is seeded with the flagship #64/#61 defect (linked to its capture-regression-matrix entry, status locked) plus the 4 CHG-REVERIFY-confirmed entries (3 alive + 1 minor) and the 8 CHG needs-reverify UX-report specs — 13 entries total, none evaporated (QUEUE-01 success criterion 1)"
    - "The CHG cargo enters the queue immediately, flagged needsReverify: true for the 8 unverified specs; re-verification is a future triage action performed FROM INSIDE the queue, not a precondition for entry (D-05)"
    - "Overlapping root causes (the agda_auto flag leak <-> RT4, the flagship staleness family <-> RT8, the agda_proof_status mislabeling <-> RT6's sub-case) are cross-referenced via relatedFingerprint rather than double-entered as independent defects, so the dashboard's backlog/close-rate metrics never double-count one underlying bug (RESEARCH.md Open Question 1)"
    - "Every seeded fingerprint is computed via fingerprintBugReport() reused verbatim, and the 3 grounded entries with raw Agda error text (flagship, agda_auto, agda_give) use that RAW TEXT as the identity's observed field (not a prose paraphrase) and expected: \"\"/reproduction: [] — the SAME shape Plan 04-02's improved register-capture-session.ts fingerprint call would produce for a live re-capture of the identical error under the same server version, maximizing (within the pre-existing serverVersion-scoped limits of fingerprintBugReport itself) the odds a future live re-capture bumps recurrence instead of appearing as a disconnected new entry (RESEARCH.md Open Question 4, Pitfall 3)"
    - "Re-running scripts/queue/seed-initial-cargo.mjs is idempotent: it bumps recurrence on already-seeded fingerprints via upsertQueueEntry rather than duplicating entries (D-06), proven against the REAL queue file, not just synthetic fixtures"
    - "closedAt/timestamps live as plain fields on each entry (createdAt/closedAt), never a separate event log (D-02 continuation; RESEARCH.md Open Question 3)"
    - "The flagship entry's matrixEntryId links it to capture-regression-matrix.json's issue-64-61-transitive-staleness row, and its status (locked) matches that row's status exactly — the queue's locked corresponds to the regression matrix's locked (D-08); this linkage is manual/data-level in v1, per D-08's manual-first status-transition philosophy"
  artifacts:
    - path: "test/fixtures/fix-queue.json"
      provides: "the real 13-entry seed cargo (1 locked flagship, 4 triaged CHG-confirmed, 8 new/needsReverify CHG UX-report specs)"
    - path: "scripts/queue/seed-initial-cargo.mjs"
      provides: "the idempotent, re-runnable seed script (calls upsertQueueEntry 13 times with fixed field values)"
  key_links:
    - from: "scripts/queue/seed-initial-cargo.mjs"
      to: "scripts/queue/intake.mjs"
      via: "upsertQueueEntry(...) call per seed entry"
      pattern: "upsertQueueEntry"
    - from: "test/fixtures/fix-queue.json"
      to: "test/fixtures/capture-regression-matrix.json"
      via: "matrixEntryId: \"issue-64-61-transitive-staleness\" on the flagship entry"
      pattern: "issue-64-61-transitive-staleness"
---

<objective>
## Phase Goal

**As a** maintainer (and the AI agents dogfooding this server), **I want to** have every captured defect persist, flow, and get prioritized/classified inside a durable in-repo queue, **so that** intake neither evaporates at session end nor outpaces fix throughput into a write-only graveyard.

*(Same phase-level user story as 04-01 — see that plan's objective for the full MVP_MODE adaptation note. This plan is the slice that makes the queue's SSOT claim REAL: after it lands, the queue is not an empty shell, it holds every already-known Loop-② defect this milestone has surfaced so far.)*

Populate the queue (Plan 04-01's engine) with real cargo: the already-`locked` flagship #64/#61 defect, the 4 defects `.planning/research/CHG-REVERIFY.md` re-verified today (3 alive, 1 minor), and the 8 turn-key UX-report specs from the private `Codex-Homotopy-Group` corpus (measured on v0.6.7, not yet re-verified against current `main`) — resolving all 4 of RESEARCH.md's Open Questions concretely rather than leaving them as future work.

Purpose: an empty queue proves nothing about QUEUE-01's actual claim ("captured defects persist... rather than evaporating"); this plan is the concrete, checkable proof.
Output: 13 real entries in `test/fixtures/fix-queue.json`, an idempotent seed script, and tests proving internal consistency (uniqueness, referential integrity of cross-links, and that the seeded classifier fields match a live re-run of `classifyAgdaError`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-fix-queue/04-CONTEXT.md
@.planning/phases/04-triage-fix-queue/04-PATTERNS.md
@.planning/research/CHG-REVERIFY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed the real cargo via an idempotent seed script</name>
  <files>test/fixtures/fix-queue.json, scripts/queue/seed-initial-cargo.mjs</files>
  <read_first>
    - scripts/queue/intake.mjs (Plan 04-01) — `upsertQueueEntry(entryData, queueJsonPath)` is the exact function this script calls 13 times; it validates and bump-or-appends, so this script itself does not need to duplicate any validation logic.
    - test/fixtures/capture-regression-matrix.json — confirms the flagship's exact `id: "issue-64-61-transitive-staleness"`, `issue: [64, 61]`, `status: "locked"`.
    - .planning/research/CHG-REVERIFY.md — Defects 1-4 (flagship / agda_auto / agda_give / agda_search_definitions) and the related agda_proof_status sub-finding under Defect 1 — the source of the exact raw error text quoted below.
    - src/repo-root.ts — `SERVER_REPO_ROOT` to resolve the real queue file path.
    - scripts/test-with-sentinel.mjs — `isMainModule` for the CLI-entry guard.
  </read_first>
  <action>
    Create `scripts/queue/seed-initial-cargo.mjs` exporting a `SEED_ENTRIES` array of exactly the 13 plain objects specified below (every field value is fixed and pre-computed — transcribe them verbatim, do not re-derive or re-word them) and a `scriptMain(queueJsonPath)` that, for each entry in order, calls `upsertQueueEntry(entry, queueJsonPath)` (imported from `./intake.mjs`), defaulting `queueJsonPath` to `join(SERVER_REPO_ROOT, "test/fixtures/fix-queue.json")` when not supplied. Import the `isMainModule` CLI-entry guard from `../test-with-sentinel.mjs`. After writing the script, run it for real EXACTLY ONCE via `npx tsx scripts/queue/seed-initial-cargo.mjs` so `test/fixtures/fix-queue.json` is populated with the real 13 entries at `recurrence: 1` each, matching this plan's field specification below verbatim — do NOT run it a second time against the real committed file (a second real run would bump every `recurrence` to 2, diverging from the exact values Task 2's tests assert against). Prove idempotency WITHOUT touching the committed file: copy `test/fixtures/fix-queue.json` to a scratch path (e.g. under the project's own scratch/tmp convention), call `scriptMain(scratchPath)` a second time against that COPY, and confirm every entry's `recurrence` becomes exactly 2 with the array length still 13 (no duplicate rows) — then discard the scratch copy. This scratch-copy check is a one-time manual verification during this task's execution, not a committed test file (Plan 04-01's `queue-intake.test.ts` already covers the generic bump-in-place mechanism against synthetic tmpdir fixtures).

    The 13 entries, one bullet per entry, `field=value` pairs (all string values are literal, transcribe exactly including punctuation and escaped quotes):

    - Entry 1 (flagship): `fingerprint="e6f0c1169032b9d5"`, `status="locked"`, `defectKind="false-green"`, `triageClass="proof-obligation"`, `triageConfidence=0.72`, `recurrence=1`, `title="Transitive dependency staleness reports false ok-complete (flagship #64/#61)"`, `summary="agda_load_no_metas reported ok-complete after a dependency file changed underneath an already-loaded session; fresh agda correctly rejected with UnequalTerms. Fixed by the strict terminus guard (Phase 3.1); capture-regression-matrix entry issue-64-61-transitive-staleness is locked."`, `affectedTool="agda_load_no_metas"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId="issue-64-61-transitive-staleness"`, `issue=[64,61]`, `relatedFingerprint=["3306edf4c2d01c53"]`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt="2026-07-02T00:00:00.000Z"`, `notes="Backfilled: matches capture-regression-matrix.json's issue-64-61-transitive-staleness entry (status locked by Phase 3.1). createdAt/closedAt are approximate (Phase 3.1 completion date) since no per-entry historical capture timestamp was tracked before this queue existed (D-02: git history remains the authoritative audit trail)."`
    - Entry 2 (agda_auto flag leak): `fingerprint="5abecc959e43fef3"`, `status="triaged"`, `defectKind="wrong-result"`, `triageClass="mechanical-import"`, `triageConfidence=0.87`, `recurrence=1`, `title="agda_auto concatenates unescaped CLI-flag-shaped hints into the Agsy search payload"`, `summary="hints/excludeHints values are concatenated verbatim into the Agsy CLI search-flag string with zero escaping; a flag-shaped hint (e.g. \"-t 999999\") is sent as literal Agsy flags, and the resulting ok:true/hasSolution:true response's solution text is actually Agda's own rejection of the injected flag."`, `affectedTool="agda_auto"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `relatedFingerprint=["004d161b839ce725"]`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: .planning/research/CHG-REVERIFY.md Defect 2, re-verified alive 2026-07-02 against current main. buildAutoSearchPayload() (src/agda/refactor-helpers.ts) needs escaping/validation of hints/excludeHints."`
    - Entry 3 (agda_give ok:true-wraps-error): `fingerprint="bfcba437f5426fd6"`, `status="triaged"`, `defectKind="false-green"`, `triageClass="proof-obligation"`, `triageConfidence=0.72`, `recurrence=1`, `title="agda_give reports ok:true while data.result carries Agda's own rejection text"`, `summary="Giving an ill-typed expression into a goal returns top-level ok:true/classification:ok while data.result embeds Agda's own rejection text verbatim and data.replacementText is null (the give was not applied). CHG-REVERIFY.md calls this the single highest-risk pattern for an agent silently committing a broken proof step."`, `affectedTool="agda_give"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: .planning/research/CHG-REVERIFY.md Defect 3, re-verified alive 2026-07-02 against current main. session.goal.give() (src/agda/goal-operations.ts) should distinguish a rejected give from a real success."`
    - Entry 4 (agda_search_definitions hardcoded layout): `fingerprint="eb7439cb3ed9d6b9"`, `status="triaged"`, `defectKind="missing-feature"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="agda_search_definitions hardcodes an agda/ layout, dead on src/-layout projects"`, `summary="agda_search_definitions hardcodes its search root to <PROJECT_ROOT>/agda/ (or agda/<tier>); an identical query against a src/-layout project (agda-unimath's real layout) returns ok:false/classification:not-found even though the target symbol exists."`, `affectedTool="agda_search_definitions"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: .planning/research/CHG-REVERIFY.md Defect 4, re-verified alive 2026-07-02 against current main. Not an Agda compiler error (triageClass null, not applicable) - a server-side path-resolution bug in src/tools/file/search-definitions.ts."`
    - Entry 5 (agda_proof_status mislabeling): `fingerprint="fdc90bfde12fb938"`, `status="triaged"`, `defectKind="wrong-result"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="agda_proof_status says \"All goals solved\" while constraintsText carries a real error"`, `summary="After a stale reload surfaces a real type error in data.constraintsText (hasConstraints:true), data.text's human-readable summary still ends with the literal line \"All goals solved.\", contradicting the tool's own structured fields."`, `affectedTool="agda_proof_status"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: .planning/research/CHG-REVERIFY.md Defect 1's related sub-finding, re-verified 2026-07-02. A labeling/presentation bug in the server's own summary text, not a raw Agda error (triageClass null, not applicable)."`
    - Entry 6 (RT1): `fingerprint="03f7c711c0209369"`, `status="new"`, `needsReverify=true`, `defectKind="false-green"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="RT1: a visible hole must never yield fileComplete:true"`, `summary="A file with at least one visible interaction hole ({!!}) is reported with fileComplete:true (v0.6.7 measurement, not yet re-verified against current main)."`, `affectedTool="agda_load"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: emilyriehl/Codex-Homotopy-Group agda-mcp-ux-report/README.md spec RT1 (turn-key regression spec, v0.6.7 measurement). Re-verify against current main before promoting past new."`
    - Entry 7 (RT2): `fingerprint="e5f6de1fa365b887"`, `status="new"`, `needsReverify=true`, `defectKind="false-green"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="RT2: a not-in-scope query must return ok:false/NotInScope, never a bare success"`, `summary="A query referencing an out-of-scope identifier returns a bare successful envelope instead of ok:false with a structured NotInScope classification (v0.6.7 measurement)."`, `affectedTool="agda_query"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: emilyriehl/Codex-Homotopy-Group agda-mcp-ux-report/README.md spec RT2 (v0.6.7 measurement). affectedTool is best-inferred (query-family) - confirm the exact tool during re-verification."`
    - Entry 8 (RT3): `fingerprint="eaea6321183bdf7b"`, `status="new"`, `needsReverify=true`, `defectKind="false-green"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="RT3: a failing context-check must never say \"no checked term\" inside a success result"`, `summary="A failing context/type-check is reported inside an ok:true result whose text says \"no checked term\", rather than as a failure (v0.6.7 measurement)."`, `affectedTool="agda_context"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: emilyriehl/Codex-Homotopy-Group agda-mcp-ux-report/README.md spec RT3 (v0.6.7 measurement). affectedTool is best-inferred - confirm the exact tool during re-verification."`
    - Entry 9 (RT4, relates to Entry 2): `fingerprint="004d161b839ce725"`, `status="new"`, `needsReverify=true`, `defectKind="wrong-result"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="RT4: agda_auto must not treat CLI-flag-shaped hints as a term or report a diagnostic as a solution"`, `summary="Same root cause as the re-verified agda_auto flag-leak entry (see relatedFingerprint): CLI-flag-shaped hint text is concatenated unescaped into the Agsy search payload and a diagnostic/rejection can be reported as hasSolution:true."`, `affectedTool="agda_auto"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `relatedFingerprint=["5abecc959e43fef3"]`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: emilyriehl/Codex-Homotopy-Group agda-mcp-ux-report/README.md spec RT4. Cross-references the re-verified-alive agda_auto entry - do not double-count on the dashboard; this spec is retained separately as its own future regression-test target."`
    - Entry 10 (RT5): `fingerprint="a0ae86c7deb9754e"`, `status="new"`, `needsReverify=true`, `defectKind="wrong-result"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="RT5: a mutation tool that fails to reload must return partial/failure with post-reload diagnostics"`, `summary="A mutation tool whose post-write reload fails reports success rather than a partial/failure result carrying the post-reload diagnostics (v0.6.7 measurement)."`, `affectedTool="agda_apply_edit"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: emilyriehl/Codex-Homotopy-Group agda-mcp-ux-report/README.md spec RT5 (v0.6.7 measurement). affectedTool is best-inferred (mutation-tool family) - confirm the exact tool during re-verification."`
    - Entry 11 (RT6, relates to Entry 5): `fingerprint="ad2b6d31f58f1759"`, `status="new"`, `needsReverify=true`, `defectKind="missing-feature"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="RT6: agda_load must distinguish visible goals/hidden metas/constraints/source holes/file-completeness"`, `summary="agda_load's response conflates these five states into one signal; the no-goals-but-constraints-hold-an-error sub-case is the same labeling issue tracked by the agda_proof_status entry (see relatedFingerprint)."`, `affectedTool="agda_load"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `relatedFingerprint=["fdc90bfde12fb938"]`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: emilyriehl/Codex-Homotopy-Group agda-mcp-ux-report/README.md spec RT6 (v0.6.7 measurement). Cross-references the agda_proof_status mislabeling entry for its sub-case; the broader five-state-conflation ask remains its own tracked item."`
    - Entry 12 (RT7): `fingerprint="b6821f42952c6ff8"`, `status="new"`, `needsReverify=true`, `defectKind="missing-feature"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="RT7: a timeout must identify whether Agda exited, is still running, or produced no protocol response"`, `summary="On a command timeout, the response does not distinguish whether the underlying Agda process exited, is still running, or simply produced no protocol response in the window (v0.6.7 measurement)."`, `affectedTool="agda_load"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: emilyriehl/Codex-Homotopy-Group agda-mcp-ux-report/README.md spec RT7 (v0.6.7 measurement). affectedTool is best-inferred (most timeout-prone command family) - confirm the exact tool during re-verification."`
    - Entry 13 (RT8, relates to Entry 1): `fingerprint="3306edf4c2d01c53"`, `status="new"`, `needsReverify=true`, `defectKind="false-green"`, `triageClass=null`, `triageConfidence=null`, `recurrence=1`, `title="RT8: a stale reload must report the previous+new classification together with the reason"`, `summary="Same family as the re-verified flagship transitive-staleness false-green (see relatedFingerprint): re-verify whether agda_load_no_metas and related tools in this family still lack a combined previous+new classification report."`, `affectedTool="agda_load_no_metas"`, `capturePath=null`, `verdictPath=null`, `matrixEntryId=null`, `relatedFingerprint=["e6f0c1169032b9d5"]`, `createdAt="2026-07-02T00:00:00.000Z"`, `closedAt=null`, `notes="Source: emilyriehl/Codex-Homotopy-Group agda-mcp-ux-report/README.md spec RT8. Cross-references the flagship entry - current-main agda_load already attaches a session-regression diagnostic with previousClassification; re-verify whether this spec's ask is already satisfied or still gapped for agda_load_no_metas specifically."`

    Omit `needsReverify` entirely on Entries 1-5 (it defaults to absent/false — only Entries 6-13 set it `true`). Omit `issue` entirely on Entries 2-13 (only Entry 1 has a known pre-existing GitHub issue array). Omit `relatedFingerprint` entirely on Entries 3, 4, 5, 6, 7, 8, 10, 12 (only Entries 1, 9, 11, 13 carry one).
  </action>
  <acceptance_criteria>
    - Behavior: `test/fixtures/fix-queue.json` contains exactly 13 entries, each at `recurrence: 1`, after running the seed script once against the real file; the scratch-copy check (in the action above) proves a second run would bump recurrence rather than duplicate, WITHOUT that second run ever touching the committed file.
    - Test command: `node -e "const q=JSON.parse(require('fs').readFileSync('test/fixtures/fix-queue.json','utf8')); if(q.length!==13){console.error('expected 13, got '+q.length); process.exit(1);} console.log('ok: 13 entries present');"` (read-only — safe to re-run any number of times, unlike the seed script itself).
    - Source assertion: `grep -c fingerprint test/fixtures/fix-queue.json` equals 13.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const q=JSON.parse(require('fs').readFileSync('test/fixtures/fix-queue.json','utf8')); if(q.length!==13){console.error('expected 13, got '+q.length); process.exit(1);} console.log('ok: 13 entries present');"</automated>
  </verify>
  <done>test/fixtures/fix-queue.json holds the real 13-entry cargo at recurrence:1 each; idempotency (bump-not-duplicate on a second run) is proven via a scratch copy, never by mutating the committed file twice.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the seed data is internally consistent</name>
  <files>test/unit/fixtures/fix-queue.test.ts</files>
  <read_first>
    - test/unit/fixtures/fix-queue.test.ts (Plan 04-01's Task 1 output) — the existing "loads-empty"/schema-acceptance/refinement tests this task extends, not replaces.
    - src/agda/error-classifier.ts — `classifyAgdaError`, imported directly to re-derive Entries 1-3's expected triageClass/triageConfidence from their own raw error text (hardcoded as literals in this test, matching Task 1's Entry 1-3 raw-text values).
  </read_first>
  <action>
    Extend `test/unit/fixtures/fix-queue.test.ts` with: (1) a count assertion — `fixQueue.length === 13`; (2) a uniqueness assertion — every `fingerprint` is unique (`new Set(fixQueue.map(e => e.fingerprint)).size === fixQueue.length`); (3) a referential-integrity assertion — for every entry, every string in its `relatedFingerprint` array (when present) must equal SOME OTHER entry's `fingerprint` in the same `fixQueue` array (no dangling cross-reference); (4) a spot-check on the flagship entry (`fixQueue.find(e => e.fingerprint === "e6f0c1169032b9d5")`) — `status === "locked"`, `closedAt` is non-null, `matrixEntryId === "issue-64-61-transitive-staleness"`, `issue` contains both `64` and `61`; (5) a `needsReverify` count — exactly 8 entries have `needsReverify === true`; (6) a classifier-accuracy re-check — hardcode the 3 raw error-text literals from Task 1's Entries 1-3 (the flagship's `"6.12-20: error: [UnequalTerms]\nBool !=< Nat\nwhen checking that the expression getValue has type Nat"`, the agda_auto entry's `"1.1-3: error: [NotInScope]\nNot in scope:\n  -d at 1.1-3\nwhen scope checking -d"`, and the agda_give entry's `"1.1-5: error: [UnequalTerms]\nBool !=< Nat\nwhen checking that the expression true has type Nat"`), call `classifyAgdaError` on each, and assert the result's `category`/`confidence` exactly matches the corresponding seeded entry's `triageClass`/`triageConfidence` — this re-proves the plan's precomputed values are correctly embedded and gives future-proof regression protection if the classifier is ever retuned.
  </action>
  <acceptance_criteria>
    - Behavior: all 6 new assertions pass against the real seeded `fix-queue.json`.
    - Test command: `npx vitest run test/unit/fixtures/fix-queue.test.ts`
    - Source assertion: `grep -c "classifyAgdaError" test/unit/fixtures/fix-queue.test.ts` is at least 1; `grep -c "relatedFingerprint" test/unit/fixtures/fix-queue.test.ts` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/fixtures/fix-queue.test.ts</automated>
  </verify>
  <done>All 6 new assertions plus the 4 pre-existing Plan-04-01 assertions in this file pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan only writes planner-authored, git-tracked seed data through Plan 04-01's already-validated `upsertQueueEntry` path. No external/untrusted input, network, or subprocess is involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | scripts/queue/seed-initial-cargo.mjs | mitigate | Every seed entry is validated by `upsertQueueEntry`'s `fixQueueEntrySchema.parse(...)` call (Plan 04-01) before being written — a malformed hand-authored entry fails loudly rather than silently corrupting the real queue file. |
| T-04-03-02 | Information Disclosure | seed entry `notes`/`summary` fields | accept | All quoted raw error text and descriptions originate from `.planning/research/CHG-REVERIFY.md` and the already-planning-committed CHG UX-report paraphrase — no new private-corpus source, diff, or capture-bundle content is introduced by this plan. |

</threat_model>

<verification>
Run `npx vitest run test/unit/fixtures/fix-queue.test.ts` — all 10 assertions (4 from Plan 04-01 + 6 new) green. Confirm `test/fixtures/fix-queue.json` has 13 entries via `node -e "console.log(JSON.parse(require('fs').readFileSync('test/fixtures/fix-queue.json','utf8')).length)"`.
</verification>

<success_criteria>
- test/fixtures/fix-queue.json holds all 13 real defect entries with correct cross-references.
- scripts/queue/seed-initial-cargo.mjs is idempotent (proven by a second run bumping recurrence, not duplicating).
- All fix-queue.test.ts assertions (schema + seed-data) pass.
</success_criteria>

<output>
Create `.planning/phases/04-triage-fix-queue/04-03-SUMMARY.md` when done
</output>
