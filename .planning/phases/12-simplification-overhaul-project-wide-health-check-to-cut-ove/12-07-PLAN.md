---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 07
type: execute
wave: 3
depends_on: ["12-06"]
files_modified:
  - .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md
autonomous: false
requirements: [D-03]

must_haves:
  truths:
    - "The user reviewed 12-HEALTH-REPORT.md's full cut list and responded with an explicit per-item decision before any execution plan is allowed to run"
    - "Every CUT-NN id from 12-HEALTH-REPORT.md appears exactly once in 12-APPROVED-CUTS.md's normalized decision table"
    - "Any CUT-NN id the user's response did not explicitly mention is recorded as deferred, never silently treated as approved"
  artifacts:
    - path: ".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md"
      provides: "The durable, normalized record of which cut-list items the user approved, rejected, or deferred, consumed by every back-half execution plan"
      contains: "Normalized decisions"
  key_links:
    - from: "12-APPROVED-CUTS.md normalized decisions"
      to: "Plans 12-08, 12-09, 12-10, 12-11"
      via: "each execution plan's task filters 12-HEALTH-REPORT.md's rows by category and only acts on rows marked approved here"
      pattern: "approved"
---

<objective>
Present the full severity-graded cut list from 12-HEALTH-REPORT.md to the user for individual, item-by-item sign-off, per decision D-03 (no cut lands before sign-off). After the user responds, record their literal decision durably into a normalized, per-CUT-NN approval table that every back-half execution plan reads to know exactly what it is authorized to touch. This plan structurally blocks every execution plan in this phase: nothing in Wave 4 or later can run until this plan's checkpoint is answered and its recording task completes.

Purpose: D-03 is a locked decision, not a discretionary checkpoint — the phase's entire back half is gated on it. The recording task exists because the approved list must survive into separately-executed plan files, not just live in this session's own conversation memory.
Output: 12-APPROVED-CUTS.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md

<interfaces>
Canonical category enum (defined by Plan 12-06; this plan carries it forward unchanged): the category column below MUST hold exactly one of the four lowercase tokens `pipeline` | `docs-residue` | `tools` | `src-subtraction`, copied verbatim from each CUT-NN row's Category field in 12-HEALTH-REPORT.md — never a re-worded synonym or the prose section header. Plans 12-08/12-09/12-10/12-11 filter this column on `category pipeline` / `category tools` / `category src-subtraction` / `category docs-residue` respectively, so a drifted token silently drops an approved cut from execution.

12-APPROVED-CUTS.md schema (this plan defines and produces it; Plans 12-08 through 12-12 consume it as a fixed contract): a Summary line at the top stating the approved, rejected, deferred, and unmapped counts. A Verbatim human response section quoting the user's literal reply text in full. A Normalized decisions table with one row per CUT-NN id from 12-HEALTH-REPORT.md: CUT-NN id, category (one of the four canonical tokens above), decision of approved, rejected, or deferred, and notes. An Unmapped approvals section, present even if empty, for any response the user gave that does not cleanly map to a known CUT-NN id or category — items here are never executed by any downstream plan until a human explicitly clarifies them in a future session.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Present the cut list for item-by-item sign-off</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md</files>
  <read_first>
    - 12-HEALTH-REPORT.md in full — every CUT-NN row across all four categories, the Regression-Lock Exclusion List, the RT6/RT7 verdicts, and the baseline metrics.
    - 12-CONTEXT.md's D-03 exact wording (the user approves cut-list items individually; the back half executes only the approved list; no cut lands before sign-off).
  </read_first>
  <what-built>
    A single, consolidated, severity-graded health report and cut list (12-HEALTH-REPORT.md), covering the pipeline, docs and planning residue, MCP tool surface, and src low-risk subtraction categories, plus a definitive RT6 and RT7 re-evaluation verdict and a fresh before-cut baseline metrics snapshot.
  </what-built>
  <action>
    Present every CUT-NN row from 12-HEALTH-REPORT.md to the user, grouped by category, each with its Issue, Files, Impact, Fix approach, and Severity already visible in the report. Ask the user to respond with an explicit decision for every item they want to approve or reject; anything they do not mention will be recorded as deferred, not approved, by Task 2. Do not proceed to Task 2 until the user has responded.
  </action>
  <how-to-verify>
    1. Read 12-HEALTH-REPORT.md's full cut list, organized by category.
    2. For each CUT-NN item, decide approve, reject, or defer. Severity and Impact are shown per item to inform the decision; RT6 and RT7 have no approve or reject action needed since D-04 already restricts this phase to a verdict only, with implementation explicitly out of scope.
    3. Reply with one line per CUT-NN id you want to explicitly decide, for example: CUT-01 approve, CUT-02 reject, CUT-03 defer reason optional. Any CUT-NN id you do not mention will be recorded as deferred by default, never approved by default.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md && grep -c "CUT-[0-9]" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - The material being presented (12-HEALTH-REPORT.md) exists and contains at least one CUT-NN row, confirmed by the automated precondition check above.
    - The user has provided an explicit, literal response identifying which CUT-NN items to approve, reject, or defer; this specific fact has no automated proxy and is confirmed only by Task 2's successful parse of that response.
  </acceptance_criteria>
  <resume-signal>Reply with one line per CUT-NN id you want to decide (for example, "CUT-01 approve", "CUT-02 reject", "CUT-03 defer"). Anything unmentioned is recorded as deferred, not approved.</resume-signal>
  <done>The user has responded with their per-item decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Record the normalized approval decisions durably</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md</files>
  <read_first>
    - Task 1's resume message: the user's literal, verbatim response text captured when this plan continued after the checkpoint.
    - 12-HEALTH-REPORT.md's full CUT-NN list (to enumerate every id that must appear in the normalized table, including ones the user did not mention).
    - This plan's own interfaces schema above (the exact 12-APPROVED-CUTS.md shape to produce).
  </read_first>
  <action>
    Parse the user's literal response from Task 1 into a decision per CUT-NN id. Any CUT-NN id present in 12-HEALTH-REPORT.md that the response did not explicitly mention must be recorded as deferred; this is a fail-closed default, not a judgment call, and must never be treated as approved. Create 12-APPROVED-CUTS.md using this plan's interfaces schema: a Summary line with the approved, rejected, deferred, and unmapped counts; a Verbatim human response section quoting the user's reply text exactly as given, with no paraphrasing; a Normalized decisions table with exactly one row per CUT-NN id from 12-HEALTH-REPORT.md; and an Unmapped approvals section for anything in the user's response that does not cleanly resolve to a known CUT-NN id (for example, a typo'd id or a category-level blanket statement) — populate this section explicitly rather than guessing a mapping, and state in it that any item here is not actionable until a human clarifies in a future session.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md && grep -c "Normalized decisions" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md</automated>
  </verify>
  <acceptance_criteria>
    - 12-APPROVED-CUTS.md exists with all four required sections (Summary, Verbatim human response, Normalized decisions, Unmapped approvals).
    - The Normalized decisions table's row count exactly equals 12-HEALTH-REPORT.md's total CUT-NN count; no id is missing and none is duplicated.
    - Every row's decision value is exactly one of approved, rejected, or deferred; no other value appears.
    - Every row's category value is exactly one of the four canonical tokens (pipeline, docs-residue, tools, src-subtraction), copied from the corresponding 12-HEALTH-REPORT.md row's Category field; no prose header or synonym appears.
    - The Summary line's counts sum to the total CUT-NN count.
  </acceptance_criteria>
  <done>12-APPROVED-CUTS.md exists, is complete, fail-closed on anything unmentioned, and is ready for every back-half execution plan to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The human's approval decision versus every downstream execution plan | This is the core control boundary the entire phase's D-03 gate exists to enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Elevation of Privilege | The entire back half of this phase | mitigate | This plan IS the mitigation: a blocking checkpoint gate plus a fail-closed unmentioned-equals-deferred default in Task 2, structurally required before Wave 4 can start via depends_on |
| T-12-14 | Repudiation | The approval record itself | mitigate | Task 2 quotes the user's literal response verbatim in the Verbatim human response section, so the normalized table's provenance is auditable, not just asserted |
</threat_model>

<verification>
12-APPROVED-CUTS.md exists with a complete, fail-closed, CUT-NN-indexed normalized decision table whose row count matches 12-HEALTH-REPORT.md exactly. The checkpoint in Task 1 was answered by the user before Task 2 ran.
</verification>

<success_criteria>
Every cut-list item has an explicit, durably-recorded decision; nothing is approved by default; every back-half execution plan has a single, unambiguous source of truth for what it may touch.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-07-SUMMARY.md when done
</output>
