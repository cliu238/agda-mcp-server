---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-TOOLS.md
autonomous: true
requirements: [D-02, C-05]

must_haves:
  truths:
    - "Every one of the 74 registered MCP tools has a recorded real-invocation count derived from JSON-RPC-aware transcript parsing, not a naive string grep"
    - "Every zero-usage tool considered for a cut-list row is explicitly cross-checked against RESEARCH.md's Security Domain table before inclusion, with excluded tools named and their exclusion reason stated"
    - "Every D-02 candidate row pre-maps the full tool-deletion lockstep checklist so the back-half execution plan has a ready-made process to follow"
  artifacts:
    - path: ".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-TOOLS.md"
      provides: "74-tool usage-evidence table and severity-graded MCP tool-surface cut-list candidates with pre-mapped lockstep checklists"
      contains: "Cut-List Candidates"
  key_links:
    - from: "12-AUDIT-TOOLS.md cut-list rows"
      to: "12-06 consolidated health report"
      via: "Plan 12-06 Task 1 transcribes this doc's rows into the MCP Tool Surface section"
      pattern: "MCP Tool Surface"
---

<objective>
Audit the 74-tool MCP surface for redundancy per decision D-02 (direct deletion allowed, no deprecation period). Build a real per-tool usage-evidence table from local dogfooding transcripts using the JSON-RPC-envelope-aware method RESEARCH.md specifies (not a naive grep, which over-counts), cross-reference every low-usage candidate against the Security Domain controls before including it, and pre-map the full deletion lockstep checklist into each candidate row so Plan 12-09's execution work is a checklist walk, not a re-derivation. This plan is read-only, producing exactly one new report artifact for Plan 12-06 to consolidate.

Purpose: D-02 explicitly allows one-pass tool deletion, but C-05 requires the audit, not a guess, to pick which tools. RESEARCH.md's Pitfall 6 warns that low usage is not the same signal as low security value; this plan's cross-check step exists specifically to prevent that mistake from reaching the cut list.
Output: 12-AUDIT-TOOLS.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md

<interfaces>
Reuse the same cut-list row shape Plan 12-01 defines (Issue, Files, Impact, Fix approach, Severity), with two additions specific to this category: a Real-usage-count field (the number this plan measures, plus the local-sample-size caveat) and a Lockstep-checklist field pre-listing the six concrete steps from 12-PATTERNS.md's Shared Pattern B (manifest or session-registration-file entry, coverage-matrix entry, tool-family-examples entry if present, README and assistant-workflows.md doc mentions, tool-recommendation.ts addIfAvailable entry, and an orphaned-test-file check) so Plan 12-09 can execute against this row directly.

From src/tools/manifest.ts: listToolManifest() returns ToolManifestEntry[] with a name field and a category field (ToolCategory). clearToolManifest() resets the process-global registry. registerCoreTools(server, session, projectRoot) from src/tools/register-core-tools.ts wires all 13 top-level register() groups; 8 of the 74 tools additionally register through src/session/load-tool-registration.ts and src/session/process-tool-registration.ts rather than through register-core-tools.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 74-tool real-usage evidence table</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-TOOLS.md</files>
  <read_first>
    - 12-RESEARCH.md Code Examples section, the Python script titled "Real tool-usage aggregation (JSON-RPC-envelope-aware, not a flat grep)" — copy its logic exactly (filter on direction equal to to-server and method equal to tools/call, read params.name), do not write a simpler grep-based substitute.
    - 12-RESEARCH.md Pitfall 3 (why a naive grep over-counts by roughly 3x, since agda_tools_catalog's own response payload embeds every tool name inside its listing) and Assumption A5 (the local sample is real but small and biased toward RT1 through RT8 probes; treat as one weak input, never a sole deletion criterion).
    - test/fixtures/e2e/mcp-tool-coverage.json (the 74-entry source of truth for tool name, suite, scenario, requiresLiveAgda, requiresBackend fields).
    - src/tools/manifest.ts exports listToolManifest, clearToolManifest, and the ToolCategory type definition, for cross-referencing each tool's category alongside its usage count.
  </read_first>
  <action>
    Create 12-AUDIT-TOOLS.md with a Real Usage Evidence section. Run the JSON-RPC-aware aggregation script from RESEARCH.md's Code Examples verbatim against every .agda-mcp/runs/star star/transcript.jsonl file present at execution time (re-glob fresh, the run count may exceed the 21 recorded at research time). Build a 74-row table: tool name, category (from the coverage-matrix's suite grouping or the manifest's ToolCategory), real-invocation-count from this run, requiresLiveAgda, requiresBackend. Add an explicit disclaimer paragraph immediately below the table, citing Assumption A5 verbatim in substance: this evidence is one weak input among several and must never be treated as a sole deletion criterion.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-TOOLS.md</automated>
  </verify>
  <acceptance_criteria>
    - The Real Usage Evidence table has exactly 74 rows, one per tool in test/fixtures/e2e/mcp-tool-coverage.json.
    - The invocation-count column was produced by the JSON-RPC-aware method (direction and method fields checked), not a flat string count; the doc states which glob pattern and how many run directories were scanned at execution time.
    - The disclaimer paragraph explicitly states the evidence is one weak input, never a sole deletion criterion.
  </acceptance_criteria>
  <done>All 74 tools have a real, JSON-RPC-aware usage count with an explicit reliability disclaimer.</done>
</task>

<task type="auto">
  <name>Task 2: Redundancy analysis with security cross-check, draft D-02 cut-list rows</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-TOOLS.md</files>
  <read_first>
    - 12-RESEARCH.md Security Domain section in full (the V2 through V6 ASVS table and the five-row Known Threat Patterns table) and Pitfall 6 (low usage is not the same signal as low security value, with its four named examples: safe-source-io.ts's O_NOFOLLOW guard and byte cap, archive-extract.mjs's layered extraction defense, issue-key.mjs's timingSafeEqual comparison, and proof-actions.ts's two-sided rejection-detection SSOT).
    - 12-PATTERNS.md Shared Pattern B in full (the six-step D-02 lockstep checklist) and its reverse-checklist precedent quoting Phase 10 Plan 10-03's SUMMARY provides field, read as a tool-ADD checklist to invert for a tool-DELETE.
    - src/session/tool-recommendation.ts lines showing the addIfAvailable call shape (around a dozen call sites) — this is what Plan 12-09 will need to search for a mirror-image removal.
    - src/tools/register-core-tools.ts (13 register calls) and the fact that 8 of the 74 tools instead register through src/session/load-tool-registration.ts and src/session/process-tool-registration.ts.
  </read_first>
  <action>
    Using Task 1's usage table plus each tool's manifest description or category, identify candidates that show at least one of: overlapping purpose with another registered tool, zero recorded local usage with no Security Domain match, or a name and description close enough to another tool's that an agent could plausibly mis-select between them. Before including any zero-usage candidate, explicitly cross-check it against every row of RESEARCH.md's Security Domain and Known Threat Patterns tables; if a candidate touches or exposes one of those controls, exclude it and record the exclusion with a one-line reason in a dedicated Excluded-on-Security-Grounds subsection, rather than silently dropping it from consideration. For every genuine candidate that survives the cross-check, draft a Cut-List Candidates row using this plan's interfaces shape, with the Lockstep-checklist field pre-filled per Shared Pattern B's six steps (manifest or session-registration-file entry to delete; coverage-matrix entry to delete; tool-family-examples.json entry to delete if present; README.md and docs/assistant-workflows.md mentions to grep and fix; tool-recommendation.ts addIfAvailable entry to check and remove; orphaned single-tool test file to check and possibly delete) so Plan 12-09 can execute the row directly without re-deriving the checklist.
  </action>
  <verify>
    <automated>grep -c "Excluded-on-Security-Grounds" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-TOOLS.md</automated>
  </verify>
  <acceptance_criteria>
    - The document contains an Excluded-on-Security-Grounds subsection naming every zero-usage tool that was considered and rejected specifically due to a Security Domain match, each with its matching control cited by name.
    - Every Cut-List Candidates row has a fully filled Lockstep-checklist field naming all six concrete steps, not a reference like see Shared Pattern B without restating the steps.
    - Zero rows is an acceptable outcome only if explicitly stated with reasoning; the document never silently omits this section.
  </acceptance_criteria>
  <done>Redundancy candidates are drafted with the Security Domain filter demonstrably applied and the full six-step lockstep checklist pre-mapped per row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dogfooding transcripts (.agda-mcp/runs) read by Task 1 | May contain real Agda proof source text and file paths from local corpora; this plan aggregates only tool names and call counts, never full payloads |
| This audit's redundancy judgment versus security-load-bearing tools | A tool judged redundant purely on low usage could otherwise remove a control RESEARCH.md's Security Domain names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | .agda-mcp/runs transcript aggregation (Task 1) | mitigate | The aggregation script prints only tool names and call counts, per RESEARCH.md's own script shape; this plan's report never quotes raw transcript payloads, proof source text, or file paths from the scanned runs |
| T-12-06 | Tampering (control removal via mis-applied low-usage heuristic) | Any tool touching a Security Domain control | mitigate | Task 2's mandatory Security Domain cross-check and Excluded-on-Security-Grounds subsection make the filter's application auditable rather than assumed |
</threat_model>

<verification>
12-AUDIT-TOOLS.md exists with both required sections (a 74-row Real Usage Evidence table and Cut-List Candidates with pre-mapped lockstep checklists), plus the Excluded-on-Security-Grounds subsection. No file under src/, docs/, or test/ is modified by this plan.
</verification>

<success_criteria>
All 74 tools have real, JSON-RPC-aware usage evidence; redundancy candidates are drafted only after an explicit Security Domain cross-check; every candidate row is immediately executable by Plan 12-09 without further research.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-03-SUMMARY.md when done
</output>
