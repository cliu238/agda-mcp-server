---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 06
type: execute
wave: 2
depends_on: ["12-01", "12-02", "12-03", "12-04", "12-05"]
files_modified:
  - .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md
  - .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-BASELINE-TOOLS.txt
  - test/fixtures/fix-queue.json
autonomous: true
requirements: [D-03, D-04, C-05]

must_haves:
  truths:
    - "Every cut-list row from all five audit docs (12-01 through 12-05) appears in 12-HEALTH-REPORT.md with a unique CUT-NN id, transcribed verbatim, not re-summarized or re-interpreted"
    - "The severity scale is defined exactly once, with all four tiers named consistently across every transcribed row"
    - "The baseline metrics in 12-HEALTH-REPORT.md were produced by re-running RESEARCH.md's Code Examples commands this session, not copied from RESEARCH.md's own stale 2026-07-06 numbers"
    - "fix-queue.json's RT6 and RT7 entries each gained an appended, dated re-evaluation note without any other field or entry changing"
  artifacts:
    - path: ".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md"
      provides: "The single consolidated, severity-graded health report and cut list the D-03 sign-off checkpoint presents to the user"
      contains: "Regression-Lock Exclusion List"
    - path: ".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-BASELINE-TOOLS.txt"
      provides: "Machine-readable pre-cut tool-name baseline (one agda_ tool name per line, from the current coverage matrix) so Plan 12-11's doc-reference check can flag references to tools this phase actually deleted, not never-built proposed names"
      contains: "agda_"
  key_links:
    - from: "12-HEALTH-REPORT.md CUT-NN rows"
      to: "12-07's sign-off checkpoint"
      via: "Plan 12-07 Task 1 presents every CUT-NN row from this report for individual approval"
      pattern: "CUT-[0-9]+"
    - from: "fix-queue.json RT6/RT7 notes"
      to: "the project's durable defect ledger"
      via: "an appended, dated note recording this phase's re-evaluation verdict, never overwriting prior content"
      pattern: "RE-EVALUATED"
---

<objective>
Merge the five front-half audit reports (pipeline, docs and residue, MCP tool surface, src low-risk subtraction, regression locks and RT6/RT7) into one severity-graded health report and cut list, assign every candidate a stable CUT-NN id, capture a fresh before-cut baseline metrics snapshot, and durably record the RT6/RT7 verdicts into fix-queue.json as an append-only note. This is the last plan before the D-03 sign-off checkpoint; nothing here is itself a cut and no code, script, or doc file changes.

Purpose: D-03 requires a single, coherent cut list for the user to sign off on item by item — five separate audit docs are not directly presentable. D-04 requires the RT6/RT7 verdict to be recorded durably, not just left inside a phase-scoped audit doc a future session might not find.
Output: 12-HEALTH-REPORT.md in the phase directory; an append-only edit to test/fixtures/fix-queue.json's RT6 and RT7 entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-PIPELINE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-DOCS.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-TOOLS.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-SRC-DEBT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-LOCKS-RT6RT7.md

<interfaces>
Canonical category enum (this plan pins it; every downstream plan keys off these exact four lowercase tokens — do not invent synonyms):
`pipeline` | `docs-residue` | `tools` | `src-subtraction`. The mapping from this report's human-readable section headers to the canonical token is fixed here: Pipeline section → `pipeline`; Docs and Planning Residue section → `docs-residue`; MCP Tool Surface section → `tools`; src Low-Risk Subtraction section → `src-subtraction`. Each CUT-NN row MUST carry its category token verbatim in a `Category` field so Plan 12-07's approval table and Plans 12-08/12-09/12-10/12-11's execution filters (which select on `category pipeline` / `category tools` / `category src-subtraction` / `category docs-residue` respectively) can key off the token, not the prose header.

12-HEALTH-REPORT.md schema (this plan defines it; Plans 12-07 through 12-12 consume it as a fixed contract):
A frontmatter-style header block naming the phase, the audit date, a status of cut-list-pending-signoff, the four-tier severity scale with a one-line definition per tier, the canonical category enum above, and a per-category candidate count. Then, in order: a Pipeline section, a Docs and Planning Residue section, an MCP Tool Surface section, and a src Low-Risk Subtraction section, each containing that category's transcribed rows with a unique CUT-NN id prefixed onto each row (CUT-01, CUT-02, and so on, assigned once, sequentially, across all four categories combined) and each row carrying its canonical `Category` token. Then a Regression-Lock Exclusion List section (transcribed verbatim from 12-05's Task 1 output). Then an RT6/RT7 Re-Evaluation Verdicts section (transcribed verbatim from 12-05's Task 2 output). Then a Baseline Metrics (pre-cut) section with numbers produced by this plan's own commands, not copied from any prior document. This plan also emits a sibling machine-readable file 12-BASELINE-TOOLS.txt: the sorted list of every agda_ tool name in the current test/fixtures/e2e/mcp-tool-coverage.json (one per line), captured before any Wave-4 deletion, so Plan 12-11's final doc-surface check can compute the deleted-tool set (baseline minus post-deletion manifest) and flag only references to tools this phase actually removed.

12-APPROVED-CUTS.md schema (Plan 12-07 defines and produces this; noted here only so this plan's CUT-NN ids are assigned in a way Plan 12-07 can reference cleanly): every CUT-NN id assigned in this report must be unique and stable, since Plan 12-07's approval table keys off these exact ids.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge five audit docs into the severity-graded health report with baseline metrics</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md</files>
  <read_first>
    - All five audit docs in full: 12-AUDIT-PIPELINE.md, 12-AUDIT-DOCS.md, 12-AUDIT-TOOLS.md, 12-AUDIT-SRC-DEBT.md, 12-AUDIT-LOCKS-RT6RT7.md.
    - 12-PATTERNS.md Pattern 1 in full (the frontmatter shape borrowed from v1.1-MILESTONE-AUDIT.md and the per-finding field shape from CONCERNS.md) and its No Analog Found table entry confirming no repo-wide severity scale exists, meaning this plan's definition is authoritative for the rest of the phase.
    - 12-RESEARCH.md Code Examples section, the exact "Baseline metrics snapshot" command block (five find and wc -l style commands plus the 74-tool count derivation), to be re-run verbatim, not copied from RESEARCH.md's own 2026-07-06 numbers which are explicitly marked stale by RESEARCH.md's own Metadata section.
  </read_first>
  <action>
    Create 12-HEALTH-REPORT.md using this plan's interfaces schema exactly. Transcribe every Cut-List Candidates row from all five audit docs verbatim (do not re-summarize, re-word, or drop any field) into the matching category section, assigning each a sequential CUT-NN id starting at CUT-01 and stamping each row with its canonical Category token from this plan's interfaces enum (pipeline | docs-residue | tools | src-subtraction) per the section-to-token mapping. If two audit docs defined the severity scale slightly differently (12-01 is the primary definition; later docs were told to reconcile wording with it), normalize all rows onto one single four-tier scale defined once in this report's header, preserving each row's original severity judgment, only normalizing the label wording if it drifted. Transcribe 12-05's Regression-Lock Exclusion List and RT6/RT7 Re-Evaluation Verdicts sections verbatim into their own top-level sections. Re-run RESEARCH.md's exact baseline metrics commands now, against the current working tree, and paste the live command output (not RESEARCH.md's stale numbers) into a Baseline Metrics (pre-cut) section, noting today's date next to each number. Additionally, write 12-BASELINE-TOOLS.txt as the sorted, one-per-line list of every agda_ tool name currently in test/fixtures/e2e/mcp-tool-coverage.json (e.g. via node reading the matrix's tool field), so it captures the pre-cut tool set for Plan 12-11's deleted-tool doc check.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md && test -s .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-BASELINE-TOOLS.txt && grep -c "^CUT-\|CUT-[0-9]" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - Every cut-list row present across all five audit docs appears in 12-HEALTH-REPORT.md exactly once with a unique CUT-NN id and a canonical Category token drawn from the fixed enum (pipeline | docs-residue | tools | src-subtraction); the total CUT-NN count matches the sum of candidate rows across the five source docs (state this sum explicitly in the report header).
    - The Regression-Lock Exclusion List and RT6/RT7 Re-Evaluation Verdicts sections are present and match their source docs' content with no summarization.
    - The Baseline Metrics section's numbers are dated today and were produced by commands actually run this session (state the exact commands run, not just the resulting numbers).
    - 12-BASELINE-TOOLS.txt exists and is non-empty, containing one agda_ tool name per line matching the current test/fixtures/e2e/mcp-tool-coverage.json tool set (row count equals the live tool count stated in the baseline metrics).
  </acceptance_criteria>
  <done>12-HEALTH-REPORT.md exists as the single, complete, CUT-NN-indexed consolidation of all five audits plus a freshly-measured baseline, and 12-BASELINE-TOOLS.txt captures the pre-cut tool-name set for Plan 12-11's deleted-tool doc check.</done>
</task>

<task type="auto">
  <name>Task 2: Record RT6 and RT7 verdicts durably into fix-queue.json</name>
  <files>test/fixtures/fix-queue.json</files>
  <read_first>
    - 12-HEALTH-REPORT.md's RT6/RT7 Re-Evaluation Verdicts section, just written by Task 1.
    - test/fixtures/fix-queue.json's current full entries for fingerprint ad2b6d31f58f1759 and fingerprint b6821f42952c6ff8.
    - test/fixtures/fix-queue.json entry 0bc76d15c2fec8df's notes field, which documents this queue's own established precedent for hand-authored, source-inspection-derived note content, as the style precedent for this task's append.
  </read_first>
  <action>
    Append a dated note to both ad2b6d31f58f1759's and b6821f42952c6ff8's notes field, prefixed literally with the text RE-EVALUATED followed by today's date and the text Phase 12, then a one-sentence summary of that item's verdict from 12-HEALTH-REPORT.md's RT6/RT7 section plus a citation to that report by its exact path. Append only: preserve every character of each entry's existing notes content unchanged, adding the new text after it. Do not change either entry's status field; both remain triaged, since a re-evaluation verdict is not itself a fix. Do not touch any other field or any other entry in the file.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('test/fixtures/fix-queue.json','utf8')); const rt6=d.find(e=>e.fingerprint==='ad2b6d31f58f1759'); const rt7=d.find(e=>e.fingerprint==='b6821f42952c6ff8'); if(!/RE-EVALUATED/.test(rt6.notes)||!/RE-EVALUATED/.test(rt7.notes)) process.exit(1); if(rt6.status!=='triaged'||rt7.status!=='triaged') process.exit(1); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - test/fixtures/fix-queue.json still parses as valid JSON.
    - Both ad2b6d31f58f1759 and b6821f42952c6ff8's notes fields contain the literal string RE-EVALUATED after their original content, and both entries' status fields remain triaged.
    - A diff of this file against its state before this task shows changes confined to exactly these two entries' notes fields, with no other field, entry, or array-length change.
  </acceptance_criteria>
  <done>Both RT6 and RT7 carry a durable, dated, append-only re-evaluation note citing the health report, with their triaged status and every other field unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This consolidation's transcription fidelity versus the human's sign-off decision | A mis-transcribed severity, Impact, or Fix approach field could mislead the human into approving something different from what the audit actually found |
| This task's edit to test/fixtures/fix-queue.json | A tracked data file the project's own tooling (dashboard generation, cron judge) also reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Tampering | 12-HEALTH-REPORT.md transcription | mitigate | Task 1 requires verbatim transcription of every field, never a re-summary; the automated verify step confirms the CUT-NN row count matches the source docs' combined candidate count |
| T-12-12 | Tampering | test/fixtures/fix-queue.json (Task 2's edit) | mitigate | Append-only discipline, a scripted before/after diff check confined to exactly two entries' notes fields, and an explicit status-unchanged assertion in the automated verify command |
</threat_model>

<verification>
12-HEALTH-REPORT.md exists with all required sections and a CUT-NN row count matching the sum of the five audit docs' candidates. fix-queue.json parses as valid JSON, with only the two RT6/RT7 notes fields changed and every other entry byte-identical to before this plan ran.
</verification>

<success_criteria>
A single, coherent, CUT-NN-indexed health report exists, ready for the D-03 sign-off checkpoint; RT6 and RT7's verdicts are durably recorded in the project's own defect ledger.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-06-SUMMARY.md when done
</output>
