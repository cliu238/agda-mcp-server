---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 08
type: execute
wave: 4
depends_on: ["12-07"]
files_modified:
  - scripts/dogfood/**
  - scripts/oracle/**
  - scripts/queue/**
  - scripts/team/**
  - test/unit/tools/dogfood-*.test.ts
  - test/unit/tools/oracle-*.test.ts
  - test/unit/tools/queue-*.test.ts
  - test/unit/tools/team-*.test.ts
  - package.json
autonomous: true
requirements: [D-01, C-01, C-02]

must_haves:
  truths:
    - "Only CUT-NN items tagged category pipeline with decision approved in 12-APPROVED-CUTS.md are executed; every other pipeline candidate is left untouched"
    - "No file named in 12-HEALTH-REPORT.md's Regression-Lock Exclusion List is deleted or weakened by this plan"
    - "Neither known-open oracle defect (fingerprints 2eb1768df88bfb07 and 1220f2840142aab8) is silently resolved by omission; each has an explicit disposition note if a cut touches its code path"
    - "The full local verification gate, including the real-Agda integration lane, is green after every batch this plan commits"
  artifacts:
    - path: "scripts/dogfood/run-id.mjs"
      provides: "The shared assertSafeRunId implementation, if that CUT-NN item was approved — created only if approved, otherwise this path does not exist"
  key_links:
    - from: "12-APPROVED-CUTS.md"
      to: "this plan's Task 1 and Task 2 filters"
      via: "category equals pipeline and decision equals approved"
      pattern: "pipeline"
---

<objective>
Execute every approved pipeline cut-list item from 12-APPROVED-CUTS.md, batched by deploy-relevance: non-deploy-relevant cuts (scripts/dogfood, and scripts/queue's dashboard, mirror-github, priority, and seed-initial-cargo files) land in one local batch with a plain push, while deploy-relevant cuts (scripts/oracle, scripts/queue/intake.mjs, scripts/team) land in a second batch that gets a watched redeploy per the Phase 10 D-10 push-cadence pattern. Every cut is cross-checked against the C-01 regression-lock exclusion list before any test/ file is touched, and the two known-open oracle defects are never silently masked.

Purpose: This is the back half's pipeline category, per D-01's cutting-emphasis decision and D-03's audit-then-signoff-then-execute shape. This plan executes ONLY what 12-07's checkpoint approved — it does not re-derive or second-guess the cut list.
Output: The approved pipeline cuts committed in two batches (non-deploy-relevant, then deploy-relevant with a watched redeploy), full local suite green after each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md
@.agents/skills/upstream-sync/SKILL.md

<interfaces>
Read every approved pipeline CUT-NN row's Fix approach field from 12-HEALTH-REPORT.md directly; this plan does not re-derive fix approaches, it executes the ones the audit already specified. The generic process for any scripts file deletion is Shared Pattern A from 12-PATTERNS.md: remove the file, grep every other scripts file for an import of its exports and update or remove that call site plus any now-false header-comment prose, find and update or delete the corresponding test/unit/tools file (do not assume a strict one-to-one naming match, check with find first), and remove a package.json scripts entry only if the deleted file is one of the top-level-wired utilities named there (none of the 25 pipeline scripts are). The specific process for the assertSafeRunId extraction, if approved, is 12-PATTERNS.md's Pattern 3 and Pattern 4: create scripts/dogfood/run-id.mjs exporting assertSafeRunId with the --run-id-flag-shaped message text, delete both CLIs' local copies, add the import to each CLI's existing sibling-import block, and create test/unit/tools/dogfood-run-id.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute approved non-deploy-relevant pipeline cuts</name>
  <files>scripts/dogfood/**, scripts/queue/dashboard.mjs, scripts/queue/mirror-github.mjs, scripts/queue/priority.mjs, scripts/queue/seed-initial-cargo.mjs, test/unit/tools/dogfood-*.test.ts, test/unit/tools/queue-*.test.ts, package.json</files>
  <read_first>
    - 12-APPROVED-CUTS.md's Normalized decisions table, filtered to category pipeline and decision approved.
    - 12-HEALTH-REPORT.md's Pipeline section for the exact Fix approach, Files, and Deploy-relevant fields of every approved row.
    - 12-HEALTH-REPORT.md's Regression-Lock Exclusion List section in full — cross-check every file this task is about to touch against it before touching any test/ file.
    - 12-PATTERNS.md Shared Pattern A (the script-deletion lockstep checklist) and Pattern 3/4 (the assertSafeRunId extraction, if that item is approved).
  </read_first>
  <action>
    For every row in 12-APPROVED-CUTS.md with category pipeline and decision approved whose Files fall under scripts/dogfood or under scripts/queue's dashboard, mirror-github, priority, or seed-initial-cargo files (the non-deploy-relevant set per 12-HEALTH-REPORT.md's Deploy-relevant field), execute that row's specific Fix approach following Shared Pattern A's checklist: remove the file if the approach is deletion, grep every other scripts file for a now-broken import and fix the call site plus any stale header-comment prose, find and update or delete the matching test/unit/tools file, and remove a package.json scripts entry only if applicable. If the assertSafeRunId extraction row is approved, execute it per Pattern 3 and Pattern 4 exactly as specified in this plan's interfaces block. Before touching any file under test/, confirm it does not appear in 12-HEALTH-REPORT.md's Regression-Lock Exclusion List; if it does, stop executing that specific sub-item, leave it untouched, and record the conflict explicitly in this plan's own summary output rather than overriding C-01. Once every approved non-deploy-relevant item in this batch is applied, run the full local verification gate: export the mise Node 24 path, then npm ci, npm run build, npx tsc -p tsconfig.test.json --noEmit, and RUN_AGDA_INTEGRATION=1 npx vitest run. Commit all of this task's changes together as one commit once the gate is green, then push to origin main.
  </action>
  <verify>
    <automated>RUN_AGDA_INTEGRATION=1 npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - RUN_AGDA_INTEGRATION=1 npx vitest run exits 0 and npx tsc -p tsconfig.test.json --noEmit exits 0, both run with the mise-managed Node 24 on PATH.
    - git log -1 --stat shows a single commit containing exactly the approved non-deploy-relevant files, pushed to origin main.
    - No file listed in 12-HEALTH-REPORT.md's Regression-Lock Exclusion List appears in that commit's diff.
    - Every approved pipeline CUT-NN item in the non-deploy-relevant set is either executed or explicitly recorded as blocked with a stated reason; none is silently skipped.
  </acceptance_criteria>
  <done>All approved non-deploy-relevant pipeline cuts are committed and pushed, with the full local gate green and zero regression-locked files touched.</done>
</task>

<task type="auto">
  <name>Task 2: Execute approved deploy-relevant pipeline cuts with a watched redeploy</name>
  <files>scripts/oracle/**, scripts/queue/intake.mjs, scripts/team/**, test/unit/tools/oracle-*.test.ts, test/unit/tools/queue-intake.test.ts, test/unit/tools/team-*.test.ts</files>
  <read_first>
    - 12-APPROVED-CUTS.md's Normalized decisions table, filtered to category pipeline and decision approved, this time restricted to scripts/oracle, scripts/queue/intake.mjs, and scripts/team files.
    - 12-HEALTH-REPORT.md's Pipeline section for these rows' exact Fix approach fields, and its Regression-Lock Exclusion List section, cross-checked the same way as Task 1.
    - test/fixtures/fix-queue.json entries with fingerprint 2eb1768df88bfb07 and fingerprint 1220f2840142aab8 in full — both currently triaged and unfixed; this task must not silently resolve either by omission.
    - .agents/skills/upstream-sync/SKILL.md Section 4, steps 6 through 8 in full (the exact watched-redeploy sequence: list the new deploy-ingest.yml run, watch it to exit status, then poll the healthz endpoint for HTTP 200; and the rollback procedure via git revert if the workflow fails or healthz never returns 200).
  </read_first>
  <action>
    For every row in 12-APPROVED-CUTS.md with category pipeline and decision approved whose Files fall under scripts/oracle, scripts/queue/intake.mjs, or scripts/team (the deploy-relevant set), execute that row's Fix approach following Shared Pattern A's checklist, the same discipline as Task 1. Before executing any cut whose Files overlap the exact function or file that fix-queue fingerprints 2eb1768df88bfb07 or 1220f2840142aab8 are filed against, re-read that fingerprint's notes field and confirm the approved row's Fix approach in 12-HEALTH-REPORT.md explicitly addresses the defect (either by fixing it properly as part of the cut, or by explicitly carrying the defect note forward unchanged); if the Fix approach does neither, stop, leave that sub-item unexecuted, and record the conflict in this plan's summary rather than silently deleting the buggy code path. Cross-check every file about to be touched against the Regression-Lock Exclusion List exactly as Task 1 does. Once every approved deploy-relevant item is applied, run the full local verification gate (same four commands as Task 1). Once green, commit all of this task's changes as one commit, push to origin main, then watch the deploy exactly per upstream-sync SKILL.md Section 4: list the new deploy-ingest.yml run for branch main, watch it to exit status, then poll the healthz endpoint for HTTP 200, retrying over several minutes since the rollout takes time. If the workflow fails or healthz never returns 200, follow SKILL.md's rollback procedure: revert the merge commit with the merge-parent flag, push the revert, and re-watch to green before finishing this task.
  </action>
  <verify>
    <automated>RUN_AGDA_INTEGRATION=1 npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - The full local gate is green before this task pushes anything.
    - The watched deploy-ingest.yml run exits with success status, and the healthz endpoint returns HTTP 200 after the deploy completes.
    - Neither fix-queue fingerprint 2eb1768df88bfb07 nor 1220f2840142aab8 is silently closed or has its defect-relevant code path deleted without an explicit disposition note in this task's summary explaining what happened to it.
    - If the deploy went red, a revert was pushed and re-watched to green before this task is considered done; main is never left broken.
  </acceptance_criteria>
  <done>All approved deploy-relevant pipeline cuts are committed, pushed, and watched to a green deploy; neither known-open oracle defect is silently masked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/team/archive-extract.mjs, scripts/team/issue-key.mjs (potentially modified by Task 2) | Layered tar-extraction defense and timingSafeEqual bearer-key comparison — RESEARCH.md Security Domain |
| The deploy-relevant push in Task 2 | Every push to origin main triggers a live cluster deploy (decision D-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-15 | Tampering | scripts/team/archive-extract.mjs, scripts/team/issue-key.mjs (if an approved cut touches either) | mitigate | Any approved cut touching these two files must pass their own dedicated tests (team-archive-extract.test.ts, team-issue-key.test.ts) explicitly, in addition to the aggregate full-suite gate, before this task commits |
| T-12-16 | Denial of Service (masked defect) | scripts/oracle/** code paths named in fix-queue fingerprints 2eb1768df88bfb07 and 1220f2840142aab8 | mitigate | Task 2's explicit pre-cut cross-check against both fingerprints' notes fields, with a stop-and-record rule if the approved Fix approach does not address the defect |
| T-12-SC | Supply chain (package installs) | N/A | accept | This plan installs zero new npm packages |
</threat_model>

<verification>
RUN_AGDA_INTEGRATION=1 npx vitest run and npx tsc -p tsconfig.test.json --noEmit both exit 0 after each task's batch. Task 2's push results in a green deploy-ingest.yml run and an HTTP 200 healthz response.
</verification>

<success_criteria>
Every approved pipeline CUT-NN item is executed exactly once, batched correctly by deploy-relevance, with the full local gate green after each batch and the deploy-relevant batch watched to a healthy deploy.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-08-SUMMARY.md when done
</output>
