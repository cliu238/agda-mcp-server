---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 11
type: execute
wave: 5
depends_on: ["12-08", "12-09", "12-10"]
files_modified:
  - docs/**
  - README.md
  - .agents/skills/agda-dogfooding/SKILL.md
  - .agents/skills/upstream-sync/SKILL.md
  - .planning/research/**
  - .gitignore
autonomous: true
requirements: [D-01, D-02]

must_haves:
  truths:
    - "Only CUT-NN items tagged category docs-residue with decision approved in 12-APPROVED-CUTS.md are executed; every other candidate is left untouched"
    - "No doc line already edited by Plan 12-09 for a tool deletion is double-edited or conflictingly re-edited by this plan"
    - "No file carrying a Regenerated-by or never-hand-edit marker is hand-edited directly by this plan"
    - "Phase 11's kept-intact artifacts show zero diff after this plan runs"
    - "A fresh scan of docs/*.md, README.md, and both SKILL.md files finds zero references to any tool this phase actually deleted (the set 12-BASELINE-TOOLS.txt minus the post-deletion manifest); references to never-built proposed tool names in historical docs are not in scope for this check"
  artifacts: []
  key_links:
    - from: "12-APPROVED-CUTS.md"
      to: "this plan's Task 1 filter"
      via: "category equals docs-residue and decision equals approved"
      pattern: "docs-residue"
    - from: "Plan 12-09's SUMMARY.md"
      to: "this plan's Task 1 double-edit avoidance check"
      via: "read before touching any README.md or docs/assistant-workflows.md line"
      pattern: "deleted tool"
---

<objective>
Execute every approved docs-and-planning-residue cut-list item from 12-APPROVED-CUTS.md, then perform a final full-doc-surface cross-reference against the post-deletion tool manifest to catch any stale reference the D-02 tool-deletion lockstep in Plan 12-09 might have missed. This plan runs after all three Wave 4 execution plans complete specifically so it has full knowledge of what actually got cut before doing its own doc consistency pass, and so it never double-edits a line Plan 12-09 already touched.

Purpose: This is the back half's docs-and-residue category, the phase's other D-01 cutting-emphasis target alongside the pipeline. It also closes RESEARCH.md's documented validation gap (no automated doc-reference check exists) with a manual final pass, per the phase's own Validation Architecture.
Output: The approved docs and planning-residue cuts committed; a final confirmation that zero stale tool references remain anywhere in the doc surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-BASELINE-TOOLS.txt
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-09-SUMMARY.md

<interfaces>
Read every approved docs-residue CUT-NN row's Fix approach and Generated-or-authored fields from 12-HEALTH-REPORT.md directly. Before editing any docs markdown file, re-check its first five lines for a Regenerated-by or never-hand-edit marker per Shared Pattern C in 12-PATTERNS.md; if present, retarget the edit to the generator script (coordinating with whether Plan 12-08 already handled that generator) instead of hand-editing the rendered file. Read Plan 12-09's SUMMARY.md before starting Task 1 to get the exact list of tool names and doc lines it already touched, so this plan's docs pass never conflicts with it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute approved docs and gitignore-hygiene cuts</name>
  <files>docs/**, README.md, .agents/skills/agda-dogfooding/SKILL.md, .agents/skills/upstream-sync/SKILL.md, .planning/research/**, .gitignore</files>
  <read_first>
    - 12-APPROVED-CUTS.md's Normalized decisions table, filtered to category docs-residue and decision approved.
    - 12-HEALTH-REPORT.md's Docs and Planning Residue section for each approved row's exact Fix approach and Generated-or-authored fields.
    - Plan 12-09's SUMMARY.md, to know exactly which README.md and docs/assistant-workflows.md lines it already edited for tool deletions.
    - A fresh git status --short run at execution time, to re-confirm whether the .planning/graphs and graphify-out gitignore-hygiene finding still applies.
    - 12-CONTEXT.md's explicit carve-out that Phase 11's artifacts under .planning/phases/11-auto-sync-productionization are not planning residue and must not be touched.
  </read_first>
  <action>
    For every row in 12-APPROVED-CUTS.md with category docs-residue and decision approved, execute that row's exact Fix approach from 12-HEALTH-REPORT.md. Before editing or deleting any docs markdown file, re-check its first five lines for a Regenerated-by marker; if present, retarget the edit to its generator script instead of the rendered file directly, and if that generator was already modified by Plan 12-08, only confirm the regenerated output stays consistent rather than re-editing it by hand. If the approved list includes the gitignore-hygiene finding for .planning/graphs and graphify-out, add the exact directory entries to .gitignore, and only delete the directories' existing content if the specific approved Fix approach says to do so (a gitignore-only fix and a delete-the-content fix are different actions; execute exactly what was approved). Before touching any line in README.md or docs/assistant-workflows.md, cross-check it against Plan 12-09's SUMMARY.md to confirm it was not already edited there for a tool deletion; skip any line already handled to avoid a conflicting double-edit. Do not touch any file under .planning/phases/11-auto-sync-productionization.
  </action>
  <verify>
    <automated>git diff --quiet HEAD -- .planning/phases/11-auto-sync-productionization && git diff --quiet HEAD -- docs/FIX-QUEUE-DASHBOARD.md && echo "phase11-and-generated-dashboard-untouched"</automated>
  </verify>
  <acceptance_criteria>
    - The automated check exits 0, proving this plan's working tree introduced zero changes to any file under .planning/phases/11-auto-sync-productionization and zero direct edits to the generated docs/FIX-QUEUE-DASHBOARD.md; a non-zero exit is a hard fail, not a manual read of the output.
    - A manual `git status --short` scoped to docs/, README.md, .agents/skills/, .gitignore, and .planning/research/ shows only files corresponding to approved CUT-NN rows (human cross-check against 12-APPROVED-CUTS.md; no automated proxy exists for "matches the approved list").
    - No file carrying a Regenerated-by marker (beyond the dashboard the automated check already guards) shows a direct hand-edit in its diff; only its generator script or its own consistency was touched.
    - Every approved docs-residue CUT-NN item is either fully executed or explicitly recorded as blocked with a stated reason; none is silently skipped.
  </acceptance_criteria>
  <done>All approved docs and planning-residue cuts are applied, generated docs are never hand-edited directly, and Phase 11's artifacts are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Final cross-reference of the doc surface against the post-deletion manifest</name>
  <files>docs/**, README.md, .agents/skills/agda-dogfooding/SKILL.md, .agents/skills/upstream-sync/SKILL.md</files>
  <read_first>
    - 12-BASELINE-TOOLS.txt (the pre-cut tool-name set captured by Plan 12-06 before any Wave-4 deletion).
    - The current, post-Plan-12-09 tool manifest (re-derive the live tool-name list the same way Plan 12-02's Task 1 did, by cross-referencing test/fixtures/e2e/mcp-tool-coverage.json's now-updated entries).
    - README.md, docs/assistant-workflows.md, and both SKILL.md files in their current, post-Task-1 state.
    - 12-AUDIT-DOCS.md's Task 1 method (the exact backtick-quoted agda_ name search pattern used during the audit), to repeat identically now.
  </read_first>
  <action>
    Compute the deleted-tool set as the names in 12-BASELINE-TOOLS.txt that are absent from the current, post-deletion test/fixtures/e2e/mcp-tool-coverage.json tool list — these are exactly the tools this phase removed. Scan README.md, every docs markdown file, and both SKILL.md files for references to any name in that deleted set. If any deleted-tool reference is found (a doc line that named a now-removed tool but was missed by Plan 12-09's manual doc pass), fix it directly in this task; this is corrective cleanup of an already-approved-and-executed deletion's ripple effect, not a new cut requiring separate sign-off, so no additional checkpoint is needed. Do not flag references to never-built proposed tool names (e.g. a name that appears in a historical release-triage doc but was never in 12-BASELINE-TOOLS.txt) — those are pre-existing documentation matters outside this deletion-ripple check, and belong to Plan 12-02's audit categorization if anywhere. Commit any fixes found as part of this task.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs"),cp=require("child_process");const base=new Set(fs.readFileSync(".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-BASELINE-TOOLS.txt","utf8").split("\n").map(s=>s.trim()).filter(Boolean));const cur=new Set(JSON.parse(fs.readFileSync("test/fixtures/e2e/mcp-tool-coverage.json","utf8")).flatMap(e=>Object.values(e)).filter(v=>typeof v==="string"&&v.startsWith("agda_")));const deleted=[...base].filter(t=>!cur.has(t));if(!deleted.length){console.log("no tools deleted this phase; doc-ref check vacuously clean");process.exit(0)}const docs=cp.execSync("grep -rhoE \x27agda_[a-z_]+\x27 docs README.md .agents/skills/agda-dogfooding/SKILL.md .agents/skills/upstream-sync/SKILL.md || true").toString();const bad=deleted.filter(t=>new RegExp("(^|[^a-z_])"+t+"([^a-z_]|$)").test(docs));if(bad.length){console.error("STALE refs to DELETED tools: "+bad.join(", "));process.exit(1)}console.log("no stale refs to deleted tools ("+deleted.length+" deleted)")'</automated>
  </verify>
  <acceptance_criteria>
    - The command exits 0: no reference to any deleted tool (12-BASELINE-TOOLS.txt minus the current coverage-matrix tool set) remains in docs/, README.md, or either SKILL.md. A surviving reference is printed and forces a non-zero exit — pass/fail is mechanical, not a human read.
    - When this phase deleted zero tools, the command reports the vacuously-clean case and exits 0 (there is no deletion ripple to check).
    - Any deleted-tool reference the command surfaces is fixed and the command re-run to exit 0 before this task is considered done.
  </acceptance_criteria>
  <done>The full doc surface is confirmed free of stale tool references after all Wave 4 deletions have landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc edits versus Plan 12-09's already-committed tool-deletion doc edits | A conflicting double-edit could reintroduce a stale reference or corrupt a line Plan 12-09 already fixed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-21 | Tampering | Generated docs (docs/FIX-QUEUE-DASHBOARD.md and any other Regenerated-by-marked file) | mitigate | Task 1's mandatory marker check before any docs markdown edit, redirecting to the generator script instead of a direct hand-edit |
| T-12-22 | Information Disclosure | None new | accept | Pure documentation editing; no secrets or tokens are involved |
</threat_model>

<verification>
git status confined to approved docs-residue files only. The final agda_ reference scan across the full doc surface returns zero stale matches. Phase 11's artifacts show zero diff.
</verification>

<success_criteria>
Every approved docs and planning-residue cut is executed without touching Phase 11's protected artifacts or hand-editing a generated doc, and the full doc surface is confirmed free of any stale tool reference after all tool deletions have landed.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-11-SUMMARY.md when done
</output>
