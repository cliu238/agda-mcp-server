---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-PIPELINE.md
autonomous: true
requirements: [C-01, C-02, C-05, D-01]

must_haves:
  truths:
    - "Every one of CONCERNS.md's pipeline-scoped known-debt findings (assertSafeRunId duplication, 3 named oracle-tooling defects) has a 2026-07-06-or-later re-verification status, not a re-cited 2026-07-04 status"
    - "Every one of the 25 scripts/{dogfood,oracle,queue,team}/*.mjs files has a recorded has-test / deploy-relevant / Loop-②-stage classification"
    - "No cut-list row proposes removing an entire Loop ② stage (oracle judging, queue filing, team ingest) — only redundant implementation detail within a stage (C-02)"
  artifacts:
    - path: ".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-PIPELINE.md"
      provides: "Re-verified pipeline known-debt status, a 25-row script inventory table, and severity-graded pipeline cut-list candidates"
      contains: "## Cut-List Candidates"
  key_links:
    - from: "12-AUDIT-PIPELINE.md cut-list rows"
      to: "12-06's consolidated 12-HEALTH-REPORT.md"
      via: "Plan 12-06 Task 1 transcribes this doc's rows verbatim into the Pipeline section"
      pattern: "Pipeline \\(scripts/\\)"
---

<objective>
Audit the unpublished Loop ② pipeline (`scripts/{dogfood,oracle,queue,team}/`, 25 files) for redundant implementation detail worth cutting, per D-01's cutting-emphasis decision. Re-verify every pipeline-scoped known-debt finding from `CONCERNS.md` against current `HEAD` rather than re-citing stale 2026-07-04 measurements (RESEARCH.md Pattern 1), inventory all 25 scripts for test coverage / deploy-relevance / Loop-②-stage status, and draft severity-graded cut-list rows. This plan does not delete or modify any pipeline file — it is read-only, producing exactly one new report artifact for Plan 12-06 to consolidate.

Purpose: C-05 requires concrete cut targets to come from the audit, not from CONTEXT.md's discussion — this plan IS that audit for the pipeline category. C-02 requires that no Loop ② stage itself gets proposed for removal, only redundant implementation within a stage; this plan's classification step makes that boundary explicit and checkable before anything is ever proposed for a cut.
Output: `.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-PIPELINE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- This plan's sole output feeds Plan 12-06 verbatim. Use this exact per-row
     shape (from 12-PATTERNS.md Pattern 1) so no re-formatting is needed downstream. -->

Cut-list row shape (one per candidate, under a `## Cut-List Candidates` heading):
```markdown
- **Issue:** <one or two sentences: what is redundant/duplicated/orphaned and why>
  - Files: <exact path(s)>
  - Impact: <what breaks if cut — be specific: "none, zero importers" / "breaks X's Y call site unless updated">
  - Fix approach: <extract to shared module / delete outright / merge into Z — cite Shared Pattern A or Pattern 3/4 from 12-PATTERNS.md by name>
  - Severity: <critical|high|medium|low — define each tier once in this doc's header; no repo-wide convention exists (12-PATTERNS.md "No Analog Found")>
  - Deploy-relevant: <yes|no, per RESEARCH.md's Deployment & Runtime Surface table>
  - Loop②-stage risk: <none|flag — "none" means this is implementation detail within a stage, never the stage itself, per C-02>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify pipeline-scoped known debt against HEAD</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-PIPELINE.md</files>
  <read_first>
    - `.planning/codebase/CONCERNS.md` lines 25-31 (assertSafeRunId duplication finding) and lines 33-50 (three named oracle-tooling defects: `.agda-lib` re-materialization gap, unconditional cold-replay hole-scan, postulate-block multi-line mis-split in `orcl-02-soundness-scan.mjs`).
    - `12-RESEARCH.md` "Pattern 1: Re-verify known debt before treating it as a fresh finding" (methodology) and its worked file-size example (the exact re-verify-don't-recite discipline to apply here).
    - `12-PATTERNS.md` Pattern 3 in full (the exact current byte-diff between `dogfood-run.mjs:101-116` and `dogfood-wrapup.mjs:425-440`'s `assertSafeRunId` copies — message-text drift, identical logic) and the "Calibration note for the audit" at the bottom of `12-PATTERNS.md` (flags two claims needing re-verification: "every `scripts/*.mjs` is unit-tested" and the assertSafeRunId byte-identity claim).
    - `test/fixtures/fix-queue.json` entries with fingerprint `2eb1768df88bfb07` (note this entry's own text contains a 2026-07-04 CORRECTION narrowing its affected-file scope — read fingerprint `fb57abbe7df6dfe8`'s notes too, which documents that correction) and `1220f2840142aab8` — both currently `triaged`, both must stay open/visible in this report, never silently marked resolved without a fresh re-check.
    - `scripts/dogfood/dogfood-run.mjs` lines 90-160 and `scripts/dogfood/dogfood-wrapup.mjs` lines 420-470 (the current `assertSafeRunId` bodies to diff against Pattern 3's quoted excerpt).
  </read_first>
  <action>
    Create `12-AUDIT-PIPELINE.md` with a `## Known Pipeline Debt Re-Verification` section. For each of the five findings named above (assertSafeRunId duplication, the three named oracle defects, and the two open fix-queue fingerprints), re-run the specific check the finding was based on against current `HEAD` — re-read the exact file/line range, not the prose description — and record one row per finding: finding name, original source (CONCERNS.md line number, or fix-queue fingerprint), re-verification date (today), and status (`confirmed-still-present` / `already-resolved` / `severity-changed`, with the specific evidence). Explicitly resolve `12-PATTERNS.md`'s own calibration note: run `find test -iname "*install-dogfood-skill*"` and record whether `test/unit/tools/dogfood-install-skill.test.ts` exists (this session's own prior check found it does — confirm and correct the calibration note's claim rather than propagating it). State the assertSafeRunId diff exactly (both copies' current condition body and error-message text) so Task 2 does not need to re-derive it.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-PIPELINE.md && grep -c "Known Pipeline Debt Re-Verification" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-PIPELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `12-AUDIT-PIPELINE.md` exists with a `## Known Pipeline Debt Re-Verification` section containing exactly 5 re-verified findings (assertSafeRunId + 3 oracle defects + a combined 2eb1768df88bfb07/1220f2840142aab8 row), each with an explicit re-verification date of today and a status value from the 3-value enum above.
    - The calibration-note correction is explicit: the doc states plainly whether `test/unit/tools/dogfood-install-skill.test.ts` exists (yes/no) rather than repeating `12-PATTERNS.md`'s unverified claim.
    - `grep -c "confirmed-still-present\|already-resolved\|severity-changed" 12-AUDIT-PIPELINE.md` is at least 5.
  </acceptance_criteria>
  <done>All five known pipeline findings have a dated, HEAD-based re-verification status; the install-dogfood-skill.mjs test-coverage claim is corrected if wrong.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory 25 pipeline scripts, classify, draft cut-list rows</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-PIPELINE.md</files>
  <read_first>
    - `12-RESEARCH.md` "Deployment & Runtime Surface (cut-relevance map)" table in full (which `scripts/` subtrees are deploy-relevant) and "Architectural Responsibility Map" row for "Loop ② pipeline."
    - `12-RESEARCH.md` "Package Legitimacy Audit" and "Environment Availability" table's `npx knip` row (explicitly documents knip as skippable at this repo's size).
    - `12-PATTERNS.md` "Shared Patterns" section A (script deletion lockstep checklist, real precedent commit `a88e369`) and C (generated-doc marker convention, relevant to `scripts/queue/dashboard.mjs` as `docs/FIX-QUEUE-DASHBOARD.md`'s generator).
    - `package.json` `scripts` block (confirms which top-level `scripts/*.mjs` utilities — none of the 25 pipeline scripts — are wired there).
    - This session's own confirmed finding: `scripts/queue/seed-initial-cargo.mjs` has zero dedicated test file under `test/` (`find test -iname "*seed-initial-cargo*"` returns nothing) and its own header/`.planning/codebase/STRUCTURE.md` describe it as a "one-off historical seeding script" — verify this still holds and treat it as a candidate to evaluate, not a pre-decided cut.
  </read_first>
  <action>
    Build a 25-row inventory table (one row per `scripts/{dogfood,oracle,queue,team}/*.mjs` file) with columns: script path, has-dedicated-test (`test/unit/tools/<prefix>-*.test.ts`, cross-referenced by direct `find` — do not assume the `<prefix>-<basename>` naming convention holds without checking), deploy-relevant (per RESEARCH's table: yes for `oracle/**`, `queue/intake.mjs`, `team/**`; no otherwise), Loop②-stage (oracle=judge, queue=file, team=ingest/lock; dogfood=capture-adjacent tooling, not itself a required stage). This plan does NOT run `npx knip` — RESEARCH.md's own Environment Availability table confirms the manual/inventory methodology above is sufficient at this repo's size and knip is explicitly skippable; knip's package identity is tagged `[ASSUMED]` in RESEARCH's Package Legitimacy Audit, and running it would require a `checkpoint:human-verify` gate this autonomous plan does not have — treat this as an explicit, justified exclusion, not an oversight. From the inventory plus Task 1's re-verified findings, draft one `## Cut-List Candidates` row (using this plan's `<interfaces>` shape exactly) per genuine candidate — at minimum evaluate: the assertSafeRunId duplication (Fix approach: extract to `scripts/dogfood/run-id.mjs` per Pattern 3/4, keeping the `--run-id`-flag-shaped message text since PATTERNS.md judges it more informative), and `scripts/queue/seed-initial-cargo.mjs` (a one-off already-run seed script with zero test coverage — evaluate genuinely, do not pre-conclude either way). For every candidate touching `scripts/oracle/**`, `scripts/queue/intake.mjs`, or `scripts/team/**`, explicitly state in the row's Loop②-stage field why the cut preserves the stage (implementation-only) per C-02 — if a candidate cannot be framed that way, exclude it and say why rather than silently dropping it. Define the severity scale (critical/high/medium/low, one sentence each) once in this doc's header, since no existing repo convention provides one (`12-PATTERNS.md` "No Analog Found").
  </action>
  <verify>
    <automated>grep -c "^| scripts/" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-PIPELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - The inventory table has exactly 25 script rows (matches the confirmed `scripts/{dogfood,oracle,queue,team}/*.mjs` file count).
    - A severity-scale definition (4 tiers, one sentence each) appears once in the doc header.
    - `## Cut-List Candidates` has at least 2 rows (assertSafeRunId + the seed-script evaluation, whichever direction each concludes), each using every field from this plan's `<interfaces>` row shape with no field left blank.
    - Every candidate row touching `oracle/`, `queue/intake.mjs`, or `team/` states an explicit C-02 stage-preservation justification.
  </acceptance_criteria>
  <done>25/25 scripts classified; cut-list candidates drafted in the exact consumable shape Plan 12-06 needs; knip's exclusion is explicitly justified, not silently skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This audit's classification -> the eventual cut-list | A mis-classified "redundant" row could later get approved and executed against a security-load-bearing file |
| `scripts/team/archive-extract.mjs`, `scripts/team/issue-key.mjs` (read, not modified, by this plan) | Layered tar-extraction defense (zip-slip/decompression-bomb) and `timingSafeEqual` bearer-key comparison — RESEARCH.md Security Domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `scripts/team/archive-extract.mjs`, `scripts/team/issue-key.mjs` (audit-time classification only) | mitigate | Task 2's inventory records these as deploy-relevant + Loop②-stage=team; any cut-list row touching them must be evaluated against RESEARCH.md's Security Domain table before inclusion — deferred concretely to Plan 12-08's execution-time re-check, which re-runs `team-archive-extract.test.ts`/`team-issue-key.test.ts` explicitly |
| T-12-02 | Information Disclosure | This plan's own report output | accept | Read-only audit; no secrets/tokens are read or transcribed (pipeline scripts are inspected for structure/test-coverage only, not run) |
| T-12-SC | Supply chain (package installs) | `npx knip` (considered, explicitly not used) | accept | This plan does not invoke `npx knip` — its package identity is `[ASSUMED]` per RESEARCH's Package Legitimacy Audit and would require a `checkpoint:human-verify` gate this autonomous plan does not carry; the manual inventory method is used instead |
</threat_model>

<verification>
`12-AUDIT-PIPELINE.md` exists, contains both required sections (`## Known Pipeline Debt Re-Verification` with 5 dated rows, `## Cut-List Candidates` with >=2 fully-specified rows), and the 25-row script inventory table. No file under `scripts/`, `src/`, `docs/`, or `test/` is modified by this plan.
</verification>

<success_criteria>
Pipeline known-debt is re-verified against HEAD (not re-cited stale), all 25 pipeline scripts are classified by test-coverage/deploy-relevance/Loop②-stage, and cut-list candidates are drafted in the exact shape Plan 12-06 consumes — with zero proposal to remove an entire Loop ② stage.
</success_criteria>

<output>
Create `.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-01-SUMMARY.md` when done
</output>
