---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-LOCKS-RT6RT7.md
autonomous: true
requirements: [C-01, D-04]

must_haves:
  truths:
    - "Every fix-queue.json entry with status locked, plus both capture-regression-matrix.json entries, appears in the exclusion list with its specific test file or test-case name named and confirmed to exist on disk"
    - "RT6 (fingerprint ad2b6d31f58f1759) and RT7 (fingerprint b6821f42952c6ff8) each have an independent, definitive do-it, do-not, or how verdict with a HEAD re-reproduction check, and neither verdict includes any implementation"
  artifacts:
    - path: ".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-LOCKS-RT6RT7.md"
      provides: "The complete C-01 regression-lock exclusion list and the RT6/RT7 definitive re-evaluation verdicts"
      contains: "Regression-Lock Exclusion List"
  key_links:
    - from: "12-AUDIT-LOCKS-RT6RT7.md exclusion list"
      to: "every back-half execution plan (12-08 through 12-11)"
      via: "each execution task's read_first requires cross-checking touched files against this list before any test/ modification"
      pattern: "Regression-Lock Exclusion List"
    - from: "12-AUDIT-LOCKS-RT6RT7.md RT6/RT7 verdicts"
      to: "12-06 consolidated health report"
      via: "Plan 12-06 Task 2 transcribes both verdicts verbatim and records them into fix-queue.json's notes fields"
      pattern: "RT6 Verdict|RT7 Verdict"
---

<objective>
Build the definitive C-01 regression-lock exclusion list (the set of test files and named test cases that must never be deleted or weakened by any cut in this phase) and produce independent, definitive re-evaluation verdicts for RT6 and RT7 per decision D-04. The exclusion list is built from every locked fix-queue.json entry's notes field plus the two capture-regression-matrix.json entries, not from the matrix file alone, since RESEARCH.md's Pitfall 5 confirms the matrix under-counts. This plan is read-only and produces no implementation for RT6 or RT7 — only a verdict.

Purpose: This is the single foundational precondition every other plan in this phase depends on for its own C-01 safety check. It must exist before any back-half execution plan touches a test/ file. D-04 requires RT6 and RT7's re-evaluation trigger (fired at Phase 10's completion) to receive a definitive verdict this phase, with implementation explicitly deferred to a future phase.
Output: 12-AUDIT-LOCKS-RT6RT7.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md
@docs/LOAD-TERMINUS-ADJUDICATION.md
@test/fixtures/fix-queue.json
@test/fixtures/capture-regression-matrix.json

<interfaces>
Exclusion-list row shape, one row per locked fix-queue.json entry plus one row per capture-regression-matrix.json entry: fingerprint or matrix id, the specific test file path or named vitest test-case string quoted from the notes field, an existence-check result (found on disk, yes or no, with the exact path used to confirm), and a one-line description of what the lock proves.

RT6/RT7 verdict shape, one independent section per item: fingerprint, current status (must remain triaged, this plan does not change status), verdict of do-it, do-not, or how (if how, a target-shape sketch sufficient for a future phase to plan from, with no implementation), a HEAD re-reproduction check result with the specific evidence checked, and a citation into docs/LOAD-TERMINUS-ADJUDICATION.md's relevant Phase 10 decision.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the C-01 regression-lock exclusion list</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-LOCKS-RT6RT7.md</files>
  <read_first>
    - 12-RESEARCH.md Pitfall 5 in full (the matrix file alone under-counts; locked fixes are regression-locked as individually named vitest test cases documented only in fix-queue.json's free-text notes field, using inconsistent phrasing of Regression lock colon in most entries and Regression evidence colon in at least fingerprint 0bc76d15c2fec8df) and its paired Code Examples "Regression-lock extraction from fix-queue.json prose" Python snippet.
    - test/fixtures/fix-queue.json in full — every entry with status locked (ten entries at planning time: e6f0c1169032b9d5, 5abecc959e43fef3, bfcba437f5426fd6, eb7439cb3ed9d6b9, fdc90bfde12fb938, e5f6de1fa365b887, eaea6321183bdf7b, 004d161b839ce725, 3306edf4c2d01c53, 0bc76d15c2fec8df — re-confirm this count and these fingerprints are still current, do not assume the list is frozen) — read every one's full notes field, not a summary.
    - test/fixtures/capture-regression-matrix.json in full (two entries at planning time: issue-64-61-transitive-staleness and guard-no-metas-clean-load-under-fault-injection, both mapping to fix-queue fingerprint e6f0c1169032b9d5's Backfilled note).
  </read_first>
  <action>
    Run the regression-lock extraction Python snippet from RESEARCH.md's Code Examples verbatim against test/fixtures/fix-queue.json, extending its regex to also match the Regression evidence colon phrasing (confirmed present in fingerprint 0bc76d15c2fec8df's notes), then manually read every locked entry's full notes field end to end to catch anything the regex pattern misses or mis-parses. For every locked entry, extract the specific test file path and, where named, the exact test-case description strings the notes cite as this fix's regression lock. Cross-reference every extracted path against the real test tree with a direct existence check, recording found or not-found explicitly rather than assuming the notes are accurate. Add the two capture-regression-matrix.json entries as their own two rows, cross-referenced to fingerprint e6f0c1169032b9d5. Create 12-AUDIT-LOCKS-RT6RT7.md with a Regression-Lock Exclusion List section containing one row per locked fix-queue entry plus the two matrix entries, using this plan's interfaces row shape.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-LOCKS-RT6RT7.md && grep -c "Regression-Lock Exclusion List" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-LOCKS-RT6RT7.md</automated>
  </verify>
  <acceptance_criteria>
    - The exclusion list has exactly one row per current locked-status fix-queue.json entry plus exactly two rows for the capture-regression-matrix.json entries, with the current locked-entry count stated explicitly (confirm it against a fresh read, not assumed to be ten).
    - Every row's named test file path has an explicit existence-check result; any not-found result is flagged prominently, not silently included as if verified.
    - The doc states explicitly that both the Regression lock colon and Regression evidence colon phrasings were searched, and that every locked entry's notes field was read in full, not sampled.
  </acceptance_criteria>
  <done>The exclusion list is complete, existence-checked, and built from a full manual read of every locked entry's notes plus the matrix file, not from the matrix file or the regex alone.</done>
</task>

<task type="auto">
  <name>Task 2: RT6 and RT7 definitive re-evaluation verdicts</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-LOCKS-RT6RT7.md</files>
  <read_first>
    - test/fixtures/fix-queue.json entries with fingerprint ad2b6d31f58f1759 (RT6, agda_load's response schema conflates five distinct load states) and b6821f42952c6ff8 (RT7, no timeout or process-state taxonomy on command timeout) — read both notes fields in full.
    - docs/LOAD-TERMINUS-ADJUDICATION.md in full — Phase 10's recorded decision that upstream's whole-file load-terminus architecture (src/session/agda-transport.ts, src/session/command-completion.ts, src/agda/session-load-impl.ts) was adopted for completion-signal detection, fatal-stderr handling, and inactivity timeout.
    - 12-CONTEXT.md's exact D-04 wording (the re-evaluation trigger was after the merge lands, and that trigger fired at Phase 10's completion; the verdict must be definitive but implementation is explicitly a separate future phase).
    - src/agda/session-load-impl.ts and src/session/command-completion.ts current state (the two files RT6 and RT7 concern), and src/protocol/responses/load-display.ts (the response shape RT6's conflation concerns).
  </read_first>
  <action>
    Read RT6's and RT7's full fix-queue notes fields. Cross-reference docs/LOAD-TERMINUS-ADJUDICATION.md's recorded Phase 10 decision to determine, for each item independently, whether the original conflation or taxonomy problem still reproduces against current HEAD unchanged, has changed shape because of the adopted upstream architecture, or was incidentally resolved. Produce one independent verdict section per item using this plan's interfaces shape: a do-it, do-not, or how conclusion (if how, sketch the target response-shape change at a level of detail sufficient for a future phase to plan from, explicitly without writing any implementation code or modifying any source file), a HEAD re-reproduction check citing the specific current code path read, and a severity or urgency assessment now that Phase 10 has landed. Do not modify test/fixtures/fix-queue.json in this task — recording the verdict there is Plan 12-06's Task 2 responsibility, not this plan's.
  </action>
  <verify>
    <automated>grep -c "RT6 Verdict\|RT7 Verdict" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-LOCKS-RT6RT7.md</automated>
  </verify>
  <acceptance_criteria>
    - The document contains two independently headed sections, one for RT6 and one for RT7, each with an explicit do-it, do-not, or how conclusion stated as a single clear sentence.
    - Each section cites a specific HEAD re-reproduction check (the exact file and code path read) and a specific citation into docs/LOAD-TERMINUS-ADJUDICATION.md.
    - Neither section contains an implementation, a code diff, or a modification to any source file; test/fixtures/fix-queue.json is unchanged by this plan.
  </acceptance_criteria>
  <done>RT6 and RT7 each have an independent, definitive, HEAD-grounded verdict with implementation explicitly out of scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The exclusion list this plan builds versus every later cut in this phase | An incomplete exclusion list would let a later plan legally delete or weaken a regression-locked test without realizing it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering | The regression-lock exclusion list itself | mitigate | Dual-method construction (regex extraction plus a full manual read of every locked entry's notes) with an explicit existence check against the real test tree for every named path, per RESEARCH.md's Pitfall 5 |
| T-12-10 | Repudiation | RT6/RT7 verdict provenance | mitigate | Every verdict cites a specific, re-run HEAD check rather than trusting the original 2026-07 fix-queue filing text as still accurate |
</threat_model>

<verification>
12-AUDIT-LOCKS-RT6RT7.md exists with both required sections (Regression-Lock Exclusion List with an existence-checked row per locked entry and matrix entry, and independent RT6/RT7 Verdict sections). No file under test/, src/, or fix-queue.json is modified by this plan.
</verification>

<success_criteria>
The C-01 exclusion list is complete and existence-verified, ready for every back-half execution plan to consult; RT6 and RT7 each have a definitive, HEAD-grounded verdict with zero implementation.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-05-SUMMARY.md when done
</output>
