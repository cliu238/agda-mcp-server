---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-DOCS.md
autonomous: true
requirements: [C-05, D-01]

must_haves:
  truths:
    - "Every docs/*.md, README.md, and .agents/skills/*/SKILL.md file has a generated-vs-authored classification and a stale-tool-reference scan result"
    - "Phase 11's 5 plans plus its RESEARCH/PATTERNS/VALIDATION/CONTEXT/DISCUSSION-LOG artifacts are explicitly confirmed NOT planning residue, with a citation to CONTEXT.md's carve-out"
    - "The VALIDATION.md-missing debt across 4+ phases in both milestone audits is explicitly resolved as out-of-cut-list-scope (a completion gap, not a simplification target), not silently omitted"
  artifacts:
    - path: ".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-DOCS.md"
      provides: "Doc generated-versus-authored classification, stale-reference scan, planning-residue scan, and cut-list candidates for the docs-and-residue category"
      contains: "Cut-List Candidates"
  key_links:
    - from: "12-AUDIT-DOCS.md cut-list rows"
      to: "12-06 consolidated health report"
      via: "Plan 12-06 Task 1 transcribes this doc's rows into the Docs and Planning Residue section"
      pattern: "Docs and Planning Residue"
---

<objective>
Audit the documentation surface (docs markdown files, README.md, the two SKILL.md files) and the .planning residue for cut candidates, per decision D-01's cutting-emphasis choice. Cross-reference every doc file against the live tool manifest for stale agda_* references, classify generated-versus-authored docs, and scan .planning for genuinely stale planning artifacts, while explicitly protecting Phase 11's kept-intact artifacts and correctly routing the VALIDATION.md-missing debt (a completion gap, not a cut target) out of scope. This plan is read-only, producing exactly one new report artifact for Plan 12-06 to consolidate.

Purpose: RESEARCH.md's own Validation Architecture section confirms no automated check exists for doc-reference staleness outside src/. This plan is the one-time manual substitute the phase needs before any doc gets cut. Decision C-05 requires concrete targets to come from this audit, not from guesses.
Output: 12-AUDIT-DOCS.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md

<interfaces>
Reuse the same cut-list row shape Plan 12-01 uses (sourced from 12-PATTERNS.md Pattern 1), so Plan 12-06 needs no reformatting. Each row records: Issue (one or two sentences on what is stale, generated-but-hand-edited, or superseded, and why), Files (exact paths), Impact (what breaks if cut), Fix approach (specific action), Severity (critical, high, medium, or low, using the same tier names Plan 12-01 defines), and Generated-or-authored (generated docs are never hand-edited directly; retarget the generator script instead).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doc surface cross-reference and generated-authored classification</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-DOCS.md</files>
  <read_first>
    - README.md (full, 104 lines) and docs/assistant-workflows.md (full, about 215 lines).
    - docs/FIX-QUEUE-DASHBOARD.md (first 10 lines), docs/DEPLOY-OPERATIONS.md, docs/TEAM-ONBOARDING.md, docs/extensions.md, docs/literate-agda-assessment.md, docs/release-0.7.0-triage.md, docs/LOAD-TERMINUS-ADJUDICATION.md, docs/team-intro.html.
    - .agents/skills/agda-dogfooding/SKILL.md and .agents/skills/upstream-sync/SKILL.md in full, re-scanned specifically for agda_ prefixed tool-name references this time.
    - test/unit/tools/no-dead-tool-references.test.ts lines 63 through 107 for the exact backtick-quoted agda_name regex and offender-collection approach, to replicate by hand against docs instead of src.
    - test/fixtures/e2e/mcp-tool-coverage.json, the current 74-tool-name source of truth to cross-check every found reference against (this file equals the live manifest per the mechanically enforced mcp-e2e-coverage.test.ts).
    - 12-PATTERNS.md Pattern 2 (the optional doc-invariant-test analog, read for context only since this task does a one-time manual equivalent, not the durable test) and the Shared Patterns section item C (generated-doc marker convention).
  </read_first>
  <action>
    Create 12-AUDIT-DOCS.md with a Doc Surface Classification section. For every file in the read_first list, search for backtick-quoted agda_ prefixed identifiers and cross-check each match against mcp-tool-coverage.json's tool-name list; record any match not present in that list as a stale reference, citing the exact file and line and the exact string found. For every docs markdown file, read its first five lines and classify it as generated (carries a Regenerated-by or never-hand-edit marker per Shared Pattern C; confirm docs/FIX-QUEUE-DASHBOARD.md is the one known generated doc) or authored. Build a table with columns: file, generated-or-authored, stale-reference-count, notes. State explicitly, do not omit, that zero stale references found is a valid and reportable outcome for a file.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-DOCS.md</automated>
  </verify>
  <acceptance_criteria>
    - 12-AUDIT-DOCS.md exists with a Doc Surface Classification section covering all ten docs files, README.md, and both SKILL.md files (twelve rows total).
    - Every row has an explicit stale-reference count (a number, including zero), not a vague description.
    - Every docs markdown file is explicitly tagged generated or authored, with docs/FIX-QUEUE-DASHBOARD.md tagged generated.
  </acceptance_criteria>
  <done>All twelve doc-surface files are classified with a stale-reference count and a generated-or-authored tag.</done>
</task>

<task type="auto">
  <name>Task 2: Planning-residue scan and cut-list candidates</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-DOCS.md</files>
  <read_first>
    - Top-level listing of .planning (codebase, milestones, phases, quick, research directories plus the loose top-level files) and the twelve files under .planning/research.
    - 12-RESEARCH.md Open Question 2 (whether .claude/skills/agda-mcp-k8s-deploy should be promoted, with its explicit recommendation to flag as an informational note, not a cut-list row) and Open Question 3 (the VALIDATION.md-missing debt direction question).
    - 12-CONTEXT.md's explicit carve-out: Phase 11's five plans plus its RESEARCH, PATTERNS, VALIDATION, CONTEXT, and DISCUSSION-LOG artifacts are kept intact for zero-rework revival and are NOT planning residue.
    - A fresh run of git status --short at execution time (the planning-time snapshot found .planning/graphs and graphify-out both untracked and both absent from .gitignore, roughly ten megabytes combined; re-confirm this still holds rather than trusting this stale note).
  </read_first>
  <action>
    Enumerate the twelve files under .planning/research and determine which are milestone-scoped and safely supersede-able (for example CHG-REVERIFY.md, RT-REVERIFY.md, FUEL-CORPORA.md) versus still load-bearing (UPSTREAM-SYNC.md is cited as a canonical constraint carrier by Phase 10 and by this phase's own CONTEXT.md; do not flag it). Run git status --short fresh and confirm whether .planning/graphs and graphify-out remain untracked and absent from .gitignore; if so, record this as its own cut-list row with Fix approach being a .gitignore addition, not a content deletion, and tag it distinctly since it is a gitignore-hygiene fix rather than a feature or doc cut. Resolve the VALIDATION.md-missing debt direction explicitly: state in this doc that backfilling missing VALIDATION.md files is an addition, not a subtraction, and therefore stays out of this phase's cut list unless a specific instance is itself found to be over-engineered (flag only if such a specific instance is actually found, otherwise state plainly that none was found). Add the one-line informational note about .claude/skills/agda-mcp-k8s-deploy's promotion question per Open Question 2's recommendation, explicitly marked as informational only and not a cut-list row requiring sign-off. Explicitly confirm in writing that Phase 11's artifacts were checked and are excluded from every cut-list row in this document. Draft a Cut-List Candidates section (same row shape as Task 1 uses) with one row per genuine planning-residue or doc candidate found; zero rows is an acceptable, explicitly stated outcome.
  </action>
  <verify>
    <automated>grep -c "Cut-List Candidates" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-DOCS.md</automated>
  </verify>
  <acceptance_criteria>
    - The document contains an explicit sentence confirming Phase 11's artifacts were checked and excluded from the cut list, citing CONTEXT.md.
    - The document contains an explicit VALIDATION.md-missing resolution paragraph stating the out-of-scope conclusion (or naming a specific found instance, if any).
    - The document records the current, freshly-run git status result for .planning/graphs and graphify-out, not the stale planning-time snapshot.
    - The document contains the one-line informational note about agda-mcp-k8s-deploy promotion, explicitly marked as informational, not a cut-list row.
  </acceptance_criteria>
  <done>Planning residue is scanned with Phase 11 explicitly protected, the VALIDATION.md-missing question is explicitly resolved out of scope, the gitignore-hygiene finding is freshly re-verified, and the skill-promotion question is surfaced as an informational note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This audit's residue classification versus Phase 11's protected artifacts | Mis-classifying a protected artifact as residue would risk an accidental deletion downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | Phase 11 artifacts under .planning/phases/11-auto-sync-productionization | mitigate | Task 2 requires an explicit written confirmation that these files were checked and excluded from every cut-list row, citing CONTEXT.md's carve-out directly |
| T-12-04 | Information Disclosure | This plan's own report output | accept | Read-only documentation review; no secrets or tokens are present in any file this plan reads |
</threat_model>

<verification>
12-AUDIT-DOCS.md exists with both required sections (Doc Surface Classification covering twelve files, and Cut-List Candidates), the Phase 11 protection statement, and the VALIDATION.md-missing resolution. No file under docs/, README.md, .agents/, or .planning/ other than this new report is modified by this plan.
</verification>

<success_criteria>
The full doc surface is classified for staleness and generated-versus-authored status, planning residue is scanned with Phase 11 explicitly protected, and cut-list candidates are drafted in the shape Plan 12-06 consumes.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-02-SUMMARY.md when done
</output>
