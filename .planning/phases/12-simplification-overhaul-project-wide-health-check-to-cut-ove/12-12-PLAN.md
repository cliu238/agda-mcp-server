---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 12
type: execute
wave: 6
depends_on: ["12-11"]
files_modified:
  - .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md
autonomous: true
requirements: [D-03, C-01]

must_haves:
  truths:
    - "12-HEALTH-REPORT.md contains an After Metrics section produced by re-running the exact same commands as the Baseline Metrics section, against the fully-cut working tree"
    - "The Before/After Diff section's numbers are internally consistent with the actual git history of Plans 12-08 through 12-11, spot-checked, not merely asserted"
    - "The full local verification gate, including the real-Agda integration lane, is green one final time against the complete, fully-cut working tree"
    - "If any deploy-relevant cut landed during this phase, the current, latest deploy's healthz endpoint returns HTTP 200 at the time this plan runs"
  artifacts:
    - path: ".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md"
      provides: "The phase's finalized closing artifact, with before-and-after metrics proving the project is measurably simpler"
      contains: "Before/After Diff"
  key_links:
    - from: "12-HEALTH-REPORT.md Baseline Metrics (pre-cut)"
      to: "12-HEALTH-REPORT.md After Metrics (post-cut)"
      via: "identical commands re-run at two points in time"
      pattern: "find src -name"
---

<objective>
Re-run the exact baseline metrics commands from 12-HEALTH-REPORT.md's pre-cut snapshot against the fully-cut working tree, compute the before-and-after diff, and finalize the health report as the phase's closing artifact. Then run the full local verification gate one final time as the phase-level gate, distinct from each individual plan's own gate, proving the sum of all independent cuts across Waves 4 and 5 did not interact badly, and confirm the current deployed instance's health if any deploy-relevant cut landed during this phase.

Purpose: The phase's pinned goal requires the project to be measurably simpler, not just asserted to be simpler; this plan is where that measurement is actually taken and proven. It is also the final C-01 checkpoint: a single aggregate gate after every independent execution plan, catching any cross-plan interaction the individual plans' own gates could not see.
Output: 12-HEALTH-REPORT.md finalized with After Metrics and Before/After Diff sections; a final green full-suite run; a final healthz confirmation if applicable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md
@.agents/skills/upstream-sync/SKILL.md

<interfaces>
Read 12-HEALTH-REPORT.md's existing Baseline Metrics (pre-cut) section for the exact commands to re-run verbatim (do not invent new measurement commands; the before and after numbers are only comparable if produced identically). For the spot-check, read the SUMMARY.md files Plans 12-08 through 12-11 produced (each execution plan's own output) as the source of "what actually got touched" to cross-check against the measured deltas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure baseline metrics and finalize the before-after diff</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md</files>
  <read_first>
    - 12-HEALTH-REPORT.md's existing Baseline Metrics (pre-cut) section in full, for the exact commands and the exact pre-cut numbers to diff against.
    - 12-APPROVED-CUTS.md's Summary line, for the final approved, rejected, deferred, and unmapped tally.
    - The SUMMARY.md files from Plans 12-08, 12-09, 12-10, and 12-11, for the spot-check cross-reference.
  </read_first>
  <action>
    Re-run the exact same baseline metrics commands from 12-HEALTH-REPORT.md's Baseline Metrics (pre-cut) section against the current, fully-cut working tree: the src file count and total line count, the scripts file count and total line count, the test file count, and the current registered-tool count cross-verified against test/fixtures/e2e/mcp-tool-coverage.json's new entry count. Append an After Metrics (post-cut) section to 12-HEALTH-REPORT.md with these fresh numbers, dated today. Append a Before/After Diff section computing the delta for each metric (files removed, lines removed, tools removed, scripts removed), plus the final approved, rejected, deferred, and unmapped tally transcribed from 12-APPROVED-CUTS.md's Summary line. Spot-check at least two of the four deltas against the actual git diff stat for the relevant commits (for example, confirm the scripts file-count delta matches the count of scripts files actually deleted per Plan 12-08's SUMMARY.md). State explicitly if any category had zero approved items, rather than omitting that category from the diff.
  </action>
  <verify>
    <automated>grep -c "Before/After Diff" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - 12-HEALTH-REPORT.md contains both an After Metrics (post-cut) section and a Before/After Diff section, each with concrete numbers, not placeholders.
    - At least two of the four deltas are explicitly spot-checked against a real git diff stat, with the specific commit or commits cited.
    - Every category (pipeline, docs and residue, tools, src subtraction) is named in the diff, even if its delta is zero.
  </acceptance_criteria>
  <done>12-HEALTH-REPORT.md is finalized with a fresh, spot-checked before-and-after metrics diff.</done>
</task>

<task type="auto">
  <name>Task 2: Final full-suite phase gate and deploy-health confirmation</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md</files>
  <read_first>
    - 12-PATTERNS.md Shared Pattern D (the exact LOCAL verification gate command block, reused verbatim across this phase).
    - .agents/skills/upstream-sync/SKILL.md Section 4, steps 6 through 8, for the exact healthz confirmation command, used here as a confirmation only, not a new deploy trigger.
    - Plan 12-08's SUMMARY.md, to know whether a deploy-relevant push already happened during this phase and whether any later plan's push (Plan 12-09's tag push, or any push from Plan 12-10 or 12-11) would have triggered a subsequent redeploy that needs its own confirmation instead of the earlier one.
  </read_first>
  <action>
    Run the full LOCAL verification gate one final time against the fully-cut working tree: export the mise Node 24 path, then npm ci, npm run build, npx tsc -p tsconfig.test.json --noEmit, and RUN_AGDA_INTEGRATION=1 npx vitest run. This is the phase-level gate proving the sum of all independent cuts across Waves 4 and 5 did not interact badly, distinct from each individual plan's own gate. If any push during this phase (Plan 12-08's deploy-relevant batch, or any subsequent push from Plans 12-09 through 12-11) triggered a redeploy, confirm the CURRENT, latest deploy is green by polling the healthz endpoint now, not by re-trusting an earlier plan's confirmation from before later pushes landed. Commit the finalized 12-HEALTH-REPORT.md from Task 1 as this phase's closing artifact.
  </action>
  <verify>
    <automated>RUN_AGDA_INTEGRATION=1 npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - RUN_AGDA_INTEGRATION=1 npx vitest run exits 0 with zero failures, and npm run build exits 0, both against the fully-cut working tree with every prior plan's changes present.
    - If any push occurred during this phase, curl against the healthz endpoint returns HTTP 200 at the time this task runs, checked against the CURRENT latest deploy, not an earlier one.
    - git log for 12-HEALTH-REPORT.md shows this task's finalize commit as the most recent change to that file.
  </acceptance_criteria>
  <done>The phase-level full-suite gate is green, the current deploy is confirmed healthy if applicable, and the finalized health report is committed as the phase's closing artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The phase's closing claim of measurable simplification and full-suite health | A false-green close would misrepresent the phase's actual state to future sessions reading STATE.md and this health report |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-23 | Repudiation (false-green phase close) | This plan's own closing claim | mitigate | Task 2 requires an actual, freshly-run full local gate and a freshly-polled healthz check, not a re-assertion of an earlier plan's already-stale result |
| T-12-SC | Supply chain (package installs) | N/A | accept | This plan installs zero new npm packages |
</threat_model>

<verification>
RUN_AGDA_INTEGRATION=1 npx vitest run and npm run build both exit 0 against the complete, fully-cut working tree. 12-HEALTH-REPORT.md contains a finalized, spot-checked Before/After Diff section. The current deploy's healthz endpoint returns HTTP 200 if any deploy-relevant push occurred this phase.
</verification>

<success_criteria>
The project's measurable simplification is proven with a spot-checked before-and-after metrics diff, the full local suite is green one final time against the complete cut set, and the phase's closing artifact is committed.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-12-SUMMARY.md when done
</output>
