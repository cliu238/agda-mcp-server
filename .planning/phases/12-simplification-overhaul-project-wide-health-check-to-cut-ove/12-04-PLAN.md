---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-SRC-DEBT.md
autonomous: true
requirements: [C-03, C-04, D-01, C-05]

must_haves:
  truths:
    - "Every src/-scoped item in CONCERNS.md's Tech Debt, Known Bugs, Fragile Areas, and Test Coverage Gaps sections, plus every Info-grade item in both milestone audits, has a 2026-07-06-or-later re-verification status"
    - "A fresh, full-repo file-size sweep identifies any file that has newly crossed into the 400-plus-line watch zone since the 2026-07-04 CONCERNS.md snapshot"
    - "Every src/ subtraction candidate is explicitly tagged low-risk-deletion versus restructuring, and every candidate touching a Phase 10 upstream-overlap file states a converge-toward-upstream framing per C-03"
  artifacts:
    - path: ".planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-SRC-DEBT.md"
      provides: "Re-verified src/-scoped known-debt status, a fresh file-size sweep, and severity-graded src/ low-risk-subtraction cut-list candidates"
      contains: "Cut-List Candidates"
  key_links:
    - from: "12-AUDIT-SRC-DEBT.md cut-list rows"
      to: "12-06 consolidated health report"
      via: "Plan 12-06 Task 1 transcribes this doc's rows into the src Low-Risk Subtraction section"
      pattern: "src Low-Risk Subtraction"
---

<objective>
Re-verify src/-scoped known debt (CONCERNS.md plus both milestone audits) against current HEAD, re-measure the file-size watch list, and identify concrete dead-code, duplicate-implementation, or unused-export candidates confined to src/ excluding the MCP tool-registration surface (owned by Plan 12-03). Every candidate is filtered through decision C-03 (upstream convergence, never a restructuring) and decision C-04 (architecture invariants stay) before being drafted as a cut-list row. This plan is read-only, producing exactly one new report artifact for Plan 12-06 to consolidate.

Purpose: D-01 permits src/ only low-risk deletions, never restructurings; C-03 and C-04 are forced constraints this audit judges against, not decisions up for debate. RESEARCH.md's Pattern 1 (re-verify, do not rediscover) and Pitfall 6 (low usage is not low security value) both apply directly here.
Output: 12-AUDIT-SRC-DEBT.md in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md
@ARCHITECTURE.md

<interfaces>
Reuse the same cut-list row shape Plan 12-01 defines (Issue, Files, Impact, Fix approach, Severity), with two additions specific to this category: a Restructuring-or-deletion field (must read low-risk-deletion; any candidate that would read restructuring must be excluded, not included with a caveat) and an Upstream-overlap field (yes or no; if yes, the Fix approach must read as converging toward upstream, never diverging further).

Ownership boundary with Plan 12-03: if a candidate would require deleting a tool's registration entry (a src/tools/star star file, or src/session/load-tool-registration.ts, process-tool-registration.ts, or tool-recommendation.ts), it belongs in Plan 12-03's MCP Tool Surface category, not here. This plan's candidates are everything else in src/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify src-scoped known-debt ledger and file-size watch list against HEAD</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-SRC-DEBT.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md in full: Tech Debt section (lines 5 through 30), Known Bugs (31 through 61), Fragile Areas (96 through 109), and Test Coverage Gaps (143 onward) — excluding the pipeline-scoped items already owned by Plan 12-01 (assertSafeRunId duplication and the three named oracle-tooling defects).
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md frontmatter tech_debt block (lines 19 through 38) and .planning/milestones/v1.1-MILESTONE-AUDIT.md frontmatter tech_debt block (lines 10 onward), both in full.
    - 12-RESEARCH.md Pattern 1 (re-verify methodology and its worked file-size example naming seven files: agda-transport.ts, session.ts, agent-ux/project-tools.ts, tool-registration.ts, analysis-tools.ts, project-config.ts, import-graph.ts) and the State of the Art "Deprecated/outdated" list (two items already confirmed deleted by Phase 9 and one by Phase 10 — do not re-flag scripts/verify-cold-replay.mjs, scripts/promote-capture.mjs, or src/session/load-terminus-tracker.ts).
    - 12-RESEARCH.md's Anti-Pattern "Treating few or zero import-graph consumers as dead code" (src/agda/import-graph.ts has four real non-test consumers backing six live tools; do not re-litigate this without genuinely new evidence not already present in RESEARCH.md).
  </read_first>
  <action>
    Create 12-AUDIT-SRC-DEBT.md with a Known src Debt Re-Verification section. For every src/-scoped item in CONCERNS.md's four named sections (excluding the pipeline-scoped items Plan 12-01 owns) and every Info-grade tech_debt entry in both milestone audits, re-run the specific check the finding was based on against current HEAD, re-reading the exact file and line range named rather than the prose summary, and record one row per finding: finding name, original source citation, today's re-verification date, and status of confirmed-still-present, already-resolved, or severity-changed with the specific evidence. Re-measure the file-size list using a fresh full-repo sweep of every .ts file under src sorted by line count descending, not just the seven originally-named files, to catch any new file that has crossed into the 400-plus-line watch zone since 2026-07-04. Explicitly state in the doc that scripts/verify-cold-replay.mjs, scripts/promote-capture.mjs, and src/session/load-terminus-tracker.ts are confirmed already deleted and are not re-flagged, and that src/agda/import-graph.ts is confirmed to have four real non-test consumers and is not re-litigated as dead code absent new evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-SRC-DEBT.md</automated>
  </verify>
  <acceptance_criteria>
    - The Known src Debt Re-Verification section covers every src/-scoped CONCERNS.md item outside the pipeline-owned ones, plus every milestone-audit Info-grade item, each with a dated status from the three-value enum.
    - The fresh file-size sweep is a full src/ scan (not limited to the seven originally-named files), sorted descending, with the 500-line ceiling marked against each entry at or above 450 lines.
    - The doc explicitly states the three confirmed-already-deleted items are not re-flagged and that import-graph.ts is not re-litigated without new evidence.
  </acceptance_criteria>
  <done>Src-scoped known debt has a dated HEAD-based status for every item; the file-size watch list is freshly re-measured across all of src, not just the previously-named seven files.</done>
</task>

<task type="auto">
  <name>Task 2: Dead-code and duplicate-export scan with C-03 and C-04 filters, draft cut-list rows</name>
  <files>.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-SRC-DEBT.md</files>
  <read_first>
    - 12-CONTEXT.md's C-03 (upstream conflict-surface constraint: prefer deleting extra code or converging toward upstream in overlap files, avoid restructurings) and C-04 (single AgdaSession, command-builder.ts as the sole IOTCM string source, the 500-line ceiling, and protocol to agda to session to tools layering, all non-negotiable).
    - ARCHITECTURE.md's Anti-Patterns section (hand-built IOTCM command strings outside command-builder.ts, fat tool handlers with embedded domain logic, a second parallel AgdaSession instance) and Architectural Constraints section.
    - 12-RESEARCH.md's Security Domain section in full and Pitfall 6 (low usage is not low security value), naming src/repo-root.ts, src/session/safe-source-io.ts, src/agda/refactor-helpers.ts's assertValidAutoHint, and src/protocol/responses/proof-actions.ts as the specific files requiring justification beyond low usage alone.
    - .planning/phases/10-upstream-reconcile/10-CONTEXT.md and docs/LOAD-TERMINUS-ADJUDICATION.md, to identify the six files Phase 10's MERGE-01 named as conflict files (src/agda/refactor-helpers.ts, src/agda/session-load-impl.ts, src/session/agda-transport.ts, src/session/command-completion.ts, src/tools/goal-tools.ts, test/unit/agda/agent-ux.test.ts) — any candidate touching one of these six is an upstream-overlap candidate under C-03.
  </read_first>
  <action>
    From Task 1's re-verified findings plus a targeted scan for unused exports and duplicate implementations confined to src/ (grep exported symbols and cross-reference their import sites by hand; this plan does not run npx knip, since scoping it correctly would still need an explicit entry-point config and the manual method is sufficient at this repo's size per RESEARCH.md's own Environment Availability guidance), identify concrete candidates. Exclude anything that would require deleting a tool's registration entry (that belongs to Plan 12-03, per this plan's interfaces ownership boundary). For every candidate touching one of the six Phase 10 MERGE-01 files, frame the Fix approach explicitly as converging toward upstream or deleting extra code, never as a restructuring, per C-03. For every candidate touching src/repo-root.ts, src/session/safe-source-io.ts, src/agda/refactor-helpers.ts, or src/protocol/responses/proof-actions.ts, require a justification beyond low usage alone before including it, and record any considered-but-excluded candidate from these files in a dedicated Excluded-on-Security-or-Invariant-Grounds subsection. Draft Cut-List Candidates rows using this plan's interfaces shape, tagging each row's Restructuring-or-deletion field as low-risk-deletion (never restructuring) and its Upstream-overlap field as yes or no.
  </action>
  <verify>
    <automated>grep -c "Excluded-on-Security-or-Invariant-Grounds" .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-AUDIT-SRC-DEBT.md</automated>
  </verify>
  <acceptance_criteria>
    - The document states which scan method was used (manual export or import cross-reference) and explicitly states why npx knip was not run for this category.
    - Every Cut-List Candidates row has Restructuring-or-deletion set to low-risk-deletion; any candidate that could not be framed this way is excluded and named in the Excluded-on-Security-or-Invariant-Grounds subsection with a reason.
    - Every candidate touching one of the six Phase 10 MERGE-01 files has Upstream-overlap set to yes and a Fix approach phrased as convergence, never restructuring.
  </acceptance_criteria>
  <done>Src/ subtraction candidates are drafted with C-03 and C-04 filters demonstrably applied, and every considered-but-excluded security or invariant-adjacent candidate is named with its exclusion reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This audit's low-risk-deletion judgment versus src/'s architecture invariants | A candidate mislabeled low-risk-deletion when it is actually a restructuring would violate C-04 if later approved and executed |
| This audit's candidates versus RESEARCH.md's named security-load-bearing src/ files | repo-root.ts, safe-source-io.ts, refactor-helpers.ts, and proof-actions.ts each implement a control that must survive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | src/repo-root.ts, src/session/safe-source-io.ts, src/agda/refactor-helpers.ts, src/protocol/responses/proof-actions.ts (audit-time consideration only) | mitigate | Task 2 requires a justification beyond low usage before including any candidate touching these files, and an Excluded-on-Security-or-Invariant-Grounds subsection naming every one considered and rejected |
| T-12-08 | Elevation of Privilege | Any candidate that could be mislabeled a low-risk deletion while actually being a restructuring | mitigate | Task 2's explicit Restructuring-or-deletion tagging requirement, checked against ARCHITECTURE.md's Anti-Patterns and Architectural Constraints sections |
</threat_model>

<verification>
12-AUDIT-SRC-DEBT.md exists with both required sections (Known src Debt Re-Verification and Cut-List Candidates), the fresh full-repo file-size sweep, and the Excluded-on-Security-or-Invariant-Grounds subsection. No file under src/ is modified by this plan.
</verification>

<success_criteria>
Src/-scoped known debt is re-verified against HEAD, the file-size watch list is freshly re-measured across all of src/, and src/ subtraction candidates are drafted with C-03 upstream-convergence and C-04 architecture-invariant filters demonstrably applied.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-04-SUMMARY.md when done
</output>
