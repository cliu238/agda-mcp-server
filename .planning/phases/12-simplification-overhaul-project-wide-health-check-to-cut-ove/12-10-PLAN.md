---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 10
type: execute
wave: 4
depends_on: ["12-07"]
files_modified:
  - src/agda/**
  - src/protocol/**
  - src/reporting/**
  - src/index.ts
  - src/repo-root.ts
  - src/json-data.ts
  - src/server-version.ts
  - src/agda-process.ts
  - src/session/agda-transport.ts
  - src/session/apply-batch-edits.ts
  - src/session/apply-goal-edit.ts
  - src/session/apply-proof-edit.ts
  - src/session/apply-text-edit.ts
  - src/session/command-completion.ts
  - src/session/command-wait-diagnostics.ts
  - src/session/goal-catalog.ts
  - src/session/goal-positions.ts
  - src/session/goal-state.ts
  - src/session/goal-terminus.ts
  - src/session/literate-extraction.ts
  - src/session/literate/**
  - src/session/load-tool-shared.ts
  - src/session/project-config-diagnostics.ts
  - src/session/project-config.ts
  - src/session/register-agda-apply-edit.ts
  - src/session/register-agda-load-no-metas.ts
  - src/session/register-agda-load.ts
  - src/session/register-agda-typecheck.ts
  - src/session/reload-and-diagnose.ts
  - src/session/safe-source-io.ts
  - src/session/session-namespaces.ts
  - src/session/session-snapshot.ts
  - src/session/session-state.ts
  - src/session/stdout-line.ts
  - src/session/tool-presentation.ts
autonomous: true
requirements: [D-01, C-03, C-04, C-01]

must_haves:
  truths:
    - "Only CUT-NN items tagged category src-subtraction with decision approved in 12-APPROVED-CUTS.md are executed; every other candidate is left untouched"
    - "Every executed item touching one of Phase 10's six MERGE-01 upstream-overlap files has a commit message explicitly stating convergence toward upstream, per C-03"
    - "No approved item that would require deleting a tool's registration entry is executed here — that ownership belongs exclusively to Plan 12-09"
    - "No new architecture-invariant violation is introduced: exactly one AgdaSession construction site, zero hand-built IOTCM strings outside command-builder.ts, and no file newly crosses the 500-line ceiling"
  artifacts: []
  key_links:
    - from: "12-APPROVED-CUTS.md"
      to: "this plan's Task 1 filter"
      via: "category equals src-subtraction and decision equals approved"
      pattern: "src-subtraction"
    - from: "test/unit/protocol/no-bare-command-strings.test.ts"
      to: "this plan's Task 2 invariant re-check"
      via: "the existing invariant test is re-run explicitly, not assumed to still pass"
      pattern: "no-bare-command-strings"
---

<objective>
Execute every approved src/ low-risk subtraction cut-list item from 12-APPROVED-CUTS.md, applying each item's specific Fix approach from 12-HEALTH-REPORT.md exactly, with a mandatory convergence framing for any Phase 10 upstream-overlap file per C-03, and a mandatory architecture-invariant re-verification pass at the end per C-04. This plan's file ownership explicitly excludes the MCP tool-registration surface (src/tools/**, plus src/session/load-tool-registration.ts, process-tool-registration.ts, and tool-recommendation.ts), which belongs exclusively to Plan 12-09.

Purpose: This is the back half's src/ subtraction category, the one D-01 restricts most tightly (low-risk deletions only, never restructurings). C-03 and C-04 are forced constraints this plan enforces at execution time, not just at audit time.
Output: The approved src/ subtraction cuts committed, full local gate green, architecture invariants re-verified intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md
@ARCHITECTURE.md

<interfaces>
Read every approved src-subtraction CUT-NN row's Fix approach, Restructuring-or-deletion, and Upstream-overlap fields from 12-HEALTH-REPORT.md directly; this plan executes what the audit already specified, it does not re-derive fix approaches. Ownership boundary with Plan 12-09: if executing an approved row would require deleting a tool's registration entry (any file under src/tools/**, or src/session/load-tool-registration.ts, process-tool-registration.ts, or tool-recommendation.ts), do not execute it here; record it as out-of-scope-for-this-plan in the summary and note that it belongs to Plan 12-09's category instead — this should not occur if 12-04's audit applied its own ownership boundary correctly, but this plan re-checks it defensively.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute approved src low-risk subtraction cuts</name>
  <files>src/agda/**, src/protocol/**, src/reporting/**, src/index.ts, src/repo-root.ts, src/json-data.ts, src/server-version.ts, src/agda-process.ts, src/session/** (excluding load-tool-registration.ts, process-tool-registration.ts, tool-recommendation.ts)</files>
  <read_first>
    - 12-APPROVED-CUTS.md's Normalized decisions table, filtered to category src-subtraction and decision approved.
    - 12-HEALTH-REPORT.md's src Low-Risk Subtraction section for each approved row's exact Fix approach, Restructuring-or-deletion, and Upstream-overlap fields, and its Regression-Lock Exclusion List section.
    - 12-CONTEXT.md's C-03 (upstream convergence, never a restructuring) and C-04 (single AgdaSession, command-builder.ts as sole IOTCM source, 500-line ceiling, protocol to agda to session to tools layering).
    - ARCHITECTURE.md's Anti-Patterns section, to recognize if an approved Fix approach would accidentally introduce one of the three named anti-patterns while being executed.
  </read_first>
  <action>
    For every row in 12-APPROVED-CUTS.md with category src-subtraction and decision approved, confirm first that its Files do not require deleting a tool's registration entry (per this plan's interfaces ownership boundary); if one does, skip it here and record it as belonging to Plan 12-09 instead. For every remaining approved row, execute its exact Fix approach from 12-HEALTH-REPORT.md. For any row whose Upstream-overlap field is yes, verify the change is a pure subtraction or convergence toward upstream, never a restructuring that moves code across the protocol to agda to session to tools layer boundary or introduces a second AgdaSession-like construction site; if executing the specified Fix approach would require either, stop, leave that item unexecuted, and record it as an architecture-invariant conflict in this plan's summary rather than executing a workaround. Cross-check every file about to be touched against 12-HEALTH-REPORT.md's Regression-Lock Exclusion List before touching any test/ file. Run npx tsc -p tsconfig.test.json --noEmit after each individual cut, not only at the end, to catch a dangling import immediately. Once every approved item in this task is applied, run the full local verification gate: export the mise Node 24 path, then npm ci, npm run build, npx tsc -p tsconfig.test.json --noEmit, and RUN_AGDA_INTEGRATION=1 npx vitest run.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.test.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc -p tsconfig.test.json --noEmit exits 0 after every individual cut in this task.
    - RUN_AGDA_INTEGRATION=1 npx vitest run exits 0 after the full batch.
    - Every commit produced by this task that touches one of Phase 10's six MERGE-01 files has a commit message explicitly stating convergence toward upstream, not a generic message.
    - Every approved src-subtraction CUT-NN item is either fully executed, explicitly deferred to Plan 12-09 as a tools-ownership item, or explicitly recorded as blocked on an architecture-invariant conflict; none is silently skipped.
    - No file listed in the Regression-Lock Exclusion List appears in this task's diff.
  </acceptance_criteria>
  <done>All approved src low-risk subtraction cuts are applied with C-03 convergence framing and zero regression-locked files touched.</done>
</task>

<task type="auto">
  <name>Task 2: Independent architecture-invariant re-verification and final commit</name>
  <files>src/**</files>
  <read_first>
    - ARCHITECTURE.md's Architectural Constraints section (single AgdaSession, command-builder.ts SSOT, 500-line ceiling, layering) and Anti-Patterns section.
    - test/unit/protocol/no-bare-command-strings.test.ts (the existing mechanical check for hand-built IOTCM strings outside command-builder.ts).
    - The diff produced by Task 1 in this same plan.
  </read_first>
  <action>
    Independently re-verify, rather than trust Task 1's own running commentary, that this plan's batch of cuts did not introduce a regression against any of the three named architecture invariants. Confirm there is exactly one AgdaSession construction site in src/index.ts and none elsewhere in src/. Run the existing no-bare-command-strings invariant test explicitly to confirm no hand-built IOTCM string was introduced outside command-builder.ts. Re-run a full src/ file-size sweep and diff it against 12-04's audit-time baseline sweep to confirm no file newly crosses the 500-line ceiling as a side effect of this plan's edits (a file that was already over or near the ceiling before this plan ran is not a new violation). If this pass finds any fixup needed, apply it and commit it as a new commit, never amending a prior commit.
  </action>
  <verify>
    <automated>npx vitest run test/unit/protocol/no-bare-command-strings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run test/unit/protocol/no-bare-command-strings.test.ts exits 0.
    - Counting the literal construction pattern for a new AgdaSession across all of src/ finds exactly one occurrence, in src/index.ts.
    - The fresh file-size sweep shows no file crossing 500 lines that was not already at or over that line before this plan started.
  </acceptance_criteria>
  <done>Architecture invariants are independently re-confirmed intact after this plan's batch of cuts, with any needed fixup committed as its own new commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/repo-root.ts, src/session/safe-source-io.ts, src/agda/refactor-helpers.ts, src/protocol/responses/proof-actions.ts (potentially touched here) | Path sandbox, symlink-safe atomic writes, CLI-injection guard, and rejection-detection SSOT — RESEARCH.md Security Domain |
| Files overlapping Phase 10's merge conflict surface | Divergence from upstream here re-inflates a future manual sync's conflict burden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-19 | Tampering | src/repo-root.ts, src/session/safe-source-io.ts, src/agda/refactor-helpers.ts, src/protocol/responses/proof-actions.ts (if an approved cut touches any of these) | mitigate | Any approved cut touching these files must pass that file's own existing dedicated tests explicitly, in addition to the aggregate suite, before this task commits |
| T-12-20 | Elevation of Privilege | Architecture invariants (single AgdaSession, command-builder.ts SSOT, layering, 500-line ceiling) | mitigate | Task 2's independent re-verification pass, run regardless of what Task 1 believed it did |
</threat_model>

<verification>
npx tsc -p tsconfig.test.json --noEmit, RUN_AGDA_INTEGRATION=1 npx vitest run, and npx vitest run test/unit/protocol/no-bare-command-strings.test.ts all exit 0. A fresh file-size sweep shows no new 500-line-ceiling violation.
</verification>

<success_criteria>
Every approved src/ low-risk subtraction item is executed with C-03 and C-04 respected, verified by an independent re-check rather than assumed, with zero regression-locked files touched.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-10-SUMMARY.md when done
</output>
