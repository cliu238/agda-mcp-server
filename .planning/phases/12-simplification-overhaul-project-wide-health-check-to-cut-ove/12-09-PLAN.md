---
phase: 12-simplification-overhaul-project-wide-health-check-to-cut-ove
plan: 09
type: execute
wave: 4
depends_on: ["12-07"]
files_modified:
  - src/tools/**
  - src/session/load-tool-registration.ts
  - src/session/process-tool-registration.ts
  - src/session/tool-recommendation.ts
  - test/fixtures/e2e/mcp-tool-coverage.json
  - README.md
  - docs/assistant-workflows.md
autonomous: true
requirements: [D-02, C-01]

must_haves:
  truths:
    - "Only CUT-NN items tagged category tools with decision approved in 12-APPROVED-CUTS.md are deleted; every other tool is left registered and unchanged"
    - "test/unit/tools/mcp-e2e-coverage.test.ts and test/unit/tools/tool-family-examples.test.ts both pass before and after every individual tool deletion, not just at the end"
    - "Every deleted tool's name is grepped out of README.md and docs/assistant-workflows.md, with zero remaining references"
    - "A new git tag following this repository's existing v1.x scheme is created and pushed once all approved deletions are committed"
  artifacts:
    - path: "test/fixtures/e2e/mcp-tool-coverage.json"
      provides: "The 74-minus-N tool coverage matrix after approved deletions, still in strict bidirectional set equality with the live manifest"
  key_links:
    - from: "12-APPROVED-CUTS.md"
      to: "this plan's Task 1 filter"
      via: "category equals tools and decision equals approved"
      pattern: "tools"
    - from: "src/tools/manifest.ts listToolManifest()"
      to: "test/fixtures/e2e/mcp-tool-coverage.json"
      via: "strict bidirectional set equality enforced by mcp-e2e-coverage.test.ts"
      pattern: "matrixNames.*manifestNames"
---

<objective>
Execute every approved MCP tool deletion from 12-APPROVED-CUTS.md following the full six-step deletion lockstep checklist, verifying the mechanical coverage-matrix invariant before and after each individual deletion, then perform the manual documentation pass and cut a new release tag per decision D-02 (direct deletion allowed, no deprecation period, ship a new tag with updated onboarding docs in the same phase).

Purpose: This is the back half's MCP tool surface category. D-02 explicitly authorizes one-pass deletion; this plan executes ONLY what 12-07's checkpoint approved, using the project's own existing mechanical safety nets (the coverage-matrix and tool-family-examples tests) rather than trusting an unverified deletion.
Output: The approved tools deleted from the manifest and every lockstep file, README.md and docs/assistant-workflows.md updated, a new version tag pushed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-CONTEXT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-RESEARCH.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-PATTERNS.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-HEALTH-REPORT.md
@.planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-APPROVED-CUTS.md

<interfaces>
Read every approved tools CUT-NN row's pre-mapped Lockstep-checklist field from 12-HEALTH-REPORT.md directly (Plan 12-03 pre-filled all six steps per candidate); this plan executes that checklist, it does not re-derive it. From src/tools/manifest.ts: listToolManifest() returns the live registry, clearToolManifest() resets it, and registerManifestEntry throws on a duplicate name but not on an orphaned or missing one, so the coverage-matrix test, not a startup crash, is the enforcement point. From test/unit/tools/mcp-e2e-coverage.test.ts: the strict bidirectional set-equality assertion between mcpToolCoverageMatrix's tool names and listToolManifest()'s names, both sorted, compared with toEqual.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute approved tool deletions with mechanical lockstep</name>
  <files>src/tools/**, src/session/load-tool-registration.ts, src/session/process-tool-registration.ts, src/session/tool-recommendation.ts, test/fixtures/e2e/mcp-tool-coverage.json</files>
  <read_first>
    - 12-APPROVED-CUTS.md's Normalized decisions table, filtered to category tools and decision approved.
    - 12-HEALTH-REPORT.md's MCP Tool Surface section for each approved row's exact Lockstep-checklist field.
    - 12-PATTERNS.md Shared Pattern B in full (the six-step checklist and the reverse-checklist precedent from Phase 10's 10-03-SUMMARY.md, read as a tool-ADD checklist to invert).
    - src/tools/register-core-tools.ts (the 13 register call sites) and confirm which of the approved tools register there versus through src/session/load-tool-registration.ts or src/session/process-tool-registration.ts.
    - test/unit/tools/mcp-e2e-coverage.test.ts and test/unit/tools/tool-family-examples.test.ts in full, to run both before starting and after each individual deletion.
  </read_first>
  <action>
    Run npx vitest run test/unit/tools/mcp-e2e-coverage.test.ts test/unit/tools/tool-family-examples.test.ts once before making any change, to confirm the current green baseline. For every row in 12-APPROVED-CUTS.md with category tools and decision approved, execute that row's pre-mapped six-step checklist from 12-HEALTH-REPORT.md in order: delete the tool's register call site (checking both register-core-tools.ts and the two session registration files), delete the matching entry from test/fixtures/e2e/mcp-tool-coverage.json, delete the matching entry from src/tools/data/tool-family-examples.json if present, check src/session/tool-recommendation.ts for an addIfAvailable entry recommending the deleted tool and remove it, and check whether the deletion orphans a test/unit/tools tool-family test file's only remaining case, deleting that file too if it becomes empty. Run npx vitest run test/unit/tools/mcp-e2e-coverage.test.ts test/unit/tools/tool-family-examples.test.ts again immediately after each individual tool's deletion, not batched at the end, per the discipline Phase 10's 10-03-SUMMARY.md demonstrates (explicitly verify rather than assume). Leave README.md and docs/assistant-workflows.md untouched in this task; that manual pass is Task 2's job.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/mcp-e2e-coverage.test.ts test/unit/tools/tool-family-examples.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run test/unit/tools/mcp-e2e-coverage.test.ts test/unit/tools/tool-family-examples.test.ts exits 0 after every individual deletion in this task, not just at the very end.
    - Every approved tools CUT-NN item is either fully executed through all six checklist steps or explicitly recorded as blocked with a stated reason; none is silently skipped or partially applied.
    - No tool outside the approved list is removed from the manifest, the coverage matrix, or tool-family-examples.json.
  </acceptance_criteria>
  <done>All approved tool deletions are applied with the full six-step lockstep, verified green per-deletion, not just in aggregate.</done>
</task>

<task type="auto">
  <name>Task 2: Manual doc pass, full suite gate, version tag, commit and push</name>
  <files>README.md, docs/assistant-workflows.md</files>
  <read_first>
    - The list of tools actually deleted by Task 1 (from this plan's own running record of what was executed).
    - README.md's current "What it can do" section and docs/assistant-workflows.md in full, to locate every mention of a deleted tool's name.
    - git tag --sort=-creatordate output (the existing v1.x tag scheme: v1.1.1, v1.1, v0.6.8, v1.0, and older) to pick the next sequential tag name for a breaking tool-surface change.
    - 12-PATTERNS.md Shared Pattern B step 4 (the manual doc-lockstep step, since no automated check exists for prose documentation per RESEARCH.md's Validation Architecture).
  </read_first>
  <action>
    For every tool actually deleted by Task 1, grep README.md and docs/assistant-workflows.md for that exact tool name and remove or rewrite the reference so the doc no longer implies the tool exists. Run the full local verification gate: export the mise Node 24 path, then npm ci, npm run build, npx tsc -p tsconfig.test.json --noEmit, and RUN_AGDA_INTEGRATION=1 npx vitest run. Once green, commit Task 1's and this task's changes (one commit per deleted tool if the approved list treats tools as independently revertible, or one combined commit if they were approved as a single batch — match the granularity 12-APPROVED-CUTS.md's decisions imply). Push to origin main. Tag a new release following the existing v1.x scheme (increment appropriately for a breaking tool-surface removal) and push the tag.
  </action>
  <verify>
    <automated>RUN_AGDA_INTEGRATION=1 npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Grepping README.md and docs/assistant-workflows.md for each deleted tool's exact name returns zero matches.
    - RUN_AGDA_INTEGRATION=1 npx vitest run and npm run build both exit 0.
    - git tag --sort=-creatordate lists a new tag not present before this task started, and it has been pushed to origin.
    - Every approved tools CUT-NN item from Task 1 is reflected in the pushed commit history.
  </acceptance_criteria>
  <done>Documentation no longer references any deleted tool, the full suite is green, and a new version tag is cut and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A deleted tool's zod input-validation schema and its handler | Removing a tool must not leave a dangling registration reachable via a stale client cache or partial import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17 | Tampering / Input Validation regression | Any deleted tool's zod schema and manifest entry | mitigate | The mechanical coverage-matrix test is run before AND after each individual deletion, not just once at the end, catching a partial or dangling removal immediately |
| T-12-18 | Information Disclosure | None new | accept | This plan removes surface, it does not add any new input or output path |
</threat_model>

<verification>
npx vitest run test/unit/tools/mcp-e2e-coverage.test.ts test/unit/tools/tool-family-examples.test.ts and RUN_AGDA_INTEGRATION=1 npx vitest run both exit 0. A new git tag is pushed. README.md and docs/assistant-workflows.md contain zero references to any deleted tool.
</verification>

<success_criteria>
Every approved MCP tool deletion is executed through the full lockstep checklist, verified per-deletion, documented, and released under a new version tag.
</success_criteria>

<output>
Create .planning/phases/12-simplification-overhaul-project-wide-health-check-to-cut-ove/12-09-SUMMARY.md when done
</output>
