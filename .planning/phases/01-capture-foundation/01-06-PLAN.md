---
phase: 01-capture-foundation
plan: 06
type: execute
gap_closure: true
wave: 4
depends_on: ["01-05"]
files_modified:
  - src/tools/register-capture-session.ts
  - test/unit/tools/register-capture-session.test.ts
  - src/agda/session.ts
  - src/agda/session-process-lifecycle.ts
  - src/agda/session-load-helpers.ts
  - test/unit/agda/session-capture/manifest-builder.test.ts
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, CAP-04]
must_haves:
  truths:
    - "Two agda_capture_session calls with an identical fingerprint and recurrence in the same session stage two distinct, independently-readable files under .agda-mcp/captures/ - neither call silently overwrites the other (CR-03)"
    - "When the staged artifact write fails after the CAP-04 recorder was drained (disk full, permission error, or any throw before writeFileAtomic succeeds), the recorder buffer still holds the drained actions afterward so a retry does not lose them (WR-01)"
    - "Two captures whose session.currentFile differs route to different dedup fingerprints even when classification and note are identical, so unrelated defects on different files no longer collapse into one dedup identity (WR-02)"
    - "register-capture-session.test.ts stages every capture into a fresh per-test temp directory and never creates or depends on test/fixtures/agda/.agda-mcp/ (WR-12)"
    - "After a strict agda_typecheck (Cmd_load_no_metas) call, or after the Agda process dies mid-command, the next capture's replay manifest reports mergedArgv reflecting the CURRENT session state, never a stale prior load()'s flags (WR-08)"
  artifacts:
    - path: "src/tools/register-capture-session.ts"
      provides: "collision-proof staged filenames, durable-write-then-reset ordering, a discriminating dedup fingerprint"
      min_lines: 200
    - path: "src/agda/session-process-lifecycle.ts"
      provides: "resetFileBoundStateIfProcDied also clears lastDispatchedLoadArgv alongside the other load-history fields"
      min_lines: 260
    - path: "src/agda/session.ts"
      provides: "loadNoMetas() resets lastDispatchedLoadArgv to [] before dispatching Cmd_load_no_metas"
      min_lines: 460
  key_links:
    - from: "src/tools/register-capture-session.ts"
      to: "src/agda/session-capture/recorded-transport.ts"
      via: "resetRecordedActions() is called only after writeFileAtomic resolves successfully, not immediately after drainRecordedActions()"
      pattern: "writeFileAtomic\\(stagedPath[\\s\\S]*resetRecordedActions\\(\\)"
    - from: "src/tools/register-capture-session.ts"
      to: "src/reporting/bug-report.ts"
      via: "fingerprintBugReport is called with an agdaCommandFamily field derived from manifest.importClosureHash / session.currentFile"
      pattern: "agdaCommandFamily"
    - from: "src/agda/session.ts"
      to: "src/agda/session-capture/manifest-builder.ts"
      via: "loadNoMetas() pre-assigns lastDispatchedLoadArgv = [] before runLoadNoMetas dispatches Cmd_load_no_metas"
      pattern: "lastDispatchedLoadArgv = \\[\\]"
---

<objective>
Close the single BLOCKER gap from 01-VERIFICATION.md (silent staged-artifact overwrite, CR-03) plus its
directly-linked durability defect (WR-01), and two same-subsystem correctness defects the verifier asked
to fold into the same pass (WR-02 fingerprint coarseness, WR-12 test-fixture pollution), plus one
discretionary CAP-01 fidelity gap (WR-08 stale mergedArgv). All five fixes live in the capture tool's
write path or the session state it reads from, and all five ship with a regression test proving the
previously-buggy behavior is now correct.

Purpose: Phase 1's stated deliverable is "the foundational substrate the rest of the loop reads" — a
substrate that silently discards a capture under the explicitly-designed-for "two captures per session"
flow does not meet that bar. This plan makes durable multi-capture retention actually durable before
Phase 2's oracle triad starts reading from it.
Output: A hardened src/tools/register-capture-session.ts (collision-proof filenames, write-before-reset
ordering, discriminating fingerprint), a corrected src/agda/session.ts + session-process-lifecycle.ts
(accurate mergedArgv after loadNoMetas / process death), and an isolated register-capture-session.test.ts
that no longer pollutes the shared fixture tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-capture-foundation/01-VERIFICATION.md
@.planning/phases/01-capture-foundation/01-REVIEW.md
@.planning/phases/01-capture-foundation/01-CONTEXT.md
</context>

<interfaces>
Current src/tools/register-capture-session.ts capture callback (read the full current file before
editing — this plan patches specific statements, it does not rewrite the file):
- Staged filename today: `${dedup.fingerprint}-${dedup.recurrence}.json` under
  `join(repoRoot, ".agda-mcp", "captures")` — collides whenever fingerprint+recurrence repeat in one
  session (CR-03).
- Drain/reset today runs back-to-back with nothing in between:
  `const { actions, truncated, droppedCount } = drainRecordedActions(); resetRecordedActions();`
- Fingerprint call today: `fingerprintBugReport({ kind: "new-bug", affectedTool: "agda_capture_session",
  classification, observed, expected: "", reproduction: [], serverVersion })` — no `agdaCommandFamily`
  field passed.

From src/reporting/bug-report.ts (fingerprintBugReport's accepted input — unchanged by this plan, it
already supports the field this plan needs):
  function fingerprintBugReport(input: { kind, affectedTool, classification, observed, expected,
    reproduction, diagnostics?, toolPayload?, agdaCommandFamily?: string, serverVersion }): string;
  Internally: `agdaCommandFamily: input.agdaCommandFamily ?? ""` — omitting the field today is
  equivalent to passing "".

From src/agda/session-capture/recorded-transport.ts (unchanged by this plan):
  function drainRecordedActions(): { actions: RecordedAction[]; truncated: boolean; droppedCount: number };
    — a READ-ONLY snapshot, does not clear the buffer.
  function resetRecordedActions(): void; — the only thing that clears the buffer.

From src/agda/session-process-lifecycle.ts (resetFileBoundStateIfProcDied — signature unchanged):
  function resetFileBoundStateIfProcDied(session: AgdaSession, proc: ChildProcess): void;
  Today resets: currentFile, goalIds, lastLoadedMtime, lastClassification, lastLoadedAt,
  lastInvisibleGoalCount — NOT lastDispatchedLoadArgv. Called from session-command-dispatch.ts's
  `finally` block, strictly AFTER the current command's sendCommand attempt resolves or throws.

From src/agda/session.ts (loadNoMetas — signature unchanged):
  async loadNoMetas(filePath: string): Promise<LoadResult> { return runLoadNoMetas(this, filePath); }
  Does not touch this.lastDispatchedLoadArgv today (contrast with load(), which pre-assigns it before
  calling runLoad).

From test/helpers/repo-root.ts (unused by this plan's new tests, being removed from
register-capture-session.test.ts's imports since every test switches to a per-test sandbox):
  export const TEST_FIXTURE_PROJECT_ROOT: string;
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collision-proof staged filenames, write-before-reset durability, and isolated test fixtures (CR-03, WR-01, WR-12)</name>
  <files>src/tools/register-capture-session.ts, test/unit/tools/register-capture-session.test.ts</files>
  <read_first>
    src/tools/register-capture-session.ts
    test/unit/tools/register-capture-session.test.ts
    src/agda/session-capture/recorded-transport.ts
  </read_first>
  <behavior>
    - Test 1 (CR-03): two back-to-back `agda_capture_session` calls on the same session, same inputs
      (so fingerprint and recurrence are identical both times, since the dedup index was never
      promoted between them), produce `data.fingerprint === data.fingerprint` and
      `data.recurrence === data.recurrence` across both calls (dedup semantics unchanged), but
      `dataA.stagedPath !== dataB.stagedPath`, and both paths exist on disk and parse as valid JSON.
    - Test 2 (WR-01): with `AGDA_MCP_CAPTURE=1` and one prior tool call already recorded, pre-creating
      `<repoRoot>/.agda-mcp` as a plain FILE (not a directory) forces the callback's
      `mkdirSync(captureDir, { recursive: true })` to throw before `writeFileAtomic` ever runs. The
      capture call returns `result.structuredContent.ok === false`, and a subsequent
      `drainRecordedActions()` call still contains the prior tool's action (the buffer was never reset).
    - Test 3 (WR-12, infrastructure): re-running the existing two tests (state-agnostic capture, and
      AGDA_MCP_CAPTURE=1 draining) against a per-test `mkdtempSync` sandbox `repoRoot` instead of
      `TEST_FIXTURE_PROJECT_ROOT` still passes unchanged, and `test/fixtures/agda/.agda-mcp/` is never
      created by running this file.
  </behavior>
  <action>
    In src/tools/register-capture-session.ts:
    1. Add a module-level `let captureSequence = 0;` declaration near the top of the file (after the
       imports, before `captureReferenceDataSchema` or `registerCaptureSession`) — a monotonic counter
       that never resets for the life of the process, mirroring the single-AgdaSession-per-process
       invariant the rest of session-capture/ already relies on.
    2. Remove the `resetRecordedActions();` statement that currently runs immediately after
       `const { actions, truncated, droppedCount } = drainRecordedActions();`. Do not remove the
       `drainRecordedActions()` call itself.
    3. Change the `stagedPath` computation from
       `` `${dedup.fingerprint}-${dedup.recurrence}.json` `` to
       `` `${dedup.fingerprint}-${dedup.recurrence}-${Date.now()}-${captureSequence++}.json` `` — the
       timestamp keeps filenames sortable/debuggable, the counter guarantees uniqueness even for two
       calls landing in the same millisecond (a real risk in a fast unit test), and neither depends on
       fingerprint or recurrence, satisfying "collision-proof independent of the fingerprint/recurrence
       pair." Do not switch to an `existsSync`-then-write check-and-act pattern — that reintroduces a
       TOCTOU window PITFALLS.md already flags as a named security mistake for new capture/replay code.
    4. Insert `resetRecordedActions();` as a new statement immediately AFTER the
       `await writeFileAtomic(stagedPath, JSON.stringify(artifact, null, 2));` line succeeds, before the
       `const data: CaptureReference = {...}` construction. Add a one-line comment above it:
       "Reset only after the artifact is durably staged (WR-01) — a write failure above must leave the
       buffer intact for a retry, not silently discard the drained log." Everything between the old
       reset position and the new one (buildOracleSubstrate, diagnostics assembly, fingerprint/dedup
       computation, artifact assembly, mkdirSync, writeFileAtomic) is otherwise unchanged by this step.

    In test/unit/tools/register-capture-session.test.ts:
    5. Change the `node:fs` import to also bring in `mkdtempSync`, `rmSync`, `writeFileSync` (alongside
       the existing `existsSync`, `readFileSync`); add `import { tmpdir } from "node:os";` and
       `import { join } from "node:path";`. Remove the now-unused
       `import { TEST_FIXTURE_PROJECT_ROOT } from "../../helpers/repo-root.js";` line. Add
       `drainRecordedActions` to the existing `recorded-transport.js` import alongside
       `resetRecordedActions`.
    6. Add a module-level `let repoRoot: string;`. Extend the existing `beforeEach` to also run
       `repoRoot = mkdtempSync(join(tmpdir(), "agda-mcp-capture-session-test-"));` (after the existing
       env-var/resetRecordedActions lines). Extend the existing `afterEach` to also run
       `rmSync(repoRoot, { recursive: true, force: true });` (after the existing env restore/
       resetRecordedActions lines) — this is WR-12's fix, replacing every use of the shared,
       never-cleaned `TEST_FIXTURE_PROJECT_ROOT` with a fresh, auto-cleaned sandbox per test.
    7. In BOTH existing tests, replace every `new AgdaSession(TEST_FIXTURE_PROJECT_ROOT)` with
       `new AgdaSession(repoRoot)`, and every `registerCaptureSession(server as unknown as McpServer,
       session, TEST_FIXTURE_PROJECT_ROOT)` with `registerCaptureSession(server as unknown as McpServer,
       session, repoRoot)`. No other change to those two tests' bodies/assertions is needed — neither
       calls `session.load()`, so neither depends on real fixture `.agda` files being present under
       `repoRoot`.
    8. Add a new test proving CR-03 (per Test 1's behavior above): call `clearToolManifest()`, register
       `agda_capture_session` once against `repoRoot`, invoke its callback twice in a row with identical
       inputs (e.g. both `{}`), and assert on the two returned `data` objects per Test 1's behavior spec.
    9. Add a new test proving WR-01 (per Test 2's behavior above): set `process.env.AGDA_MCP_CAPTURE =
       "1"`, call `clearToolManifest()`, `writeFileSync(join(repoRoot, ".agda-mcp"), "blocking-file",
       "utf8")` BEFORE registering/calling anything else, register one throwaway tool via
       `registerStructuredTool` (mirroring the existing `prior_tool_one`/`prior_tool_two` pattern) and
       call it once so the recorder buffer has a known entry, then register and call
       `agda_capture_session`, and assert per Test 2's behavior spec using the already-imported
       `drainRecordedActions`.
  </action>
  <acceptance_criteria>
    - src/tools/register-capture-session.ts contains `captureSequence++` in the stagedPath template
      literal and does NOT contain the exact substring `` `${dedup.fingerprint}-${dedup.recurrence}.json` ``
      (unsuffixed) anywhere
    - src/tools/register-capture-session.ts's `resetRecordedActions();` call site textually follows its
      `await writeFileAtomic(` call site (source-order check, not just presence)
    - test/unit/tools/register-capture-session.test.ts contains zero occurrences of
      `TEST_FIXTURE_PROJECT_ROOT`
    - `npx vitest run test/unit/tools/register-capture-session.test.ts --reporter=dot` exits 0 with 4
      passing tests (the 2 pre-existing, now sandbox-scoped, plus the 2 new ones)
    - After that run, `test ! -e test/fixtures/agda/.agda-mcp` is true (the shared fixture tree was never
      touched)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/tools/register-capture-session.test.ts --reporter=dot && test ! -e test/fixtures/agda/.agda-mcp && echo ISOLATION_CONFIRMED</automated>
  </verify>
  <done>Two same-fingerprint captures in one session never collide on disk, a failed staged write never destroys the drained action log, and the test file no longer mutates the shared fixture tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Discriminate dedup fingerprints by loaded file, not just classification/note (WR-02)</name>
  <files>src/tools/register-capture-session.ts, test/unit/tools/register-capture-session.test.ts</files>
  <read_first>
    src/tools/register-capture-session.ts
    src/reporting/bug-report.ts
    test/unit/tools/register-capture-session.test.ts
  </read_first>
  <behavior>
    - Test: two `agda_capture_session` calls on the SAME session instance and SAME repoRoot, with
      identical classification (both pre-load, `sessionClassification: null`) and identical (absent)
      note, but with `session.currentFile` set to two different non-null string values between the two
      calls, produce two DIFFERENT `data.fingerprint` values. This proves the fingerprint now
      discriminates by loaded file even though CR-03's filename fix alone would have made the two
      captures land in different files regardless — this test specifically locks the DEDUP IDENTITY
      change, not the filename change.
  </behavior>
  <action>
    In src/tools/register-capture-session.ts, inside the existing `fingerprintBugReport({...})` call
    (built from `kind`, `affectedTool`, `classification`, `observed`, `expected`, `reproduction`,
    `serverVersion`), add one new field: `agdaCommandFamily: manifest.importClosureHash ??
    session.currentFile ?? ""`. Both `manifest` and `session` are already in scope at this call site.
    This mirrors the review's exact recommended discriminator: when a file is loaded and its closure
    hash is available, use that (most specific — changes whenever the loaded file's transitive closure
    changes); when a file is loaded but the closure hash could not be computed (e.g. the file is outside
    the project's import graph), fall back to the raw `session.currentFile` path string (still
    discriminates by file identity); when nothing is loaded, fall back to `""`, which is exactly
    `fingerprintBugReport`'s own existing default for an omitted `agdaCommandFamily` — so a pre-load
    capture's fingerprint is byte-for-byte unchanged by this fix (no behavior change for the two
    existing tests' pre-load scenarios).

    In test/unit/tools/register-capture-session.test.ts, add a new test (after Task 1's two new tests):
    call `clearToolManifest()`, construct one `AgdaSession(repoRoot)` and register
    `agda_capture_session` against it once. Call the callback once (`session.currentFile` is still its
    default `null`) and capture `data.fingerprint` from the result. Then directly assign
    `session.currentFile = join(repoRoot, "SomeOtherProject.agda")` (a path that does not need to exist
    on disk — `buildOracleSubstrate`'s `readAgdaSourceFile` and
    `import-closure-hash.ts`'s `closureFileSet` both already degrade gracefully to null/empty for a
    nonexistent or out-of-graph file, per their existing try/catch and `computeImpact`-null-return
    contracts — confirm this by reading both files if any doubt remains). Call the callback again and
    capture the second `data.fingerprint`. Assert the two fingerprints are NOT equal.
  </action>
  <acceptance_criteria>
    - src/tools/register-capture-session.ts's `fingerprintBugReport({...})` call contains the exact
      substring `agdaCommandFamily: manifest.importClosureHash ?? session.currentFile ?? ""`
    - `npx vitest run test/unit/tools/register-capture-session.test.ts --reporter=dot` exits 0 with 5
      passing tests total (Task 1's 4 plus this task's 1 new test)
    - The two pre-existing tests' assertions (unrelated to fingerprint value, only structural/dedup-kind
      checks) still pass unmodified, confirming the pre-load fingerprint is unchanged by this fix
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/unit/tools/register-capture-session.test.ts --reporter=dot</automated>
  </verify>
  <done>Two captures of different loaded files never collapse into the same dedup fingerprint merely because their classification and note happen to match.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stop a stale mergedArgv from leaking into the replay manifest after a strict reload or process death (WR-08)</name>
  <files>src/agda/session.ts, src/agda/session-process-lifecycle.ts, src/agda/session-load-helpers.ts, test/unit/agda/session-capture/manifest-builder.test.ts</files>
  <read_first>
    src/agda/session.ts
    src/agda/session-process-lifecycle.ts
    src/agda/session-load-helpers.ts
    src/agda/session-load-impl.ts
    src/agda/session-command-dispatch.ts
    test/unit/agda/session-capture/manifest-builder.test.ts
  </read_first>
  <behavior>
    - Test 1 (fast, no Agda binary needed): calling `resetFileBoundStateIfProcDied(session, deadProc)`
      directly — where `session.lastDispatchedLoadArgv` was previously set to a non-empty sentinel array
      and `deadProc` is a plain object shaped like `{ exitCode: 1, signalCode: null, killed: false }`
      (so `isProcLive` reports false) — leaves `session.lastDispatchedLoadArgv` equal to `[]` afterward.
    - Test 2 (RUN_AGDA_INTEGRATION-gated, real Agda): after `session.load("CompleteFixture.agda", {
      commandLineOptions: ["--flag", "--flag"] })` sets `session.lastDispatchedLoadArgv` to
      `["--flag", "--flag"]`, calling `session.loadNoMetas("CompleteFixture.agda")` resets
      `session.lastDispatchedLoadArgv` to `[]` (Cmd_load_no_metas dispatches no options list at all, so
      `[]` is the accurate value, not a placeholder).
  </behavior>
  <action>
    IMPORTANT — read this before editing anything: the review (01-REVIEW.md, WR-08) literally suggests
    clearing `lastDispatchedLoadArgv` inside `invalidatePriorLoadState` (session-load-helpers.ts). DO NOT
    do this. `session.load()` (session.ts) synchronously pre-assigns `this.lastDispatchedLoadArgv` to the
    real, correct argv BEFORE calling `runLoad(this, filePath, ...)`, and `runLoad`'s very first
    statement is `invalidatePriorLoadState(session)` — if that function also cleared
    `lastDispatchedLoadArgv`, it would immediately wipe out the value `load()` just set, on every single
    call, breaking Plan 01-02's already-shipped, already-tested CAP-01 argv fidelity. Do not touch
    `invalidatePriorLoadState` or `resetProcBoundState` (session-process-lifecycle.ts, used by
    `ensureProcessForSession` on respawn) for the same reason — `resetProcBoundState` can also fire
    mid-flow between `session.load()`'s pre-assignment and the command actually reaching Agda, via
    `ensureProcess()`'s respawn-on-dead-process path inside `dispatchSessionCommand`
    (session-command-dispatch.ts line ~61), so clearing there has the identical wipe-out risk.

    Instead, make exactly these two changes:
    1. In src/agda/session.ts's `loadNoMetas(filePath: string)` method, add
       `this.lastDispatchedLoadArgv = [];` as the FIRST statement in the method body, before
       `return runLoadNoMetas(this, filePath);`. Add a one-line comment above it: "Cmd_load_no_metas
       dispatches no options list at all (session-load-impl.ts's runLoadNoMetas) — [] is the true value,
       not a placeholder; set it here (mirroring load()'s own pre-assignment) so a strict reload never
       leaves an earlier load()'s flags stale in the replay manifest (WR-08)."
    2. In src/agda/session-process-lifecycle.ts's `resetFileBoundStateIfProcDied(session, proc)`
       function, add `session.lastDispatchedLoadArgv = [];` as a new line alongside the existing six
       resets (after the existing `session.lastInvisibleGoalCount = 0;` line). This function runs
       strictly AFTER the current command's own `sendCommand` attempt resolves or throws (it is the
       `finally`-block reset in `dispatchSessionCommand`), so it has none of the mid-flow wipe-out risk
       `invalidatePriorLoadState`/`resetProcBoundState` have — by the time it fires for THIS command, no
       later `session.load()` call has had a chance to pre-assign anything yet.
    3. In src/agda/session-load-helpers.ts, add a one-line comment inside or directly above
       `invalidatePriorLoadState`'s existing JSDoc block: "lastDispatchedLoadArgv is deliberately NOT
       reset here — session.load() pre-assigns it before calling into runLoad/this function, and
       clearing it here would wipe that assignment on every load. See loadNoMetas() and
       resetFileBoundStateIfProcDied for WR-08's actual reset points." This is documentation only — no
       behavior change to this file.

    In test/unit/agda/session-capture/manifest-builder.test.ts:
    4. Add `import { resetFileBoundStateIfProcDied } from
       "../../../../src/agda/session-process-lifecycle.js";` and
       `import type { ChildProcess } from "node:child_process";` to the existing import list.
    5. Add a new, ungated `test(...)` (runs unconditionally, no Agda binary needed) implementing Test 1
       from this task's `<behavior>` above: construct a session via `new AgdaSession(mkdtempSync(join(
       tmpdir(), "agda-mcp-manifest-repo-")))` (matching this file's existing repoRoot pattern), manually
       set `session.lastDispatchedLoadArgv = ["--stale-flag"]`, build a `deadProc` object literal typed
       `as unknown as ChildProcess` with `exitCode: 1, signalCode: null, killed: false`, call
       `resetFileBoundStateIfProcDied(session, deadProc)`, and assert
       `session.lastDispatchedLoadArgv` equals `[]`.
    6. Add a new `it(...)` test (using the file's existing `agdaAvailable`/`RUN_AGDA_INTEGRATION`-gated
       `it` already defined near line 59) implementing Test 2 from this task's `<behavior>` above.
  </action>
  <acceptance_criteria>
    - src/agda/session.ts's `loadNoMetas` method body contains `this.lastDispatchedLoadArgv = [];` as
      its first statement
    - src/agda/session-process-lifecycle.ts's `resetFileBoundStateIfProcDied` function body contains
      `session.lastDispatchedLoadArgv = [];`
    - src/agda/session-load-helpers.ts's `invalidatePriorLoadState` function body does NOT contain
      `lastDispatchedLoadArgv` (grep -c confirms 0 occurrences of that identifier inside the function
      body itself, only in the new explanatory comment above/around it)
    - `npx vitest run test/unit/agda/session-capture/manifest-builder.test.ts --reporter=dot` exits 0
      and the new fast (non-gated) test passes
    - `RUN_AGDA_INTEGRATION=1 npx vitest run test/unit/agda/session-capture/manifest-builder.test.ts --reporter=dot` exits 0 with the new loadNoMetas test showing as PASSED (not SKIPPED) in the reporter output — requires a local `agda` binary
  </acceptance_criteria>
  <verify>
    <automated>RUN_AGDA_INTEGRATION=1 npx vitest run test/unit/agda/session-capture/manifest-builder.test.ts --reporter=dot</automated>
  </verify>
  <done>A replay manifest built after a strict typecheck or a mid-command process death always reports the CURRENT session's argv (or [] when there truly is none), never a stale prior load's flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| MCP agent -> agda_capture_session -> .agda-mcp/captures/ filesystem | The staged-artifact write path must remain collision-proof and durable regardless of how many times, or how quickly, an agent calls the tool within one session |
| AgdaSession live process state -> ReplayManifest fields | `lastDispatchedLoadArgv` crosses from transient in-process state into a persisted, later cold-replayed artifact; staleness here silently corrupts the "ground truth" Phase 2's oracle triad will pin to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-06-01 | Tampering | src/tools/register-capture-session.ts staged artifact path | mitigate | Append a monotonic `${Date.now()}-${captureSequence++}` suffix to the staged filename so two captures can never resolve to the same path, closing the silent-overwrite defect (CR-03, Task 1) |
| T-06-02 | Repudiation | src/tools/register-capture-session.ts recorder drain/reset ordering | mitigate | Move `resetRecordedActions()` to run only after `writeFileAtomic` durably succeeds, so a write failure leaves the drained action log recoverable for a retry instead of silently discarding evidence of what happened in the session (WR-01, Task 1) |
| T-06-03 | Tampering | src/tools/register-capture-session.ts dedup fingerprint identity | mitigate | Fold `manifest.importClosureHash ?? session.currentFile ?? ""` into `fingerprintBugReport`'s `agdaCommandFamily` input so unrelated defects on different loaded files no longer collapse into (and silently overwrite, per T-06-01) the same dedup identity (WR-02, Task 2) |
| T-06-04 | Tampering | src/agda/session.ts + session-process-lifecycle.ts replay-manifest argv fidelity | mitigate | Reset `lastDispatchedLoadArgv` in `loadNoMetas()` and in `resetFileBoundStateIfProcDied` (never in `invalidatePriorLoadState`/`resetProcBoundState`, which would wipe a live `load()`'s correct value) so a stale prior load's flags can never be stamped into a manifest Phase 2 will treat as authoritative (WR-08, Task 3) |

</threat_model>

<verification>
Run `npx vitest run test/unit/tools/register-capture-session.test.ts --reporter=dot` after Task 1 and
again after Task 2.
Run `RUN_AGDA_INTEGRATION=1 npx vitest run test/unit/agda/session-capture/manifest-builder.test.ts --reporter=dot`
after Task 3 (requires a local `agda` binary; the loadNoMetas regression test is `test.skip`ped
otherwise — confirm from the reporter output that it PASSED, not SKIPPED).
Run `npm test` (add `RUN_AGDA_INTEGRATION=1` if a local Agda binary is available) once at the end of this
plan to confirm zero cross-cutting regressions across Plans 01-01..01-05 and this gap-closure plan.
Confirm `test ! -e test/fixtures/agda/.agda-mcp` after the full run — the shared fixture tree must never
be touched by this test file again.
</verification>

<success_criteria>
- Two agda_capture_session calls with an identical fingerprint/recurrence in one session stage two
  distinct, independently-readable files under .agda-mcp/captures/ (CR-03)
- A failed artifact write (mkdirSync/writeFileAtomic throwing) leaves the CAP-04 recorder buffer intact
  for a retry, verified by drainRecordedActions() still returning the pre-failure actions (WR-01)
- Two captures with different session.currentFile values route to different dedup fingerprints even when
  classification/note are identical (WR-02)
- register-capture-session.test.ts stages every capture into a per-test temp directory and never leaves
  an .agda-mcp/ directory under test/fixtures/agda/ (WR-12)
- session.loadNoMetas() and resetFileBoundStateIfProcDied both correctly reset lastDispatchedLoadArgv to
  [] so a replay manifest never reports a stale prior load's flags (WR-08)
- npm test is green with zero regressions across Plans 01-01..01-05 and this gap-closure plan
</success_criteria>

<output>
Create `.planning/phases/01-capture-foundation/01-06-SUMMARY.md` when done
</output>
