---
phase: 03-regression-lock-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - test/integration/mcp/capture-regression.test.ts
  - test/fixtures/capture-regression-matrix.json
  - test/fixtures/agda/FixtureDeps/TransitiveStaleness/Main.agda
  - test/fixtures/agda/FixtureDeps/TransitiveStaleness/Dep.agda
  - test/fixtures/agda/FixtureDeps/TransitiveStaleness/Dep.broken.agda
autonomous: true
requirements: [REPRO-01, LOCK-01, LOCK-03]

must_haves:
  truths:
    - "The generic replay runner (D-01's ONE runner, D-04's test.fails/test split) iterates whatever the capture-regression matrix contains — zero entries is a valid, green, zero-test run until the flagship task populates it"
    - "The #64/#61 flagship defect is captured through a REAL live session (two real agda_capture_session calls in one session — baseline then false-green — both durably retained thanks to Plan 03-01's collision fix), never a hand-built synthetic bundle (D-09 part 1)"
    - "The emitted flagship matrix entry's expected value is ORCL-01's own cold-replay tuple/categories for this exact capture, not a value invented by the planner or executor (LOCK-02)"
    - "RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts reports the flagship entry as a passing test.fails (its inner assertion currently fails against un-fixed runLoadNoMetas) — the concrete from-RED proof LOCK-03 requires"
    - "The matrix contains exactly one entry after this plan (the flagship); no CHG-derived specs or candidate defects are locked here (D-10 — deferred to Phase 4 queue intake)"
    - "REPRO-01's fixture is already minimal (2 healthy-state files, no filler content) and its 'still reproduces' check is Phase 2's existing warm-green-AND-cold-red differential, reused as-is via ORCL-01 inside the emitter — no new trimming heuristic or ddmin-style script is built (D-07 defers heuristics to v2)"
    - "This plan never modifies src/agda/session-load-impl.ts or any other src/ load-path file — fixing the underlying runLoadNoMetas terminus-tracking gap is explicitly Phase 3.1's job (D-09 part 2), not this phase's"
    - "The flagship matrix entry's entryFile/mutation.targetFile/mutation.sourceFile fields are bare filenames (\"Main.agda\", \"Dep.agda\", \"Dep.broken.agda\") — never fixtureDir-prefixed — confirming the 03-02 BLOCKER fix holds end-to-end against the REAL captured artifact (whose inlinedFirstPartySources ARE fixtureDir-prefixed), not just synthetic unit-test shapes"
  artifacts:
    - path: "test/integration/mcp/capture-regression.test.ts"
      provides: "The one generic replay runner (D-01), test.fails for red / test for locked (D-04)"
      exports: []
    - path: "test/fixtures/agda/FixtureDeps/TransitiveStaleness/Main.agda"
      provides: "Flagship fixture — the importing module"
      contains: "module FixtureDeps.TransitiveStaleness.Main where"
    - path: "test/fixtures/agda/FixtureDeps/TransitiveStaleness/Dep.agda"
      provides: "Flagship fixture — the healthy transitive dependency (replay's baseline state)"
      contains: "module FixtureDeps.TransitiveStaleness.Dep where"
    - path: "test/fixtures/agda/FixtureDeps/TransitiveStaleness/Dep.broken.agda"
      provides: "Flagship fixture — the mutation payload swapped in mid-replay"
      contains: "module FixtureDeps.TransitiveStaleness.Dep where"
    - path: "test/fixtures/capture-regression-matrix.json"
      provides: "The flagship's matrix entry (status: red), produced by the emitter, never hand-authored"
      contains: "issue-64-61-transitive-staleness"
  key_links:
    - from: "test/integration/mcp/capture-regression.test.ts"
      to: "test/helpers/capture-regression-runner.ts"
      via: "replayCaptureRegressionEntry import"
      pattern: "replayCaptureRegressionEntry"
    - from: "test/fixtures/capture-regression-matrix.json"
      to: "test/fixtures/agda/FixtureDeps/TransitiveStaleness"
      via: "entry.fixtureDir field"
      pattern: "FixtureDeps/TransitiveStaleness"
    - from: "test/integration/mcp/capture-regression.test.ts"
      to: "scripts/emit-regression.mjs"
      via: "matchesExpected import — the SAME D-05 self-check comparator, reused rather than re-implemented (Warning-2 fix)"
      pattern: "matchesExpected"
---

<objective>
Build the one generic capture-regression vitest runner, then drive the empirically-verified #64/#61 transitive-staleness scenario through a real live session, capture it twice (baseline + false-green), run the Wave-2 emitter against both captures, and confirm the resulting matrix entry is a genuine, currently-RED `test.fails` regression.

Purpose: This is LOCK-03's flagship proof — the single end-to-end demonstration that capture -> oracle -> emit produces a real, from-RED lock, not a theoretical pipeline. RESEARCH.md already validated this exact trigger sequence 6/6 and 5/5 against real Agda 2.8.0; this plan reproduces it through the actual shipped tools (not scratch scripts) and commits only the automated outputs (fixture trio + matrix entry + runner).
Output: A working `test/integration/mcp/capture-regression.test.ts`, plus the flagship fixture trio and its matrix entry — the phase's single closed-loop proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-regression-lock-pipeline/03-CONTEXT.md
@.planning/phases/03-regression-lock-pipeline/03-RESEARCH.md
@.planning/phases/03-regression-lock-pipeline/03-PATTERNS.md
</context>

<interfaces>
<!-- test/integration/mcp/mcp-server.test.ts:1-39 — the authoritative D-02/D-03 gating preamble to mirror exactly -->
```typescript
const agdaVersion = detectAgdaVersion();
const agdaAvailable = agdaVersion !== undefined;
const it = agdaAvailable && process.env.RUN_AGDA_INTEGRATION === "1" ? test : test.skip;
```

<!-- test/integration/agda/agda-fixture-matrix.test.ts:94-106 — the matrix-iteration for-loop shape to mirror -->
```typescript
for (const fixture of selectedFixtures()) {
  const runFixture = skipForVersion ? test.skip : it;
  runFixture(`${fixture.name}: ...`, async (ctx) => { /* ... */ });
}
```

<!-- test/helpers/capture-regression-runner.ts (Plan 03-02, Task 1) — the shared replay function this runner consumes -->
```typescript
export async function replayCaptureRegressionEntry(
  entry: CaptureRegressionEntry, fixturesRoot: string
): Promise<{ observed: { classification: string; success: boolean; goalCount: number; invisibleGoalCount: number; hasHoles: boolean; errorCategories: string[] } }>;
```

<!-- scripts/emit-regression.mjs (Plan 03-02, Task 3) — the shared D-05 comparator, reused by Task 1's runner (Warning-2 fix) -->
```typescript
export function matchesExpected(
  observed: { classification: string; success: boolean; goalCount: number; invisibleGoalCount: number; hasHoles: boolean; errorCategories: string[] },
  expected: { classification: string; success: boolean; goalCount: number; invisibleGoalCount: number; hasHoles: boolean; errorCategories: string[] },
): boolean;
```

<!-- scripts/emit-regression.mjs (Plan 03-02) — the CLI this task invokes for real -->
```
npx tsx scripts/emit-regression.mjs <primary-artifact.json> --baseline <baseline-artifact.json> --id <id> --issue <n,n> --fixture-dir <dir> --tool <name> --server-env KEY=VALUE [--server-env KEY=VALUE ...]
```

<!-- The empirically-verified flagship fixture content (03-RESEARCH.md "Code Examples", corrected to fully-dotted module names per 03-PATTERNS.md) -->
```
-- Dep.agda (healthy baseline)
module FixtureDeps.TransitiveStaleness.Dep where

data Nat : Set where
  zero : Nat
  suc  : Nat -> Nat

getValue : Nat
getValue = suc zero
```
```
-- Dep.broken.agda (mutation payload — same module name, spliced over Dep.agda mid-replay)
module FixtureDeps.TransitiveStaleness.Dep where

data Nat : Set where
  zero : Nat
  suc  : Nat -> Nat

data Bool : Set where
  true false : Bool

getValue : Bool
getValue = true
```
```
-- Main.agda
module FixtureDeps.TransitiveStaleness.Main where

open import FixtureDeps.TransitiveStaleness.Dep

_+_ : Nat -> Nat -> Nat
zero  + n = n
suc m + n = suc (m + n)

useValue : Nat
useValue = getValue
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generic capture-regression replay runner</name>
  <files>test/integration/mcp/capture-regression.test.ts</files>
  <read_first>
    test/integration/agda/agda-fixture-matrix.test.ts (full — matrix-iteration for-loop)
    test/integration/mcp/mcp-server.test.ts lines 1-39 (gating preamble)
    test/helpers/agda-version.ts (full, 11 lines — itSince)
    test/helpers/capture-regression-runner.ts (Plan 03-02, Task 1)
    test/fixtures/capture-regression-matrix.ts (Plan 03-01, Task 2)
    scripts/emit-regression.mjs (Plan 03-02, Task 3 — exports `matchesExpected(observed, expected): boolean`; import and reuse it here instead of re-implementing per-field `expect()` assertions, per Warning-2)
    .planning/phases/03-regression-lock-pipeline/03-PATTERNS.md ("capture-regression.test.ts" section, Analog 3 — test.fails)
  </read_first>
  <action>
    Create test/integration/mcp/capture-regression.test.ts. Gate exactly as mcp-server.test.ts does: `const agdaVersion = detectAgdaVersion(); const agdaAvailable = agdaVersion !== undefined; const it = agdaAvailable && process.env.RUN_AGDA_INTEGRATION === "1" ? test : test.skip;`. Define `const FIXTURES_ROOT = resolve(import.meta.dirname, "../../fixtures/agda");`. Import `matchesExpected` from `../../../scripts/emit-regression.mjs` (`@ts-expect-error script module lacks types`, mirroring `emit-regression.test.ts`'s own `.mjs` import style) — this is the SAME comparator the emitter's D-05 self-check uses at emit time (Plan 03-02, Task 3), so this runner and the emitter can never silently diverge on what counts as a match (Warning-2 fix). For each `entry` of `captureRegressionMatrix` (imported from `../../fixtures/capture-regression-matrix.js`): `const runEntry = entry.status === "red" ? it.fails : it;` then call `runEntry(\`${entry.id}: ${entry.tool} matches ORCL-01 cold expected value\`, async () => { const { observed } = await replayCaptureRegressionEntry(entry, FIXTURES_ROOT); expect(matchesExpected(observed, entry.expected)).toBe(true); });`. When the matrix is empty (its Plan 03-01 seed state), this file legitimately produces zero test cases — a passing, empty vitest run. That is the expected and correct state at the end of THIS task; Task 2 below populates the matrix for real.
  </action>
  <verify>
    <automated>npx vitest run test/integration/mcp/capture-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run test/integration/mcp/capture-regression.test.ts` exits 0 with 0 tests run (against the still-empty matrix); `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts` also exits 0 with 0 tests (confirms the gating/import machinery is wired correctly even with nothing to iterate); `grep -n "it.fails" test/integration/mcp/capture-regression.test.ts` confirms the `status === "red"` branch is wired to `test.fails`; `grep -n "matchesExpected" test/integration/mcp/capture-regression.test.ts` confirms the shared comparator is imported and used (never re-implemented inline).
  </acceptance_criteria>
  <done>The one generic runner exists and is wired to the matrix + shared replay helper + the shared matchesExpected comparator; ready to pick up whatever Task 2 appends.</done>
</task>

<task type="auto">
  <name>Task 2: Flagship end-to-end proof — capture, emit, and verify the #64/#61 RED lock</name>
  <files>test/fixtures/capture-regression-matrix.json, test/fixtures/agda/FixtureDeps/TransitiveStaleness/Main.agda, test/fixtures/agda/FixtureDeps/TransitiveStaleness/Dep.agda, test/fixtures/agda/FixtureDeps/TransitiveStaleness/Dep.broken.agda</files>
  <read_first>
    .planning/phases/03-regression-lock-pipeline/03-RESEARCH.md ("Central Research Question" and "Code Examples" sections in full — the empirically-verified 6/6 and 5/5 trigger sequence)
    .planning/phases/03-regression-lock-pipeline/03-PATTERNS.md (fixture-content dotted-module-name correction section)
    test/helpers/mcp-harness.ts (full — createMcpHarness/extraEnv)
    scripts/emit-regression.mjs (Plan 03-02 — the CLI to invoke; note it derives entryFile/mutation fields as BARE fixtureDir-relative paths internally, stripping any fixtureDir prefix from the captured artifact's inlinedFirstPartySources — no --entry-file flag exists or is needed)
    test/integration/mcp/capture-regression.test.ts (Task 1, just written — what will consume the entry this task produces)
    src/tools/register-capture-session.ts (post-BLOCKER-fix, Plan 03-01 Task 1 — confirms two captures per session are now safe)
  </read_first>
  <action>
    Write a throwaway driver script (NOT committed to the repo — write it to a scratch/tmp location such as the session scratchpad, run it, then delete it) that: (1) creates a fresh directory (`mkdtempSync`) laid out as `<dir>/FixtureDeps/TransitiveStaleness/{Main.agda,Dep.agda}`, seeded with the exact content shown in this plan's `<interfaces>` block (healthy `Dep.agda` + `Main.agda`) — fully-dotted module names, matching the FINAL committed layout exactly so the captured `inlinedFirstPartySources` paths need no renaming; (2) spawns ONE `createMcpHarness({ serverRepoRoot: <repo root>, projectRoot: <dir>, extraEnv: { AGDA_MCP_IDLE_COMPLETION_MS: "1", AGDA_MCP_POST_STATUS_IDLE_MS: "1", AGDA_MCP_CAPTURE: "1" } })` (all three env vars required: the first two are the fault-injection tuning that forces the completion-detection race deterministically; `AGDA_MCP_CAPTURE=1` is required so `recordedActions` is non-empty, or `findWarmLoadTuple` — and hence the whole oracle/emitter pipeline — would see nothing to diff against); (3) calls `agda_load_no_metas` with `file: "FixtureDeps/TransitiveStaleness/Main.agda"` and confirms the response is `ok-complete` (the healthy baseline); (4) calls `agda_capture_session` with a distinguishing `note` (e.g. `"flagship baseline"`) — this is the BASELINE capture; record its `data.stagedPath` (only safely distinguishable from the next capture below because of Plan 03-01 Task 1's collision fix, since both captures will report the identical `sessionClassification: "ok-complete"`); (5) overwrites the SAME session's on-disk `<dir>/FixtureDeps/TransitiveStaleness/Dep.agda` with the broken content shown in `<interfaces>`; (6) calls `agda_load_no_metas` again on `Main.agda` in the SAME harness/session, and prints the observed classification — confirm it reports `ok-complete`/`success: true` (the false green). If it does NOT reproduce on the first attempt, consult 03-RESEARCH.md's Pitfall 2 before concluding anything about the defect's status — do not weaken the tuned env values without first re-reading the empirical trigger sequence; (7) calls `agda_capture_session` again with a distinguishing note (e.g. `"flagship false-green"`) — this is the PRIMARY (defect) capture; record its `data.stagedPath`; (8) closes the harness.

    Then run, for real: `npx tsx scripts/emit-regression.mjs <primary-stagedPath> --baseline <baseline-stagedPath> --id issue-64-61-transitive-staleness --issue 64,61 --fixture-dir FixtureDeps/TransitiveStaleness --tool agda_load_no_metas --server-env AGDA_MCP_IDLE_COMPLETION_MS=1 --server-env AGDA_MCP_POST_STATUS_IDLE_MS=1` (no `--entry-file` flag — the emitter, per 03-02 Task 2/3's BLOCKER fix, derives `entryFile` itself from the artifact's own warm-load tuple, stripped of any `fixtureDir` prefix). Confirm it exits 0, prints the RED evidence (observed-vs-expected diff), and does NOT refuse — if it refuses, read the printed reason (naming the failing ORCL-01/ORCL-02 predicate) before retrying; do not bypass a refusal with `--force` for the flagship (a forced lock would violate D-06). Confirm afterward that `test/fixtures/agda/FixtureDeps/TransitiveStaleness/{Main.agda,Dep.agda,Dep.broken.agda}` now exist with fully-dotted module names AT EXACTLY THAT PATH — critically, verify there is NO nested `test/fixtures/agda/FixtureDeps/TransitiveStaleness/FixtureDeps/` directory (that nested duplication is exactly the BLOCKER 03-02 Tasks 2-3 fixed: since this session's captured `inlinedFirstPartySources[].path` values are themselves `FixtureDeps/TransitiveStaleness`-prefixed, a still-buggy emitter would double-prepend `fixtureDir` here). Also confirm the written matrix entry's `entryFile`, `mutation.targetFile`, and `mutation.sourceFile` are bare filenames (`"Main.agda"`, `"Dep.agda"`, `"Dep.broken.agda"` respectively — no `/` in any of the three), and that `test/fixtures/capture-regression-matrix.json` now contains exactly one entry with `id: "issue-64-61-transitive-staleness"` and `status: "red"`.

    (Forward-compat note, no code change needed this phase — record in the SUMMARY for future-phase awareness: `Dep.broken.agda`'s content deliberately declares the SAME module name as `Dep.agda` (`module FixtureDeps.TransitiveStaleness.Dep where`) because the replay runner's mutation step overwrites `Dep.agda`'s file CONTENT with `Dep.broken.agda`'s content in place — the swapped-in content's module name must match what `Main.agda`'s `open import` expects, so the shared module name is correct and required, not a bug. This does mean a future broad `buildImportGraph`/ORCL-02 scan that recursively walks the ENTIRE `test/fixtures/agda/` tree would find two files declaring the same module name — not a Phase-3 threat today since no current test performs such a scan, and the RED mechanism here never depends on `buildImportGraph`/module-name uniqueness — only on path-based file content swapping.)

    Finally run `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts` and confirm it reports 1 passed test — the `test.fails`-wrapped flagship entry passing BECAUSE its inner assertion correctly fails against the live, unfixed `runLoadNoMetas`. This is the concrete, observable proof LOCK-03 requires. Delete the throwaway driver script and its scratch mkdtempSync directory; do not commit them or leave residue outside the 5 files listed in this plan's `files_modified`.
  </action>
  <verify>
    <automated>RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `git status --porcelain test/fixtures/agda/FixtureDeps/TransitiveStaleness/` shows exactly 3 new files; `test ! -d test/fixtures/agda/FixtureDeps/TransitiveStaleness/FixtureDeps` succeeds (no doubled-nesting from the BLOCKER); a Node one-liner confirms the matrix has exactly 1 entry AND its `entryFile`/`mutation.targetFile`/`mutation.sourceFile` are all bare (no `/`) — e.g. `node -e "const e=JSON.parse(require('fs').readFileSync('test/fixtures/capture-regression-matrix.json','utf8'))[0]; const bare=[e.entryFile,e.mutation.targetFile,e.mutation.sourceFile]; console.log(bare.join(' ')); process.exit(bare.some(p=>p.includes('/'))?1:0)"` prints `Main.agda Dep.agda Dep.broken.agda` and exits `0`; `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts` exits 0 with `1 passed`; `grep -l 'module FixtureDeps.TransitiveStaleness' test/fixtures/agda/FixtureDeps/TransitiveStaleness/*.agda | wc -l` reports `3` (all three files use the fully-dotted module name); `git diff --stat -- src/` is empty (this task never touches `src/`).
  </acceptance_criteria>
  <done>The #64/#61 flagship is a genuine, currently-RED, test.fails-protected vitest regression, produced end-to-end through the real capture -> oracle -> emit pipeline exactly as LOCK-03 requires, with correctly bare (non-fixtureDir-prefixed) entryFile/mutation fields throughout; Phase 3.1 has a concrete RED test to flip GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matrix entry (`fixtureDir`/`entryFile`/`mutation` fields) → `capture-regression-runner.ts` | Drives which files get copied/overwritten inside an isolated tmpdir at replay time |
| Throwaway rehearsal driver's tuned `serverEnv` → spawned MCP server process | Pre-existing, shipped, documented production tunables, used here as principled fault injection against a local harness only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | `test/helpers/capture-regression-runner.ts`'s isolated copy + mutation swap (consumed by Task 1's runner) | mitigate | `resolveFileWithinRoot` applied to `fixtureDir`/`entryFile`/`mutation.targetFile`/`mutation.sourceFile` against both the source (`test/fixtures/agda/`) and destination (`mkdtempSync` tmpdir) roots before any read/copy/write (inherited from Plan 03-02 Task 1) — never mutate the checked-in fixture tree in place |
| T-03-03-02 | Tampering | Rehearsal driver's tuned `AGDA_MCP_IDLE_COMPLETION_MS`/`AGDA_MCP_POST_STATUS_IDLE_MS`/`AGDA_MCP_CAPTURE` | accept | Pre-existing, documented, shipped production tunables (RESEARCH.md); used here as principled fault injection against a throwaway local harness, never exposed to an untrusted caller |
| T-03-03-SC | Tampering | npm/pip/cargo installs | N/A | No new package-manager installs in this phase |
</threat_model>

<verification>
- `npx vitest run test/integration/mcp/capture-regression.test.ts` — 0 tests before Task 2, exactly 1 passed after Task 2.
- `RUN_AGDA_INTEGRATION=1 npm run build && RUN_AGDA_INTEGRATION=1 npm test` — full suite green (the flagship's `test.fails` counts as a pass while the defect is live).
- Manual confirmation the flagship entry is currently RED (not accidentally already passing) before closing Phase 3.
</verification>

<success_criteria>
- The generic runner exists and correctly no-ops against an empty matrix.
- The #64/#61 flagship is captured via a real live session, emitted via the real emitter, and proven RED via the real runner — REPRO-01, LOCK-01, and LOCK-03 are all satisfied by one concrete artifact set.
- Exactly one matrix entry exists; nothing beyond the flagship is locked (D-10 honored).
</success_criteria>

<output>
Create `.planning/phases/03-regression-lock-pipeline/03-03-SUMMARY.md` when done
</output>
