---
phase: 03-regression-lock-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/register-capture-session.ts
  - test/unit/tools/register-capture-session.test.ts
  - test/fixtures/capture-regression-matrix.ts
  - test/fixtures/capture-regression-matrix.json
  - test/unit/fixtures/capture-regression-matrix.test.ts
autonomous: true
requirements: [REPRO-01, LOCK-02]

must_haves:
  truths:
    - "Two agda_capture_session calls in the same session — even when both share the identical fingerprint/recurrence (e.g. two 'ok-complete'-classified captures, exactly the flagship's baseline-then-false-green shape) — never collide on stagedPath; both artifacts remain durably readable afterward (closes 01-VERIFICATION.md's CR-03 BLOCKER; hard prerequisite for D-09 part1's two-capture flagship rehearsal in 03-03)"
    - "The capture-regression matrix (D-01) loads through a zod-validated typed loader identical in idiom to release-bug-matrix.ts/fixture-matrix.ts; a malformed or schema-violating entry is rejected at load time, never silently coerced"
    - "Every field in the matrix's expected object is exactly D-03's normalized classification tuple + error/warning category set (classification, success, goalCount, invisibleGoalCount, hasHoles, errorCategories) — schema-enforced so no future entry can assert on raw text, wire order, or timing instead"
    - "The matrix schema's optional serverEnv and mutation fields are additive only — they extend the NEW capture-regression matrix, never Phase-1's ReplayManifest or Phase-2's verdict schema (RESEARCH.md Pitfall 4 / the additive-schema-gap finding)"
    - "REPRO-01's 'still reproduces' / re-trigger predicate is satisfied by REUSE of Phase 2's existing judgeOrcl01/runColdLoadAndDiff differential (warm-green AND cold-red) — this plan builds no new trimming heuristic, ddmin script, or re-trigger checker (D-07 defers heuristics to v2; the predicate itself is reuse, not new construction)"
  artifacts:
    - path: "src/tools/register-capture-session.ts"
      provides: "Collision-proof staged capture filenames"
      contains: "stagedFileSequence"
    - path: "test/fixtures/capture-regression-matrix.ts"
      provides: "Zod-validated typed loader for the capture-regression matrix (D-01 idiom)"
      exports: ["captureRegressionEntrySchema", "captureRegressionMatrix"]
    - path: "test/fixtures/capture-regression-matrix.json"
      provides: "Matrix SSOT, seeded empty — Wave 3 appends the flagship entry via the emitter, never by hand"
      contains: "[]"
  key_links:
    - from: "test/fixtures/capture-regression-matrix.ts"
      to: "test/helpers/json-data.ts"
      via: "loadValidatedJsonData(import.meta.dirname, ...)"
      pattern: "loadValidatedJsonData"
    - from: "src/tools/register-capture-session.ts"
      to: "stagedPath construction"
      via: "per-process monotonic sequence counter appended to the filename"
      pattern: "stagedFileSequence"
---

<objective>
Close the Phase-1 capture-staging BLOCKER (silent overwrite of a same-session, same-fingerprint capture — 01-VERIFICATION.md CR-03) and define the capture-regression matrix's typed contract (D-01), so Wave 2 (emitter) and Wave 3 (replay runner + flagship rehearsal) have both a safe capture substrate and a fixed schema to code against.

Purpose: The flagship #64/#61 rehearsal (03-03) will capture the SAME session twice — once at the healthy baseline, once at the false-green moment — and both captures report the identical "ok-complete" classification, which is exactly the collision shape CR-03 describes. Without this fix, the second capture silently destroys the first's data with no error. The matrix contract must exist before any code writes to or reads from it.
Output: A collision-proof `agda_capture_session`, plus `test/fixtures/capture-regression-matrix.{ts,json}` and its unit test, seeded empty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-regression-lock-pipeline/03-CONTEXT.md
@.planning/phases/03-regression-lock-pipeline/03-RESEARCH.md
@.planning/phases/03-regression-lock-pipeline/03-PATTERNS.md
@.planning/phases/01-capture-foundation/01-VERIFICATION.md
</context>

<interfaces>
<!-- Current (buggy) stagedPath construction — src/tools/register-capture-session.ts:162-168 -->
```typescript
const captureDir = join(repoRoot, ".agda-mcp", "captures");
mkdirSync(captureDir, { recursive: true });
const stagedPath = join(
  captureDir,
  `${dedup.fingerprint}-${dedup.recurrence}.json`,
);
await writeFileAtomic(stagedPath, JSON.stringify(artifact, null, 2));
```
Nothing else in the codebase parses this filename to recover `fingerprint`/`recurrence` — `promote-capture.mjs` and `dedup-index.ts` both read those fields from the artifact's own JSON `dedup` object, never from the filename. Changing the filename format is safe and fully additive.

<!-- Matrix + typed-loader idiom to copy exactly — test/fixtures/release-bug-matrix.ts (full file) -->
```typescript
import { z } from "zod";
import { loadValidatedJsonData } from "../helpers/json-data.js";

const releaseBugEntrySchema = z.object({ /* ... */ });
export type ReleaseBugEntry = z.infer<typeof releaseBugEntrySchema>;
export const releaseBugMatrix: ReleaseBugEntry[] = loadValidatedJsonData(
  import.meta.dirname,
  "./release-bug-matrix.json",
  z.array(releaseBugEntrySchema),
);
```

<!-- test/helpers/json-data.ts (full file, 12 lines) -->
```typescript
export function loadJsonData(path: string): unknown {
  return JSON.parse(readFileSync(path, "utf8"));
}
export function loadValidatedJsonData<T>(moduleDir: string, relativePath: string, schema: ZodType<T>): T {
  return schema.parse(loadJsonData(resolve(moduleDir, relativePath)));
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the Phase-1 capture-staging collision (BLOCKER)</name>
  <files>src/tools/register-capture-session.ts, test/unit/tools/register-capture-session.test.ts</files>
  <read_first>
    src/tools/register-capture-session.ts (full — the fix target, 207 lines)
    test/unit/tools/register-capture-session.test.ts (full — existing test file to extend, 197 lines)
    .planning/phases/01-capture-foundation/01-VERIFICATION.md (CR-03 gap description, "Independently Re-Verified Critical Findings" table, "Behavioral Spot-Checks" table)
    src/agda/session-capture/dedup-index.ts (full — confirms fingerprint/recurrence come from artifact JSON, never the filename)
  </read_first>
  <behavior>
    - Two `agda_capture_session` calls in one session with IDENTICAL `dedup.fingerprint`/`dedup.recurrence` (both `new-bug`/`1` — the CR-03 repro shape) MUST produce two DIFFERENT `stagedPath` values.
    - Both staged files MUST exist and be independently readable after the second call (the first's `capturedAt` must not equal the second's, proving no overwrite).
  </behavior>
  <action>
    In register-capture-session.ts, add a module-level monotonic counter declared near the top-level imports/constants (e.g. `let stagedFileSequence = 0;`), and change the `stagedPath` construction to append it before `.json`: filename becomes `${dedup.fingerprint}-${dedup.recurrence}-${stagedFileSequence++}.json`. This makes every capture in a process produce a structurally-unique path regardless of whether fingerprint/recurrence repeat — exactly the scenario CR-03 reproduced (two same-session captures both classified "ok-complete", which is precisely what the Plan 03-03 flagship rehearsal's baseline-then-false-green sequence will do, since BOTH loads report "ok-complete" — one truly, one falsely). Do not touch `dedup.fingerprint`/`dedup.recurrence` themselves, `scripts/promote-capture.mjs`, or `dedup-index.ts` — confirmed nothing parses the staged filename to recover those fields. Leave `resetRecordedActions()` ordering (01-VERIFICATION.md's WR-01, a separate lower-severity warning about write-failure edge cases) untouched — it is not required for this phase's flagship rehearsal to succeed and is explicitly out of scope here; note this in the SUMMARY as an intentionally-deferred, orthogonal item for a future Phase-1 gap-closure pass, not silently dropped.

    Extend test/unit/tools/register-capture-session.test.ts with one new test (same file, same `makeCapturingServer()`/`AgdaSession` pattern the existing two tests use): construct one zero-interaction session, register the tool once, call `server.get("agda_capture_session")!.callback({})` twice in a row (no `note`, so both calls share the identical fingerprint/recurrence — `new-bug`/`1`). Assert `data1.stagedPath !== data2.stagedPath`; assert `existsSync(data1.stagedPath)` and `existsSync(data2.stagedPath)` are both true; assert `JSON.parse(readFileSync(data1.stagedPath, "utf8")).capturedAt !== JSON.parse(readFileSync(data2.stagedPath, "utf8")).capturedAt` (proves the first artifact survived, not just that a file exists at both paths).
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/register-capture-session.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run test/unit/tools/register-capture-session.test.ts` passes with 3 tests (the 2 pre-existing + the new collision test); `wc -l src/tools/register-capture-session.ts` stays under 500; `grep -n "stagedFileSequence" src/tools/register-capture-session.ts` shows both the declaration and its use in the `stagedPath` template literal.
  </acceptance_criteria>
  <done>Two same-session, same-fingerprint captures produce two distinct, both-durable staged files; full test file green.</done>
</task>

<task type="auto">
  <name>Task 2: Define the capture-regression matrix contract (D-01 idiom)</name>
  <files>test/fixtures/capture-regression-matrix.ts, test/fixtures/capture-regression-matrix.json, test/unit/fixtures/capture-regression-matrix.test.ts</files>
  <read_first>
    test/fixtures/release-bug-matrix.ts (full — the exact idiom to copy)
    test/fixtures/release-bug-matrix.json (full — sibling data file shape)
    test/fixtures/agda/fixture-matrix.ts (full — shows the `.optional()`/regex-validated field idiom)
    test/unit/reporting/release-bug-matrix.test.ts (full — the unit-test structure to mirror)
    test/helpers/json-data.ts (full, 12 lines — `loadValidatedJsonData`, do not reimplement)
    .planning/phases/03-regression-lock-pipeline/03-RESEARCH.md (Pattern 1, and Pitfall 4 "the serverEnv/tuned-timing technique isn't part of the standard ReplayManifest")
  </read_first>
  <action>
    Create test/fixtures/capture-regression-matrix.ts exporting a zod schema `captureRegressionEntrySchema` and a typed constant `captureRegressionMatrix`, loaded via `loadValidatedJsonData(import.meta.dirname, "./capture-regression-matrix.json", z.array(captureRegressionEntrySchema))` — same idiom as release-bug-matrix.ts. Schema fields: `id: z.string().min(1)` (unique matrix key); `issue: z.array(z.number().int().positive())` (defaults conceptually to `[]`, but required as a present array); `status: z.enum(["red", "locked"])` (D-04); `tool: z.string().min(1)` (e.g. `"agda_load_no_metas"`); `fixtureDir: z.string().min(1)` (relative to `test/fixtures/agda/`, e.g. `"FixtureDeps/TransitiveStaleness"`, no leading slash); `entryFile: z.string().min(1)` (relative to `fixtureDir`, the file passed to `tool`); `mutation: z.object({ targetFile: z.string().min(1), sourceFile: z.string().min(1) }).optional()` (both relative to `fixtureDir` — when present, the replay runner loads `entryFile` once, then overwrites `targetFile`'s content with `sourceFile`'s content, then reloads `entryFile`; this is the recorded "trigger sequence" REPRO-01 requires); `serverEnv: z.record(z.string(), z.string()).optional()` (threaded into `createMcpHarness`'s `extraEnv` — additive-only, never touches Phase-1's `ReplayManifest`); `expected: z.object({ classification: z.string(), success: z.boolean(), goalCount: z.number().int().nonnegative(), invisibleGoalCount: z.number().int().nonnegative(), hasHoles: z.boolean(), errorCategories: z.array(z.string()) })` (D-03's normalized classification tuple + category set — ORCL-01's own cold-result shape, never raw text).

    Create test/fixtures/capture-regression-matrix.json seeded as an empty array `[]` — Wave 3's flagship task appends the real entry by RUNNING the emitter against a real capture; never hand-author a matrix entry directly (that would violate D-09 part1's "no hand-built synthetic bundles").

    Create test/unit/fixtures/capture-regression-matrix.test.ts (mirror test/unit/reporting/release-bug-matrix.test.ts's structure): assert `captureRegressionMatrix` is an array and parses successfully even when empty; assert every `entry.id` in the matrix is unique (`new Set(ids).size === ids.length`); for every non-empty entry, assert `existsSync(resolve(REPO_ROOT, "test/fixtures/agda", entry.fixtureDir, entry.entryFile))`, and when `entry.mutation` is present assert both `resolve(REPO_ROOT, "test/fixtures/agda", entry.fixtureDir, entry.mutation.targetFile)` and `...sourceFile` resolve to existing files; assert every string in `entry.expected.errorCategories` contains no whitespace (a bare category tag, never a raw message — LOCK-02's "normalized envelope, never raw text" requirement).
  </action>
  <verify>
    <automated>npx vitest run test/unit/fixtures/capture-regression-matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run test/unit/fixtures/capture-regression-matrix.test.ts` passes against the empty-array seed; `cat test/fixtures/capture-regression-matrix.json` prints exactly `[]`; `npm run build` compiles cleanly with the new schema file (confirms zod v4 typings are satisfied).
  </acceptance_criteria>
  <done>Matrix contract exists, loads, and validates; empty seed passes its own test; Wave 2/3 have a fixed schema to code against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture-regression-matrix.json (on-disk data) → test/fixtures/capture-regression-matrix.ts loader | JSON parsed and used later (Wave 2/3) to derive filesystem paths and drive test execution — must be schema-validated at load time, not trusted verbatim |
| register-capture-session.ts staged filename → local filesystem | Purely local, single-user correctness fix (no new external input) — included for audit-trail integrity, not because it is attacker-reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | test/fixtures/capture-regression-matrix.ts loader | mitigate | zod schema validation via `loadValidatedJsonData`; a malformed/unexpected-shape JSON throws at load time rather than being silently coerced or partially trusted |
| T-03-01-02 | Repudiation | src/tools/register-capture-session.ts stagedPath collision | mitigate | Append a per-process monotonic sequence counter to the staged filename so two same-session captures can never silently overwrite each other's audit record (closes 01-VERIFICATION.md CR-03) |
| T-03-01-SC | Tampering | npm/pip/cargo installs | N/A | No new package-manager installs in this task (RESEARCH.md: Package Legitimacy Audit not applicable to Phase 3) |
</threat_model>

<verification>
- `npx vitest run test/unit/tools/register-capture-session.test.ts test/unit/fixtures/capture-regression-matrix.test.ts` — both files green.
- `npm run build` — compiles with no type errors.
</verification>

<success_criteria>
- Repeat same-session captures never collide on disk.
- The capture-regression matrix contract is defined, loads (even empty), and is validated by its own unit test.
- No plan or task beyond this one needs to touch the matrix schema shape again.
</success_criteria>

<output>
Create `.planning/phases/03-regression-lock-pipeline/03-01-SUMMARY.md` when done
</output>
