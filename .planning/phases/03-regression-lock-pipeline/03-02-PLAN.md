---
phase: 03-regression-lock-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - test/helpers/capture-regression-runner.ts
  - test/unit/tools/capture-regression-runner.test.ts
  - scripts/emit-regression.mjs
  - test/unit/tools/emit-regression.test.ts
autonomous: true
requirements: [LOCK-01, LOCK-02]

must_haves:
  truths:
    - "A single shared replay function (test/helpers/capture-regression-runner.ts) drives the MCP tool-call boundary (D-02) for a matrix entry — the emitter's D-05 self-check and the Wave-3 vitest runner both call this SAME function; neither duplicates MCP-harness-driving or mutation logic"
    - "The emitter (scripts/emit-regression.mjs) refuses to lock a capture when ORCL-01 is inconclusive, ORCL-02 is cheat-flagged, or ORCL-02 is no-policy with non-empty findings (D-06) — refusals exit non-zero and name the failing predicate"
    - "The emitter never golden-masters a capture whose observed replay already matches the proposed expected value — D-05's RED self-check runs before the matrix write is finalized, rolling back any already-materialized fixture files; a passing (non-RED) self-check is an emitter error, not a lock"
    - "Fixture materialization into the TRACKED test/fixtures/agda/ tree is path-sandboxed via resolveFileWithinRoot/PathSandboxError (D-08) — the same primitive materializeCaptureEnvironment already uses for tmpdir writes, applied here to a NEW containment site"
    - "The emitter emits data (a matrix entry), never a generated standalone .test.ts per defect (D-01); the emitted expected value is copied verbatim from ORCL-01's own cold tuple/categories, never re-derived or invented"
    - "The emitter derives entry.entryFile and mutation.targetFile/mutation.sourceFile as BARE paths relative to fixtureDir — even when the captured inlinedFirstPartySources[].path values are themselves fixtureDir-prefixed (the real flagship capture shape, since the capturing session's projectRoot contains fixtureDir as a subdirectory) — so materialized fixture writes never double-prepend fixtureDir and every downstream consumer (Task 1's replayCaptureRegressionEntry, Wave-3's runner) resolves the exact bare paths the matrix schema already documents"
    - "matchesExpected — the single field-by-field/array-equality comparator the emitter's D-05 self-check uses to decide RED-vs-not — is exported from scripts/emit-regression.mjs and reused verbatim by Wave-3's vitest runner, so the two can never independently drift on what counts as a match"
  artifacts:
    - path: "test/helpers/capture-regression-runner.ts"
      provides: "Shared isolated-copy + drive-tool + optional-mutate-reload + normalize replay mechanics"
      exports: ["replayCaptureRegressionEntry"]
    - path: "scripts/emit-regression.mjs"
      provides: "The regression-test emitter: refusal gate, fresh oracle run, baseline-diff-aware materialization with fixtureDir-relative bare-path derivation, matrix write, RED self-check via the shared matchesExpected comparator, CLI"
      exports: ["judgeRefusal", "materializeFixtureFiles", "composeEntry", "writeMatrixEntry", "matchesExpected", "scriptMain"]
  key_links:
    - from: "scripts/emit-regression.mjs"
      to: "scripts/oracle/run-oracle.mjs"
      via: "runOracle(artifactPath) import, called fresh at emit time"
      pattern: "runOracle\\("
    - from: "scripts/emit-regression.mjs"
      to: "test/helpers/capture-regression-runner.ts"
      via: "replayCaptureRegressionEntry import for the D-05 self-check"
      pattern: "replayCaptureRegressionEntry"
    - from: "scripts/emit-regression.mjs"
      to: "src/repo-root.ts"
      via: "resolveFileWithinRoot for every fixture write into test/fixtures/agda/"
      pattern: "resolveFileWithinRoot"
    - from: "scripts/emit-regression.mjs"
      to: "scripts/oracle/orcl-01-differential.mjs"
      via: "findWarmLoadTuple(primaryArtifact) import, used to derive the bare entryFile"
      pattern: "findWarmLoadTuple"
---

<objective>
Build the shared replay mechanics helper and the regression-test emitter — the two pieces LOCK-01/LOCK-02 require: a path-sandboxed materializer that turns a captured defect into a fixture under `test/fixtures/agda/`, and a refusal-gated emitter that writes exactly one capture-regression matrix entry only after demonstrating RED (D-05).

Purpose: This is the composition core of the phase. Every mechanical piece (materializer, differential, verdict schema, MCP harness) already exists in Phase 1/2 — this plan wires them together into the one new artifact type (a matrix entry) Phase 3 needs, per RESEARCH.md's own framing ("the main engineering work is wiring, not invention").
Output: `test/helpers/capture-regression-runner.ts` (shared replay function) and `scripts/emit-regression.mjs` (the complete emitter), both fully unit-tested against synthetic artifacts — no real capture exists yet (that is Wave 3's job).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-regression-lock-pipeline/03-CONTEXT.md
@.planning/phases/03-regression-lock-pipeline/03-RESEARCH.md
@.planning/phases/03-regression-lock-pipeline/03-PATTERNS.md
@.planning/phases/02-the-oracle-triad-server-faithfulness-soundness-hygiene-confo/02-CONTEXT.md
</context>

<interfaces>
<!-- test/fixtures/capture-regression-matrix.ts (Plan 03-01, Task 2) — the fixed contract this plan codes against -->
```typescript
export interface CaptureRegressionEntry {
  id: string;
  issue: number[];
  status: "red" | "locked";
  tool: string;
  fixtureDir: string;   // relative to test/fixtures/agda/
  entryFile: string;    // relative to fixtureDir
  mutation?: { targetFile: string; sourceFile: string }; // both relative to fixtureDir
  serverEnv?: Record<string, string>;
  expected: {
    classification: string; success: boolean; goalCount: number;
    invisibleGoalCount: number; hasHoles: boolean; errorCategories: string[];
  };
}
```

<!-- scripts/oracle/run-oracle.mjs — exported entry point, ALREADY reads/writes verdict sidecar + metrics -->
```javascript
export async function runOracle(artifactPath, options = {}): Promise<{
  schemaVersion: 1; capturePath: string; fingerprint: string; recurrence: number;
  computedAt: string; orcl01: {...}; orcl02: {...}; orcl03: {...};
  consistencyProbe: { attempted: false }; trueGreen: boolean;
}>
```

<!-- scripts/oracle/orcl-01-differential.mjs:355-523 — runColdLoadAndDiff's outcome shapes -->
```javascript
// { kind: "pass" }
// { kind: "server-false-green-candidate", warmTuple, coldTuple, warmCategories, coldCategories }
// { kind: "inconclusive", probe: string, detail: string }
```
`coldTuple` fields: `success, goalCount, invisibleGoalCount, hasHoles, classification`. `coldCategories` is the sorted, deduplicated array from `categorySet(...)`.

<!-- scripts/oracle/orcl-02-soundness-scan.mjs:539-562 — judgeOrcl02's exact return shapes to gate on -->
```javascript
// { kind: "no-target", reason }
// { kind: "no-policy", findings }       // findings.length > 0 means D-06 must refuse
// { kind: "clean", findings }
// { kind: "cheat-flagged", findings }   // D-06 must always refuse
```

<!-- src/repo-root.ts:69-76 — the sandbox primitive, reuse directly -->
```typescript
export function resolveFileWithinRoot(projectRoot: string, targetPath: string): string; // throws PathSandboxError on escape
```

<!-- src/session/safe-source-io.ts:157-173 — writeFileAtomic, reuse for the matrix JSON write -->
```typescript
export async function writeFileAtomic(filePath: string, content: string): Promise<void>;
```

<!-- test/helpers/mcp-harness.ts — the replay vehicle, extraEnv already supported -->
```typescript
export async function createMcpHarness(opts: { serverRepoRoot?: string; repoRoot?: string; projectRoot?: string; extraEnv?: Record<string,string> }): Promise<{
  callTool(name: string, args?: Record<string, unknown>): Promise<any>;
  close(): Promise<void>;
  getStderr(): string;
}>
```

<!-- scripts/oracle/orcl-01-differential.mjs:250-260 — reuse verbatim, never re-derive -->
```javascript
export function extractErrorCategory(message): string;
export function categorySet(messages): string[];  // sorted, deduplicated
```

<!-- scripts/oracle/orcl-01-differential.mjs:269-295 — findWarmLoadTuple, needed to derive entryFile (BLOCKER fix) -->
```javascript
export function findWarmLoadTuple(artifact): {
  file: string; // the repoRoot-relative loaded path
  tuple: { success: boolean; goalCount: number; invisibleGoalCount: number; hasHoles: boolean; classification: string };
  categories: string[];
} | null;
```

<!-- ROOT CAUSE CONTEXT (cross-plan BLOCKER) — src/agda/session-capture/manifest-builder.ts + src/agda/import-graph.ts -->
<!-- buildReplayManifest(session) -> inlineFirstPartySources(session.repoRoot, currentFile, ...) -> computeImpact() -->
<!-- records EVERY inlinedFirstPartySources[].path as `relative(session.repoRoot, absPath)` (import-graph.ts:299,311). -->
<!-- session.repoRoot is whatever `projectRoot` the capturing harness/session was constructed with. Wave-3's flagship -->
<!-- driver sets that `projectRoot` to an OUTER mkdtemp root that CONTAINS `FixtureDeps/TransitiveStaleness/` as a -->
<!-- subdirectory (matching the FINAL committed layout) — so every captured path is ALREADY fixtureDir-prefixed, e.g. -->
<!-- "FixtureDeps/TransitiveStaleness/Main.agda". materializeFixtureFiles (Task 2 below) MUST strip this prefix before -->
<!-- using a path as a bare entryFile/mutation field or as a write-target's trailing segment — never use it verbatim. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Shared replay mechanics helper</name>
  <files>test/helpers/capture-regression-runner.ts, test/unit/tools/capture-regression-runner.test.ts</files>
  <read_first>
    test/helpers/mcp-harness.ts (full, 98 lines)
    test/helpers/isolated-agda-dir.ts (full, 29 lines — mkdtempSync/try-finally/rmSync idiom to mirror)
    test/fixtures/capture-regression-matrix.ts (Plan 03-01, Task 2 — the CaptureRegressionEntry contract)
    src/repo-root.ts (full — resolveFileWithinRoot/PathSandboxError)
    scripts/oracle/orcl-01-differential.mjs lines 250-260 (categorySet/extractErrorCategory)
    test/fixtures/agda/CompleteFixture.agda (existing single-file, dependency-free fixture — optional content model for Test 1's OWN synthetic no-mutation fixture; Test 1 builds a small mkdtempSync'd single-file root rather than pointing replayCaptureRegressionEntry at the shared 139-file test/fixtures/agda/ tree, per Warning-5 cost concerns — do not point fixtureDir: "." at TEST_FIXTURE_PROJECT_ROOT)
    .planning/phases/03-regression-lock-pipeline/03-PATTERNS.md ("capture-regression.test.ts" section, "Isolation idiom" subsection)
  </read_first>
  <action>
    Create test/helpers/capture-regression-runner.ts exporting `async function replayCaptureRegressionEntry(entry: CaptureRegressionEntry, fixturesRoot: string): Promise<{ observed: { classification: string; success: boolean; goalCount: number; invisibleGoalCount: number; hasHoles: boolean; errorCategories: string[] } }>`. Implementation: `mkdtempSync` a fresh tmpdir (prefix `"agda-mcp-capture-regression-"`, mirroring isolated-agda-dir.ts's convention); recursively copy `resolveFileWithinRoot(fixturesRoot, entry.fixtureDir)`'s contents into `<tmpdir>/<entry.fixtureDir>` (preserve the subdirectory nesting — never flatten, per PATTERNS.md's explicit warning); construct the harness via `createMcpHarness({ serverRepoRoot: TEST_SERVER_REPO_ROOT, projectRoot: tmpdir, extraEnv: entry.serverEnv })` (import `TEST_SERVER_REPO_ROOT` from `../helpers/repo-root.js`); call `harness.callTool(entry.tool, { file: join(entry.fixtureDir, entry.entryFile) })`. If `entry.mutation` is present: resolve both `mutation.targetFile` and `mutation.sourceFile` against the TMPDIR copy (via `resolveFileWithinRoot(join(tmpdir, entry.fixtureDir), ...)`, never the original `fixturesRoot`), `writeFileSync` the target with the source's content, then call `harness.callTool` a second time on the same `entryFile` — this second result is authoritative. Extract `result.structuredContent.data` fields directly (`success`, `goalCount`, `invisibleGoalCount`, `hasHoles`, `classification`) plus `categorySet([...data.errors, ...data.warnings])` (imported from `../../scripts/oracle/orcl-01-differential.mjs`) for `errorCategories`. Always `harness.close()` and `rmSync(tmpdir, { recursive: true, force: true })` in a `finally` block regardless of outcome, so a thrown mid-replay error never leaks the harness process or the tmpdir.

    Create test/unit/tools/capture-regression-runner.test.ts (mirror the `RUN_AGDA_INTEGRATION`-gated style of test/unit/tools/oracle-orcl-01.test.ts): Test 1 (no mutation) — inside the test itself, `mkdtempSync` a small synthetic fixture ROOT containing exactly one trivially-complete, self-contained `.agda` file (e.g. a bare `module Complete where` plus one trivial definition — no imports, no holes, no errors; content can be lifted from `test/fixtures/agda/CompleteFixture.agda` or freely re-authored, either is fine since correctness only depends on it loading clean), build a synthetic `CaptureRegressionEntry` pointing `fixtureDir: "."`, `entryFile: "<that file's name>"`, `tool: "agda_load"`, call `replayCaptureRegressionEntry(entry, <the mkdtempSync root>)`, assert `observed.classification === "ok-complete"`. Deliberately do NOT point this test at `TEST_FIXTURE_PROJECT_ROOT` (the shared `test/fixtures/agda/` tree, 139 files) — `fixtureDir: "."` combined with that root would recursively copy the ENTIRE shared fixture tree into a tmpdir for a single-file replay; a synthetic single-file root keeps the same assertion coverage at a fraction of the I/O cost, and avoids the copy step depending on how many sibling fixtures the tree happens to accumulate over time. Test 2 (mutation round-trip) — inside the test itself, `mkdtempSync` a small synthetic fixture directory containing one trivially-complete `.agda` file and a second "broken" sibling file with a deliberate type error (both self-contained, no library deps), point a synthetic entry's `fixtureDir`/`entryFile`/`mutation` at them, call `replayCaptureRegressionEntry`, assert the observed classification flips from what a first load would show to `"type-error"` after the mutation is applied — this proves the copy-then-swap-then-reload mechanics work generically, without depending on the flagship fixture (which does not exist until Wave 3).
  </action>
  <verify>
    <automated>RUN_AGDA_INTEGRATION=1 npm run build && RUN_AGDA_INTEGRATION=1 npx vitest run test/unit/tools/capture-regression-runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Both tests pass under `RUN_AGDA_INTEGRATION=1`; without that env var both are skipped (never fail); `grep -n "resolveFileWithinRoot" test/helpers/capture-regression-runner.ts` shows at least 2 call sites (the fixtureDir copy source AND the mutation target/source, both sandboxed).
  </acceptance_criteria>
  <done>A single reusable, path-sandboxed replay function exists and is proven against real Agda for both the plain and mutation-driven replay shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The emitter — refusal gate, fresh oracle run, baseline-diff materialization</name>
  <files>scripts/emit-regression.mjs, test/unit/tools/emit-regression.test.ts</files>
  <read_first>
    scripts/oracle/run-oracle.mjs (full, 296 lines — runOracle's exported contract)
    scripts/oracle/verdict-schema.mjs (full, 91 lines — composeVerdict's kind enums, documented in the header comment)
    scripts/oracle/orcl-01-differential.mjs lines 85-131 (materializeCaptureEnvironment — the sandboxing pattern to mirror), lines 250-260 (extractErrorCategory/categorySet), and lines 262-295 (findWarmLoadTuple — returns `{ file, tuple, categories } | null`; `.file` is the repoRoot-relative loaded path, needed to derive `entryFile`)
    scripts/oracle/orcl-02-soundness-scan.mjs lines 539-562 (judgeOrcl02's exact return shapes)
    scripts/test-with-sentinel.mjs (full, 68 lines — isMainModule)
    src/repo-root.ts (full — resolveFileWithinRoot/PathSandboxError)
    src/agda/session-capture/manifest-builder.ts (full — confirms `inlinedFirstPartySources[].path` is recorded as `relative(session.repoRoot, absPath)` via `computeImpact`; when the harness's `projectRoot` CONTAINS `fixtureDir` as a subdirectory — as the Wave-3 flagship driver does — every captured path is ALREADY `fixtureDir`-prefixed, e.g. `"FixtureDeps/TransitiveStaleness/Main.agda"`. This is the root cause this task's stripping logic must handle.)
    test/unit/tools/oracle-orcl-01.test.ts lines 64-104, 120-135 (baseArtifact/writeArtifact synthetic-fixture pattern + the exact path-traversal test shape to mirror)
    .planning/phases/03-regression-lock-pipeline/03-PATTERNS.md ("scripts/emit-regression.mjs" section in full — header convention, refusal-gate logic, materializer contrast)
  </read_first>
  <behavior>
    - `judgeRefusal(verdict, options)` returns a refusal reason string when: `verdict.orcl01.kind === "inconclusive"` (reason names `verdict.orcl01.probe`); `verdict.orcl02.kind === "cheat-flagged"`; `verdict.orcl02.kind === "no-policy"` with `findings.length > 0`; or `verdict.orcl01.kind !== "server-false-green-candidate"` AND `options.force !== true` (nothing meaningful to lock).
    - `judgeRefusal` returns `null` (no refusal) only when `verdict.orcl01.kind === "server-false-green-candidate"` and neither ORCL-02 condition above holds.
    - `verdict.orcl03` is NEVER read by `judgeRefusal` (advisory, D-06).
  </behavior>
  <action>
    Create scripts/emit-regression.mjs. Export `judgeRefusal(verdict, options = {})` as a small, pure, independently-testable function implementing exactly the behavior above (this is the "task-level TDD" candidate — write its unit tests first, per the `<behavior>` block, then implement).

    Define a small internal (non-exported) helper `stripFixtureDirPrefix(path, fixtureDir)`: if `path === fixtureDir` or `path.startsWith(fixtureDir + "/")`, return `path.slice(fixtureDir.length + 1)`; otherwise return `path` unchanged. This is defensive-only stripping (some captures may already record bare paths) — never assume the prefix is present.

    **Fixes the cross-plan BLOCKER (fixtureDir double-prepending):** `primaryArtifact.manifest.inlinedFirstPartySources[].path` values are recorded relative to the LIVE SESSION's `repoRoot` (see `src/agda/session-capture/manifest-builder.ts`'s `buildReplayManifest` -> `inlineFirstPartySources` -> `computeImpact`'s `relative(projectRoot, absPath)`). When the capturing session's `projectRoot` CONTAINS `fixtureDir` as a subdirectory (as Wave-3's flagship driver does — the harness's `projectRoot` is an OUTER mkdtemp root, and the loaded file lives at `<root>/FixtureDeps/TransitiveStaleness/Main.agda`), every captured `path` is ALREADY `fixtureDir`-prefixed. The matrix schema (03-01 Task 2) requires `entryFile`/`mutation.targetFile`/`mutation.sourceFile` to be BARE (relative to `fixtureDir`), and every consumer (Task 1's `replayCaptureRegressionEntry`, Wave-3's runner) already assumes bare filenames — so this task must strip the prefix before using any captured `path` as a bare field, and before appending it under `test/fixtures/agda/<fixtureDir>/` (never double-prepend).

    Implement (same file, used by Task 3 below): `materializeFixtureFiles({ primaryArtifact, baselineArtifact, fixtureDir, repoRoot })` — reads `primaryArtifact.manifest.inlinedFirstPartySources`; if `baselineArtifact` is supplied, also reads its `inlinedFirstPartySources`. For each entry in the primary's sources, first compute `barePath = stripFixtureDirPrefix(entry.path, fixtureDir)`: if a baseline was supplied AND the SAME raw `entry.path` exists in the baseline's sources (compare raw, un-stripped paths — both artifacts share the same capturing session's `repoRoot`, so raw paths are directly comparable) AND its content differs from the primary's, this is the mutated file — write the BASELINE's content under `<repoRoot>/test/fixtures/agda/<fixtureDir>/<barePath>` and the PRIMARY's (differing) content under the same directory with `.broken` inserted before `barePath`'s file extension (e.g. bare `Dep.agda` -> `Dep.broken.agda`), and record `{ targetFile: barePath, sourceFile: bareBrokenPath }` (BOTH bare, never `fixtureDir`-prefixed) to return as the entry's `mutation`. If MORE than one path differs, throw with a message naming all differing (bare) paths (ambiguous — this emitter version supports exactly one mutated file, matching the flagship's verified shape; do not silently pick one). Every other (non-differing, or baseline-omitted) source is written verbatim once, using its OWN `barePath` under the same directory. EVERY write MUST go through `resolveFileWithinRoot(repoRoot, join("test/fixtures/agda", fixtureDir, barePath))` (note: `barePath`, never the raw prefixed `entry.path`) wrapped in try/catch for `PathSandboxError` — skip (never write) any entry whose path escapes, mirroring `materializeCaptureEnvironment`'s existing skip-on-traversal behavior (D-08's NEW containment site: the TRACKED repo tree, not a tmpdir). NEVER copy `scripts/verify-cold-replay.mjs`'s unfixed `materializeSources` — it has the CR-01 traversal hole.

    Also in `materializeFixtureFiles`: derive `entryFile` by calling `findWarmLoadTuple(primaryArtifact)` (imported from `./oracle/orcl-01-differential.mjs`) — if it returns non-null, `entryFile = stripFixtureDirPrefix(result.file, fixtureDir)`; if it returns `null` (no load-family recorded action — only possible with a degenerate/synthetic test artifact, since the real refusal gate in `scriptMain` already guarantees a load-family action exists for any artifact that reaches this function), leave `entryFile` as `undefined` rather than throwing (schema validation in `composeEntry`, Task 3, is the correct place to surface a missing-entryFile error). Return `{ mutation, entryFile }` from `materializeFixtureFiles` (not just `mutation`) — Task 3's `scriptMain` passes BOTH straight into `composeEntry`, with no CLI flag needed for `entryFile` (it is always derived here, from the artifact, never accepted as user input).

    Wire the fresh-oracle-run step: `import { runOracle } from "./oracle/run-oracle.mjs";` and call `await runOracle(primaryArtifactPath)` — never read or trust a pre-existing `.verdict.json` sidecar (Open Question 2's resolution: freshness matches D-05's own principle).

    Write test/unit/tools/emit-regression.test.ts (mirror oracle-orcl-01.test.ts's synthetic-artifact style, `@ts-expect-error script module lacks types` on the `.mjs` import): Test A — `judgeRefusal` returns a non-null reason naming the probe when `orcl01.kind: "inconclusive"`. Test B — non-null when `orcl02.kind: "cheat-flagged"`. Test C — non-null when `orcl02.kind: "no-policy"` with non-empty `findings`. Test D — non-null when `orcl01.kind: "pass"` and `options.force` is not set. Test E — `null` when `orcl01.kind: "server-false-green-candidate"` and `orcl02.kind: "clean"`. Test F — `materializeFixtureFiles` with a hand-crafted `inlinedFirstPartySources` entry whose `path` is `"../PWNED.agda"` never writes outside `test/fixtures/agda/` (mirror oracle-orcl-01.test.ts:120-135's exact assertion shape, pointed at a temp `repoRoot` fixture). Test G — `materializeFixtureFiles` throws when baseline and primary differ on 2+ paths. **Test G2 (BLOCKER regression — the real flagship shape)** — build a synthetic `primaryArtifact` whose `inlinedFirstPartySources` are `fixtureDir`-PREFIXED (e.g. `{ path: "FixtureDeps/TransitiveStaleness/Main.agda", content: "..." }` and `{ path: "FixtureDeps/TransitiveStaleness/Dep.agda", content: "healthy" }`) plus a `recordedActions` entry whose `normalizedResponse.data.file` is the same prefixed `"FixtureDeps/TransitiveStaleness/Main.agda"`, and a differing `baselineArtifact` with the same prefixed `Dep.agda` path but different content; call `materializeFixtureFiles({ primaryArtifact, baselineArtifact, fixtureDir: "FixtureDeps/TransitiveStaleness", repoRoot: <temp dir> })`; assert the write lands at exactly `<repoRoot>/test/fixtures/agda/FixtureDeps/TransitiveStaleness/Main.agda` (NOT a doubled `.../FixtureDeps/TransitiveStaleness/FixtureDeps/TransitiveStaleness/Main.agda`), and assert the returned `mutation.targetFile === "Dep.agda"`, `mutation.sourceFile === "Dep.broken.agda"`, and `entryFile === "Main.agda"` (all three bare, no `/`). **Test G3 (defensive fallback)** — same shape as G2 but with a NON-prefixed `path` (e.g. `"Main.agda"` directly, no `fixtureDir/` prefix) — assert `stripFixtureDirPrefix` leaves it unchanged and the write still lands correctly (proves the stripping is conditional, not unconditional slicing that would corrupt an already-bare path).
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/emit-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run test/unit/tools/emit-regression.test.ts` passes tests A-G plus G2/G3 with zero real Agda spawn required (pure logic + filesystem only); `grep -n "export function judgeRefusal\|export.*materializeFixtureFiles" scripts/emit-regression.mjs` shows both exports present.
  </acceptance_criteria>
  <done>The refusal gate and path-sandboxed baseline-diff materializer are implemented and unit-tested in isolation from the CLI/matrix-write plumbing, including the fixtureDir-prefixed real-flagship-shape regression (Test G2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The emitter — matrix-entry compose, RED self-check, CLI wrapper</name>
  <files>scripts/emit-regression.mjs, test/unit/tools/emit-regression.test.ts</files>
  <read_first>
    scripts/emit-regression.mjs (Task 2's output in this same plan — extend, do not re-derive judgeRefusal/materializeFixtureFiles/stripFixtureDirPrefix)
    test/helpers/capture-regression-runner.ts (Task 1 — replayCaptureRegressionEntry, the D-05 self-check dependency)
    test/fixtures/capture-regression-matrix.ts (Plan 03-01 — captureRegressionEntrySchema, to validate the composed entry against)
    src/session/safe-source-io.ts lines 125-173 (writeFileAtomic — reuse for the matrix write)
    scripts/oracle/run-oracle.mjs lines 233-296 (sidecar-path convention + scriptMain CLI wrapper shape)
    scripts/oracle/orcl-01-differential.mjs lines 495-509 (runColdLoadAndDiff's own `tupleMatches`/`categoriesMatch` logic — mirror this exact comparison in `matchesExpected`, never invent different semantics)
    scripts/promote-capture.mjs (full, 125 lines — the simpler CLI-wrapper variant)
    .planning/phases/03-regression-lock-pipeline/03-PATTERNS.md ("CLI wrapper convention", "Fresh-verdict-at-emit-time", "Write-safety note" subsections)
  </read_first>
  <behavior>
    - `composeEntry(...)` copies `verdict.orcl01.coldTuple`'s fields and `verdict.orcl01.coldCategories` into `expected` verbatim (no transformation, no re-derivation) and always sets `status: "red"` (this emitter never writes `"locked"` — that flip is a later, manual one-line edit per D-04/Claude's Discretion).
    - `writeMatrixEntry(...)` refuses (throws) when an entry with the same `id` already exists in the target matrix.
    - `matchesExpected(observed, expected)` is exported and is the ONLY place match semantics are implemented — the D-05 self-check below and Wave-3's vitest runner both call it; neither re-implements field/array equality independently (Warning-2 fix).
    - The D-05 self-check refuses (and rolls back any already-materialized fixture files) when `matchesExpected(observed, expected)` is `true` (the assertion would currently pass, not fail).
  </behavior>
  <action>
    Extend scripts/emit-regression.mjs (from Task 2) with: `composeEntry({ id, issue, tool, fixtureDir, entryFile, mutation, serverEnv, verdict })` — builds a `CaptureRegressionEntry`-shaped plain object per the `<behavior>` block above, then validates it via `captureRegressionEntrySchema.parse(...)` (import the schema from `../test/fixtures/capture-regression-matrix.js`), surfacing the zod error message on failure rather than writing an invalid entry. `entryFile` here is ALWAYS the value `materializeFixtureFiles` (Task 2) returned — `composeEntry` itself does no path derivation or stripping, it only assembles + validates.

    `writeMatrixEntry(entry, matrixJsonPath)` — reads the existing matrix array (or `[]` if the file is empty/missing), refuses per the `<behavior>` block on a duplicate `id`, appends the new entry, and writes the WHOLE array back via `writeFileAtomic` (prefer this over plain `writeFileSync` — matches `run-oracle.mjs`'s more defensive Phase-2 choice for a repeatedly-hand-run script, per PATTERNS.md's explicit write-safety note).

    Export `matchesExpected(observed, expected)`: a small, pure comparator — equality on `classification`/`success`/`goalCount`/`invisibleGoalCount`/`hasHoles`, plus same-length + same-index array equality on `errorCategories` (mirroring `runColdLoadAndDiff`'s own `tupleMatches`/`categoriesMatch` logic in `scripts/oracle/orcl-01-differential.mjs:505-509` — do not invent different comparison semantics). Returns `true` only when every field matches.

    `scriptMain(argv)` — CLI wrapper following `run-oracle.mjs`'s shape: positional arg 1 = primary artifact path; flags `--baseline <path>`, `--id <string>`, `--issue <comma-separated-numbers>`, `--fixture-dir <relative-dir>`, `--tool <name>`, `--server-env KEY=VALUE` (repeatable, collected into a `Record<string,string>`), `--force`, `--dry-run` (no `--entry-file` flag — `entryFile` is always derived from the artifact by `materializeFixtureFiles`, Task 2, never accepted as CLI input). Missing positional arg -> `Usage:` line to stderr + `process.exitCode = 1`, matching the other `scripts/oracle/*.mjs` CLIs. Flow: (a) call `runOracle(primaryPath)` fresh, then `judgeRefusal` (Task 2) — if non-null, print the reason to stderr, set `process.exitCode = 1`, return WITHOUT writing anything. (b) `const { mutation, entryFile } = materializeFixtureFiles(...)` (Task 2) to write fixture files into `test/fixtures/agda/<fixtureDir>/` and derive the bare `entryFile`. (c) `composeEntry({ ..., entryFile, mutation, ... })` to build the matrix entry, passing both values through from step (b) untouched. (d) D-05 self-check: call `replayCaptureRegressionEntry(entry, <repoRoot>/test/fixtures/agda)` (import from `../test/helpers/capture-regression-runner.js`) and evaluate `matchesExpected(observed, entry.expected)`. If `true` (the assertion would currently PASS, not fail), this is NOT RED: delete the fixture files just written in step (b) (roll back), print "cannot demonstrate RED — the observed result already matches the expected value; nothing to lock" to stderr, `process.exitCode = 1`, return. Otherwise print the observed-vs-expected diff as the RED evidence. (e) If `--dry-run`, stop here, printing the fixture paths + composed entry JSON without touching the matrix file or leaving the materialized fixtures in place (roll back the dry-run's own writes too). (f) Otherwise `writeMatrixEntry(entry, matrixJsonPath)`. Use `isMainModule(import.meta.url, process.argv[1])` as the entry-point guard.

    Add tests H-K to test/unit/tools/emit-regression.test.ts: Test H — `composeEntry` copies `verdict.orcl01.coldTuple`/`coldCategories` into `expected` verbatim and always sets `status: "red"`. Test I — `writeMatrixEntry` refuses (throws) when the target matrix already contains an entry with the same `id`. Test J — `writeMatrixEntry` appends to a pre-existing non-empty matrix file (a temp JSON with one existing entry) without disturbing that existing entry. Test K — `matchesExpected(observed, expected)` returns `true` for a hand-built `observed` that equals `expected` in every field (including a same-content, same-order `errorCategories` array), and returns `false` when any single scalar field OR the `errorCategories` array differs — tested directly via the exported function, without driving the full CLI/harness. This is the SAME function Wave-3's vitest runner (`test/integration/mcp/capture-regression.test.ts`) imports and reuses for its own pass/fail assertion (Warning-2 fix: one shared comparator, never two independently-drifting implementations).
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/emit-regression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run test/unit/tools/emit-regression.test.ts` passes all tests (A-G, G2, G3, H-K — 13 total); `npx tsx scripts/emit-regression.mjs` (no args) prints a `Usage:` line to stderr and exits 1; `grep -n "writeFileAtomic" scripts/emit-regression.mjs` confirms the matrix write uses the atomic helper, not plain `writeFileSync`; `grep -n "export function matchesExpected\|export const matchesExpected" scripts/emit-regression.mjs` confirms the comparator is exported for Wave-3 reuse.
  </acceptance_criteria>
  <done>The emitter is a complete, CLI-invokable script: refuse-gate -> materialize (fixtureDir-relative bare paths) -> compose -> self-verify-RED via the shared matchesExpected comparator -> write, fully unit-tested; ready for Wave 3 to invoke against a real capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Captured CaptureArtifact JSON (`.agda-mcp/captures/`, gitignored, locally-produced) → emitter | `manifest.inlinedFirstPartySources[].path` values are shaped like untrusted/agent-controlled data (D-08); the emitter is the FIRST code in this phase to write such paths into the TRACKED repo tree |
| Oracle verdict (orcl01/orcl02 kind) → refusal gate | Determines whether a capture may ever become a permanent regression lock |
| Matrix entry `serverEnv` field → spawned MCP server process env | First-party, checked-in, reviewed data — not runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | scripts/emit-regression.mjs `materializeFixtureFiles` | mitigate | `resolveFileWithinRoot`/`PathSandboxError` applied before every write into `test/fixtures/agda/`; any `..`/absolute-escaping `inlinedFirstPartySources[].path` is skipped, never written (D-08's new containment site beyond ORCL-01's tmpdir case) |
| T-03-02-02 | Tampering | scripts/emit-regression.mjs `judgeRefusal` + D-05 self-check | mitigate | Never write a matrix entry when ORCL-01 is inconclusive, ORCL-02 is cheat-flagged, ORCL-02 is no-policy-with-findings, or the observed replay already matches expected (D-06/D-05) — prevents a soundness cheat, an untrustworthy differential, or an already-fixed defect from becoming a permanent "this is correct" assertion |
| T-03-02-03 | Tampering | `serverEnv` field threaded into `createMcpHarness`'s `extraEnv` | accept | Matrix entries are first-party, code-reviewed, checked-in data, not runtime user input; the Plan 03-01 zod schema constrains `serverEnv` to `Record<string,string>` (no nested/complex injection surface) — matches RESEARCH.md's own Security Domain rationale |
| T-03-02-SC | Tampering | npm/pip/cargo installs | N/A | No new package-manager installs in this phase |
</threat_model>

<verification>
- `npx vitest run test/unit/tools/capture-regression-runner.test.ts test/unit/tools/emit-regression.test.ts` — green (with `RUN_AGDA_INTEGRATION=1 npm run build` first for the runner test).
- `grep -n "materializeSources" scripts/emit-regression.mjs` returns nothing (confirms the unfixed CR-01 materializer was never copied).
</verification>

<success_criteria>
- The shared replay helper and the complete emitter (refusal gate, materialization, compose, RED self-check, CLI) are fully unit-tested against synthetic artifacts.
- Zero real captures or matrix entries exist yet — this plan proves the mechanism, Wave 3 proves it against the real flagship.
</success_criteria>

<output>
Create `.planning/phases/03-regression-lock-pipeline/03-02-SUMMARY.md` when done
</output>
