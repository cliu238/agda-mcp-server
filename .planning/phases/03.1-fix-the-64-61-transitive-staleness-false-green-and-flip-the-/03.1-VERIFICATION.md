---
phase: 03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-
verified: 2026-07-02T16:20:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 03.1: Fix the #64/#61 transitive-staleness false-green and flip the flagship lock to green — Verification Report

**Phase Goal:** The first loop-surfaced defect is closed end-to-end — `agda_load_no_metas` stops reporting a false `ok-complete` on a transitive-staleness truncation, and Phase 3's flagship regression flips from RED to locked — proving the capture → lock → fix → stay-locked loop throughput.
**Verified:** 2026-07-02T16:20:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

All verification claims below were checked against the actual codebase and, where possible, **re-executed live** in this session (not taken from SUMMARY.md text) — including the real-Agda `RUN_AGDA_INTEGRATION=1` suite, using the locally available `agda 2.8.0` binary.

### Observable Truths

Truths 1–4 are the ROADMAP.md Success Criteria (the authoritative contract for this phase). Truths 5–7 are additional distinct must-haves declared in the two PLAN.md frontmatters that are not subsumed by the four SCs.

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | `runLoadNoMetas` gains a fail-closed terminus/completion guard — a truncated strict-load stream reports failure, never a false `ok-complete` (SC1) | ✓ VERIFIED | `src/agda/session-load-impl.ts:214-218` calls `session.sendCommand(..., undefined, { loadTerminusMode: "strict" })`. Live-executed `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts` → **2 passed**, including `issue-64-61-transitive-staleness: agda_load_no_metas matches ORCL-01 cold expected value` (the exact truncation repro), confirming the fix is real, not merely present in source. |
| 2 | The guard is a VARIANT — `isLoadTerminusSatisfied` for "strict" returns `sawLoadError` ALONE, so a legitimately goal-less strict load is not false-RED'd (SC2/D-02) | ✓ VERIFIED (see caveat) | `src/session/load-terminus-tracker.ts:76-79`: `if (state.mode === "strict") return state.sawLoadError;`. Unit tests confirm both directions: `isLoadTerminusSatisfied: strict mode is NOT satisfied by the metas-style positive signal` (false when sawInteractionPoints/sawAllGoalsWarnings true but sawLoadError false) and `...IS satisfied by sawLoadError alone` (`test/unit/session/load-terminus-tracker.test.ts:40-64`) — both ran and pass. Transport-level D-02 guard test also exists and passes: `loadTerminusMode: strict resolves via the widened window when no further response ever arrives (D-02 guard)` (`test/unit/session/agda-transport.test.ts:120`). **Caveat** (self-caught by this phase's own code review, WR-03, and transparently disclosed — see Anti-Patterns): the *matrix* guard entry `guard-no-metas-clean-load-under-fault-injection` does NOT itself discriminate the fix (it passes identically with or without `loadTerminusMode:"strict"`, since `shouldResolveOnIdle` always eventually resolves once `responseCount > 0` — only the delay changes). The load-bearing proof of this truth is the unit tests above, not that matrix entry; `capture-regression-matrix.ts`'s header comment was corrected to say exactly this. The underlying property (no false-RED) is still genuinely proven — just via a different artifact than the entry's name implies. |
| 3 | The `issue-64-61-transitive-staleness` matrix entry flips `status: "red"` → `"locked"`, and the runner asserts `matchesExpected(observed, expected) === true` (SC3/D-03) | ✓ VERIFIED | `test/fixtures/capture-regression-matrix.json:8` shows `"status": "locked"`. Live-executed run (see #1) shows this specific test **passing** (`matchesExpected === true`), not merely a status-label edit. |
| 4 | No regression: metas path unchanged, full suite green, 500-line ceiling honored (SC4/D-04/D-05) | ✓ VERIFIED | `wc -l src/session/agda-transport.ts` → **497** (≤500). `git diff --stat 7ce0005..HEAD -- src/` touches exactly the 6 expected files (`session-command-dispatch.ts`, `session-load-impl.ts`, `session.ts`, `agda-transport.ts`, `command-completion.ts`, `load-terminus-tracker.ts` new) — **not** `parse-load-responses.ts`/`session-load-helpers.ts` (no new classification path). Live-executed `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/agda/agda-load.test.ts` (metas-path control) → **37 passed, 0 failed** (matches SUMMARY's recorded baseline exactly). Live-executed `RUN_AGDA_INTEGRATION=1 AGDA_FIXTURE_PHASES=strict npx vitest run test/integration/agda/agda-fixture-matrix.test.ts` → **73 passed, 0 failed**. Live-executed `RUN_AGDA_INTEGRATION=1 npm test` (whole suite) → **189 test files passed / 4 skipped (193), 1655 tests passed / 5 skipped (1660), 0 failed** — matches the SUMMARY's claimed figures (1655/5/0) almost exactly, an independent re-run producing the same result. `npm run build` clean. |
| 5 | Zero wire-level IOTCM command-string changes; command-builder SSOT intact | ✓ VERIFIED | `runLoadNoMetas`'s IOTCM construction is unchanged: `command("Cmd_load_no_metas", quoted(absPath))` (`session-load-impl.ts:215`) — identical to before; only the third `sendCommand` argument (options) changed. `test/unit/protocol/no-bare-command-strings.test.ts` (the project's own invariant test) re-run live → passes. |
| 6 | No new `LoadResult` classification value introduced — `runLoadNoMetas` still only ever reaches `ok-complete` or `type-error` | ✓ VERIFIED | `session-load-impl.ts:239`: `const classification = success ? "ok-complete" : "type-error";` — unchanged ternary, same two values as pre-fix. The fix operates one layer down (transport completion-detection), not by adding a new classification branch. |
| 7 | No other live defect (`agda_auto` flag leak, `agda_give` `ok:true`-wrapping-error, `agda_search_definitions` hardcoded layout) touched or locked by this plan (D-04 scope fence) | ✓ VERIFIED | `git diff --stat 7ce0005..HEAD -- src/ test/` contains no `auto`/`give`/`search-definitions` files (grep returned zero matches). Full diff confined to the terminus-tracking surface + the capture-regression matrix/test files. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `src/session/load-terminus-tracker.ts` | `LoadTerminusMode`/`LoadTerminusOptions`/`LoadTerminusState` + 3 pure functions, sole owner of terminus state/predicate | ✓ VERIFIED | 81 lines. `grep -c "^export "` → 6 (matches plan's ≥6 requirement). Class-less snapshot+pure-function shape as specified. |
| `test/unit/session/load-terminus-tracker.test.ts` | Unit coverage for mode resolution + D-02 guard | ✓ VERIFIED | 13 tests (verified by reading the file), all pass live. |
| `src/session/agda-transport.ts` | Delegates to tracker; `sendCommand` accepts `LoadTerminusOptions` | ✓ VERIFIED | Imports `createLoadTerminusState`/`isLoadTerminusSatisfied`/`recordLoadTerminusResponse` (lines 18-23); single `terminus` field (line 67); 497 lines. |
| `src/agda/session-load-impl.ts` | `runLoadNoMetas`'s corrected call site | ✓ VERIFIED | `grep -n 'loadTerminusMode: "strict"'` → exactly 1 occurrence (line 217, inside `runLoadNoMetas`). `grep -n 'awaitGoalTerminus: true'` → exactly 1 occurrence (line 86, inside `runLoad`, untouched). |
| `src/agda/session.ts` | `sendCommand`'s widened options type (pass-through) | ✓ VERIFIED | Line 225: `options: LoadTerminusOptions = {}`; body (`return dispatchSessionCommand(this, command, timeoutMs, options)`) confirmed as literal pass-through, not dropped. |
| `src/agda/session-command-dispatch.ts` | `dispatchSessionCommand`'s widened options type (pass-through) | ✓ VERIFIED | Line 45: `options: LoadTerminusOptions = {}`; line 78 confirms literal pass-through to `session.transport.sendCommand(proc, command, timeoutMs, options)`. |
| `test/fixtures/capture-regression-matrix.json` | Flagship flipped to `locked`, D-05 guard entry added | ✓ VERIFIED | 2 entries, both `"status": "locked"` (confirmed via `node -e` one-liner and direct read). |
| `test/fixtures/capture-regression-matrix.ts` | Clarified header-comment scope for hand-authored entries | ✓ VERIFIED | Header comment extended with the exact WR-03 relabeling ("It is NOT fix-discriminating... The single fix-discriminating lock is the MUTATION entry"). |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `session-load-impl.ts` (`runLoadNoMetas`) | `session.ts` | `session.sendCommand(..., { loadTerminusMode: "strict" })` | ✓ WIRED | Confirmed at `session-load-impl.ts:214-218`; matches plan pattern exactly. |
| `session.ts` | `session-command-dispatch.ts` | options pass-through | ✓ WIRED | `dispatchSessionCommand(this, command, timeoutMs, options)` — options forwarded unmodified. |
| `session-command-dispatch.ts` | `agda-transport.ts` | options pass-through | ✓ WIRED | `session.transport.sendCommand(proc, command, timeoutMs, options)` (line 78) — options forwarded unmodified. |
| `agda-transport.ts` | `load-terminus-tracker.ts` | `createLoadTerminusState`/`recordLoadTerminusResponse`/`isLoadTerminusSatisfied` imports | ✓ WIRED | Imported (lines 18-23) and called at all 3 reset sites (`sendCommand:284`, `sendFireAndForgetCommand:207`, `destroy:127`) plus `recordCollectedResponse:452` and `bumpIdleCompletionTimer:478`. |
| `capture-regression-matrix.json` | `capture-regression.test.ts` | `captureRegressionMatrix` iteration, `status !== "red"` ⇒ `matchesExpected === true` | ✓ WIRED | Live-executed: both entries produce a passing "matches" assertion (verbose test names confirmed, not a coincidental pass — see Data-Flow Trace below). |
| `capture-regression-matrix.json` (guard entry) | `test/fixtures/agda/FixtureDeps/TransitiveStaleness` | `fixtureDir` field, no `mutation` field | ✓ WIRED | Confirmed: guard entry has no `mutation` key in the JSON; runner treats it as a single clean-load call (per `capture-regression-runner.ts` design, referenced in plan and unmodified by this phase). |

### Data-Flow Trace (Level 4)

This phase is backend/infra (no rendered UI), so "data flow" here means: does the terminus predicate genuinely gate the observed classification, end-to-end, against a real Agda process — not a mocked/stubbed one?

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `runLoadNoMetas`'s `classification` | `parsed.success` → `type-error`/`ok-complete` | Real `agda --interaction-json` subprocess stdout, normalized via `normalizeAgdaResponse`, gated by `isLoadTerminusSatisfied`/idle-window timing | Yes — reconfirmed live against `agda 2.8.0`, not a fake/mock transport | ✓ FLOWING |
| Flagship matrix entry `expected` block | `classification: "type-error"`, `errorCategories: ["UnequalTerms"]` | Real cold-Agda-derived tuple (Phase 3's ORCL-01), replayed live via `capture-regression-runner.ts` against the real fixture pair with a real mutation swap | Yes | ✓ FLOWING |
| Guard entry `expected` block | `classification: "ok-complete"`, `success: true` | Real clean load of `Main.agda`, no mutation | Yes, but **non-discriminating** for the fix itself (see Truth #2 caveat) — still a real value, not a hardcoded stub | ✓ FLOWING (non-discriminating, disclosed) |

### Behavioral Spot-Checks / Real-Agda Proof (executed live in this session)

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Flagship + D-05 guard both pass under real Agda | `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts --reporter=verbose` | `issue-64-61-transitive-staleness: ... matches ORCL-01 cold expected value` ✓, `guard-no-metas-clean-load-under-fault-injection: ... matches ORCL-01 cold expected value` ✓ — **2 passed, 0 failed** | ✓ PASS |
| Metas path unaffected (control) | `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/agda/agda-load.test.ts` | **37 passed, 0 failed** | ✓ PASS |
| 72-fixture strict-path breadth | `RUN_AGDA_INTEGRATION=1 AGDA_FIXTURE_PHASES=strict npx vitest run test/integration/agda/agda-fixture-matrix.test.ts` | **73 passed, 0 failed**, 128.97s | ✓ PASS |
| Whole suite green | `RUN_AGDA_INTEGRATION=1 npm test` | **189 files / 1655 tests passed, 4 files / 5 tests skipped, 0 failed**, 215.89s | ✓ PASS |
| Pure (non-Agda) unit+property suite | `npx vitest run test/unit test/property` | **175 files / 1475 tests passed, 1 file / 17 tests skipped, 0 failed** | ✓ PASS |
| `tsc` build | `npm run build` | Clean, no errors | ✓ PASS |
| 500-line ceiling | `wc -l src/session/agda-transport.ts` | 497 | ✓ PASS |
| Command-builder SSOT / no dead tool refs invariants | `npx vitest run test/unit/protocol/no-bare-command-strings.test.ts test/unit/tools/no-dead-tool-references.test.ts` | 6 passed | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention exists in this project, and neither PLAN.md declares any probe script. **Step 7c: SKIPPED (no runnable probe scripts declared or found)** — the real-Agda vitest suite above serves the equivalent role for this project and was executed directly.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| FIX-01 | 03.1-01, 03.1-02 (both declare `requirements: [FIX-01]`) | First loop-surfaced defect fixed end-to-end; flagship flips RED→locked; no regression | ✓ SATISFIED | All 7 truths above. `.planning/REQUIREMENTS.md:106` currently shows `FIX-01 \| Phase 03.1 \| Pending` — this is expected/acceptable at verification time per the phase-completion bookkeeping workflow (status flips to Complete during phase-completion, which runs after this verification). No orphaned requirements: `grep -n "Phase 03.1" REQUIREMENTS.md` returns only the FIX-01 row; both plans declare exactly `[FIX-01]`, nothing more, nothing less. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `test/fixtures/capture-regression-matrix.json` / `.ts` | guard entry + header comment | The `guard-no-metas-clean-load-under-fault-injection` matrix entry is not fix-discriminating (passes identically with the strict-mode fix reverted) | ℹ️ Info | Self-caught by this phase's own code review (WR-03) and transparently relabeled in the `.ts` header comment rather than hidden. Does not affect the flagship fix-lock (D-03's actual definition of done) or the unit-level D-02 proof. No action needed — already disclosed. |
| `src/session/agda-transport.ts:478` | hoisted `snapshot` object literal | 247-character single-line object literal (IN-01 from code review) | ℹ️ Info | Cosmetic only; no Prettier/lint CI gate in this repo enforces wrapping. Explicitly deferred by the phase's own review-fix pass. |
| `src/session/load-terminus-tracker.ts:17-20` | `LoadTerminusOptions` | Dual API surface — legacy `awaitGoalTerminus` boolean alias coexists with the new `loadTerminusMode` enum (IN-02 from code review) | ℹ️ Info | Documented as intentional backward-compat; one internal caller of each spelling. Explicitly deferred by the phase's own review-fix pass. |
| `src/agda/session-load-impl.ts:206-213` | doc comment | Residual timing risk: an `Error` arriving later than `AGDA_MCP_LOAD_TERMINUS_IDLE_MS` (default 2000ms) after the last response is still missed (WR-02 from code review) | ℹ️ Info | Inherent to the silence-based-inference design chosen for a signal with no positive success terminus (D-02's forced constraint); strictly better than the pre-fix fully-unguarded path; explicitly documented in the code comment per the review-fix. Out of this phase's scope fence (D-04 forbids "rewrite the idle-timeout heuristic"). |

No blockers. No unresolved `TBD`/`FIXME`/`XXX` markers found in any of the 6 phase-modified `src/` files (`grep -rn "TBD|FIXME|XXX"` returned zero matches).

### Human Verification Required

None. This phase is entirely backend completion-detection/timing logic with no UI/UX surface. Every claim was independently verified either via static code trace (imports, call-site pass-through, line counts) or via **live re-execution** of the real-Agda-gated test suite in this verification session (not a re-statement of SUMMARY.md's claimed numbers) — including the single most important check, the flagship + D-05 guard matrix run, which reproduced 2/2 passing exactly as claimed.

### Gaps Summary

No gaps. All 4 ROADMAP Success Criteria and all 3 additional plan-level must-haves are verified against the live codebase and, where a real-Agda binary was available (it was — `agda 2.8.0` on PATH), re-executed directly rather than trusted from SUMMARY.md text:

- The fix is present, wired end-to-end (`session-load-impl.ts` → `session.ts` → `session-command-dispatch.ts` → `agda-transport.ts` → `load-terminus-tracker.ts`), and behaviorally correct — proven by a live re-run of `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts`, which reproduced the claimed 2/2 pass (flagship + guard) exactly.
- No regression: live re-runs of the metas-path control (37/37), the 72-fixture strict breadth (73/73), and the whole suite (1655 passed/5 skipped/0 failed) all matched the SUMMARY's claimed figures almost exactly — a strong independent confirmation, not a restatement.
- The 500-line ceiling (497) and the scope fence (git diff confined to exactly the 6 expected `src/` files, no `agda_auto`/`agda_give`/`agda_search_definitions` touched) both hold.
- FIX-01 traces cleanly to both plans with no orphaned requirements.

One nuance is worth carrying forward (not a gap, listed transparently above under Anti-Patterns / Truth #2): the `guard-no-metas-clean-load-under-fault-injection` matrix entry, despite its name, does not itself discriminate whether the fix is present — this was caught by the phase's own code review (WR-03) and honestly relabeled in the fixture module's header comment rather than papered over. The actual D-02 guarantee (a legitimately goal-less strict load is never false-RED'd) is independently and soundly proven by the unit tests at `load-terminus-tracker.test.ts` and `agda-transport.test.ts`, both of which were confirmed to exist and pass. This is exactly the kind of self-correcting rigor the loop is supposed to produce, not a defect in it.

---

_Verified: 2026-07-02T16:20:00Z_
_Verifier: Claude (gsd-verifier)_
