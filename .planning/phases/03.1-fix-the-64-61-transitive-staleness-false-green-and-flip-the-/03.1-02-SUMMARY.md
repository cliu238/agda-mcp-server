---
phase: 03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-
plan: 02
subsystem: testing
tags: [agda, capture-regression, regression-testing, vitest, false-green, mcp-server]

# Dependency graph
requires:
  - phase: 03.1-01
    provides: "runLoadNoMetas's loadTerminusMode: \"strict\" fix — the transport-level completion-detection guard this plan's matrix flip proves end-to-end against real Agda"
provides:
  - "test/fixtures/capture-regression-matrix.json: issue-64-61-transitive-staleness flipped status \"red\" -> \"locked\" (D-03's definition of done), proven under RUN_AGDA_INTEGRATION=1 to match ORCL-01's cold expected value against the Plan 03.1-01 fix"
  - "test/fixtures/capture-regression-matrix.json: new guard-no-metas-clean-load-under-fault-injection entry proving the D-02 false-RED risk stays closed at the real MCP-tool-call boundary, under the exact fault-injection env that exposed the original bug"
  - "test/fixtures/capture-regression-matrix.ts: header comment clarified — the \"no hand-authored entries\" restriction is about a captured defect's content provenance, not a blanket ban on ever adding an entry by hand"
  - "Full no-regression sweep proof, real Agda: metas-path control 37/37 unchanged, 72-fixture strict-path breadth 73/73, whole suite 1655 passed / 5 skipped / 0 failed"
affects: ["Phase 4 fix queue (agda_auto flag leak, agda_give ok:true-wrapping-error, agda_search_definitions hardcoded layout remain untouched, exactly as D-04 fences)", "Future capture-regression matrix entries (establishes the mutation-less guard-entry precedent)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Mutation-less guard entry in the capture-regression matrix: a status:\"locked\" entry with no `mutation` field asserts a legitimate clean load stays green under fault injection, reusing the SAME already-captured fixture content rather than fabricating a new one"

key-files:
  created: []
  modified:
    - test/fixtures/capture-regression-matrix.json
    - test/fixtures/capture-regression-matrix.ts

key-decisions:
  - "Reused the exact matrix-entry shape from 03.1-RESEARCH.md Pattern 3 (empirically validated 6/6) rather than authoring a new standalone integration test — zero new test infrastructure"
  - "Preserved the existing on-disk JSON pretty-print style (arrays expanded one-element-per-line) for the new entry, rather than the more compact single-line-array style shown in the plan's interfaces block — semantically identical, schema-validated, and keeps the file internally consistent"

patterns-established:
  - "Mutation-less guard-entry precedent: a hand-added matrix entry over already-real, already-captured fixture content (no new fixture files, no `mutation` field) is schema-conformant and not the kind of hand-built synthetic bundle the file's header comment warns against — usable by future D-05-style negative-proof entries"

requirements-completed: [FIX-01]

# Metrics
duration: 12min
completed: 2026-07-02
---

# Phase 03.1 Plan 02: Flip the Flagship Lock and Prove the No-Regression Sweep Summary

**Flipped the `issue-64-61-transitive-staleness` capture-regression entry from `red` to `locked` and added a mutation-less D-05 guard entry, both proven green under `RUN_AGDA_INTEGRATION=1` against the Plan 03.1-01 `runLoadNoMetas` fix, with zero regression across the metas path, the 72-fixture strict breadth, and the whole suite.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-07-02T19:38:00Z (approximate)
- **Completed:** 2026-07-02T19:49:37Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Flipped the flagship `issue-64-61-transitive-staleness` matrix entry from `red` to `locked`; `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts` confirms `matchesExpected(observed, entry.expected) === true` for it — D-03's definition of done, closed for real against the Plan 03.1-01 fix
- Added the mutation-less `guard-no-metas-clean-load-under-fault-injection` entry (no `mutation` field — a single clean-load call), proving a legitimately goal-less `agda_load_no_metas` load under the exact fault-injection env (`AGDA_MCP_IDLE_COMPLETION_MS=1`/`AGDA_MCP_POST_STATUS_IDLE_MS=1`) is NOT false-RED'd by the widened idle window (D-02/D-05)
- Clarified `capture-regression-matrix.ts`'s header comment (comment-only change — zod schema, loader, and exported type all untouched)
- Ran the full no-regression sweep, all real-Agda-gated: metas-path control (`agda-load.test.ts`) 37/37, matching the recorded baseline exactly; 72-fixture strict-path breadth (`agda-fixture-matrix.test.ts`, `AGDA_FIXTURE_PHASES=strict`) 73/73, matching the recorded baseline, ~131.6s (within the disclosed 81-132s latency-tradeoff estimate); whole suite (`RUN_AGDA_INTEGRATION=1 npm test`) 189 test files passed / 4 skipped, 1655 tests passed / 5 skipped, 0 failed
- `git diff --stat -- src/` confirmed EMPTY throughout — this plan is pure proof, zero `src/` changes (Wave 1 already shipped the fix)

## Task Commits

Each task was committed atomically:

1. **Task 1: Flip the flagship lock and add the D-05 guard entry** - `f32e013` (fix)
2. **Task 2: Full no-regression sweep — metas-path control, strict-path breadth, whole suite** - no commit (verification-only task per the plan's own `<files>` spec: "none modified"; its "done" state is the passing sweep documented under Verification below)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `test/fixtures/capture-regression-matrix.json` - Flipped `issue-64-61-transitive-staleness`'s `"status"` from `"red"` to `"locked"` (every other field, including its `expected` block, untouched); appended the new `guard-no-metas-clean-load-under-fault-injection` entry (`status: "locked"`, no `mutation` field, same fixture + `serverEnv` as the flagship, `expected` = ok-complete/success:true/zero goals/zero errors)
- `test/fixtures/capture-regression-matrix.ts` - Extended the header comment's "no hand-authored entries" line with one clarifying sentence distinguishing defect-content provenance from a blanket ban on hand-added entries; zero code/schema/type changes

## Decisions Made
- Reused the exact matrix-entry shape from `03.1-RESEARCH.md` Pattern 3 (empirically validated 6/6 across repeated runs) rather than authoring a new standalone integration test using `createMcpHarness` directly — the plan's primary recommendation, needing zero new test infrastructure.
- Kept the new entry's on-disk JSON formatting (arrays expanded one-element-per-line, matching the existing entry's already-committed style) rather than the more compact single-line-array style shown in the plan's `<interfaces>` block — a purely cosmetic choice; both are valid JSON and validate identically against the unchanged zod schema.

## Deviations from Plan

None - plan executed exactly as written. Both `capture-regression-matrix.json`/`.ts` edits match Task 1's action precisely; Task 2 was a pure verification task with no files to modify, and all three required commands (the flagship+guard run, the metas-path control, the strict-path breadth run, and the whole-suite run) passed on the first attempt with zero auto-fixes needed.

## Issues Encountered

None blocking. Two environmental notes, both anticipated by the plan/prompt and resolved without deviation:
- Ambient Node was v22.22.0 (repo requires `>=24` per `package.json`/`.nvmrc`); used `mise exec --` (resolving to Node v24.16.0) for every `npm install`/`npm run build`/`vitest`/`npm test` invocation in this plan, per the prompt's explicit environment guidance.
- `node_modules` was absent at the start of this plan; ran `npm install` once (145 packages added, 0 vulnerabilities) before any build/test command, then confirmed a clean `tsc` build before running the target verification.
- The `AGDA_FIXTURE_PHASES=strict` fixture-matrix run took ~131.6s — within `03.1-RESEARCH.md`'s disclosed 81-132s latency-tradeoff window (every genuinely clean fixture now pays the widened idle window once) — not a regression, matching the plan's own expectation.
- The whole-suite run emitted several `[agda-mcp] WARN: sendCommand timed out` / `post-load metas reconciliation failed` lines to stderr; these are expected output from tests that intentionally exercise timeout/failure paths (e.g. `agda-load-impl` reconciliation-failure tests), not indicative of a real problem — the suite reported 0 failed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The #64/#61 transitive-staleness capture -> lock -> fix -> stay-locked loop is now closed end-to-end and durable: both the flagship and the D-05 guard entry are `status: "locked"` and green under real Agda, re-checked by every future `RUN_AGDA_INTEGRATION=1` run — this is Loop ②'s first fully-closed defect cycle.
- Phase 4's fix queue remains exactly as scoped by D-04: the three other live CHG-REVERIFY defects (`agda_auto` CLI-flag leak, `agda_give` `ok:true`-wrapping-error, `agda_search_definitions` hardcoded layout) are untouched by this plan.
- No blockers identified for subsequent phases.

---
*Phase: 03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-*
*Completed: 2026-07-02*

## Verification

All commands run under real Agda (`agda --version` -> 2.8.0), `mise exec --` (Node v24.16.0):

- `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts` -> 2 passed, 0 failed. Verbose names confirm both `issue-64-61-transitive-staleness: agda_load_no_metas matches ORCL-01 cold expected value` and `guard-no-metas-clean-load-under-fault-injection: agda_load_no_metas matches ORCL-01 cold expected value` now pass.
- `node -e "..."` one-liner confirms exactly 2 matrix entries, both `status: "locked"` -> exit 0.
- `git diff --stat -- src/` -> empty (confirmed after Task 1 and again after Task 2).
- `RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/agda/agda-load.test.ts` -> 37 passed, 0 failed (matches the recorded baseline exactly — the metas path is unaffected).
- `RUN_AGDA_INTEGRATION=1 AGDA_FIXTURE_PHASES=strict npx vitest run test/integration/agda/agda-fixture-matrix.test.ts` -> 73 passed, 0 failed, 131.60s (matches the recorded baseline; the latency increase is the disclosed, intentional tradeoff).
- `RUN_AGDA_INTEGRATION=1 npm test` -> 189 test files passed / 4 skipped (193), 1655 tests passed / 5 skipped (1660), 0 failed, 205.32s.

## Self-Check: PASSED

- `test/fixtures/capture-regression-matrix.json` FOUND — parses as valid JSON, 2 entries, both `status: "locked"`.
- `test/fixtures/capture-regression-matrix.ts` FOUND — header comment updated, zod schema/loader/type byte-identical to before.
- Commit `f32e013` FOUND in `git log --oneline --all`.
- `git diff --stat -- src/` -> empty, confirming this plan touched zero `src/` files.
- All four verification commands above re-confirmed exit 0 with the exact pass counts documented.
