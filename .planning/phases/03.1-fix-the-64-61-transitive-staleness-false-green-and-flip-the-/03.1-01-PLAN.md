---
phase: 03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/session/load-terminus-tracker.ts
  - test/unit/session/load-terminus-tracker.test.ts
  - src/session/agda-transport.ts
  - test/unit/session/agda-transport.test.ts
  - src/agda/session.ts
  - src/agda/session-command-dispatch.ts
  - src/agda/session-load-impl.ts
  - src/session/command-completion.ts
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "runLoadNoMetas passes { loadTerminusMode: \"strict\" } to sendCommand instead of no options, closing the e38f90a metas-vs-no_metas terminus-detection asymmetry (D-01)"
    - "The strict terminus predicate is satisfied ONLY by sawLoadError — never by the metas-style InteractionPoints+AllGoalsWarnings signal a clean strict load never emits — so a genuinely goal-less load is not false-RED'd by the new guard (D-02)"
    - "src/session/agda-transport.ts ends up at or under 500 lines after extracting terminus-tracking state into the new src/session/load-terminus-tracker.ts sibling (D-05's 500-line ceiling, honoring the documented existing-outlier status rather than compounding it)"
    - "The metas path is byte-for-byte behaviorally unchanged: runLoad's { awaitGoalTerminus: true } call is untouched, the awaitGoalTerminus alias still resolves to loadTerminusMode: \"metas\" inside the transport, and every existing unit test (agda-transport.test.ts, command-completion.test.ts, session-load-impl.test.ts) passes unmodified (D-04's scope fence, D-05's no-regression requirement)"
    - "Zero wire-level IOTCM command-string changes — the command()/quoted() call sites in session-load-impl.ts are untouched; this is entirely a TypeScript-level sendCommand options change (command-builder SSOT intact)"
    - "No new LoadResult classification value is introduced — runLoadNoMetas still only ever reaches ok-complete or type-error, exactly as today"
  artifacts:
    - path: "src/session/load-terminus-tracker.ts"
      provides: "LoadTerminusMode/LoadTerminusOptions/LoadTerminusState types plus createLoadTerminusState/recordLoadTerminusResponse/isLoadTerminusSatisfied pure functions — the sole owner of terminus-tracking state and the mode-dependent predicate"
      exports: ["LoadTerminusMode", "LoadTerminusOptions", "LoadTerminusState", "createLoadTerminusState", "recordLoadTerminusResponse", "isLoadTerminusSatisfied"]
    - path: "test/unit/session/load-terminus-tracker.test.ts"
      provides: "Unit coverage for mode resolution and the D-02 strict-mode false-RED guard, isolated from any child-process/transport fixture"
      min_lines: 30
    - path: "src/session/agda-transport.ts"
      provides: "Delegates all terminus tracking to the tracker module; sendCommand accepts LoadTerminusOptions"
      contains: "createLoadTerminusState"
    - path: "src/agda/session-load-impl.ts"
      provides: "runLoadNoMetas's corrected call site"
      contains: "loadTerminusMode: \"strict\""
    - path: "src/agda/session.ts"
      provides: "sendCommand's widened options type (pure pass-through)"
      contains: "LoadTerminusOptions"
    - path: "src/agda/session-command-dispatch.ts"
      provides: "dispatchSessionCommand's widened options type (pure pass-through)"
      contains: "LoadTerminusOptions"
  key_links:
    - from: "src/agda/session-load-impl.ts"
      to: "src/agda/session.ts"
      via: "runLoadNoMetas's session.sendCommand(..., { loadTerminusMode: \"strict\" }) call"
      pattern: "loadTerminusMode: \"strict\""
    - from: "src/agda/session.ts"
      to: "src/agda/session-command-dispatch.ts"
      via: "sendCommand's options pass-through to dispatchSessionCommand"
      pattern: "dispatchSessionCommand\\(this, command, timeoutMs, options\\)"
    - from: "src/agda/session-command-dispatch.ts"
      to: "src/session/agda-transport.ts"
      via: "dispatchSessionCommand's options pass-through to transport.sendCommand"
      pattern: "session\\.transport\\.sendCommand\\(proc, command, timeoutMs, options\\)"
    - from: "src/session/agda-transport.ts"
      to: "src/session/load-terminus-tracker.ts"
      via: "createLoadTerminusState/recordLoadTerminusResponse/isLoadTerminusSatisfied imports"
      pattern: "from \"\\./load-terminus-tracker\\.js\""
---

<objective>
Close the `runLoadNoMetas` terminus-detection gap (D-01/D-02) by generalizing `AgdaTransport`'s `awaitGoalTerminus` boolean into a `loadTerminusMode` enum, extracting the terminus-tracking state into a new sibling module so `agda-transport.ts` stays within the 500-line ceiling instead of compounding its existing-outlier debt (D-05), threading the widened option through all three intermediate call sites, and finally flipping `runLoadNoMetas`'s actual call site to request the strict guard. This reproduces 03.1-RESEARCH.md's built-and-empirically-validated prototype exactly, anchored to 03.1-PATTERNS.md's current-codebase line numbers (independently re-verified against the live source this planning session — every cited line matched).

Purpose: this is the actual code fix — the mechanism plus the switch-flip. Plan 03.1-02 then proves it end-to-end against real Agda (the flagship lock flip, the D-05 guard entry, and the full no-regression sweep) — that real-Agda proof is deliberately NOT part of this plan's own verification, matching 03.1-RESEARCH.md's own tiered Sampling Rate (fast unit checks per task, real-Agda checks per wave/phase gate).
Output: `src/session/load-terminus-tracker.ts` (+ its unit test), a rewired `AgdaTransport` threaded through `session.ts`/`session-command-dispatch.ts`, and `runLoadNoMetas` requesting `{ loadTerminusMode: "strict" }`.

Known, disclosed residual limitation (03.1-RESEARCH.md Pitfall 5 — not this phase's job to close, per D-04's scope fence): this fix closes only the IDLE-TIMING truncation mechanism that #64/#61 actually exhibits. It does not close a process-crash-during-load truncation — `AgdaTransport.handleProcessClose()` resolves immediately regardless of `loadTerminusMode`, exactly as today, which is a different failure mode from the one FIX-01 targets. Carry this forward into the phase SUMMARY as a named, disclosed limitation, not as a gap in this fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-/03.1-CONTEXT.md
@.planning/phases/03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-/03.1-RESEARCH.md
@.planning/phases/03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-/03.1-PATTERNS.md
</context>

<interfaces>
<!-- src/session/load-terminus-tracker.ts — the exact contract Task 1 creates and Tasks 2/3 consume. -->
```typescript
export type LoadTerminusMode = "metas" | "strict" | null;

export interface LoadTerminusOptions {
  awaitGoalTerminus?: boolean;
  loadTerminusMode?: LoadTerminusMode;
}

export interface LoadTerminusState {
  mode: LoadTerminusMode;
  sawInteractionPoints: boolean;
  sawAllGoalsWarnings: boolean;
  sawLoadError: boolean;
}

export function createLoadTerminusState(options?: LoadTerminusOptions): LoadTerminusState;
export function recordLoadTerminusResponse(state: LoadTerminusState, response: AgdaResponse): void;
export function isLoadTerminusSatisfied(state: LoadTerminusState): boolean;
```

<!-- src/agda/types.ts:44-47 — the AgdaResponse shape the tracker consumes (already exported, unchanged by this plan). -->
```typescript
export interface AgdaResponse {
  kind: string;
  [key: string]: unknown;
}
```

<!-- The three call sites that must ALL accept LoadTerminusOptions for the build to compile (session.ts:221-227, session-command-dispatch.ts:40-45+77, agda-transport.ts:269-291) currently share this duplicated inline literal type — Task 2 replaces it everywhere with the one exported LoadTerminusOptions above, per PATTERNS.md's explicit "planner's choice" flag: -->
```typescript
options: { awaitGoalTerminus?: boolean } = {}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the load-terminus-tracker module (interface-first)</name>
  <files>src/session/load-terminus-tracker.ts, test/unit/session/load-terminus-tracker.test.ts</files>
  <read_first>
    03.1-RESEARCH.md "Pattern 1: Generalize a boolean gate into a small mode enum" (the built-and-tested predicate: strict mode = sawLoadError only, metas mode unchanged) and "Pattern 2: Extract before the file grows past the ceiling" (the design sketch, flagged there as untested — this task is what tests it)
    03.1-PATTERNS.md "src/session/load-terminus-tracker.ts (NEW — utility/state-tracker, transform)" section in full — the three analogs (command-completion.ts's snapshot+pure-function shape, session-state.ts's smaller sibling, session-command-dispatch.ts's extraction-convention header comment) and the target shape sketch
    src/session/command-completion.ts full file (182 lines) — CommandCompletionSnapshot interface (lines 1-14) and idleCompletionDelay (lines 72-98) as the structural analog: a small snapshot interface consumed by pure top-level functions, no class
    src/session/session-state.ts full file (35 lines) — the smaller sibling precedent (SessionStateSnapshot + deriveSessionPhase)
    src/agda/session-command-dispatch.ts lines 1-11 (header comment) — this project's own "extract to escape the 500-line ceiling, free functions over a self-contained state object" convention statement to echo in this new file's own header comment
    src/agda/types.ts lines 44-47 — the AgdaResponse type ({ kind: string; [key: string]: unknown }) recordLoadTerminusResponse consumes
    test/unit/session/command-completion.test.ts full file (206 lines) — the "plain test(), no describe blocks, direct import of pure functions, plain-object-literal inputs" test-shape convention to mirror (this new module reads no process.env, so skip that file's env-var save/restore idiom entirely — it does not apply here)
  </read_first>
  <behavior>
    - createLoadTerminusState(options) resolves loadTerminusMode over the awaitGoalTerminus alias: { loadTerminusMode: "strict" } -> mode "strict"; { awaitGoalTerminus: true } -> mode "metas"; {} or no argument -> mode null; both fields present together -> loadTerminusMode wins.
    - isLoadTerminusSatisfied: mode "strict" is satisfied ONLY by sawLoadError:true. A state shaped like { mode:"strict", sawInteractionPoints:true, sawAllGoalsWarnings:true, sawLoadError:false } — the metas-style signal a clean strict load never actually emits — MUST return false. This is the D-02 false-RED guard at the unit level: if this ever returned true, the extraction would have silently re-imposed the metas gate on the strict path.
    - isLoadTerminusSatisfied: mode "metas" is unchanged — behaves exactly as today's inline `sawLoadError || (sawInteractionPoints && sawAllGoalsWarnings)` expression, including the case where only one of the two positive signals is present (must return false).
    - recordLoadTerminusResponse is a no-op (mutates no field) when state.mode is null. When mode is non-null it flips sawInteractionPoints true on a response with kind "InteractionPoints", flips sawAllGoalsWarnings true on a response with kind "DisplayInfo" and info.kind "AllGoalsWarnings", and flips sawLoadError true on a response with kind "DisplayInfo" and info.kind "Error".
  </behavior>
  <action>
    Create src/session/load-terminus-tracker.ts as a new, self-contained sibling module — no class, matching command-completion.ts's/session-state.ts's snapshot-object-plus-pure-functions shape (NOT session-command-dispatch.ts's whole-object-reference shape), because the terminus fields are a small, self-contained, cohesive unit rather than something needing the full AgdaTransport instance. Export a LoadTerminusMode type ("metas" | "strict" | null); a LoadTerminusOptions interface with two optional fields, awaitGoalTerminus (boolean) and loadTerminusMode (LoadTerminusMode) — this becomes the ONE shared, exported type Task 2 imports at all three downstream call sites instead of tripling a new inline union type, while each call site keeps its existing inline-object-literal PARAMETER style (this consolidation choice is deliberate — see the interfaces block above); a LoadTerminusState interface (mode, sawInteractionPoints, sawAllGoalsWarnings, sawLoadError); and three functions. createLoadTerminusState(options: LoadTerminusOptions = {}) resolves `options.loadTerminusMode ?? (options.awaitGoalTerminus ? "metas" : null)` into the returned state's mode field, with the other three booleans starting false — design this so it can be called EITHER with the real per-call options bag (from sendCommand, in Task 2) OR with no argument at all (from destroy()/sendFireAndForgetCommand()'s resets, also Task 2) and produce the right result both ways, since that is what keeps every reset call site in Task 2 to exactly one line. recordLoadTerminusResponse(state, response: AgdaResponse) is a behavioral port of the current private recordGoalTerminusResponse method body (src/session/agda-transport.ts lines 467-476) operating on the passed-in state object instead of `this`, including its `if (!state.mode) return;` early exit. isLoadTerminusSatisfied(state) is the mode-dependent predicate: return state.sawLoadError when state.mode === "strict" (D-02's resolved design — no positive success event exists for a clean strict load, so only the failure signal can satisfy it); otherwise return the unchanged metas-mode expression `state.sawLoadError || (state.sawInteractionPoints && state.sawAllGoalsWarnings)`. Import AgdaResponse as a type-only import from ../agda/types.js. Give the file a short header comment, matching session-command-dispatch.ts's convention, stating it was extracted from agda-transport.ts specifically to stay under the 500-line ceiling, and stating the D-02 asymmetry in one or two sentences: metas has a positive terminus to await; strict has none, so only failure is awaitable and success is inferred from silence after the widened idle window in command-completion.ts.

    Then create test/unit/session/load-terminus-tracker.test.ts mirroring command-completion.test.ts's plain-test(), no-describe-blocks, direct-pure-function-import convention, covering every case in this task's behavior list: mode resolution (including the both-fields-present precedence case), the D-02 strict-mode guard (both the false case with the metas-shaped signal and the true case with sawLoadError alone), metas-mode parity with today's expression (including a partial-signal false case), and recordLoadTerminusResponse's no-op-when-null behavior plus its three-field tracking across InteractionPoints/AllGoalsWarnings/Error responses.
  </action>
  <verify>
    <automated>npx vitest run test/unit/session/load-terminus-tracker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run test/unit/session/load-terminus-tracker.test.ts` passes with at least 4 test cases covering: mode resolution (loadTerminusMode wins over the awaitGoalTerminus alias; both absent -> null), the D-02 strict-mode guard (metas-shaped signal does NOT satisfy strict; sawLoadError alone DOES), metas-mode parity with today's inline expression, and recordLoadTerminusResponse's no-op-when-null plus its tracking behavior. `grep -c "^export " src/session/load-terminus-tracker.ts` reports at least 6 (LoadTerminusMode, LoadTerminusOptions, LoadTerminusState, createLoadTerminusState, recordLoadTerminusResponse, isLoadTerminusSatisfied). `git diff --stat -- src/session/agda-transport.ts src/agda/session.ts src/agda/session-command-dispatch.ts src/agda/session-load-impl.ts` is empty — this task only lays the (not-yet-wired) foundation Task 2 consumes; it does not touch any existing file.
  </acceptance_criteria>
  <done>load-terminus-tracker.ts exists, is fully unit-tested in isolation, and is not yet imported anywhere — the pure, validated building block Task 2 wires into AgdaTransport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire AgdaTransport to delegate to the tracker and thread loadTerminusMode through session.ts / session-command-dispatch.ts</name>
  <files>src/session/agda-transport.ts, test/unit/session/agda-transport.test.ts, src/agda/session.ts, src/agda/session-command-dispatch.ts</files>
  <read_first>
    src/session/agda-transport.ts lines 1-18 (imports), 49-71 (class fields + the goal-terminus comment/fields being replaced), 121-133 (destroy(), reset at line 131), 170-267 (sendFireAndForgetCommand(), reset at line 211), 269-291 (sendCommand()'s options param at line 273 + reset block at lines 288-291), 416-462 (recordCollectedResponse(), the call site at line 459), 464-483 (recordGoalTerminusResponse/sawGoalTerminus — the two private methods being deleted), 499-534 (bumpIdleCompletionTimer(), the two object literals passed to shouldResolveOnIdle/idleCompletionDelay)
    03.1-PATTERNS.md "src/session/agda-transport.ts (transport, streaming/event-driven) — MODIFY" section in full — exact line anchors, independently re-verified this planning session against the live file
    03.1-RESEARCH.md "Pattern 1: Generalize a boolean gate into a small mode enum" (the validated, built-and-tested prototype diff this task reproduces)
    src/agda/session.ts lines 221-227 (sendCommand method — the pass-through to widen)
    src/agda/session-command-dispatch.ts lines 40-45 and 77 (dispatchSessionCommand's signature + the pass-through call to transport.sendCommand)
    test/unit/session/agda-transport.test.ts lines 52-84 (the existing "awaitGoalTerminus holds completion until the load goal-state terminus arrives" test — the direct clone template for the new strict-mode test) and lines 86-111 ("without awaitGoalTerminus a non-load command resolves on the short idle window" — confirms the fast path for commands with no loadTerminusMode stays unaffected)
    src/session/load-terminus-tracker.ts (Task 1's output — createLoadTerminusState/recordLoadTerminusResponse/isLoadTerminusSatisfied/LoadTerminusOptions/LoadTerminusMode)
  </read_first>
  <behavior>
    - A sendCommand call with { loadTerminusMode: "strict" } behaves like today's { awaitGoalTerminus: true } metas case in ONE respect: a Status+highlighting arriving promptly, followed by a DisplayInfo/Error arriving after a gap longer than the short idle window (mirroring the existing awaitGoalTerminus test's ~45ms gap over a ~5ms window with a tuned AGDA_MCP_LOAD_TERMINUS_IDLE_MS), still surfaces that DisplayInfo/Error response in the resolved array — the widened window bridges the gap instead of truncating the stream.
    - A sendCommand call with { loadTerminusMode: "strict" } and NO further response after Status/highlighting (a clean, hole-less load) still resolves once the long idle window elapses, with no InteractionPoints/AllGoalsWarnings ever having arrived — proving the widened window itself correctly treats silence as completion for the strict path, independent of the real-Agda/real-classification proof Plan 03.1-02 supplies.
    - Every EXISTING test in agda-transport.test.ts and command-completion.test.ts continues to pass unmodified — this task only ADDS a new option value; { awaitGoalTerminus: true } continues to work exactly as before via the backward-compatible alias inside createLoadTerminusState.
  </behavior>
  <action>
    In src/session/agda-transport.ts: add an import of createLoadTerminusState, isLoadTerminusSatisfied, recordLoadTerminusResponse, and the type LoadTerminusOptions from "./load-terminus-tracker.js", placed alongside the existing "./command-completion.js" import. Replace the four private fields awaitGoalTerminus/sawInteractionPoints/sawAllGoalsWarnings/sawLoadError (lines 68-71, with their preceding lines 60-67 explanatory comment) with exactly one field: `private terminus = createLoadTerminusState();` preceded by a short one-to-two-line pointer comment to load-terminus-tracker.ts (the old 8-line explanation now lives in that module's own header comment, so do not duplicate it here — keep this replacement to at most 2 lines total, since the 500-line ceiling depends on it). Replace every reset call site with a single-line reset so this is a strict shrink: in destroy() (currently `this.awaitGoalTerminus = false;`) and in sendFireAndForgetCommand() (currently the same line), change both to `this.terminus = createLoadTerminusState();`; in sendCommand(), replace the four-line reset block with the single line `this.terminus = createLoadTerminusState(options);` (this only works once sendCommand's options parameter is widened to LoadTerminusOptions below, since options is then already shaped exactly like what createLoadTerminusState expects). Delete the two private methods recordGoalTerminusResponse and sawGoalTerminus entirely, including their JSDoc — their logic now lives in the imported recordLoadTerminusResponse/isLoadTerminusSatisfied. Update recordCollectedResponse()'s call site from `this.recordGoalTerminusResponse(response);` to `recordLoadTerminusResponse(this.terminus, response);`. Hoist bumpIdleCompletionTimer()'s two duplicate 5-property object literals (currently built separately for the shouldResolveOnIdle call at lines 506-512 and the idleCompletionDelay call at lines 527-533) into ONE shared `const snapshot = { sawStatusDone: this.sawStatusDone, responseCount: this.responseQueue.length, lastResponseKind: this.lastResponseKind, awaitGoalTerminus: this.terminus.mode !== null, sawGoalTerminus: isLoadTerminusSatisfied(this.terminus) };` declared once, immediately after the method's existing `if (!this.collecting) { return; }` guard, then call `shouldResolveOnIdle(snapshot)` and, further down inside the same setTimeout's delay argument, `idleCompletionDelay(snapshot)` — reusing the identical snapshot object at both call sites instead of building it twice. This hoist is a MANDATORY part of this task, not optional cleanup: it is what clears the 500-line ceiling once the new load-terminus-tracker.js import is counted (it removes the ~4-6 duplicated property lines the two separate literals cost today), and it is semantically inert — command-completion.ts's shouldResolveOnIdle/idleCompletionDelay already consume an opaque snapshot-shaped object per PATTERNS.md (confirmed by this task's own read_first), so behavior is unchanged; command-completion.ts itself needs ZERO changes. Finally widen sendCommand's options parameter from `options: { awaitGoalTerminus?: boolean } = {}` to `options: LoadTerminusOptions = {}`, importing the type instead of re-declaring the inline literal.

    In src/agda/session.ts: widen the sendCommand method's options parameter (lines 221-227, currently `options: { awaitGoalTerminus?: boolean } = {}`) to `options: LoadTerminusOptions = {}`, adding a type-only import of LoadTerminusOptions from "../session/load-terminus-tracker.js". This is a pure pass-through type change — the method body (`return dispatchSessionCommand(this, command, timeoutMs, options);`) is unchanged.

    In src/agda/session-command-dispatch.ts: widen dispatchSessionCommand's options parameter (lines 40-45, same current literal type) the same way, with the same type-only import. The pass-through call at line 77 (`responses = await session.transport.sendCommand(proc, command, timeoutMs, options);`) is unchanged.

    Then extend test/unit/session/agda-transport.test.ts with one new test cloning the structure of the existing "awaitGoalTerminus holds completion until the load goal-state terminus arrives" test (lines 52-84): construct a transport, call sendCommand with `{ loadTerminusMode: "strict" }` instead of `{ awaitGoalTerminus: true }`, simulate Status and highlighting arriving promptly followed by a DisplayInfo response with info.kind "Error" arriving after a gap longer than the short idle window (mirroring the existing test's timing shape and env-var tuning via the same withEnv helper already defined in this file), and assert the resolved response array contains that DisplayInfo response — proving the strict mode's widened window bridges the gap, keyed on the error signal alone rather than the metas-style positive signal. Add a second case (either a second test or additional assertions in the same test) confirming a loadTerminusMode:"strict" command that receives NO further response after Status/highlighting still resolves once the long window elapses — the transport-level version of the D-02 guard, one layer below the full MCP-boundary matrix-entry guard Plan 03.1-02 adds.
  </action>
  <verify>
    <automated>npx vitest run test/unit/session/agda-transport.test.ts test/unit/session/command-completion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `wc -l src/session/agda-transport.ts`, run AFTER the complete edit described above — including the new load-terminus-tracker.js import AND the bumpIdleCompletionTimer() snapshot hoist — reports 500 or fewer: this is a REQUIRED, actually-executed check against the real diff, never asserted or estimated. The bumpIdleCompletionTimer() snapshot hoist is mandatory groundwork for clearing the ceiling (it removes the ~4-6 duplicated lines the new tracker import would otherwise push back over 500) and must already be applied before this count is taken. If the count is STILL over 500 after the hoist, condense ONLY within the lines this task itself touched (the retained pointer comment above `private terminus`, and the three one-line reset call sites) before considering anything else; do NOT shorten or remove any pre-existing comment this task did not already plan to touch (for example the PR #56 regression-history comments inside sendFireAndForgetCommand) just to close the gap — if condensing this task's own lines plus the hoist is still insufficient, STOP and report the actual line count rather than silently overshooting the ceiling (D-05 is a hard FORCED invariant; an overshoot is not an acceptable outcome). `npx vitest run test/unit/session/agda-transport.test.ts test/unit/session/command-completion.test.ts` passes with every pre-existing test unmodified and green (confirms the metas-path awaitGoalTerminus:true behavior — D-05's byte-for-byte-unchanged claim — still holds) plus the new loadTerminusMode:"strict" test(s) passing. `grep -n "private awaitGoalTerminus\|private sawInteractionPoints\|private sawAllGoalsWarnings\|private sawLoadError" src/session/agda-transport.ts` returns no matches — confirming the four scattered fields are fully replaced by the single `terminus` field, not partially. `npm run build` succeeds — confirms the widened LoadTerminusOptions type threads through all three touched files (agda-transport.ts, session.ts, session-command-dispatch.ts) without a TypeScript error.
  </acceptance_criteria>
  <done>AgdaTransport delegates all terminus tracking to load-terminus-tracker.ts, accepts loadTerminusMode end-to-end through session.ts and session-command-dispatch.ts, stays at or under 500 lines, and is behavior-identical to today for every existing caller — nothing yet passes loadTerminusMode:"strict" in production code; that is Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Flip runLoadNoMetas to strict terminus mode and correct the stale doc comments</name>
  <files>src/agda/session-load-impl.ts, src/session/command-completion.ts</files>
  <read_first>
    src/agda/session-load-impl.ts lines 182-200 (runLoadNoMetas's current unguarded call and its now-stale doc comment) and lines 78-87 (runLoad's guarded call — the three-argument call shape being mirrored; confirmed UNCHANGED by this task)
    03.1-PATTERNS.md "src/agda/session-load-impl.ts (service, request-response) — MODIFY" section (the exact validated replacement, plus the "Confirmations for Claude's Discretion" note confirming loadIncompleteNoTerminus/classifyLoadResult need zero changes)
    03.1-RESEARCH.md "State of the Art" section (the "Deprecated/outdated" doc-comment analysis explaining exactly why the current comment is half-superseded) and "Common Pitfalls" Pitfall 1 (why a trailing Status(checked=true) does NOT exist for strict success — the fact the new comment must state)
    src/session/command-completion.ts lines 55-66 (configuredGoalTerminusIdleMs()'s doc comment — currently describes the metas path only)
    test/unit/agda/session-load-impl.test.ts line 548 (confirms an existing stub already accepts a sendCommand call with extra arguments and ignores them beyond the first — proving this call-site change is invisible to that suite)
  </read_first>
  <action>
    In src/agda/session-load-impl.ts's runLoadNoMetas, change the current unguarded call (`session.sendCommand(session.iotcmFor(absPath, command("Cmd_load_no_metas", quoted(absPath))));`) to a three-argument call matching runLoad's shape immediately above it in the same file: pass `undefined` for timeoutMs (keeping the existing default) and `{ loadTerminusMode: "strict" }` for options. Rewrite the preceding five-line comment (currently starting "No awaitGoalTerminus / terminus guard here") to state the corrected design: Cmd_load_no_metas now passes loadTerminusMode:"strict", whose only awaitable positive signal is failure (the same sawLoadError signal every hole/error/type-error shape reliably emits); a clean, hole-less strict load emits no trailing confirmation event at all — highlighting, then nothing further, ever, confirmed against real Agda 2.8.0 — so success is inferred from silence after the widened idle window (AGDA_MCP_LOAD_TERMINUS_IDLE_MS, the same tunable the metas path already uses) rather than from a positive event. Do not restate the old, now-superseded "no terminus is normal completion so no guard is needed" reasoning — that is exactly the reasoning that let a truncated stream masquerade as a clean load before this fix. Everything below this call in runLoadNoMetas (the parseLoadResponses call, the strictFallbackTriggered/hasHoles logic, the returned LoadResult) is unchanged — do not touch it, and do not add a new classification value; only ok-complete/type-error remain reachable.

    In src/session/command-completion.ts, extend configuredGoalTerminusIdleMs()'s doc comment (currently describing only "a metas Cmd_load") to also name the strict Cmd_load_no_metas path, adding one or two sentences noting the asymmetry: metas waits for a positive terminus; strict waits out a silence, because no positive terminus exists for a clean strict load. Do not change the function's name, body, or the env var it reads (AGDA_MCP_LOAD_TERMINUS_IDLE_MS) — 03.1-RESEARCH.md's explicit recommendation is to reuse it verbatim for both paths; this task only updates the comment's accuracy.
  </action>
  <verify>
    <automated>npx vitest run test/unit/agda/session-load-impl.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run test/unit/agda/session-load-impl.test.ts` passes unmodified — confirms the stubbed-sendCommand suite (which ignores extra call arguments) is unaffected by the new third argument. `grep -n 'loadTerminusMode: "strict"' src/agda/session-load-impl.ts` finds exactly one occurrence, inside runLoadNoMetas. `grep -n 'awaitGoalTerminus: true' src/agda/session-load-impl.ts` still finds exactly one occurrence, inside runLoad — confirming the metas path's call site is untouched (D-05). A diff of src/agda/session-load-impl.ts shows no changes below the sendCommand call inside runLoadNoMetas (the parse/classify/return logic is untouched). `npm run build` succeeds.
  </acceptance_criteria>
  <done>runLoadNoMetas requests the strict terminus guard exactly as D-01/D-02 prescribe; both load-path doc comments are accurate; the fix is code-complete and unit-verified against every fake-sendCommand-based test. Plan 03.1-02 supplies the real-Agda proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Agda subprocess stdout -> AgdaTransport | Pre-existing boundary, unchanged shape; the new strict-mode predicate reads the SAME already-normalized response stream (via normalizeAgdaResponse) the existing metas-mode predicate already reads — no new parsing path added |
| Operator env config (AGDA_MCP_LOAD_TERMINUS_IDLE_MS) -> transport idle timer | Pre-existing, reused verbatim for a second internal path; not exposed to any tool-input or untrusted-caller surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01-01 | Tampering | Operator-set `AGDA_MCP_LOAD_TERMINUS_IDLE_MS` (e.g. an extreme value) now also gates the strict path | accept | Pre-existing, already-accepted risk of the metas path's own tunable (03.1-RESEARCH.md Security Domain); extending its use to a second internal path does not change its trust model — this is first-party operator config, never runtime user input |
| T-03.1-01-02 | Tampering | New src/session/load-terminus-tracker.ts reads response.kind / response.info.kind off the same normalized stream agda-transport.ts already consumes | accept | No new parsing logic — reuses the SAME already-normalized AgdaResponse shape (normalizeAgdaResponse), no raw-string/regex scanning introduced |
| T-03.1-01-SC | Tampering | npm/pip/cargo installs | N/A | No new package-manager installs in this plan |
</threat_model>

<verification>
- `npx vitest run test/unit/session/ test/unit/agda/` — every unit suite touched by this plan (load-terminus-tracker, agda-transport, command-completion, session-load-impl) green, zero regressions.
- `npm run build` succeeds — TypeScript compiles cleanly across all four threading sites.
- `wc -l src/session/agda-transport.ts` reports 500 or fewer.
- Real-Agda end-to-end proof (the flagship flip, the D-05 guard, the metas-path control, the 72-fixture strict breadth, and the full suite) is deliberately Plan 03.1-02's job, not this plan's — matching 03.1-RESEARCH.md's tiered Sampling Rate (fast per-task-commit checks here; real-Agda per-wave/phase-gate checks next).
</verification>

<success_criteria>
- src/session/load-terminus-tracker.ts exists, is fully unit-tested, and is the sole owner of terminus-tracking state and the mode-dependent predicate.
- src/session/agda-transport.ts delegates to it, stays at or under 500 lines, and every existing metas-path test passes unmodified.
- loadTerminusMode compiles cleanly through all four sites: session-load-impl.ts -> session.ts -> session-command-dispatch.ts -> agda-transport.ts.
- runLoadNoMetas requests { loadTerminusMode: "strict" }; runLoad's { awaitGoalTerminus: true } call is untouched.
- Zero wire-level IOTCM command-string changes (command-builder SSOT intact); no new LoadResult classification value introduced.
</success_criteria>

<output>
Create `.planning/phases/03.1-fix-the-64-61-transitive-staleness-false-green-and-flip-the-/03.1-01-SUMMARY.md` when done
</output>
