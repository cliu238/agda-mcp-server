---
phase: 10-upstream-reconcile
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/tools/register-core-tools.ts
  - src/session/tool-recommendation.ts
  - src/tools/data/tool-family-examples.json
  - README.md
  - docs/assistant-workflows.md
autonomous: true
requirements: [ADOPT-01, ADOPT-02]

must_haves:
  truths:
    - "agda_goal_candidates appears in listToolManifest() / agda_tools_catalog output"
    - "agda_goal_candidates is recommended by tool-recommendation.ts when goals are open"
    - "A driving agent can discover agda_goal_candidates from README.md or docs/assistant-workflows.md without reading source"
    - "The tool name is kept verbatim, never renamed (D-08)"
  artifacts:
    - path: "src/tools/register-core-tools.ts"
      provides: "registerGoalCandidates(server, session, projectRoot) wired as the 14th register() call"
      contains: "registerGoalCandidates"
    - path: "src/tools/data/tool-family-examples.json"
      provides: "a proof-family example entry for agda_goal_candidates"
      contains: "agda_goal_candidates"
  key_links:
    - from: "src/tools/register-core-tools.ts"
      to: "src/tools/register-goal-candidates.ts"
      via: "import + registerGoalCandidates(server, session, projectRoot) call"
      pattern: "registerGoalCandidates"
    - from: "src/session/tool-recommendation.ts"
      to: "agda_goal_candidates"
      via: "addIfAvailable(...) inside the has-holes branch"
      pattern: "agda_goal_candidates"
---

<objective>
Wire upstream's already-merged `agda_goal_candidates` tool (type-directed term search + Mimer auto-fix, carried in net-new by Plan 10-01's merge, no conflict) through this project's own discoverability conventions: the manifest SSOT (automatic once registered), `tool-recommendation.ts`, `tool-family-examples.json`, and the human-facing docs (README.md, docs/assistant-workflows.md). Keep the tool name verbatim per D-08 — renaming creates a permanent re-conflict on every future 3-day sync.

Purpose: ADOPT-01/02 require the tool to work through the manifest/recommendation system AND be documented well enough that a driving agent (Codex/Claude Code) discovers it without reading source.
Output: `agda_goal_candidates` registered, recommended when goals are open, and mentioned in both README.md and docs/assistant-workflows.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-upstream-reconcile/10-CONTEXT.md
@.planning/phases/10-upstream-reconcile/10-RESEARCH.md
@.planning/phases/10-upstream-reconcile/10-PATTERNS.md
</context>

<interfaces>
`src/tools/register-goal-candidates.ts` (carried in verbatim by the merge, already read in
full during planning): exports `registerGoalCandidates(server: McpServer, session: AgdaSession,
_projectRoot: string): void`. It internally calls `registerStructuredTool` (same helper every
other tool in this file uses), which auto-calls `registerManifestEntry` — so once wired, the
manifest, `agda_tools_catalog`, and `listToolManifest()` all pick up the tool with ZERO
additional code. Its own `logger.warn("goal_candidates typeContext query failed", ...)` best-
effort-catch site is ALREADY covered by an `expectWarning(...)` call inside the carried-in
`test/unit/tools/goal-candidates.test.ts` — do not add a second registration for it, and do
not treat it as MERGE-02's business.

`src/tools/register-core-tools.ts` (13 existing `register()` calls, flat sequence, each
taking exactly `(server, session, projectRoot)`, no conditional logic) is where the 14th call
goes — follow the exact same import + call shape as the other 13, alphabetically-unordered
(the file has no ordering convention to preserve; append at the end after
`registerAgentUxTools`).

`src/session/tool-recommendation.ts` (270 lines) has a "has holes" branch (the
`if (input.goalIds.length > 0) { ... }` block) that already recommends `agda_goal_catalog`
(priority 2), `agda_goal_type` (3), `agda_context` (4), `agda_auto` (5), `agda_case_split` (6),
`agda_solve_all` (7), `agda_refine` (8), each via `addIfAvailable(recommendations, toolSet,
input.availableTools, { tool, rationale, priority, knownArgs, blockers })`.
`addIfAvailable` no-ops safely if the tool name isn't yet in `toolSet` (manifest-derived), so
wiring order between this file and register-core-tools.ts does not matter for correctness.

`test/fixtures/e2e/mcp-tool-coverage.json` ALREADY has an `agda_goal_candidates` entry
(carried in by the merge, verified live — this is NOT something you need to add; it auto-
merged cleanly). `test/unit/tools/mcp-e2e-coverage.test.ts` asserts `matrixNames ===
manifestNames` (a strict 1:1 lockstep) — this test is RED right now (coverage entry exists,
manifest entry does not, because the tool isn't wired yet) and will go GREEN the moment this
plan's Task 1 completes. Confirming it turns green IS this plan's proof that ADOPT-01's
manifest-registration requirement is met — no new test needs to be authored.

`src/tools/data/tool-family-examples.json`'s own `$comment` warns "format change must be
matched by `src/tools/tool-family-examples.ts`" — you are only adding a data array entry
under the existing `"proof"` family (same shape as the existing `agda_case_split`/
`agda_give`/`agda_auto` entries), not changing the format, so no `.ts` consumer change is
needed.

`README.md`'s "What it can do" section (around line 86-93) already names representative
tools per category and explicitly defers to `agda_tools_catalog` for the full list — add
`agda_goal_candidates` to the "proof actions" clause there as a named example, do not restructure
the section.

`docs/assistant-workflows.md`'s "Goal-level work: all goals in one call" section (around
line 59-77) documents the `agda_goal_catalog` all-goals-in-one-call workflow — add a short
mention of `agda_goal_candidates` there (or immediately after) as the type-directed-fillable-
term counterpart, since it serves the same "avoid looping per-goal" discoverability goal this
section already targets.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire the manifest + recommendation layers</name>
  <files>src/tools/register-core-tools.ts, src/session/tool-recommendation.ts</files>
  <read_first>
    src/tools/register-core-tools.ts
    src/tools/register-goal-candidates.ts
    src/session/tool-recommendation.ts
  </read_first>
  <action>
In `src/tools/register-core-tools.ts`, add `import { registerGoalCandidates } from
"./register-goal-candidates.js";` alongside the other 13 tool-group imports, then add
`registerGoalCandidates(server, session, projectRoot);` as a new call inside
`registerCoreTools`, following the exact same `(server, session, projectRoot)` signature
every other call uses — no feature flag, no conditional.

In `src/session/tool-recommendation.ts`'s has-holes branch, add a new `addIfAvailable(...)`
call for `agda_goal_candidates` alongside the existing entries. Give it a rationale describing
its type-directed local-candidate-search purpose (distinct from `agda_auto`'s full proof
search and `agda_case_split`'s structural splitting), a `knownArgs: {}` (it takes no required
args, just an optional `limitPerGoal`), `blockers: []`, and a priority slot that places it
sensibly among the existing 2-8 range (e.g. immediately after `agda_context` since both are
read-only inspection tools that typically precede a write action like `agda_auto`/
`agda_case_split`/`agda_refine`) — renumber neighbors only if truly necessary, prefer a
decimal or an unused integer slot to minimize diff noise.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/mcp-e2e-coverage.test.ts test/unit/session/tool-recommendation.test.ts test/unit/tools/goal-candidates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "registerGoalCandidates" src/tools/register-core-tools.ts` shows both the import line and the call line
    - `grep -n "agda_goal_candidates" src/session/tool-recommendation.ts` shows a new `addIfAvailable` entry inside the has-holes branch
    - `npx vitest run test/unit/tools/mcp-e2e-coverage.test.ts` passes (matrixNames === manifestNames, proving the tool is now manifest-registered)
    - `npx vitest run test/unit/tools/goal-candidates.test.ts` still passes unmodified
  </acceptance_criteria>
  <done>agda_goal_candidates is registered through register-core-tools.ts exactly like the other 13 tool groups, is manifest-visible (mcp-e2e-coverage.test.ts green), and is recommended by tool-recommendation.ts when goals are open.</done>
</task>

<task type="auto">
  <name>Task 2: Documentation + discoverability polish</name>
  <files>src/tools/data/tool-family-examples.json, README.md, docs/assistant-workflows.md</files>
  <read_first>
    src/tools/data/tool-family-examples.json
    README.md
    docs/assistant-workflows.md
  </read_first>
  <action>
Add one entry to `src/tools/data/tool-family-examples.json`'s `"proof"` array (same shape as
the existing `agda_case_split`/`agda_give`/`agda_auto` entries: `tool`, `summary`, `args`,
optional `note`) for `agda_goal_candidates` — a representative invocation with a `goalId` arg,
summarizing it as a type-directed local-term-candidate search that covers every open goal in
one call.

Edit README.md's "What it can do" section: extend the proof-actions clause
(`agda_case_split`, `agda_give`, `agda_refine`, `agda_auto`) to also name
`agda_goal_candidates` as the type-directed term-search / Mimer-auto-fix tool, without
restructuring the surrounding sentence or removing the existing "call agda_tools_catalog for
the full list" deferral.

Edit docs/assistant-workflows.md's "Goal-level work: all goals in one call" section: add a
short paragraph or bullet noting that `agda_goal_candidates` complements `agda_goal_catalog`
by additionally listing type-directed fillable terms per goal in a single call, pointing an
agent at it as the next step after `agda_goal_catalog` when it wants concrete candidate terms
rather than just goal types/contexts.
  </action>
  <verify>
    <automated>npx vitest run test/unit/tools/tool-family-examples.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "agda_goal_candidates" src/tools/data/tool-family-examples.json` shows a new entry inside the `"proof"` array
    - `grep -n "agda_goal_candidates" README.md` shows a new mention in the "What it can do" section
    - `grep -n "agda_goal_candidates" docs/assistant-workflows.md` shows a new mention in the goal-level-work section
    - `npx vitest run test/unit/tools/tool-family-examples.test.ts` passes (JSON still validates against its zod schema)
  </acceptance_criteria>
  <done>agda_goal_candidates is discoverable from README.md, docs/assistant-workflows.md, and agda_tools_catalog's worked-example list without reading source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| agent-supplied limitPerGoal -> registerStructuredTool schema | Untrusted MCP client input into the tool's one parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Tampering | tool naming / manifest routing | mitigate | Keep the exact upstream name `agda_goal_candidates` (D-08) and route exclusively through `registerStructuredTool`/`registerManifestEntry` (the existing SSOT), never a hand-rolled parallel registration path |
| T-10-10 | Denial of Service | goal-candidate generation loop | accept | The tool's own `limitPerGoal` input is already zod-bounded (`z.number().int().min(1).max(100).optional()`, verified against the merged source) and the goal-iteration loop is bounded by the session's already-loaded goal count — no new unbounded surface introduced by adoption |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new package installs in this plan |

</threat_model>

<verification>
`npx vitest run test/unit/tools/mcp-e2e-coverage.test.ts test/unit/session/tool-recommendation.test.ts test/unit/tools/goal-candidates.test.ts test/unit/tools/tool-family-examples.test.ts` must all pass.
</verification>

<success_criteria>
- agda_goal_candidates registered via register-core-tools.ts, manifest-visible, recommended when goals are open
- Documented in tool-family-examples.json, README.md, and docs/assistant-workflows.md
- Tool name unchanged from upstream (D-08)
- mcp-e2e-coverage.test.ts green (was red immediately post-merge, now proven fixed)
</success_criteria>

<output>
Create `.planning/phases/10-upstream-reconcile/10-03-SUMMARY.md` when done
</output>
</content>
