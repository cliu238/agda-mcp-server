---
phase: 10-upstream-reconcile
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  # NOTE: this list is the UNION of Task 2's mutually-exclusive GREEN/RED branches —
  # a single execution touches only one branch's subset PLUS the unconditional files
  # (docs/LOAD-TERMINUS-ADJUDICATION.md and test/unit/agda/session-load-impl.test.ts,
  # which needs correction in BOTH branches per Plan 10-01's CLAUDE.md fix).
  - src/session/agda-transport.ts
  - src/session/command-completion.ts
  - src/agda/session-load-impl.ts
  - src/session/load-terminus-tracker.ts
  - test/unit/session/load-terminus-tracker.test.ts
  - src/agda/session.ts
  - src/agda/session-command-dispatch.ts
  - test/unit/agda/session-load-impl.test.ts
  - test/unit/session/agda-transport.test.ts
  - docs/LOAD-TERMINUS-ADJUDICATION.md
autonomous: true
requirements: [MERGE-03]

must_haves:
  truths:
    - "Both capture-regression-matrix agda_load_no_metas entries (issue-64-61-transitive-staleness, guard-no-metas-clean-load-under-fault-injection) pass"
    - "Upstream's own #68/#69 regression test (agda-stale-dependency.test.ts) passes"
    - "A durable adjudication record exists naming ours/theirs/hybrid + rationale + referee evidence for each of the 3 named sub-behaviors"
    - "Whichever implementation is kept, a fatal protocol stderr unblocks the load terminus instead of waiting the full 120s command timeout"
    - "test/unit/agda/session-load-impl.test.ts asserts the return-based load-incomplete-no-terminus semantics (never rejects.toThrow) and contains the goal-ID recovery test, in BOTH adjudication branches"
  artifacts:
    - path: "docs/LOAD-TERMINUS-ADJUDICATION.md"
      provides: "One table row per sub-behavior (completion-signal detection, fatal-stderr handling, inactivity timeout): decision, rationale, referee evidence"
      contains: "capture-regression-matrix"
  key_links:
    - from: "test/integration/mcp/capture-regression.test.ts"
      to: "src/agda/session-load-impl.ts"
      via: "MCP tool-call boundary (agda_load_no_metas)"
      pattern: "agda_load_no_metas"
---

<objective>
Run the actual MERGE-03 referee (the two capture-regression-matrix.json agda_load_no_metas entries, plus upstream's own agda-stale-dependency.test.ts) against Plan 10-01's candidate (upstream's whole-file architecture, already corrected for the two forced CLAUDE.md/regression fixes), and act on the empirical result exactly as D-04 specifies: green means confirm upstream's architecture and document it; red means revert to ours via the pre-merge parent commit, graft upstream's fatal-stderr + inactivity-watchdog hardening regardless, restore the two files whose auto-merge silently narrowed a shared type, and rewrite (never delete) the upstream-authored test assertions that assumed the losing architecture. In BOTH branches, correct test/unit/agda/session-load-impl.test.ts's throw-based truncation assertions — Plan 10-01's unconditional CLAUDE.md fix (return-based loadIncompleteNoTerminus + recovery-block port) makes upstream's auto-merged assertions stale regardless of the adjudication outcome.

Purpose: MERGE-03 is explicitly empirical, "do not pre-decide winners" (D-04). This plan is the one place in the phase that runs the actual referee and commits to a documented, evidence-based decision.
Output: docs/LOAD-TERMINUS-ADJUDICATION.md recording the decision; both referee suites green; the codebase in a single coherent state (never a hybrid) regardless of which side won.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-upstream-reconcile/10-CONTEXT.md
@.planning/phases/10-upstream-reconcile/10-RESEARCH.md
@.planning/phases/10-upstream-reconcile/10-PATTERNS.md
@docs/DEPLOY-OPERATIONS.md
@CLAUDE.md
</context>

<interfaces>
Ground truth verified live during planning via an actual dry-run merge and git-show against
the merge-base — do not re-derive, use directly.

The referee command (real Agda, must actually run, this is the load-bearing check, not a
stand-in): export PATH to the Node 24 mise install, then
RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts
test/integration/agda/agda-stale-dependency.test.ts (see the automated verify line for the
exact one-liner). The first file drives BOTH matrix entries at the real MCP tool-call
boundary (test/helpers/capture-regression-runner.ts's replayCaptureRegressionEntry, which
spawns a real server subprocess via createMcpHarness and calls agda_load_no_metas,
implementation-agnostic, so it works regardless of which architecture is currently in
session-load-impl.ts). The second file is upstream's OWN agda_load (metas path) regression
test for the same historical bug class (#61/#64), MERGE-03's success text requires "BOTH
repos' regression suites green," and this file is the concrete stand-in for "upstream's own
#68/#69 regression tests." Do NOT treat RT8 (test/unit/session/register-agda-load-no-metas.test.ts)
as a referee input, it mocks the session entirely and never discriminates between
implementations (verified during research); it should simply stay green throughout and is
not evidence either way.

git show HEAD^1:PATH is how to recover our exact pre-merge content for any of the 3
architectural files (HEAD is Plan 10-01's merge commit, ^1 is its first parent, i.e. our main
tip before the merge) — use this for the RED branch below rather than trying to
hand-reconstruct the pre-merge file from memory.

Two files are NOT in the 6-conflict list but auto-merged to upstream's narrower shape as a
side effect of adopting upstream's architecture in Plan 10-01: src/agda/session.ts and
src/agda/session-command-dispatch.ts both dropped their "import type LoadTerminusOptions from
../session/load-terminus-tracker.js" and now type their options parameter as the narrower
"{ awaitGoalTerminus?: boolean }" (upstream's shape has no loadTerminusMode concept at all,
since it deleted the strict/metas distinction). If the RED branch below applies, these two
files must be manually restored to import and use the wider LoadTerminusOptions type again
(re-imported from ../session/load-terminus-tracker.js, which the RED branch also restores),
otherwise runLoadNoMetas's restored "{ loadTerminusMode: 'strict' }" call will fail to
typecheck.

Two test files already auto-merged to assert UPSTREAM's semantics specifically (verified
live, neither is in the 6-conflict list): test/unit/agda/session-load-impl.test.ts (its
runLoad/runLoadNoMetas tests now assert rejects.toThrow(...) instead of a returned
classification, and its dropped-goal-ID recovery test was replaced) and
test/unit/session/agda-transport.test.ts (two new tests: "inactivity watchdog keeps a
steadily-progressing load alive past the timeout window" and "a fatal protocol stderr
completes a load instead of hanging until timeout", both exercise upstream-only mechanisms).
Per D-05, these may be REWRITTEN to assert the adjudicated semantics (with rationale), never
deleted.

IMPORTANT — the session-load-impl.test.ts correction is UNCONDITIONAL, not branch-dependent:
Plan 10-01 Task 2 already replaced the throw-based !parsed.sawLoadTerminus guards with
return-based finalizeEarlyReturn(session, loadIncompleteNoTerminus(...)) in BOTH runLoad and
runLoadNoMetas (its acceptance criterion is literally zero throw-new-Error hits inside those
guards), and ported back the goal-ID recovery block — regardless of what the referee decides
about the strict-load architecture. Upstream's auto-merged throw-based assertions in this
test file are therefore stale in the GREEN branch too, and the pre-merge recovery test is
missing in both. Task 2 below folds this correction in unconditionally.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the referee against Plan 10-01's candidate</name>
  <files>(no source edits, evidence-gathering only)</files>
  <read_first>
    test/fixtures/capture-regression-matrix.json
    test/integration/mcp/capture-regression.test.ts
    test/integration/agda/agda-stale-dependency.test.ts
    src/agda/session-load-impl.ts
  </read_first>
  <action>
Confirm the merge commit from Plan 10-01 is present (git log -1 --format=%s shows the
"merge: upstream/main" commit) and that src/agda/session-load-impl.ts currently carries
upstream's candidate (grep for the runLoadNoMetas body sending plain Cmd_load with an empty
options list, the marker that the strict path currently sends Cmd_load, not
Cmd_load_no_metas). Run the referee command from the interfaces block exactly as written
(both integration test files, RUN_AGDA_INTEGRATION=1, real Node 24 on PATH). Record the raw
pass/fail outcome for: (a) issue-64-61-transitive-staleness, (b)
guard-no-metas-clean-load-under-fault-injection, (c) upstream's agda-stale-dependency.test.ts's
single test. If the run reports an unexpected logger.warn-guard failure (rather than a
classification mismatch) on any of these — possible since vitest.config.ts's
setupFiles: ["test/helpers/warn-guard.ts"] is globally active starting from Plan 10-01's
merge commit — treat that as a SEPARATE MERGE-02 concern (Plan 10-04's job), not evidence
against the architecture; note it for that plan rather than trying to fix it here.
  </action>
  <verify>
    <automated>export PATH="/Users/eric/.local/share/mise/installs/node/24/bin:$PATH" && RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts test/integration/agda/agda-stale-dependency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both capture-regression-matrix agda_load_no_metas test names ran (not skipped, confirm RUN_AGDA_INTEGRATION=1 and a real agda binary were actually picked up, not silently test.skip'd)
    - The raw pass/fail result for all 3 referee tests is recorded in your own working notes, to feed Task 2 (no file write required yet)
  </acceptance_criteria>
  <done>The referee has actually run against real Agda, and you have an unambiguous green/red verdict for the strict-load architecture question.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the adjudication and write the durable record</name>
  <files>docs/LOAD-TERMINUS-ADJUDICATION.md (new) and test/unit/agda/session-load-impl.test.ts (both unconditional), plus conditionally per branch: src/session/agda-transport.ts, src/session/command-completion.ts, src/agda/session-load-impl.ts, src/session/load-terminus-tracker.ts (restore), test/unit/session/load-terminus-tracker.test.ts (restore), src/agda/session.ts, src/agda/session-command-dispatch.ts, test/unit/session/agda-transport.test.ts</files>
  <read_first>
    docs/DEPLOY-OPERATIONS.md
    src/session/agda-transport.ts
    test/unit/agda/session-load-impl.test.ts
  </read_first>
  <action>
UNCONDITIONAL (both branches — forced by Plan 10-01's CLAUDE.md correction, independent of
the referee verdict): correct test/unit/agda/session-load-impl.test.ts to match the restored
return-based semantics. (a) The auto-merged test currently named "runLoad throws (no
fabricated success) when the process ended before the goal state" must assert the RETURNED
LoadResult instead of rejects.toThrow: result.success === false, result.classification ===
"load-incomplete-no-terminus", result.errors[0] matching /no terminal goal-state event/i (the
loadIncompleteNoTerminus message text), session.lastClassification ===
"load-incomplete-no-terminus" (finalizeEarlyReturn records the attempt — NOT null, which the
upstream version asserted after its throw), while keeping the existing metasCalls === 0,
session.goalIds deep-equal [], and session.currentFile null assertions. The pre-merge version
of this exact test (recoverable via git show HEAD^1:test/unit/agda/session-load-impl.test.ts,
test name "runLoad reports incomplete when the response stream has no terminal event") is the
reference shape. (b) Re-add the pre-merge test "runLoad recovers dropped visible goal IDs via
a metas re-query when source has holes" that upstream's auto-merge replaced — recover its
exact body from git show HEAD^1, and KEEP upstream's replacement test ("runLoadNoMetas
accepts a clean strict load reporting an empty goal state") too; the two coexist. (c) The
strict-path truncation test ("runLoadNoMetas throws (no false green) when the process ended
before the goal state") is the one branch-DEPENDENT rewrite: in the GREEN branch (upstream
architecture + 10-01's correction, where runLoadNoMetas has a sawLoadTerminus guard), rewrite
it the same way as (a) — assert a returned "load-incomplete-no-terminus" classification and
lastClassification, never a throw; in the RED branch (ours restored, where the strict path
deliberately has NO sawLoadTerminus check — the documented D-02 asymmetry), restore the
pre-merge expectation instead ("runLoadNoMetas accepts a clean strict load with no goal-state
terminus" asserting ok-complete, recoverable from git show HEAD^1). Preserve every
expectWarning(...) registration upstream added to this file (they are MERGE-02 assets).

IF Task 1's referee was GREEN for all 3 tests: no further SOURCE change is needed, Plan
10-01's candidate (upstream's architecture) stands as the adjudicated winner for all three
sub-behaviors (completion-signal detection AND fatal-stderr handling AND inactivity timeout
come bundled in the same adopted files). Confirm test/unit/session/agda-transport.test.ts
passes as-is (it already asserts upstream's transport semantics) and that the
unconditionally-corrected session-load-impl.test.ts above passes against the merged
implementation.

IF Task 1's referee was RED on either capture-regression-matrix entry: revert to ours and
graft the hardening, per D-04's explicit fallback text. Concretely: restore
src/session/agda-transport.ts, src/session/command-completion.ts, and
src/agda/session-load-impl.ts from git show HEAD^1:PATH (our pre-merge originals). Restore
src/session/load-terminus-tracker.ts and test/unit/session/load-terminus-tracker.test.ts the
same way (git checkout HEAD^1 -- PATH). Restore the wider LoadTerminusOptions type in
src/agda/session.ts and src/agda/session-command-dispatch.ts per the interfaces block. Then
GRAFT upstream's two hardening improvements into the restored (ours) implementation
regardless of this being the "keep ours" branch, D-04 mandates this unconditionally: (1)
fatal-stderr-as-terminus — make recordLoadTerminusResponse (or the transport's
response-recording path) also flip a sawFatalStderr-style flag when a StderrOutput response
matches isFatalProtocolStderr (present in the already-merged src/agda/protocol-errors.ts,
exported net-new by upstream's version alongside the still-present
throwOnFatalProtocolStderr), and make isLoadTerminusSatisfied treat that flag as satisfying
either mode; (2) inactivity watchdog — port the resetInactivityTimer / re-arm-on-each-response
mechanism from upstream's sendCommand into ours, replacing the fixed absolute timeoutMs
semantics with a silence-reset watchdog (the command-wait-diagnostics.ts helper upstream
introduced for its warn-message shape is net-new and unconflicted, safe to reuse either way).
Then, in test/unit/session/agda-transport.test.ts, confirm the two upstream-only new tests
("inactivity watchdog...", "a fatal protocol stderr completes a load...") now pass against
the grafted hardening — if they still fail after grafting, adjust the grafted implementation
until they do, since these tests correctly describe behavior D-04 requires regardless of
which side "won" (rewrite assertions only per D-05, with rationale, never delete).

Either way, write docs/LOAD-TERMINUS-ADJUDICATION.md following docs/DEPLOY-OPERATIONS.md's
established durable-record shape (H1 title, framing paragraph naming Phase 10/MERGE-03 as
origin, then one table row per sub-behavior). Cover all three sub-behaviors explicitly,
including inactivity-timeout even in the GREEN branch (state plainly that it ships bundled
with the same file swap, not separately adjudicated). Each row: sub-behavior name, decision
(ours/theirs/hybrid), rationale, referee evidence (which of the 3 referee tests ran,
pass/fail, command used).
  </action>
  <verify>
    <automated>export PATH="/Users/eric/.local/share/mise/installs/node/24/bin:$PATH" && RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts test/integration/agda/agda-stale-dependency.test.ts test/unit/session/agda-transport.test.ts test/unit/agda/session-load-impl.test.ts test/unit/session/register-agda-load-no-metas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - docs/LOAD-TERMINUS-ADJUDICATION.md exists, contains a table with one row per sub-behavior (completion-signal detection, fatal-stderr handling, inactivity timeout), and cites the exact referee command + pass/fail evidence
    - All 3 referee tests from Task 1 pass
    - `grep -c "ended before completing" test/unit/agda/session-load-impl.test.ts` returns 0 (no rejects.toThrow-based truncation assertions remain — rewritten to return-based classification assertions in both branches)
    - `grep -n "recovers dropped visible goal IDs" test/unit/agda/session-load-impl.test.ts` shows the pre-merge recovery test re-added
    - `grep -c "load-incomplete-no-terminus" test/unit/agda/session-load-impl.test.ts` returns at least 1 (the rewritten truncation test asserts the returned classification)
    - test/unit/session/agda-transport.test.ts passes in full, including the two upstream-authored tests exercising the inactivity watchdog and fatal-stderr-as-terminus behavior
    - test/unit/agda/session-load-impl.test.ts passes in full
    - test/unit/session/register-agda-load-no-metas.test.ts (RT8) still passes, unaffected
    - `npx tsc -p tsconfig.test.json --noEmit` exits 0 (catches the session.ts/session-command-dispatch.ts typing casualty if the RED branch was taken and not fully restored)
  </acceptance_criteria>
  <done>The adjudication decision is made, documented with referee evidence, session-load-impl.test.ts asserts the return-based semantics (both branches) with the recovery test restored, and the codebase reflects a single coherent implementation (never a hybrid) with the mandatory fatal-stderr and inactivity-watchdog hardening present regardless of which side won.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| fault-injected test harness -> real Agda subprocess | Idle-timing fault injection (AGDA_MCP_IDLE_COMPLETION_MS=1) driving the referee |
| Agda stderr -> transport completion signal | Fatal protocol rejection text parsed as a trust signal for early unblock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | src/agda/session-load-impl.ts strict-load path | mitigate | Decision is made ONLY from the real referee run (Task 1), never from code-reading/taste; both capture-regression-matrix entries must independently pass before any doc is written |
| T-10-06 | Repudiation | docs/LOAD-TERMINUS-ADJUDICATION.md | mitigate | Every row cites the exact command and pass/fail result actually observed, not an assumed outcome |
| T-10-07 | Denial of Service | fatal-protocol-stderr / inactivity handling | mitigate | Fatal-stderr-as-terminus and the inactivity watchdog are grafted regardless of which architecture wins (D-04 unconditional requirement) — prevents a malformed IOTCM or a stalled Agda process from hanging a load for the full 120s command timeout |
| T-10-08 | Tampering | CLAUDE.md error-handling convention | mitigate | Plan 10-01 already restored return-based `loadIncompleteNoTerminus` semantics; this plan's UNCONDITIONAL test correction keeps session-load-impl.test.ts aligned with that in both branches — the throw-based upstream assertions are never allowed to stand |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new package installs in this plan |

</threat_model>

<verification>
Referee suite plus the two directly-affected unit test files plus RT8 must all be green before this plan is considered complete: `export PATH="/Users/eric/.local/share/mise/installs/node/24/bin:$PATH" && RUN_AGDA_INTEGRATION=1 npx vitest run test/integration/mcp/capture-regression.test.ts test/integration/agda/agda-stale-dependency.test.ts test/unit/session/agda-transport.test.ts test/unit/agda/session-load-impl.test.ts test/unit/session/register-agda-load-no-metas.test.ts`. Follow with `npx tsc -p tsconfig.test.json --noEmit` to catch any typing casualty from the RED branch.
</verification>

<success_criteria>
- Both capture-regression-matrix agda_load_no_metas entries pass
- Upstream's own agda-stale-dependency.test.ts passes
- docs/LOAD-TERMINUS-ADJUDICATION.md exists with a decision + rationale + referee evidence row per sub-behavior
- session-load-impl.test.ts asserts return-based load-incomplete-no-terminus semantics and contains the restored recovery test (both branches)
- The codebase reflects one coherent implementation (no hybrid), with fatal-stderr-as-terminus and the inactivity watchdog present regardless of outcome
- typecheck:test exits 0
</success_criteria>

<output>
Create `.planning/phases/10-upstream-reconcile/10-02-SUMMARY.md` when done
</output>
</content>
