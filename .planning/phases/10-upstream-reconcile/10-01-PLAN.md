---
phase: 10-upstream-reconcile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agda/refactor-helpers.ts
  - test/unit/agda/agent-ux.test.ts
  - src/tools/goal-tools.ts
  - src/tools/goal-write-tools.ts
  - src/session/agda-transport.ts
  - src/session/command-completion.ts
  - src/agda/session-load-impl.ts
  - src/session/load-terminus-tracker.ts
  - test/unit/session/load-terminus-tracker.test.ts
autonomous: true
requirements: [MERGE-01]

must_haves:
  truths:
    - "D-01: git log on main shows a single real merge commit (git merge --no-ff, never rebase) bringing in upstream d4497a2, with all 6 conflict files resolved by hand in this attended phase"
    - "D-02: the merge target stays pinned at d4497a2 — any upstream commits landing after it are out of scope (Phase 11's first routine sync picks them up)"
    - "D-03: package.json's 0.6.8 version bump is accepted mechanically via its clean auto-merge; no v1.x milestone tag work happens in this phase"
    - "No conflict markers (<<<<<<<, =======, >>>>>>>) remain anywhere in the tree after the commit"
    - "npm run build exits 0"
    - "npm run typecheck:test exits 0"
    - "Our T-06-12/RT4 flag-shaped-hint regression guard (assertValidAutoHint) is still present and still enforced by agda_auto's default code path"
  artifacts:
    - path: "src/agda/refactor-helpers.ts"
      provides: "buildAutoSearchPayload(options, engine) with both mimer and agsy syntax branches, assertValidAutoHint preserved in the agsy branch"
    - path: "src/tools/goal-write-tools.ts"
      provides: "agda_auto registration dispatching engine via usesMimerProofSearch(session.getAgdaVersion())"
    - path: "docs/LOAD-TERMINUS-ADJUDICATION.md"
      provides: "not created by this plan — Plan 10-02 owns it; this plan only leaves the architectural candidate in a state Plan 10-02 can adjudicate"
  key_links:
    - from: "src/tools/goal-write-tools.ts"
      to: "src/agda/version-support.ts"
      via: "usesMimerProofSearch(session.getAgdaVersion()) import"
      pattern: "usesMimerProofSearch"
    - from: "src/agda/session-load-impl.ts"
      to: "src/agda/session-load-helpers.ts"
      via: "loadIncompleteNoTerminus(...) return (not throw)"
      pattern: "loadIncompleteNoTerminus"
---

<objective>
Execute the real upstream merge (D-01/D-02: `git merge --no-ff upstream/main`, never rebase, pinned at `d4497a2`) and resolve all 6 documented conflict files to a single, buildable, typecheck-clean merge commit. Four files (refactor-helpers.ts, agent-ux.test.ts, goal-tools.ts + its sibling goal-write-tools.ts) are mechanical/structural and resolved definitively in this plan. The three architectural files (agda-transport.ts, command-completion.ts, session-load-impl.ts) are resolved to upstream's candidate implementation as the FIRST TESTABLE STATE — Plan 10-02 runs the actual referee against this candidate and may revert it; this plan's job is only to get to a coherent, buildable starting point, not to make the final MERGE-03 call.

Purpose: MERGE-01 requires the merge to actually exist as a real merge commit with all 6 conflicts resolved and build/typecheck green. This is also the substrate every other Phase 10 plan depends on.
Output: One merge commit on `main` (local only — nothing pushed per D-10), `src/session/load-terminus-tracker.ts` and its unit test deleted (D-04's default-candidate branch), `npm run build` and `npm run typecheck:test` both exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-upstream-reconcile/10-CONTEXT.md
@.planning/phases/10-upstream-reconcile/10-RESEARCH.md
@.planning/phases/10-upstream-reconcile/10-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Verified live via an actual throwaway dry-run merge + git-show against the merge-base
     (e38f90a) during planning — these are the REAL conflict-marker contents, not a two-way
     diff approximation. Do not re-derive; use this directly. -->

Re-run the divergence check first (upstream may have moved since planning):
`git fetch upstream && git log --oneline main..upstream/main` — expect exactly 5 commits
ending at `d4497a2` (390a502 #68, 974cc38 #69, b717ad4 #70, 9409131 version bump, d4497a2
warn-harness). If the head SHA differs, STOP and escalate per D-02 (do not widen scope
mid-phase) rather than merging further commits.

`src/agda/refactor-helpers.ts` — the ONLY conflict is the JSDoc comment immediately above
`buildAutoSearchPayload`. The function body itself (both the `"mimer"` short-circuit branch
AND the `"agsy"` branch's calls to `assertValidAutoHint`) auto-merges cleanly OUTSIDE the
conflict markers — git already unioned upstream's new `engine` parameter/mimer branch with
our `assertValidAutoHint` validation calls in the agsy branch. Resolve the conflict by
keeping ONE coherent doc comment (upstream's is more complete — describes both engines;
folding in our T-06-12 fingerprint reference is a nice-to-have, not required). Do NOT touch
anything outside the two `<<<<<<</=======/>>>>>>>` marker lines — the merged function body is
already correct.

`test/unit/agda/agent-ux.test.ts` — the ONLY conflict is inside the `buildAutoSearchPayload`
describe block: our four T-06-12/RT4 tests (HEAD side, ending in "allows a hyphenated
identifier that does not lead with '-'") vs upstream's one new mimer-mode test. Resolution:
keep ALL of ours, then upstream's new test, in sequence (textual union, D-05: never remove a
from-RED test). KNOWN BREAKAGE: the kept-verbatim test `"allows a hyphenated identifier that
does not lead with '-'"` calls `buildAutoSearchPayload({ hints: ["Nat-helper"] })` with NO
`engine` argument and asserts `payload === "-h Nat-helper"` — but the merged function now
defaults `engine` to `"mimer"`, whose branch returns bare `"Nat-helper"` (no `-h` prefix) for
a no-engine-arg call. This test will fail post-merge unless fixed. Fix it by adding an
explicit `"agsy"` second argument to that one call (it is specifically testing the Agsy flag
syntax, so pin it explicitly rather than relying on the now-changed default).

`src/tools/goal-tools.ts` — the conflict is upstream's ENTIRE monolithic write-tool block
(`agda_case_split`, `agda_give`, `agda_refine`, `agda_refine_exact`, `agda_intro`, `agda_auto`,
~330 lines) presented against EMPTY on our side, because we already extracted all of this
into the sibling `src/tools/goal-write-tools.ts` (RESEARCH.md Pattern 2 — a false architectural
disagreement, not a real one). Resolve BOTH conflict hunks (the import-list hunk near the top
AND the giant registration-block hunk) by taking the EMPTY/HEAD side — i.e. discard upstream's
insertion entirely, keeping our file exactly as it reads pre-merge (ends with
`registerGoalWriteTools(server, session, repoRoot);`). Do not introduce upstream's now-unused
imports (`applyEditAndReload`, `hasReplacementText`, `buildAutoSearchPayload`,
`usesMimerProofSearch`) into this file — they belong in goal-write-tools.ts instead (next).

`src/tools/goal-write-tools.ts` (NOT a conflict file — plain manual edit) — its existing
`agda_auto` registration currently calls `buildAutoSearchPayload({ depth, listCandidates,
excludeHints, hints })` with no second argument, so it silently gets whatever the merged
function's default `engine` is. Upstream's OWN discarded goal-tools.ts conflict block (the one
you just threw away above) already contains the exact wiring to port verbatim: import
`usesMimerProofSearch` from `../agda/version-support.js` (this function already ships via the
merge — `version-support.ts` is NOT a conflict file and auto-merges upstream's addition
cleanly; confirmed present at the same line/signature as `hasStructuredGiveResult`:
`usesMimerProofSearch(agdaVersion: AgdaVersion | null): boolean`), then in the `agda_auto`
callback compute `const engine = usesMimerProofSearch(session.getAgdaVersion()) ? "mimer" :
"agsy";` and pass it as `buildAutoSearchPayload({...}, engine)`. Also port the
`droppedOnMimer` UX note upstream added (a one-line addition to the output text when
`depth`/`listCandidates`/`excludeHints` get silently ignored because Mimer only accepts bare
hints) — cheap, directly relevant, and improves ADOPT-adjacent discoverability of why those
options sometimes have no effect.

`src/session/agda-transport.ts`, `src/session/command-completion.ts`,
`src/agda/session-load-impl.ts` — these three are the genuine MERGE-03 architectural
conflicts (D-04: empirical, referee-decided, NOT this plan's call). Resolve ALL of them by
taking upstream's whole-file content (`git checkout --theirs -- <path>` for each of these
three specific paths) rather than hunk-by-hunk — verified live that git's raw 3-way merge
ALREADY silently auto-applied several of upstream's non-conflicting-hunk changes throughout
these files (e.g. `shouldResolveOnIdle`/`idleCompletionDelay` in command-completion.ts already
read as upstream's simplified versions even outside marked conflict regions), so a
hunk-by-hunk resolution risks an incoherent hybrid. Taking the whole file gives Plan 10-02 a
clean, internally-consistent upstream candidate to run the referee against.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Merge + resolve mechanical/structural conflicts</name>
  <files>src/agda/refactor-helpers.ts, test/unit/agda/agent-ux.test.ts, src/tools/goal-tools.ts, src/tools/goal-write-tools.ts</files>
  <read_first>
    src/agda/refactor-helpers.ts
    test/unit/agda/agent-ux.test.ts
    src/tools/goal-tools.ts
    src/tools/goal-write-tools.ts
    src/agda/version-support.ts
    .planning/phases/10-upstream-reconcile/10-CONTEXT.md
  </read_first>
  <action>
Re-verify divergence per the interfaces block (`git fetch upstream && git log --oneline main..upstream/main` must show exactly the 5 commits ending at `d4497a2`; escalate per D-02 if not). Start the merge: `git merge --no-ff upstream/main` (do NOT commit yet — leave the merge in progress across this whole plan; commit happens at the end of Task 3). This produces exactly 6 conflicted files (verified during planning) plus clean auto-merges elsewhere (`package.json` to 0.6.8, `vitest.config.ts` gaining `setupFiles: ["test/helpers/warn-guard.ts"]` while KEEPING our `ciQuarantine`/`exclude`/`passWithNoTests` block, `test/fixtures/e2e/mcp-tool-coverage.json` gaining an `agda_goal_candidates` entry, several net-new files like `src/session/goal-terminus.ts`, `src/tools/register-goal-candidates.ts`, `test/helpers/warn-guard.ts`, `src/session/command-wait-diagnostics.ts`). Do not hand-edit any auto-merged file per the RESEARCH.md anti-pattern warning.

Resolve `src/agda/refactor-helpers.ts` per the interfaces block: only the JSDoc comment above `buildAutoSearchPayload` conflicts. Keep one coherent doc comment describing both `"mimer"`/`"agsy"` engines (upstream's wording is more complete); the function body outside the markers is already correctly merged and must not be touched.

Resolve `test/unit/agda/agent-ux.test.ts` per the interfaces block: keep every one of our existing T-06-12/RT4 tests verbatim, append upstream's new mimer-mode test after them in the same describe block. Then fix the one now-stale assertion: the test `"allows a hyphenated identifier that does not lead with '-'"` must call `buildAutoSearchPayload({ hints: ["Nat-helper"] }, "agsy")` (explicit second argument) so it continues to exercise the Agsy flag-prefix format its assertion (`toBe("-h Nat-helper")`) checks — without the explicit argument the new default (`"mimer"`) would silently change this test's meaning and it would fail.

Resolve `src/tools/goal-tools.ts` per the interfaces block: discard upstream's entire inserted write-tool block AND its accompanying import-list hunk (take the empty/HEAD side for both marked regions) — our file already delegates this functionality to `registerGoalWriteTools(server, session, repoRoot)` at its end; do not introduce unused imports.

Edit `src/tools/goal-write-tools.ts` (not itself conflicted): add `import { usesMimerProofSearch } from "../agda/version-support.js";` alongside its existing `buildAutoSearchPayload` import from `../agda/agent-ux.js`. In the `agda_auto` callback, compute `engine` via `usesMimerProofSearch(session.getAgdaVersion())` (true → `"mimer"`, false → `"agsy"`) and pass it as the second argument to `buildAutoSearchPayload`. Port upstream's `droppedOnMimer` output-text note (the one-line warning shown when `depth`/`listCandidates`/`excludeHints` were supplied but silently ignored under the mimer engine) into this callback's output-building logic, following the same conditional shape upstream used in its now-discarded `goal-tools.ts` block.
  </action>
  <verify>
    <automated>npx vitest run test/unit/agda/agent-ux.test.ts test/unit/tools/goal-tools-give.test.ts test/unit/tools/goal-tools-write-rejected.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<<<<<<<\|=======\|>>>>>>>" src/agda/refactor-helpers.ts test/unit/agda/agent-ux.test.ts src/tools/goal-tools.ts` returns 0 for all three files
    - `grep -n "assertValidAutoHint" src/agda/refactor-helpers.ts` still shows the function definition AND at least 2 call sites (agsy branch)
    - `grep -n "usesMimerProofSearch" src/tools/goal-write-tools.ts` shows the import and one call site inside the `agda_auto` callback
    - `grep -n '"Nat-helper"\], }, "agsy")\|"Nat-helper"\] }, "agsy")' test/unit/agda/agent-ux.test.ts` confirms the explicit engine argument was added (adjust grep to actual formatting; the point is the call now passes `"agsy"` explicitly)
    - `npx vitest run test/unit/agda/agent-ux.test.ts` passes with zero failures
  </acceptance_criteria>
  <done>refactor-helpers.ts, agent-ux.test.ts, goal-tools.ts have zero conflict markers; goal-write-tools.ts's agda_auto dispatches engine via usesMimerProofSearch; the T-06-12/RT4 guard is untouched and still enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve architectural conflicts to upstream's candidate + apply forced corrections</name>
  <files>src/session/agda-transport.ts, src/session/command-completion.ts, src/agda/session-load-impl.ts, src/session/load-terminus-tracker.ts, test/unit/session/load-terminus-tracker.test.ts</files>
  <read_first>
    src/session/agda-transport.ts
    src/session/command-completion.ts
    src/agda/session-load-impl.ts
    src/session/load-terminus-tracker.ts
    src/agda/session-load-helpers.ts
    CLAUDE.md
  </read_first>
  <action>
For each of the three architectural conflict files (`src/session/agda-transport.ts`, `src/session/command-completion.ts`, `src/agda/session-load-impl.ts`), take upstream's whole-file content: `git checkout --theirs -- <path>`. Then `git rm src/session/load-terminus-tracker.ts test/unit/session/load-terminus-tracker.test.ts` — upstream's `goal-terminus.ts` (already carried in net-new by the merge, no conflict) replaces it as this plan's initial candidate; D-04's "locks stay green" verdict is Plan 10-02's job, not this task's.

Apply one MANDATORY correction regardless of the eventual MERGE-03 verdict: upstream's `session-load-impl.ts` replaces our `!parsed.sawLoadTerminus` truncation-handling in BOTH `runLoad` and `runLoadNoMetas` with a bare `throw new Error(...)`. This violates this repo's own documented CLAUDE.md Error Handling convention, which names `loadIncompleteNoTerminus()` BY NAME as the canonical example of "return a plain data object with a classification field, don't throw, for an expected domain-level failure." Restore the return-based shape in both functions: replace each `throw new Error(...)` in the `!parsed.sawLoadTerminus` guards with `return finalizeEarlyReturn(session, loadIncompleteNoTerminus(absPath, parsed.warnings, parsed.profiling));` (mirroring the pre-merge pattern exactly; `finalizeEarlyReturn` and `loadIncompleteNoTerminus` are both still imported/exported from `session-load-helpers.ts` — confirm `loadIncompleteNoTerminus` itself did NOT get silently deleted by the auto-merge of `session-load-helpers.ts`; if it is missing, re-add it verbatim from `git show HEAD^1:src/agda/session-load-helpers.ts` since that file auto-merges upstream's own deletion of this function when no conflict is flagged).

Apply a second correction: our pre-merge `runLoad` has a "Recovery: source has a hole but we captured no goal IDs" block (a second `reconcileGoalsViaMetas` re-query triggered when `sourceHoleCount > 0`) that upstream's `runLoad` does not have at all — confirm this by diffing against `git show HEAD^1:src/agda/session-load-impl.ts`. Port this recovery block into the now-adopted `runLoad`, adapting it to upstream's variable names/control flow (the surrounding `goals`/`goalIds` local variables and the `reconcileGoalsViaMetas` call signature are unchanged). Without this port, a real regression silently reappears: the file's own pre-merge unit test `"runLoad recovers dropped visible goal IDs via a metas re-query when source has holes"` (in `test/unit/agda/session-load-impl.test.ts`) was clobbered by the auto-merge — Plan 10-02 will need to re-add or rewrite it once you confirm this recovery mechanic is back in place (do not attempt that test-file rewrite in this task; Plan 10-02 owns test/unit/agda/session-load-impl.test.ts).
  </action>
  <verify>
    <automated>grep -rn "loadTerminusMode\|LoadTerminusOptions\|LoadTerminusState" src/session/agda-transport.ts src/session/command-completion.ts src/agda/session-load-impl.ts | grep -v '^.*:.*//'  | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/session/load-terminus-tracker.ts` returns "No such file or directory"
    - `grep -rn "load-terminus-tracker" src/ test/ --include=*.ts | grep -v load-terminus-tracker.test.ts` returns nothing referencing the deleted module by import path (session.ts/session-command-dispatch.ts should already be free of it via their own clean auto-merge to the narrower inline options type)
    - `grep -n "throw new Error" src/agda/session-load-impl.ts` shows zero hits inside the `!parsed.sawLoadTerminus` guards (both occurrences replaced with `loadIncompleteNoTerminus`-based returns)
    - `grep -n "loadIncompleteNoTerminus" src/agda/session-load-impl.ts src/agda/session-load-helpers.ts` shows the function both defined (session-load-helpers.ts) and called twice (session-load-impl.ts, once per runLoad/runLoadNoMetas)
    - `grep -n "sourceHoleCount > 0" src/agda/session-load-impl.ts` shows the ported recovery block present inside `runLoad`
  </acceptance_criteria>
  <done>All three architectural files carry upstream's candidate implementation with the two CLAUDE.md/regression-preserving corrections applied; load-terminus-tracker.ts and its unit test are deleted; the merge is still uncommitted, ready for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the merge commit + verify build/typecheck green</name>
  <files>(commit only — no further source edits)</files>
  <read_first>
    package.json
  </read_first>
  <action>
Run `git diff --check` and `grep -rln "<<<<<<<\|=======\|>>>>>>>" src/ test/` to confirm zero leftover conflict markers anywhere in the tree (not just the 6 named files — a stray marker elsewhere would indicate a missed hunk). Stage everything (`git add -A`) and complete the merge commit with a message of the form `merge: upstream/main (5 commits, through d4497a2)` per the upstream-sync skill's own message convention (Section 4, step 3), noting in the body that this is Phase 10's attended reconcile and that the 3 architectural files carry a provisional candidate pending Plan 10-02's referee-driven adjudication. Run `export PATH="/Users/eric/.local/share/mise/installs/node/24/bin:$PATH"` first (Node 24 is required; the ambient shell may resolve to v22). Run `npm ci` (sanity-check the lockfile only picked up the version bump, no unexpected dependency diff), then `npm run build`, then `npm run typecheck:test`. Both must exit 0 — this is MERGE-01's literal acceptance gate; a full green test suite is explicitly NOT required yet (MERGE-02/ADOPT-01 land in later plans and some tests — e.g. `test/unit/tools/mcp-e2e-coverage.test.ts` — are expected red until `agda_goal_candidates` is wired in Plan 10-03).
  </action>
  <verify>
    <automated>npm run build && npm run typecheck:test</automated>
  </verify>
  <acceptance_criteria>
    - `git status` shows a clean working tree (merge committed, nothing staged/unstaged)
    - `git log -1 --format=%P` shows two parent hashes (confirms a real merge commit, not a rebase or squash)
    - `git log -1 --format=%s` matches `merge: upstream/main*`
    - `npm run build` exits 0
    - `npm run typecheck:test` exits 0
    - `git diff --stat HEAD^1 HEAD -- package-lock.json` shows no unexpected dependency changes beyond the version bump
  </acceptance_criteria>
  <done>A single real merge commit exists on local main bringing in upstream d4497a2; build and typecheck:test both exit 0; nothing has been pushed (D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| upstream repo -> local main | Merging 5 externally-authored commits into the auto-deploying `main` branch |
| conflict resolution -> load/verdict pipeline | Manual/candidate resolution of the false-green-critical seam (agda-transport.ts, session-load-impl.ts, command-completion.ts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | git merge of upstream/main | accept | Upstream (InvariantHoldings/agda-mcp-server) is a trusted co-maintainer relationship per the settled one-way-sync decision, not an adversarial input source (RESEARCH.md Security Domain) — not re-litigated here; commit range/SHA is pinned and re-verified before merging (D-02) |
| T-10-02 | Tampering | src/agda/session-load-impl.ts, src/session/agda-transport.ts, src/session/command-completion.ts | mitigate | Do not resolve by taste — adopt upstream's whole-file candidate coherently in this plan, defer the actual green/red verdict to Plan 10-02's referee-test run against the real capture-regression-matrix; never hand-pick individual hunks that could produce an inconsistent hybrid |
| T-10-03 | Repudiation | merge commit | mitigate | Commit message documents the exact upstream range merged and flags the architectural files as provisional pending Plan 10-02 |
| T-10-04 | Tampering | package-lock.json | mitigate | `npm ci` + explicit lockfile diff check as a merge-sanity step (Task 3) — confirms no unexpected dependency changed hands during the merge |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new package-manager installs in this phase per RESEARCH.md's Package Legitimacy Audit; the only "install" step is `npm ci` re-resolving the existing lockfile, verified unchanged beyond the version bump |

</threat_model>

<verification>
Full local gate (run once Task 3 completes): `export PATH="/Users/eric/.local/share/mise/installs/node/24/bin:$PATH" && npm ci && npm run build && npm run typecheck:test`. Do NOT run the full `RUN_AGDA_INTEGRATION=1 npx vitest run` suite yet — that is Plan 10-04's job after MERGE-03/ADOPT-01 land; some tests are expected red until then (documented above).
</verification>

<success_criteria>
- Real merge commit exists locally with two parents, bringing in upstream d4497a2
- Zero conflict markers anywhere in the tree
- `npm run build` and `npm run typecheck:test` both exit 0
- load-terminus-tracker.ts deleted; its consumers (agda-transport.ts, command-completion.ts, session-load-impl.ts) carry upstream's candidate architecture
- The T-06-12/RT4 flag-hint guard and the goal-ID recovery re-query mechanic are both provably still present after the merge
- Nothing pushed to origin (D-10)
</success_criteria>

<output>
Create `.planning/phases/10-upstream-reconcile/10-01-SUMMARY.md` when done
</output>
</content>
