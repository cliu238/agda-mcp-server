---
phase: 10-upstream-reconcile
plan: 05
type: execute
wave: 4
depends_on: ["10-04"]
files_modified: []
autonomous: true
requirements: [ACCEPT-01, ACCEPT-02, ACCEPT-03]

must_haves:
  truths:
    - "npm run build, npx tsc -p tsconfig.test.json --noEmit, and RUN_AGDA_INTEGRATION=1 npx vitest run all exit 0 in one final combined run"
    - "A real dogfood session against the merged server completed and invoked agda_goal_candidates at least once (D-09)"
    - "D-10: exactly ONE push to origin main happened at phase end (intermediate commits stayed local), triggering deploy-ingest.yml; a red deploy is fixed forward, never auto-reverted"
    - "GitHub Actions reports the triggered deploy-ingest.yml run as successful"
    - "curl https://dev.sites.idies.jhu.edu/agda-mcp/healthz returns 200/ok after the deploy"
  artifacts:
    - path: ".agda-mcp/runs/<run-id>/run-report.json"
      provides: "the dogfood session's finalized run report, produced by dogfood-run.mjs + dogfood-wrapup.mjs"
  key_links:
    - from: "git push origin main"
      to: ".github/workflows/deploy-ingest.yml"
      via: "push-triggered GitHub Actions workflow"
      pattern: "deploy-ingest"
---

<objective>
Run the phase's full acceptance gate: (1) a final combined verify (build + typecheck:test + the real-Agda full suite) confirming Plans 10-01 through 10-04 collectively hold; (2) one real dogfood session against the merged server, on the pinned CHG corpus, driven by Codex headless, that genuinely invokes `agda_goal_candidates` at least once (D-09); (3) exactly one push to `origin main` (D-10), watched through its triggered deploy to a green `/healthz`.

Purpose: ACCEPT-01/02/03 require proving the reconcile made the server stronger, not just different — the merge quality AND the newly adopted feature are proven in the same real use, not just by tests and docs, and the final push's auto-deploy is watched to green before the phase is considered done.
Output: Full verify green; a completed, wrapped-up dogfood run report; a green deploy with `/healthz` returning `ok`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-upstream-reconcile/10-CONTEXT.md
@.planning/phases/10-upstream-reconcile/10-RESEARCH.md
@.agents/skills/agda-dogfooding/SKILL.md
@.agents/skills/upstream-sync/SKILL.md
</context>

<interfaces>
ACCEPT-02 mechanics (per `.agents/skills/agda-dogfooding/SKILL.md`, read in full): the dogfood
proxy (`scripts/dogfood/dogfood-run.mjs`) is a hard gate — it refuses to start without a task
manifest (`scripts/dogfood/task-manifest.mjs`'s `loadTaskManifest`, schema in
`test/fixtures/task-manifest-schema.ts`: a non-empty JSON array of `{ target, expectedSignature,
corpus, notes? }`). The pinned corpus is `codex-homotopy-group` (local clone confirmed on disk
at `~/projects6/Codex-Homotopy-Group`, pinned ref `81a2f26f19845decccd7ad840d3d8ab5840762c3` in
`scripts/data/fuel-corpora.json`) per D-09. Launch shape (Claude Code):
`claude mcp add --transport stdio agda-dogfood --env AGDA_MCP_CAPTURE=1 -- npx tsx
/absolute/path/to/agda-mcp-server/scripts/dogfood/dogfood-run.mjs --manifest
<path-to-task-manifest.json> --corpus-root ~/projects6/Codex-Homotopy-Group` — must launch from
THIS repo's local checkout with `npm run build` already run (the proxy spawns the compiled
`dist/index.js`), and `--corpus-root` must be a different path from this repo (the CHG clone).
After the session, the proxy prints a `<run-id>`; run
`npx tsx scripts/dogfood/dogfood-wrapup.mjs <run-id>` from this repo's checkout to judge staged
captures through the oracle triad and finalize the run report.

D-09's hard requirement: the session must GENUINELY invoke `agda_goal_candidates` at least
once — this is not optional polish, it is the acceptance criterion proving the adopted
feature works in real use, not just in unit tests. Confirm this from the run's transcript/run-
report (search for the tool name in the recorded tool-call log) before considering ACCEPT-02
satisfied; if the session completes without ever calling it, the session does not count and
must be extended or re-run with an explicit instruction to call it.

ACCEPT-03 mechanics (per `.agents/skills/upstream-sync/SKILL.md` Section 4, steps 6-8, and
D-10): exactly ONE push to `origin main` at the end of this phase — all local commits from
Plans 10-01 through 10-04 stay squashed-in-spirit as local history until this single push
(D-10: intermediate commits stay local). After pushing, get the new run id with `gh run list
--workflow=deploy-ingest.yml --branch main --limit 1`, then `gh run watch <id> --exit-status`,
then `curl -fsS https://dev.sites.idies.jhu.edu/agda-mcp/healthz` — retry the curl a few times
over several minutes since rollout takes time. If the deploy goes red or healthz never returns
200: per D-10 this is ATTENDED work — fix forward rather than auto-revert (the skill's `git
revert -m 1` rule is for the UNATTENDED routine, not this attended phase).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ACCEPT-01 — final combined verify</name>
  <files>(no source edits)</files>
  <read_first>
    package.json
  </read_first>
  <action>
Run the full local gate one final time as the phase-level confirmation that Plans 10-01
through 10-04 collectively hold: export the Node 24 mise PATH, then `npm ci && npm run build
&& npx tsc -p tsconfig.test.json --noEmit && RUN_AGDA_INTEGRATION=1 npx vitest run`. This
should already be green from Plan 10-04's work — if anything is red here, that is a genuine
regression introduced between plans (e.g. an interaction Plan 10-04's per-file triage missed)
and must be fixed before proceeding to Task 2; do not proceed to the dogfood session or push
with a red gate.

Nyquist/latency note: this full real-Agda suite run (several minutes) intentionally exceeds
the 30s feedback-latency target — it IS the phase acceptance gate (ACCEPT-01); no per-file
filter constitutes acceptance. Accepted and deliberate.
  </action>
  <verify>
    <automated>export PATH="/Users/eric/.local/share/mise/installs/node/24/bin:$PATH" && npm run build && npx tsc -p tsconfig.test.json --noEmit && RUN_AGDA_INTEGRATION=1 npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npx tsc -p tsconfig.test.json --noEmit` exits 0
    - `RUN_AGDA_INTEGRATION=1 npx vitest run` exits 0 with zero failures
  </acceptance_criteria>
  <done>The full real-Agda combined suite, typecheck, and build are all green as a single final confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: ACCEPT-02 — real dogfood session invoking agda_goal_candidates</name>
  <files>(no repo source edits — produces a run report under .agda-mcp/runs/, outside src/)</files>
  <read_first>
    .agents/skills/agda-dogfooding/SKILL.md
    scripts/data/fuel-corpora.json
  </read_first>
  <action>
Confirm `npm run build` output is current (re-run if Task 1's build predates any later change
— it should not, since Task 1 ran last). Inspect the local CHG clone
(`~/projects6/Codex-Homotopy-Group`) to identify a real, currently-open proof goal with a
verifiable expected top-level signature (read the corpus's own source directly — do not
invent a signature; per the skill, if none is confidently known, this is a stop-and-escalate
condition, not a guessing one). Author a task manifest JSON file (array of one or more
`{ target, expectedSignature, corpus: "codex-homotopy-group", notes? }` entries, matching
`test/fixtures/task-manifest-schema.ts`) at a path outside this repo's tracked tree (e.g. the
scratchpad). Launch the dogfood proxy per the interfaces block, then drive a live Codex
headless session (`codex exec`, per standing practice — do not pause for a checkpoint) against
the target, making at least one genuine call to `agda_goal_candidates` during the session as
part of real proof work (not a no-op smoke call). When the session ends, run
`npx tsx scripts/dogfood/dogfood-wrapup.mjs <run-id>` from this repo's checkout. Confirm the
run report shows `finalized: true` and that the recorded tool-call log includes at least one
`agda_goal_candidates` invocation.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const path=process.argv[1];const r=JSON.parse(fs.readFileSync(path,'utf8'));if(!r.finalized){console.error('not finalized');process.exit(1)};process.exit(0)" .agda-mcp/runs/RUN_ID/run-report.json</automated>
  </verify>
  <acceptance_criteria>
    - A task manifest with a real, non-invented expectedSignature was used (not skipped or bypassed)
    - The dogfood session completed and `dogfood-wrapup.mjs` ran to completion against its run id
    - The run report's recorded tool-call log includes at least one `agda_goal_candidates` call (grep the run's transcript/report for the tool name)
    - `run-report.json`'s `finalized` field is `true`
  </acceptance_criteria>
  <done>A real proof session ran against the merged server via the agda-dogfooding skill's mechanics, genuinely invoked agda_goal_candidates at least once, and was wrapped up through the oracle triad.</done>
</task>

<task type="auto">
  <name>Task 3: ACCEPT-03 — single push, watch deploy to green</name>
  <files>(no repo source edits — git push + external verification only)</files>
  <read_first>
    docs/DEPLOY-OPERATIONS.md
  </read_first>
  <action>
Confirm Tasks 1 and 2 are both complete and green before this irreversible step (per D-10,
exactly ONE push ends the phase). Push local main to origin: `git push origin main`. Get the
newly triggered run id: `gh run list --workflow=deploy-ingest.yml --branch main --limit 1`.
Watch it: `gh run watch <id> --exit-status`. Once the workflow succeeds, poll
`curl -fsS https://dev.sites.idies.jhu.edu/agda-mcp/healthz` a few times over the following
minutes (rollout takes time) until it returns HTTP 200 with a healthy body. If the workflow
fails OR healthz never returns 200 within a reasonable window: this is attended work per D-10
— diagnose and fix forward (do not `git revert -m 1`, that rule is for the unattended 3-day
sync routine, not this attended phase), then push the fix and re-watch.
  </action>
  <verify>
    <automated>curl -fsS https://dev.sites.idies.jhu.edu/agda-mcp/healthz</automated>
  </verify>
  <acceptance_criteria>
    - `git log origin/main -1` matches local `main`'s HEAD (the push landed)
    - `gh run watch <id> --exit-status` reports the workflow succeeded
    - `curl -fsS https://dev.sites.idies.jhu.edu/agda-mcp/healthz` returns HTTP 200 with an `ok`-style body
  </acceptance_criteria>
  <done>Exactly one push to origin main triggered deploy-ingest.yml, the workflow succeeded, and the cluster's /healthz endpoint confirms the deployed pod is healthy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| local main -> origin main -> auto-deploy | A single push triggers an unattended CI/CD pipeline that rolls out to a live cluster |
| Codex headless session -> private CHG corpus | Driving a live coding agent against a maintainer-owned private repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Tampering / Availability | git push origin main | mitigate | Exactly one push, only after ALL prior gates (Task 1) are green (D-10) — never push with a red gate |
| T-10-14 | Availability | deploy-ingest.yml / cluster healthz | mitigate | `gh run watch` + polled `curl` confirm success before considering the phase done; red outcome is fixed forward (attended), never silently ignored |
| T-10-15 | Information Disclosure | dogfood session against private CHG corpus | accept | CHG is a maintainer-owned private repository already used for the proven E2E-01 path (Phase 7); no new access surface, existing `gh` auth already has write access (per PROJECT.md) |
| T-10-SC | Tampering | npm/pip/cargo installs, deploy credentials | accept | No new package installs in this plan; deploy credentials (`agda-mcp-ghcr` pull secret, `K8S_SSH_PRIVATE_KEY`) are pre-existing from Phase 8, unchanged and unrotated by this plan |

</threat_model>

<verification>
Task 1's full local gate, Task 2's finalized run report showing an `agda_goal_candidates` invocation, and Task 3's green `gh run watch` + 200 `/healthz` together constitute phase-level acceptance.
</verification>

<success_criteria>
- Full real-Agda verify green (build, typecheck:test, full suite)
- One real dogfood session completed, wrapped up, and provably invoked agda_goal_candidates
- Exactly one push to origin main; its triggered deploy succeeded; /healthz returns ok
</success_criteria>

<output>
Create `.planning/phases/10-upstream-reconcile/10-05-SUMMARY.md` when done
</output>
</content>
