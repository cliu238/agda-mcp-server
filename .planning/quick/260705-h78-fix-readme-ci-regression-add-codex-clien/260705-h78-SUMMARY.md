---
phase: quick-260705-h78
plan: 01
subsystem: docs
tags: [readme, ci-fix, codex, mcp-client-config, onboarding]

# Dependency graph
requires:
  - phase: quick-260705-gn0
    provides: "Earlier today's end-user rewrite of README.md that dropped the examples/extensions/README.md link, breaking CI"
provides:
  - "CI-passing root README.md (test/examples/extensions-catalog.test.ts green again on main)"
  - "Copy-pasteable `codex mcp add` MCP client config alongside the Claude Code example"
  - "README Install section split into a minimal end-user path vs. a team/dogfooding path"
  - "docs/TEAM-ONBOARDING.md led by a TL;DR fast path, rationale-essay verbosity cut, live k8s endpoint status"
affects: [readme, docs, ci, onboarding]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified: [README.md, docs/TEAM-ONBOARDING.md]

key-decisions:
  - "Reproduced the plan's target-content blocks verbatim rather than re-deriving content from the facts bullets, per plan instruction that the fenced blocks are the literal deliverable."
  - "Kept the two files as two separate atomic commits (README first) so the CI-fixing change is independently cherry-pickable from the TEAM-ONBOARDING simplification."

patterns-established: []

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-07-05
---

# Phase quick-260705-h78 Plan 01: Fix README CI Regression + Codex Client Config + TEAM-ONBOARDING Simplification Summary

**Fixed the red CI build by restoring the `examples/extensions/README.md` link in README.md, added a copy-pasteable `codex mcp add` MCP client config, split README's Install section into a minimal end-user path vs. team path, and rewrote docs/TEAM-ONBOARDING.md with a TL;DR fast path and a live k8s endpoint status.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-07-05T16:33:48Z
- **Completed:** 2026-07-05T16:35:38Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- Unbroke CI on `main`: `test/examples/extensions-catalog.test.ts` now passes because README.md's Developing section links both `docs/extensions.md` and `examples/extensions/README.md`.
- README's Requirements section now states the server's real Agda compatibility range (`2.6.4.3`–`2.9.0`, from `package.json`'s `agdaMcpServer` contract) instead of the dogfooding loop's pinned `2.8.0`.
- README's Install section split into a minimal end-user path (clone, checkout latest tag, `npm ci`, `npm run build` — no private-repo access needed) plus a one-sentence pointer to the team installer and `docs/TEAM-ONBOARDING.md`.
- README's "Connect an MCP client" section gained a copy-pasteable `codex mcp add` block alongside the existing Claude Code JSON example.
- `docs/TEAM-ONBOARDING.md` now leads with a TL;DR block containing the entire zero-to-uploading happy path, cuts D-01 through D-05 rationale-essay prose (keeping the bare `(D-0N)` tags), replaces the stale "live once shipped" hedge with a direct live-endpoint statement plus the `/healthz` URL, and preserves Step 1 through Step 5 labeling unchanged (required by `scripts/team/install-pinned-env.mjs`'s literal "Step 2"/"Step 4" print statements).

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix README.md CI regression, add Codex config, split user/team install** - `5a6eedd` (fix)
2. **Task 2: Simplify docs/TEAM-ONBOARDING.md with a TL;DR fast path** - `c17ac3a` (docs)

**Plan metadata:** committed separately by the orchestrator (not included in this executor's commits).

## Files Created/Modified
- `README.md` - CI-passing (links `examples/extensions/README.md`), states real Agda compat range `2.6.4.3`–`2.9.0`, splits Install into end-user vs. team paths, adds `codex mcp add` client config. 115 lines (ceiling 130).
- `docs/TEAM-ONBOARDING.md` - Leads with a `## TL;DR` fast path, rationale-essay prose cut, live k8s endpoint statement with `/healthz` check URL, Step 1–5 labeling preserved unchanged. 92 lines (ceiling 110).

## Decisions Made
- Reproduced both target-content blocks verbatim from the plan rather than re-deriving wording, per explicit plan instruction that those fenced blocks are the literal deliverable, not an illustration of intent.
- Preserved README.md's and docs/TEAM-ONBOARDING.md's separation into two atomic commits (README first) so the CI fix is independently cherry-pickable from the onboarding-doc simplification.

## Deviations from Plan

None - plan executed exactly as written. Both target-content blocks were applied as exact overwrites via the Write tool, and every verification gate specified in the plan passed on the first attempt.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. This is a documentation-only change; no code path, dependency, or runtime behavior was touched.

## Next Phase Readiness
- CI on `main` is unblocked: `test/examples/extensions-catalog.test.ts` passes.
- Both README.md and docs/TEAM-ONBOARDING.md are within their line-count ceilings and satisfy all plan-specified grep gates.
- No blockers or concerns for follow-on work.

---
*Phase: quick-260705-h78*
*Completed: 2026-07-05*

## Self-Check: PASSED

- FOUND: README.md
- FOUND: docs/TEAM-ONBOARDING.md
- FOUND: .planning/quick/260705-h78-fix-readme-ci-regression-add-codex-clien/260705-h78-SUMMARY.md
- FOUND commit: 5a6eedd
- FOUND commit: c17ac3a
