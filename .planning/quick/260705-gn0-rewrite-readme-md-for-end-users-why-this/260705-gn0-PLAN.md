---
phase: quick-260705-gn0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "An end user reading the top of README.md understands, in 2-4 sentences, why cliu238/agda-mcp-server exists as a separate fork instead of just using the upstream MCP server (InvariantHoldings/agda-mcp-server) directly."
    - "An end user can go from a clean checkout to a running MCP server using only the README's install/run instructions, with the full multi-step onboarding walkthrough linked to docs/TEAM-ONBOARDING.md rather than duplicated."
    - "A developer can find the exact local dev commands (install/build/test/verify, plus the real-Agda integration test invocation) directly in README.md, with deeper architecture/convention docs linked (ARCHITECTURE.md, AGENTS.md) rather than restated."
    - "README.md contains zero factually wrong claims about this fork: no npm-publish instructions, no `npm install -g`, no badges/links pointing at the old upstream repos (LionOfJewdah / InvariantHoldings as if they were this repo), no stale architecture file-tree listing nonexistent modules."
    - "README.md is concise — well under 150 lines — and every remaining line is content an end user or a contributor actually needs, per the user's locked scope decision."
  artifacts:
    - path: "README.md"
      provides: "end-user-facing rewrite: why this fork exists, how to use it as a consumer, how to start developing it"
      min_lines: 40
  key_links:
    - from: "README.md 'why this fork' section"
      to: "upstream repository"
      via: "link naming the upstream project this fork tracks"
      pattern: "InvariantHoldings/agda-mcp-server"
    - from: "README.md install instructions"
      to: "docs/TEAM-ONBOARDING.md"
      via: "markdown link to the canonical onboarding walkthrough"
      pattern: "TEAM-ONBOARDING\\.md"
    - from: "README.md install instructions"
      to: "scripts/team/install-pinned-env.sh"
      via: "exact installer command referenced"
      pattern: "install-pinned-env\\.sh"
    - from: "README.md developing section"
      to: "ARCHITECTURE.md"
      via: "markdown link to the architecture doc instead of restating it"
      pattern: "ARCHITECTURE\\.md"
---

<objective>
Replace the current byte-identical-to-upstream README.md (615 lines, targeted at a general OSS audience, with npm-publish instructions and badges that are factually wrong for this fork) with a concise, end-user-focused README for `cliu238/agda-mcp-server`.

The new README answers exactly three questions, in this order: why does this fork exist instead of just using upstream; how do I use it as a consumer of the MCP server; how do I start developing it. Anything an end user does not need — exhaustive tool tables, CI/community/maintenance boilerplate, a hand-maintained architecture file tree, npm publishing instructions — is deleted, not shrunk. Where deeper detail already exists in a canonical doc (docs/TEAM-ONBOARDING.md, ARCHITECTURE.md, AGENTS.md, docs/extensions.md), the new README links to it instead of duplicating it.

Purpose: the current README actively misleads readers about this fork (wrong badges, wrong distribution model, describes source files that no longer match reality) and buries the two things a new user/dev actually needs under ~500 lines of content that belongs to upstream's OSS-marketing README, not this internal fork.

Output: rewritten README.md (target ~70-110 lines, hard ceiling well under 150).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@README.md
@docs/TEAM-ONBOARDING.md
@.agents/skills/upstream-sync/SKILL.md
@.planning/PROJECT.md

<facts>
<!-- Verified during planning — use these directly, do not re-derive or guess. -->

- Fork identity: origin = `https://github.com/cliu238/agda-mcp-server.git`; upstream = `https://github.com/InvariantHoldings/agda-mcp-server.git` (push disabled — sync is strictly one-way, upstream → fork). Fork is ~488 commits ahead / 5 behind upstream at authoring time.
- Why the fork exists: this repo runs its own closed self-improvement loop on top of the upstream server — every real proof session is captured, judged for defects, filed to a fix queue, fixed, and locked with a regression test, then reused. That loop, plus a live team feedback channel (an ingest endpoint on a JHU cluster) and a pinned-environment distribution model, require controlling release cadence and infrastructure that upstream does not provide — so active development happens here, and fixes land in this fork first.
- Distribution is git-only (locked decision D-01 in docs/TEAM-ONBOARDING.md): no npm publishing, no `npm install -g`, no npm account anywhere. The current README's "Publishing" section and its `npm install`-style framing are wrong for this fork and must be deleted, not merely reworded.
- Canonical install (from docs/TEAM-ONBOARDING.md, summarize in the README, do not re-derive or duplicate the full walkthrough):
  `git clone https://github.com/cliu238/agda-mcp-server.git`, `cd agda-mcp-server`, `git fetch --tags`, `git checkout "$(git tag --list 'v*' | sort -V | tail -1)"` (always the highest `v*` tag by version sort — never hardcode a version string), then `bash scripts/team/install-pinned-env.sh` (checks Node >= 24 and git, verifies a local Agda 2.8.0, clones the 4 fuel corpora, runs `npm ci`).
- Run it: `npm run build` then `AGDA_MCP_ROOT=/path/to/agda/project node dist/index.js` (`AGDA_MCP_ROOT` defaults to the current working directory if omitted).
- MCP client config: stdio server, command `node`, args pointing at the built `dist/index.js`, env `AGDA_MCP_ROOT` set to the target Agda project root. Claude Code is the primary example client to show; note any stdio-capable MCP client works the same way.
- Tool surface (mention briefly, do NOT reproduce the ~50-tool reference table): load/typecheck, goal inspection (`agda_goal_type`, `agda_metas`, `agda_goal_catalog`), proof actions (`agda_case_split`, `agda_give`, `agda_refine`, `agda_auto`), expression queries (`agda_compute`, `agda_infer`), navigation/search, session capture for bug reports. Point readers who want the full current list at the `agda_tools_catalog` MCP tool itself (it is manifest-derived and always in sync with the running server — a living source of truth, not a doc that goes stale).
- Dev workflow: `npm install`, `npm run build`, `npm test`, `npm run verify`. Integration tests need a real Agda binary: `RUN_AGDA_INTEGRATION=1 npm run test:integration`. Point to `ARCHITECTURE.md` (layering/module map) and `AGENTS.md` (repo conventions for coding agents) for depth — `CLAUDE.md` mirrors the same constraints for Claude Code specifically, do not restate all three. Constraints worth a one-line callout: Node >= 24, TypeScript strict/ESM, a hard 500-line-per-file ceiling in `src/`, tests via `vitest`.
- Extension authors: point to `docs/extensions.md` (already exists) instead of duplicating extension setup instructions.
- License: MIT, `LICENSE` file already exists at repo root — keep a one-line License section linking to it.
- README.md is not on the upstream-sync guarded-files list (`.agents/skills/upstream-sync/SKILL.md` Section 3) — a future upstream merge conflict on README is acceptable and resolved by keeping this fork's version. No need to hedge the rewrite for mergeability.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for end users</name>
  <files>README.md</files>
  <action>
Overwrite README.md in full using the Write tool (do not patch the existing 615-line file piecemeal — replace it wholesale) with exactly this section outline, in this order, and nothing else:

1. `# agda-mcp-server` title, followed by a 2-4 sentence intro paragraph: what the server is (a stateful MCP server that keeps a long-lived `agda --interaction-json` process alive so an AI agent can load a file, inspect goals, and iterate on a proof without restarting Agda per request), linking "Model Context Protocol" to https://modelcontextprotocol.io and "Agda" to https://agda.readthedocs.io/ inline. No badges anywhere in the file — the old npm-version and CI badges point at the wrong repos (`LionOfJewdah`/upstream org) and this fork does not publish to npm, so there is nothing correct left to badge; do not invent replacement badges either.

2. `## Why this fork exists` — a short paragraph (aim for 3-5 sentences) built from the `<facts>` "Why the fork exists" and "Fork identity" bullets above: this fork tracks `InvariantHoldings/agda-mcp-server` (link it) but runs its own closed self-improvement loop on top of it (use it, capture defects, judge, fix, lock with a regression test, reuse) plus a live team feedback channel and a pinned-environment distribution model that need control over release cadence and infrastructure upstream doesn't provide — so active development, and where fixes land first, is here. State the sync direction is one-way (upstream to fork only). Close with one sentence: readers who just want the plain upstream server without this workflow should use the upstream repo directly.

3. `## Using it` with these subsections, in order:
   - `### Requirements` — a short bullet list: Node.js >= 24, git, Agda 2.8.0 on PATH (exact version, matching the pinned fuel corpora).
   - `### Install` — one sentence stating this is a git install, not an npm package (no `npm install -g`, no npm account anywhere, per this fork's locked distribution decision), then a fenced bash code block containing, one command per line: `git clone https://github.com/cliu238/agda-mcp-server.git`, `cd agda-mcp-server`, `git fetch --tags`, `git checkout "$(git tag --list 'v*' | sort -V | tail -1)"`, `bash scripts/team/install-pinned-env.sh`. Follow with one sentence noting the installer verifies your Agda version and runs `npm ci`, and a link to docs/TEAM-ONBOARDING.md for the full walkthrough (multi-teammate setup, fuel corpora, upload keys) — do not restate that walkthrough's content here.
   - `### Run it` — a fenced bash code block with `npm run build` then `AGDA_MCP_ROOT=/path/to/your/agda/project node dist/index.js`, followed by one sentence noting `AGDA_MCP_ROOT` defaults to the current directory if omitted.
   - `### Connect an MCP client` — one sentence introducing the example, then a fenced JSON code block showing a Claude Code `mcpServers` entry named `"agda"` with `"command": "node"`, `"args"` pointing at the absolute path to the built `dist/index.js`, and `"env"` setting `AGDA_MCP_ROOT` to the target project root. Follow with one sentence noting any stdio-capable MCP client works the same way (command `node`, args = path to `dist/index.js`, env = `AGDA_MCP_ROOT`).
   - `### What it can do` — one short paragraph (2-4 sentences) naming the tool categories from the `<facts>` "Tool surface" bullet (load/typecheck, goal inspection, proof actions, expression queries, navigation/search, session capture) with a couple of representative tool names in backticks, then a closing sentence pointing readers who want the full current tool list at calling the `agda_tools_catalog` MCP tool itself rather than a hand-maintained table, since it is manifest-derived and cannot go stale.

4. `## Developing` — a fenced bash code block with, one per line: `npm install`, `npm run build`, `npm test`, `npm run verify`. Follow with one sentence: integration tests need a real Agda binary, run with `RUN_AGDA_INTEGRATION=1 npm run test:integration`. Follow with one sentence pointing to `ARCHITECTURE.md` for the layering/module map and `AGENTS.md` for repo conventions (note `CLAUDE.md` mirrors the same constraints for Claude Code specifically) — do not restate their content. Close with one sentence listing the hard constraints worth knowing up front: Node >= 24, TypeScript strict/ESM, a 500-line-per-file ceiling in `src/`, tests via `vitest`. Add one final sentence pointing extension authors at `docs/extensions.md`.

5. `## License` — one line: MIT, linking to the `LICENSE` file.

Explicit DELETE list — none of this content from the current README.md may appear in the new version, in any form (not shrunk, not moved into a link's surrounding prose as a summary — fully removed, with only a link substituted where a canonical doc already covers it): the npm-version and CI badges; the entire "Publishing" section; the "Continuous integration" section; the "Community and maintenance files" section; the full "Tool reference" section and all of its per-category markdown tables (Session management, Display and highlighting, Backend commands, Reporting and agent introspection, Goal inspection and proof interaction, Navigation and environment inspection); the "Protocol coverage" section; the hand-maintained "Architecture overview" file-tree code block; the "Protocol notes" section; the numbered "Examples" walkthroughs (load-a-file, refine-a-hole, check-an-expression, stateless-validation); the "Typical interactive workflow" numbered walkthrough; the "Stateless vs stateful operations" section; the "Troubleshooting" section; the "Session model" / "Write-back for proof actions" deep-dive section; the "Semantic outputs" section; the "Extension modules" section body (replaced by the single closing sentence in Developing above).

Keep the file in English throughout. Do not create any new files — this task touches README.md only.
  </action>
  <verify>
    <automated>cd /Users/eric/projects6/agda-mcp-server && LINES=$(wc -l < README.md) && [ "$LINES" -le 130 ] && echo "line count OK ($LINES)" && grep -q "^## Why this fork exists" README.md && grep -q "^## Using it" README.md && grep -q "^## Developing" README.md && grep -q "InvariantHoldings/agda-mcp-server" README.md && grep -q "TEAM-ONBOARDING.md" README.md && grep -q "install-pinned-env.sh" README.md && grep -q "ARCHITECTURE.md" README.md && grep -q "agda_tools_catalog" README.md && ! grep -qi "npm install -g" README.md && ! grep -qi "npm publish" README.md && ! grep -q "LionOfJewdah" README.md && ! grep -q "^## Publishing" README.md && ! grep -q "^## Tool reference" README.md && ! grep -q "^## Continuous integration" README.md && ! grep -q "^## Community and maintenance files" README.md && ! grep -q "^## Architecture overview" README.md && ! grep -q "img.shields.io" README.md && echo "content gates OK"</automated>
  </verify>
  <done>
    README.md is rewritten in full per the five-section outline above (title/intro, Why this fork exists, Using it with its five subsections, Developing, License), contains none of the DELETE-list content, is at or under 130 lines, links to docs/TEAM-ONBOARDING.md / ARCHITECTURE.md / AGENTS.md / docs/extensions.md / LICENSE instead of duplicating their content, and both automated verify gates above pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only change. No code path, no new dependency, no runtime behavior is touched — README.md is not imported or executed by the server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-gn0-01 | Tampering | README.md install instructions | accept | Instructions reference only existing, already-reviewed repo scripts (`scripts/team/install-pinned-env.sh`) and public git/npm-ecosystem commands already documented in docs/TEAM-ONBOARDING.md; no new script or command is introduced by this task. |
| T-gn0-02 | Information Disclosure | README.md content | accept | No secrets, tokens, or internal-only infrastructure details (upload keys, cluster credentials) are added — the team-upload-key flow stays in docs/TEAM-ONBOARDING.md, linked but not reproduced. |
| T-gn0-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — this task only edits README.md prose; no `package.json` or lockfile change. |
</threat_model>

<verification>
After the task completes:

1. `git diff --stat -- README.md` shows only README.md changed, no other files touched.
2. `wc -l README.md` reports a line count well under 150 (gate enforces <= 130).
3. Manually skim the rendered README top-to-bottom: the "Why this fork exists" section reads coherently to someone unfamiliar with the project's internal history, the install/run steps are copy-pasteable as written, and no dangling reference to a deleted section remains (e.g. no leftover "see Tool reference above" pointing at removed content).
</verification>

<success_criteria>
- README.md is rewritten for end users: why this fork exists, how to use it, how to develop it — nothing else.
- No factually wrong content survives (npm publish, npm install -g, wrong-repo badges, stale architecture file tree).
- Deep-dive content is linked (docs/TEAM-ONBOARDING.md, ARCHITECTURE.md, AGENTS.md, docs/extensions.md) rather than duplicated.
- File is well under 150 lines.
</success_criteria>

<output>
Create `.planning/quick/260705-gn0-rewrite-readme-md-for-end-users-why-this/260705-gn0-SUMMARY.md` when done.
</output>
