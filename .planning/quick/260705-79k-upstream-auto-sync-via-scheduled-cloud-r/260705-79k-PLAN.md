---
phase: quick-260705-79k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/skills/upstream-sync/SKILL.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "A tracked bounded-autonomy merge-policy skill exists at .agents/skills/upstream-sync/SKILL.md (80-250 lines, English) encoding: merge-NEVER-rebase, silent exit when upstream has no new commits, the 9 guarded load/capture-seam paths (any conflict there = no push, escalate), env-adaptive verification gates (local real-Agda full suite vs cloud ci.yml-derived quarantine-aware lane), push-then-watch (deploy-ingest.yml run + healthz curl), `git revert -m 1` rollback on red deploy, and escalation via `upstream-sync` branch + `gh` issue — never leaving main broken."
    - "Claude Code discovers the skill locally: .claude/skills/upstream-sync is a symlink resolving to the tracked .agents/skills content (gitignored — .claude/ is wholesale-excluded — mirroring the agda-dogfooding install pattern), so there is no second copy to drift."
    - "A one-shot cloud trial routine was created BY THE ORCHESTRATOR (not the executor) and produced a consolidated findings report covering toolchain capability (node/npm/gh/agda versions + OS, npm ci under engine-strict Node>=24, build, test-typecheck, quarantine-aware vitest) and push capability (probe branch push+delete, repo .permissions, branch-protection 404) — without triggering any deploy or CI run."
    - "The production every-3-days routine exists if and only if the trial verdict was green; its prompt delegates entirely to the tracked skill in auto mode, and the skill commit is pushed to origin/main before the routine is armed."
    - "Nothing under .github/workflows/, k8s/, src/, or existing scripts/ is modified by this plan."
  artifacts:
    - path: ".agents/skills/upstream-sync/SKILL.md"
      provides: "bounded-autonomy upstream merge policy — the production routine's entire instruction source"
      min_lines: 80
      contains: "git revert -m 1"
    - path: ".claude/skills/upstream-sync"
      provides: "local Claude Code discovery symlink (gitignored, absolute target, never committed)"
  key_links:
    - from: "SKILL.md Guarded files section"
      to: "false-green-critical seams"
      via: "explicit path list including the session-capture glob"
      pattern: "src/agda/session-capture"
    - from: "SKILL.md cloud verification gate"
      to: ".github/workflows/ci.yml"
      via: "lane commands/env copied at authoring time, incl. the quarantine exclude list"
      pattern: "AGDA_MCP_CI_QUARANTINE"
    - from: "production routine prompt (Task 3)"
      to: "tracked skill file"
      via: "repo-path reference the cloud agent reads at run time"
      pattern: "\\.agents/skills/upstream-sync/SKILL\\.md"
    - from: "SKILL.md deploy watch + rollback"
      to: "live JHU service"
      via: "healthz curl gating the revert decision"
      pattern: "dev\\.sites\\.idies\\.jhu\\.edu/agda-mcp/healthz"
---

<objective>
Establish unattended upstream auto-sync for this fork via a Claude Code scheduled cloud routine, in three steps: (1) author the tracked bounded-autonomy merge-policy skill at `.agents/skills/upstream-sync/SKILL.md` plus a local gitignored discovery symlink; (2) orchestrator runs a one-shot cloud trial routine probing the cloud environment's toolchain and push-to-main capability without touching main; (3) if the trial is green, orchestrator arms the production every-3-days routine whose prompt simply delegates to the tracked skill.

Repo facts this plan encodes (all locked): origin = cliu238/agda-mcp-server, and main auto-deploys to a live JHU cluster on EVERY push (D-06, ~30 min cycle, accepted). upstream = InvariantHoldings/agda-mcp-server, push deliberately disabled. Current divergence 488 ahead / 5 behind (confirmed live during planning); the 5 upstream commits textually conflict in 6 files, 4+ of which are on the guarded list — so the FIRST real sync will escalate by design; automation pays from sync #2.

Path correction discovered during planning: the symlink-installer precedent is `scripts/dogfood/install-dogfood-skill.mjs` (NOT `scripts/team/...` as earlier notes said). Its pattern is mirrored manually — no new installer script is written.

Purpose: upstream drift stops being a manual chore; mechanical merges land themselves with verification + rollback protection, and anything requiring judgment escalates to a branch + issue instead of touching auto-deploying main.

Output: tracked SKILL.md (committed), local symlink (gitignored), one-shot trial routine findings, and — gated on trial green — a live every-3-days production routine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.agents/skills/agda-dogfooding/SKILL.md
@scripts/dogfood/install-dogfood-skill.mjs
@.github/workflows/ci.yml

<interfaces>
<!-- Facts already extracted from the repo during planning — verified 2026-07-05.
     Use these directly; re-read only ci.yml (Task 1 requires copying its
     then-current lane env verbatim into the skill). -->

**Skill placement (Pitfall 1):**
- `.gitignore:51-52` wholesale-excludes `.claude/` and `.codex/`; `.agents/` is tracked. Canonical skill content therefore lives in `.agents/skills/`, and Claude Code (which only discovers `.claude/skills/`) gets a LOCAL symlink.
- Existing precedent link (created by `scripts/dogfood/install-dogfood-skill.mjs`): `.claude/skills/agda-dogfooding -> /Users/eric/projects6/agda-mcp-server/.agents/skills/agda-dogfooding` (absolute target, `dir` symlink; a pre-existing REAL directory at the link path is never clobbered).
- House skill style (`.agents/skills/agda-dogfooding/SKILL.md`): YAML frontmatter with `name:` and a one-sentence `description:` in "Use when <situation> ... to <outcome>" form; then `# <Title> runbook` and numbered `## N. Section` headings.

**Git remotes / divergence (confirmed live during planning):**
- `origin  https://github.com/cliu238/agda-mcp-server.git` (fetch+push)
- `upstream https://github.com/InvariantHoldings/agda-mcp-server.git` (fetch); push URL literally `DISABLED-push-to-cliu238-origin-instead`
- `git rev-list --count upstream/main..main` = 488; `main..upstream/main` = 5
- `git diff --name-only main...upstream/main` includes src/agda/session-load-impl.ts, src/agda/session-load-helpers.ts, src/agda/parse-load-responses.ts, src/session/agda-transport.ts, src/session/command-completion.ts, src/session/register-agda-load-no-metas.ts — i.e. the guarded list overlaps the real divergence; first sync escalates by design.

**CI facts (`.github/workflows/ci.yml`, read during planning):**
- `verify` job (no Agda): `npm ci` → `npm run typecheck:test` → `npm audit --audit-level=high` → `npm run verify`
- `integration` job (real Agda 2.7.0.1 via wenkokke/setup-agda): `npm ci` → `npm run test:all` with env `RUN_AGDA_INTEGRATION: "1"` and `AGDA_MCP_CI_QUARANTINE: "test/integration/mcp/dogfood-flake-classify-live.test.ts,test/integration/mcp/capture-regression.test.ts,test/unit/agda/session-capture/manifest-builder.test.ts,test/unit/tools/oracle-orcl-01.test.ts,test/unit/tools/oracle-orcl-03.test.ts"` (5 quarantined pre-existing Linux-lane failures, fix-queue fb57abbe7df6dfe8)
- npm scripts (`package.json`): `build` = `tsc -p tsconfig.json && node scripts/copy-json-assets.mjs`; `typecheck:test` = `tsc -p tsconfig.test.json --noEmit`; `test` = `vitest run`; `test:all` = `node scripts/test-all-continuing.mjs`; `verify` = `npm test && npm pack --dry-run`
- `vitest.config.ts:8-15` consumes `AGDA_MCP_CI_QUARANTINE` directly as a comma-separated `exclude` list — so a plain `AGDA_MCP_CI_QUARANTINE="<list>" npx vitest run` honors the quarantine without `test:all`. Its comment also documents that Agda-gated integration tests register `test.skip(...)` when `RUN_AGDA_INTEGRATION` is unset (they self-skip; no Agda needed).
- `.npmrc`: `engine-strict=true`; `package.json` engines: node `>=24` — on a cloud host with node < 24, `npm ci` FAILS BY DESIGN (that failure is itself a probe finding).

**Deploy facts:**
- Workflow: `.github/workflows/deploy-ingest.yml`, name "Deploy agda-mcp ingest to k8s-dev", triggers on EVERY push to main + workflow_dispatch (D-06 locked: no change-detection gate — do not "fix"). NO `concurrency:` group exists in it.
- Watch recipe: `gh run list --workflow=deploy-ingest.yml --branch main --limit 1` (get run id) then `gh run watch <id> --exit-status`.
- Live health endpoint: `curl -fsS https://dev.sites.idies.jhu.edu/agda-mcp/healthz` (expect HTTP 200).
- Local pinned Node for gates: `export PATH="/Users/eric/.local/share/mise/installs/node/24/bin:$PATH"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .agents/skills/upstream-sync/SKILL.md (bounded-autonomy merge policy) + idempotent local discovery symlink</name>
  <files>.agents/skills/upstream-sync/SKILL.md, .claude/skills/upstream-sync (gitignored symlink — never committed)</files>
  <action>
Write `.agents/skills/upstream-sync/SKILL.md` (new file, English, 80-250 lines, house style per `.agents/skills/agda-dogfooding/SKILL.md`). Before writing, read `.github/workflows/ci.yml` and copy its then-current lane commands/env — especially the exact `AGDA_MCP_CI_QUARANTINE` list — verbatim into the cloud gate section (ci.yml is the SSOT; the list in this plan's interfaces block is the planning-time snapshot).

Frontmatter: `name: upstream-sync`; `description:` one sentence in house form — "Use when syncing this fork (cliu238/agda-mcp-server) with upstream InvariantHoldings/agda-mcp-server, to merge, verify, push, and watch-or-roll-back within a bounded-autonomy policy — escalating instead of guessing whenever judgment is required." Then `# Upstream sync runbook` and numbered sections exactly as follows.

Section 1 — Overview: why this exists. origin main auto-deploys to a live JHU cluster on EVERY push (D-06, ~30 min cycle, accepted); upstream push is deliberately disabled (push URL `DISABLED-push-to-cliu238-origin-instead`); divergence at authoring time is 488 ahead / 5 behind and the 5 upstream commits conflict in 6 files, 4+ of them guarded — so the FIRST real sync is EXPECTED to escalate; that is the policy working, not a bug. Automation pays from sync #2. Bounded autonomy in one line: mechanical merges proceed unattended; judgment work always escalates.

Section 2 — Policy: allowed — fetch upstream; `git merge upstream/main` (MERGE, NEVER rebase: 488 published commits, tags like v1.1, and an auto-deploying main make history rewriting destructive); resolve conflicts only OUTSIDE guarded files; run the env-appropriate verification gate; push main only when everything is green; watch the deploy and roll back on red. Forbidden — rebase, force-push, resolving guarded-file conflicts, pushing with any gate red, leaving main in a broken or mid-merge state.

Section 3 — Guarded files: any merge conflict touching ANY of these = NO push to main, escalate (Section 6). List exactly: `src/agda/session-load-impl.ts`, `src/agda/session-load-helpers.ts`, `src/agda/parse-load-responses.ts`, `src/session/agda-transport.ts`, `src/session/command-completion.ts`, `src/session/load-terminus-tracker.ts`, `src/session/register-agda-load-no-metas.ts`, `src/protocol/command-builder.ts`, `src/agda/session-capture/**`. One line of why: these are the false-green-critical seams — the load/verdict/capture pipeline this server's whole credibility rests on.

Section 4 — Flow (the algorithm, numbered): (1) `git fetch upstream`; (2) `git rev-list --count main..upstream/main` — if 0, EXIT SILENTLY (no log entry, no commit); (3) confirm clean worktree on main, then `git merge --no-ff upstream/main` with message form `merge: upstream/main (<N> commits, through <short-sha>)`; (4) on conflicts, `git diff --name-only --diff-filter=U` — if ANY conflicted path matches Section 3, go to Section 6 (escalate); otherwise resolve non-guarded conflicts and complete the merge commit; (5) run the Section 5 gate for the current environment; (6) all green → `git push origin main` (this triggers the deploy); (7) watch the deploy: `gh run list --workflow=deploy-ingest.yml --branch main --limit 1` for the new run id, then `gh run watch <id> --exit-status`, then `curl -fsS https://dev.sites.idies.jhu.edu/agda-mcp/healthz` expecting HTTP 200 (retry the curl a few times over several minutes — rollout takes time); (8) workflow failure OR healthz not returning 200 = deploy red → ROLLBACK: `git revert -m 1 <merge-commit-sha>`, push the revert, re-watch workflow + healthz until green, then escalate-notify per Section 6 (issue only; main is already safe); (9) append the bookkeeping entry (Section 7).

Section 5 — Verification gates per environment. Detect: `command -v agda` succeeds → LOCAL; otherwise CLOUD. LOCAL gate: `export PATH="/Users/eric/.local/share/mise/installs/node/24/bin:$PATH"`, then `npm ci`, `npm run build`, `npx tsc -p tsconfig.test.json --noEmit`, then `RUN_AGDA_INTEGRATION=1 npx vitest run` (full suite against real Agda; note inline: `RUN_AGDA_INTEGRATION=1` is what makes the Agda-gated integration tests actually run instead of self-skipping — vitest.config.ts contract — fulfilling the locked "full, real Agda" intent; quarantine env stays UNSET locally, macOS is not the quarantined lane). CLOUD gate: `npm ci` (engine-strict requires Node >= 24 — an engine failure means the environment cannot run this repo at all: escalate), `npm run build`, `npx tsc -p tsconfig.test.json --noEmit`, then `npx vitest run` with `AGDA_MCP_CI_QUARANTINE` set to the exact comma-separated list copied from ci.yml's integration job and `RUN_AGDA_INTEGRATION` UNSET (Agda-gated integration tests self-skip; the quarantine excludes the 5 documented pre-existing Linux-lane failures so only NEW regressions turn the gate red). Fix-iteration bound: at most 2 fix attempts on a red gate; still red after 2 → escalate.

Section 6 — Escalation. Triggers: (a) any guarded-file conflict; (b) any from-RED regression lock fails and the fix is not mechanically obvious; (c) upstream adds new tools/features under `src/tools` (needs manifest/tool-recommendation/docs integration — judgment work); (d) >2 fix iterations with the gate still red; (e) engine/toolchain failure in cloud. Procedure: preserve work on a branch, never main — if mid-merge on main, `git switch -c upstream-sync` (carries the in-progress merge state; main's ref never moves), complete a best-effort merge commit there (guarded-file resolutions are PROVISIONAL and must be flagged as unreviewed), `git push -u origin upstream-sync`; if even that is untenable, `git merge --abort` and push nothing. Then `gh issue create` on cliu238/agda-mcp-server with a digest: upstream commit range + count, conflicted files (guarded ones flagged), gate results so far, branch name if pushed, and exact next-human-step notes. Hard rule stated verbatim: NEVER leave main broken — main is either untouched, green-merged, or reverted-back-to-green.

Section 7 — Bookkeeping (env-adaptive): cloud has NO gsd-sdk — use plain `git` commits only. Every non-silent run appends one entry to `docs/UPSTREAM-SYNC-LOG.md` (create the file on first append), committed WITH the merge (or with the revert, for rollbacks): date, environment (local/cloud), upstream range merged (`<old>..<new>`, N commits), conflicted files if any, gate result, deploy run id + healthz outcome, final outcome (merged / rolled-back / escalated with issue #). Local/manual runs MAY wrap the whole sync in `/gsd-quick` instead; cloud runs must not attempt it.

Section 8 — Cadence notes: the production routine runs every 3 days. Do NOT tighten the cadence below daily without first adding a `concurrency:` group to `.github/workflows/deploy-ingest.yml` — it has none today, and two rapid syncs racing deploys is a known Info-level gap. Each sync push costs one accepted ~30 min deploy cycle (D-06).

Then create the local discovery symlink, mirroring `scripts/dogfood/install-dogfood-skill.mjs`'s end state manually (do NOT write a new installer script): from the repo root run `mkdir -p .claude/skills && ln -sfn "$(pwd)/.agents/skills/upstream-sync" .claude/skills/upstream-sync`. Absolute target matches the existing agda-dogfooding link; re-running is idempotent; if a REAL (non-symlink) directory already occupies the path, `ln` fails loudly — do not force-remove it, surface the situation instead.

Commit ONLY the tracked skill file (the symlink lives under wholesale-gitignored `.claude/` and cannot be committed): `git add .agents/skills/upstream-sync/SKILL.md` and commit with message `feat(quick-79k): upstream-sync bounded-autonomy merge-policy skill`. Do NOT push — the push to origin/main is deliberately deferred to Task 3's arming step (it triggers a D-06 deploy; batch it with arming the routine).
  </action>
  <verify>
    <automated>test -f .agents/skills/upstream-sync/SKILL.md && L=$(wc -l < .agents/skills/upstream-sync/SKILL.md) && [ "$L" -ge 80 ] && [ "$L" -le 250 ] && for tok in "src/agda/session-load-impl.ts" "src/agda/session-load-helpers.ts" "src/agda/parse-load-responses.ts" "src/session/agda-transport.ts" "src/session/command-completion.ts" "src/session/load-terminus-tracker.ts" "src/session/register-agda-load-no-metas.ts" "src/protocol/command-builder.ts" "src/agda/session-capture" "git revert -m 1" "AGDA_MCP_CI_QUARANTINE" "RUN_AGDA_INTEGRATION" "dev.sites.idies.jhu.edu/agda-mcp/healthz" "deploy-ingest.yml" "docs/UPSTREAM-SYNC-LOG.md" "upstream-sync"; do grep -qF "$tok" .agents/skills/upstream-sync/SKILL.md || { echo "MISSING TOKEN: $tok"; exit 1; }; done && grep -q "^name: upstream-sync" .agents/skills/upstream-sync/SKILL.md && test -L .claude/skills/upstream-sync && test -f .claude/skills/upstream-sync/SKILL.md && git log -1 --name-only --pretty=format: | grep -qF ".agents/skills/upstream-sync/SKILL.md" && echo SKILL-OK</automated>
  </verify>
  <done>
    `.agents/skills/upstream-sync/SKILL.md` exists (80-250 lines) with house frontmatter and all eight sections; every guarded path, the revert recipe, both gate environments (incl. the quarantine list copied from ci.yml), the healthz URL, deploy workflow, log path, and escalation branch name appear verbatim; `.claude/skills/upstream-sync` is a symlink resolving to the tracked content; the skill file is committed locally (not pushed); nothing under `.github/workflows/`, `k8s/`, `src/`, or existing `scripts/` changed.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: ORCHESTRATOR creates + runs the one-shot cloud trial probe routine</name>
  <files>(none — schedule-system state only; the probe itself pushes/deletes a throwaway `probe/schedule-env` branch on origin)</files>
  <action>
This task CANNOT be performed by the executor: creating/managing scheduled routines requires the orchestrator session's schedule skill + Cron tools. Executor: stop here and return control. Orchestrator: perform the following, then close the checkpoint with the evidence below.

1. Using the schedule skill / Cron tools, create a ONE-SHOT (run once, as soon as possible) cloud routine targeting repo cliu238/agda-mcp-server, branch main, with EXACTLY the prompt in the block below (it is self-contained by design — the cloud agent gets no other context; note the trial does NOT need Task 1's skill and does not read it).

2. Wait for the run to complete and fetch its output/report.

The routine prompt (copy verbatim):

```
You are probing this repository's scheduled-cloud environment for a future unattended upstream-sync routine. Do NOT push to main or modify it. Run EVERY step even when earlier steps fail; record each step's outcome (success/failure + key output), then finish with one consolidated findings report.

1. Environment: report `uname -a`, `node --version`, `npm --version`, `gh --version`, `agda --version`. An absent agda binary is an expected, valid finding — not an error.
2. Install: run `npm ci`. This repo pins engines.node >=24 with engine-strict=true, so if npm ci fails on the engine check, that failure IS the finding — report the exact error and continue.
3. Build: run `npm run build`.
4. Test typecheck: run `npx tsc -p tsconfig.test.json --noEmit`.
5. Tests, replicating .github/workflows/ci.yml: read that file, then run `npx vitest run` with the env var AGDA_MCP_CI_QUARANTINE set to the exact comma-separated list found in ci.yml's integration job env, and WITHOUT setting RUN_AGDA_INTEGRATION (Agda-gated integration tests must self-skip). Report pass/fail/skip counts and every failing file name.
6. Push probe (deploy-safe: deploys and CI trigger only on main pushes / PRs): `git switch -c probe/schedule-env`, `git commit --allow-empty -m "probe: scheduled-cloud push capability"`, `git push -u origin probe/schedule-env`, then `git push origin --delete probe/schedule-env`. Report whether the push and the delete each succeeded.
7. Permissions: run `gh api repos/cliu238/agda-mcp-server --jq .permissions` and `gh api repos/cliu238/agda-mcp-server/branches/main/protection`. For the second call an HTTP 404 is the EXPECTED good result (no branch protection = direct push to main possible). Report both raw outputs.
8. Final report: a findings table with one row per step 1-7 and its outcome, followed by exactly two verdict lines:
   PUSH_TO_MAIN: possible|blocked (possible = step 6 push succeeded AND step 7 shows push:true with a 404 on branch protection)
   TOOLCHAIN: green|red (<first failing step>) (green = steps 2-5 all succeeded)
```
  </action>
  <verify>
    <automated>git ls-remote --heads origin probe/schedule-env | wc -l | grep -qx '0' && echo "probe branch absent on origin (pre-state or cleaned up) — real closure evidence is the findings report"</automated>
  </verify>
  <done>
    The one-shot routine was created and ran; its consolidated findings report is in hand and contains: all versions + OS from step 1; npm ci / build / tsc / vitest outcomes (with the quarantine list actually applied); push-probe push+delete outcomes; the .permissions JSON and the branch-protection 404 (or its absence); and the two verdict lines `PUSH_TO_MAIN:` and `TOOLCHAIN:`. No deploy and no CI run were triggered by the probe.
  </done>
  <resume-signal>Orchestrator pastes the findings report (at minimum the per-step table + both verdict lines) and states either "trial green" (both verdicts good) or "trial red: <step/verdict>".</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: ORCHESTRATOR arms the production every-3-days sync routine (gated on trial green)</name>
  <files>(none — schedule-system state; plus one `git push origin main` publishing Task 1's already-made commit)</files>
  <action>
GATE: proceed ONLY if Task 2 reported `TOOLCHAIN: green` AND `PUSH_TO_MAIN: possible`. If the trial was red: do NOT create the routine; record the blocking findings in the SUMMARY and finish the quick task there — the Task 1 skill remains fully valid for local/manual syncs, and this checkpoint can be re-run after the environment gaps are fixed.

This task CANNOT be performed by the executor (orchestrator-only schedule skill + Cron tools). Orchestrator steps:

1. Publish the skill: `git push origin main` (Task 1 committed but did not push). The cloud routine reads the skill from the repo at run time, so it MUST be on origin/main before the routine ever fires. This push triggers exactly one accepted D-06 deploy cycle (~30 min) — expected, no action needed beyond noting it.

2. Using the schedule skill / Cron tools, create a RECURRING cloud routine: cadence every 3 days (do NOT schedule tighter — the skill's Section 8 cadence note: deploy-ingest.yml has no concurrency group), repo cliu238/agda-mcp-server, branch main, prompt EXACTLY: "Read .agents/skills/upstream-sync/SKILL.md from this repository and execute it in auto mode."

3. Confirm creation: list the scheduled routines and capture the new routine's id, cadence, and prompt text.

Expectation-setting for the first firing (from the skill's Overview): with the current 488/5 divergence and guarded-file conflicts, the FIRST production run is EXPECTED to end in an escalation (an `upstream-sync` branch + a `gh` issue, main untouched). That is the policy working.
  </action>
  <verify>
    <automated>gh api repos/cliu238/agda-mcp-server/contents/.agents/skills/upstream-sync/SKILL.md --jq .path 2>/dev/null | grep -qx '.agents/skills/upstream-sync/SKILL.md' && echo "skill visible on origin default branch"</automated>
  </verify>
  <done>
    Either: (a) trial was green, the skill commit is on origin/main (gh contents check passes), and a recurring every-3-days routine exists whose prompt is exactly the delegation sentence — evidenced by the schedule listing showing id + cadence + prompt; or (b) trial was red, no routine was created, and the SUMMARY records the exact blocking findings + the re-arm path.
  </done>
  <resume-signal>Orchestrator confirms with the routine id + schedule-listing evidence (and the push having landed), or states "trial red — production routine deferred" with the recorded findings.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream/main commits → fork main | third-party-authored code crosses into a branch that auto-deploys to a live JHU cluster on every push |
| scheduled cloud agent → push-capable repo credential | a recurring unattended routine holds direct push-to-main power |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79k-01 | Tampering | merged upstream code auto-deploying to the live cluster | mitigate | guarded-file no-touch list + env-adaptive verification gate before any push + deploy-watch (workflow + healthz) + `git revert -m 1` rollback; all judgment work escalates to branch + issue, never lands on main |
| T-79k-02 | Elevation of Privilege | recurring routine's push-to-main capability | mitigate | the routine's ONLY instruction source is the tracked, repo-reviewed SKILL.md; the one-shot trial proves minimal capability before any recurring routine is armed; policy forbids rebase/force-push; escalation path writes only to the `upstream-sync` branch |
| T-79k-03 | Denial of Service | deploy churn / two syncs racing deploys (no concurrency group in deploy-ingest.yml) | accept | every-3-days cadence plus the skill's explicit "no tighter than daily without adding a concurrency group" note; D-06 deploy-per-push already an accepted cost |
| T-79k-04 | Repudiation | unattended merges with no audit trail | mitigate | mandatory `docs/UPSTREAM-SYNC-LOG.md` entry committed with every merge/revert (range, conflicts, gate results, deploy run id, healthz outcome) |
| T-79k-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NOTHING new; all gates run `npm ci` strictly from the committed lockfile (upstream lockfile changes ride the merge commit and are exercised by the gate before any push) |
</threat_model>

<verification>
After all tasks complete:

1. `.agents/skills/upstream-sync/SKILL.md` passes Task 1's token gate (all 9 guarded paths, revert recipe, both gates, healthz URL, log path, escalation branch) and is 80-250 lines.
2. `.claude/skills/upstream-sync` is a symlink whose `SKILL.md` is readable; `git status --porcelain -- .claude/` shows nothing staged (gitignored).
3. `git status --porcelain -- .github/workflows/ k8s/ src/ scripts/` returns empty — no collateral edits.
4. Task 2's findings report exists with the per-step table and both verdict lines; no deploy-ingest.yml run and no CI run correspond to the probe (its branch push triggers neither).
5. Task 3: either the routine listing shows the every-3-days routine with the exact delegation prompt AND the gh contents check finds the skill on origin/main, or the SUMMARY documents the red-trial findings and the deferred state.
</verification>

<success_criteria>
- The bounded-autonomy merge policy is a single tracked artifact that both Codex (via .agents/skills/) and Claude Code (via the symlink) discover, and that the cloud routine executes verbatim — one SSOT, no drift.
- Cloud capability is proven by measurement (trial findings), not assumed: toolchain viability under engine-strict Node>=24 and push-to-main capability are each individually evidenced before any recurring automation is armed.
- The production routine, if armed, contains zero policy content of its own — updating sync behavior later means editing SKILL.md on main, never touching the schedule.
- main can never be left broken by this automation: silent-exit, green-merge, revert-to-green, or escalate-to-branch+issue are the only terminal states the skill permits.
</success_criteria>

<output>
Create `.planning/quick/260705-79k-upstream-auto-sync-via-scheduled-cloud-r/260705-79k-SUMMARY.md` when done.
</output>
