---
phase: quick-260705-79k
plan: 01
subsystem: automation
tags: [upstream-sync, skill, bounded-autonomy, scheduled-cloud, merge-policy]
requires: []
provides:
  - "Tracked bounded-autonomy upstream merge-policy skill at .agents/skills/upstream-sync/SKILL.md (production routine's entire instruction source)"
  - "Local Claude Code discovery symlink .claude/skills/upstream-sync (gitignored, absolute main-repo target)"
affects: [scheduled-cloud-routines, deploy-watch]
tech-stack:
  added: []
  patterns: ["skill-as-SSOT for unattended routine policy", ".agents/ tracked + .claude/ symlink discovery (agda-dogfooding install pattern)"]
key-files:
  created:
    - .agents/skills/upstream-sync/SKILL.md
    - .claude/skills/upstream-sync (symlink, gitignored — never committed)
  modified: []
decisions:
  - "Skill quarantine list copied byte-identical from ci.yml integration job at authoring time; ci.yml stays SSOT on drift"
  - "Symlink target is the MAIN repo absolute path (/Users/eric/projects6/agda-mcp-server/.agents/skills/upstream-sync), not the worktree path — worktree is deleted after merge"
metrics:
  duration: ~6 min
  completed: 2026-07-05
---

# Quick Task 260705-79k: Upstream Auto-Sync via Scheduled Cloud Routine Summary

**One-liner:** Bounded-autonomy upstream merge-policy skill (177 lines, 8 sections) authored, committed (ed20063) and merged with discovery symlink; the cloud trial probe was blocked at routine-creation by a claude.ai ORG-LEVEL GitHub-sync gate ("GitHub sync isn't available for your organization" — GitHub App itself verified installed with All-repositories access), so the production routine is deferred with a recorded re-arm path; interim recommendation = local headless (launchd) carrier for the same skill.

## Task Status

| Task | Name | Status | Commit |
| ---- | ---- | ------ | ------ |
| 1 | Author .agents/skills/upstream-sync/SKILL.md + local discovery symlink | COMPLETE | ed20063 |
| 2 | ORCHESTRATOR runs one-shot cloud trial probe routine | BLOCKED-BY-ORG-POLICY (see Task 2 Outcome) | — |
| 3 | ORCHESTRATOR arms production every-3-days routine (gated on trial green) | DEFERRED (gate unreachable; re-arm path recorded) | — |

## Task 2 Outcome (orchestrator, 2026-07-05)

**The probe routine could not be CREATED: the claude.ai organization blocks GitHub
sync.** Evidence chain, gathered live:

1. `RemoteTrigger create` (extended 9-step probe prompt: original 7 steps + gsd-sdk
   cloud-availability step + gh-issue report delivery) → HTTP 401
   `"Connect your GitHub account before saving a routine that uses a GitHub repository"`,
   three times across the session (before and after each browser-side fix attempt).
   `RemoteTrigger list` → HTTP 200 (token itself valid; routines API reachable).
2. Browser verification (user-authorized Chrome session, account Eric · JHU DSAI
   Engineering): GitHub side is fully set up — Claude GitHub App installation
   99276110 exists with **All repositories** access on the user's GitHub account.
3. The claude.ai OAuth callback (`/connect/github/callback?...&installation_id=99276110`)
   lands on: **"GitHub App Installed — GitHub sync isn't available for your
   organization. You can close this window."** The org-level feature gate, not any
   missing user step, is what keeps the account-connection unset and therefore the
   401 permanent.
4. claude.ai/code repo picker shows "No repos match" (consistent with the gate);
   `claude.ai/customize/connectors` shows only org-managed connectors, no GitHub
   sync toggle exposed to this user.

**Conclusion:** cloud routines referencing GitHub repos are unavailable on this
claude.ai org account until an org admin enables GitHub sync (admin console), or
the work moves to a claude.ai account where the feature is available. The extended
probe prompt (incl. the GSD_SDK/gsd-sdk cloud-availability step and issue-based
report delivery) is preserved verbatim in session history and in the plan for
instant reuse the day the gate opens.

## Task 3 Outcome (orchestrator, 2026-07-05)

DEFERRED — the Task 3 gate (`TOOLCHAIN: green` AND `PUSH_TO_MAIN: possible`)
is unreachable while Task 2 is org-blocked. No routine was created; nothing to
clean up. **Re-arm path:** (1) org admin enables GitHub sync for JHU DSAI
Engineering → (2) re-run the Task 2 probe verbatim → (3) if green, Task 3 as
planned. Independent of that: the skill is environment-adaptive by design, so the
**local headless path (launchd + `claude -p` executing /upstream-sync)** works
today with a strictly stronger verification gate (real Agda, full suite) — that
alternative is the recommended interim carrier and belongs to the next milestone's
auto-sync phase, not this quick task.

Additional durable side effect of this task's investigation: the Claude GitHub App
is now installed (All repositories) on the user's GitHub account — harmless today,
required the day the org gate opens.

## What Was Built (Task 1)

`.agents/skills/upstream-sync/SKILL.md` — 177 lines, house style per agda-dogfooding
(frontmatter `name:` + one-sentence "Use when ... to ..." description, `# Upstream
sync runbook` title, numbered `## N.` sections):

1. **Overview** — auto-deploy-on-every-push (D-06), upstream push disabled, 488/5
   divergence, first-sync-escalates-by-design, bounded autonomy in one line.
2. **Policy** — allowed (fetch, merge-NEVER-rebase, non-guarded conflict resolution,
   gate, green push, watch/rollback) vs forbidden (rebase, force-push, guarded
   resolutions, red push, broken/mid-merge main).
3. **Guarded files** — the exact 9 false-green-critical seam paths including the
   `src/agda/session-capture/**` glob; any conflict there = no push, escalate.
4. **Flow** — 9-step algorithm: fetch → count (0 = silent exit) → `--no-ff` merge →
   conflict triage → gate → push → watch (deploy-ingest.yml run + healthz curl at
   dev.sites.idies.jhu.edu/agda-mcp/healthz) → `git revert -m 1` rollback on red →
   bookkeeping.
5. **Verification gates** — LOCAL (`command -v agda`; pinned mise Node 24 PATH,
   `RUN_AGDA_INTEGRATION=1 npx vitest run` full real-Agda suite, quarantine UNSET)
   vs CLOUD (engine-strict `npm ci`, `AGDA_MCP_CI_QUARANTINE` set to the 5-file
   list copied verbatim from ci.yml, `RUN_AGDA_INTEGRATION` unset so Agda-gated
   tests self-skip); 2-fix-attempt bound.
6. **Escalation** — 5 triggers; preserve work on `upstream-sync` branch (never
   main), provisional resolutions flagged unreviewed, `gh issue create` digest;
   hard rule: NEVER leave main broken.
7. **Bookkeeping** — cloud has no gsd-sdk (plain git only); `docs/UPSTREAM-SYNC-LOG.md`
   entry committed with every merge/revert; local runs may use `/gsd-quick`.
8. **Cadence** — every 3 days; never tighter than daily without adding a
   `concurrency:` group to deploy-ingest.yml.

Discovery symlink created at `/Users/eric/projects6/agda-mcp-server/.claude/skills/upstream-sync`
→ `/Users/eric/projects6/agda-mcp-server/.agents/skills/upstream-sync` (absolute
target, `ln -sfn`, mirrors the agda-dogfooding link; existing real-directory guard
applied — nothing was clobbered).

## Verification (Task 1 gate)

- File exists, 177 lines (bounds 80-250): PASS
- All 16 required tokens present (9 guarded paths, `git revert -m 1`,
  `AGDA_MCP_CI_QUARANTINE`, `RUN_AGDA_INTEGRATION`, healthz URL,
  `deploy-ingest.yml`, `docs/UPSTREAM-SYNC-LOG.md`, `upstream-sync`): PASS
- `^name: upstream-sync` frontmatter: PASS
- Quarantine list byte-identical to `.github/workflows/ci.yml` integration job
  (re-read at authoring time; matches planning snapshot): PASS
- Symlink is a symlink with the correct main-repo absolute target: PASS
- Commit ed20063 contains exactly `.agents/skills/upstream-sync/SKILL.md`: PASS
- No collateral edits under `.github/workflows/`, `k8s/`, `src/`, `scripts/`: PASS
- NOT pushed (deferred to Task 3's arming step per plan, batching the D-06 deploy)

## Deviations from Plan

**1. [Worktree adaptation] Symlink created at the MAIN repo path, dangling until merge**
- **Found during:** Task 1 (executor runs in a git worktree that is deleted after merge)
- **Issue:** The plan's recipe (`ln -sfn "$(pwd)/.agents/skills/upstream-sync" ...` from
  repo root) would, in a worktree, produce a symlink target that dies with the worktree.
- **Fix:** Per orchestrator constraint, the symlink was created in the main repo's
  `.claude/skills/` with the main repo's absolute `.agents/...` path as target. It
  DANGLES until this branch merges to main (the target directory does not exist in
  the main checkout yet); it resolves automatically the moment the merge lands. The
  plan verify's `test -f .claude/skills/upstream-sync/SKILL.md` therefore passes only
  post-merge — all other verify clauses pass now.
- **Files modified:** none tracked (`.claude/` is wholesale-gitignored)
- **Commit:** n/a (symlink is never committed by design)

No other deviations — Task 1 executed as written.

## Tasks 2-3 Handoff Notes (for orchestrator)

- Task 2 trial-probe prompt is self-contained in the plan (lines 148-161); it does
  NOT read the skill, so it can run before the skill reaches origin/main.
- Task 3 gate: only arm if `TOOLCHAIN: green` AND `PUSH_TO_MAIN: possible`. Its step 1
  (`git push origin main`) publishes commit ed20063 (after merge) and triggers exactly
  one accepted D-06 deploy cycle.
- If this worktree's symlink creation had failed, the recovery command is:
  `ln -sfn /Users/eric/projects6/agda-mcp-server/.agents/skills/upstream-sync /Users/eric/projects6/agda-mcp-server/.claude/skills/upstream-sync`
  — NOT needed; the symlink exists.

## Self-Check: PASSED

- `.agents/skills/upstream-sync/SKILL.md` exists in worktree: FOUND
- `.claude/skills/upstream-sync` symlink exists in main repo with correct target: FOUND
- Commit ed20063 exists on worktree-agent-a48e57edfd6143846: FOUND
